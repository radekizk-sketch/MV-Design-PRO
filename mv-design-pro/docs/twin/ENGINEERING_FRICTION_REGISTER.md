# MV-DESIGN-PRO — REJESTR TARĆ INŻYNIERSKICH (ENGINEERING FRICTION REGISTER, mandat §5, §68, §168, §181)

> **Status od 2026-09-04 (kontrakt MAX PLATFORM):** materiał wejściowy i dowodowy programu konwergencji. Źródło kanoniczne architektury: `../architecture/PRODUCT_CAPABILITY_MODEL.md`, `../architecture/CANONICAL_DIGITAL_TWIN.md`, `../architecture/REVISION_SCENARIO_EXECUTION_MODEL.md`, `../architecture/COMPUTATIONAL_BOUNDARY.md`, `../architecture/FUTURE_CAPABILITY_REVIEW.md`, `../reference-networks/REFERENCE_NETWORK_REGISTRY.md`, `../evidence/CONVERGENCE_EVIDENCE.md`. Przy rozbieżności obowiązuje dokument kanoniczny; w szczególności: nie powstaje nowa klasa `TwinModel` (Canonical Project Twin = rozwinięty ENM), `network_model` jest pochodnym IR, program nie zatrzymuje się po M0, lecz prowadzi konwergencję wycinkami CV-0…CV-6 z bramkami B-01/B-02 i procedurą kasacji.


**Status:** WYNIK AUDYTU (stan zastany na HEAD `a1ab2959`, 2026-09-02; READ-ONLY — nic nie naprawiono)
**Gałąź:** `claude/mv-design-pro-twin-audit-u4lhy0` · **Autor pomiaru:** audytor A12 (Engineering UX Architect + projektant SN/nN); zebrane i zredagowane przez koordynatora programu twin.
**Metoda:** dla każdego z 14 workflowów (mandat §68) prześledzono w kodzie: skąd dane → gdzie wpisywane → gdzie liczone → dokąd trafia wynik → co użytkownik robi ręcznie. Każde ustalenie ma dowód `plik:linia` (ścieżki względem `mv-design-pro/`). Nie oceniano wyglądu (werdykt wizualny B-02 należy do właściciela) — oceniano przepływ danych i decyzji.
**Odpowiedź projektowa na ten rejestr:** `MV_DESIGN_PRO_TARGET_ENGINEERING_WORKFLOW.md` (FAZA C) i `MV_DESIGN_PRO_DESIGN_OPTIMIZATION_ARCHITECTURE.md` (FAZA D, cz. 2). Synteza całego audytu: `MV_DESIGN_PRO_DIGITAL_TWIN_AUDIT.md`.

**Pomiar bazowy (własny, HEAD):** 24 pliki `Kreator*.tsx` (23 kreatory + rama); kontrolki formularza: źródło 45 · stacja 44 · źródło OZE 28 · źródło dyspozycyjne 19 · odbiór 16 · transformator 14 · odcinek nN 11 · rozdzielnica nN 9 · magistrala 8 · pole 7 · pomiar 6 · przekaźnik 5 · łącznik 5 · pierścień 4 · aparat nN 4; 58 operacji kanonicznych (`domain/canonical_operations.py`); `dialog_completeness_guard` obejmuje 24 z nich; 100 kodów gotowości (30 z `fix_navigation.modal`, 3 bez `fix_action_id`); ≈130 unikalnych ścieżek `/api/*` wołanych z ui2; 32 zakładki przestrzeni „Wyniki"; „Uruchom obliczenie" oferuje 2 z 8 rodzajów biegu i 2 z 25 zdolności; 55 wpisów `TODO-KARTA` w ui2.

**Skala:** koszt = minuty/kliki na jedno przejście; ryzyko = prawdopodobieństwo błędu inżynierskiego. Priorytet: **P0** = rwie łańcuch §181 lub błąd merytoryczny; **P1** = duże tarcie z ryzykiem; **P2** = tarcie bez ryzyka błędu; **P3** = kosmetyka przepływu.

---

## 1. Rejestr EF-001…EF-060 (14 workflowów W1–W14)

Skala kosztu: **min** = minuty na jedno przejście; **klik** = liczba klików/ekranów; **ryzyko** = prawdopodobieństwo błędu inżynierskiego. Priorytet: P0 = rwie łańcuch §181 / błąd merytoryczny; P1 = duże tarcie z ryzykiem; P2 = tarcie bez ryzyka błędu; P3 = kosmetyka przepływu.

| ID | WF | current_behavior (dowód) | engineering_cost | risk | root_cause | target_behavior | automation_opportunity | P |
|---|---|---|---|---|---|---|---|---|
| EF-001 | W1 | Założenia projektu rozproszone w 4 miejscach: Sk″/U/R-X w kreatorze źródła (`zrodloModel.ts:41-46`), `sc_simplified_sk_mva` w konfiguracji przypadku (`types.ts:65-71`; konsumowane przez `solver_input/builder.py:445-453`), moc/cosφ/tryb w kaflu OSD (`enm/models.py:126-137`), c/tk/base_mva w `StudyCaseConfig`. | 10–20 min szukania; ryzyko niespójności Sk″ między modelem a przypadkiem | WYSOKIE (dwa Sk″ → dwa różne Ik″ w jednym projekcie) | brak encji „Project Assumptions"; historyczne warstwy V11/V12 | Jeden rejestr założeń projektu (Sk″min/max, U, cosφ, T, normy, c, tk) z provenance i propagacją do case/solver | Import warunków OSD z dokumentu; walidacja spójności Sk″ model↔case | P0 |
| EF-002 | W1 | Temperatura otoczenia/gruntu, rezystywność cieplna, normy — brak pola w modelu i przypadku; `KartaPrzypadku.tsx:138-147` renderuje wiersze „Wkrótce" (`spaces/obliczenia/strings.ts:72`); warunki ułożenia podawane per odcinek nN (`KreatorOdcinkaNn.tsx:63,116,352-413`) i per tor DER. | powtarzanie warunków ułożenia przy każdym odcinku; zaślepka w UI | WYSOKIE (obciążalność liczona dla warunków katalogowych producenta) | brak modelu założeń środowiskowych | Założenia środowiskowe projektu z dziedziczeniem per trasa/strefa i override per odcinek | Domyślne z założeń; korekta Iz′ automatyczna we wszystkich doborach | P0 |
| EF-003 | W1 | Konfiguracja przypadku (c, base_mva, tolerancja, profil OSD, tryb SC) nieedytowalna w ui2: `NowyPrzypadek.tsx` zbiera nazwę/opis/klon; `createCase` bez `config` (`przypadkiAdapter.ts:233`); legacy `CaseConfigPage` wygaszony (`legacyRegistry.ts:72-74`). | brak możliwości ustawienia wariantu (np. c_min) bez API | ŚREDNIE | migracja ui→ui2 bez okna konfiguracji | Edytor założeń przypadku z jawnym pochodzeniem | Warianty min/max generowane automatycznie z jednego przypadku | P1 |
| EF-004 | W1 | Sprzeczne domyślne c_min: tekst zaszyty „1,10 / 1,00" (`KreatorZrodloZasilania.tsx:904`) vs `DEFAULT_STUDY_CASE_CONFIG.c_factor_min: 0.95` (`ui/study-cases/types.ts:157-158`) vs `DOMYSLNY_C = 1.1` w scenariuszu (`PanelScenariuszy.tsx:34`). | dezinformacja projektanta | WYSOKIE (Ik_min dla nN wg IEC 60909: c=0,95 vs 1,00) | prezentacja literałów zamiast danych z kontraktu | Jedno źródło współczynników c (rejestr założeń) wyświetlane wszędzie | — | P1 |
| EF-005 | W1 | Czas cieplny tk wpisywany w 3 miejscach: kreator źródła `thermal_time_s` (`zrodloModel.ts:78`, `KreatorZrodloZasilania.tsx:889-901`), przypadek `thermal_time_seconds`, scenariusz `czasCieplnyS` (`PanelScenariuszy.tsx:35,48`). | 3× wpis; ryzyko Ith z innym tk niż czas zabezpieczenia | ŚREDNIE | brak jednego rejestru założeń | tk z założeń projektu, nadpisywalne per przypadek | tk z czasu zadziałania zabezpieczenia (wynik koordynacji) | P1 |
| EF-006 | W1 | `wymagany_cos_phi` z warunków OSD nie zasila żadnego kreatora ani doboru (grep w ui2 poza `spaces/projekt` = 0); kreatory mają własne domyślne: 0,95 (`magistralaModel.ts:44`), 0,93 (`odbiorModel.ts:77`), 1,0 (`DoborToruSn.tsx:83`), 0,95 `DOMYSLNY_COSFI_MIN` (`EkranKompensacji.tsx`). | ponowne wpisywanie; niespójne cosφ w ΔU/doborze/kompensacji | ŚREDNIE | system zna daną, pyta ponownie (§172) | Propagacja cosφ z warunków OSD jako domyślnej wartości w każdym torze | — | P1 |
| EF-007 | W1 | Un magistrali „z kontekstu GPZ, domyślnie 15" (`magistralaModel.ts:27,45`); `ENMDefaults.sn_nominal_kv` (`enm/models.py:123`) bez edytora w ui2. | niewielki | NISKIE (backend waliduje napięcia) | zaszyte 15 kV | Un zawsze z szyny źródłowej; brak stałych w UI | — | P2 |
| EF-008 | W1 | Cel projektu (4 kafle `CelProjektu.tsx:16-35`) nie jest utrwalany — steruje tylko nazwą/opisem/trybem (`OtworzProjektKontener.tsx:10,52,170`). | brak przełożenia celu na wymagany zestaw analiz/dokumentów | ŚREDNIE | brak modelu „zakres opracowania" | Cel projektu → lista wymaganych analiz i dokumentów (definicja „gotowe") | Automatyczny plan analiz per cel | P1 |
| EF-009 | W1/W14 | Projekt = nazwa+opis (`NowyProjektDialog.tsx:9-10`); brak inwestora/wnioskodawcy/adresu/nr zlecenia/rewizji; wniosek OSD prosi o nie ręcznie (`EkranWniosku.tsx:281-334`). | wpisywanie metadanych w każdym dokumencie | NISKIE | brak metryki projektu | Metryka projektu (zamawiający, obiekt, nr, rewizja, projektant) używana przez wszystkie generatory | — | P1 |
| EF-010 | W2 | Kreator źródła: 7 kroków, 45 kontrolek, pełny WN/SN i 4 sekcje w jednym oknie; brak trybu „z warunków OSD" (header bez pól zwarciowych). | ~10–15 min na GPZ | NISKIE (podgląd IEC 60909 z backendu) | opcja MAX bez profilu wejścia | Tryb „warunki OSD → GPZ" (Sk″max/min, U, R/X z dokumentu) z rozwinięciem do pełnego modelu na żądanie | Import z PDF/XLSX warunków | P2 |
| EF-011 | W3 | Podgląd ΔU wymaga ręcznego prądu (`prad_a`, domyślnie In kabla — `magistralaModel.ts:24-26,155-170`); system nie liczy prądu z odbiorów (buduje się „od źródła"). | ręczny domysł prądu; dobór po In zamiast po Ib | WYSOKIE (przewymiarowanie lub przekroczenie ΔU po dodaniu odbiorów) | brak modelu zapotrzebowania/tryb budowy top-down | Dobór przekroju z zapotrzebowania (planowane obciążenie ciągu) + re-walidacja po rozpływie | Auto-dobór przekroju po Ib/Iz′/ΔU/I²t z rankingiem | P0 |
| EF-012 | W3 | `connect_secondary_ring_sn` osiągalny tylko z `ProcessPanel.tsx:546`; brak w menu SLD (`contextMenuRegistry.ts:40-120`) i `sldActionExecutor.ts:295-320`. | skakanie do panelu procesu | NISKIE | brak wpięcia akcji kanwy | Akcja „Domknij pierścień między …" z menu dwóch końców na kanwie | Kandydaci NOP z backendu | P1 |
| EF-013 | W3 | Brak optymalizacji NOP/podziału sieci (straty, prądy wyrównawcze) — brak modułu (grep sizing/ranking); PROPOZYCJE P32 otwarte. | ręczne próbowanie wariantów | ŚREDNIE | brak analizy | Ranking lokalizacji NOP | Batch rozpływów per kandydat | P2 |
| EF-014 | W4 | Kreator stacji: 9 kroków/44 kontrolki/2 646 linii; brak wejścia „moc zapotrzebowana"; dobór TR = zgodność napięć + sort po mocy; kryterium mocy tylko dla falownika (`stacjaModel.ts:415-429,505-512`; `InsertStationFormHelpers.ts:164-170`). | ~15–25 min na stację; TR dobierany „na oko" | WYSOKIE (przeciążenie TR wykryte dopiero po rozpływie) | brak encji zapotrzebowania/jednoczesności | Dobór TR z zapotrzebowania (Σ odbiory × k_j, rezerwa) z kandydatami i uzasadnieniem | Auto-propozycja TR + aparatów pola z Ik″ | P0 |
| EF-015 | W4 | `dry_run` zwraca skutki topologiczne (`halves`, `invalidated_results`, `affected_proof_packs`, `missing_data_after` — `domain_operations.py:6689-6700,6836-6846`), bez skutków elektrycznych (ΔIk, ΔU, obciążenia). | projektant nie wie, co zmieni wstawienie stacji w liczbach | ŚREDNIE | preview = walidacja, nie symulacja | Impact preview §56 z szybkim rozpływem/zwarciem na kopii | Symulacja na kopii migawki bez unieważniania | P2 |
| EF-016 | W4 | Aparat KAŻDEGO pola SN wskazywany jawnie (B-12, `domain_operations.py`≈`:5935-5945`); brak auto-doboru In/Ik aparatu pola z wyniku zwarciowego poza torem DER (`der_selection_preview` `field_apparatus`). | wybór z listy bez filtrów per pole | ŚREDNIE | dobór aparatu tylko w DER | Dobór aparatu pola (Un, In ≥ Ib, Icu ≥ Ik″) z rankingiem | Reuse `field_apparatus` z DER dla wszystkich pól | P1 |
| EF-017 | W4/W10 | Obwód wtórny CT/VT (długość, przekrój, moc aparatów) wpisywany w kreatorze, „NIE trafia do payloadu operacji stacyjnej" (`stacjaModel.ts:131-160`); kryterium `SekcjaBilansuCtVt.tsx` liczone tylko w chwili kreatora. | dane tracone; ponowne wpisywanie przy każdej weryfikacji | ŚREDNIE | brak pola w ENM | Obwód wtórny jako dane assetu (Measurement) z re-walidacją w werdykcie | Domyślne z szablonu rozdzielnicy | P1 |
| EF-018 | W4/W6 | Po zapisie stacji/odbioru sekcja „Co to uruchamia" = tekst bez akcji (`stacja/strings.ts:506-511` „dobór CT/VT i nastaw … uzupełnisz w edycji stacji"; `rama/gotowosc.tsx:96-115`). | dead-end po zapisie | NISKIE | komponent prezentacyjny | Następny krok jako akcja (otwórz dobór CT/VT / uruchom wymagane analizy) | Auto-bieg jak w DER (`podsumowanieApi.ts:140-152`) | P2 |
| EF-019 | W5 | Transformator z `<select>` (`rama/pola.tsx:296-334`) — brak filtrów Un/S, kandydatów, rankingu, „dlaczego ten" (§55). | przeglądanie listy | ŚREDNIE | picker generyczny | Picker katalogu z filtrami z kontekstu + kandydaci + odrzuceni z powodem | Reuse wzorca `der-selection-preview` | P1 |
| EF-020 | W6 | Odbiór nN wymaga ręcznego modelu ZIP (8 współczynników — `odbiorModel.ts:24-41,46-66`); ENM `Load` bez typu odbiorcy/zapotrzebowania (`enm/models.py:422-433`). | 8 pól na odbiór; brak profili | ŚREDNIE | katalog OBCIAZENIE bez profili typowych | Profile odbiorców (komunalny/przemysłowy/…) z ZIP i k_j z katalogu | Zapotrzebowanie agregowane per szyna/TR | P2 |
| EF-021 | W6 | Kreatory nN (odcinek/rozdzielnica/aparat/sprzęgło) zarejestrowane (`operationFormRegistry.tsx:72-75`), ale nie znaleziono wejścia (szukałem: statyczne `openOperationForm('add_nn_*'` = 0; dynamiczne callery: `InspectorEngineeringView` bez op nN, `NnSwitchgearCard.tsx:330-352` tylko DER/odbiór, `LvDomainView.tsx` bez `openOperationForm`, `ProcessPanel` bez nN). A11 §1: ekran doboru nN skasowany w `c8f253d3`. | budowa nN w żywej powłoce nieosiągalna bez API | WYSOKIE (§181: nN nie da się zaprojektować) | usunięcie „nN STUDIO" bez zastępczego wejścia | Drzewo/kanwa nN z akcjami „dodaj odcinek/rozdzielnicę/aparat" | Kreator obwodu nN jednym przebiegiem (odcinek+aparat+odbiór) | P0 |
| EF-022 | W6 | Dobór aparatu nN (`nn_device_selection.py:1-45`, 4 kryteria IEC 60364, ranking; endpoint `api/enm.py:454-477`) — 0 konsumentów w UI. | dobór ręczny | WYSOKIE (SWZ/Icu bez sprawdzenia) | zdolność bez ekranu | Dobór aparatu z rankingiem w kreatorze aparatu nN i w werdykcie | Auto-propozycja przy tworzeniu obwodu | P0 |
| EF-023 | W6 | SWZ per obwód (`api/enm.py:438-451`) wyłącznie jako nakładka SLD (`ui/sld/v3/canvas/overlay.ts:293-330`); brak w werdykcie projektowym i „co wymaga uwagi". | werdykt nN niewidoczny w weryfikacji | WYSOKIE | brak agregacji nN | SWZ jako kryterium werdyktu (E5) z akcją „dobierz aparat" | Bieg FAULT_LOOP_NN/SWZ_NN w „required analyses" | P1 |
| EF-024 | W6 | Iz′ dla nN bez podglądu w kreatorze (`KreatorOdcinkaNn.tsx:12-24` — świadomie pominięty, bo endpoint SN liczy inne tablice). | dobór bez korekty | ŚREDNIE | brak endpointu nN | Podgląd Iz′ nN z `wspolczynniki_nn` | — | P2 |
| EF-025 | W7 | 7 parametrów doboru toru DER wpisywanych per kreator (`DoborToruSn.tsx:72-84`). | powtarzanie przy każdym DER | NISKIE | brak założeń projektu | Domyślne z rejestru założeń | — | P1 |
| EF-026 | W7/W12 | Parametry NC RfG modułu: łańcuchy formularza z zaszytymi domyślnymi (droop 5 %, dead band 0,2 Hz, Q ±0,33 — `macierzModel.ts:64-83,182-193`), choć kreator OZE utrwala `frequency_droop_percent`, `lfsm_deadband_hz`, `q_min/max_mvar`, `cos_phi_target` (`zrodloOzeModel.ts:139-150`). | podwójny wpis; ryzyko oceny zgodności na innych danych niż rozpływ | WYSOKIE (werdykt RfG ≠ model) | druga prawda (store DER + defaults UI) | Macierz czyta parametry z modelu; brak danych = „brak danych", nie 5 % | — | P0 |
| EF-027 | W7/W12 | Wniosek OSD: punkt przyłączenia jako tekst (`EkranWniosku.tsx:262-274`) mimo `/api/insights/network-boundary` (`insightsApi.ts:75-80`); przebiegi wybierane po `run.id` (`:224-226,248-250`). | przepisywanie ref węzła; identyfikatory techniczne | ŚREDNIE | brak wiązania z granicą sieci | Punkt przyłączenia z granicy sieci (1 klik), przebiegi po nazwie i dacie | Auto-wybór najnowszych aktualnych biegów | P1 |
| EF-028 | W7 | Strumień OZE czyta drugi store DER synchronizowany z modelu (`synchronizacjaZModelu.ts:1-30`); brak reakcji na `model-zmieniony` w `ui2/oze` (grep = 0); `ncRfgStore.wynik` nie unieważnia się po zmianie modelu. | wynik NC RfG może dotyczyć nieaktualnego modelu | WYSOKIE | shadow store | Wynik RfG jako bieg z rewizją modelu i znacznikiem świeżości | Unieważnienie przez magistralę | P1 |
| EF-029 | W8 | „Uruchom obliczenie" oferuje 2 z 8 typów biegu (`UruchomObliczenie.tsx:27`; `types.ts:203-211`) i 2 z 25 zdolności; pozostałe analizy odpalane per ekran (`quality_analysis_runs`, `oze-analysis/*`, `v126`, koordynacja). | 4–6 miejsc, żeby policzyć komplet | WYSOKIE (pominięta analiza) | brak orkiestratora (A3-02) | „Uruchom wymagane analizy" jako jeden bieg z listą wg celu | Plan analiz z pokrycia + pre-flight | P0 |
| EF-030 | W8 | Brak biegu Ik_min: `RODZAJE=['SC_3F','LOAD_FLOW']`; koordynacja: „Ik_min wymaga OSOBNEGO biegu minimalnego" (`pradyZBiegow.ts:19-22`). | czułość zabezpieczeń nieobliczalna z ui2 | WYSOKIE | typy biegu = SC max | Para biegów max/min automatycznie | — | P0 |
| EF-031 | W8 | Pasek „Oblicz": rodzaj z `activeAnalysisType` (domyślnie `null` → SC_3F, `useLegacyOrchestrator.ts:71-82`); setter tylko w legacy. | niejawny rodzaj biegu | NISKIE | resztka starej ramy | Jeden przycisk = plan analiz | — | P2 |
| EF-032 | W8 | Bramka gotowości przy biegu pokazuje tylko pierwszą blokadę (`uruchomObliczenie.ts:137-146`). | iteracje „napraw→uruchom" | NISKIE | toast | Lista blokad z akcjami | — | P2 |
| EF-033 | W8 | Brak przypadku UUID → bieg tworzy losowy UUID + „Zwarcie maksymalne IEC 60909" (`uruchomObliczenie.ts:150-158`). | ukryty przypadek | ŚREDNIE | kompatybilność | Bieg zawsze na jawnie wybranym przypadku | — | P2 |
| EF-034 | W8/W11 | Serie tylko dla scenariuszy zwarciowych jednego przypadku (`batch_execution.py:9-12`); brak serii LF+SC+nastawy, brak serii między przypadkami. | ręczne uruchamianie | ŚREDNIE | brak orkiestratora | Serie po typach analiz i przypadkach | — | P1 |
| EF-035 | W8/W10 | Bieg nastaw `protection.overcurrent.v0` (`protection_overcurrent_settings.py:25,57-63`) — trigger wyłącznie w `ui/sld/v2/protection/ProtectionRunButton.tsx:106`, niezamontowanym; `SekcjaNastaw` mapuje `fix_navigation.panel` na przestrzeń (`SekcjaNastaw.tsx:37-44`), nie uruchamia biegu. | nastawy z analizy nieosiągalne | WYSOKIE | trigger w martwym module | Bieg nastaw w planie analiz; akcja „wyznacz nastawy" w E-28 | — | P0 |
| EF-036 | W9 | „Popraw w modelu" = selekcja + Schemat dla 8/9 rodzajów (`akcjeNaprawcze.ts:125-135`); brak wejścia w konfigurator kabla/TR (`:33-38`). | ręczne odnalezienie właściwego pola w property-grid | ŚREDNIE | brak konfiguratorów doboru | Wynik → „Zmień typ (kandydaci)" z porównaniem skutków | Ranking alternatyw z impact preview | P1 |
| EF-037 | W9 | Brak modułu doboru przekroju SN/nN (Ib/Iz′/ΔU/Ik/I²t); jedynie `ocenaDoboru` po fakcie (`KreatorMagistralaSn.tsx:286-290`); backend: grep sizing → tylko DER/kompensacja/nN aparat. | dobór przekroju w głowie/Excelu | WYSOKIE (§181) | brak solvera doboru | Solver doboru przekroju z kandydatami/odrzuconymi/uzasadnieniem (wzór DER) | Auto-dobór przy kreatorze + re-walidacja po biegu | P0 |
| EF-038 | W10 | Dwie prawdy zabezpieczeń: ENM (`add_relay` bez nastaw `KreatorPrzekaznika.tsx:183-190`; `update_relay_settings` bez wejścia UI) vs urządzenia koordynacji w `ProtectionConfig.overrides` przypadku (`nastawyPrzypadku.ts:1-16`) tworzone z szablonów o zaszytych nastawach (`protection-coordination/types.ts:586-618`). | odtwarzanie ręczne przekaźników w koordynacji | WYSOKIE (nastawy w case ≠ nastawy w modelu/SLD/raporcie) | zablokowany zapis do ENM (kanon V11) | Nastawy jako dane assetu w ENM z rewizją; koordynacja czyta model | Import przekaźników stacji do koordynacji 1:1 | P0 |
| EF-039 | W10 | Lokalizacja urządzenia koordynacji = szyna/gałąź/TR (`lokalizacjeZModelu.ts:29-37`), nie pole z przekaźnikiem; brak automatycznego importu zabezpieczeń ze stacji. | ręczne mapowanie | WYSOKIE | model urządzenia koordynacji osobny od Bay | Urządzenie = `protection_assignment` pola; prądy z pola | — | P0 |
| EF-040 | W10 | Selektywność poza automatem werdyktu (`werdykt_projektowy.py:249-256`); pokrycie: „dowody zabezpieczeniowe nie są produkowane z żadnego przebiegu kanonicznego" (`pokrycie_analiz.py:12-14`; `coverage_score/builder.py:62-73`). | brak werdyktu selektywności w „gotowe" | WYSOKIE | brak biegu kanonicznego zabezpieczeń | Bieg zabezpieczeń kanoniczny → kryterium werdyktu | — | P0 |
| EF-041 | W10 | Dobór CT/VT: przekładnia i klasa z katalogu (`KreatorPomiaru.tsx:78-129`), brak propozycji z In pola/Ik″ i mocy obwodu. | wybór z listy | ŚREDNIE | brak kryterium doboru | Propozycja CT (In, ALF, Sn) z prądów biegu i obwodu wtórnego | — | P1 |
| EF-042 | W11 | Scenariusz zwarciowy: `element_ref` jako pole tekstowe (`PanelScenariuszy.tsx:194-195`); brak generatora scenariuszy. | przepisywanie ref | ŚREDNIE | formularz surowy | Wybór z modelu + generator („każda szyna SN", „każda stacja") | Batch generowany automatycznie | P1 |
| EF-043 | W11 | What-if na modelu = destrukcyjna edycja jedynego modelu + unieważnienie wszystkich wyników (`result_invalidator.py:5-14`); porównanie tylko run-vs-run (`porownanieModel.ts:1-35`). | brak bezpiecznego eksperymentowania | WYSOKIE | brak wariantów delta (A2-14/15) | Warianty projektu (delta na bazie) z porównaniem A/B i „zastosuj" | Sandbox na kopii migawki | P1 |
| EF-044 | W11 | N-1: ręczne zaznaczanie (`EkranKontyngencji.tsx:354-556`), 2,64 s/kontyngencja (`kontyngencje/api.ts:20-24`), poza werdyktem/NBA. | minuty na bieg | NISKIE | brak cache/dependency graph (A2-11) | N-1 w planie analiz z zakresem domyślnym | Cache + selektywne przeliczenie | P2 |
| EF-045 | W13 | NBA kończy się na E5 „odczytaj wyniki" (`nastepnaAkcja.ts:19-26,47-49`); sygnały: snapshot, readiness, dowolny bieg DONE, `results_valid` (`procesAdapter.ts:34-71`); nie zna pokrycia analiz, werdyktu, nastaw, dokumentów. | projektant sam ustala co dalej po pierwszym biegu | ŚREDNIE | brak WorkflowEngine (A8-04) | NBA z pokrycia + werdyktu + planu analiz + dokumentów (E6–E8) | Silnik workflow oparty o „definicję gotowego" | P0 |
| EF-046 | W13 | Akcja naprawcza gotowości = selekcja + „Schemat" (`AppRoot.tsx:306-311`); `fix_navigation` (30 kodów z `modal`, `canonical_operations.py:725-734`) i legacy executor (`fixActionSurfaceExecutor.ts:119`) nieużywane w ui2 (grep = 0); brak fix_action → „Wymaga interwencji projektanta" (`gotowosc/strings.ts:132`). | „przejdź do ekranu" zamiast 1 klik naprawy; 100 % akcji to nawigacja | ŚREDNIE | integracja ui2 uprościła executor | Fix action = otwarcie właściwego formularza z fokusem pola (executor już istnieje) | Auto-fix dla kodów katalogowych (przypisz typ) | P0 |
| EF-047 | W13 | FAIL kryterium: stan + element + przyczyna + generyczny przycisk (`EkranWerdyktu.tsx:181-218`); `WerdyktResponse` (`werdykt/api.ts:26-46`) bez „co może naprawić / co zmieni naprawa" (§174). | inżynier zgaduje remedium | ŚREDNIE | brak modelu remediów | Kryterium → lista remediów z szacunkiem skutku | Symulacja remedium na kopii | P0 |
| EF-048 | W13 | Werdykt = 10 kryteriów; 4 jawnie poza automatem (`werdykt_projektowy.py:249-285`: selektywność, wytrzymałość aparatury na całym modelu, obciążalność z korektą ułożenia, ekonomiczna gęstość); brak SWZ nN, ΔU nN per obwód, arkusza nN. | „gotowe" nie oznacza zweryfikowane | WYSOKIE | agregator bez dostawców | Komplet kryteriów SN+nN w jednym werdykcie | Dostawcy: SWZ, nn_circuit_sheet, wytrzymałość aparatury z modelu | P0 |
| EF-049 | W14 | Hub dokumentacji bez karty schematu SLD (grep `sld|schemat` w `dokumentacja/model.ts` = brak karty) mimo eksportów w `ui/sld/v2/export`, `ui/sld/v3/export/exportPdfV3.ts`; BOM tylko toru DER (`der_sn_documents.py`); arkusz nN bez UI. | dokumentacja składana ręcznie z 3 miejsc | ŚREDNIE | hub = 3 karty | Pakiet projektu: raport + dowody + SLD (rewizja, tabliczka) + BOM + arkusz nN + wniosek | Generacja pakietu jednym klikiem | P1 |
| EF-050 | W14 | Brak rewizji projektu/dokumentów (magazyn: lista+pobranie `document_store.py:1-13`; archiwum `schema_version` `archiwum/api.ts:46,60`); brak stanu „wydany/uzgodniony". | brak śladu rewizji do OSD | ŚREDNIE | brak modelu rewizji (A2-10) | Rewizje projektu + dziennik decyzji + status dokumentu | — | P2 |
| EF-051 | Inspektor | Fallback ui2: `wyniki: []`, `dowody: []`, `powiazania` puste (`inspectorAdapter.ts:69-81`); `onOtworzDowod={() => undefined}` (`AppRoot.tsx:461`). | martwy klik; brak „urządzenie → analizy" | NISKIE | TODO-KARTA | Jeden inspektor: właściwości, wyniki per analiza, dowody, powiązania, akcje (A8-05) | — | P2 |
| EF-052 | Szukaj | Indeks obiektów = tylko szyny (`AppRoot.tsx` `snapshot?.buses`), 4 polecenia (`searchIndex.ts:99-126`). | brak wyszukania stacji/odcinka/pola | NISKIE | indeks częściowy | Indeks wszystkich elementów + akcje obiektowe | — | P2 |
| EF-053 | Role | Tryby `basic/extended/expert` (`modeModel.ts:10`), brak ról (§168); `userScope: 'local'` TODO (`useShellStore.ts:8-10`). | każdy widzi wszystko | NISKIE | brak modelu ról | Profile ról (widoki, domyślne plany analiz, uprawnienia) | — | P2 |
| EF-054 | Świeżość | Lista przebiegów: „Rewizja modelu — wkrótce" (`przebiegiAdapter.ts:31-37`), choć kontrakt biegu niesie `analysis_case_context.rewizja_modelu` (`shellStatus.ts:13-19`). | niespójna informacja | NISKIE | adapter read-only | Rewizja per bieg w liście i w porównaniach | — | P2 |
| EF-055 | §175 | Po zmianie modelu: unieważnienie ✔ (`api/enm.py:160-172`), znacznik ✔, dziennik ✔, ale przeliczenie ręczne i per typ (NBA R5 → „Obliczenia" → wybór → „Uruchom"); brak „przelicz wszystko, co było liczone". | 3–4 kliki × liczba analiz po każdej zmianie | WYSOKIE (część analiz zostaje nieaktualna) | brak dependency graph (A2-11) | „Przelicz nieaktualne" (zbiór z rejestru biegów) + selektywne przeliczenie | Auto-recalc wg polityki (natychmiast/na żądanie) | P0 |
| EF-056 | Wyniki | 32 zakładki w jednym tablist (`WynikiWarsztat.tsx:64-96`); „Co wymaga uwagi" agreguje tylko rozpływ + werdykt („DŁUG NAZWANY" `co-wymaga-uwagi/model.ts:36-42`). | skakanie między ekranami | ŚREDNIE | brak agregatu przekroczeń w backendzie | Jeden rejestr przekroczeń przypadku (backend) + zakładki wg celu | — | P1 |
| EF-057 | Import | XLSX ✔ (`EkranImportuArkusza`), brak importu warunków OSD, brak CIM (STAN_REPO D-02). | ręczne przepisywanie z dokumentów | NISKIE | zakres | Import warunków przyłączenia i profili operatora | — | P3 |
| EF-058 | Guard | `dialog_completeness_guard` mapuje 24 z 58 operacji (`scripts/dialog_completeness_guard.py:59-120`), liczy modale bez konsumenta (A8-16). | fałszywa pewność | NISKIE | guard nie mierzy osiągalności | Guard „operacja ma żywe wejście w powłoce" | — | P2 |
| EF-059 | W2–W6 | 23 kreatory × średnio 5–9 kroków; przy 50 stacjach ≈ 50×(9 kroków) bez kopiowania stacji/typowego układu (brak `copy_station`; jest tylko `copy_nn_feeder` w backendzie, bez UI — grep `openOperationForm('copy_nn_feeder'` = 0). | godziny powtarzalnej pracy | NISKIE | brak „stacji typowej" jako szablonu użytkownika z pełnym wyposażeniem (są `szablonyUzytkownika.ts`, tylko konfiguracja rozdzielnicy) | Kopiuj/powiel stację z wyposażeniem; wstaw N stacji wzdłuż ciągu | Generator ciągu z rozstawem stacji | P1 |
| EF-060 | W4 | Krok „pomiar" stacji wymaga wyboru CT/VT/przekaźnika per pole (`KartaWyposazeniaPola.tsx:1-40`), a kryteria bilansu liczą się dopiero po podaniu obwodu wtórnego — bez podpowiedzi „typowy obwód dla tej rodziny rozdzielnicy". | ~2–3 min na pole | NISKIE | brak domyślnych z szablonu | Domyślne wyposażenie z szablonu rodziny (FABRYCZNY/OPCJA już istnieje) rozszerzone o obwód wtórny | — | P2 |

**Bilans:** 60 pozycji: P0 = 17, P1 = 21, P2 = 20, P3 = 2. Klasy tarcia wg §5: dane 2× (EF-001,004,005,006,017,025,026 = 7), ręczne przepisywanie (EF-011,027,042 = 3), skakanie między ekranami (EF-012,029,035,056 = 4), wynik→urządzenie (EF-036,047,051 = 3), urządzenie→analizy (EF-018,051 = 2), brak batch (EF-029,034,055 = 3), brak compare/what-if (EF-043,044 = 2), brak next action (EF-018,045 = 2), brak rekomendacji/auto-doboru (EF-014,016,019,020,022,037,041 = 7), brak impact preview (EF-015), brak wyjaśnienia FAIL (EF-047,048), system zna dane a pyta (EF-006,007,011,026,027), brak propagacji (EF-028,055).

---


---

## 2. Ocena ról §168 (8 ról × 7 kryteriów, skala 1–10)

| Rola | speed | clarity | automation | control | traceability | error prevention | decision support | Σ/70 |
|---|---|---|---|---|---|---|---|---|
| Projektant SN (ciągi/pierścienie) | 4 | 6 | 3 | 7 | 7 | 6 | 3 | 36 |
| Projektant stacji SN/nN | 4 | 6 | 3 | 8 | 6 | 6 | 3 | 36 |
| Projektant nN | 2 | 3 | 2 | 5 | 4 | 4 | 2 | 22 |
| Zabezpieczeniowiec | 2 | 3 | 2 | 4 | 5 | 3 | 3 | 22 |
| Specjalista OZE/BESS | 6 | 5 | 7 | 7 | 8 | 6 | 6 | 45 |
| Analityk sieci | 5 | 5 | 4 | 4 | 8 | 6 | 5 | 37 |
| Eksploatacja / SCADA | 2 | 2 | 1 | 2 | 3 | 2 | 1 | 13 |
| Audytor (WHITE BOX) | 5 | 6 | 4 | 5 | 8 | 5 | 4 | 37 |

Uzasadnienie (dowód w kodzie):
- **Projektant SN:** speed 4 — kreator źródła 7 kroków/45 kontrolek, magistrala z ręcznym prądem (`magistralaModel.ts:24-26`); clarity 6 — NBA + gotowość wg celów (`grupowanieCelow.ts:139-148`); automation 3 — brak doboru przekroju (EF-037), pierścień z panelu (EF-012); control 7 — katalog-first + łańcuchowanie `next_step` (`trunkContinuation.ts:15-28`); traceability 7 — dziennik zmian (`dziennikApi.ts`), hash migawki; error prevention 6 — walidacja katalog-first, bramy kontraktu pomiaru; decision support 3 — akcje naprawcze generyczne (`akcjeNaprawcze.ts:125-135`).
- **Projektant stacji:** control 8 — szablony rodzin, karta wyposażenia FABRYCZNY/OPCJA (`KartaWyposazeniaPola.tsx:9-22`), dry_run; automation 3 — TR bez zapotrzebowania (EF-014), aparat pola jawnie (EF-016), CT/VT bez propozycji (EF-041); speed 4 — 9 kroków/44 kontrolki, brak kopiowania stacji (EF-059).
- **Projektant nN:** speed 2/automation 2 — kreatory nN bez wejścia (EF-021), dobór aparatu bez UI (EF-022), ZIP ręczny (EF-020); traceability 4 — arkusz nN bez UI; error prevention 4 — SWZ tylko overlay (EF-023); clarity 3 — brak przestrzeni nN w kanonie 7 przestrzeni (`spaces.ts:15-22`).
- **Zabezpieczeniowiec:** speed 2/automation 2 — bieg nastaw bez triggera (EF-035), urządzenia z szablonów ręcznie (EF-038/039); clarity 3 — dwie prawdy (ENM vs case overrides); traceability 5 — TCC z API + WHITE BOX (`ProtectionCoordinationPage` TracePanel); error prevention 3 — selektywność poza werdyktem (EF-040); decision support 3 — „Popraw nastawy" prowadzi do edytora (FLOW §3 F-E6.2).
- **OZE/BESS:** automation 7 — dobór toru z kandydatami/odrzuconymi (`derSelectionApi.ts:71-76`), auto-bieg LF+SC + raport + BOM (`podsumowanieApi.ts:140-152`); traceability 8 — ślady KaTeX, provenance karty falownika (STAN_REPO §3); clarity 5 — 12 zakładek OZE + drugi store (EF-028); decision support 6 — ranking PCC, zdolność przyłączeniowa, „Przyłącz źródło w tym węźle" (`PrzylaczZrodloPrzycisk.tsx:40-49`); ale double entry NC RfG (EF-026).
- **Analityk sieci:** traceability 8 — dowód WHITE BOX, dziennik, `Co się zmieniło`; error prevention 6 — pre-flight + diagnoza biegu (`kodyDiagnozy.ts`); automation 4 — 2 typy biegu, N-1 ręczne, brak serii LF+SC; control 4 — konfiguracja przypadku nieedytowalna (EF-003); clarity 5 — 32 zakładki (EF-056).
- **Eksploatacja/SCADA:** brak modelu operacyjnego: stany łączników per przypadek bez UI (`dialog_completeness_guard.py` NO_MODAL_NEEDED `set_case_switch_state`; A2-03), pomiary jako placeholder (A1-18), brak trybu SCADA prezentacji (A7-04); jedyne elementy: podgląd schematu (`podgladSchematu`), NOP.
- **Audytor:** traceability 8 — hash migawki, dowody, założenia w ekranach (`SekcjaZalozen`), rejestr kodów gotowości; decision support 4 — brak rejestru założeń (A6-14) i dziennika decyzji (A2-16); control 5 — brak rewizji dokumentów (EF-050).

---


---

## 3. Test §181 — gdzie łańcuch się rwie („mam punkt zasilania i wymagania" → „kompletny, zweryfikowany, optymalny, udokumentowany projekt SN+nN")

Test przeprowadzony w kodzie na torze ui2 (bez Excela/CAD/kalkulatora obok):

| # | Ogniwo | Stan | Dowód | Konsekwencja |
|---|---|---|---|---|
| 1 | Wymagania OSD → założenia projektu | RWIE SIĘ | tylko 3 pola OSD (`enm/models.py:126-137`); Sk″ w kreatorze; T/normy brak; cosφ nie propagowany (EF-001/002/006) | założenia trzymane „w głowie"/w Excelu |
| 2 | Założenia → GPZ | DZIAŁA | podgląd IEC 60909 z backendu, katalog-first | — |
| 3 | GPZ → ciąg SN (dobór przekroju) | RWIE SIĘ | brak solvera doboru; ΔU z ręcznym prądem (EF-011/037) | przekrój liczony w Excelu |
| 4 | Ciąg → stacja (TR, aparaty, CT/VT) | CZĘŚCIOWO | TR bez zapotrzebowania, aparat jawny, CT/VT bez propozycji (EF-014/016/041); dry_run topologiczny ✔ | dobór TR/aparatów poza systemem |
| 5 | Stacja → nN (obwody, aparaty, SWZ) | RWIE SIĘ | kreatory nN bez wejścia, dobór aparatu i arkusz nN bez UI, SWZ tylko overlay (EF-021/022/023) | nN projektowane poza systemem |
| 6 | Model → gotowość | DZIAŁA | gotowość wg celów, NBA R2/R3, fix-action (choć jako nawigacja) | — |
| 7 | Gotowość → komplet obliczeń | RWIE SIĘ | 2 typy biegu; brak Ik_min; bieg nastaw bez triggera (EF-029/030/035) | analizy pominięte lub uruchamiane w 4 miejscach |
| 8 | Wyniki → werdykt kompletny | CZĘŚCIOWO | 10 kryteriów, 4 poza automatem, brak nN (EF-040/048) | „zweryfikowany" tylko dla SN rozpływ/zwarcia |
| 9 | Werdykt FAIL → decyzja → urządzenie | CZĘŚCIOWO | generyczne „Popraw w modelu" (EF-036/047); brak remediów | decyzja bez wsparcia |
| 10 | Decyzja → propagacja (recalc/revalidate/docs) | CZĘŚCIOWO | unieważnienie ✔, przeliczenie ręczne per typ (EF-055) | część wyników nieaktualna |
| 11 | Zabezpieczenia → koordynacja → nastawy w modelu | RWIE SIĘ | dwie prawdy, ręczne urządzenia, brak zapisu do ENM (EF-038/039) | nastawy w Excelu/w głowie |
| 12 | DER → RfG → wniosek OSD | DZIAŁA z tarciem | auto-bieg + raport + BOM ✔; NC RfG double entry, punkt przyłączenia ręcznie (EF-026/027) | — |
| 13 | Projekt → dokumentacja kompletna | CZĘŚCIOWO | raport/dowód/studium/archiwum ✔; brak SLD w hubie, BOM całości, arkusza nN, rewizji (EF-049/050) | pakiet składany ręcznie |
| 14 | „Optymalny" | BRAK | brak doborów z rankingiem poza DER/kompensacją; brak wariantów delta (EF-043) | optymalność nieweryfikowalna |

**Werdykt §181:** projektant NIE przejdzie łańcucha bez narzędzi obok. Pierwsze twarde zerwanie następuje w ogniwie 3 (dobór przekroju SN), a najgłębsze w ogniwach 5 (nN), 7 (komplet obliczeń) i 11 (zabezpieczenia). Najlepiej domknięty łańcuch to tor DER-SN (ogniwo 12) — wzorzec do uogólnienia.

---


---

## 4. Klasy tarcia wg mandatu §5 — bilans

| Klasa tarcia (§5) | Pozycje rejestru | Stan |
|---|---|---|
| dane wpisywane dwa razy | EF-001, 004, 005, 006, 017, 025, 026 | 7 pozycji; Sk″ ×2, tk ×3, cosφ ×4, c ×3 |
| ręczne przepisywanie | EF-011, 027, 042 | 3 |
| skakanie między ekranami | EF-012, 029, 035, 056 | 4 |
| wynik → urządzenie | EF-036, 047, 051 | 3 |
| urządzenie → analizy | EF-018, 051 | 2 |
| brak serii/batch | EF-029, 034, 055 | 3 |
| brak porównania / what-if | EF-043, 044 | 2 (jedyna klasa częściowo spełniona: A/B run-vs-run) |
| brak następnej akcji | EF-018, 045 | 2 |
| brak rekomendacji / auto-doboru | EF-014, 016, 019, 020, 022, 037, 041 | 7 |
| brak podglądu skutków (impact preview) | EF-015 | 1 |
| FAIL bez wyjaśnienia „co naprawić" | EF-047, 048 | 2 |
| system zna dane, a pyta | EF-006, 007, 011, 026, 027 | 5 |
| brak propagacji | EF-028, 055 | 2 |

**Bilans:** 60 pozycji: P0 = 17, P1 = 21, P2 = 20, P3 = 2. Klasy §5 niespełnione w 13 z 14. Najlepiej domknięty tor: DER-SN (dobór z kandydatami i odrzuconymi → auto-bieg LF+SC → raport zgodności → BOM → magazyn dokumentów) — wzorzec §175 do uogólnienia na wszystkie dobory (FAZA C §2.4, FAZA D cz. 2 §4).

## 5. Do zachowania (wzorce z dowodem — A12 §5)

1. Kreatory katalog-first z podglądem z backendu (`grid-source-preview`, `cable-voltage-drop-preview`, `transformer-rated-currents-preview`; zero fizyki w UI).
2. Łańcuchowanie realnej kolejnej operacji (`ui/network-build/trunkContinuation.ts:15-28,148-159`; `nastepnyKrok` w kreatorze stacji).
3. Tor DER-SN jako wzorzec §175 (`derSelectionApi.ts:71-137`, `podsumowanieApi.ts:140-152`, `KreatorZrodlaOze.tsx:449-463`).
4. Gotowość jako jedna prawda (`gotowoscAdapter.ts:42-53`, `grupowanieCelow.ts`, rejestr kodów z `fix_navigation` w `canonical_operations.py:725-734`).
5. NBA jako czysta funkcja kontraktowa (`ui2/proces/nastepnaAkcja.ts:154-231`) — do rozszerzenia, nie wymiany.
6. Świeżość i unieważnienie serwerowe (`api/enm.py:160-172`, `result_invalidator.py`, `snapshotAdapter.ts:83-95`, `znacznikSwiezosci.ts:74-98`, `PanelCoSieZmienilo.tsx`).
7. `dry_run` stacji (`stacjaPodglad.ts`, `domain_operations.py:6689-6700`) — fundament impact preview.
8. Diagnoza biegu (`ui2/spaces/obliczenia/diagnoza/kodyDiagnozy.ts:36-77`) z testem dwustronnym kompletności.
9. Werdykt projektowy jako agregator z jawnym zakresem „poza automatem" (`werdykt_projektowy.py:15-60`) — uczciwość stanów; do rozbudowy o dostawców.
10. Generator raportu: każda kontrolka = parametr backendu (`generator/model.ts:5-9`); brama dokumentacji wykonawczej przed eksportem (`generator/api.ts:63-90`).
11. Kryteria wyposażenia w miejscu decyzji (`SekcjaBilansuCtVt` w kroku „pomiar" kreatora stacji).
12. Hydratacja powłoki z serwera (`useHydratacjaPowloki.ts`).
