# MV-DESIGN-PRO — CONVERGENCE ROADMAP (kolejność migracji sterowana dowodami + stan i kontynuacja)

**Status:** KANONICZNY, ŻYWY (konstytucja §31, §40, §41). Zastępuje §7 dawnego `CANONICAL_DIGITAL_TWIN.md` i §7 dawnego `REVISION_SCENARIO_EXECUTION_MODEL.md`; plan M0–M7 z `../twin/MV_DESIGN_PRO_MIGRATION_PLAN.md` (FAZA F) jest materiałem wejściowym — obowiązuje ten dokument. Architektura: `CANONICAL_TWIN_ARCHITECTURE.md`. Dowody: `../evidence/CONVERGENCE_EVIDENCE.md`.

## 0. Zasady migracji (konstytucja §31)
Bez big-bang. Każda migrowana odpowiedzialność: CURRENT RUNTIME INVENTORY → CANONICAL OWNER → ONE COMPLETE VERTICAL PATH → PARITY / ENGINEERING EVIDENCE → CONSUMER MIGRATION → DELETE OBSOLETE PATH → GUARD AGAINST RESURRECTION. Bez trwałego dual-write, bez ukrytego fallbacku legacy. Kasacja wyłącznie procedurą siedmiu kroków (inventory → consumer search → data export → parity → cutover → post-cutover observation → removal). Miara migracji nie jest ilościowa (LOC, liczba dokumentów) — kryterium to jedna prawda, pełna fizyka, traceability, brak pracy ręcznej między modułami, zdolność do dowolnej realnej sieci SN+nN.

## 1. STAN I KONTYNUACJA (konstytucja §41 — punkt wejścia każdej przyszłej sesji)

| Pozycja | Stan (2026-09-04) |
|---|---|
| Gałąź / HEAD | `claude/mv-design-pro-twin-audit-u4lhy0`; `main` = `7e84753a` (#473); pełna historia i pomiary w `../evidence/CONVERGENCE_EVIDENCE.md` |
| Decyzje przyjęte | `DECISION_FREEZE_REGISTER.md` (DT-1…DT-16, status per decyzja) |
| Odrzucone alternatywy | tamże, kolumna „Odrzucone alternatywy" |
| Rzeczywisty runtime | ENM per `case_id` (fasada) → CV-1 w toku (rdzeń `9667235a`; przepięcie konsumentów: karta CV-1-W); tor kanoniczny biegów `execution_runs` → `canonical_analysis` (P1/S1) → `enm/assembler.py` (CV-4.1: jedno złożenie wejścia PF/SC z migawki i opcji, guard P0 z zapadką 7 plików / 31 konstrukcji toru legacy); legacy ORM `network_*` żywy dla XLSX/wizard; rejestry biegów po CV-3.3-B: R1 (`canonical_runs`, jedyny tor porównań/zabezpieczeń) + R2 tabela `analysis_runs`/`analysis_runs_index` żywa wyłącznie jako zależność legacy kreatora/koordynacji nastaw (serwis `AnalysisRunService` skasowany) + R4 resztki (`_runs` V12.6, `_batches` — CV-3.3-C); R3 `study_runs`/`study_results` skasowana; 6 modeli scenariusza |
| Rzeczywisty runtime | ENM per `case_id` (fasada) → CV-1 w toku (rdzeń `9667235a`; przepięcie konsumentów: karta CV-1-W); tor kanoniczny biegów `execution_runs` → `canonical_analysis` (P1/S1); legacy ORM `network_*` żywy dla XLSX/wizard; rejestry biegów po CV-3.3-B: R1 (`canonical_runs`, jedyny tor porównań/zabezpieczeń) + R2 tabela `analysis_runs`/`analysis_runs_index` żywa wyłącznie jako zależność legacy kreatora/koordynacji nastaw (serwis `AnalysisRunService` skasowany) + R4 resztki (`_runs` V12.6; `_batches` ZREALIZOWANE — CV-3.3-C, tabela `run_batches`); R3 `study_runs`/`study_results` skasowana; 6 modeli scenariusza |
| Aktywna granica migracji | **CV-1 Project owns ENM** (rdzeń wdrożony; wiring konsumentów + guard klucza + przegląd adwersaryjny w toku) |
| Dowody uzyskane | `../evidence/CONVERGENCE_EVIDENCE.md` §A (CI), §C (determinizm), §F (testy), rejestr sieci (`../reference-networks/REGISTRY_TABLE.md`) |
| Otwarte blokery | CI: karty CI-A (guard podstawień), CI-B (tsconfig), CI-D (12 e2e od #472); `main` bez ochrony (owner action) |
| Nierozstrzygnięte decyzje właściciela | `../evidence/CONVERGENCE_EVIDENCE.md` §I (OD-1 ochrona `main`, OD-2 projekty w legacy ORM, OD-3 wyrocznie G01) |
| Dokładny następny wycinek | CV-1 domknięcie (wiring + guard + adwersaryjny) → CV-2 (`ModelRevision` + `RevisionEnvelope`) — §2 |
| Stan CI (2026-09-05) | **9/9 zielone** na `ef9d6790` (7 workflowów) + `e2a0dc17` (E2E full 408 speców, E2E smoke; path-filtered, ta sama treść kodu) — pierwszy w pełni zielony szczyt gałęzi; `main` (7e84753a) nadal czerwony na E2E full i SLD Determinism od #472. Odebrane karty CV-0: CI-A/B/C/D, FAB-A/B/C, PERF-0, CV-1-G; własna naprawa klasy `KLUCZE_ROZPLYWU` (1e9f21c5). W toku: CV-1-W, FAB-D1/D2/E/F, SLD-LOC (SUB-52s wykonana, wstrzymana do SLD-LOC). Dowody: `../evidence/CONVERGENCE_EVIDENCE.md` §A/§E/§F |
| Karta FAB-K (2026-09-05, worktree) | **odebrana** — łańcuch danych DER: `materialized_params` jedyne źródło zapisu/odczytu wiązań katalogowych (FE+BE, parytet+round-trip 38 backend/20 frontend testów), punkt przyłączenia SN jako element modelu ISTNIEJĄCY (nie enum-widmo — `connection_variant` 2 wartości, `sn_connection_bus_ref`, `CableJoint` skasowany jako rodzaj punktu), 3 phantomy pól DER usunięte (`cable_catalog_ref`/`controller_catalog_ref`/`regulation_profile_ref`), `voltage_level_ref` (drugi phantom, priorytet nad szyną modelu) skasowany — `connection_voltage_kv` jedynym źródłem napięcia; bramka gotowości R5 (wzorzec `KreatorMagistralaSn`) w `KreatorZrodlaOze`/`KreatorZrodloZasilania`. Kontynuuje wprost dwa znaleziska zgłoszone przez FAB-J (§E, wiersz FAB-J, pozycje (a)/(b)). Znalezisko przy pełnej regresji, naprawione tą samą kolejką (Zero-Debt §1): (1) luka bramy katalogowej dla nowego `battery_catalog_ref` (dług 1 z V12K-316) — wiersz w obu inwentarzach z uzasadnieniem świadomego `bramkowana=False`; (2) regresja SPRZED tej karty (FAB-J `0346a527`) — `useSynchronizacjaDerZModelu`@`AppRoot` woła `useQuery` bezwarunkowo, 4 pliki testowe (App/shell-a11y/integracja/etap10-acceptance) spoza wszystkich dotychczasowych scoped-vitest bramek padały „No QueryClient set" (19 testów) niewykryte od FAB-J — naprawiony współdzielony helper `renderWithQueryClient` (RTL `wrapper`, nie ręczne JSX — jedyny sposób poprawnego działania z `rerender`) + 3 pliki bez adopcji helpera; (3) 14 dalszych plików testowych poza pierwotnym inwentarzem R1-R6 niosło TĘ SAMĄ klasę pól/wartości sprzed R3/R4 — naprawione u źródła, `tsconfig_gate_guard` skorygowany z błędnie zameldowanego FAIL (ocena z podglądu top-15, nie pełnego wyjścia) na rc=0 zielono (137→129, próg utrwalony); (4) `DerConfigurator.tsx` niósł DRUGĄ, martwą kopię etykiet punktu SN (4 klucze nieosiągalne od R3) — zamieniona na reużycie `punktPrzylaczeniaOpisPl`; (5) **odkrycie e2e, nie inspekcji kodu**: naprawa R1 odsłoniła pre-existing lukę bramy certyfikatu zgodności PTPiREE (`zbierz_braki`) — moduł klasy A z ISTNIEJĄCYM certyfikatem PTPiREE miał zawsze `required_count == 0` (żaden test katalogu NC RfG nie ma klasy A w domyślnych wymaganiach) i był BŁĘDNIE blokowany jako „brak podstawy do certyfikacji", mimo że certyfikat producenta jest silniejszym dowodem niż bieg testów; naprawione polem addytywnym `certificate_status` na wyniku solvera + warunkiem zwalniającym w bramce, 3 nowe testy iloczynu cech (klasa A × certyfikat/brak certyfikatu/inny brak). Koordynator w trakcie: cherry-pick `51cdb9df` (E2E-FULL-FIX) scalony bez konfliktu. Szczegóły, inwentarz R1-R6, tabela weryfikacji: `../evidence/CONVERGENCE_EVIDENCE.md` §E (wiersz FAB-K) |
| Karta FAB-L (2026-09-05, worktree) | **wykonana i odebrana** (agent → cherry-pick `e6b87f36` + domknięcie `f8b93d6c`, 2026-09-05; pełna regresja + guardy CI + e2e krytyczne w odbiorze, szczegóły: CONVERGENCE_EVIDENCE §E/§F) — zamknięcie klasy „harness/UI niesie WŁASNĄ kopię katalogu backendu" w dwóch miejscach: `station-der/catalogs.ts` (dane FIZYCZNE liczone w UI + druga kopia danych już serwowanych przez `/api/v1/catalog/audit2/snapshot`) i e2e-harness `creator-harness-main.tsx` (18 tras mockowanych ręcznie + kompozycja `complete-bay-templates`, częściowo identyfikatorami ZMYŚLONYMI) — kontynuacja E2E-FULL-FIX (patrz wpis wyżej: „Pozostałe kopie katalogów w harnessie … → FAB-L L6 z inwentarzem"). **L1**: `BESS_OPERATION_MODE_CATALOG`/`TAP_CHANGER_CATALOG`/`MV_NEUTRAL_GROUNDING_CATALOG` skasowane z frontu — 5 konsumentów (`AddDerWizard`, `StationConfigurator`+2 karty, `InspectorEngineeringView`, `DerSurfaces`) czytają WYŁĄCZNIE `useAudit2CatalogSnapshot` (typy `BessOperationModeItem`/`TapChangerItem`/`MvNeutralGroundingItem` 1:1 z backendem, dotąd `ReadonlyArray<unknown>`); `bess_operation_mode_refs` dopięte do `DER_PROFILE_KEYS` z walidacją. **L2**: `DER_FAULT_CURRENT_DATA_CATALOG`/`computeKappa`/`fault_current_data_ref` USUNIĘTE — TRZECIE pole tej samej klasy fantomów po dwóch usuniętych przez FAB-K (`cable_catalog_ref`/`controller_catalog_ref`/`regulation_profile_ref`); inwentarz solvera (`short_circuit_iec60909.py`+`enm/mapping.py`) potwierdził zero konsumenta — było RÓWNOLEGŁĄ FIZYKĄ obok solvera (naruszenie NOT-A-SOLVER), nie fantomem bez konsumenta. **L3**: `DER_DYNAMIC_MODEL_CATALOG` zastąpiony nową końcówką `GET /api/catalog/der-dynamic-profiles` (realny katalog `network_model.catalog.der_dynamic`, konsumowany przez solvery RMS/FRT-HVRT); wybór WYŁĄCZNIE jawny, auto-selekcja „po rodzaju urządzenia" zniesiona. **L4**: martwe eksporty skasowane pomiarem grepu. **L6**: `CATALOG_FIXTURES` (18 tras) + kompozycja `complete-bay-templates` USUNIĘTE z harnessu (trasy idą dziś do realnego backendu przez proxy Vite); dane 4 rekordów DER w harnessu + 3 specy e2e (`creator-screenshot.spec.ts`, `fk7-dobor-screenshot.spec.ts`, `kreator-oze-max.spec.ts`) przepięte na realne identyfikatory backendu; nowy test parytetu `backend/tests/e2e/test_creator_harness_katalogi_parytet.py` (7 testów) woła TEN SAM predykat domenowy (`_nieznane_referencje_katalogowe`), nie reimplementuje go. Znaleziska przy pełnej regresji, naprawione tą samą kolejką (Zero-Debt §1): (1) `kreator-oze-max.spec.ts` (realny backend, NIE harness) nadal wypełniał usunięty testid `dane-zwarciowe`/pole `fault_current_data_ref` i asercję materializacji — naprawiony na wzorzec pickera modelu dynamicznego już użyty dla CT/VT/zabezpieczenia (index-based selection, nie zaszyty literał — zgodnie z własnym wzorcem pliku); (2) `fk7-dobor-screenshot.spec.ts` używał zmyślonego falownika 900 kW w scenie sprzężonej z REALNYM solverem doboru toru SN (`preview_der_selection`) — zamiana na realny falownik 1 MW zweryfikowana BEZPOŚREDNIM wywołaniem solvera (nie zgadywaniem): identyczny wynik doboru (TR 2,5 MVA, kabel 50→70 mm² po korekcie warunków ułożenia), przypięte nowym testem zamiast zaufania samej nazwie identyfikatora; (3) `tsconfig_gate_guard` ponownie złapany na WŁASNĄ pułapkę obcięcia podglądu do 15 linii (ten sam błąd metodyczny, który FAB-K już udokumentował) — root-caused pomiarem izolowanym w osobnym `git worktree --detach` (po incydencie kolizji `git stash` ze współbieżnie działającym agentem na tym samym repo — odzyskane bez utraty pracy, opisane w meldunku wykonawcy), 3 pliki testowe (`StationConfigurator.test.tsx`, `audit-round3-wiring.test.tsx`, `audit-round4-wiring.test.tsx`) potrzebowały realnych fikstur `mvNeutralGroundings`/`tapChangers` po tym, jak L1 zrobił te propsy WYMAGANYMI — budżet zostaje 128/128 (bez zmiany, zielono). Świadomie POZA KARTĄ (uzasadnione, nie przemilczane): (a) `catalog_ref` kandydatów doboru kompensacji w harnessu (`cap-0v3`/`cap-0v6`/`cap-0v9`, scena „kompensacja-wynik") — wynik analizy/solvera (wyjątek karty (a)), terminalny, nigdy nie odsyłany do żadnego pickera; zmiana wymagałaby przeliczenia od nowa demonstracji V12K-040 B bez odpowiadającej drabinki w realnym katalogu (0,6/1,2/2,4 Mvar 15 kV, nie 0,3/0,6/0,9); (b) `protection-catalogs.ts::HV_FUSE_CATALOG` — DRUGA kopia `hv_fuses` (identyczne identyfikatory `fuse_15kv_50a_full` i in.), poza zakresem karty (§0 nazwał WYŁĄCZNIE `catalogs.ts`) — zgłoszone jako znalezisko tej samej klasy dla osobnej karty; (c) `DerWiazaniaEditor.tsx` nie dostaje pickera modelu dynamicznego mimo istniejącego katalogu — mechanizm pickera ekranu (`TypeCategory`/`TypePicker`) jest zamknięty na kształt generycznego katalogu, a resolver DER-dynamic jest bespoke; zdolność zostaje w pełni osiągalna przez kreator OZE (L3), więc brak jest wygody edytora, nie luki pokrycia — rozszerzenie `TypeCategory` nazwane jako osobna karta. Weryfikacja: backend `pytest -m "not pandapower"` **10992 passed, 10 deselected** (948 s), mypy src 0 (776 plików), ruff/black (src, tests) 0, katalog+API+determinizm+UI guardy (pcc_zero/domain_no_guessing/arch/solver_boundary/canonical_ops/catalog_binding/catalog_enforcement/catalog_gate/catalog_metadata/api_lifecycle/route_prefix/readiness_codes/audit_contract/grep_zero/docs/ui_no_physics/overlay_no_physics/dead_click/dialog_completeness/no_codenames/forbidden_ui_terms/ui_terminology/severity_contract/resultset_v1_schema/load_flow_no_heuristics/protection_no_heuristics/trace_ui_leak/reference_networks/no_direct_fault_params/v12xx_canon/legacy_public_path/transformer_catalog_voltage/physics_label/sld_determinism/trace_determinism/fault_scenarios_determinism/repo_hygiene/import_graph/vulture/docs_archive/local_truth/utf8_mojibake/interaction_matrix/guard_ux_flow_v1/nn_source_menu/fix_action_completeness/verification_phantom_paths) rc=0 (44 guardy), `tsconfig_gate_guard` **128/128 (rc=0, bez zmiany)**, `solver_input_substitute_guard` 3600 pól (pin 3601→3600 zaktualizowany z uzasadnieniem semantyki zbioru, nie sumy), testy własne guardów (`scripts/`) **533 passed**, snapshot OpenAPI **317→318 ścieżek / 228 schematów** (+`GET /api/catalog/der-dynamic-profiles`); frontend `tsc --noEmit` 0, `eslint . --max-warnings 0` (cały projekt) 0, vitest gate-scope (`network-build workspace sld/v2/canvas ui2/kreatory ui2/oze ui2/spaces/wyniki`) **248 plików / 2899 passed** (jedna regresja znaleziona i naprawiona w toku biegu — `InspectorEngineeringView.generator.test.tsx` czekał na stary format etykiety modelu dynamicznego sprzed L3, fikstura+asercja przepisane na realny profil `default_pv_gfl` i realne formatowanie `formatDerDynamicProfileLabelPl`, drugi pełny bieg zielony); e2e na własnych portach (8071/5241) `critical-der-config`/`critical-oze-evidence`/`critical-engineer-flow` **10 passed (1,1 min), 0 failed** (świeża baza SQLite/ENM per bieg). Szczegóły, pełny inwentarz klasy, tabela weryfikacji: `../evidence/CONVERGENCE_EVIDENCE.md` §E (wiersz FAB-L) |
| Karta FAB-M (2026-09-05, worktree na `0d76d43c`) | **wykonana i odebrana** (agent → cherry-pick `c16c497d`, 2026-09-05; weryfikacja frontu + parytet katalogów + bramka tsconfig w odbiorze, szczegóły: CONVERGENCE_EVIDENCE §E/§F). Zgłoszenie FAB-L (nota proweniencji `catalogs.ts`): `protection-catalogs.ts::HV_FUSE_CATALOG` — DRUGA KOPIA 4 pozycji wkładek SN, identyczne identyfikatory z backendowym `audit2_catalogs.HV_FUSE_CATALOG`, jedyny konsument `StationConfigBaysCard.tsx`. Inwentarz KLASY (zmierzony grepem, nie z listy — cel M1): plik niósł 5 katalogów — poza `HV_FUSE_CATALOG` (duplikat backendu, migrowany) trzy inne (`PROTECTION_FUNCTION_CATALOG` + 3 selektory, `SPZ_CATALOG` + selektor, `SZR_CATALOG`) i jeden predykat (`isCtClassValidForMetering`) miały ZERO konsumentów produkcyjnych (wyłącznie testy) — dług L4 (wzorzec FAB-L) widniejący już otwarty w `docs/v12xx/REJESTR_KONFLIKTOW.md` (wiersz K-O, „karta K-V") i `docs/plan/PLAN_DOKONCZENIA_100_2026-08-14.md` (punkt 3): kasacja z testami, bez migracji (nie ma czego migrować). `CtClass`+`isCtClassValidForProtection` zostają — realny konsument `readiness.ts`. Migracja HV_FUSE: typ 1:1 (`audit2-api.ts::HvFuseItem`/`HvFusePasmoTcc`, pole `hv_fuses` snapshotu odtypowane z `unknown[]`), `StationConfigBaysCard.tsx` czyta katalog jako prop `hvFuses` (ze snapshotu, wątek `StationConfiguratorSurface.tsx`) — dwie stałe PL (`POWOD_BRAK_PASMA_BEZPIECZNIKA_PL`/`ETYKIETA_BRAK_PASMA_BEZPIECZNIKA_PL`) zastąpione polami liczonymi przez backend per pozycja (`pasmo_brak_powod_pl`/`pasmo_brak_etykieta_pl`) — zero literału PL po stronie frontu. M3 (fizyka w UI): zmierzone, brak — `selectHvFusesForRating` filtrował WYŁĄCZNIE po progach numerycznych katalogu (nie liczył nic), i tak zero konsumenta produktu → usunięty z testem, nie migrowany do przyjmowania katalogu jako parametru (wzorzec `selectTapChangersForTransformer` dotyczy wyłącznie selektorów z realnym konsumentem). Backend: `test_audit2_katalogi_parytet.py` — test parytetu bloku zastąpiony testem NIEOBECNOŚCI bloku (wzorem `test_trzy_katalogi_nie_maja_juz_bloku_mirrora_w_froncie` z FAB-L, POZA MOIM zakresem — te 3 katalogi w `catalogs.ts` NIETKNIĘTE) + NOWY test (M2): każdy identyfikator bezpiecznika użyty w fikstarach/testach/e2e CAŁEGO drzewa `frontend/src`+`frontend/e2e` musi istnieć w katalogu backendu (regex `fuse_\d+kv_\d+a_[a-z]+`, odróżniony od niepowiązanego `fuse_set`/rodziny ETI VV z myślnikami). Tabela weryfikacji, czego NIE zweryfikowano: `../evidence/CONVERGENCE_EVIDENCE.md` §E (wiersz FAB-M) |
| **Raport fazy CV-0 → CV-4.2b (2026-09-05, szczyt `c5f769de`)** | `../evidence/RAPORT_FAZY_CV_2026-09-05.md` (format §42 A–J). Stan: CV-1/CV-2/CV-3 domknięte (rdzeń + wiring + kasacje procedurą + guardy; DT-2 i DT-6 FROZEN w `DECISION_FREEZE_REGISTER.md`, DT-7/DT-8 EVIDENCE-GATED), CV-4 w połowie (`enm/assembler.py` jedynym producentem wejścia PF/SC toru kanonicznego, P11 i wariantów, bez własnej bazy; zapadka 3 pliki / 16 konstrukcji wyłącznie w `application/reference_networks/**`). CI na szczycie: **9/9 zielone** (Python tests 4886, Frontend checks 3367, E2E full 350, E2E smoke 2528, Docs 3511, Physics 1580, Arch 3134, P0 1581, SLD 3416). **Dokładny następny wycinek: CV-4.3** — benchmarki i sieci referencyjne jako ENM przez assembler (`BENCHMARK_DICT_ZASTANE` {G07: 1, B-BENCH: 12} → {}), kasacja P6–P10/S5/S6 i własnego NR (zapadka assemblera → 0), `TopologyService` jedna implementacja (18 → 1), `backend_no_physics_guard` z pustą allowlistą, 3 trasy E2 uruchomienia; potem CV-4.4 legacy ORM procedurą (krok 3 po OD-2) → CV-5. Decyzje właściciela otwarte: OD-1…OD-6 (`../evidence/CONVERGENCE_EVIDENCE.md` §I) |

## 2. Wycinki konwergencji CV-0…CV-6 (kolejność wiążąca; każdy = vertical strangler)

| Wycinek | Zakres | Parity evidence | Guard przeciw wskrzeszeniu | DoD §40 |
|---|---|---|---|---|
| **CV-0 Trust foundation** | CI 8/9 → 9/9 zielone na gałęzi; polityka determinizmu (`CANONICAL_TWIN_ARCHITECTURE.md` §C.5); ochrona `main` lub owner action; fabrykacje (FAB-A/B); rejestr sieci wzorcowych; inwarianty | wyniki CI, guardy, testy | `verification_phantom_paths_guard` (istnieje), `guardy_z_ci` | 18, 19, 20 |
| **CV-1 Project owns ENM** | magazyn per projekt, fasada case→project, migracja per-case ENM → rewizja/warianty | hash ENM projektu = hash ENM aktywnego przypadku; wszystkie e2e na fasadzie zielone | guard: zapis magazynu kluczem `case_id` = czerwony | 1, 2 |
| **CV-2 Rewizje i envelope** | `ModelRevision` z dziennika (bez limitu 500, checkpointy), `checkout(rev)`; `RevisionEnvelope` na `CanonicalRun`; świeżość z envelope; `catalog_revision_set`, `assumptions_revision` | `checkout(rev_n)` odtwarza `hash_sha256` rewizji n dla wszystkich sieci rejestru | guard: bieg bez envelope = czerwony | 12, 13 |
| **CV-3 Scenariusze** | jeden `OperatingScenario`, jedna funkcja `apply_scenario` → `EffectiveNetworkSnapshot`; migracja N-1/hosting/pq_area/odpowiedz_osd/FaultScenario; kasacja C2/C3/C4/C5 | wyniki N-1 i hosting bit-identyczne przed/po | guard: `copy.deepcopy(snapshot)` poza `apply_scenario` = czerwony | 7, 8 |
| **CV-4 Granica obliczeniowa** | jeden assembler ES → TV → IR → kontrakt; kasacja P2–P12/S2–S7 i własnego NR; `TopologyService` jedna implementacja; legacy ORM procedurą | 12 benchmarków IEEE/CIGRE + rejestr sieci: wyniki identyczne (tolerancja zadeklarowana) | guard: konstrukcja `PowerFlowInput(`/`ShortCircuitInput(` poza assemblerem = czerwony; `backend_no_physics_guard` | 9, 10, 11, 12 |
| **CV-5 Terminale, fazy, uziemienie** | T-2, F-1…F-4; walidator; migracja `meta.field_specs` → `Bay` | scena SLD i projekcja nN identyczne; Z0 identyczne | `meta_field_specs_resurrection_guard`; walidator terminali | 3, 4, 5, 6 |
| **CV-6 G01 vertical slice** | sieć G01 (§31) zbudowana komendami domenowymi; pełny łańcuch EDIT → … → REPORT; analiza doziemna kompensowana; PV; nN ABCN/SWZ w zakresie gotowym | wyrocznie z rejestru (analityczne + benchmark) | test e2e G01 jako bramka CI | 21 |
| dalej | ownership zabezpieczeń (14), SLD jako projekcja (15), SN/nN bez równoległych modeli (16), White Box (17), kasacje legacy (23, 24) | | | |

Każdy wycinek kończy się raportem w formacie §43 konstytucji (A–K) i wpisem w `../evidence/CONVERGENCE_EVIDENCE.md`; granica przechodzi w **FROZEN** dopiero po: dowodzie implementacji, wyroczni inżynierskiej, `DECISION_FREEZE_REGISTER.md`, przeglądzie adwersaryjnym i bramce CI (§39). Ponowne otwarcie wymaga dowodu defektu (inżynierskiego, numerycznego, spójności danych, niezdolności do zdolności z `PRODUCT_CAPABILITY_MODEL.md`, bezpieczeństwa, wydajności) albo nowego jawnego wymagania właściciela — preferencja architektoniczna nie jest dowodem.


## 3. Kroki CV-1 → CV-3 (parity, guardy)

| Krok | Działanie | Parity evidence | Guard |
|---|---|---|---|
| CV-1.1 | magazyn ENM kluczem `project_id`; fasada `case_id → project_id` w `api/enm.py` | wszystkie e2e i testy API zielone przez fasadę; hash ENM projektu = hash aktywnego przypadku | zapis kluczem `case_id` = czerwony |
| CV-1.2 | migracja danych: ENM przypadków ≠ aktywnego → `NetworkVariation` (diff komend) albo raport migracji | liczba przypadków z własnym ENM BEFORE/AFTER; 0 utraconych modeli (eksport ZIP) | — |
| CV-2.1 | **DONE** — `ModelRevision` PRZY MAGAZYNIE ENM (`enm/rewizje.py`): niezmienna migawka per rewizja `<digest>.rev/<n>.json.gz` (gzip `mtime=0`, kanoniczny JSON, adresowana hashem treści) + wpis dziennika z `hash_sha256`, `rodzic`, PEŁNYM ładunkiem komendy (`ZrodloZmiany.ladunek`); kolejność: dziennik (roboczy) → migawka (robocza) → HEAD → migawka → dziennik; HEAD autorytatywny; sierota (migawka > HEAD) usuwana przy wczytaniu, brakująca migawka/wpis bieżącej rewizji odtwarzane z HEAD z opisem nazywającym brak; `LIMIT_WPISOW` usunięty; `checkout(klucz, n)`; migracja klucza przypadku (CV-1) kopiuje dziennik i migawki pod klucz projektu (wcześniej historia lądowała w `legacy_przypadki/`); import archiwum daje migawkę + wpis „import" | `checkout(n).hash == revision[n].hash` dla całego rejestru sieci wzorcowych; dziennik zmian = projekcja rewizji (parity z dzisiejszym API dziennika) | bieg bez envelope = czerwony |
| CV-2.2 | **DONE (rdzeń)** — `RevisionEnvelope` (`enm/envelope.py`: `project_id`, `model_revision`, `snapshot_hash`, `catalog_fingerprint` z `network_model/catalog/odcisk.py`, `options_hash` = `input_hash`, `semantic_fingerprint`) na `CanonicalRun.envelope` (kolumna addytywna `envelope_json`, dokładana do istniejących baz); świeżość z koperty (`evaluate_envelope_freshness`, lista zmian z dziennika, przyczyna „katalog zmieniony"); H1/H2/H3 skasowane. **W toku (karta CV-2-W):** status przypadku liczony z biegów w API, kasacja pisarzy `ResultInvalidator`/`mark_*`/końcówek `invalidate*`, ładunek komendy w `api/enm.py`, ekran świeżości z listą zmian | wyniki biegów bit-identyczne przed/po (envelope nie zmienia fizyki) — golden nietknięte | `provenance_constant_guard`: literał wersji/katalogu w polu proweniencji = czerwony |
| CV-3.1 | **DONE (rdzeń, 2026-09-05)** — `enm/scenariusze.py`: `OperatingScenario` (pola WYŁĄCZNIE z dostawcą: `out_of_service`, `setpoints`, `gen_scaling`, `injections`, `probe_shunts`, `fault_spec`), `apply_scenario` → `EffectiveNetworkSnapshot` (proweniencja każdego nadpisania, tożsamość stanu normalnego, `hash_migawki_enm` ze słownika), magazyn scenariuszy nazwanych (`<digest>.scen/`, rewizje append-only, nagrobek, migracja klucza CV-1 za modelem), `create_run(scenariusz=)` z walidacją migawki efektywnej, koperta v2 (`scenario_ref`/`scenario_hash`; v1 = stan normalny bit w bit), świeżość `SCENARIUSZ_ZMIENIONY`/`SCENARIUSZ_USUNIETY`, `bieg_wariantu` (jedyna fabryka wariantu w pamięci). **DONE (2026-09-05):** złote hashe parytetu D1–D6 zebrane PRZED migracją (`tests/golden/parytet_scenariuszy/`, 28 hashy, karta PARITY-CV3 `1cae2f84`); migracje D1–D6 na `apply_scenario` + `bieg_wariantu` + `wykonaj_bieg_w_pamieci` z parytetem bit w bit (CV-3-W: D4–D6 `dc5fedd0`, D1–D3 `9faa78c7`; zapadka `scenario_copy_guard` **19 → 0**; prywatne pomocniki rodzin usunięte; dyspozytor przyjmuje gotowy graf rozpływu — jedna budowa grafu na kontyngencję zachowana). **DONE (2026-09-05, C6-PERSIST `f53b780a`):** C6 trwały — `FaultScenarioService` bezstanowy na magazynie scenariuszy (`kind=FAULT_STUDY`, `fault_spec`), `klucz` argumentem każdej metody, „ma biegi" z `koperta.scenario_ref` dla pojedynczego biegu i serii, `register_run` skasowany, `revision` w odpowiedzi API; lokalizacja zwarcia honorowana w torze SC: BUS/NODE → jeden węzeł (parytet fizyki), BRANCH/BRANCH_POINT → jawna odmowa kodem `fault.location_on_branch_requires_assembler` w eligibility i w solverze (do czasu assemblera CV-4) | N-1, hosting, pq_area, odpowiedz_osd, kompensacja, nastawy: wyniki bit-identyczne (złote hashe sprzed migracji) | `scenario_copy_guard.py` (CI): import `_execute_*` / `CanonicalRun(...)` poza fabryką / `deepcopy(migawka)` / `dict(migawka)`+zapis = czerwony; zapadka 19 zastanych |
| CV-3.2 | **DONE (C2/C3/C4, 2 commity)** — kasacja procedurą 7 kroków (inventory → consumer search → export → parity → cutover → observation → removal). C2 (`domain/study_case_engine.py`) i C3 (9 operacji domenowych v2 `study_cases[]` w ENM-dict) usunięte w pierwszym commicie po ponownej weryfikacji grepem (0 importów). C4 (`application/study_scenario/**`, `analysis/scenario_comparison/**`) w pierwszym commicie zatrzymany na `analysis/reporting/pdf/**` (P24+) — jedynym produkcyjnym konsumencie `ScenarioComparisonEntry`/`View` poza samym builderem — po decyzji architektonicznej (P24+ = ten sam byt co C4, „raport bez trasy": 0 wołających w `backend/src` poza własnym re-eksportem, 0 tras HTTP) skasowany razem z C4 w drugim commicie. C5 (legacy ORM) POZOSTAJE — NIE w tej karcie, patrz wiersz CV-4 poniżej | 0 importów, 0 tras, 0 testów wskazujących skasowane byty (dla C2/C3/C4) — zweryfikowane grepem obu commitów | `legacy_public_path_guard` rozszerzony o skasowane nazwy (C2: `StudyCaseEngine`/`SolverProtocol`; C3: 9 nazw operacji jako klucze rejestru; C4+P24+: `ScenarioComparisonBuilder`/`application.study_scenario`/`analysis.scenario_comparison`/`export_p24_plus_report_pdf`/katalogi pakietów) |
| CV-3.3 | E2/E3 kasacja; E4 do orkiestratora; R2/R3/R4 kasacja | 1 rejestr biegów; porównania i zabezpieczenia czytają R1 | guard: `analysis_runs`/`study_runs` w kodzie = czerwony |
| CV-3.3-B | **DONE (B1–B3 + B5, 2026-09-05; B4 → CV-3.3-C)** — porównania A/B (rozplyw P20c, zabezpieczenia P15b, ogólne P10b) przepięte na R1 (B1): trzy serwisy (`application/{power_flow_comparison,protection_comparison,comparison}/service.py`) przepisane na czytanie WYŁĄCZNIE `ResultSetV1` przez `build_resultset_v1_from_canonical_run` (JEDYNY producent projekcji — zero własnego parsowania `raw_result`); arytmetyka delt wspólna w `analysis/comparison_diffs/diffs.py` (nowy moduł, przeniesiony z pierwotnej ścieżki `analysis/scenario_comparison/` po kolizji `legacy_public_path_guard` z INNYM, wcześniej skasowanym modułem CV-3.2/C4 tej samej nazwy — `dopasuj_klucze`/`delta_lub_none`/`procent_lub_none`/`RunProvenance`, zero duplikacji między trzema serwisami, KLASA NIE INSTANCJA); porównania BEZSTANOWE (`comparison_id` = `"{run_a}::{run_b}"` dekodowalny, przeliczane od nowa — R3 skasowana, nie ma czego cache'ować). Bieg zabezpieczeń jest odtąd `CanonicalRun.analysis_type="protection_sn"` (B2): `enm.canonical_analysis._execute_protection` zastępuje `ProtectionAnalysisService` (skasowany), `api/protection_runs.py` przepisany na ten sam kontrakt HTTP. Kasacja procedurą 7 kroków (R2 `AnalysisRunService` + R3 `study_runs`/`study_results`, B3) — zob. wiersze R2/R3 w `CANONICAL_TWIN_ARCHITECTURE.md` B.2 dla pełnego inwentarza i **sprzeczności wykrytej i rozstrzygniętej** (`analysis_runs_index` NIE jest częścią R2 — niezależny, żywy mechanizm koordynacji nastaw). **Dwa realne defekty produkcyjne znalezione i naprawione W TEJ KARCIE (zero-dług, nie zgłoszone osobno):** (1) `_execute_protection` budował WŁASNY silnik/sesję z `DATABASE_URL` (`_uow_factory_biezacy`) zamiast fabryki wołającego — każdy przypadek istniejący naprawdę zgłaszał się jako nieznaleziony w KAŻDYM wdrożeniu, którego `app.state.uow_factory` nie pochodzi z tego env var (co obejmuje każdy test) — naprawa: `execute_run`/`_wykonaj_analize_biegu`/`_execute_protection` przyjmują `uow_factory` od wołającego (addytywny parametr, domyślnie `None` = zachowanie wsteczne dla wywołań spoza granicy API), `api/protection_runs.py::execute_protection_run` przekazuje własną fabrykę. (2) `PowerFlowComparisonTraceResponse` (pydantic) niosła martwe pole `snapshot_id_a/b` po zmianie domeny na `snapshot_hash_a/b` (B1) — endpoint `/power-flow-comparisons/{id}/trace` kończył się 500 (`ResponseValidationError`) na KAŻDYM wywołaniu; naprawione przez ujednolicenie nazw pól. **Trzeci defekt (dług, nie regresja) znaleziony i naprawiony:** `provenance_a`/`provenance_b` (dowód CO było porównywane — B1) dodane do trzech DOMENOWYCH DTO porównania, ale NIGDY do trzech pydantic modeli odpowiedzi API — pole ginęło na granicy HTTP mimo poprawnej domeny; naprawione dodaniem `RunProvenanceResponse` (+pól) do `PowerFlowComparisonResultResponse`/`ProtectionComparisonResultResponse`/`RunComparisonResponse` (`domain/results.py::RunComparisonResult` też dostał `provenance_a/b` — generyczne porównanie ich wcześniej NIE niosło wcale, mimo że B1 obejmuje wszystkie trzy endpointy jednym akapitem). Wszystkie trzy defekty złapane DOPIERO nowymi testami API tej karty (`tests/api/test_run_comparisons_r1_api.py`) — zero istniejącego pokrycia HTTP dla tych trzech endpointów przed tą kartą (wyłącznie testy domenowe na fikstywnych obiektach). E4 (`BatchExecutionService`) **podzielone do CV-3.3-C** (wiersz niżej, preautoryzowany podział z inwentarzem). **B5 (frontend, dokończone w kontynuacji sesji):** `ui/power-flow-comparison/{types,api}.ts` + `ui/protection-comparison/{types,api}.ts` wyrównane do R1 (`RunProvenance`, `snapshot_hash`/`model_revision`/`scenario_ref` na liście biegów); etykieta selektora A/B (`etykietaPrzebiegu` w `ui2/wyniki/porownanie/porownanieModel.ts` — LIVE ekran — oraz `RunSelector` w obu DEAD stronach `ui/*-comparison/*Page.tsx`, uaktualnione dla spójności klasy) niesie rodzaj + rewizja/scenariusz + krótki `snapshot_hash`, nie sam UUID; nowy panel `mvd-por-proweniencja` w `EkranPorownania.tsx` (wyłącznie tryb ekspercki) spełnia wymóg B1 „ekran porównania pokazuje kopertę obu biegów" — bez niego wymóg B1 był spełniony wyłącznie w API, nie w UI. Honest zero-state (brak dwóch biegów → komunikat + nawigacja `fix_navigation`) był już obecny w `EkranPorownania.tsx` przed tą kartą — zweryfikowany, nie dodany. **Znalezisko:** `PowerFlowComparisonPage.tsx`/`ProtectionComparisonPage.tsx` (stary `ui/`) są MARTWE — zero konsumentów w `frontend/src` poza własnym katalogiem (`EkranPorownania.tsx` w `ui2/` jest jedynym żywym ekranem porównania rozpływu; **protection-comparison NIE MA żywego ekranu w ui2 wcale** — luka produktowa, poza zakresem B5, który naprawia wyłącznie dane/typy istniejących ekranów, nie buduje nowych). Nie skasowane (poza mandatem wiązania danych tej karty), udokumentowane. **Cztery kolejne defekty złapane w kontynuacji sesji (środowisko odtworzone od zera — poetry venv i `node_modules` puste po wznowieniu, co ujawniło luki niewidoczne przy ciągłym środowisku):** (4) brakujący `GET /projects/{project_id}/protection-runs` — endpoint NIGDY nie istniał (zweryfikowane na `git show 1fdeec44`), `fetchProtectionRuns` frontendu zawsze dostawał 404 i cicho zwracał puste — dodany endpoint + `ProtectionRunListItemResponse`. (5) `api_lifecycle_guard`/`MACIERZ_KOMPATYBILNOSCI_API.md` bez wiersza dla tego nowego endpointu — dopisany. (6) `tsconfig_gate_guard`: dwa NIEZALEŻNE fixture'y (`ui/power-flow-comparison/__tests__/power-flow-comparison.test.ts`, `ui/protection-comparison/__tests__/protection-comparison.test.ts` — osobne od `ui2/wyniki/porownanie/__tests__/fixtures.ts`, które miało poprawkę od razu) budowały `PowerFlowComparisonResult`/`ProtectionComparisonResult` literałem bez nowych pól `provenance_a/b` (137→139 poza bramką); naprawione + przy okazji zdjęty 1 pre-existing martwy import (`PowerFlowComparisonTab`, TS6133) w tym samym imporcie — budżet 136. (7) `scenario_copy_guard` (R2: `CanonicalRun(...)` poza fabryką) zameldował 1 trafienie w `application/project_archive/service.py` — DESERIALIZACJA historycznego biegu z importu ZIP, INNA KLASA niż dług scenariuszy (obie fabryki `create_run`/`bieg_wariantu` czytają BIEŻĄCY model i strukturalnie nie potrafią odtworzyć gotowego wyniku z archiwum); udokumentowany jako jedyny dopuszczony wyjątek w `ZASTANE`, nie naprawiony „inaczej" (naprawa przez fabrykę byłaby fikcją zgodności). Osobno: obrona w głąb — `run.model_revision !== null` (trzy miejsca: `porownanieModel.ts` + oba `RunSelector`) zamieniona na `typeof run.model_revision === 'number'`, bo `undefined` (fixture bez pola) renderował się jako literalny tekst „rew. undefined"; oraz mock `/api/power-flow-comparisons` w `frontend/src/creator-harness-main.tsx` (scena e2e/screenshot „porownanie", tryb ekspercki) bez `provenance_a/b` — nowy panel wywalał render na `undefined.analysis_type`, `flow-ekspert-screenshot.spec.ts` scena „porownanie" czerwona (`mvd-por-wynik` nigdy niewidoczny); naprawione dopisaniem pól do mocka, spec zielony (16/16 w pliku, oba motywy zrzutów zaktualizowane — `docs/audit/visual/flow-ekspert/{flow_porownanie,porownanie-q}_{light,dark}.png`). Weryfikacja: pełny backend pytest (`tests/api tests/application tests/analysis tests/enm tests/invariants tests/infrastructure tests/e2e tests/domain tests/ci`, `-m "not pandapower"` — parytet z CI, 5889 passed/0 failed/10 deselected), mypy/ruff/black 0, `scripts/` guardów pytest (533 passed) + wszystkie guardy z listy CLAUDE.md pojedynczo (RC 0), OpenAPI przeliczony, frontend (tsc/eslint/vitest 153 passed dla trzech obszarów + `tsconfig_gate_guard` budżet obniżony do 136), e2e `flow-ekspert-screenshot.spec.ts`/`wszystkie-sceny-screenshot.spec.ts` scena „porownanie" — pełna tabela w meldunku karty (`../evidence/CONVERGENCE_EVIDENCE.md` §F wpis CV-3.3-B) | zero R2/R3 w kodzie (guard); 3 endpointy porównania czytają WYŁĄCZNIE R1; delty porównania spójne ze znakiem realnej zmiany modelu (nie tylko arytmetyka własnej odpowiedzi) — nowe testy `tests/api/test_run_comparisons_r1_api.py`; ekran porównania pokazuje kopertę obu biegów (nowy test `ekranPorownania.test.tsx::proweniencja biegów A/B widoczna wyłącznie w trybie eksperckim`) | `legacy_public_path_guard` rozszerzony (nazwy/ścieżki R2/R3 skasowane); `solver_input_substitute_guard` pin przeliczony; `tsconfig_gate_guard` budżet 137→136; `scenario_copy_guard` +1 udokumentowany wyjątek (R2) **Odbiór (2026-09-05, Fable):** cherry-pick bez konfliktów; domknięcie 1 — wyjątek per plik w zapadce R2 `scenario_copy_guard` (import archiwum) zastąpiony trzecią fabryką `enm.canonical_analysis.odtworz_bieg_z_archiwum` (jeden dom konstrukcji `CanonicalRun`; zapadka z powrotem na ZERZE, test własny pinuje zero). Domknięcie 2 — `readiness_consumption_guard` czerwony: jedyny emiter `source.connection_missing` był w skasowanym R2, a assembler pomijał źródło bez istniejącej szyny cichym `continue` → reguła walidatora `sources.bus_missing` (BLOKADA) odwzorowana mostem na kanon + odmowa assemblera z nazwą (zero cichych podstawień). Znaleziska agenta z kartami: E4 → **CV-3.3-C** (`karta_cv33c.md`); porównanie zabezpieczeń BEZ ekranu ui2 mimo A16 inwentarza funkcji + martwe strony `ui/{power-flow,protection}-comparison/*Page.tsx` → **CV-3.3-B2** (`karta_cv33b2.md`: jeden ekran porównań z rodzajem rozpływ/zabezpieczenia, kasacja martwych stron); tabela `analysis_runs` + `AnalysisRunRepository` (zależność legacy kreatora) → CV-4 A4/P2 |
| CV-4.1 | **wykonana (Fable osobiście, 2026-09-05, worktree `fable-cv4`; odbiór = pełna regresja po rebase na szczyt po FAB-K, wynik w `../evidence/CONVERGENCE_EVIDENCE.md` §E/§F).** **Krok 0** (`6d08d914`): zbieranie złotych hashy assemblera na CAŁYM rejestrze sieci (`tests/golden/registry.py`) + scenariuszach nN × {PF, SC 3F max/min, 1F, 2F, 2FG} przez `kwantyzuj_kontrakt` (tryb ścisły §35) OBALIŁO twierdzenie „wynik kanoniczny jest kontraktem liczb”: wyspa nN bez zasilania dawała `NaN` w `node_voltage_kv` (węzły nierozwiązane) oraz κ ≈ 3·10¹², i_p ≈ 2·10¹⁴ A, I_th = ∞ w zwarciu (solver FROZEN liczy κ z R/X impedancji zastępczej o ujemnym R albo X; IEC 60909-0 §4.3.1.1 ogranicza κ do [1,02; 2,0]). Naprawa u źródła w WYKONAWCACH (solver nietknięty, B-01 → OD-6): węzeł nierozwiązany → `None` (predykaty parami: `NaN` ⇔ `not_solved_nodes`, każdy inny `NaN` wypisany w `non_finite_fields`), wiersz zwarcia z wartością niefinitową albo κ poza pasmem → `reporting_status="not_reportable"`, `proof_status="partial"`, ograniczenie `solver_result_non_physical` + `non_physical_fields`; bieg niesie `non_reportable_fault_node_ids`; ślad White Box sanityzowany tą samą funkcją; sieci zdrowe bit w bit bez zmian (`tests/enm/test_wyniki_finitowe.py`, 3 testy klasy). `tests/golden/parytet_assemblera/zlote_hashe.json`: 252 wpisy (42 odmowy przypięte jako kontrakt: 24× dwa SLACK w scenariuszu nN, 10× osobliwa Y0, 8× osobliwa Y) — stan PRZED wycięciem. **Krok 1** (`d4c506c3`): `enm/assembler.py` — `zloz_wejscie_rozplywu(snapshot, options, graph=None) -> WejscieRozplywu`, `zloz_wejscie_zwarcia(snapshot, options) -> WejscieZwarcia`, `zbuduj_graf(snapshot)`; 12 pomocników przeniesionych 1:1 (m.in. `_short_circuit_type_from_options`, `_short_circuit_requires_z0`, `_build_snapshot_graph_element_context`, `_study_frequency_hz` — sygnatura: migawka zamiast biegu, `_build_shunt_specs_from_snapshot`, `_build_converter_control_by_node`, `_maybe_load_audit2_extensions`); assembler NIE importuje `CanonicalRun` (zależy wyłącznie od migawki i opcji — brak cyklu); wykonawcy wołają assembler i dalej tylko rozwiązują i montują wynik (`canonical_analysis.py` 3873 → 3419 linii); 14 plików testów przepiętych na `enm.assembler` (uczciwy dom, zero re-eksportu nazw prywatnych); test inspekcji źródła audit2 pinuje, że wykonawcy wołają `zloz_wejscie_*(`; parytet 252/252 bit w bit. **Krok 2**: guard `scripts/solver_input_assembler_guard.py` — wywołanie konstruktora `PowerFlowInput(`/`ShortCircuitPayload(`/`LoadFlowPayload(`/`PQSpec(`/`SlackSpec(`/`PVSpec(`/`ShuntSpec(` (AST, nie grep) poza domem `enm/assembler.py` = czerwony; zapadka ZASTANE w OBIE strony: 7 plików / 31 konstrukcji toru legacy (P12 `api/solver_input.py` 2; P2/S4 `network_wizard/service.py` 6; P5 `power_flow_input_builder.py` 5; P6–P8 `reference_networks/{frozen_solver_input,sld_substrate_power_flow,station_archetype_substrate}.py` 6+3+7; P11 `solver_input/builder.py` 2 — kontrakt LOCKED wypełniany docelowo przez assembler) schodzi WYŁĄCZNIE w dół (kasacje procedurą w CV-4.2/CV-4.3); allowlista z powodem: `power_flow_gauss_seidel.py` (rdzeń FROZEN buduje `PowerFlowInput` fallbacku GS→NR z pól istniejącego `pf_input`, nie z modelu); `ShortCircuitInput` kreatora (`network_wizard/dtos.py`) celowo poza listą — DTO kreatora, nie kontrakt solvera (pomiar CV-4.0 §1.3.3), ginie z kreatorem w CV-4.2; 15 testów własnych; krok CI w `p0-extended-guards.yml` + lista P0 zestawu przedpushowego. Zapadka `solver_input_substitute_guard` PRZENIESIONA 1:1 razem z kodem (7 pozycji z bloku `canonical_analysis.py` do bloku `assembler.py`: `defaults.frequency_hz`, `options.base_mva`, `node.active_power`/`reactive_power`, `options.tolerance`/`max_iter`/`thermal_time_seconds` — dług bez zmian, suma korzenia `enm` 84; +8 pól kontraktu = nazwy pól dataclass wyniku assemblera, pomiar różnicy zbiorów wobec szczytu sprzed karty). A3-04 (`pv_bus_ids=[]` → PV z modelu, z pomiarem) → **CV-4.1b** (`karta_cv41b_a304.md`, agent po scaleniu). | 252 złote wpisy PRZED/PO wycięciu: lokalnie bit w bit (szkielet + liczby + ścieżki), na CI szkielet KONTRAKTU dokładnie (bez poddrzew śladu White Box — CI-PARYTET-4: solver FROZEN buduje ślad progiem zerowym na liczbie, członkostwo gałęzi jest funkcją szumu) + liczby kontraktu w tolerancji 2·10⁻⁶ (CI-PARYTET-2 — surowy wynik solvera nie jest przenośny między maszynami przy żadnej siatce), mapa skrótów poddrzew w złotym pliku wskazuje ścieżkę rozbieżności; CI-PARYTET-5: wiersze wysp bez impedancji do odniesienia kwalifikowane z topologii w wykonawcy (liczby → `None`), nie po szumie κ; guard czerwony na konstrukcji poza domem i na spadku bez obniżenia zapadki (testy własne); wynik kanoniczny bez `NaN`/`inf` | `solver_input_assembler_guard` (P0, zapadka 7/31); `solver_input_substitute_guard` pin przeniesiony (64 pliki / 312, bez wzrostu) |
| CV-4.2 | **wykonana i odebrana (2026-09-05, worktree agenta → cherry-pick `52f39980`/`a2936c57`/`866595ce` + domknięcie `469af0a1`; odbiór = pełna regresja `tests/` + guardy CI + snapshot OpenAPI, wynik w `../evidence/CONVERGENCE_EVIDENCE.md` §E/§F; dług zastany DB assemblera → CV-4.2b).** Kasacja czterech ścieżek równoległych (K1–K4, procedurą 7 kroków) + przepięcie P11 na assembler (K5): **P2/S4** (`network_wizard/service.py::build_power_flow_input`/`build_short_circuit_input` + 6 pomocników wyłącznych + `ShortCircuitInput` DTO) — 0 konsumentów produkcyjnych (pomiar potwierdzony grepem), skasowane (`service.py` 2481→2228 linii). **P5** (`application/power_flow_input_builder.py`) — 0 konsumentów, skasowany razem z testem parity P2↔P5 (`test_wezel_zlozony_wejscie_rozplywu.py`, 1032 linii) po dowiedzeniu, że fizyka pozostaje pokryta niezależnie na torze kanonicznym (`tests/enm/{test_prosument_falownik_odbior,test_oze_pf_inverter_control,test_zip_wiring}.py`). **P13** (`domain/load_flow_input.py`+`load_flow_validation.py`) — skasowane z testami istniejącymi wyłącznie dla nich. **P12** (`api/solver_input.py::run_audit2_power_flow`, fabrykacja `pq=[]`/`slack-stub`) — endpoint `POST /api/cases/audit2-power-flow` skasowany; JEDYNY konsument FE (`ui/network-build/station-der`, zweryfikowany grepem literału trasy) przepięty na bieg kanoniczny (`POST /api/execution/study-cases/{id}/runs` → `.../execute` → `.../results`, `global_results.audit2_applied`); `MACIERZ_KOMPATYBILNOSCI_API.md` → `usuniety`, OpenAPI snapshot przeliczony. **P11/S7** (K5, `solver_input/builder.py` — kontrakt LOCKED v1.1, ZERO zmian pliku) — `api/solver_input.py::_graph_for_analysis` przepięty z własnego `_get_graph_for_case`/`_get_graph_for_snapshot` na `enm.assembler.zloz_wejscie_rozplywu`/`zloz_wejscie_zwarcia` (ten sam tor co bieg kanoniczny). **Odkrycie przy tej karcie (poza enumeracją K1–K7, naprawione w tym samym mandacie, Zero-Debt):** `_get_graph_for_case` ZAWSZE zwracał `NetworkGraph(network_model_id=case_id)` — graf PUSTY niezależnie od treści przypadku — czyli oba endpointy P11 HTTP (`GET .../solver-input/{typ}`, `GET .../eligibility`) fabrykowały wejście KLASY P12 od początku, bez wiedzy dzwoniącego; ta ścieżka nie była nazwana w konstytucji jako defekt odrębny od P12, ale mieści się wprost w mandacie K5 („zero własnego składania równolegle do assemblera") i została naprawiona jako część tej samej karty, nie odłożona. Dosłowny „parytet BEFORE/AFTER" z K5 jest więc NIEWYKONALNY (BEFORE = zawsze zdegenerowany, nie punkt odniesienia) — zamiast tego dowiedziony: (a) `build_solver_input` niezmieniony (zero-diff), (b) payload sieci rejestru NIE jest zdegenerowany (`tests/golden/parytet_p11/test_parytet_p11.py::test_payload_nie_jest_zdegenerowany_pustym_grafem`), (c) payload PO przepięciu jest stabilny — pierwszy golden dla kontraktu P11 (`tests/golden/parytet_p11/`, harness reużywa `widok_parytetu`/`porownaj_wpis` z `parytet_assemblera` — NIE kopiuje, K5), 126 wpisów/3 sieci-warianty (load_flow, short_circuit_{3f,1f}) × rejestr, 17 odmów fizycznych/topologicznych krzyżowo zgodnych z `parytet_assemblera` dla tych samych sieci (te same przyczyny: dwa SLACK, Y0 osobliwa). `klucz: KluczTwin` dodany do obu endpointów P11 (404 dla przypadku spoza projektu — ten sam wzorzec co inne końcówki `{case_id}`). Pole addytywne K7: `WejscieRozplywu.audit2_applied` (ślad korekt audit2 na grafie, wypełniany PRZEZ assembler zamiast osobno przez wykonawcę) — `enm/assembler.py` zmieniony WYŁĄCZNIE addytywnie (nowe pole, warunkowe dołączenie w `canonical_analysis.py` wzorcem OLTC), zero zmiany semantyki istniejących pól; pełny `parytet_assemblera` (252/252) po zmianie bit w bit. **Zastany dług NIE naprawiony w tej karcie (poza mandatem K7 — konflikt z kartą równoległą A3-04 edytującą `enm/assembler.py`):** `_uow_factory_biezacy`/`_maybe_load_audit2_extensions` budują WŁASNE połączenie DB z `DATABASE_URL` (env), niezależne od `uow_factory` żądania wywołującego — ta sama klasa defektu, którą CV-3.3-B naprawiła dla `_execute_protection` (`uow_factory` addytywny parametr), tu NIE naprawiona (wymagałaby przewleczenia parametru przez sygnatury `zloz_wejscie_*`, nieaddytywne). Testy integracyjne obchodzą to WYŁĄCZNIE poprzez `monkeypatch.setenv("DATABASE_URL", ...)` dopasowujący do pliku, którego już używa `app_client` (`_align_audit2_db_env`), z jawnym komentarzem — do wpisania jako dług do naprawy osobną kartą. Guard `scripts/solver_input_assembler_guard.py`: zapadka ZASTANE 7→3 pliki / 31→16 konstrukcji (WYŁĄCZNIE `application/reference_networks/**`, CV-4.3); `solver_input/builder.py` → ALLOWLIST (ten sam wzorzec co `power_flow_gauss_seidel.py`: konstrukcja z grafu podanego przez assembler, nie z modelu). Guard `scripts/legacy_public_path_guard.py`: rozszerzony o `FORBIDDEN_CV42_FILES`/`FORBIDDEN_CV42_CLASS_NAMES`/`FORBIDDEN_CV42_FUNCTION_NAMES` (resurrekcja P2/P5/P12/P13 = czerwony). | Backend: 12/12 testów `test_solver_input_audit2_integration.py` (P12→kanon rewiring); `parytet_p11` 5/5 (nowy golden, 126 wpisów); `parytet_assemblera` 5/5 po polu addytywnym (252/252 bit w bit); pełna regresja `pytest tests/ -m "not pandapower"`. Frontend: `audit2-hooks`/`audit2-api`/`ProofSurfaceAudit2PowerFlow` testy + e2e `audit2-backend-integration.spec.ts` (test Q przepisany na bieg kanoniczny z realnym case/network przez domain-ops), tsc/eslint czyste. | `solver_input_assembler_guard` (zapadka 3/16, ZERO wzrostu); `solver_input_substitute_guard` (dług fizyczny bez wzrostu, martwy wpis P5 usunięty z zapadki); `legacy_public_path_guard` (5 nowych testów resurrekcji P2/P5/P12/P13); `api_lifecycle_guard`/`route_prefix_guard` (trasa P12 zdjęta) |
| CV-3.3-C | **DONE (2026-09-05)** — E4 `BatchExecutionService` przeszło z trzech słowników w pamięci (`_batches: dict[UUID, BatchJob]`, `_case_batches: dict[UUID, list[UUID]]`, `_pinned_hashes: dict[UUID, dict[UUID, str]]`) na trwały rejestr `run_batches` (nowa tabela SQL, ORM `RunBatchORM`, migracja `infrastructure/migrations/011_run_batches.sql`, repozytorium `RunBatchRepository` obok `CanonicalRunRepository` — reużyty `get_canonical_run_session_factory`, ta sama baza co `canonical_runs`). Domena przeniesiona `domain/batch_job.py` → `domain/run_batch.py`: `RunBatch`/`RunBatchItem` (frozen dataclass), status serii pięciowartościowy (CREATED/RUNNING/FINISHED/FAILED/PARTIAL — słownik dzielony z `CanonicalRun.status` zamiast osobnego PENDING/DONE, KLASA NIE INSTANCJA), `finalize_batch_status` wyprowadza status z pozycji (PARTIAL = mieszanka, nigdy cicho FINISHED). **Wykonanie CIĄGŁE** (zmiana zachowania wymuszona kartą §0 C2): poprzednia wersja zatrzymywała serię na PIERWSZEJ awarii (pozostałe pozycje NIGDY nie próbowane) — odtąd KAŻDA pozycja próbowana niezależnie w porządku `position`; `stop_on_failure` sprawdzone w kontrakcie API (nie istnieje) — nie dodano fantomu. `BatchExecutionService` BEZSTANOWY (wzorzec `FaultScenarioService`, C6-PERSIST) — singleton `_batch_service` w `api/batch_execution.py::get_batch_service` zostaje wyłącznie jako punkt wstrzyknięcia testów. Kontrakt HTTP (4 końcówki) zachowany bit w bit + pola addytywne: `finished_at`, `name` (kolumna schematu bez dostawcy UI w tej karcie — świadomie zawsze `null`), `envelope` (koperta rewizji Z CHWILI UTWORZENIA serii, budowana `enm.envelope.zbuduj_koperte` — TA SAMA funkcja co bieg pojedynczy, `scenario_ref=None`; seria NIE kopiuje tej migawki do pozycji — każda pozycja czyta model na własny rachunek przy `create_run`, jak pojedynczy bieg), `items[]` (position/scenario_id/analysis_type/options_hash/canonical_run_id/status/error_message + `result_freshness`/`result_freshness_reason`/`result_freshness_reason_pl` liczone NA ŻYWO z koperty biegu pozycji przy KAŻDYM odczycie — `application/result_freshness.swiezosc_biegu_kanonicznego`, ta sama funkcja co nakładka pojedynczego biegu — nigdy „zielona na zawsze"). Konsument FE `frontend/src/ui/study-cases/batchStore.ts` (już architektura cache'u odczytu bez własnego stanu trwałego — bez zmian) + `ui2/spaces/obliczenia/serie/**` (`types.ts::BatchStatus/BatchItem`, `serie/strings.ts`, `SeriePanel.tsx` — nowy słownik statusów, trójstanowe ogłoszenie po wykonaniu FINISHED/PARTIAL/FAILED). Testy: `backend/tests/test_batch_execution.py` (685 linii) przepisane 1:1 na repozytorium + 3 nowe klasy (`TestTrwaloscPoRestarcie` — symulowany restart procesu przez zniszczenie/odtworzenie silnika SQLAlchemy na bazie plikowej, `TestKopertaWspolna`, `TestSwiezoscPerPozycja` — dowód „nie zielone na zawsze" przez edycję modelu po wykonaniu) + wariant „wszystkie pozycje FAILED" (odróżniony od PARTIAL); e2e `frontend/e2e/critical-batch-flow.spec.ts` (nowy spec — `grep -l batch e2e/*.spec.ts` przed kartą: brak). **Znalezisko uboczne naprawione (Zero-Debt, zero konsumentów produkcyjnych zmierzone grepem):** `application/read_models/results_workspace_projection.py` (PR-22/PR-23, duckypował na `domain.batch_job.BatchJob`/`BatchJobStatus`, ZERO wołających w `src` poza własnym modułem) + 2 pliki testów (`test_results_workspace_projection.py`, `test_results_workspace_hash.py`) skasowane zamiast migrowane na nową domenę — martwy kod, nie funkcja do utrzymania. | 1 rejestr serii; seria przetrwa restart procesu (test `TestTrwaloscPoRestarcie`); zero częściowego sukcesu cichego (PARTIAL jawny) | `legacy_public_path_guard` rozszerzony (`domain.batch_job`, `BatchJob`/`BatchJobStatus`/`new_batch_job` — bramka wskrzeszenia); `solver_input_substitute_guard` CONTRACT_SOURCES `domain/batch_job.py` → `domain/run_batch.py`; `scenario_copy_guard` bez zmian zapadki (seria woła WYŁĄCZNIE `create_run`/`execute_run`, zero konstrukcji `CanonicalRun` poza fabryką, zero kopii migawki) |
| CV-3.3-C | **NIE ZROBIONE — preautoryzowany podział przy CV-3.3-B (2026-09-05), z inwentarzem (nie „później" bez uzasadnienia).** E4 `BatchExecutionService` → trwały rejestr `run_batches`. **Inwentarz klasy** (wymagany przed naprawą wg reguły KLASA NIE INSTANCJA, `CLAUDE.md`): stan procesowy do przeniesienia — `_batches: dict[UUID, BatchJob]`, `_case_batches: dict[UUID, list[UUID]]`, `_pinned_hashes: dict[UUID, dict[UUID, str]]` (`application/batch_execution_service.py`, singleton `_batch_service` w `api/batch_execution.py::get_batch_service`); 4 końcówki REST (`POST .../batches`, `POST .../batches/{id}/execute`, `GET .../batches`, `GET .../batches/{id}`); konsument FE `frontend/src/ui/study-cases/batchStore.ts` (mapuje 1:1 na REST, ZERO wiedzy o mechanizmie trwałości — repointing bez zmian FE, jeśli kontrakt HTTP zachowany bit w bit); testy istniejące `tests/test_batch_execution.py` (685 linii) do przepisania na tor trwały; ZERO istniejącej infrastruktury `run_batches` w repo (potwierdzone grepem — od zera: ORM, repozytorium, migracja). Rozmiar porównywalny z pełnym zakresem B1–B3 karty CV-3.3-B (przepięcie 3 serwisów porównań + kasacja R2/R3 procedurą 7 kroków + ~15 plików testowych) — realizacja W TEJ karcie zagroziłaby jakością (weryfikacja end-to-end reszty B1–B3, guardy, OpenAPI, dokumentacja) bez proporcjonalnej korzyści dla rdzenia B1 (porównania), który E4 nie dotyczy. | 1 rejestr serii; seria przetrwa restart procesu | guard: `_batches`/`_case_batches`/`_pinned_hashes` jako JEDYNY magazyn stanu serii (bez odpowiednika SQL) = czerwony |
| CV-4.1b | **wykonana i odebrana (agent, 2026-09-05 → cherry-pick `c5dc110f`/`3a7cd8fd` + domknięcie `dd28a5e8`/`396c397d`: walidator bramki profilem NC RfG `generators.voltage_control_profile_missing`/`..._not_permitted` + kreator OZE bez cichego resetu trybu; domyka A3-04 z CV-4.1).** Defekt: generator z regulacją napięcia (AVR synchroniczny albo falownik w trybie `voltage_control`) był ZAWSZE liczony jako węzeł PQ — `pv_bus_ids=[]` niezależnie od danych sieci, bo żadne ogniwo łańcucha ENM→walidator→IR→assembler→kreator nie niosło nastawy napięcia jako danej wejściowej. Inwentarz KLASY (wszystkie miejsca, gdzie nastawa jest czytana/pisana/walidowana/wyświetlana): (1) `enm/models.py::Generator.meta` (dict, pole wolnej postaci — reużyte, bez nowego formalnego pola pydantic, wzorem `cos_phi`/`qu_slope_pu_per_pu`) — magazyn `control_mode`/`u_set_pu`; (2) `enm/validator.py` — nowa reguła `generators.voltage_control_incomplete` (BLOCKER: brak `u_set_pu` w [0,9; 1,1] pu albo brak `q_min_mvar<q_max_mvar`), DOWOLNY `gen_type` (nie tylko DER, w odróżnieniu od sąsiednich reguł E028/E029); (3) `domain/canonical_operations.py::READINESS_CODES["generator.voltage_setpoint_missing"]` — kod kanonu z `fix_navigation`; (4) `domain/readiness_bridge.py::ODWZOROWANIE_WALIDATOR_NA_KANON` — mostek walidator→kanon; (5) `enm/mapping.py::map_enm_to_network_graph` — buduje `NodeType.PV` (`voltage_magnitude=u_set_pu`) zamiast PQ; odmowa: dwa generatory regulujące na jednej szynie, szyna SLACK jako PV (mirror `_build_converter_control_by_node`, „jedna charakterystyka na węzeł"); (6) `enm/assembler.py::zloz_wejscie_rozplywu` — buduje `PVSpec(node_id, p_mw, u_pu, q_min_mvar, q_max_mvar)` z granic Q migawki dla węzłów PV (PQSpec pomija węzły PV); ZNALEZISKO przy wdrożeniu (KLASA NIE INSTANCJA): węzeł PV z DODATKOWĄ regulacją falownika (cosφ/Q(U)) innego generatora na tej samej szynie — kombinacja nowo możliwa wyłącznie dzięki temu wycinku — jawna odmowa, nie ciche pominięcie; (7) `enm/canonical_analysis.py::_execute_power_flow` — `power_flow_trace.pv_bus_ids` z `pf_input.pv` (było: zawsze `[]`); (8) `enm/domain_operations_v2.py::_resolve_converter_defaults` — pisze `u_set_pu` we WSZYSTKICH 3 gałęziach technologii (PV/BESS/wiatr); PRZY OKAZJI naprawione DWA pre-existing bugi TEJ SAMEJ klasy w gałęzi BESS (PV/wiatr niosły oba, BESS żadnego): (a) brak `control_mode` w ogóle — regulacja Q kreatora OZE dla BESS była phantomem (kontrolka UI bez skutku w domenie); (b) ZNALEZISKO DRUGIE, przy weryfikacji Q-granic REGULACJA_NAPIECIA (KLASA NIE INSTANCJA §5 — przegląd całego modułu, nie tylko miejsca z audytu): brak `q_min_mvar`/`q_max_mvar` w `meta` — BESS w trybie REGULACJA_NAPIECIA miałby granice Q wypełnione w UI, zero w modelu, więc walidator ENM blokowałby bieg mimo poprawnie wypełnionego formularza (`generator.limits.q_min_mvar`, zapisywany osobną operacją `update_element_parameters` sekwencji K9-A, to ODDZIELNY magazyn — nie ten, który czyta walidator/assembler); (9) `enm/domain_ops_models.py::AddConverterSourcePayload.u_set_pu` — pole payloadu operacji domenowej; (10)-(13) frontend: `ui2/kreatory/zrodlo-oze/{zrodloOzeModel.ts,strings.ts,WykresyNcRfg.tsx,KreatorZrodlaOze.tsx}` — opcja „Regulacja napięcia (U = const)" widoczna WYŁĄCZNIE gdy profil operatora NC RfG (`voltage_control_modes`) ją dopuszcza (gating przez `fetchNcRfgOperators()`+`useState`/`useEffect`, wzorem sąsiedniego `KrokZgodnosc` — NIE hook React Query, bo kreator nie ma przodka `QueryClientProvider`), pole liczbowe U-setpoint, walidacja lustrzana wobec walidatora ENM, zapis przez tę samą operację domenową co pozostałe tryby; `ui/sld/shared/detailDrawerData.ts` + `ui/sld/v2/canvas/SldDetailDrawer.tsx` — odczyt trybu+nastawy w liście DER stacji (generyczny, wszystkie 4 tryby, nie tylko nowy). Pomiar na rejestrze referencyjnym (`tests/golden/registry.py`, 17 wpisów): PRZED 0/17 wpisów miało zmaterializowaną sieć z generatorem w regulacji napięcia (G06 istniał jako wpis szkieletowy, `budowniczowie=()`); PO 1/17 (G06, 2 warianty: `build_gn06_pv_regulacja_napiecia` nastawa osiągalna / `_nasycenie` poza zasięgiem Q, oba na topologii `build_gn04_sn_nn_oze` — KLASA NIE INSTANCJA, jeden budowniczy topologii). Złote hashe parytetu assemblera (baza `40e49c22` — koordynator skorygował bazę dwukrotnie w trakcie karty, `5793a3e0`→`158d9831`→`40e49c22`, przyczyna: CI wykazało, że ŻADNA siatka kwantyzacji surowego wyniku nie jest przenośna międzymaszynowo, docelowo szkielet maskuje też napisy śladu White Box `"<s>"` i skróty SHA-256 `proof_ref`/`proof_binding` `"<sha256>"`, ATOL 1e-6 jednostki kontraktu; widok tolerancyjny `porownaj_wpis`: szkielet dokładnie, liczby kontraktu `\|a−b\|≤1e-6+2e-6·\|a\|`): 252→264 wpisów, +12 WYŁĄCZNIE G06 (2 warianty × {PF, SC_1F/2F/2FG/3F_max, SC_3F_min}), 0 rozbieżności ponad tolerancję w 252 wspólnych wpisach (rachunek powtórzony PO obu znaleziskach BESS z pkt (8) — potwierdzone: `meta.q_min_mvar/q_max_mvar`/`u_set_pu` dodane jako `None` nie zmieniają `raw_result` solvera, `.get()` widzi klucz nieobecny i klucz `None` identycznie — G00/G04/G05 z generatorem BESS bez rozbieżności), 0 usuniętych. Dowód semantyczny niezależną wyrocznią: `tests/application/reference_networks/test_pandapower_cross_validation.py::TestPvNodeCrossValidatePandapower` (pandapower `create_gen(vm_pu=...)`; marker `pandapower`, nieuruchamialny w tym venv z przyczyn środowiskowych — zweryfikowany niezależnie stroną kanoniczną: moduł U węzła PV = 1,02 pu dokładnie, `pv_to_pq_switches=[]`, Q≈5,72 Mvar w granicach ±10 Mvar). Solver FROZEN (B-01) nietknięty — cała naprawa w ENM/IR/assembler/kreator. ZNALEZISKO TRZECIE (pełna regresja backendu): `tests/application/test_companions_generated.py::test_fixtura_sld_network_53_jest_bajtowo_rowna_generatorowi` czerwony — fikstura GENERATED `sldNetwork53.ts` (substrat 52 stacji, rejestr G00, generator BESS materializowany przez `add_converter_source`) niosła `source_hash` sprzed dodania pól `u_set_pu`/`q_min_mvar`/`q_max_mvar` do `meta`; naprawa SANKCJONOWANA regeneracją (`scripts/emit_sld_network_fixture.py`, wskazana przez sam test) — diff DOKŁADNIE 1 linia (`source_hash`), reszta layoutu 53 stacji bit w bit. | 252 wspólne wpisy 0 rozbieżności (`porownaj_wpis`, tolerancja), +12 G06 uzasadnione (jedyna sieć z generatorem regulującym napięcie w rejestrze); `tests/enm/test_enm_validator.py::TestGeneratorVoltageControlIncomplete` (iloczyn cech, 11 przypadków); `tests/enm/test_enm_mapping.py::TestGeneratorVoltageControlPV` (5 testów: PV, inny tryb→PQ, dwa generatory→odmowa, SLACK→odmowa, nastawa niekompletna→odmowa); `tests/enm/test_generator_voltage_control_e2e.py` (6 testów assembler+bieg kanoniczny, w tym nasycenie Q→przełączenie PV→PQ); `tests/application/test_companions_generated.py` 4/4 po regeneracji fikstury | `solver_input_assembler_guard` bez nowych wpisów (PVSpec wyłącznie w assemblerze); `solver_input_substitute_guard` zapadka `enm/assembler.py:A:or:node.active_power` 1→2 (ten sam uzasadniony dług co istniejąca gałąź PQSpec, `reactive_power` bez zmian) + self-test zaktualizowany; `readiness_codes_guard`/`readiness_consumption_guard` zielone (nowy kod kanonu ma emitera); `mypy_ratchet_guard` Success (0 błędów, próg 0) |
| CV-4.2b | **wykonana (Fable osobiście, 2026-09-05, worktree `fable-cv4`; odbiór = pełna regresja `tests/` + guardy CI z lintem + snapshot OpenAPI, wynik w `../evidence/CONVERGENCE_EVIDENCE.md` §E/§F).** Klasa „odczyt stanu bazy WŁASNYM silnikiem/sesją z `DATABASE_URL` zamiast fabryki `UnitOfWork` wołającego" + podklasa „zapytanie ORM `StationAudit2ConfigORM` poza repozytorium" (ta sama klasa, którą CV-3.3-B naprawiła dla `_execute_protection` — wtedy INSTANCJA, teraz KLASA). Inwentarz (pomiar): własny silnik — `enm/assembler.py::_uow_factory_biezacy` (użycie `_maybe_load_audit2_extensions`) i zapas w `canonical_analysis._execute_protection`; wejścia wykonania BEZ fabryki mimo jej zasięgu — `api/execution_runs.execute_run` (końcówka ogólna, `app.state`), `api/power_flow_runs.execute_power_flow_run`, `api/enm.run_short_circuit`/`run_power_flow` → `run_*_now`, `api/batch_execution.execute_batch` → `BatchExecutionService`, warianty w pamięci `hosting_capacity`/`pq_area`/`kontyngencje_n1` (opcje dziedziczone z biegu bazowego, więc para audytu 2 też) i `protection_settings/batch_run` (+ `pakiet_nastaw`; tu ROZBIEŻNOŚĆ: warianty nastaw liczyły model BEZ korekt audytu 2 kotwicy — naprawione, para dziedziczona); zapytania ORM poza persystencją — `api/audit2_station_config.py` ×5 (list/get/upsert/delete/apply) + `_aggregate_loads_per_station_for_project` (ProjectORM), `api/solver_input.py` (drugi odczyt tej samej konfiguracji obok assemblera), `api/enm.py::_bay_device_withstand`, assembler; `catalog_governance/service.py` (SwitchEquipmentAssignmentORM, „repozytorium tego nie wystawia"). Naprawa: (1) repozytorium `UnitOfWork.audit2_station_configs` (get/list_for_project/upsert z zachowaniem `id`/delete) + `NetworkWizardRepository.list_switch_equipment_assignments` + `ProjectRepository.get_orm` w routerze; (2) assembler BEZ bazy — `zloz_wejscie_rozplywu`/`zloz_wejscie_zwarcia` przyjmują `rozszerzenia_audit2` jako dane; (3) JEDEN odczyt dla wykonawców `canonical_analysis.rozszerzenia_audit2_dla_opcji(options, uow_factory)` (brak pary → `None` bez dotykania bazy; połowa pary/zły UUID/brak fabryki → jawny `ValueError`, bieg FAILED z powodem; para bez zapisu → `None`) i JEDEN konwerter `solver_input.audit2_der_payload.rozszerzenia_audit2_z_konfiguracji` (bieg kanoniczny, P11, diagnostyka `_apply-to-network-model`); (4) fabryka wołającego przewleczona przez KAŻDE wejście: `execute_run`/`run_*_now`/`wykonaj_bieg_w_pamieci` → `_wykonaj_analize_biegu` (zdolność kontekstu, nie dane biegu — przyjmowana dla każdego typu, podawana PF/SC/zabezpieczeniom) → wykonawcy; końcówki i serwisy wyżej podają ją jawnie (`Depends(get_uow_factory)`/`app.state.uow_factory`); (5) zapas `_uow_factory_biezacy` skasowany także w zabezpieczeniach (bieg bez fabryki = FAILED z powodem); (6) bramka wskrzeszenia `legacy_public_path_guard::FORBIDDEN_CV42B_FUNCTION_NAMES` + test własny; (7) `guardy_z_ci.py` dostał TRZECIĄ część kroku CI — lint jak CI (black/ruff `src tests`, black `--config pyproject.toml ../scripts`, ruff `../scripts`) + test własny, bo runy 4879/4881 były czerwone na `black --check ../scripts` przy „KOMPLET ZIELONY" bramki. Testy klasy (iloczyn cech): {brak pary, połowa pary, zły UUID, brak fabryki, para bez zapisu, para z zapisem} × {PF, SC} (`tests/enm/test_canonical_analysis_audit2.py`), warianty w pamięci × {bez fabryki → błąd, fabryka bez zapisu → bieg, fabryka trafia do odczytu} (`test_wykonaj_bieg_w_pamieci.py`), warianty nastaw dziedziczą parę + odmowa bez fabryki (`test_protection_settings_batch_run.py`), HTTP: PF/SC z parą przez końcówkę ogólną BEZ wyrównywania `DATABASE_URL` (maskująca pomoc `_align_audit2_db_env` skasowana — testy przechodzą tylko przy jednej bazie), połowa pary → FAILED z powodem, bieg zabezpieczeń przez końcówkę ogólną → DONE, `execute_run` bez fabryki → FAILED z powodem, biegi natychmiastowe `api/enm` dostają `app.state.uow_factory`, seria podaje fabrykę każdemu biegowi pozycji; repozytorium (get/list/upsert-id/delete/rozszerzenia); źródło assemblera bez `session`/`DATABASE_URL`/`create_engine` (AST bez docstringów). Poza klasą świadomie (merytorycznie, nie „poza zakresem"): `canonical_run_repository` (R1) ma własny silnik z `DATABASE_URL`, ale jest jedynym czytelnikiem i pisarzem `canonical_runs` — brak drugiego uchwytu do tej samej tabeli; unifikacja pod wspólną fabrykę to decyzja rejestru CV-2. Snapshot OpenAPI bez zmian (opis końcówki wykonania jako komentarz, nie docstring; `Depends`/`Request` nie zmieniają kontraktu). |
| CV-4.3 K3 | **wykonana (Fable osobiście, 2026-09-05, worktree `fable-cv4`; odbiór = pełna regresja `tests/` + guardy CI + parytet assemblera, wynik w `../evidence/CONVERGENCE_EVIDENCE.md` §E/§F).** `TopologyService` = konsolidacja (konstytucja C.2.2): jądro `network_model/core/topologia.py` (leaf; składowe spójne, union-find z reprezentantem = min klasy — port 1:1 z `ybus.py`, przegląd wszerz z rodzicem, wspólnym zbiorem odwiedzonych i wariantem z wielu korzeni naraz, cykle, ścieżki) + serwis `enm/topology.py::derive(snapshot) -> TopologyView` (węzły topologiczne, wyspy ze źródłami odniesienia, sekcje, punkty otwarte, krawędzie pominięte). Inwentarz KLASY (pomiar grepem `deque(`/`pop(0)`/`nx.*`/union-find w `backend/src`): 14 miejsc → 12 przepiętych na jądro (`graph.py`, `ybus.py`, walidator E003+E060, `topology_ops` ×3, `_identify_trunk`, energizacja nN ×2, `graph_view`, `fault_loop/route`, `czas_wylaczenia_galezi`, `segment_decomposition`, `sld/layout`), 2 zostają w `reference_networks` (kasacja K2, zapadka guarda), 2 w rdzeniu solvera FROZEN (allowlista z powodem B-01). Guard `topology_single_impl_guard` + test własny + krok P0. Parytet z 3 wyroczniami zastanymi i `networkx` na całym rejestrze; złote hashe assemblera 264 bez zmian (macierz Y bit w bit). Znalezisko przy przepięciu (fikstury JSON scenariuszy nN): ekspansja domeny z WIELU nasion naraz (głębokość od najbliższego) ≠ kolejne przeglądy ze wspólnym zbiorem — obie semantyki nazwane w jądrze (`przeglad_wszerz_od` vs `przeglad_wszerz(..., odwiedzone=)`) i przypięte testem. Pomiar A3-05: 4 sieci rejestru odmawiane (2 źródła sieciowe; 2× osobne wyspy → wykonawca per wyspa, 2× jedna wyspa → B-01/OD-7). Pozostałe podkarty CV-4.3: K1 benchmarki jako ENM, K2 kasacja `reference_networks`, K4 `backend_no_physics_guard`, K5 trasy E2 — po inwentarzu agenta |
| CV-4.3 K3b | **wykonana (Fable osobiście, 2026-09-06, worktree `fable-cv4`; odbiór = pełna regresja `tests/` + guardy CI + parytety złote + wyrocznia pandapower w izolowanym venv, wynik w `../evidence/CONVERGENCE_EVIDENCE.md` §E/§F/§G).** A3-05: rozpływ PER WYSPA (IR przyjmuje SLACK per źródło; assembler buduje z `TopologyView` po jednym `PowerFlowInput` na wyspę zasiloną, wykonawca scala `enm/rozplyw_wysp.py`); dwa źródła w jednej wyspie = odmowa NAZWANA `source.multiple_grid_sources_in_island` (assembler + bramka gotowości, jeden predykat) — polityka rozdziału mocy między GPZ w jednej wyspie zostaje decyzją OD-7; zwarcie: wyspy pływające poza grafem solvera (8 wpisów „Y-bus singular" → wyniki), dwa GPZ w jednej wyspie superpozycją Y_Q (wyrocznia pandapower ≤ 10⁻⁴). Inwentarz KLASY: 4 sieci rejestru × 6 wariantów odmawiane w IR + 8 osobliwych + 42 wiersze z szumem + 4 testy nN i 2 fikstury FE pinujące odmowę — wszystkie domknięte; złote hashe 264 (30 odmów → wyniki, 2 nazwane, 42 bez zmiany liczb zasilanych, 190 bit w bit), P11 132 (12 zmienionych). Testy iloczynu cech `tests/enm/test_rozplyw_per_wyspa.py` (12), wyrocznia `tests/golden/wyrocznie` (6, marker `pandapower`; job CI czyta cały `tests`). Pozostałe podkarty CV-4.3: K1/K4/K5 u agentów, K2 po K1 |
| CV-4.3 K4 | **wykonana (2026-09-06, worktree agenta, karta `karta_cv43_a3.md`; odbiór = pełna regresja `tests/ -m "not pandapower"` + guardy CI, wynik w `../evidence/CONVERGENCE_EVIDENCE.md` §E/§F).** `backend_no_physics_guard` z pustą allowlistą: nowy, ADDYTYWNY podpakiet `network_model/pochodne/wielkosci_pochodne.py` (15 czystych funkcji + stałe `SQRT3`/`SQRT2`, zero zależności poza `math` — liść grafu importów) staje się JEDYNYM recenzowanym miejscem dla formuł algebraicznych poza rdzeniami solverów (√3 — napięcie fazowe/prąd z mocy pozornej/zwarciowej; κ IEC 60909 — WYŁĄCZNIE narracja podstawienia dowodu, K4.2; całka Joule'a I²t; korekta temperaturowa IEC 60909-0; P/(√3·U·cosφ) i S=P/cosφ; impedancja/moc bazowa Z=U²/S). Inwentarz KLASY (pomiar AST, nie grep — inwentarz ręczny `scratchpad/inwentarz_cv43.md` D.2-D.9 uzupełniony odkryciami z głębszego czytania i AST-em, bo narzędzie mechaniczne pierwotnego pomiaru gubiło trafienia przez nawiasy i aliasy stałych): 59 miejsc w 31 plikach przepiętych (pomiar końcowy z `git diff --stat` — poprzedni szacunek ~65/30 skorygowany po ostatnim, wyczerpującym grepie rodzin, który doszukał 1 pominięte miejsce; m.in. `network_model/core/{machine,generator,branch,ybus}.py` — import NA POZIOMIE MODUŁU, bez odroczenia: `pochodne/` leży SIOSTRZANO wobec `core/` i `solvers/` (relokacja architekta, w locie tej samej karty — pierwsza wersja umieściła `pochodne/` pod `solvers/pochodne/`, co WYMUSZAŁO odroczenie importu do wnętrza funkcji w tych 4 plikach, bo `network_model/solvers/__init__.py` FROZEN gorliwie importuje solvery zależne od `network_model.core.graph.NetworkGraph`, a te 4 pliki są importowane W TRAKCIE inicjalizacji `graph.py` — cykl; jako siostra, `pochodne/` importuje WYŁĄCZNIE `math`, nie zależy od `core` ani nie jest importowana przez `network_model/__init__.py` — prawdziwy liść grafu importów, bez cyklu niezależnie od momentu importu); `enm/{canonical_analysis,der_sn_validation,validator,mapping,zero_sequence_transformer,catalog_completion,domain_operations,domain_operations_v2}.py`; `application/proof_engine/proof_generator.py` — 12 miejsc, w tym κ/√3/√2 narracyjne (K4.2: Result ZAWSZE ze śladu solvera, nigdy z lokalnego przeliczenia); `application/equipment_proof/generator.py` — I²t, kryterium PASS/FAIL zostaje (K4.3, zgoda właściciela Karta S-C); `application/solvers/lv_temperature_correction.py`; `infrastructure/cgmes/cgmes_exporter.py`; `solver_input/v126_contracts.py`; i inne). Test tożsamości bit w bit (`backend/tests/network_model/pochodne/test_wielkosci_pochodne.py`, 1058 testów) na wejściach z rejestru sieci (`Bus.voltage_kv`, `Source.sk3_mva`) i wartościach brzegowych — funkcje matematycznie równoważne, ale ze ZMIERZONĄ różną kolejnością skalowania jednostek (×1000 vs ×1e6/×1e3) zostają OSOBNYMI funkcjami (IEEE 754: mnożenie przemienne, NIE łączne). Guard `scripts/backend_no_physics_guard.py` (AST, 5 rodzin, wzorzec `topology_single_impl_guard`) + 39 testów własnych + krok w `p0-extended-guards.yml`; zapadka ZASTANE WYŁĄCZNIE `application/reference_networks/computation.py` (własny Newton-Raphson, dług już nazwany CV-4.3 A.2, pakiet kasowany w karcie A2). Poza kartą (dokumentowane, NIE naprawiane — formuły wieloczłonowe/inne normy, poza 6 rodzin karty): `network_model/core/machine.py` (K_G, X″d — IEC 60909 §6.3/§6.7 wieloczłonowe), `infrastructure/cgmes/cgmes_exporter.py::_emit_transformer` (Z/R transformatora z uk%/Pk), `analysis/arc_flash/builder.py` (IEEE 1584 regresje wielomianowe), `analysis/lf_sensitivity/builder.py` (stosunek wrażliwości napięciowej), `application/analyses/dobor_kompensacji.py` (model Q=Q_rated·V² kondensatora, samo-oznaczony „NIE fizyka pola" w komentarzu) |
| CV-3.3-A | **DONE (2026-09-05)** — E3 (`ExecutionEngineService`, zero konsumenta produkcyjnego) skasowany; E2-widmo `unified_runs`+wyłączny dyspozytor `analysis_dispatch` skasowane (`/api/runs/*` zdjęte, zweryfikowana wyłączność konsumenta grepem — 0 innych); R2 podmoduły martwe (`export_service`, `results_inspector`, `orchestrator`, `catalog_context`, `dtos` — 1618 linii, zero konsumentów poza własnymi testami) skasowane. Testy asertujące fizykę (zbieżność, znak mocy, prądy zwarciowe, kontrakt ResultSetV1) przepisane na tor kanoniczny na tej samej sieci (`tests/enm/test_load_flow_migracja_e3_*.py`, `tests/enm/test_short_circuit_migracja_e3_golden.py`); testy mechaniki silnika usunięte. **Konsumenci ukryci poza pierwotnym inwentarzem karty** (znalezione dopiero pełnym grepem importów po pierwszym pełnym pytest — inwentarz kart pisanych przed CV-3-W/C6-PERSIST bywa nieaktualny, zgodnie z zastrzeżeniem zlecenia): (1) `tests/test_pr19_fault_scenario.py` (plik z granicy C6, „nie dotykać") — klasa `TestExecutionEngineScenarioIntegration` (5 testów SC3F/SC2F/SC1F/determinizm/3F>2F przez `ExecutionEngineService.execute_run_by_scenario`) i jej prywatne pomocniki (`_create_golden_graph`, `_create_engine_with_case`) skasowane chirurgicznie — reszta pliku (53 testy domeny/API/hash scenariusza C6) nietknięta; fizyka ma równoważny dowód na torze kanonicznym w `tests/test_fault_scenarios_run_integration.py` (`create_run_from_scenario`, CV-1-W/C6-PERSIST) i w nowym `tests/enm/test_short_circuit_migracja_e3_golden.py` tej karty. (2) `tests/test_fault_scenarios_run_integration.py` (też granica C6) — fikstura `_reset_services` wołała `api.execution_runs.get_engine()` (usunięty §0.2 tej karty); zamieniona na sam `reset_canonical_runs()`, którego ten plik i tak jedynie realnie potrzebuje (tor testu od zawsze idzie przez `enm.canonical_analysis`). Uzasadnienie dotknięcia plików „nie dotykać": granica chroni WŁASNĄ logikę/fizykę C6 (nietknięta w 100%), nie zezwala na pozostawienie w nich martwego importu, który mój wykasowany moduł zostawiłby jako błąd kolekcji całego pliku — zero-dług i grep-zero z definicji ukończenia obejmują też te dwa pliki. Snapshot OpenAPI 318→315 ścieżek (dokładnie `/api/runs/*`). E4 (`BatchExecutionService`→orkiestrator) i R2 tabela żywa/R3/R4 — **POZA TĄ KARTĄ** (CV-3.3-B). **Znalezisko poza kartą** (skutek uboczny kasacji E3/E2-widmo, zero konsumenta produkcyjnego dziś): `application/result_mapping/{sc_binding_meta.py, short_circuit_to_resultset_v1.py::map_short_circuit_to_resultset_v1, load_flow_to_resultset_v1.py::map_power_flow_to_resultset_v1, protection_to_resultset_v1.py::map_protection_to_resultset_v1, protection_to_overlay_v1.py::map_protection_to_overlay_v1}` — E3 był ich jedynym wołającym w `src/`; `short_circuit_to_resultset_v1.py`/`protection_to_resultset_v1.py` dzielą prywatne `_build_element_results`/`_build_global_results`. Ta sama klasa: `domain/analysis_kind.py::AnalysisKind` (`SHORT_CIRCUIT/POWER_FLOW/PROTECTION/FAULT_LOOP_NN/SWZ_NN`) — zbudowany wyłącznie „to unify dispatch" dla `AnalysisDispatchService` (E2-widmo, skasowany), zero konsumentów w `src/` i `tests/` po kasacji. Domknięte kartą **CV-3.3-A2** (poniżej) |
| CV-3.3-A2 | **DONE (2026-09-05)** — domknięcie klastra osieroconego `application/result_mapping/**` + `domain/analysis_kind.py::AnalysisKind` znalezionego przy CV-3.3-A, na polecenie koordynatora (odbiór CV-3.3-A pkt 6.2, NIC NA POTEM). Inwentarz zweryfikowany grepem KAŻDEGO modułu z osobna w `src/`, `tests/`, `scripts/` (guardy z allowlistami/zapadkami), `docs/` (poza archiwum) — nie przyjęty z pierwotnej listy karty macierzystej. **DECYZJA PER MODUŁ (nie jedna decyzja na cały klaster — dokładnie ta sytuacja, przed którą ostrzega §0 karty macierzystej):** (1) `AnalysisKind` — **USUNIĘTY**: zero konsumentów w `src/`/`tests/`/`scripts/`/`docs/` poza sobą, zero testów własnych, docstring sam przyznaje jedyny cel („Used by AnalysisDispatchService"). (2) `load_flow_to_resultset_v1.py::map_power_flow_to_resultset_v1` (→ lokalny `LoadFlowResultSetV1`, NIE `domain.result_contract_v1.ResultSetV1`) — **USUNIĘTY** razem z dedykowanym `tests/test_load_flow_resultset.py` (158 linii, czysta mechanika mapowania na syntetycznej fikstury, zero fizyki, zero kontraktu zewnętrznego): zero konsumenta, zero ochrony guardem; tor kanoniczny ma WŁASNEGO, ujednoliconego producenta ResultSetV1 dla WSZYSTKICH typów analiz (patrz niżej). (3) `protection_to_overlay_v1.py::map_protection_to_overlay_v1` (→ `OverlayPayloadV1`) — **USUNIĘTY** razem z dedykowanym `tests/test_protection_overlay_v1.py` (572 linii, 8 klas, każda woła ten mapper) + chirurgicznie z `tests/test_protection_determinism_guards.py` (klasa `TestOverlayDeterminism` skasowana, krok nakładki wycięty z `TestEndToEndPipeline` — determinizm silnika/koordynacji/raportu zostaje dowiedziony bez niej): **pomiar źródłowy** — żywa końcówka `GET .../sld/{diagram_id}/protection-overlay` w `api/protection_runs.py` buduje nakładkę WŁASNYM, prostszym mechanizmem inline (dict ręczny z `trip_state`/`t_trip_s`/`margin_percent`), NIE przez ten mapper; żywa końcówka ma własną regresję (`tests/api/test_protection_overlay_swiezosc.py`) niezależną od skasowanego pliku — zero utraconej ochrony produktu. (4) **`sc_binding_meta.py`, `short_circuit_to_resultset_v1.py`, `protection_to_resultset_v1.py` — STOP, NIE USUNIĘTE (B-01).** Pomiar odkrył, że `scripts/resultset_v1_schema_guard.py::PROTECTED_FILES` jawnie zamraża `short_circuit_to_resultset_v1.py` i `protection_to_resultset_v1.py` („Kontrakt SC/Protection ResultSet v1 jest zamrożony i NIE MOŻE być modyfikowany") — kasacja pliku to modyfikacja tego kontraktu, czyli edycja zamrożonego rdzenia bez zgody właściciela (B-01, jedno z 3 dozwolonych zatrzymań w `CLAUDE.md`), niezależnie od tego, że oba pliki mają dziś zero konsumenta produkcyjnego w `src/` (potwierdzone grepem: jedyne trafienia poza plikiem to KOMENTARZE, nie importy — `enm/canonical_analysis.py:1484`, `domain/protection_current_source.py:85`). `sc_binding_meta.py` (poza tą listą guarda) zostaje razem z nimi z tego samego powodu: tworzy JEDNĄ testowaną parę z zamrożonym `short_circuit_to_resultset_v1.py` w `tests/test_pr18_sc_integration.py::TestResultMapper` (nietknięta, zostaje w całości) — rozdzielenie pary rozstrzygałoby za właściciela, którą połowę zamrożonego kontraktu wolno ruszyć. Dopisana notatka w nagłówku `resultset_v1_schema_guard.py` tłumacząca, dlaczego guard zostaje zielony mimo zera importerów — żeby ta sama konfuzja nie wróciła w kolejnej karcie. **USTALENIE PRODUCENTA KANONICZNEGO (pkt 2 polecenia):** `application/result_mapping/canonical_run_to_resultset_v1.py::build_resultset_v1_from_canonical_run` — JEDYNE miejsce składające `domain.result_contract_v1.ResultSetV1` (prawdziwy, zewnętrznie zwalidowany kontrakt „ResultSet v1", odrębny od `domain.execution.ResultSet`, do którego mapują zamrożone pliki SC/Protection) z `CanonicalRun`, analiz-typ-agnostyczny (obsługuje SC/PF/Protection JEDNĄ funkcją); żywi konsumenci: `api/result_contract_v1.py:46`, `application/analyses/lv_domain/projection_v1.py:151`. Kasacja `load_flow_to_resultset_v1.py`/`protection_to_overlay_v1.py` nie zostawia więc żadnej analizy bez producenta. Guardy: `legacy_public_path_guard.py` +3 wpisy importu (`application.result_mapping.load_flow_to_resultset_v1`, `.protection_to_overlay_v1`, `domain.analysis_kind`) + 4 wpisy nazw (`LoadFlowResultSetV1`, `map_power_flow_to_resultset_v1`, `map_protection_to_overlay_v1`, `AnalysisKind`) + 1 nowy test własny (cały klaster jedną syntetyczną trasą — analogicznie do CV-3.3-A); `protection_no_heuristics_guard.py` SCAN_FILES -1 (protection_to_overlay_v1.py, guard degraduje się łagodnie na brakujący plik — potwierdzone czytaniem `_scan_file`, nie założeniem); `no_direct_fault_params_guard.py` bez zmian (jego wpis `WHITELISTED_PATHS` wskazuje WYŁĄCZNIE zamrożony, nieusunięty `short_circuit_to_resultset_v1.py`). **Znalezisko przy okazji (pre-existing od CV-3.3-A, ten guard nie był w jej Definition of Done):** `scripts/solver_input_substitute_guard.py::ZASTANE_ZASTEPNIKI` niósł martwy wpis `application/analysis_run/results_inspector.py` (1 pole) od kasacji R2 w CV-3.3-A — usunięty teraz; `scripts/test_solver_input_substitute_guard.py`'s pin (3652 pól/602 plików/66×346 dług) był NIEPRZELICZONY od CV-3.3-A — przeliczony na zmierzony stan obu kart razem (3619 pól/586 plików/65×345 dług — pełne wyprowadzenie liczb w komentarzu przy asercji). Snapshot OpenAPI: BEZ ZMIAN (315 ścieżek/225 schematów, plik po regeneracji bajt w bajt identyczny z HEAD sprzed karty — żaden z 3 skasowanych modułów nigdy nie miał własnej trasy `/api` ani nie zasilał response_model żywej trasy) |

## 4. Kolejność dalsza (po CV-3)
CV-4 (jeden assembler, `TopologyService`, kasacja P2–P12/S2–S7 i dialektu benchmarków, legacy ORM procedurą) → CV-5 (terminale, fazy, uziemienie; `meta.field_specs` → `Bay`) → CV-6 (G01 end-to-end) → ownership zabezpieczeń (ADR-022, `ProtectionCapabilityRegistry`) → SLD jako projekcja TV (program SLD, B-02) → kasacje legacy z guardami → dokumentacja (archive-first, manifest supersesji).

## 5. Raportowanie
Po każdym wycinku: raport w formacie §43 konstytucji (A–K) i aktualizacja `../evidence/CONVERGENCE_EVIDENCE.md`; granica przechodzi w FROZEN w `DECISION_FREEZE_REGISTER.md` dopiero po dowodzie implementacji, wyroczni, przeglądzie future-capability, przeglądzie adwersaryjnym i bramce CI.
