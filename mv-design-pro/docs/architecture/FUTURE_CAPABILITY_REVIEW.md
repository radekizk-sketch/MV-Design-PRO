# MV-DESIGN-PRO — FUTURE CAPABILITY REVIEW (test każdej decyzji fundamentalnej wobec pełnej koperty zdolności)

**Status:** KANONICZNY, ŻYWY (kontrakt MAX PLATFORM 2026-09-04, §2, §26, §39). Każda decyzja fundamentalna przechodzi ten przegląd PRZED zamrożeniem; przegląd nie jest implementacją zdolności — jest dowodem, że decyzja ich **nie blokuje**. Obszary sprawdzane (§26): SN · nN ABCN · zwarcia doziemne · zabezpieczenia · DER · BESS · RfG · jakość energii · dynamika · N-1 · optymalizacja · SLD · GIS · dokumentacja · eksploatacja. Koperta: `PRODUCT_CAPABILITY_MODEL.md`.

Legenda werdyktu: **NIE BLOKUJE** (z argumentem), **NIE BLOKUJE POD WARUNKIEM** (warunek jest wymaganiem wpisanym do wycinka), **BLOKUJE** (decyzja odrzucona).

| ID | Decyzja (źródło) | Argument per obszar | Warunek | Werdykt |
|---|---|---|---|---|
| DT-1 | ENM = Canonical Project Twin; brak nowej klasy `TwinModel` (`CANONICAL_DIGITAL_TWIN.md` §1) | model Pydantic rozszerzalny addytywnie (`exclude_none` — hashe stabilne); SN/nN: już jeden graf; doziemne/zabezpieczenia/DER/BESS: nowe encje jako kolekcje (`NeutralGrounding`, `EarthingSystem`, dekompozycja DER, stan BESS); PQ/dynamika: parametry częstotliwościowe i dynamiczne to pola katalogowe/assetowe, nie zmiana rdzenia; N-1/optymalizacja: operują na scenariuszach/wariantach, nie na modelu; SLD/GIS: projekcje; dokumentacja/eksploatacja: envelope i stany | operacje domenowe przechodzą z `dict` na typowany model stopniowo (A1-19), bez big-bangu | NIE BLOKUJE |
| DT-2 | Project owns ENM; `StudyCase` = konfiguracja (§2) | wszystkie obszary wymagają lineage projektu; N-1/optymalizacja/warianty niemożliwe przy kopii per case; RfG/dokumentacja wymagają jednej rewizji odniesienia | migracja per-case ENM → warianty (CV-1.2) bez utraty danych | NIE BLOKUJE POD WARUNKIEM |
| DT-3 | `Bus` ≡ `ConnectivityNode`; terminal wyprowadzany z trwałych `from/to_bus_ref` + fazy; TN wyprowadzany (§3 T-1…T-3) | SN: node-breaker już de facto; nN ABCN: fazy na terminalu; doziemne: sieć zerowa buduje się na TN + uziemieniach; zabezpieczenia: strefy = zbiory terminali; eksploatacja: uziemniki jako `SwitchBranch.kind=EARTHING_SWITCH`, stan EARTHED; SLD: scena z TV; GIS: współrzędne na CN/asset; dynamika/PQ: bez wpływu | urządzenia wieloterminalowe (TR 3-uzw., przyszłe) przez jawną listę `terminals` — pole addytywne; walidator egzekwuje spójność | NIE BLOKUJE |
| DT-4 | `PhaseSet` na terminalu; `ABC` jako definicja klasy urządzeń SN; nN jawnie (§4 F-1) | nN ABCN/SWZ: jawne A/B/C/N/PEN/PE; SN: składowe symetryczne bez zmian; PQ/asymetria: fazowy IR z tego samego ENM; dynamika: bez wpływu; DER jednofazowe nN: `AN` itd. | zakaz globalnego „3ph" jako granicy — IR zgodny i fazowy budowane z tego samego assemblera | NIE BLOKUJE |
| DT-5 | `NeutralGrounding` i `EarthingSystem` jako encje z fizyką; konsolidacja 6 reprezentacji (§4 F-2, F-3) | doziemne SN: cewka/rezystor/izolowany w sieci zerowej; zabezpieczenia 67N/Y0>: polaryzacja z tej samej encji; nN: TN/TT/IT i punkt rozdziału PEN; eksploatacja: uziemniki osobno (stan łącznika); RfG: bez wpływu; GIS: bez wpływu | parity Z0 dla modeli liczących dziś Z0; B-01 jeśli rdzeń IEC 60909 wymaga rozszerzenia o cewkę (ADR-021) | NIE BLOKUJE POD WARUNKIEM |
| DT-6 | `ModelRevision` = dziennik komend + checkpointy; `RevisionEnvelope` (`REVISION_SCENARIO_EXECUTION_MODEL.md` §3) | dokumentacja/RfG/zabezpieczenia: lineage kompletny; N-1/optymalizacja: envelope per scenariusz; SLD: scena z envelope; eksploatacja: aktor i czas w dzienniku | zapis modelu i dziennika w jednej transakcji (CV-2); `ResultSetV1` FROZEN nietknięty (envelope na biegu) | NIE BLOKUJE |
| DT-7 | `NetworkVariation` = komendy; `OperatingScenario` = typowane nadpisania; `EffectiveNetworkSnapshot = apply_scenario(...)` (§3) | N-1: zbiór wyłączeń; BESS: tryb per scenariusz; DER: tryby sterowania per scenariusz; PQ/dynamika: `TIME_STEP` i profile; optymalizacja: wariant = kandydat; eksploatacja: sekwencja = lista scenariuszy stanów; SLD: nakładka per scenariusz | nowe rodzaje nadpisań dodawane jako pola scenariusza (addytywnie), nie jako nowe silniki | NIE BLOKUJE |
| DT-8 | `network_model` = Derived Immutable IR; jeden assembler; `TopologyService` jedna implementacja (`COMPUTATIONAL_BOUNDARY.md` §1–§2) | wszystkie analizy: jedna interpretacja ENM; PQ/dynamika: IR(f) i IR dynamiczny to nowe widoki assemblera, nie nowe modele; GIS: TV niesie osiągalność; SLD: TV jedyna prawda o energizacji | kasacja legacy ORM procedurą (D-03); benchmarki jako parity | NIE BLOKUJE POD WARUNKIEM |
| DT-9 | rdzenie solverów FROZEN + B-01; rejestr zdolności jako bramka (§3) | doziemne/ABCN/dynamika/PQ: NOWE solvery obok rdzeni (addytywnie), status `PLANNED` widoczny; bez cichego przybliżenia | B-01 pakiet dowodowy dla każdego rozszerzenia rdzenia | NIE BLOKUJE |
| DT-10 | `ResultSetV1` FROZEN; envelope na rekordzie biegu; `ResultSetV2` jako nowa wersja (ADR-018) | dokumentacja/porównania: envelope dostępny z biegu; nowe wielkości (fazowe, doziemne, PQ) → v2 z pełnym envelope | v2 addytywnie, z okresem współistnienia i kasacją v1 procedurą po migracji konsumentów | NIE BLOKUJE |
| DT-11 | kwantyzacja 9 cyfr na granicy kontraktu; tryb ścisły dla niefinitów (`COMPUTATIONAL_BOUNDARY.md` §5) | żaden obszar nie wymaga > 9 cyfr w kontrakcie (dokładność danych 3–4 cyfry); dynamika/PQ: szeregi kwantyzowane elementowo; „nieskończona sieć sztywna" jako jawne pole | zakaz kwantyzacji w solverach/IR; ta sama polityka dla każdego nowego kontraktu | NIE BLOKUJE |
| DT-12 | `ExecutionBackend` (pula procesów teraz, kolejka później); jeden rejestr biegów (§5) | N-1/optymalizacja/QSTS: wsady równoległe; dynamika: długie biegi jako zadania z postępem; skala L: backend kolejkowy bez zmiany orkiestratora | determinizm niezależny od backendu (ten sam zestaw testów na obu) | NIE BLOKUJE |
| DT-13 | Postgres docelowo, SQLite dev/test; kontrakt współbieżności (`expected_revision`, `actor`, `command_id`, 409) | eksploatacja/wieloosobowość: konflikt jawny; agent/asystent: `PROPOSED`; GIS/import: aktor = import | identyczne kontrakty na obu silnikach (testy na obu) | NIE BLOKUJE |
| DT-14 | SLD = projekcja twin; backend semantyka, frontend geometria; IEC 60617; IEC 81346 przez profil (`docs/twin/MV_DESIGN_PRO_SLD_PRESENTATION_ARCHITECTURE.md`) | SLD SN/nN/stacje/nakładki: jedna scena; GIS/SCADA: kolejne polityki prezentacji tej samej sceny; eksploatacja: stany łączeń w scenie z TV; dokumentacja: arkusze z tej samej sceny | B-02 na rzeczywistych arkuszach (właściciel) | NIE BLOKUJE |
| DT-15 | klasy proweniencji wartości; zero fabrykacji; profile referencyjne zamiast fikcyjnych producentów (§5; D-33) | katalog/dobór/PQ (widma)/dynamika (parametry)/RfG (profile): każda liczba ze źródłem; dokumentacja: łańcuch wartość → źródło → wydanie | reużycie istniejącego mechanizmu proweniencji (K-E/K-O/K-Q), nie drugi | NIE BLOKUJE |
| DT-16 | rejestr sieci wzorcowych z klasami wyroczni; G01 jako pierwszy slice (`../reference-networks/REFERENCE_NETWORK_REGISTRY.md`) | doziemne/zabezpieczenia/nN/DER: G01 przecina wszystkie warstwy; PQ/dynamika/optymalizacja/GIS: własne klasy G10–G13 z wyroczniami analitycznymi | wpis bez wyroczni = `REGRESSION_ONLY`, nie dowód | NIE BLOKUJE |

## Rejestr przeglądów wykonanych
| Data | Decyzje | Wynik | Uwagi adwersaryjne |
|---|---|---|---|
| 2026-09-04 | DT-1…DT-16 (projekt) | 16 × NIE BLOKUJE (4 z warunkiem wpisanym do wycinków CV-1, CV-2, CV-4, CV-5) | przegląd adwersaryjny (§38) zaplanowany po każdym wycinku; wynik do `../evidence/CONVERGENCE_EVIDENCE.md` §G |

## Co unieważnia werdykt (§39)
Dowód: defekt inżynierski, defekt numeryczny, awaria spójności danych, niezdolność do obsłużenia zdolności z `PRODUCT_CAPABILITY_MODEL.md`, problem bezpieczeństwa, bloker wydajności, nowe jawne wymaganie właściciela. Preferencja architektoniczna kolejnego agenta **nie jest** dowodem.
