# MV-DESIGN-PRO — DECISION FREEZE REGISTER (decyzje fundamentalne: dowód, odrzucone alternatywy, warunki ponownego otwarcia, status)

**Status:** KANONICZNY, ŻYWY (konstytucja §26, §30, §39, §42 „FUTURE-PROOFING"). Zastępuje `FUTURE_CAPABILITY_REVIEW.md` (treść przeniesiona: kolumna „Argument per obszar" = przegląd future-capability). Każda granica fundamentalna ma tu jeden wiersz; granica bez wiersza nie jest przyjęta.
**Statusy:** `PROPOSED` (decyzja projektowa, brak dowodu implementacji) · `EVIDENCE-GATED` (implementacja/dowód częściowy, przed przeglądem adwersaryjnym) · `FROZEN` (po: dowodzie implementacji, wyroczni inżynierskiej, przeglądzie future-capability, przeglądzie adwersaryjnym §38, bramce CI) · `REOPENED` (z dowodem z listy §2).
**Obszary przeglądu future-capability (§26):** SN · nN ABCN · zwarcia doziemne · zabezpieczenia · DER · BESS · RfG · jakość energii · dynamika · N-1 · optymalizacja · SLD · GIS · dokumentacja · eksploatacja.

## 1. Rejestr

| ID | Decyzja (źródło w `CANONICAL_TWIN_ARCHITECTURE.md`) | Argument future-capability per obszar (dowód, że nie blokuje) | Odrzucone alternatywy | Warunek | Werdykt | Status |
|---|---|---|---|---|---|---|
| DT-1 | ENM = Canonical Project Twin; brak nowej klasy `TwinModel` (`CANONICAL_TWIN_ARCHITECTURE.md` §A.1) | model Pydantic rozszerzalny addytywnie (`exclude_none` — hashe stabilne); SN/nN: już jeden graf; doziemne/zabezpieczenia/DER/BESS: nowe encje jako kolekcje (`NeutralGrounding`, `EarthingSystem`, dekompozycja DER, stan BESS); PQ/dynamika: parametry częstotliwościowe i dynamiczne to pola katalogowe/assetowe, nie zmiana rdzenia; N-1/optymalizacja: operują na scenariuszach/wariantach, nie na modelu; SLD/GIS: projekcje; dokumentacja/eksploatacja: envelope i stany | nowa klasa `TwinModel` obok ENM (łańcuch ENM→Twin→NetworkModel→Solver); big-bang nowy model bez adaptera | operacje domenowe przechodzą z `dict` na typowany model stopniowo (A1-19), bez big-bangu | NIE BLOKUJE | PROPOSED → dowód: CV-1/CV-5 + przegląd adwersaryjny |
| DT-2 | Project owns ENM; `StudyCase` = konfiguracja (§2) | wszystkie obszary wymagają lineage projektu; N-1/optymalizacja/warianty niemożliwe przy kopii per case; RfG/dokumentacja wymagają jednej rewizji odniesienia | ENM per `StudyCase` (stan zastany); kopiowanie sieci przy klonowaniu przypadku | migracja per-case ENM → warianty (CV-1.2) bez utraty danych | NIE BLOKUJE POD WARUNKIEM | EVIDENCE-GATED (rdzeń `9667235a`; wiring w toku) |
| DT-3 | `Bus` ≡ `ConnectivityNode`; terminal wyprowadzany z trwałych `from/to_bus_ref` + fazy; TN wyprowadzany (§3 T-1…T-3) | SN: node-breaker już de facto; nN ABCN: fazy na terminalu; doziemne: sieć zerowa buduje się na TN + uziemieniach; zabezpieczenia: strefy = zbiory terminali; eksploatacja: uziemniki jako `SwitchBranch.kind=EARTHING_SWITCH`, stan EARTHED; SLD: scena z TV; GIS: współrzędne na CN/asset; dynamika/PQ: bez wpływu | bus-branch z portami jako metadanymi (`Port*`, stan zastany); trwały `TopologicalNode`; przemianowanie `Bus`→`ConnectivityNode` w całym kodzie (churn bez treści) | urządzenia wieloterminalowe (TR 3-uzw., przyszłe) przez jawną listę `terminals` — pole addytywne; walidator egzekwuje spójność | NIE BLOKUJE | PROPOSED |
| DT-4 | `PhaseSet` na terminalu; `ABC` jako definicja klasy urządzeń SN; nN jawnie (§4 F-1) | nN ABCN/SWZ: jawne A/B/C/N/PEN/PE; SN: składowe symetryczne bez zmian; PQ/asymetria: fazowy IR z tego samego ENM; dynamika: bez wpływu; DER jednofazowe nN: `AN` itd. | globalne `phase=3ph` jako granica; osobny trwały model nN | zakaz globalnego „3ph" jako granicy — IR zgodny i fazowy budowane z tego samego assemblera | NIE BLOKUJE | PROPOSED |
| DT-5 | `NeutralGrounding` i `EarthingSystem` jako encje z fizyką; konsolidacja 6 reprezentacji (§4 F-2, F-3) | doziemne SN: cewka/rezystor/izolowany w sieci zerowej; zabezpieczenia 67N/Y0>: polaryzacja z tej samej encji; nN: TN/TT/IT i punkt rozdziału PEN; eksploatacja: uziemniki osobno (stan łącznika); RfG: bez wpływu; GIS: bez wpływu | uziemienie jako parametr „obok" (6 reprezentacji, stan zastany); osobny „earth fault model" | parity Z0 dla modeli liczących dziś Z0; B-01 jeśli rdzeń IEC 60909 wymaga rozszerzenia o cewkę (ADR-021) | NIE BLOKUJE POD WARUNKIEM | PROPOSED |
| DT-6 | `ModelRevision` = dziennik komend + checkpointy; `RevisionEnvelope` (`CANONICAL_TWIN_ARCHITECTURE.md` §B.3) | dokumentacja/RfG/zabezpieczenia: lineage kompletny; N-1/optymalizacja: envelope per scenariusz; SLD: scena z envelope; eksploatacja: aktor i czas w dzienniku | replay z dziennika bez ładunków (niemożliwy — pomiar); pełne migawki per bieg jako jedyna historia (0,78 MB/bieg) | zapis modelu i dziennika w jednej transakcji (CV-2); `ResultSetV1` FROZEN nietknięty (envelope na biegu) | NIE BLOKUJE | PROPOSED |
| DT-7 | `NetworkVariation` = komendy; `OperatingScenario` = typowane nadpisania; `EffectiveNetworkSnapshot = apply_scenario(...)` (§3) | N-1: zbiór wyłączeń; BESS: tryb per scenariusz; DER: tryby sterowania per scenariusz; PQ/dynamika: `TIME_STEP` i profile; optymalizacja: wariant = kandydat; eksploatacja: sekwencja = lista scenariuszy stanów; SLD: nakładka per scenariusz | kolejny Scenario Engine (7. implementacja); P23 `Study/Scenario` z `Any`-referencjami jako kanon | nowe rodzaje nadpisań dodawane jako pola scenariusza (addytywnie), nie jako nowe silniki | NIE BLOKUJE | PROPOSED |
| DT-8 | `network_model` = Derived Immutable IR; jeden assembler; `TopologyService` jedna implementacja (`CANONICAL_TWIN_ARCHITECTURE.md` §C.1–§C.2) | wszystkie analizy: jedna interpretacja ENM; PQ/dynamika: IR(f) i IR dynamiczny to nowe widoki assemblera, nie nowe modele; GIS: TV niesie osiągalność; SLD: TV jedyna prawda o energizacji | nowy `TopologyService` obok 20 istniejących; `network_model` jako drugi trwały model projektu (legacy ORM) | kasacja legacy ORM procedurą (D-03); benchmarki jako parity | NIE BLOKUJE POD WARUNKIEM | PROPOSED |
| DT-9 | rdzenie solverów FROZEN + B-01; rejestr zdolności jako bramka (§3) | doziemne/ABCN/dynamika/PQ: NOWE solvery obok rdzeni (addytywnie), status `PLANNED` widoczny; bez cichego przybliżenia | modyfikacja rdzeni bez wyroczni; status `implemented` hurtem (stan zastany rejestru) | B-01 pakiet dowodowy dla każdego rozszerzenia rdzenia | NIE BLOKUJE | FROZEN (rdzenie od V12) + PROPOSED (rejestr jako bramka) |
| DT-10 | `ResultSetV1` FROZEN; envelope na rekordzie biegu; `ResultSetV2` jako nowa wersja (ADR-018) | dokumentacja/porównania: envelope dostępny z biegu; nowe wielkości (fazowe, doziemne, PQ) → v2 z pełnym envelope | łamanie FROZEN `ResultSetV1` przez dopisanie envelope do ładunku | v2 addytywnie, z okresem współistnienia i kasacją v1 procedurą po migracji konsumentów | NIE BLOKUJE | PROPOSED |
| DT-11 | kwantyzacja 9 cyfr na granicy kontraktu; tryb ścisły dla niefinitów (`CANONICAL_TWIN_ARCHITECTURE.md` §C.5) | żaden obszar nie wymaga > 9 cyfr w kontrakcie (dokładność danych 3–4 cyfry); dynamika/PQ: szeregi kwantyzowane elementowo; „nieskończona sieć sztywna" jako jawne pole | 12 cyfr (fałszywa czerwień ≈ 0,4/przebieg); pełna reprezentacja float (odcisk zależny od maszyny); kwantyzacja w solverach | zakaz kwantyzacji w solverach/IR; ta sama polityka dla każdego nowego kontraktu | NIE BLOKUJE | EVIDENCE-GATED (kod + testy klasy; przegląd §36 wykonany) |
| DT-12 | `ExecutionBackend` (pula procesów teraz, kolejka później); jeden rejestr biegów (§5) | N-1/optymalizacja/QSTS: wsady równoległe; dynamika: długie biegi jako zadania z postępem; skala L: backend kolejkowy bez zmiany orkiestratora | Celery/Redis jako obowiązkowe (0 tasków); wątki (K=10 wolniej 1,81×) | determinizm niezależny od backendu (ten sam zestaw testów na obu) | NIE BLOKUJE | PROPOSED |
| DT-13 | Postgres docelowo, SQLite dev/test; kontrakt współbieżności (`expected_revision`, `actor`, `command_id`, 409) | eksploatacja/wieloosobowość: konflikt jawny; agent/asystent: `PROPOSED`; GIS/import: aktor = import | SQLite w produkcji; silent last-write-wins (stan zastany) | identyczne kontrakty na obu silnikach (testy na obu) | NIE BLOKUJE | PROPOSED |
| DT-14 | SLD = projekcja twin; backend semantyka, frontend geometria; IEC 60617; IEC 81346 przez profil (`docs/twin/MV_DESIGN_PRO_SLD_PRESENTATION_ARCHITECTURE.md`) | SLD SN/nN/stacje/nakładki: jedna scena; GIS/SCADA: kolejne polityki prezentacji tej samej sceny; eksploatacja: stany łączeń w scenie z TV; dokumentacja: arkusze z tej samej sceny | SLD jako drugi model elektryczny (klient liczy topologię — stan zastany); scalanie nN z SN dla starych testów | B-02 na rzeczywistych arkuszach (właściciel) | NIE BLOKUJE | PROPOSED |
| DT-15 | klasy proweniencji wartości; zero fabrykacji; profile referencyjne zamiast fikcyjnych producentów (§5; D-33) | katalog/dobór/PQ (widma)/dynamika (parametry)/RfG (profile): każda liczba ze źródłem; dokumentacja: łańcuch wartość → źródło → wydanie | nowy mechanizm proweniencji obok K-E/K-O/K-Q; nazwy producentów bez dokumentu | reużycie istniejącego mechanizmu proweniencji (K-E/K-O/K-Q), nie drugi | NIE BLOKUJE | EVIDENCE-GATED (FAB-A w toku) |
| DT-16 | rejestr sieci wzorcowych z klasami wyroczni; G01 jako pierwszy slice (`../reference-networks/REFERENCE_NETWORK_REGISTRY.md`) | doziemne/zabezpieczenia/nN/DER: G01 przecina wszystkie warstwy; PQ/dynamika/optymalizacja/GIS: własne klasy G10–G13 z wyroczniami analitycznymi | jedna sieć golden definiująca architekturę; fixture z tej samej implementacji jako wyrocznia | wpis bez wyroczni = `REGRESSION_ONLY`, nie dowód | NIE BLOKUJE | EVIDENCE-GATED (rejestr w kodzie `0c506744`) |

### 1.1 Decyzje właściciela wpisane jako granice (werdykt 2026-09-02, konstytucja 2026-09-04)
| ID | Decyzja | Dowód / źródło | Odrzucone alternatywy | Warunek ponownego otwarcia | Status |
|---|---|---|---|---|---|
| OW-1 | D-07: `ExecutionBackend` (pula procesów teraz, kolejka później) bez zmiany orkiestratora | `../twin/MV_DESIGN_PRO_SIMULATION_ARCHITECTURE.md` §7 | usunięcie scenariusza kolejki; Celery obowiązkowe | bloker wydajności udowodniony pomiarem | PROPOSED |
| OW-2 | D-23: optimum wielokryterialne (8 osi, Pareto); „min. spełniający + rezerwa" = polityka domyślna | `../twin/MV_DESIGN_PRO_DESIGN_OPTIMIZATION_ARCHITECTURE.md` §5.0 | jednokryterialna definicja optimum | — (decyzja produktowa właściciela) | FROZEN (decyzja właściciela) |
| OW-3 | D-34: `ProtectionCapabilityRegistry` SUPPORTED/PARTIAL/PLANNED/NOT_IMPLEMENTED; zero regresji funkcjonalnej | `../twin/MV_DESIGN_PRO_PROTECTION_ARCHITECTURE.md` §5a | wyłączanie funkcji „poza wersją 1" | — | FROZEN (decyzja właściciela) |
| OW-4 | D-39/D-03: archive-first + manifest supersesji + audyt odsyłaczy przed DELETE; kasacja procedurą 7 kroków; liczba plików/LOC nie jest KPI | `CONVERGENCE_ROADMAP.md` §0 | kasacja na podstawie celu ilościowego | — | FROZEN (decyzja właściciela) |
| OW-5 | D-33: zero fikcyjnych nazw producentów; profile referencyjne bez marki | karta FAB-A | fikcyjny katalog jako „dane testowe" | — | EVIDENCE-GATED |
| OW-6 | D-14/B-02: SLD oceniane na rzeczywistych arkuszach CAD/SCADA, osobno semantyka i grafika; agent nie certyfikuje | `CONVERGENCE_ROADMAP.md` (bramki) | golden SVG jako certyfikat | — | FROZEN (bramka właściciela) |
| OW-7 | D-09/B-01: rozszerzenia rdzeni addytywnie z niezależną wyrocznią, tożsamością starej fizyki, tolerancją, porównaniem wydajności | `CANONICAL_TWIN_ARCHITECTURE.md` §C.3 | zmiana fizyki „bo test przechodzi" | — | FROZEN (bramka właściciela) |
| OW-8 | CV-2: rewizja modelu = pełna, niezmienna migawka (append-only) + pełny ładunek komendy w dzienniku; `checkout(rev)` odtwarza hash; świeżość wyniku WYPROWADZANA z envelope, nie zapisywana; retencja (pruning rewizji niereferowanych) = decyzja właściciela OD-4 | `CANONICAL_TWIN_ARCHITECTURE.md` §B.3, karta CV-2 | delty/replay jako jedyne źródło (dziennik bez ładunków nie pozwala odtworzyć), obcinanie dziennika (`LIMIT_WPISOW`), pisarze `result_status` rozsiani po warstwach | pomiar kosztu nośnika przekraczający budżet projektu (wtedy pruning wg OD-4) | PROPOSED (do EVIDENCE-GATED po CV-2) |

## 2. Warunki ponownego otwarcia (wspólne dla wszystkich wierszy; konstytucja §30)
Dowód jednego z: defekt inżynierski · błędny wynik numeryczny · niezdolność do wymaganej fizyki · awaria spójności danych · awaria współbieżności/persystencji · defekt bezpieczeństwa · nieakceptowalna granica wydajności · jawne nowe wymaganie właściciela. Preferencja architektoniczna kolejnego agenta NIE jest dowodem. Ponowne otwarcie = wiersz `REOPENED` z dowodem i nową decyzją, nigdy cicha zmiana.

## 3. Test §42 „jaka zdolność zmusiłaby do złamania granicy?" — odpowiedzi
| Granica | Zdolność-kandydat | Dlaczego nie łamie |
|---|---|---|
| ENM jako Canonical Project Twin | dynamika RMS, harmoniczne, ABCN, 3-uzw. TR, GIS | nowe encje/pola addytywne; IR dynamiczny/częstotliwościowy/fazowy z tego samego assemblera; współrzędne na CN/asset |
| terminal wyprowadzany z `from/to_bus_ref` + fazy | urządzenia wieloterminalowe, tory ziemne, ekrany | jawna lista `terminals` (pole addytywne) dla n>2; ekrany/tory ziemne jako urządzenia przewodzące z własnymi terminalami |
| TN wyprowadzany, nigdy trwały | eksploatacja (sekwencje łączeń), N-1 masowe | TV liczona per scenariusz; wydajność przez inkrementalny union-find (budżet B1) |
| `EffectiveNetworkSnapshot = f(rev, variation, scenario)` | szeregi czasowe, QSTS, probabilistyka | `TIME_STEP` i profile w scenariuszu; probabilistyka = rodzina scenariuszy z rozkładem, nie nowy model |
| `RevisionEnvelope` na biegu (v1 FROZEN) | porównania między rewizjami, dokumenty wielorewizyjne | envelope per bieg + `ResultSetV2` z envelope w ładunku; v1 kasowany procedurą |
| jeden assembler ES→TV→IR | solvery zewnętrzne, harmoniczne, dynamika | IR = rodzina widoków assemblera (zgodny, fazowy, częstotliwościowy, dynamiczny); adapter zewnętrzny = konsument IR |
| kwantyzacja 9 cyfr na granicy kontraktu | wielkości bardzo małe/duże (pF, GVA) | kwantyzacja względna; jednostka w nazwie pola |
| `ExecutionBackend` | obliczenia rozproszone, GPU | nowa implementacja backendu bez zmiany orkiestratora |
