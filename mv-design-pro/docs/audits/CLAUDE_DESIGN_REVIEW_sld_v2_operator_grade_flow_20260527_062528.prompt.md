Review MV-DESIGN-PRO active SLD V2 as senior UX/product designer for engineering software, SCADA/CAD interaction designer, and MV power-network workflow reviewer.

Scope only: UI/UX, interaction, SLD readability, LOD, CAD behavior, layout, navigation, workflow. Do not invent electrical rules, do not change solver physics/protection logic, and do not weaken ENM/domain correctness.

Context: MV network designers need an operator-grade SLD V2. It must show GPZ 110/SN, MV sections/bays, cable heads/ports, main run, stations, branches, DER/PCC/NMO, readiness, proofs and reports. Active UI should use Polish engineering labels. Station insertion is endpoint append by default; split is explicit with preview/cancel/commit/audit. Missing values must be blockers or "brak danych/nie wyznaczono", never false 0.00.

Return exactly these sections, concise and testable:
# Summary of top UX risks
# Must-fix issues
# Suggested flow improvements
# Screen/component-level recommendations
# SLD/CAD/LOD recommendations
# Testable acceptance criteria
# Risks and trade-offs
# What not to change
# Recommended follow-up tests
