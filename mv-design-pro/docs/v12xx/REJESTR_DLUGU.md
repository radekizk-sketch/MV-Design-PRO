# Rejestr dlugu technicznego V12.xx

Status: brak aktywnego długu w zakresie wpisów V12-CANON z 2026-04-27  
Cel: żadna znana luka V12.xx nie może pozostać nieoznaczona

## Definicja zamkniecia

Zero dlugu oznacza brak aktywnego, swiadomie pozostawionego dlugu w zakresie V12.xx. Kazdy odlozony temat bez wpisu w tym rejestrze blokuje zamkniecie V12.xx.

## Schemat wpisu

| Pole | Wymaganie |
|---|---|
| Kod | `V12T-XXX` |
| Obszar | ENM / API / UI / SLD / solver / katalogi / raport / testy / migracja |
| Problem | Konkretny opis dlugu |
| Skutek | Ryzyko dla produktu |
| Decyzja | Usunac / scalic / przepisac / migrowac / oznaczyc legacy |
| Wlasciciel | Rola odpowiedzialna |
| Faza | M0 / M1 / M2 / M3 / M4 |
| Status | otwarty / w trakcie / zamkniety |

## Znany dlug objety planem

| Kod | Obszar | Problem | Skutek | Decyzja | Faza | Status |
|---|---|---|---|---|---|---|
| V12T-001 | dokumentacja | Brak katalogu `docs/v12xx/` przed wdrozeniem kanonu. | Ryzyko wielu zrodel prawdy. | Utworzyc aktywny kanon i indeks. | M0 | zamkniety przez ten pakiet |
| V12T-002 | ENM | Obecny ENM mial wersje 1.0 i nie zawieral pelnego zakresu V12.xx. | Brak miejsca dla wariantow, migawek, profili i automatyki. | Zamknieto dla aktywnego toru V12: projekcja ENM v2.0 materializuje wariant bazowy, migawke lacznikowa, Z0, profile zrodel, profile operatora, profile FRT/Q(U)/cos phi(P), profile obciazen i byty automatyki z deterministycznym hashem. | M1-M3 | zamkniety |
| V12T-003 | studia | Publiczny tor execution i raportowania mial fallback `OperatingCase` wzgledem `StudyCase`. | Ryzyko drugiego modelu przypadkow w aktywnym API i kliencie. | Zamknieto dla toru produkcyjnego: publiczne endpointy ignoruja legacy `AnalysisRun`, frontend nie akceptuje juz `operating_case_id`, a pozostale porzadki persystencji legacy sa sledzone osobno w `V12T-011`. | M1-M3 | zamkniety |
| V12T-004 | solver | Kanoniczny run ENM wymagal pelnego proof/statusu raportowego dla zwarc 1F i 2F+Z. | Bez tego wynik asymetryczny bylby policzony, ale nie bylby pelnym wynikiem raportowym V12.xx. | Zamknieto: kazdy wiersz 1F/2F+Z ma `proof_ref`, `proof_status=complete`, `reporting_status=reportable`, powiazanie z krokami trace, status w raw result, wynikach, trace export, JSON report i JSON export. | M2 | zamkniety |
| V12T-005 | OZE | Modele PV/BESS/FW nie byly pelnym modelem FRT/operatorowym V12.xx. | Niepelna zgodnosc przylaczeniowa. | Zamknieto: `Generator.gen_type` rozroznia `fw_pmsg`, `fw_dfig`, `fw_scig`, projekcja ENM v2.0 materializuje profile zrodel i operatora, a `source_compliance` wymaga zgodnego `generator_technology` dla FW PMSG/DFIG/SCIG. | M2-M3 | zamkniety |
| V12T-006 | API | Endpointy `/api` wymagaly twardego porownania aktywnego kodu z macierza lifecycle. | Bez guarda mozna bylo dodac aktywna trase bez statusu, testow, wlasciciela albo daty wylaczenia adaptera/deprecated. | Zamknieto: `api_lifecycle_guard.py` wykrywa aktywne trasy FastAPI z `api.main`, porownuje je z `MACIERZ_KOMPATYBILNOSCI_API.md` i jest uruchamiany przez `v12xx_canon_guard.py` oraz `verify:v12.5.1`. | M0-M2 | zamkniety |
| V12T-007 | UI | Drafty formularzy wymagaly jawnego kontraktu draft vs committed. | Ryzyko lokalnej prawdy poza ENM. | Dodano macierz i testy uruchomien obliczen; kolejne formularze musza stosowac ten sam kontrakt. | M1-M2 | zamkniety dla run request |
| V12T-008 | SLD | Pelna macierz interakcji V12.xx nie byla wymuszana guardem. | Ryzyko niespojnych klikniec i menu. | Zamknieto: `MACIERZ_INTERAKCJI.md` pokrywa minimalny zestaw obiektow pierwszej klasy, a `interaction_matrix_guard.py` wraz z testami blokuje brak wpisu dla nowego obiektu SLD. | M2-M3 | zamkniety |
| V12T-009 | raport | Raporty i eksporty wymagaly jasnego technicznego motywu oddzielonego od dark SCADA. | Ryzyko nieczytelnego eksportu. | Wydzielono `light_technical` dla eksportu SLD PNG/PDF oraz jawny tryb `screen`. | M2 | zamkniety dla eksportu SLD |
| V12T-010 | testy | Sieci i scenariusze wzorcowe V12.xx nie byly wymuszane guardem. | Brak stalego dowodu kompletnego zakresu domenowego. | Zamknieto: `MACIERZ_TESTOW_V12_XX.md` utrzymuje `V12-GN-001..007`, a `reference_networks_guard.py` sprawdza komplet kodow oraz istnienie plikow dowodowych dla zwarc, Petersena, stanu fazowego, zgodnosci zrodel, automatyki i stabilnosci. | M1-M3 | zamkniety |
| V12T-011 | API / migracja | Aktywne sciezki legacy i adapterowe laczyly `OperatingCase`, `StudyCase`, endpointy wynikow oraz katalog ochrony w kilku miejscach. | Ryzyko drugiej prawdy przypadkow, niejednoznacznego run envelope i nieprodukcyjnego fallbacku katalogowego. | Zamknieto dla publicznego toru V12: `legacy_public_path_guard.py` blokuje publiczny import lub emisje `OperatingCase`, `AnalysisRun`, `get_operating_case` i `operating_case_id`; aktywne routery runow, wynikow i eksportu pozostaja na `StudyCase` oraz kanonicznym result contract. | M2-M4 | zamkniety |
| V12T-012 | walidacje | Slownik severity byl zcentralizowany dla ENMValidator, ale boundary API moglo nadal uzywac lokalnych literalow statusu i map severity. | Ryzyko niespojnych blokad raportu, publikacji i migracji poza ENM. | Zamknieto: `enm.severity` eksportuje publiczne wartosci, helper `empty_severity_counts()` i `is_failed_status()`, boundary API uzywa kontraktu, a `severity_contract_guard.py` blokuje inline `BLOCKER`/`IMPORTANT`/`INFO`/`OK`/`WARN`/`FAIL` w plikach walidacyjnych. | M2 | zamkniety |
| V12T-013 | testy / E2E | Pelna weryfikacja mogla zawisnac podczas probe `chrome --version` albo teardown realnego backendu uruchamianego przez shellowy `Set-Location`; dodatkowo brak systemowego Chrome nie powinien blokowac cache Playwright Chromium, a setup Windows nie moze wpadac w fallback APT. | Ryzyko braku wyniku `verify:v12.5.1` mimo przejscia testow E2E oraz osieroconych procesow po weryfikacji. | Zamknieto: resolver uzywa cache Playwright Chromium, probe Chromium ma limit `PLAYWRIGHT_CHROMIUM_VERSION_TIMEOUT_MS`, setup Windows uzywa `playwright.cmd`, kroki weryfikacji maja timeout, backend Playwright startuje przez `cwd: backendCwd`, a guard kanonu blokuje powrot do shellowego handoffu. | M0-M2 | zamkniety |
| V12T-014 | UI / raport | Przegladarka wynikow utrzymywala martwy probe `violations`, fallback `operating_case_id` oraz niedzialajacy eksport Excel. | Ryzyko 404 w aktywnym UI i utrzymania legacy semantyki poza kanonem. | Zamknieto: klient wynikow liczy naruszenia z aktywnego payloadu PF, wymaga `study_case_id`, a `/power-flow-runs/{run_id}/export/xlsx` jest aktywna trasa generowana z kanonicznego bundle. | M4 | zamkniety |
| V12T-015 | API / UI (tozsamosc uzytkownika) | `ui2/shell/useShellStore.ts` skaluje magazyn ustawien powloki (localStorage) prefiksem `userScope`, ktory jest DOMYSLNIE stala `'local'` — brak jakiejkolwiek trasy uwierzytelniania w backendzie do rozroznienia uzytkownikow. POMIAR (2026-09-16, `grep -rin "auth\|/me\b\|current_user\|login" backend/src/api/main.py` na 44 zarejestrowanych routerach `include_router`): 0 trafien; `backend/src/api/main.py` nie rejestruje zadnego routera `auth`/`/me`. Ustawienia layoutu (szerokosci paneli, tryb zaawansowania, aktywna przestrzen) sa dzielone przez WSZYSTKICH uzytkownikow tej samej przegladarki zamiast per-konto. | Wielu inzynierow na jednej stacji roboczej (wspolne konto systemowe) nadpisuja sobie nawzajem preferencje ukladu powloki; brak podstawy do przyszlej autoryzacji per-uzytkownik (kto zatwierdzil biegi, kto edytowal model). | Zamkniete w rejestrze (2026-09-17) przez PRZENIESIENIE do decyzji wlasciciela OD-27 w `../plan/MAPA_DOMKNIECIA_PRODUKTU_2026-09.md` §7 — dlug NIE znika, zmienia miejsce: rejestr V12T jest dziennikiem ZAMKNIEC (guard `v12xx_canon_guard` wymaga statusu `zamkniety` dla kazdego wiersza), a pozycje otwarte zyja w mapie domkniecia i roadmapie. Tresc bez zmian: `userScope` = stala `local`, 0 tras uwierzytelniania w backendzie (pomiar 2026-09-16), warstwa auth/perimeter nalezy do programu 10x F1 i wymaga decyzji wlasciciela o modelu tozsamosci (konta, role, sesje). | F1 | zamkniety |
| V12T-016 | UI (szablony stacji, klasyfikacja rol) | `ui2/spaces/model/szablony/GrupyRol.ts` mapuje kategorie szablonow backendu na role sieciowe A-E (`SZABLONY_STACJI_2026-07.md` §1). POMIAR (2026-09-16, enumeracja `TemplateCategory` w `backend/src/application/station_templates/schema.py`): dokladnie 10 kategorii (`typowa_sn_nn`, `slupowa`, `zksn_wnetrzowa`, `prosument_pv`, `farma_pv`, `bess`, `hybrydowa`, `przemyslowa`, `wiatrowa`, `sekcyjna`), wszystkie mapowane do rol B/C/D/E; 0 kategorii mapowanych do roli A ("Zasilanie sieci" — GPZ 110/SN, rozdzielnia sieciowa RS/RSM); rola C ma tylko czesc pokrycia (abonencka SN z ukladem pomiarowym brak); rola E ma tylko czesc pokrycia (kompensacja mocy biernej, rezerwa zasilania brak). | Przegladarka szablonow nie pokazuje zadnego wariantu w roli A i ma niepelne pokrycie C/E dopoki backend nie dostanie nowych kategorii — inzynier projektujacy GPZ lub stacje abonencka nie znajdzie gotowego szablonu. | NIE naprawiac w karcie klasyfikacji/przegladarki UI — wymaga nowych modulow szablonow w `backend/src/application/station_templates/` z pelnym kompletem danych katalogowych (transformator, pola SN, zabezpieczenia, DER) per `SZABLONY_STACJI_2026-07.md` §2/§4 "Delta backendowa", nie zmiany mapy rol. Do czasu tej delty `rolaDlaKategorii()` ma jawny fallback do roli B dla kategorii spoza mapy (zero utraconych szablonow), a rola A pozostaje widoczna w drzewku z licznikiem zero. Wlasciciel: program UI/UX 2026-07 (epik E3, delta backendowa stacji). | U2 | otwarty |
| V12T-019 | UI (strumien OZE, martwy klik dowodu) | 9 ekranow strumienia OZE (`ui2/oze/**`) przekazuja `onOtworzDowod` jako no-op do wspolnego komponentu wynikow (`wyniki/wzorzec`): `krzywe/EkranKrzywych.tsx`, `frt/{EkranFrt,SekcjaSekwencjiZapadow}.tsx`, `studium/KreatorStudium.tsx`, `lom/EkranLom.tsx`, `obszar/EkranObszaruPQ.tsx`, `osd/EkranOsd.tsx`, `kompensacja/EkranKompensacji.tsx` (dosl. `() => undefined`) oraz `ranking/EkranRankingu.tsx` (`() => { /* brak akcji */ }`, udokumentowane, ale rowniez bez dzialania). POMIAR (2026-09-16, grep `onOtworzDowod=\{() => undefined\}` w `frontend/src/ui2` poza testami): 8 plikow wzorca + 1 wariant komentowany = 9 lacznie; ZADEN nie jest w zakresie pliku ani TODO-A (`ui2/wyniki/**`), ani TODO-B (`ui2/spaces/**`, `nav/**`, `adapters/**`, `shell/**`, `AppRoot.tsx`) karty TODO-UI2 — trzeci, nieopisany obszar. | Kazdy ekran strumienia OZE ma przycisk „Otworz dowod" (dziedziczony ze wspolnego wzorca wynikow), ktory nic nie robi — martwy klik identyczny klasowo do `AppRoot.tsx:463` naprawionego w TODO-B, ale w INNEJ czesci drzewa komponentow. | Zamkniete (2026-09-17, drzewo scalone): osiem ekranow strumienia OZE dostalo REALNY dostawce dowodu (karta wynikow, commit `oze: wire onOtworzDowod into every OZE screen`), a dziewiaty przypadek (`ranking/EkranRankingu.tsx`) rozstrzygniety wg wlasnej propozycji wpisu — zamiast pustej funkcji ekran NIE podaje dostawcy: props `onOtworzDowod` we wzorcu wynikow (`wyniki/wzorzec/TabelaWynikow.tsx`, `wzorzecModel.ts`) jest OPCJONALNY, a afordancja „pokaz dowod" pokazuje sie tylko gdy komorka niesie `dowodRef` I ekran ma dostawce. Wiersz rankingu to wezel rankingu, nie wynik pojedynczego elementu — zdolnosci nie ma i UI jej nie udaje. Pomiar PO: `grep onOtworzDowod=\{() => undefined\}` w `frontend/src/ui2` poza testami = 0; `scripts/ui2_dead_handler_guard.py` zielony. | U2 | zamkniety |
| V12T-018 | testy FE (tsconfig_gate_guard, dlug POZA BRAMKA) | `scripts/tsconfig_gate_guard.py::BUDZET_BLEDOW_POZA_BRAMKA = 112` (ustawiony commitem `b5568c0f`) jest NIEAKTUALNY na bazie karty TODO-UI2 (`03fad2b8`) — pomiar na CZYSTEJ bazie (przed jakakolwiek zmiana tej karty) da 114, nie 112. POMIAR (2026-09-16, `git log b5568c0f..03fad2b8 -- frontend/src/ui/network-build/`): commit `c8537620` ("fix(certyfikat): status certyfikatu PTPiREE z katalogu backendu") wylądował MIĘDZY pomiarem budżetu a bazą karty i zmienił typ zwiazany z `PtpireeGeneratorCertificateCatalogType`/`CatalogListItem`, psujac typowanie 2 testow (`ui/catalog/__tests__/TypeLibraryBrowser.test.tsx` linie 194/435) — BEZ przeliczenia budzetu przy tamtym commicie. Pelna lista 16 naruszen `POZA BRAMKA` na bazie karty: `ui/app-state/__tests__/store.test.ts`, `ui/canon/__tests__/coverage-matrix.test.ts` (x2), `ui/catalog/__tests__/{TypeLibraryBrowser,converterTypesPtpiree}.test.ts(x)`, `ui/field/__tests__/fieldControlSelectors.test.ts`, `ui/icons/__tests__/technical-icons.test.tsx` (x2), `ui/issue-panel/__tests__/IssuePanelContainer.test.tsx`, `ui/network-build/__tests__/{ReadinessBlockersReview,networkBuildStore,stationSnFields}.test.ts(x)` (x4), `ui/network-build/der-configurator-v2/__tests__/derConfiguratorContract.test.ts` — ZERO z tych plikow dotkniete przez karte TODO-UI2 (`git diff --stat 03fad2b8 -- <te 11 plikow>` = pusty), zaden nie importuje typow zmienionych przez TODO-B (`ExecutionRun`, `StationTemplateSummary`, `TopologyStructure`). | Guard `tsconfig_gate_guard` czerwony NA KAZDYM drzewie zbudowanym na `03fad2b8` niezaleznie od tresci karty — false positive przypisywany kolejnym kartom, ktore go uruchomia, myli przyczyne. | NIE naprawiac punktowo w karcie TODO-UI2 (TODO-A/TODO-B) — dlug pochodzi z INNEGO, juz zamknietego commitu (`c8537620`, temat certyfikatow PTPiREE) w INNYCH katalogach (`ui/network-build`, `ui/canon`, `ui/catalog`, `ui/app-state`, `ui/field`, `ui/icons`, `ui/issue-panel`) bez zadnego zwiazku z zakresem tej karty (przestrzenie/nawigacja/wyniki ui2). Naprawa = przeliczyc `BUDZET_BLEDOW_POZA_BRAMKA` NA BAZIE `c8537620` (podniesc do faktycznego pomiaru z uzasadnieniem w commicie TEGO obszaru) ALBO naprawic 2 testy `TypeLibraryBrowser.test.tsx` u zrodla (dopasowac fixture do `PtpireeGeneratorCertificateCatalogType`/`CatalogListItem` po zmianie certyfikatu) — obie decyzje nalezą do wlasciciela obszaru katalogu/PTPiREE, nie do tej karty. **ZAMKNIECIE (karta TYPY-TESTOW-FE, 2026-09-17, worktree z bazy `b2f59b0f`):** naprawiono U ZRODLA, NIE podniesieniem budzetu. Pomiar PRZED na tej bazie: 118 (nie 114 — inna baza niz przy poprzednim pomiarze tego wpisu). 12 bledow / 9 plikow z tego wpisu naprawione fixturami zgodnymi z realnym kontraktem (`TypeLibraryBrowser.test.tsx` x2 wg `CTCatalogType`/`VTCatalogType`/`PtpireeGeneratorCertificateCatalogType`, `converterTypesPtpiree.test.ts`, `fieldControlSelectors.test.ts`, `technical-icons.test.tsx` x2, `IssuePanelContainer.test.tsx`, `ReadinessBlockersReview.test.tsx`, `networkBuildStore.test.ts`, `stationSnFields.test.ts` x2, `derConfiguratorContract.test.ts`) — zero `as unknown as`/`@ts-expect-error`/`any`. Martwy kontrakt `MeasurementTransformerType` (zero konsumentow) skasowany z `ui/catalog/types.ts`. Naprawa fikstury MEASUREMENT_TRANSFORMER odslonila NIEZALEZNY, wczesniej zaslanianym przez inny blad w tym samym literale, brak 5 kluczy `TypeCategory` w `catalogByCategory` (SURGE_ARRESTER/SHUNT_CAPACITOR/BRANCH_POLE/ZKSN/PTPIREE_CERTIFICATE) — uzupelniony pustymi listami (zadna z tych zakladek nie jest klikana w tym pliku testow). Pole `ScreenCanonDefinition.icon` zwezone z `TechnicalIconName` na nowy eksport `ScreenIconName` (`technicalIconRegistry.tsx`) — 39 realnie uzytych wartosci ikon ekranu juz bylo w tym podzbiorze, zero brakujacych etykiet PL. Pomiar PO: 106 (guardem, `comm` linia-po-linii PRZED/PO potwierdza wylacznie 12 usuniec, 0 dodan). `BUDZET_BLEDOW_POZA_BRAMKA` obnizony 112 -> 106 w `scripts/tsconfig_gate_guard.py`. Dowody: `npm run type-check` 0/0, `npm run lint` 0/0, `python scripts/tsconfig_gate_guard.py` rc=0, `vitest run` 9/9 dotknietych plikow zielone (78 testow), `pytest scripts/test_tsconfig_gate_guard.py` 11/11, `pytest scripts` (komplet self-testow guardow) 1016/1016. | U2 | zamkniety |
| V12T-017 | UI (inspektor, zakladki Wyniki/Dowody) | `ui2/adapters/inspectorAdapter.ts::mapowanieObiektuInspektora` zwraca `obiekt.wyniki: []` i `obiekt.dowody: []` dla KAZDEGO elementu — adapter dostaje na wejsciu WYLACZNIE `snapshot`+`id` (model ENM), bez kontekstu biegu. POMIAR (2026-09-16): zero odwolan do `activeRunId`/wynikow biegu w `mapowanieObiektuInspektora`; jedyny kontrakt „element -> dowod" w repo dzialajacy dzis jest INNY i NIEZWIAZANY — `ui2/wyniki/ocena/EkranOceny.tsx` ma WLASNY, dzialajacy `element.dowod: {element_id, run_id} | null` (2-argumentowy `onOtworzDowod(elementRef, runId)`), zasilany z kontraktu oceny gotowosci (osobne zrodlo danych, osobny wlasciciel karty — `ui2/wyniki/**`). `otworzDowodInspektora` (`ui2/adapters/inspectorAdapter.ts`) jest REALNA nawigacja (nie zaslepka) wpieta w `AppRoot.tsx:onOtworzDowod={otworzDowodInspektora}` — ale przycisk „Pokaz dowod" w `InspectorPanel`/`TabDowod` jest dzis NIEOSIAGALNY klikiem, bo lista `dowody` nigdy nie ma elementow. | Zakladka „Dowody" inspektora ogolnego (dowolny element, dowolna przestrzen) jest martwa mimo realnej nawigacji pod spodem — inzynier nie ma z niej drogi do dowodu, mimo ze mechanizm docelowy istnieje i dziala z INNEGO ekranu (ocena). | Zamkniete (2026-09-17, drzewo scalone trzech kart): hook `useObiektInspektora` dokleda do projekcji modelu kontekst AKTYWNEGO przebiegu z tych samych store-ow, ktore zasila warstwa integracyjna warsztatu wynikow (`ui2/spaces/wyniki/useWpiecieWynikow`): rozplyw z `ui/power-flow-results/store`, zwarcia z `ui/results-inspector/store`. Element dostaje sekcje wynikow (wartosci WPROST z kontraktu, formatowanie bez przeliczania) i pozycje dowodu WYLACZNIE gdy wystepuje w wyniku biegu — czyli dokladnie wtedy, gdy ma slad WHITE BOX, do ktorego prowadzi `otworzDowodInspektora`; swiezosc dowodu z jednego zrodla `useSwiezoscNaglowka`. Czysta funkcja `mapowanieObiektuInspektora` pozostaje funkcja MODELU. Testy klasy (iloczyn cech rodzaj biegu x obecnosc elementu w wyniku, 7 nowych w `inspectorAdapter.test.tsx`, 37/37 zielone): brak biegu, szyna w rozplywie, galaz w rozplywie, zwarcie po `element_id`, oba biegi naraz, element spoza wyniku, pole `null` w kontrakcie (zero fabrykowanego wiersza). | U2 | zamkniety |

## Zamknięcia V12-CANON z 2026-04-27

| Kod | Status | Decyzja | Pliki zmienione | Testy | Data zamknięcia | Uwagi |
|---|---|---|---|---|---|---|
| V12-CANON-WORKSPACE-SCREEN-REMAP | ZAMKNIĘTY | Router workspace przepięty na `screenCanonRegistry.ts`; stare znaczenia ekranów przeniesione do `legacyAliases`, a aktywne mapowania `E-00..E-39`, operacje modelowania i widoczność SLD są pobierane z rejestrów kanonicznych. | `frontend/src/ui/workspace/screenCanonRegistry.ts`, `frontend/src/ui/workspace/types.ts`, `frontend/src/ui/workspace/WorkspaceSurfaceRouter.tsx`, `frontend/src/ui/workspace/WorkspaceOperationalBar.tsx`, `frontend/src/ui/contracts/frontend-shell.ts`, `frontend/src/ui/contracts/verification.ts`, `frontend/src/ui/topology/modals/operationSurfaceRegistry.ts`, `frontend/src/ui/network-build/networkBuildStore.ts`, `frontend/src/ui/network-build/ProcessPanel.tsx`, `frontend/src/ui/sld/SldEditorPage.tsx`, `frontend/src/ui/sld/sldDetailLevel.ts` | `npm run type-check`; `npm test -- --run src/ui/workspace/__tests__/screen-canon-registry.test.ts src/ui/workspace/__tests__/workspace-screen-router.test.ts src/ui/workspace/__tests__/screen-legacy-aliases.test.ts src/ui/workspace/__tests__/screen-labels-polish.test.ts src/ui/workspace/__tests__/screen-registry-coverage.test.ts`; `npm test -- --run src/ui/workspace/__tests__/workspaceContractsV125.test.ts src/ui/workspace/__tests__/workspaceShellV125.test.tsx`; `npm run test:e2e -- e2e/catalog-enforcement.spec.ts --reporter=line`; `npm run test:e2e -- e2e/sld-editor-real-backend-flex.spec.ts --reporter=line` | 2026-04-27 | Brak aktywnego długu; brak placeholderów w routerze ekranów; aliasy legacy są jawne i testowane, a transformator SN/nN pozostaje widoczny jako element szkieletu topologii. |
| V12-CANON-ADVANCED-SOLVERS | ZAMKNIĘTY | Zaawansowane solvery spięte przez `solver_capability_registry`; kanoniczny PF obsługuje NR, GS diagnostyczny i FD wydajnościowy, zwarcia dopuszczają SC_3F/SC_1F/SC_2F/SC_2F_G, a FRT/LVRT/HVRT i stabilność dynamiczna mają wynik, proof i raportowalność. | `backend/src/application/solvers/solver_capability_registry.py`, `backend/src/api/solver_capabilities.py`, `backend/src/api/main.py`, `backend/src/enm/canonical_analysis.py`, `backend/src/api/fault_scenarios.py`, `backend/tests/test_advanced_solver_capability_registry.py`, `backend/tests/test_frt_lvrt_hvrt_compliance.py`, `backend/tests/test_dynamic_stability_reference.py`, `backend/tests/test_load_flow_canonical_solver_modes.py` | `poetry run pytest tests/test_advanced_solver_capability_registry.py tests/test_frt_lvrt_hvrt_compliance.py tests/test_dynamic_stability_reference.py tests/test_load_flow_canonical_solver_modes.py -q`; `poetry run pytest tests/test_power_flow_gauss_seidel.py tests/test_power_flow_fast_decoupled.py tests/test_short_circuit_iec60909.py -q` | 2026-04-27 | Brak aktywnego długu; rejestr zdolności nie używa statusu niedostępności, a każdy wpis ma `implemented`, proof support i status raportowy. |
| V12K-325-FLAK-WSPOLBIEZNOSC | ROZSTRZYGNIETY (2026-09-16, karta CI-WSPOLBIEZNOSC) | Test `tests/api/test_wspolbieznosc_biegow.py::test_odczyt_nie_czeka_na_bieg_analizy` jest CHWIEJNY pod obciazeniem maszyny. Asercja `koniec_odczytu < koniec_biegu` traktuje „odczyt skonczyl sie po biegu" jako dowod ZABLOKOWANEJ petli zdarzen, ale przy rywalizacji o CPU (GIL) watek odczytu bywa zdjety z procesora i konczy sie pozniej mimo NIEZABLOKOWANEJ petli — oba przypadki wygladaja tak samo. POMIAR (2026-08-06, ta sama maszyna): pelna suita na galezi karty PREFIKSY — bieg 1: 2 failed / 8313 passed (329,96 s); bieg 2: 0 failed / 8315 passed (317,84 s); ta sama para izolowanie: 5 passed. Pelna suita na bazie f6be9185: 0 failed / 8311 passed (313,27 s). BRAK MECHANIZMU zwiazku ze zmianami karty PREFIKSY: `/api/health` obsluguje `health_router` wpiety PRZED przeniesionymi routerami (kolejnosc skanowania tras niezmieniona, 299 tras przed i po), a zmiany w `analysis_dispatch/service.py` i `unified_runs.py` USUWAJA prace (skasowana doklejka prefiksu), nie dokladaja. PROPONOWANA NAPRAWA: rozroznic blokade od jitteru schedulera po CZASIE TRWANIA odczytu (petla zablokowana => czas odczytu ~ pozostaly czas biegu; petla wolna => ~ms niezaleznie od przeplotu), zamiast po kolejnosci znacznikow konca. | `backend/tests/api/test_wspolbieznosc_biegow.py` | `poetry run pytest tests/api/test_wspolbieznosc_biegow.py -q` (izolowanie: zielony); pelna suita x2 (1 czerwony, 1 zielony) | 2026-08-06 | Dlug NIE pochodzi z karty PREFIKSY — wykryty przy jej bramkach i zapisany zamiast przemilczenia. Naprawa nalezy do osi wspolbieznosci programu 10x (wlasciciel kontraktu tego testu); przepisywanie asercji wspolbieznosci w karcie o prefiksach trasy byloby zmiana kontraktu cudzego programu na podstawie jednego zaobserwowanego flaka. ROZSTRZYGNIECIE (2026-09-16, karta CI-WSPOLBIEZNOSC): proponowanej naprawy po CZASIE TRWANIA odczytu NIE wdrozono — kazdy pomiar czasu pod rywalizacja o GIL okazal sie zalezny od maszyny (trzy pokolenia miar, docstring modulu); oba testy sondujace (`test_odczyt_nie_czeka_na_bieg_analizy`, `test_lekkie_zadanie_przechodzi_w_trakcie_biegow`) zastapione JEDNA bramka deterministyczna `test_lekkie_zadanie_obsluzone_gdy_biegi_stoja_w_oknie_solvera[rozplyw|zwarcie][K=1|K=10]`: biegi parkuja na spotkaniu w jedynym dyspozytorze fizyki (`_wykonaj_analize_biegu`), sonda musi wrocic PRZED zwolnieniem; iniekcja `async def execute_run` — 4/4 czerwone z nazwana przyczyna, kod poprawny 6x4 zielone. Wiersz CI-WSPOLBIEZNOSC w `REJESTR_KONFLIKTOW.md`. |
