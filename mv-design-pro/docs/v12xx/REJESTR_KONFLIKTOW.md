# Rejestr konfliktow V12.xx

Status: aktywny  
Cel: jawne rozstrzyganie sprzecznosci miedzy promptem, dokumentami aktywnymi, archiwum i kodem

## Regula pierwszenstwa

1. Najnowsze polecenie uzytkownika.
2. Kanon V12.xx w `docs/v12xx/KANON_V12_XX.md`.
3. Zasada SLD jako osi systemu.
4. Zasada ENM jako jedynego zrodla prawdy.
5. Zasada wyniku z uzasadnieniem.
6. Zasada 100% polskiego UI.
7. Aktywne dokumenty V12.5 wymienione w indeksie kanonicznym.
8. Dokumenty historyczne i archiwalne.

## Tabela konfliktow

| Kod | Konflikt | Zrodla | Decyzja | Wplyw na architekture | Wplyw na migracje | Koszt |
|---|---|---|---|---|---|---|
| V12K-001 | Aktywny kanon V12.5 w indeksie vs finalny kanon V12.xx | `INDEX_KANONICZNY.md`, plan V12.xx | `docs/v12xx/` staje sie nadrzednym kanonem kierunkowym. V12.5 zostaje fundamentem przejsciowym. | Indeks ma wskazywac V12.xx jako warstwe nadrzedna. | M0 identyfikuje zaleznosci V12.5. | niski |
| V12K-002 | Katalog `docs/spec` zawiera luki historyczne, ale nie jest aktywnym kanonem | Archiwum, audyty spec-vs-code | Kazde uzycie tresci z katalogu `docs/spec` wymaga wpisu konfliktu i decyzji. | Zmniejsza ryzyko drugiego kanonu. | M0 oznacza materialy historyczne. | niski |
| V12K-003 | Obecne `enm_version=1.0` vs wymagane ENM v2.0 | Kod ENM, plan V12.xx | ENM v2.0 powstaje przez migracje M0->M4. | Dodaje byty wariantow, migawek lacznikowych, profili i automatyki. | M1 projekcja, M2 single-write, M3 odciecie, M4 czyszczenie. | wysoki |
| V12K-004 | Lokalny stan formularzy vs ENM jako prawda | UI, plan V12.xx | Draft UI jest nietrwalym stanem roboczym. Tylko walidowany zapis trafia do ENM. | Formularze dostaja kontrakt draft vs committed. | M0 znajduje formularze z ryzykiem lokalnej prawdy. | sredni |
| V12K-005 | Wariant pracy vs migawka stanow lacznikowych | Plan V12.xx, obecne przypadki | Wariant jest scenariuszem, migawka lacznikowa jest wykonawczym stanem obliczenia. | Wynik referencjonuje oba byty. | Migracja rozdziela pola i adaptery. | sredni |
| V12K-006 | Trzy solvery rozplywu jako rownorzedne vs jeden wynik kanoniczny | Wymaganie NR/GS/FD | Newton-Raphson jest kanoniczny; GS diagnostyczny; FD wydajnosciowy przy warunkach stosowalnosci. | Result contract zawiera solver mode i applicability. | Testy porownawcze w M2. | sredni |
| V12K-007 | Dark SCADA ekranowy vs raporty i wydruki | UI, raporty | Ekran ma dark SCADA, eksport ma jasny techniczny motyw. | Wspolna semantyka kolorow, osobne renderery. | Brak migracji danych. | niski |
| V12K-008 | Automatyka jako dodatek do przekaznika vs byt pierwszej klasy | Material EAZ, obecny model | Automatyka jest osobnym modelem domenowym. | ENM v2.0, UI i raporty dostaja modele SPZ/SZR/SCO/FDIR i slady zadzialan. | M2 wprowadza zapis do ENM v2. | wysoki |
| V12K-009 | Wynik oparty tylko o `catalog_ref` vs reprodukowalnosc po zmianie katalogu | Katalogi, raporty | Kazdy run ma snapshot katalogowy i zmaterializowane parametry. | Result contract i raport zawieraja hash katalogu. | M1 generuje snapshoty przy nowych runach. | sredni |
| V12K-010 | Severity rozproszone w domenach vs jeden slownik blokad | Walidacje, raporty | Wspolny slownik severity jest kanoniczny. | Gotowosc i raport uzywaja jednej taksonomii. | M2 mapuje stare severity do nowego slownika. | sredni |
| V12K-011 | `CLAUDE.md` + `SYSTEM_SPEC.md` + `PLANS.md` wskazywaly `docs/spec/` jako SOURCE OF TRUTH (priorytet 1), co lamie V12K-001 (V12.xx jest kanonem nadrzednym) | `CLAUDE.md` (do 2026-05-13), `SYSTEM_SPEC.md` v4.0, `PLANS.md` v5.0 | Zaktualizowano hierarchie 2026-05-13: V12.xx wygrywa, `docs/spec/` formalnie ARCHIWALNE. `CLAUDE.md`, `SYSTEM_SPEC.md`, `PLANS.md` zaktualizowane do v4.1 / v5.1. Pointery do `docs/spec/` oznaczone jako "ARCHIVAL" w SYSTEM_SPEC § 0.1. | Brak — czysta aktualizacja meta-dokumentow. | M0 nie wymagana — zmiana czysto tekstowa. | niski |
| V12K-012 | Audyty zamkniete (24+ plikow w `docs/audit/` + 9 w `docs/audits/` duplikacie) blokuja nawigacje aktywnego kanonu | Inwentaryzacja `DOC_INVENTORY_2026-05.md` | Fizyczne przeniesienie do `docs/audit/archive/2026-05/`. Aktywne audyty pozostaja w `docs/audit/`. Duplikat `docs/audits/` skonsolidowany do `docs/audit/`. | Brak — archiwum jest tylko porzadkujace. | M0 dokumentuje archiwizacje. | niski |
| V12K-013 | Frontend SLD odbierany jako "atrapa z klockow" mimo kompletu features (3 rownolegle pipeline'y, brak port-based routing, brak LOD, brak eksportu) | Audyt `AUDYT_BRAKI_2026-05.md` § 7 | Stworzenie `SLD_INDUSTRIAL_SPEC_v1.md` + `PLAN_SLD_REWORK.md` (fazy F1-F5). Konsolidacja `GpzSwitchgearRenderer.tsx` (3392 linii) + `GpzCanonicalRenderer.tsx`. Implementacja port-based routing, LOD, eksportu, ring/double busbar primitives. | UI klasy przemyslowej (ETAP/DIgSILENT/ABB grade). | F1: biblioteka symboli IEC 60617; F2: layout engine z port routing; F3: LOD + warstwy; F4: overlay redesign; F5: visual regression w CI. | wysoki |
| V12K-014 | Protection blokowane na poziomie solver_input (stub SI-100 w `eligibility.py:169`) — bramka P0 dla pelnego E2E | Audyt `AUDYT_BRAKI_2026-05.md` § 8 | Implementacja protection-input-builder oparta o `protection_engine_v1.py`. Usuniecie stuba SI-100. | UI Protection nieblokujace, pelne E2E. | Plan E2E § 3.2. | sredni |
| V12K-015 | Proof packs `VDROP` + `Earthing/Ground Fault SN` brakuje (enum zdefiniowany w `ProofType`, brak implementacji w `packs/`) | Audyt `AUDYT_BRAKI_2026-05.md` § 6 | Implementacja `packs/vdrop.py` + `packs/earthing_ground_fault_sn.py`. Bramka audyt2 grounding + dobor przewodow. | Raport koncowy kompletny. | Plan E2E § 3.4. | sredni |
| V12K-021 | **RESOLVED (FALSE-POSITIVE).** APARAT_NN catalog JUZ ISTNIEJE z 14 entries (ABB SACE Emax2 / Tmax XT / Jean Muller NHR). Original symptom: endpoint `/api/catalog/lv-apparatus` (singular, BAD path) zwracal pusta liste. Real endpoint: `/api/catalog/lv-apparatus-types` (plural) zwraca 14 entries. K11 load attachment dziala po usuwaniu blokady przez `add_nn_outgoing_field(catalog_ref='cb_nn_400a')` + `add_nn_load(catalog_ref='load_mieszk_15kw'/...)`. | Iter K20-7 audit `docs/audit/visual_iteration_K20_7/REPORT.md` § 1, fix in `seed-gn20.mjs` commit 3f85fd8 | Cataloging issue closed. Documented K11 workflow w seederze. | Brak — symptom byl false-positive. | Test: 11/11 loads PASS dla GN20 K20 case. | niski |
| V12K-022 | **RESOLVED.** `add_converter_source` z `connection_variant=block_transformer` ma auto-resolve: jezeli `blocking_transformer_ref` nie podany ale `station_ref` ma exactly 1 transformator SN/nN, uzywa go jako block transformer (typowy K20 scenario). Multiple transformers w stacji -> `generator.block_transformer_ambiguous` error (jawny wymog). | Iter K20-2 audit § 3 V12K-022, fix commit (K20-13) | Auto-resolve logic w `add_converter_source` linia 2080-2120. | BESS/FW block_transformer attach dziala dla stacji z 1 TR (typowe K20). | Tests PASSED: `test_v12k_022_block_transformer_auto_resolve` + `test_v12k_022_block_transformer_ambiguous_with_multiple_trs`. | niski |
| V12K-023 | **RESOLVED.** PV connection variants `LV_BEHIND_STATION_TRANSFORMER` + `SOURCE_CONNECTION_STATION` zaalias-owane do kanonicznych (nn_side + block_transformer) w `add_converter_source` (commit iter K20-11). | Iter K20-2 audit § 3 V12K-023, `DerRenderer.tsx:43-48` | Alias mapping w `domain_operations_v2.add_converter_source` rozpoznaje 3 FE warianty i mapuje do 2 kanonicznych backend. | S08 PV LV_BEHIND_STATION + S10 PV farma SOURCE_CONNECTION akceptowane przez backend. | Test PASSED: `test_v12k_023_connection_variant_aliases`. | niski |
| V12K-024 | **RESOLVED.** FW `DEDICATED_MV_CONNECTION` zaalias-owane do `block_transformer` (commit iter K20-11). | Iter K20-2 audit § 3 V12K-024 | Alias mapping w `add_converter_source`. | S06 FW 800 kW + S20 FW 3 MW akceptowane (przy istniejacym block_transformer V12K-022). | Test PASSED (cz. V12K-023 alias test). | niski |
| V12K-025 | **RESOLVED.** PROTECTION execution dispatcher zwraca user-friendly error redirect do `/api/projects/{id}/protection-runs` z V12K-025 reference (commit 9d6c4c1). Architektoniczna decyzja: PROTECTION ma osobny pipeline (`protection_engine_v1` + protection_case_id z study_case.protection_config). | Iter K20-3 audit + commit 9d6c4c1 | Endpoint redirect z helpful detail message. | Brak — separacja intencjonalna, redirected do dedicated endpoint. | Test PASSED: `test_create_run_protection_redirects_to_proper_endpoint`. | niski |
| V12K-026 | **RESOLVED.** `SLD_CAD_SPEC_V3.md` § 8 wymienia v2 `LodPolicy` jako "ZOSTAJE (reuse)", ale `LodPolicy.createLodController` jest twardo typowana na 5 poziomow taksonomii ELEMENTOW v2 (`LodLevel=0..4`, `mini_block_overview` itd.) — niekompatybilna z 3-poziomowym kontraktem `SceneLod` (L0/L1/L2) przyjetym w spec § 7 i zaimplementowanym od F6a. Mapowanie 5→3 byloby utrata znaczenia progow, nie reuse. | `SLD_CAD_SPEC_V3.md` § 7 vs § 8, `v2/viewport/LodPolicy.ts`, F6b-2 `v3/canvas/camera.ts` | F6b-2 implementuje wlasna 3-poziomowa polityke LOD z histereza (czysta funkcja, progi 0.4/1.2 + margines 0.15 — kalibracja wizualna w F7). Matematyka viewportu (`ViewportController.ts`) REUZYTA przez import zgodnie z § 8. Recenzja Opus: odrzucenie obronne i udokumentowane (naglowek `camera.ts`). | v3 ma wlasny modul `canvas/camera.ts`; v2 `LodPolicy` do usuniecia w F8 razem z renderem v2. | Brak migracji danych. | niski |
| V12K-030 | **RESOLVED (ERRATA E1).** Spec SLD_CAD_SPEC_V3 par. 12.1 (Poprawka A1) wskazywala zrodlo danych jako `Bay.primary_devices` w snapshocie ENM — nieprawda: pole zyje na `BayBaseModel` (models.py:1033) w read-modelu field-view (`BayCanonicalModel`, budowany w locie przez field_read_model.py z danych pierwotnych); snapshot `EnergyNetworkModel.bays[]` NIE serializuje `primary_devices`; fixtura ma bays.length=0 (pola przez meta.field_specs). | Recenzja F9.2 (weryfikacja u zrodla), `backend/src/enm/models.py`, `field_read_model.py` | Errata E1 wpisana do spec par. 12.1. Ruling architekta dla F9.6: **c-2** — primary_devices to dane WYWIEDZIONE, nie denormalizowac na surowy `Bay`; domkniecie kanalu przez field-view dolaczany do payloadu snapshotu w jednym pobraniu (`attach_field_view`, api/enm.py). Szew adaptera F9.2 (defensywny odczyt z Bay) zostaje jako warunkowy — przy c-2 zostanie przekierowany na projekcje z field-view. | Adapter dostaje jeden, spojny moment danych (jedna prawda ENM, zero dual-channel). | F9.6 implementuje dolaczenie field-view + przekierowanie projekcji; fixtura testowa z niepustym primary_devices wymagana dla cell_sequence_probe (F9.3). | sredni |
| V12K-014 | **RESOLVED.** Protection-runs E2E flow zweryfikowany dla K20: (1) bind `template_rex100_oc` do study_case przez `PUT /api/study-cases/{id}/protection-config`, (2) create protection-run sc_run_id + protection_case_id, (3) execute → FINISHED w 56 ms, (4) results: 1 evaluation REX-100 IEC normal inverse, trip @ 165.6 ms dla I_fault=8109 A. | Iter K20-17 + commit | Bind template via existing endpoint + protection-runs API. | E2E PROTECTION flow operational dla K20. | Test backend protection_analysis tests PASS. | niski |
| V12K-026 | **RESOLVED (2026-07-15, decyzja architekta systemu; wlasciciel zlecil decyzje architektowi).** Slownik nowej IA (Program UI/UX 2026-07, makieta U0.6 ZATWIERDZONA przez wlasciciela) uzywa terminow "przypadek obliczeniowy" i "kreator", ktore `ui_terminology_guard` (Brief 1 par.2 + Brief 2 par.2, kanon V12.5.1) BANUJE w aktywnym UI (razem z Run/Proof/uruchomienie/migawka). Karta E1.1 dostarczyla stringi przez stale w `ui2/shell/strings.ts` (guard zielony, intencja guarda do rozstrzygniecia). | `scripts/ui_terminology_guard.py:49-77`, `docs/uiux/PROGRAM_UIUX_2026-07.md`, makieta U0.6 v6, raport wykonawcy E1.1 | PROPOZYCJA zarzadcy: nowa IA przyjmuje "przypadek obliczeniowy" + "kreator" jako kanon slownika (zatwierdzone posrednio makieta); karta E1.6 aktualizuje guard (allowlista terminow nowej IA dla `ui2/**` + docelowo calego UI po przelaczeniu powloki). Alternatywa: zmiana slownika nowej IA na terminy zgodne z Brief 1/2 (np. "scenariusz obliczeniowy") - tania, bo stringi sa scentralizowane. DECYZJA: slownik nowej IA przyjmuje "przypadek obliczeniowy" i "kreator" (naturalna terminologia branzowa, odpowiednik Study Case; zatwierdzona makieta U0.6). Guard zaktualizowany: allowlista NEW_IA_ALLOWED_TERMS dla sciezek `frontend/src/ui2/**`; pozostale bany obowiazuja wszedzie. Zastane naruszenia (V126AcademicSurface: Run/Proof) naprawione polskimi etykietami. | Slownik calego nowego UI. | E1.6 (guard) albo podmiana stringow w `strings.ts`. | niski |
| V12K-027 | Kolejnosc aparatow pola SN: literalna sekwencja dyrektywy uzytkownika (od szyny: CB->DS) vs wdrozony GPZ i praktyka rozdzielnic SN (od szyny: DS_szynowy->CB). Kandydat K-A. | Dyrektywa uzytkownika 2026-07-11, `SLD_POWER_PATH_AUDIT_2026-07.md` § kandydaci konfliktow K-A, `SLD_CAD_SPEC_V3.md` § 12.2 | Referencyjna sekwencja od szyny w dol: DS_szynowy -> CB -> CT -> (VT) -> DS_liniowy -> ES -> (SA) -> glowica kablowa -> kabel, zgodnie z praktyka rozdzielnic SN i wdrozonym GPZ. Literalna enumeracja dyrektywy (...DS->CB->szyna) czytana jest jako zasada "pole zaczyna sie od kabla/glowicy, szyna jest koncem toru", NIE jako wiazaca sasiedniosc aparatow — nadrzednym celem dyrektywy jest rzeczywisty fizyczny uklad rozdzielnicy, a rzeczywisty uklad niosa DANE (`placement`), nie przykladowa enumeracja. | `SLD_CAD_SPEC_V3.md` § 12.2 niesie kanoniczna sekwencje; `cell_sequence_probe` (§ 11.7) wymusza zgodnosc z `placement` z ENM. | Brak — decyzja czysto dokumentacyjna; kod (adapter `primary_devices`) realizowany w F9.2/F9.3 planu. | niski |
| V12K-028 | SA (ogranicznik przepiec) w torze pola wymagany przez dyrektywe uzytkownika (ustalenie #6), ale `BayPrimaryDeviceKind` (ENM) nie zawiera `SURGE_ARRESTER` — zadanie rysunku vs zakaz zgadywania (CLAUDE.md „no guessing", WHITE BOX). Kandydat K-B. | Dyrektywa uzytkownika 2026-07-11, `SLD_POWER_PATH_AUDIT_2026-07.md` § kandydaci konfliktow K-B, `SLD_CAD_SPEC_V3.md` § 12.5 | SA jest rysowany WYLACZNIE z danych. `BayPrimaryDeviceKind` zostanie rozszerzony o `SURGE_ARRESTER` w fazie F9.6 (zmiana domain/backend, mutacja modelu tylko w warstwie DOMAIN). Do tego czasu SA NIE jest rysowany z konwencji (zakaz zgadywania). | Brak zmiany architektury teraz; F9.6 doda kind do ENM domain + mirror FE (`frontend/src/types/enm.ts`). | F9.6 wymaga migracji modelu ENM (nowy kind w `BayPrimaryDeviceKind`) — backend + FE typy; do tego czasu SA nie jest renderowany. | sredni |
| V12K-029 | DER (PV/BESS/generator/farma wiatrowa) jako badge (`derBadges`) vs pelnoprawne widoczne zrodlo z dedykowanym symbolem w punkcie przylaczenia — dyrektywa uzytkownika ustalenia #2/#7. Kandydat K-D. | Dyrektywa uzytkownika 2026-07-11, `SLD_POWER_PATH_AUDIT_2026-07.md` § kandydaci konfliktow K-D, `SLD_CAD_SPEC_V3.md` § 13.1 | DER (PV/BESS/generator/farma wiatrowa) jest pelnoprawnym WIDOCZNYM zrodlem z dedykowanym symbolem w punkcie przylaczenia (realizacja F9.4); dotychczasowy badge DER (`derBadges`) znika z chwila wdrozenia F9.4. | `sources_visible_probe`/`source_symbol_probe` (§ 11.9-10) wymagaja symbolu DER na scenie; `apparatusContracts.ts` przestaje wykluczac role DER z pol po F9.4. | F9.4 zmienia adapter/scene (frontend v3) — DER przechodzi z badge na widoczny symbol zrodla; brak migracji danych ENM. | sredni |
| V12K-031 | Runda korekcyjna F9.3 (recenzja Opusa, FIX-2): spec § 14.3 literalnie zada rozroznienia sylwetek pol wejscie/wyjscie/odgalezienie/TR/sprzeglo/pomiar/DER, ale pola wejscie i wyjscie pola liniowego sa FIZYCZNIE IDENTYCZNA konstrukcja rozdzielnicy (ta sama sekwencja aparatow, § 12.2/§ 12.4) — rysowanie rozroznienia stosem/akcentem miedzy nimi falszowaloby roznice konstrukcyjna, ktorej NIE MA (wbrew nadrzednemu celowi dyrektywy: prawda fizyczna > litera zadania rysunkowego). | Recenzja F9.3 (Opus, REQUEST-CHANGES), `SLD_CAD_SPEC_V3.md` § 12.2/§ 12.4/§ 14.3, `compose/station.ts` `fieldSilhouetteClass`/`fieldSilhouettesAreInjective` | Klasy sylwetki sa zdefiniowane PER KONSTRUKCJA pola (fizyczny stos aparatow), NIE per rola kierunkowa: `LINE_IN`/`LINE_OUT`/`LINE_BRANCH`/`RMU_LINE`/`GPZ_LINE_BAY` naleza do JEDNEJ klasy rownowaznosci `line` (identyczna konstrukcja — DZIELA sygnature SWIADOMIE, to NIE jest naruszenie § 14.3); `TRANSFORMER`/`RMU_TRANSFORMER` → `transformer`; `COUPLER` → `coupler`; `MEASUREMENT` → `measurement`; `DER_PV`/`DER_BESS`/`DER_FW` → kazdy wlasna klasa (rozny symbol zrodla). Kierunek (wejscie vs wyjscie vs odgalezienie) NIE jest niesiony sylwetka — niesie go podpis § 9 (`kier. Sxx`/`odg. Sxx`) i, docelowo, strzalki przeplywu mocy § 14.2/F9.5 (prawda solverowa, nie geometria). Odgalezienie ROZNI SIE od wejscia/wyjscia AKCENTEM § 14.4 (`branchJunction`, wiekszy wezel), nie stosem — to jest wystarczajace rozroznienie wizualne dla tej klasy bez fabrykowania roznicy konstrukcyjnej w samym polu. | Zero zmiany geometrii/stosu aparatow; `field_silhouette_probe` (`compose/station.ts` `fieldSilhouettesAreInjective`) przepisany na dowod PO WSZYSTKICH rolach (`ALL_FIELD_ROLES`, nie per-stacja): kazde dwie role SPOZA tej samej klasy rownowaznosci maja ROZNE sygnatury (stos LUB akcent), kazde dwie role W tej samej klasie MOGA dzielic sygnature. | Brak migracji danych — zmiana dotyczy WYLACZNIE definicji wyroczni/dokumentacji (`compose/station.ts`, `SLD_CAD_SPEC_V3.md` § 14.3, testy). | niski |
| V12K-032 | **OTWARTY (koordynacja miedzywatkowa).** Kolizja identyfikatora V12K-026: watek SLD (`claude/sld-schema-cad-scada-rqvz73`, wpis WCZESNIEJSZY: polityka LOD v3, RESOLVED) i watek przebudowy interfejsu (`claude/power-network-design-ui-ir91mv`, wpis: slownik nowej IA vs ui_terminology_guard, RESOLVED) nadaly ten sam numer roznym konfliktom na rownoleglych galeziach. | `git show origin/claude/power-network-design-ui-ir91mv:mv-design-pro/docs/v12xx/REJESTR_KONFLIKTOW.md` (linia 42) vs ten plik (V12K-026), kontrakt `docs/sld/SLD_PROTECTION_MARKING_COORDINATION_2026-07.md` par. 3 | PROPOZYCJA (do potwierdzenia przez watek UI): wpis watku UI otrzymuje przy scaleniu numer V12K-040; rezerwacja zakresow: SLD = V12K-026..039, UI = V12K-040..059, kolejne watki biora nastepne dziesiatki; renumeruje watek scalany POZNIEJ. | Jednoznacznosc rejestru po scaleniu galezi. | Renumeracja jednego wpisu przy scaleniu galezi UI; zero zmian kodu. WERYFIKACJA 2026-07-16 (F11.5, `git fetch origin`): galaz UI na 17f93aea nadal niesie SWOJE V12K-026 (RESOLVED, slownik IA) i NIE zawiera kontraktu koordynacyjnego `SLD_PROTECTION_MARKING_COORDINATION_2026-07.md` — potwierdzenia par. 6 nieodhaczone; kolizja REALNA, propozycja renumeracji (UI -> V12K-040) pozostaje w mocy do scalenia. | niski |
| V12K-033 | K-D2-A: ES/VT/SA jako element boczny (lateral) vs V12K-027 (kolejnosc aparatow pola z `placement` jako pionowy stos). | Dyrektywa D2 wlasciciela 2026-07-15, `SLD_ENGINEERING_CORRECTNESS_AUDIT_2026-07.md` § D2-5/D2-6 kandydat K-D2-A, `SLD_CAD_SPEC_V3_AMENDMENT_A3_DRAFT.md` §A3-DEC-1, `SLD_CAD_SPEC_V3.md` §18.1/§18.2 | Opcja A: ES/VT/SA sa render-side galezia boczna od wezla toru glownego (`placement`/`section_side` wystarczaja, bez zmiany DOMAIN); tor GLOWNY liczony z pominieciem ES/VT/SA — `cell_sequence_probe` (§12.1) przedefiniowana, laterale maja wlasne wyrocznie `earth_switch_lateral_probe`/`vt_parallel_probe` (§18.1/§18.2). `kind` ES/VT/SA jest jawnym wyjatkiem od reguly "stos = sekwencja placement" z V12K-027 — doprecyzowanie, nie sprzecznosc. Blokada logiczna (interlock ES) = adnotacja konwencyjna bez danych; nowe pole DOMAIN interlock to kandydat przyszlej rundy (D6). | `SLD_CAD_SPEC_V3.md` §18.1/§18.2 niosa regule; render v3 (`compose/station.ts`, `buildBayStack`) wymaga wyjecia ES/VT/SA z pionowego stosu — faza F10.1. | Zero migracji danych (geometria render-side); symbole ES/VT/SA wymagaja portu bocznego/nowego wariantu glifu (V12K-037). | sredni |
| V12K-034 | K-D2-B: typ stacji wyprowadzony z topologii vs dana `Substation.station_type` jako zrodlo rysunku. | Dyrektywa D2 wlasciciela 2026-07-15, audyt § D2-3 kandydat K-D2-B, `SLD_CAD_SPEC_V3_AMENDMENT_A3_DRAFT.md` §A3-DEC-3, `SLD_CAD_SPEC_V3.md` §19.3 | Opcja A: rysunek pokazuje typ stacji WYPROWADZONY z topologii (liczba pol liniowych, `line_runs`, obecnosc sprzegla); dana `station_type` (`domain_ops_models.py:724`) sluzy WYLACZNIE walidacji (ostrzezenie o niezgodnosci, bez cichego nadpisania rysunku). Zgodne z prymatem danych §12.1 — topologia jest dana pierwotna, `station_type` jest wtorna adnotacja. | `classifyTopologicalType` (`enmToSldAdapter.ts`) zastapiony wyprowadzeniem z topologii — faza F10.2; wyrocznia `station_type_topology_probe`. | Brak migracji danych (zmiana czysto adapter/analysis); walidacja niezgodnosci `station_type` nowa (missingData). | niski |
| V12K-035 | K-D2-C: identyfikator per-aparat Q/T vs obecny `bayApparatusDesignation` (Q jako etykieta calego pola). | Dyrektywa D2 wlasciciela 2026-07-15, audyt § D2-3 kandydat K-D2-C, `SLD_CAD_SPEC_V3_AMENDMENT_A3_DRAFT.md` §A3-DEC-2, `SLD_CAD_SPEC_V3.md` §19.1 | Opcja A: nowe pole `BayPrimaryDevice.designation` (DOMAIN backend, mutacja tylko w warstwie DOMAIN, faza F10.6-DOMAIN) niesie identyfikator per-aparat; fallback konwencji (Q dla lacznikow, T dla transformatora) WYLACZNIE ze znacznikiem `data-designation-source="konwencja"`. `bayApparatusDesignation` przestaje etykietowac pole — pole dostaje oznaczenie FUNKCYJNE (liniowe/TR/sprzeglo/pomiar/...), aparaty dostaja Q/T przy symbolu. | `SLD_CAD_SPEC_V3.md` §19.1 niesie regule; `compose/directions.ts` (`bayApparatusDesignation`) zmienia semantyke w F10.2 (fallback), pelne dane po F10.6-DOMAIN. | F10.6 wymaga migracji modelu ENM (nowe pole `BayPrimaryDevice.designation`) — backend + FE typy; do tego czasu identyfikator z konwencji ze znacznikiem. | sredni |
| V12K-036 | K-D2-D: linia pomiarowa CT-przekaznik (§20.1, D2-7) vs §17.1 "okrag przekaznika nie uczestniczy w ciaglosci toru mocy". | Dyrektywa D2 wlasciciela 2026-07-15, audyt § D2-7 kandydat K-D2-D, `SLD_CAD_SPEC_V3_AMENDMENT_A3_DRAFT.md` §A3-DEC-4, `SLD_CAD_SPEC_V3.md` §17.1/§20.1 | DOPRECYZOWANIE, nie konflikt: okrag CT JEST elementem TORU MOCY (poprawny per IEC 60617, pozostaje w ciaglosci elektrycznej, bez zmian §17.1); linia pomiarowa CT-przekaznik jest warstwa WTORNA adnotacji (analogicznie do linii trip przekaznik-wylacznik) — jawnie wylaczona z `continuity_probe`/`port_probe` toru mocy. Dwie linie wtorne (pomiar `ct_ref` + trip `breaker_ref`) renderowane TERAZ (F10.5) na obecnym modelu; przekladnie CT/VT, uklad I0/open-delta, strefa 87T — kolejna runda DOMAIN. | `SLD_CAD_SPEC_V3.md` §20.1 niesie wyrocznie `secondary_link_duality_probe`; §17.1 bez zmian tresci, jawnie potwierdzony brak sprzecznosci. | Brak migracji danych (`ct_ref` juz istnieje); render dwoch linii — faza F10.5. | niski |
| V12K-037 | K-D2-E: aparaty jednoportowe ES/VT/SA jako galaz boczna wymagaja portu bocznego — obecne glify (`earthSwitch`/`voltageTransformer`) maja WYLACZNIE port `top`. | Dyrektywa D2 wlasciciela 2026-07-15, audyt § D2-4/D2-5/D2-6 kandydat K-D2-E, `SLD_CAD_SPEC_V3_AMENDMENT_A3_DRAFT.md` §A3-DEC-1, `SLD_CAD_SPEC_V3.md` §18.1/§18.2 | Zmiana ADDYTYWNA w bibliotece symboli: aparat jednoportowy (ES/VT/SA) dostaje port BOCZNY (lub nowy wariant glifu) do zaczepienia odgalezienia; regula: aparat jednoportowy = boczny z definicji, zakaz w torze szeregowym (spina §18.1/§18.2). Realizacja render-side, bez zmiany DOMAIN. | `v3/symbols/defs.ts`/`glyphs.tsx` — nowy port/wariant symbolu ES/VT/SA — faza F10.1. | Brak migracji danych; zmiana czysto renderowa (biblioteka symboli). | niski |
| V12K-038 | **ROZSTRZYGNIETY (2026-07-16, decyzja architekta; wlasciciel przekazal pelnomocnictwo "podejmuj maksymalne decyzje").** Inwentarz spec par. 10 ("edycja CAD drag/bend/snap" oraz "workflow append/split") wymienia jako funkcje v2 pozycje, ktore audyt osiagalnosci F11.4/F12 wykazal jako NIEOSIAGALNE produkcyjnie: CadOverlay renderowany tylko przy propie cadOverlay (zero produkcyjnych wolajacych w calym src/), RouteEditor.addBend/dragBend/removeBend/lockRoute i Snap wolane wylacznie w testach; WorkflowOrchestrator/AppendOnEndpointController/ConsciousSplitController konstruowane wylacznie w testach, SplitPreviewPanel bez produkcyjnego przekazania splitPreviewState (App.tsx:1431/1454, WorkspaceSurfaceRouter.tsx:3135 bez propsow). Konflikt: checklista F8c pkt 1b/5 bramkowala usuniecie v2 parytetem wobec martwego kodu. | Audyt F11.4 (wpis w SLD_CAD_REBUILD_PLAN_V3.md) + weryfikacja grep 2026-07-16; spec par. 10.1 ARCH-1/ARCH-2 | ROZSTRZYGNIECIE: Polityka Zero Regresji porownuje funkcjonalnosc OSIAGALNA — bramki 1b/5 UCHYLONE z dowodem; jedyna osiagalna edycja v2 (akcje domenowe menu/drawer) podlega parytetowi ARCH-3 (wykonawca akcji na v3, wdrazany); reczna edycja geometrii = przyszla funkcja nowa wg reguly nadpisan-jako-danych (spec par. 10.1 ARCH-1). Usuniecie sciezki renderu v2 otwarte po ARCH-3 + pelnej migracji budowniczych drawera (ARCH-4). | Spec par. 10/10.1, checklista F8c planu. | Kasacja martwego szkieletu (CadOverlay/RouteEditor/Snap-edycja/workflow) razem ze sciezka renderu v2; zero utraty funkcji osiagalnych. | sredni |
| V12K-039 | **ROZSTRZYGNIETY (2026-07-16, decyzja architekta, dyrektywa D3).** Spec par. 14.4 (akcent odgalezienia jako wezel-kropka branchJunction o gabarycie > junction) vs NOWY par. 22.1 (kropka wezlowa WYLACZNIE na realnym wezle elektrycznym ENM; skrzyzowanie bez polaczenia = mostek). Pomiar F13.2: kropka-akcent stoi na zgieciu jogu kanalu odgalezienia, 24 px od punktu, w ktorym pion kanalu PRZECINA (bez polaczenia — odczep elektryczny jest w polu odgalezym stacji-origin) przeslo magistrali innej pary stacji — FALSZYWY odczyt wezla na magistrali (D3-14). | Audyt D3 (P-1) + sonda geometryczna nadzorcy 2026-07-16 (dot center (8256,368) vs przeciecie (8256,344)); buildScene.ts lateralRow/branchJunction | ROZSTRZYGNIECIE: par. 22.1 WYGRYWA (fizyka obrazu nadrzedna). Akcent-kropka USUNIETY; wymog rozpoznawalnosci odejscia lateralu (par. 14.4) realizuja: podpis kierunkowy pola (line_bay_caption_probe) + sylwetka par. 14.3 + mostki par. 22.1 czyniace przeloty jednoznacznymi. branch_accent_probe ZASTAPIONA przez junction_dot_probe (obustronna: wezel=>kropka, kropka=>wezel) — wyrocznia SILNIEJSZA, nie oslabiona. | Spec par. 14.4 (nota o zastapieniu), par. 22.1; buildScene.ts; acceptance. | Kasacja symbolu akcentu ze sceny (render-side); zero zmian danych. | sredni |
| V12K-031-A | **KOREKTA V12K-031 (2026-07-17, recenzja NO-GO wlasciciela pkt 5 — WIAZACA).** Przeslanka rulingu V12K-031 ("wejscie/wyjscie/odgalezienie = fizycznie identyczna konstrukcja") ZOSTAJE dla pol WYLACZNIKOWYCH, ale jej rozszerzenie na RMU_LINE bylo BLEDEM zmierzonym przez wlasciciela: pole liniowe stacji KOMPAKTOWEJ (RMU: rozlacznik+ES+glowica) to INNA technologia rozdzielnicy niz pole wylacznikowe GPZ (DS+CB+CT+DS+ES+glowica) — wspolna klasa sylwetki fabrykowala CB+CT w stacjach 630 kVA ("uniwersalny szablon skopiowany z pola liniowego GPZ", recenzja pkt 5). | Recenzja NO-GO wlasciciela 2026-07-17 pkt 5/6, `docs/sld/SLD_REVIEW_NO_GO_2026-07-17.md`, `SLD_CAD_SPEC_V3.md` par. 12.4 (slownik szablonow per technologia) | Klasy sylwetek ROZDZIELONE: `rmu_line` (RMU_LINE: loadBreakSwitch+ES+cableHead — NOWY dedykowany glif rozlacznika, IEC 60617 switch-disconnector, kasuje aproksymacje LOAD_SWITCH→disconnector) i `rmu_transformer` (RMU_TRANSFORMER: fuseSwitch+ES+TR2W — ES obowiazkowy, pomiar recenzji pkt 6) sa ODREBNYMI klasami rownowaznosci; `line` zostaje dla LINE_IN/LINE_OUT/LINE_BRANCH/GPZ_LINE_BAY (intencja V12K-031 zachowana w obrebie JEDNEJ technologii). Prymat danych par. 12.1 nietkniety: primary_devices zawsze wygrywa z konwencja. | `compose/apparatusSequence.ts` (szablony), `compose/station.ts` (`fieldSilhouetteClass`), wyrocznia `bay_template_probe` (`bayTemplateGaps`, buildScene.ts) z negatywem w acceptance; `symbolIdForPrimaryDeviceKind` LOAD_SWITCH→loadBreakSwitch (mapowanie kind→symbol 1:1, dawna udokumentowana wieloznacznosc disconnector⇐{DS,LOAD_SWITCH} SKASOWANA). | Zero migracji danych (konwencja render-side); baseline'y geometrii przeliczone (krotsze stosy pol RMU). | niski |
| V12K-060 | **ROZSTRZYGNIETY (2026-07-17, dyrektywa wlasciciela „Globalna integracja referencji SLD”, 12 punktow).** Dotad istnialy ROWNOLEGLE definicje skladu pol: slownik konwencji par. 12.4 (apparatusSequence.ts), kanoniczne szablony kreatora (bay_templates.py), rodziny rozdzielnic (catalog/switchgear) — bez wspolnej semantyki walidacyjnej (required/optional/forbidden/kolejnosc) i bez walidacji na zywo. Dyrektywa: referencje (normy IEC, producenci, OSD) = Source of Truth calego modulu SLD; „nie moze istniec drugi sposob definiowania pol”. | Dyrektywa wlasciciela 2026-07-17 (3x powtorzona), docs/sld/REFERENCE_ENGINE_SPEC_V1.md, backend/src/reference_engine/ | Nowy modul reference_engine/ z wersjonowanymi pakietami JSON (iec60617, iec62271, elektrometal_e2alpha, siemens_8djh, schneider_sm6, abb_unigear, abb_safering, osd_enea). Profile pol zyja WYLACZNIE w pakiecie iec62271 (rejestr odrzuca druga definicje); slownik par. 12.4 i szablony kreatora sa IMPLEMENTACJAMI profili (parytet egzekwowany testami z negatywami: apparatusSequence.referenceParity.test.ts, test_reference_packs.py, test_pack_parity.py — mirror frontendu bajtowo identyczny). Dane rodzin producentow maja jedno zrodlo (SWITCHGEAR_FAMILY_REGISTRY) — pakiety trzymaja tylko ref. Walidacja NA ZYWO: walidator ENM emituje kody reference.* przy kazdej walidacji (spec par. 6); silnik zgodnosci + Reference Score per pakiet (spec par. 7/8). KOREKTA FAKTOGRAFICZNA: e2TANGO to sterownik polowy Elektrometal (K-3.2.8), NIE rodzina rozdzielnic — pakiet producenta oparty na rzeczywistej rodzinie e2ALPHA (spec par. 11). Numeracja: zakres SLD V12K-026..039 wyczerpany, V12K-040..059 zarezerwowane dla watku UI (V12K-032) — watek SLD bierze nastepna dziesiatke od V12K-060. | REFERENCE_ENGINE_SPEC_V1.md (calosc), enm/validator.py (kody reference.*), canonical_fallback.py (filtr slownika rodziny w kreatorze), api/reference_engine.py, Inspektor ENM zakladka Referencje | Style renderowania producenckie (pkt 6 dyrektywy) = PLAN z warunkiem danych (zweryfikowane wzorniki graficzne producentow — regula „nie fabrykuj danych producenta”); pelne wymagania OSD wymagaja przeniesienia kompletnych Zeszytow Enea Operator do danych pakietu. | sredni |
| V12K-040 | **ROZSTRZYGNIĘTE (opcja B, 2026-07-17; PIERWOTNIE zapisany jako V12K-027 w wątku UI przed dotarciem rezerwacji zakresów — RENUMERACJA przy konsolidacji gałęzi 2026-07-17, zakres UI = V12K-040…059, decyzja właściciela); pierwotna eskalacja D8→K1.** Konwencja znaku mocy biernej w `PowerFlowResult` (`slack_q`, `branch_results.q_from/q_to`) jest niespojna z intuicja "mocy biernej widzianej przez siec": poprawnie znakowany kondensator bocznikowy (+jB, znak POTWIERDZONY diagnostyka K1 — zgodny z ladownoscia linii, podnosi napiecie) przy odbiorze indukcyjnym ZWIEKSZA \|q\| galezi i \|slack_q\| zamiast je redukowac; wstrzykniecie Q jako stala moc PQ ksieguje sie przeciwnym znakiem slack_q niz shunt Y-bus. Skutek: cosφ punktu przylaczenia liczony z przeplywow galezi maleje przy kompensacji odbioru indukcyjnego. Dowod liczbowy: `tests/application/analyses/test_diagnostyka_znaku_shunt.py` (5 testow, przypadek minimalny + rachunek reczny). | Raport K1 (karta `docs/uiux/karty/U4_K1_DIAGNOSTYKA_ZNAKU_SHUNT.md`), `power_flow_result.py:230-268`, `enm/canonical_analysis.py:1119-1166` | DECYZJA WŁAŚCICIELA (2026-07-17) = **opcja B**: `PowerFlowResult` pozostaje FROZEN (bez zmian znaczenia pol); solver liczy poprawnie (dowod K1); konwencja solvera przyjeta jako obowiazujaca; niespojnosc naprawiana WYLACZNIE w warstwie aplikacyjnej przez dedykowany ADAPTER konwencji znakow (`application/analyses/konwencja_mocy.py`, karta `docs/uiux/karty/U4_K2_ADAPTER_KONWENCJI_Q.md`). Kanoniczna konwencja aplikacyjna: P>0 pobor czynnej, Q>0 pobor indukcyjny, Q<0 pojemnosciowa (kompensacja). D8 liczy cosφ z Q netto PO adapterze (kondensator na odbiorze indukcyjnym PODNOSI cosφ). Warunek odblokowania okna P42: 7 testow kontraktowych (bez/czesciowa/pelna/przekompensowanie/przeplyw odwrotny OZE-BESS/bilans q_from-q_to/bilans ukladu). Opcja A (zmiana FROZEN API) tylko gdyby wykazano wewnetrzna niespojnosc samego `PowerFlowResult` — NIE wykazano. **AKTUALIZACJA K3 (konsolidacja 2026-07-17, karta `docs/uiux/karty/U4_K3_REKALIBRACJA_PO_F98.md`)**: pierwotna przyczyna anomalii = odwrocony znak canonical PF pipeline (podwojna negacja, obciazenia jako generacja), naprawiony w watku SLD runda F9.8 (commit `6508c12f`, 2026-07-15). Po F9.8 anomalia shuntu ZNIKLA (kompensacja ZMNIEJSZA przeplyw bierny galezi); decyzja opcja B (adapter + rozdzial cosφ przekroju/punktu) POZOSTAJE w mocy, rekalibracji ulegla regula odwzorowania (tozsamosc → NEGACJA konca incydentnego) i wartosci odniesienia (przemierzone). Dodatkowo K3 wykryl i naprawil defekt nastepczy F9.8: `canonical_analysis.py:1256` montowal `p_injected_mw` szyn PQ w konwencji poboru wbrew FROZEN kontraktowi („ujemna = pobor") — negacja przy montazu wyniku przywraca kontrakt (dowod: testy D7 przechodza bez zmian asercji). | Analizy czytajace q_from/q_to/slack_q (dobor kompensacji; przyszle). | Testy diagnostyczne K1 dokumentuja stan faktyczny (wartosci po F9.8, rekalibracja K3). | wysoki |
| V12K-041 | **ROZSTRZYGNIETY (2026-07-18, dyrektywa wlasciciela: stare ekrany nie moga byc kanoniczne).** Napiecie interpretacyjne: kanon V12.xx rejestruje ekrany (ScreenCode, componentKey, coverageMatrix), a czesc programu traktowala legacy ekrany mostu jako wzorzec docelowy przy parytecie/wygaszaniu. Wlasciciel rozstrzyga: stare ekrany byly slabe i NIE sa wzorcem. | Dyrektywa wlasciciela 2026-07-18; docs/uiux/FLOW_PROJEKTANTA_2026-07.md; precedensy E-26→EkranFrt i hub E-35→EkranAnalizTechnicznych (Opcja 1) | Kanon V12.xx = rejestr ZDOLNOSCI, nie ekranow: zamrozone pozostaja kody ekranow, etykiety, obszary, przejscia i macierz pokrycia (zdolnosci systemu); componentKey jest METADANA dostawcy i zmienia sie przy podmianie dostawcy na ui2 (jak E-26). Kazdy nowy ekran projektowany OD ETAPU FLOW projektanta (E1-E8, FLOW_PROJEKTANTA_2026-07.md par. 1) wg kontraktu ekranu prowadzacego (par. 0.3); legacy = tymczasowy dostawca z terminem wygaszenia; Bramka Parytetu dotyczy zdolnosci, nie ukladu/stylu starego ekranu. | Wszystkie przyszle karty UI programu; plan wygaszania mostu; screenCanonRegistry.componentKey (metadana) | Zdolnosci kanonu niezmienione — zero zmian ScreenCode/coverageMatrix/transitions; v12xx_canon_guard pozostaje zielony. | niski |
| V12K-042 | **ROZSTRZYGNIETY (2026-07-18, dyrektywa wlasciciela „Dodaj zrodlo zasilania i cale kreatory zrob od nowa, opcja max”).** Formularze operacji domenowych (`ui/network-build/forms/**`) uzywaly klas `scada-*` i twardych heksow (`#050810`, `#00e5ff`, …), niespojnych z tokenami `--mvd-*` powloki ui2 — zrodlo „kolorowego balaganu” interfejsu. | Dyrektywa wlasciciela 2026-07-18; `docs/uiux/KREATORY_STANDARD_2026-07.md`; `GridSourceEditor.tsx` (1789 w., scada-*) | Wspolny FRAMEWORK kreatorow ui2 (`ui2/kreatory/rama`: KreatorRama + pola + gotowosc + podsumowanie, tokeny `--mvd-*`, oba motywy) wg kontraktu ekranu prowadzacego (FLOW par. 0.3). Podmiana dostawcy metoda Opcja 1 w `operationFormRegistry` — kontrakt operacji domenowej BEZ ZMIAN (payload w module modelu kreatora); legacy retirowany (usuniecie sierot + przeniesienie intencji testow do testow czystego modelu + realnej sciezki, Zero-Debt par. 5). Flagowiec: `add_grid_source_sn` → `KreatorZrodloZasilania` (`AddGridSourceForm`+`GridSourceEditor` USUNIETE). Podsumowania liczbowe (Sk''/Ik''/Z1/…) WYLACZNIE z backendu (fetchGridSourcePreview, IEC 60909) — ZERO fizyki w UI. | `operationFormRegistry.tsx`, `operationSurfaceRegistry.ts` (componentName=metadana), `ui2/kreatory/**`; pozostale 17 kreatorow do przebudowy wg standardu | Przebudowa 17 pozostalych kreatorow = program (karty do wykonawcow); framework/flagowiec/standard = robota zarzadcy. Kontrakt operacji domenowych niezmieniony (testy kontraktowe modelu). | sredni |
| V12K-043 | **ROZSTRZYGNIETY (2026-07-18, dyrektywa wlasciciela: „za malo intuicyjny i profesjonalny, chaotyczne rozmieszczenie, dodaj wiecej opcji — max bez splycania, wez audyt szerokiego grona ekspertow").** Kreator zrodla v1 (panel prawy ~320px) odrzucony: (a) wcisniety w waski panel = chaos bez hierarchii; (b) SPLYCONY — usunieto tryb WN/110kV, tryb Sk3/impedancja, reczny ekwiwalent, konfiguracje per-sekcja, ktore backend `add_grid_source_sn` REALNIE przyjmuje. | Dyrektywa wlasciciela 2026-07-18; `docs/uiux/AUDYT_KREATOR_ZRODLA_2026-07.md`; kontrakt `enm/domain_operations.py::add_grid_source_sn` (2611+) | Przeprojektowanie OD ZERA: konfigurator PELNOEKRANOWY (`openMode: expand_workspace`, region glowny — nie panel), uklad dwukolumnowy (kroki+pola | stala kolumna: podsumowanie backendu + kontrola gotowosci), 7 krokow (K1 identyfikacja … K7 zapis), KOMPLET opcji z kontraktu backendu (katalog/reczny ekwiwalent, strona SN/110kV, Sk3/impedancja, skladowa zerowa, uziemienie R/X, per-sekcja: nazwy/pola, transformatory, aparaty, parametry normowe). Zero fabrykacji (FLOW §0.6): kazda opcja mapuje na realne pole payloadu; wynik liczbowy tylko z backendu (IEC 60909). Tryb zaawansowania (Podstawowy/Rozszerzony/Ekspercki) odslania tryb reczny/impedancje. | `operationSurfaceRegistry` (openMode add_grid_source_sn), `ui2/kreatory/rama` (rozszerzenie: uklad 2-kol + nawigacja krokow), `ui2/kreatory/zrodlo/**` (przywrocenie galezi reczny/HV/impedancja/per-sekcja splyconych w v1) | Zadanie zarzadcy (opcja max). Kontrakt operacji domenowej niezmieniony. | sredni |
| V12K-044 | **ROZSTRZYGNIETY (2026-07-18, dyrektywa wlasciciela: „szablony pol producentow robilem zeby to TU wykorzystywac; brak powiazania z zabezpieczeniem polowym; mysl globalnie gdzie to dalej").** Kreator zrodla tworzy gole pola (`gpz_sections[].line_field_names`) — NIE wiaze rodziny rozdzielnicy producenta (switchgear_family), szablonu pola (bay_template_ref) ani zabezpieczenia polowego (protection_ref), mimo ze Reference Engine je dostarcza a model ENM Bay ma te pola. Zrywa lancuch danych do SLD/compliance/E-27/E-28. | Dyrektywa wlasciciela 2026-07-18; `docs/uiux/INTEGRACJA_KREATOR_ZRODLA_GLOBALNA_2026-07.md`; precedens `append_station_on_endpoint` (`domain_operations.py:6540` — field_spec z switchgear_family_ref+bay_template_ref+manufacturer_ref); API `/api/catalog/switchgear-families`, `/api/catalog/complete-bay-templates` | Krok K5 kreatora przeprojektowac na kompozycje z szablonow producenta: wybor rodziny (SwitchgearFamilyPicker), per-pole rola+bay_template_ref (BayTemplatePicker), zabezpieczenie polowe (protection_ref) — payload `gpz_sections[].bays[]` w konwencji field_spec kreatora stacji. Backend `add_grid_source_sn` materializuje Bay z bay_template_ref/switchgear_family_ref/manufacturer_ref/protection_ref (odwzorowanie proven kontraktu stacji), walidacja Reference Engine, determinizm zachowany. Zero fabrykacji: opcja bez pokrycia w backendzie nie powstaje. | `enm/domain_operations.py` (add_grid_source_sn + _normalize_gpz_section_entries), `ui2/kreatory/zrodlo` (K5), reuzycie ui/catalog Pickerow. KOORDYNACJA: Reference Engine = watek SLD (V12K-060), zmiana czyta pakiety READ-ONLY, bez zmian `ui/sld/**`. | Zmiana domenowa wymaga PELNEJ regresji pytest (~5400) + guardow + determinizmu — wykonac w dedykowanej fazie, nie na koncu dlugiej sesji. Implementacja end-to-end w toku. | wysoki |
| V12K-045 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „globalna implementacja OLTC end-to-end, opcja max, do ostatniego klika").** Stan zaczepu byl rozproszony (pozycja+krok na branchu, min/max tylko w ENM), brak kanonicznego obiektu regulacji (regulowane uzwojenie, tryb sterowania, napiecie zadane, pasmo nieczulosci, opoznienie, LDC, sterowana szyna) i brak petli regulacji OLTC w LF (przekladnia STALA). Konsumpcja tylko czesciowa (LF Y-bus, CGMES, adapter audit2). | Dyrektywa wlasciciela 2026-07-19; `docs/plan/OLTC_ARCHITEKTURA_2026-07.md`; recon: `branch.py` (get_tap_ratio), `enm/models.py` (Transformer tap_*), `solver_input/contracts.py` (TransformerPayload), `power_flow_newton.py` (TransformerTapSpec), `audit2_catalogs.py` (TapChangerItem), `audit2_solver_adjuster.py` | Kanoniczny `TapChanger` = jedyne zrodlo prawdy na transformatorze (domena+ENM), addytywny/exclude_none, wsteczna zgodnosc (brak → zachowanie jak dzis). Jeden helper przekladni efektywnej (uwzglednia regulowane uzwojenie). Petla OLTC w LF: jeden zaczep na krok, deterministyczna, WHITE BOX; pola wyniku addytywne. IEC 60909: K_T znamionowe (tap-niezalezne per §3.3.3) → plik SC WATCHED NIETKNIETY, brak podwojnego liczenia. UI kreatora GPZ (krok Transformatory) mapuje 1:1 na pola backendu (zero fabrykacji). | `network_model/core/branch.py`, `enm/models.py`, `solver_input/contracts.py`, `power_flow_*`, `ui2/kreatory/zrodlo`. KOORDYNACJA: SLD (znacznik OLTC/pozycja) = watek SLD (kartą, bez edycji `ui/sld/**`). NIE RUSZAC: `short_circuit_iec60909*.py` (solver_boundary_guard). | Zmiana wielowarstwowa: PELNA regresja backend pytest + frontend vitest + guardy + determinizm per faza (F1..F4). Frozen Result API tylko addytywnie, seed bez zmian dla payloadow bez OLTC. Implementacja fazowa w toku. | wysoki |
| V12K-046 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „wykonaj kazda to osobna funkcja dedykowana").** Domkniecie rejestru dlugu V12K-045 §7: funkcje dedykowane OLTC (profile roczne, wrazliwosc-sweep, optymalizacja, glif SLD, sekcja raportu). | Dyrektywa wlasciciela 2026-07-19; `docs/plan/OLTC_ARCHITEKTURA_2026-07.md` §8 (G1-G5) | Silniki liczace na warstwie solvera (orkiestracja wokol FROZEN solvera, ZERO wlasnej fizyki), deterministyczne, WHITE BOX, przywracaja stan zaczepu: `power_flow_oltc_studies.py` (sweep §9, annual profile §8, optimize §17 — enumeracja EXACT, bez solvera zewnetrznego). Sekcja raportu §14: `analysis/reporting/oltc_report.py` READ-ONLY z `oltc_control` (JSON/tekst/LaTeX). Glif SLD §13: karta koordynacyjna do watku SLD (kontrakt danych READ-ONLY + spec glifu), BEZ edycji `ui/sld/**`. | `network_model/solvers/power_flow_oltc_studies.py`, `analysis/reporting/oltc_report.py`, `docs/uiux/KARTA_KOORDYNACJI_SLD_02_OLTC_GLIF.md`. KOORDYNACJA: glif = watek SLD (V12K-060). | Testy: test_power_flow_oltc_studies (9), test_oltc_report (5); determinizm. Prezentacja UI silnikow (ekrany sweep/profil/optymalizacja) = kolejny krok warstwy prezentacji. | sredni |
| V12K-047 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „jako architekt przemysl i przeprojektuj w opcji na max, kazdy krok ma realna operacje domenowa").** Guided flow budowy sieci po GPZ (E2) byl rozproszony w starych formularzach (ContinueTrunkForm i in.) bez wspolnego kontraktu ekranu prowadzacego; brak jawnej mapy krok→operacja domenowa. | Dyrektywa wlasciciela 2026-07-19; `docs/uiux/PROJEKT_FLOW_BUDOWY_SIECI_2026-07.md`; inwentarz operacji `enm/domain_operations*.py` (~20 op budujacych siec) | Przeprojektowanie flow E2 jako kreatory ui2 na frameworku `kreatory/rama`, opcja MAX: KAZDY krok mapuje na realna operacje domenowa (continue_trunk_segment_sn, insert_station_on_segment_sn, add_transformer_sn_nn, add_nn_outgoing_field, add_nn_load, add_converter_source, insert_section_switch_sn, connect_secondary_ring_sn, set_normal_open_point, add_sn_bay, add_ct/vt/relay …). Katalog-first, podglad z backendu (R1/R2), zero fizyki w UI, jawny nastepny krok. Podmiana dostawcow (Opcja 1) bez zmiany kontraktow operacji. | `ui2/kreatory/*`, `operationFormRegistry`, `operationSurfaceRegistry`; retire starych form po podmianie. Bez edycji `ui/sld/**` (watek SLD V12K-060). | Wdrozenie fazowe G-MAG..G-ZAB; per faza pelne regresje + guardy + determinizm + zrzut. | wysoki |
| V12K-048 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „jako architekt przemysl i przeprojektuj w opcji na max kazdy krok i kazda realna operacje domenowa; rozbuduj i przewiduj do ostatniego klika wielowarstwowo; rozbuduj katalogi").** Audyt calego warsztatu projektanta wykryl ZERWANY LANCUCH: bateria kondensatorow SN (KOMPENSATOR_SN) ma katalog (`mv_shunt_capacitor_catalog.py`, 5 typow) + materializacje (`get_shunt_capacitor_type`) + konsumpcje solvera PF (`_build_shunt_specs_from_snapshot` → +jB) + analize `reactive_adequacy`, ale BRAK operacji domenowej, `CanonicalOpName` i kreatora — inzynier nie mogl postawic baterii, choc cala fizyka czekala. Analogiczny brak: ogranicznik przepiec (OGRANICZNIK_SN — katalog+picker sa, brak op) → GAP-2/G-OGR. | Dyrektywa wlasciciela 2026-07-19; `docs/uiux/BLUEPRINT_PROJEKTANT_SIECI_MAX_2026-07.md`; recon: `enm/canonical_analysis.py::_build_shunt_specs_from_snapshot`, `catalog/materialization.py` (KOMPENSATOR_SN/OGRANICZNIK_SN), `api/catalog.py`, `api/grid_source_preview.py` | Blueprint (V12K-048) = nadrzedny plan: pelna macierz operacja→lancuch (katalog→op→API→ENM→SLD→solver→analiza→zabezpieczenia→raport→zgodnosc), rejestr brakow, kontrakt kreatora MAX (z sekcja downstream „co to uruchamia"), fazy G-KOMP/G-STA/G-TRF/G-NN/G-OZE/…/G-OGR. Flagowy G-KOMP wdrozony end-to-end: op `add_shunt_compensator_sn` (materializuje do `shunt_capacitors`, katalog-first rated_mvar/rated_kv), endpoint katalogu `/api/catalog/shunt-capacitor-types`, solver podgladu `shunt_compensator_preview` (WHITE BOX: B=Q/U², I=Q/√3·U) + endpoint `/api/solver/shunt-compensator-preview`, kreator ui2 `KreatorKompensatoraSn` (katalog-first, podglad B/I_c z backendu, uczciwy stan zerowy, walidacja napiecia, sekcja downstream), launch z menu szyny SN. | `enm/domain_operations_v2.py`, `api/catalog.py`, `api/grid_source_preview.py`, `network_model/solvers/shunt_compensator_preview.py`, `types/domainOps.ts`, `ui/catalog/*`, `ui2/kreatory/kompensator/**`, `operationFormRegistry`, `operationSurfaceRegistry`, `context-menu/*`, `dialog_completeness_guard.py`. Bez edycji `ui/sld/**` (glif baterii = watek SLD V12K-060). | Testy: backend `test_add_shunt_compensator_sn` (9, w tym konsumpcja PF); frontend `kompensatorModel`+`KreatorKompensatoraSn` (12). Determinizm: op deterministyczny (seed z bus+catalog), preview czysto algebraiczny. Pozostale fazy blueprintu w toku. | wysoki |
| V12K-049 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „jako architekt przemysl i przeprojektuj w opcji na max kazdy krok i kazda realna operacje domenowa; rozbuduj i przewiduj do ostatniego klika wielowarstwowo").** G-TRF: kreator transformatora SN/nN byl legacy (`AddTransformerForm`/`TransformerStationEditor`) — bez podgladu pradow I1/I2 (R2 istnial, nieuzyty) i bez regulacji zaczepow (tylko plaskie `tap_position`). ZERWANY LANCUCH OLTC: model TapChanger, petla LF, badania i raporty (V12K-045/046) dzialaly, ale operacja `add_transformer_sn_nn` NIE materializowala TapChanger — regulacje dalo sie ustawic tylko na GPZ; dodatkowo `create_device` gubil pole `tap_changer` (whitelista). | Dyrektywa wlasciciela 2026-07-19; `docs/uiux/BLUEPRINT_PROJEKTANT_SIECI_MAX_2026-07.md`; recon: `add_transformer_sn_nn`, `_build_gpz_tap_changer` (reuzycie), `topology_ops.create_device` (whitelista), `transformerRatedCurrentsApi` (R2) | Backend: `create_device` zachowuje `tap_changer`; `add_transformer_sn_nn` materializuje kanoniczny TapChanger przez reuzycie proven helpera `_build_gpz_tap_changer` (controlled_bus_ref = szyna nN), gdy operator zazadal regulacji (wsteczna zgodnosc: brak → bez zaczepu). Frontend: kreator ui2 `KreatorTransformatoraSnNn` (kreatory/rama) zastepuje `AddTransformerForm` — katalog-first (TRAFO_SN_NN), podglad I1/I2 z backendu (R2), PELNA regulacja DETC/OLTC (rodzaj, uzwojenie, zakres/krok/pozycje zaczepow, tryb AVR, napiecie zadane, pasmo), uczciwa blokada (kontekst bez pary szyn/GPZ), sekcja downstream. Retire `AddTransformerForm` po podmianie. | `enm/domain_operations.py`, `enm/topology_ops.py`, `ui2/kreatory/transformator/**`, `operationFormRegistry`, `operationSurfaceRegistry`, `dialog_completeness_guard.py`. Bez edycji `ui/sld/**` (glif OLTC = watek SLD V12K-060). NIE RUSZONO short_circuit (K_T znamionowe tap-niezalezne). | Testy: backend `test_add_transformer_oltc` (3, w tym przetrwanie create_device); frontend `transformatorModel` (11) + `KreatorTransformatoraSnNn` (4). Determinizm: seed z hv/lv/katalog, pola OLTC addytywne (exclude gdy brak regulacji). Pelne regresje backend+frontend. | wysoki |
| V12K-050 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „jako architekt przemysl i przeprojektuj w opcji na max kazdy krok i kazda realna operacje domenowa; rozbuduj i przewiduj do ostatniego klika wielowarstwowo").** (a) PHANTOM cosφ: `add_nn_load` zapisywal `cos_phi` do meta, ale `q_mvar` liczyl wylacznie z jawnego `reactive_power_kvar` → odbior z P+cosφ mial Q=0 i rozplyw mocy ignorowal cosφ. (b) Legacy `AddNnLoadForm` liczyl Q w UI (fizyka w prezentacji). (c) Recon GAP-2 (ogranicznik przepiec): brak konsumenta postawionego elementu (v126 czyta odrebna kolekcje `insulation`) → budowa bylaby WYSPA. | Dyrektywa wlasciciela 2026-07-19; `docs/uiux/BLUEPRINT_PROJEKTANT_SIECI_MAX_2026-07.md` (GAP-2 wstrzymane, GAP-5); recon `add_nn_load`, konsumenci `loads` (power_flow_unbalanced, arc_flash/loader, boundary/identifier, canonical_analysis) | Backend: `add_nn_load` wyprowadza `q_mvar = P·tan(arccos cosφ)` z tabliczki, gdy Q nie podano jawnie (input-prep w domenie, nie fizyka sieci; jawny Q nadal nadrzedny). Frontend: kreator ui2 `KreatorOdbioruNn` (kreatory/rama) zastepuje `AddNnLoadForm` — P+cosφ (Q liczy backend, koniec fizyki w UI), opcjonalny katalog OBCIAZENIE z prefillem, podglad pradu/S z backendu (R1), uczciwy stan zerowy (brak odplywu), sekcja downstream. Retire `AddNnLoadForm`. GAP-2 (ogranicznik) WSTRZYMANE do czasu ustalenia konsumenta. | `enm/domain_operations_v2.py`, `ui2/kreatory/odbior/**`, `operationFormRegistry`, `operationSurfaceRegistry`, `dialog_completeness_guard.py`. Bez edycji `ui/sld/**`. | Testy: backend `test_add_nn_load_cosphi` (3); frontend `odbiorModel` (9) + `KreatorOdbioruNn` (3). Determinizm: seed niezmieniony dla payloadow z jawnym Q; nowe wyprowadzenie Q addytywne. Pelne regresje backend+frontend. | wysoki |
| V12K-051 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „przystap z maksymalna opcja przebudowy, wielowarstwowo, do ostatniego klika, przeprojektuj od zera, badz 100x lepszy, wez audyt, rozbuduj katalogi, zadaj pytania czego brakuje i rozbuduj").** Faza G-OZE: zrodla przekształtnikowe (add_converter_source PV/BESS/FW) to najglebszy lancuch DER (inverter PF, NC RfG/PTPiREE, RMS stability, machine SC, grid_strength, reactive_adequacy). Legacy `AddConverterSourceForm` (1365 w.) eksponuje technologie/wariant/katalog/liczbe/block-trafo, ale NIE tryb regulacji ani zdolnosc Q — mimo ze op je utrwala i solver PF falownika czyta (phantom-inverse); krzywe P(f)/Q(U)/FRT (NC RfG) niedostepne w UI. | Dyrektywa wlasciciela 2026-07-19; `docs/uiux/AUDYT_OZE_DER_MAX_2026-07.md`; recon `add_converter_source`, `_resolve_converter_defaults` (control_mode/q_min/q_max utrwalane), `solver_input/audit2_der_payload` (pf_curve/droop), `ncrfg_ptpiree/contracts` (has_hvrt/has_qu/has_pf_droop/droop_percent) | Audyt wielosoczewkowy + projekt kreatora MAX (`KreatorZrodlaOze`, kreatory/rama, kroki 1–5). Fazy: G-OZE-A (technologia/przylacze/katalog/moc + regulacja GAP-OZE-1 — mapuje na pola juz utrwalane przez op, zero fabrykacji, bez rozbudowy backendu); G-OZE-B (pf_curve_ref/flagi NC RfG + rozszerzenie op jesli recon potwierdzi brak utrwalania + krok 4 + wpiecie E-28); G-OZE-C (katalog krzywych P(f)/Q(U)/FRT). GAP register: OZE-1 phantom-inverse (control_mode/Q limits/power_setpoint), OZE-2 krzywe DER, OZE-3 flagi zgodnosci NC RfG, OZE-4 katalog krzywych. | `docs/uiux/AUDYT_OZE_DER_MAX_2026-07.md` (ten dokument); dalej `ui2/kreatory/zrodlo-oze/**`, `enm/domain_operations_v2.py` (G-OZE-B), `operationFormRegistry`, `operationSurfaceRegistry`. Bez edycji `ui/sld/**`. | Audyt = deliverable tej tury (projekt od zera, odpowiedz „czego brakuje"). Build fazowy w kolejnych turach; per faza pelne regresje + guardy + determinizm + zrzut. | wysoki |
| V12K-052 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „zaprojektuj prompt rundy w wersji maksimum, globalny audyt + maksymalna rozbudowa, odpowiedz na pytania ekspertow, nastepnie wdrazaj").** G-OZE-PF: naprawa forward-phantomu regulacji OZE wykrytego w audycie V12K-051. Dwuwarstwowy defekt: (a) kanoniczny PF budowal PQSpec bez inverter_control (canonical_analysis.py:1256) → wszystkie OZE pasywne; (b) niezgodnosc jezyka enumow — domena/katalog Polish (STALY_COS_PHI/Q_OD_U/P_OD_U), mapper inverter_control_from_params angielski-only → default Q_CONST. Wybor trybu regulacji OZE nie wplywal na wynik load-flow. | Dyrektywa wlasciciela 2026-07-19; `docs/uiux/PROMPT_RUNDA_AUDYT_MAX_2026-07.md` + `AUDYT_OZE_DER_MAX_2026-07.md`; recon canonical_analysis/_execute_power_flow, power_flow_inverter.inverter_control_from_params, power_flow_newton.build_inverter_table | (a) most jezyka w mapperze: STALY_COS_PHI→COSPHI_CONST, Q_OD_U→Q_U, WYLACZONE/P_OD_U→pasywne (P(U) nie modelowane w steady-state, komentarz); (b) guard „bez materialnego efektu → None" (unity cosφ / zerowe Q(U)) — determinizm dla zrodel domyslnych; (c) `_build_converter_control_by_node` + wpiecie `inverter_control` na PQSpec w kanonicznym PF, dolaczane WYLACZNIE dla realnie aktywnej regulacji (cosφ≠1 / slope≠0); (d) op `add_converter_source` utrwala `cos_phi`/`qu_slope_pu_per_pu` na meta (addytywnie). | `network_model/solvers/power_flow_inverter.py`, `enm/canonical_analysis.py`, `enm/domain_operations_v2.py`. SC/FROZEN nietkniete; bez edycji ui/sld/**. | Testy: `test_oze_pf_inverter_control` (7: most jezyka, guard pasywnosci, wpiecie, determinizm). Regresja backendu 6315 passed/0 failed — determinizm golden zachowany (istniejace snapshoty bez nowych pol → brak zmiany wyniku). Nastepne: G-OZE-B (krzywe NC RfG), G-OZE-UI (migracja formularza). | wysoki |
| V12K-053 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „kontynuuj zgodnie bez zatrzymywania").** G-SEK: legacy `InsertSectionSwitchForm` (72 w., cienki wrapper nad SectionSwitchModal) bez kontraktu ekranu prowadzacego. Weryfikacja audytowa potwierdzila, ze stan lacznika (open/closed) JEST honorowany przez kanoniczny PF (mapping.py:153 wyklucza otwarte galezie; power_flow_newton_internal.py:785 open switch → brak krawedzi Y-bus) — wiec kreator sekcjonowania nie jest kosmetyka. | Dyrektywa wlasciciela 2026-07-19; recon insert_section_switch_sn (segment_id, insert_at RATIO, switch_type, normal_state, APARAT_SN), power_flow open-switch handling | Kreator ui2 `KreatorLacznikaSekcyjnego` (kreatory/rama) zastepuje `InsertSectionSwitchForm`: katalog-first (APARAT_SN, fetchMvApparatusTypes), rodzaj aparatu, stan normalny (zamkniety/otwarty=NOP), polozenie na odcinku (RATIO 0..1), uczciwy stan zerowy (brak odcinka), sekcja downstream (PF radialny gdy otwarty, pewnosc zasilania). Retire legacy. | `ui2/kreatory/lacznik/**`, `operationFormRegistry`, `operationSurfaceRegistry`, `modalRegistryCompleteness.test` (asercja componentName), `dialog_completeness_guard.py`. Bez edycji ui/sld/**. | Testy: lacznikModel (6) + KreatorLacznikaSekcyjnego (4) + modalRegistryCompleteness (8). Pelna regresja frontendu. | sredni |
| V12K-062 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „G oze b full opcji"; wybor sciezki AskUserQuestion: „Most przez profile (reuse)").** G-OZE-B: audyt wykryl, ze konfiguracja NC RfG (krzywe/flagi) zyje w podsystemie konfiguratora stacji (station-der store + NcRfgTestsTab + macierz), NIE w kanonicznym add_converter_source — matryca NC RfG czyta der.profiles (audit2 station-config), osobna persystencja. Doprecyzowanie „most przez profile" dla kanonicznego konsumenta: kanoniczny PF falownika MA statyzm P(f)/LFSM (lfsm_droop_pct), ale `_build_converter_control_by_node` czytal tylko cosphi/qu_slope — forward-phantom (jak G-OZE-PF dla Q). | Dyrektywa wlasciciela 2026-07-20; recon: NcRfgPtpireeModuleInput z body (nie z modelu), station-der store client-side, DerSurfaces most, `_study_frequency_hz` (studium f z ENM header), `lfsm_factor`/`inverter_control_from_params` (lfsm juz wspierany) | Backend: `add_converter_source` utrwala `frequency_droop_percent` (+ `lfsm_allow_increase` dla BESS/LFSM-U) na meta generatora (PV/BESS/FW); `_build_converter_control_by_node` czyta je → `params["lfsm_droop_pct"]` + brama `active` dla statyzmu f niezaleznie od trybu Q; kanoniczny PF stosuje `lfsm_factor(control, f_studium)` — realny wplyw przy odchylce f (przy 50 Hz brak → determinizm). Frontend: krok „Regulacja" w KreatorZrodlaOze dostaje pole „Statyzm P(f)/LFSM [%]" (mapuje na frequency_droop_percent). **Odnoga NC RfG matryca (audit2 station-config) = karta G-OZE-B2 (cross-thread do watku konfiguratora/SLD), NIE tworzymy rownoleglej sciezki.** | `enm/domain_ops_models.py`, `enm/domain_operations_v2.py`, `enm/canonical_analysis.py`, `ui2/kreatory/zrodlo-oze/**`. FROZEN Result API + ui/sld/** + station-der nietkniete. | Testy: `test_oze_pf_inverter_control` +5 (P(f) attach/gate/lfsm_factor redukcja przy 50.5 Hz/determinizm) + `zrodloOzeModel` (statyzm w payloadzie). Pelne regresje backend+frontend; determinizm przy 50 Hz. | wysoki |
| V12K-073 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „zapisz zasade ze stacje wstawiamy na koncu odcinka. W sr[odku] bez sensu. Pilnuj zeby kreatory wszystkie byliby wiazane wielokierunkowo i warstwo np. z e-schematem i wszystkim innym — planuj, zapisz i wdrazaj, ustaw cel i kryteria odbioru").** (a) Regula: stacja/odbior wstawiane na KONCU odcinka (budowa wezel po wezle); wstawianie w srodku odcinka ZAKAZANE (niefizyczne) — korekta wczesniejszej propozycji „w srodku ciagu". (b) Wiazanie: 8 z 9 kreatorow konczylo samym `navigateToSld()` — nowy element NIE byl zaznaczany na schemacie (tylko magistrala wolala selectElement). Uzytkownik ladowal na SLD bez podswietlonego/wybranego swiezego elementu (brak wiazania kreator→glif→inspektor). | Dyrektywa wlasciciela 2026-07-20; recon: `buildNextContext` (ENDPOINT_APPEND/position_on_segment:1 — stacja-na-koncu juz zaimplementowana), `insert_station_on_segment_sn`, `selection_hint` (backend `_success_response` selection_id/selection_type), macierz kreator×operacja×selection_type | **Kanon: `docs/uiux/ZASADY_WIAZANIA_KREATOROW_2026-07.md`** (regula stacja-na-koncu + program wiazania: CEL + kryteria odbioru + macierz kreator×warstwa). Wspolny hook `rama/selekcjaPoOperacji.ts` (`useSelekcjaPoOperacji` + `mapujTypElementu` + `refZOperacji`): po sukcesie operacji wyluskuje ref (`selection_hint.element_id`→`created_element_ids[0]`), mapuje `selection_type` backendu na `ElementType`, wola `selectElement` (otwiera property-grid) + `centerSldOnElement` (zoom_to) + `navigateToSld`. Wpiete w 9 kreatorow (transformator/kompensator/lacznik/odbior/pierscien/pole/zrodlo/zrodlo-oze; magistrala ma wlasny wzorzec per-odcinek). **ZERO fabrykacji**: ref i typ z odpowiedzi backendu, fallback typu tylko obronnie. | `ui2/kreatory/rama/selekcjaPoOperacji.ts` (nowy) + `rama/index.ts` (barrel) + 8 kreatorow (usuniety bezposredni `navigateToSld`, dodany hook). Regula stacja-na-koncu juz wymuszona (ENDPOINT_APPEND). FROZEN API/backend/ui/sld/** nietkniete (renderowanie glifu = watek SLD). | Testy: `rama/selekcjaPoOperacji.test.tsx` (8: mapowanie typu, refZOperacji, hook select+center+nav, fallback, brak elementu) + `KreatorTransformatoraSnNn` (realna sciezka: po zapisie element zaznaczony `type:TransformerBranch` + SLD wycentrowany). Type-check/lint zielone; regresja kreatorow (203) + pelna regresja frontendu. **KOREKTA (follow-up d3e6408f): swiadomy podzial odcinka `insert_station_on_segment_sn` jest POPRAWNY — backend dzieli odcinek na dwa i stawia stacje w NOWYM wezle (= koniec odcinka), Phase 0A/0C „Conscious split with preview", heavy-tested. Retrakcja falszywej diagnozy „mid-segment leak"; zakaz dotyczy tylko phantom-trybu „stacja plywajaca bez podzialu". NIE zmieniono zachowania.** | wysoki |
| V12K-146 | **ROZSTRZYGNIETY (2026-07-23, karta S7.6-KOMPRESJA (Opus, worktree) — znalezisko Z1 audytu powykonawczego: pustka arkusza ~80%, dlugie puste piony; scalenie i weryfikacja Fable).** Rzedna Y kazdego pasma lateralnego byla zawyzana przez KORYTARZ OBROCONEJ ETYKIETY zejscia: gapAbove wszystkich 12 pasm = 248 px, z czego 242 px = szerokosc tekstu etykiety `segment-lateral`, a tylko 32 px = MIN_SUBTREE_CLEARANCE (etykieta zawyzala kazdy gap 7,75x; suma nadmiaru 2592 px). | Recon pomiarowy wykonawcy (tabela pasm przed zmiana) — przyczyna liczbowa, nie przymiotnikowa. | **Regula ogolna (zero hardcode):** etykieta zejscia opisuje ODEJSCIE OD MAGISTRALI, wiec zyje w PASIE ZEJSC tuz pod magistrala (przy channelX punktu odejscia); wysokosc pasa = najwyzszy korytarz etykiety (prepass deterministyczny z realnego bbox tekstu); gap pasm -> MIN_SUBTREE_CLEARANCE; usuniety wymog corridorFits; piony zejsc skrocone WYNIKOWO. **Metryki przed/po:** suma pionow L0 28072->21976 (-22%), L1/L2 45016->38920 (-14%); wysokosc bbox 4457->3409 (-24%); bboxUtilization +31%; inkDensity +22-25%; suma gapAbove pasm L1 2976->384 (-87%); poziomy/crossings/kolizje/M-02/nieortog/minClearance BEZ ZMIAN. Warunki S6 par. 6 spelnione jednoczesnie. Baseline'y wymienione swiadomie per stala (VERTICAL_LENGTH, SHEET_FILL_FLOOR — zapadki na nowych wartosciach). Nowy test kompresji (22) + fixtury H w budzetach. Skrypt dowodowy s7p6 z banerem w stopce (Z4 czesciowo). | frontend: v3/scene/buildScene.ts (pas etykiet zejsc + LateralShelfRecord), scripts/sld_v3_acceptance.mjs, testy (schemat10s7p6 22), scripts/render_schemat10_s7p6.tsx, zrzuty s7p6-l{0,1,2}.png; docs S7_GAP par. 11 + status w audycie powykonawczym. | Wykonawca: bramki zielone. Weryfikacja Fable po cherry-picku (f0378e1d): type-check 0, lint 0, vitest 184 plikow/3571, accept ALL PASS, 6 guardow 0; ogledziny zrzutu — rzedy dosuniete, nadmiar swiatla 0. Granice (dlug S7.5): os X (balancing poziomy CoG, TOP_LEVEL) nieredukowalna w grzebieniu; zagniezdzone laterale pomijane. | wysoki (Z1 domkniete: -24% wysokosci arkusza przy zerowym regresie niezmiennikow) |
| V12K-145 | **OTWARTY→W REALIZACJI (2026-07-23, uwagi zespolu eksperckiego wlasciciela do SLD L2 — werdykt: NIE ZATWIERDZAC; „schemat ma byc bezposrednim odwzorowaniem modelu sieci i konfiguracji kreatora, nie jednym uniwersalnym szablonem pola").** Program „L2-PRZEMYSLOWE": 20 sekcji uwag (wyposazenie pol z kreatora; pelny tor przylaczenia PV/DER z realna szyna nN; swiatla rownoleglych kabli; etykiety techniczne linii; warstwa wynikowa na L2 bez zmiany geometrii; opis stacji przy symbolu; pole TR dopowiedziane; CT/VT/SA/zabezpieczenia wg konfiguracji; uziemnik bocznie; kierunek przeplywu ze strzalka i znakiem; etykiety z wlascicielem; podwarstwy L2-A/B/C/D; zasada globalna dla wszystkich typow stacji; macierz testowa wyposazenia; warunki odbioru). | Recenzja zapisana jako WIAZACA: docs/sld/RECENZJA_L2_POLA_WYPOSAZENIE_2026-07.md (kondensacja pelnej tresci uwag + program wdrozenia W1-W5). Recon zarzadcy PRZED kartami: architektura „prymat danych nad konwencja" JUZ ISTNIEJE (F9.3/spec par. 12.1 — `bay.primaryDevices` niepuste ⇒ tor pola Z DANYCH przez `resolveBayApparatusSymbolIds`; fallback konwencji par. 12.4 tylko przy braku danych; stany cb/ds/es + protectionMarking + metering w deskryptorze) — pola wygladaja identycznie, bo siec referencyjna NIE niesie primary_devices i wszystko spada na jeden fallback. | Fazy (zarzadca Fable, wykonawcy Opus): **W1** domkniecie lancucha danych wyposazenia (kreator/szablony producenta → ENM Bay.primary_devices → adapter → scena) + komplet mapy urzadzen (LBS/FUSE/SA/VT...) + stany + fixtura z realnym wyposazeniem; **W2** pelny tor DER (wariant A: realna szyna nN z aparatem glownym, odbior+PV na szynie; wariant B: pole zrodlowe SN + TR blokowy) — absorbuje karte GS-4b (Z2 audytu powykonawczego); **W3** swiatla kabli (4 nowe kontrakty) + etykiety techniczne linii z katalogu zakotwiczone do wlasciciela; **W4** warstwa wynikowa L2 (reuzycie overlay v3, kontrakty par. 8/9/16); **W5** P1 (warianty pol, czytelnosc symboli, podwarstwy L2, macierz par. 20). Werdykt „NIE ZATWIERDZAC" nadrzedny nad warunkowym odbiorem audytu powykonawczego. | docs/sld/RECENZJA_L2_POLA_WYPOSAZENIE_2026-07.md (nowy, WIAZACY); implementacja: enm/adapter/compose/scene/overlay + kreatory (dane pol). Rownolegle w locie: S7.6 kompresja pionowa (Z1). | Kryteria: WARUNKI ODBIORU recenzji (kazde pole == konfiguracja kreatora, pelny tor PV, swiatla, etykiety linii, warstwa wynikow, szyna nN, kotwica L0/L1/L2) + macierz testowa par. 20 zielona. | wysoki (warstwa inzynierska L2 = warunek produkcyjnego schematu jednokreskowego) |
| V12K-144 | **ROZSTRZYGNIETY (2026-07-23, karta F-E8.3 (Opus, worktree) — domkniecie odlozonego dlugu z V12K-094 („Cykl zycia dokumentu = karta F-E8.3 BACKEND — NIE fabrykowac") i V12K-096 („Zestawienie materialowe NIE dodane — brak backendu → phantom").** Hub Dokumentacji (E8) nawigowal tylko do generatorow; brak REALNEGO magazynu wygenerowanych dokumentow (status/data/rozmiar/strony/Pobierz/Podglad) — pole do fabrykacji, dlatego swiadomie odlozone jako backend. | Recon: generatory JUZ istnieja (raport/dowod `analysis_run_exports.py`, studium OZE `oze_analysis_runs.py`, archiwum `project_archive`); wzorzec persystencji `canonical_run_repository_scope` (samodzielny scope, cache'owana fabryka sesji, `DATABASE_URL`); brak generatora BOM (grep pusty). | **Magazyn REUZYWA (zero duplikacji, zero nowych zaleznosci):** (1) ORM `DocumentRecordORM` (BLOB `LargeBinary` + metadane) w tym SAMYM zrodle danych co przebiegi; repo `document_store_repository` z tym SAMYM wzorcem scope co canonical_run; (2) przechwytywanie OPT-IN (`zapisz_do_magazynu`) w 9 ISTNIEJACYCH endpointach generatorow (report json/docx/pdf, proof json/latex/pdf, studium docx/pdf, archiwum zip) — GET pozostaje bezpieczny/idempotentny, zero regresu podgladu; (3) 2 nowe endpointy: lista `GET /api/projects/{id}/documents` (deterministyczna) + strumien `GET /api/documents/{id}/content?inline=` (pobranie/podglad, poprawny content-type); (4) front hub: hook `useDokumentyMagazynu` + karta „Wygenerowany [data]" z realnymi akcjami Pobierz/Podglad (URL endpointu); brak rekordu → dzisiejszy stan „Do wygenerowania". **Determinizm:** id rekordu = uuid5(project,type,format,run) → idempotentny upsert; SHA-256 tresci stabilny; strony z realnego PDF bez nowej zaleznosci (parser `/Type /Page`); JSON/ZIP/DOCX → uczciwy brak stron (exclude_none). **ZERO fabrykacji:** kazda dana karty z REALNEGO rekordu. **BOM POZA zakresem** — wymaga wlasnego generatora (jawny dlug, NIE phantom; kontrolki nie dodane). | Backend: `models.py` (+DocumentRecordORM), `repositories/document_store_repository.py` (nowy), `api/document_store.py` (nowy), `api/analysis_runs.py`/`oze_analysis_runs.py`/`project_archive.py`/`main.py` (opt-in capture), macierz API +2 wiersze. Front: `ui2/spaces/dokumentacja/{api.ts (nowy),model.ts,HubDokumentacji.tsx,strings.ts,dokumentacja.css}`. FROZEN/solvery/goldeny NIETKNIETE (pola addytywne, seed bez zmian). | Testy: backend repo 7 + API 5 = 12 nowych (zapis→lista→pobranie, determinizm 2x, SHA stabilny, izolacja per projekt, strony PDF, uczciwy brak); front hub 14→20 (+4 integracyjne magazynu na realnej sciezce fetch, +2 selektory). Pelna regresja backend 6513 pass/11 skip/4 xpass; front ui2 209 plikow/2210. Bramki: black (moje pliki czyste; 128 plikow pre-existing dryf poza zakresem — zmierzone), ruff 0 (naprawiony pre-existing F811 duplikat importu w main.py), type-check 0, eslint 0, trace_determinism 0, 10 guardow (api_lifecycle/v12xx_canon/readiness/no_codenames/forbidden_ui_terms/ui_terminology/dead_click/dialog_completeness/docs/utf8) zielone. | wysoki (odlozony dlug cyklu zycia dokumentu domkniety end-to-end backendem; zero fabrykacji; BOM pozostaje jawnym dlugiem) |
| V12K-143 | **ROZSTRZYGNIETY (2026-07-23, karta DER-MENU-V3 (Opus, worktree) — domkniecie udokumentowanej luki V12K-131 „menu DER na v3 bez podtypu"; scalenie i weryfikacja Fable).** Luka opierala sie na tezie „scena v3 nie niesie Generator.gen_type" — po GS-4 podtyp JEST w lancuchu (adapter `SldSourceView.kind` → `StationDerSourceInput.kind` → symbol derPv/derBess/derWind); menu podtypu mozna dostarczyc BEZ zgadywania. | Recon celow KAZDEJ akcji rejestru der_* przed przeniesieniem (akcja bez realnego celu nie wchodzi); wzorzec testu preselekcji E-26 (`sldActionExecutorNcRfg.test.tsx`). | **Lancuch danych domkniety end-to-end:** `ComposedSymbolInstance.derKind` (compose/station z `source.kind`) → `PreviewElementMeta.derKind` (addytywne) → scena → `SldElementClickMeta.derKind` + DOM `data-der-kind` → `elementKindForMenu(kind, derKind)`: pv→'der_pv', bess→'der_bess', wind→'der_fw'; generator/unknown → 'der' GENERYCZNE (uczciwa degradacja, zero domyslu). Akcje na v3 z realnymi celami: open-pv/bess/fw-config → E-21/E-22/E-23 (entityRef=ref generatora), show-frt-hvrt → E-26 Z PRESELEKCJA, show-ncrfg → macierz NC RfG, show-results → zakladka rozplywu; delete-* = toast roadmapy Etap 5 (pre-existing stan v2, przeniesiony uczciwie). elementId zweryfikowany: ownerRef symbolu DER = Generator.ref_id (executor akceptuje wprost). Komentarze-luki w Workspace/SldCommandService zaktualizowane (dokumentacja mowi prawde po zmianie). Zero-Debt wykonawcy: wlasny wadliwy test inwariancji geometrii (falszywe zalozenie o zmianie gen_type) zastapiony testem realnego inwariantu `symbolId === symbolIdForSourceKind(derKind)`. | frontend: v3/compose/{station,preview}, v3/scene/buildScene, v3/canvas/{SldCanvasV3,SldCanvasV3Workspace}, v2/command/SldCommandService (komentarz), testy contextMenu (realna sciezka natywna) + NOWY derKind.test (8 plikow, +322/-55). Goldeny/baseline'y NIETKNIETE (pole addytywne; 45016/71016/167 niezmienione). | Wykonawca: wszystkie bramki zielone. Weryfikacja Fable po cherry-picku (bd6089f9): type-check 0, lint 0, vitest 182 plikow/3549, accept ALL PASS, guardy sld_determinism/overlay_no_physics/no_codenames/forbidden_ui_terms/ui_terminology/dead_click/dialog_completeness 0. **Jawne dlugi:** panel drawera ISTNIEJACEGO DER (kanal danych meta.derKind gotowy, brak buildera — odrebny dostawca; domysl 'PV' w drawerze dotyczy WYLACZNIE przeplywu tworzenia z palety, gdzie derKind zawsze z akcji uzytkownika — nie jest aktywna fabrykacja); realne delete-* DER = osobny etap produktowy. | sredni (menu kontekstowe DER na v3 z pelnymi akcjami podtypu bez fabrykacji — parytet z v2 domkniety) |
| V12K-142 | **ROZSTRZYGNIETY (2026-07-23, karta S5-FINAL (Opus, worktree) — final programu SCHEMAT-10 (V12K-135): dowod domkniecia; scalenie i weryfikacja Fable).** Kryteria odbioru programu wymagaly dowodow: zoom bez zmiany tozsamosci stacji (zrzuty >=3 kroki), goldeny wymieniane swiadomie per plik, parytet funkcji testami. | Karta z par. 0; zakaz zmian silnika/gramatyki (GS-1..GS-5 zamkniete). | **(1) GOLDENY — werdykt SWIEZE, zero regeneracji:** recon wykazal, ze goldeny SLD v3 to baseline'y WBUDOWANE w sld_v3_acceptance.mjs (VERTICAL/HORIZONTAL/BEND/SHEET_FILL), brak osobnych .snap v3; dowod swiezosci = accept ALL PASS + pelna regresja 3541 na HEAD, git status bez zmian goldenow. **(2) DOWOD ZOOMEM (6 kadrow, s5-zoom-1..6):** cel wyprowadzony z danych — stacja S01 z TR + PV 500 kW ZA TR (realny przypadek GS-4); kotwica station-name rect 552,648,864 IDENTYCZNA liczbowo na L0/L1/L2; sekwencja L0->L1->L2->L2->L1->L0 z oknami swiata 5365x3219 / 2449x1470 / 1166x700; asercje TRESCI przerywaja render (kotwica identyczna, ownerRef w scenie, okno zawiera kotwice, data-scene-lod==LOD); determinizm 2x bajt-identyczne SVG. **(3) MACIERZ PARYTETU 11/11 z dowodem (S5_MACIERZ_PARYTETU_2026-07.md):** kazdy wiersz plik+blok+liczba testow na HEAD (overlay przeplywu 55+51, zwarciowy + strzalki 1-kawalkowe, wielokawalkowe V12K-133, pulse V12K-131, badge OLTC, menu+DER 72, selekcja/centrowanie/deep-linki, kreatory 8, CAD 33, eksport 14); JEDYNA luka — regula 18 gramatyki (jedna gramatyka w calym systemie) bez testu — NAPRAWIONA: grammarRule18.test.tsx (4 testy: eksport po zdjeciu literalow koloru bajt-identyczny z kanwa na L0/L1/L2 — eksport nie jest drugim rendererem). | docs/audit/visual/schemat-10/s5-zoom-{1..6}.png + README, docs/sld/S5_MACIERZ_PARYTETU_2026-07.md, frontend/scripts/render_schemat10_s5.tsx, v3/export/__tests__/grammarRule18.test.tsx. Silnik/gramatyka NIETKNIETE (10 plikow, +390, zero modyfikacji kodu produkcyjnego). | Wykonawca: wszystkie bramki zielone. Weryfikacja Fable po cherry-picku (0a66120d): type-check 0, lint 0, vitest 182 plikow/3541, accept ALL PASS, 7 guardow 0, grammarRule18 4/4. Dlugi przeniesione (nie zamiatane): luka F-1 przy odwroconej deklaracji from/to (V12K-133, kontraktowo niewystepujaca), menu DER show-frt-hvrt (V12K-131 — W REALIZACJI rownolegla karta DER-MENU-V3). **Program SCHEMAT-10: fazy S1-S8 + GS-1..GS-5 + P1 recenzji DOSTARCZONE.** | wysoki (final programu — dowod domkniecia zoomem, parytetem i goldenami zamiast deklaracji) |
| V12K-141 | **ROZSTRZYGNIETY (2026-07-23, karta S8-LOD (Opus, worktree) — faza S8/P2 recenzji eksperckiej (V12K-137): plynnosc przejsc LOD; scalenie i weryfikacja Fable).** Przejscia miedzy LOD wymagaly dowodow plynnosci: progi bez uzasadnienia czytelnosciowego, brak crossfade detalu, brak dowodu anty-trzepotania na granicach. | Recon wykonawcy PRZED zmiana (reuzycie zamiast duplikacji): automat LOD z histereza JUZ istnial (`lodFromScaleWithHysteresis`, `DEFAULT_LOD_THRESHOLDS`, `refScaleFor` z FIX-1), wskaznik LOD (`SheetFrame`<-`SCENE_LOD_LABELS_PL`) i reczna blokada (`lodOverride ?? camera.lod`) tez — zachowane, zero nowego UI. | **Progi WYPROWADZONE z czytelnosci (docstring z wyliczeniem):** l1Max=1,2 (L1<->L2, brama = etykieta t2 11px: wyjscie 11,2px ekranu, wejscie 15,2px), l0Max=0,4 (L0<->L1, brama = glif 48px vs 5,78px przy fit calosci); martwe strefy [t*0,85 .. t*1,15]. **Dowod anty-trzepotania liczbami:** drganie w martwej strefie L1<->L2 (8 probek) => 0 przelaczen; L0<->L1 (7 probek) => 0; kontrast: klasyfikacja czysta na tej samej sekwencji trzepocze (>0); pelne przekroczenie tam-i-z-powrotem => dokladnie 2. **Crossfade warstwy detalu:** natywny SMIL (wzorzec repo, zero globalnego CSS, zero timerow w logice stanu), animacja czysto prezentacyjna; eksport/harness z `animateLodTransitions=false` (deterministyczny, opacity bazowe 1). Ciaglosc selekcji/overlay miedzy LOD strukturalnie po `ownerRef` (LOD-niezalezne) + test na realnej sciezce wheel. Viewport zachowany przy przelaczeniu (JEDNA KOTWICA geometrycznie + brak resetu transformacji). | frontend: v3/canvas/{camera.ts,SldCanvasV3.tsx}, screenshot-harness-main.tsx, testy camera+sldCanvasV3 (+355/-20). Geometria/buildScene nietkniete. | Wykonawca: type-check 0, lint 0, vitest 3537/181 plikow, accept ALL PASS, 5 guardow 0. Weryfikacja Fable po cherry-picku (eb268a30): identycznie (3537, ALL PASS, lod_path_probe topologia identyczna L0/L1/L2). Bez jawnych dlugow. Fazy recenzji S6/S7/S8 wszystkie dostarczone — na stole final S5 (goldeny+zoom+parytet). | sredni (P2 domkniete: zoom = plynna zmiana skali szczegolu bez skoku swiata i bez trzepotania) |
| V12K-140 | **ROZSTRZYGNIETY (2026-07-23, karta P1-LAYOUT (Opus, worktree) — punkty P1 recenzji eksperckiej layoutu (V12K-137): gestosc lokalna, odstep stacji z footprintu, skalowalnosc 100-500 stacji, stabilnosc lokalna; scalenie i weryfikacja Fable).** Recenzja P1 wymagala dowodow, ktorych suita nie miala: brak miar rozkladu gestosci arkusza, brak dowodu ze rozstaw stacji wynika z footprintu (nie stalego slotu), brak fixtur wielkoskalowych (rodzina H WYTYCZNYCH par. 3) i brak miar przemieszczen kotwic przy zmianie lokalnej (par. 9). | Karta z par. 0 (crossings=0, JEDNA KOTWICA, zakaz hardcode = odrzucenie, buildScene nie ratuje layoutu); recon wykonawcy przed zmiana. | **P1.1** `localDensityMetrics` (okna 16xGRID z realnej geometrii, deterministyczne; refaktoryzacja `sheetFillRatio` do wspolnego `markedGridCells` — bajt-identyczna) + sonda `local_density_probe` w accept:sld-v3 (raportujaca; twardy prog po pomiarze bazy). Baza (52s): L0 srednia=0.011 maks=0.180 pustka=0.847; L1 0.018/0.234/0.797; L2 0.018/0.234/0.788. **P1.2 DOWOD zamiast zmiany (recon):** rozstaw JUZ jest footprint-driven — `requiredStationWidth` rosnie 40->744 z dlugoscia nazwy, pola pasa gornego 840/864/872/944 przy swietlach bbox-do-bbox WSZYSTKIE ==32 (TOP_LEVEL_FIELD_CLEARANCE) => footprint + stale swiatlo, nie slot; udokumentowane testem + `topBandFieldExtents`, silnik NIEZMIENIONY. **P1.3** deterministyczne generatory syntetyczne ~106/265/530 stacji (kod, nie JSON; replikacja topologii magistrala+laterale+TR+DER+NO): zero-defekt L0/L1/L2 (crossings/kolizje/nieortog/niejedn = 0), determinizm 2x, permutacja => scena identyczna, JEDNA KOTWICA; czasy (3xLOD, warm): 106=0,54 s / 265=2,97 s / 530=11,37 s, budzety ~2,6x pomiaru. **P1.4** `anchorDisplacementMetrics` (4 miary par. 9): +1 stacja => 15 kotwic przesunietych WYLACZNIE poziomo o kwant GRID, 91 bit-identycznych, suma|dx|=312, pion=0, zero nowych defektow. | frontend: v3/scene/buildScene.ts (+249, metryki addytywne), v3/scene/__tests__/{syntheticNetworks.ts,buildScene.p1Recenzja.test.ts} (37 testow), scripts/sld_v3_acceptance.mjs (+21). Geometria NIEZMIENIONA (goldeny/baseline'y S6 zielone z konstrukcji). | Wykonawca: type-check 0, lint 0, vitest 3524, accept ALL PASS, 4 guardy 0. Weryfikacja Fable po cherry-picku (d8f8511c): type-check 0, lint 0, vitest sld+engine 181 plikow/3524, accept:sld-v3 ALL PASS (local_density_probe z wartosciami identycznymi z raportem), 5 guardow 0. Otwarte dalej: twarde progi gestosci po decyzji z pomiaru bazy; S8 plynnosc LOD; final S5. | wysoki (dowod generalizacji layoutu na 100-500 stacji + miary stabilnosci produkcyjnej w suicie) |
| V12K-139 | **ROZSTRZYGNIETY (2026-07-23, uwaga wlasciciela: „render zaklada, ze wiekszosc stacji to przelotowe, a w realnej sieci to koncowe z odgalezien — powiazac z kreatorami i calym UI, rozwiazanie globalne we wszystkich warstwach"; wykonanie GS-5 Fable osobiscie).** Sylwetka mini-RMU L0 rysowala KAZDA stacje dwustronnie (dwa pola liniowe), mimo ze w sieci referencyjnej 45% stacji NIE jest przelotowych (pomiar: 53 = 29 przelotowych + 12 koncowych na koncach lateralow + 12 odgalezynych zrodlujacych laterale) — fantomowe pole WY na kazdej stacji koncowej. | Audyt WSZYSTKICH warstw lancucha: kreator stacji (ENDPOINT_APPEND ⇒ station_type='terminal', WE+TR — poprawny), backend (pola=prawda; continue_trunk_segment_sn WYMAGA wolnego pola liniowego — typ zmienia sie przez topologie, nie flage), scena L1/L2 (classifyStationTopologicalType §19.3 + prezentacja stacji ostatniej w ciagu, recenzja NO-GO pkt 7 — poprawne), sylwetka L0 (DZIURA — regula zyla tylko w galezi lod>=1). | **JEDNA regula prezentacji roli dla wszystkich LOD:** helper `presentedStationTopologicalType` (wydzielony z galezi lod>=1, zero cienia) zasila i etykiete typu L1/L2, i NOWE pole `StationCompactGlyphSummary.lineTopology` ('koncowa' = tylko pole WE, szyna konczy sie w rozdzielnicy; 'przelotowa' = dwa pola; 'odgalezna' = dwa pola + WEZEL odgalezienia na szynie — kropka junction, decyzja przestrzenna z pomiarem: trzecie pelne pole aparat+glowica lamie swiatlo minGap=3 wzgledem pola TR w kazdym wariancie zgodnym z realnym punktem odejscia; kabel zejscia rysuje scena; przy NO kropka nie jest rysowana — srodek szyny lezy w realnej przerwie; sekcyjna ⇒ 'przelotowa'+sectioned). Semantyka LOD: L0 = ROLA topologiczna w ciagu, L1/L2 = fizyczna kompletacja pol (rezerwa widoczna). Sprzecznosci kontraktu: sectioned∧topologia<>przelotowa oraz noOpen∧koncowa (NO wymaga drugiego toru) — macierz 210 kombinacji legalnych (30 TR×DER × 7 slotow topologia×NO), sygnatury DOM unikalne. | frontend: v3/scene/buildScene.ts (helper + summary), v3/compose/preview.tsx, v3/symbols/{miniRmuGrammar,glyphs}, v3/canvas/SldCanvasV3.tsx, testy symbols (GS-5 kanoniczne + macierz 210) + schemat10gs1 (rozklad 29/12/12 przybity jak golden, ostatnia stacja magistrali uczciwie 'odgalezna' bo zrodluje lateral), scripts/render_schemat10_gs1.tsx, zrzuty gs5-l0{,-detal}.png. Kreatory/backend NIETKNIETE (audyt potwierdzil poprawnosc — powiazanie juz istnialo, brakowalo konsumenta w sylwetce L0). | Weryfikacja Fable: type-check 0, eslint 0, vitest sld+engine 180 plikow/3487, accept:sld-v3 ALL PASS (JEDNA KOTWICA/lod_path/crossing bez regresu), guardy sld_determinism/overlay_no_physics/no_codenames/forbidden_ui_terms/ui_terminology 0; DOM SVG produkcyjny: 12/12 koncowych bez linii do kotwicy E, 12/12 odgaleznych z wezlem. | wysoki (koniec fikcji „kazda stacja przelotowa" na widoku calosci — rola stacji czytelna od kreatora po L0) |
| V12K-138 | **ROZSTRZYGNIETY (2026-07-23, obowiazkowa opinia inzynierska wlasciciela: „mini-RMU na L0 musi byc male, ale nie moze klamac topologicznie" — system nie odroznial zrodla przylaczonego do szyny SN od zrodla ZA transformatorem SN/nN; wykonanie GS-4 Fable osobiscie).** Sylwetka mini-RMU rysowala KAZDY DER jako odgalezienie od szyny SN (pole DER), niezaleznie od realnego miejsca przylaczenia. Pomiar bazowy: w sieci referencyjnej (sldSubstrate52s) 20/20 generatorow siedzi na szynach nN (0,4 kV) — render klamal topologicznie w 100% przypadkow. | Kanon: sekcja „GS-4" w docs/sld/GRAMATYKA_MINI_RMU_2026-07.md (kontrakt hasDerOnMvSide/hasDerBehindTransformer niezalezne, behindTr wymaga TR, klasyfikacja z REALNEJ szyny ENM, 6 testow kanonicznych); recon lancucha danych: Generator.bus_ref → Bus.voltage_kv (adapter v2), prog 0,5 kV = ISTNIEJACA konwencja szyn nN stacji (K30-37). | **Lancuch end-to-end:** (1) adapter v2 `SldSourceView.connectionSide` ('sn'/'nn'/'unknown') klasyfikowany z REALNEJ szyny generatora (brak szyny/napiecia ⇒ 'unknown', NIGDY zgadywanie strony); (2) `StationDerSourceInput.connectionSide` + `StationCompactGlyphSummary.der` ROZDZIELONE na `derOnMv`/`derBehindTr` (dominanta per strona, deterministyczna po id); strona nieznana → derOnMv (model nie mowi nN — rysunek od szyny rozdzielnicy = uczciwy punkt przylaczenia stacji; jedyny ZAKAZ kanonu dotyczy renderu z szyny gdy model MOWI nN); (3) gramatyka: nowa strefa `poleTr.derNn` (znak przy galezi pola TR PONIZEJ uzwojen, POZA enklozura — strona nN na zewnatrz rozdzielnicy SN, INNA kotwica niz poleDer; sondy spacing/ciaglosci galezi nN rozszerzone); (4) renderer: `stationDerOnMv`+`stationDerBehindTr`, blok nN zyje WEWNATRZ grupy TR (bez TR strona nN nie istnieje z konstrukcji); ten sam ksztalt rodzaju po obu stronach — rozni sie WYLACZNIE przylacze; (5) walidacja: `miniRmuFeatureContradictions` (derBehindTr bez TR = NIEDOPUSZCZALNE), macierz kombinacji 40→120 legalnych (200−80 sprzecznych), sprzecznosc modelu ⇒ jawny stopNote `station.der.behindTrBezTR`; (6) 6 testow kanonicznych + test fixture (20/20 za TR, zero na SN) + legenda z etykietami jednoznacznymi („PV za TR (nN)" vs „PV na SN"). | frontend: v2/canvas/enmToSldAdapter.ts (connectionSide), v3/compose/{sourceKind,preview}, v3/scene/buildScene.ts, v3/symbols/{miniRmuGrammar,glyphs}, v3/canvas/SldCanvasV3.tsx, testy symbols+schemat10gs1, scripts/render_schemat10_gs{1,2}.tsx, zrzuty docs/audit/visual/schemat-10/gs4-l0{,-detal}.png. Backend nietkniety. | Weryfikacja Fable: type-check 0, eslint 0, vitest sld+engine 180 plikow/3480, accept:sld-v3 ALL PASS, guardy sld_determinism/overlay_no_physics/no_codenames/forbidden_ui_terms/ui_terminology 0; pole connectionSide ADDYTYWNE (goldeny nietkniete). | wysoki (semantyka topologiczna DER na L0 zgodna z modelem — warunek koncowej akceptacji gramatyki mini-RMU) |
| V12K-137 | **OTWARTY→W REALIZACJI (2026-07-22, recenzja ekspercka layoutu od wlasciciela: topologia 10/10, layout drzewa 6,5/10, przestrzen 6/10 — „dzwignia jakosci = silnik layoutu klasy EPLAN/PowerFactory").** 15 punktow + niezmienniki energetyczne + kanoniczny potok 10 etapow. | Recenzja zapisana jako WIAZACA: docs/sld/RECENZJA_EKSPERCKA_LAYOUT_2026-07.md z mapowaniem kazdego punktu na fazy (S6 P0 / S7 P1 / S8 P2) i rozstrzygnieciem kanonicznym: layout STALY miedzy LOD (regula JEDNA KOTWICA), adaptacyjny jest wylacznie detal — swiadome odejscie od litery pkt P2 na rzecz ciaglosci tozsamosci; zmiana wymaga decyzji wlasciciela. | Fazy dopisane do PLAN_SLD_REWORK §0: **S6 (P0, Opus, w locie):** compact tree layout (Reingold-Tilford/Buchheim adaptowany do grzebienia ortogonalnego), piony proporcjonalne do dlugosci galezi, minimalizacja dlugosci/zalaman jako funkcja kosztu, global tree balancing (srodki ciezkosci poddrzew), odstepy gornego pasa +20-35%, stale minimalne swiatlo, miara wykorzystania arkusza w tescie; **S7 (P1):** globalne rozmieszczanie opisow (przemieszczanie, nie tylko odrzucanie), klastry podobnych galezi, wydruk A0/A1; **S8 (P2):** plynnosc przejsc LOD. Twarde niezmienniki: optymalizacja layoutu NIGDY nie zmienia logiki sieci (sondy accept:sld-v3, lod_path_probe, wyrocznie S2, JEDNA KOTWICA, determinizm). | docs/sld/RECENZJA_EKSPERCKA_LAYOUT_2026-07.md (nowy, WIAZACY), docs/plan/PLAN_SLD_REWORK.md §0. Implementacja: engine/sld-layout/layoutEngine.ts + v3/scene/buildScene.ts. | Kryteria: oceny recenzji layout/przestrzen/ergonomia → >=9/10 przy topologii i przeplywie bez regresu; wszystkie sondy i wyrocznie zielone; zrzuty przed/po do oceny wlasciciela. | wysoki (ostatnia dzwignia do klasy przemyslowej wg zespolu eksperckiego) |
| V12K-136 | **ROZSTRZYGNIETY (2026-07-22, program SCHEMAT-10 fazy S1+S2+S3 — wykonawcy Opus/Opus/Sonnet w worktree, scalenie i weryfikacja Fable; S4/S5 w toku pod V12K-135).** Implementacja rdzenia audytu: D1/D9/D12 (S1), D2/D3/D4/D5/D10 (S2), D6/D7/D8 (S3). | Macierz prawdy LOD (audyt §3) jako bramka; raporty wykonawcow z pomiarami. | **S1:** geometria kanwy liczona zawsze przy pelnym szczegole → srodek glifu KAZDEJ stacji i os magistrali IDENTYCZNE na L0/L1/L2 (test „JEDNA KOTWICA"); jeden slownik LOD (L0 Przeglad sieci / L1 Widok operatorski / L2 Stacje i aparatura; v2 zdeprecjonowany); footprint kolumn pod realna tresc (koniec martwych kadrow); goldeny vertical_length_probe wymienione swiadomie z lista. **S2:** silnik etykiet declutter.ts (priorytety S-id>nazwa>moc>parametry, symbol zawsze wygrywa, przegrany NIE renderuje sie, odrzucenia audytowalne w stopNotes); WYROCZNIE NA STALE w suicie: zero-kolizji (tekst-tekst i tekst-symbol, tolerancja 0, kontrole negatywne), ortogonalnosc odcinkow, zakaz surowych enumow (slownik PL enumLabelsPl.ts — koniec 'OVERHEAD'); pomiar na sieci 53 stacji: L0/L1/L2 = 0/0/0. Uczciwe ustalenie: po S1 tor byl juz bezkolizyjny z konstrukcji — S2 = instytucjonalizacja bramek + mechanizm na gestsze sieci. **S3:** colorTokens.ts = JEDNO zrodlo semantyki koloru (napiecie 110/SN/nN x stan x wyroznienie; precedencja: DER > nakladka wynikowa (NIEZMIENIONA) > NOP > napiecie); pomiar literalow 26 w 6 plikach → 1 modul; NOP #FF006E kotwiczony per LOD z testem pozycji (x,y) identycznej na L0/L1/L2; GPZ na tych samych tokenach (koniec bialej ramki); goldeny NIETKNIETE. Zero-Debt S3: pusty test NOP wykryty (fixtura bez realnego NOP → petla po zbiorze zerowym) — naprawiony helperem syntetycznego NOP + asercja niepustosci. | frontend: v3/scene/{buildScene,layout/declutter}, v3/theme/colorTokens (nowy), v3/canvas, v3/compose, v3/sheet, core/enumLabelsPl (nowy), engine/sld-layout, testy + zrzuty docs/audit/visual/schemat-10/s{1,2,3}-l{0,1,2}.png. | Weryfikacja Fable po scaleniu wszystkich trzech: type-check 0, eslint 0, vitest 173 plikow/3316, accept:sld-v3 ALL PASS (sondy §11-§22 z lod_path_probe), 4 guardy zielone. **GAP-y do S4/S5:** emisja 1x typu kabla/korytarz na L1 (decyzja wizualna), geometryczny collapse GPZ, rozjazd no_point_ref vs nop_station_ref w adapterze, voltage_kv w PreviewElementMeta dla aparatury, token selection niepodlaczony. | wysoki (rdzen jezyka wizualnego wdrozony; zoom = skala szczegolu, nie podmiana swiata) |
| V12K-135 | **OTWARTY→W REALIZACJI (2026-07-22, dyrektywa wlasciciela: „ocen schemat — 20 krokow do tylu, niespojne LOD, wez audyt schematow i przemysl od zera, wrocilismy do 2/10"; audyt Fable osobiscie).** Program „SCHEMAT-10". | Ogledziny 4 widokow sieci referencyjnej + recon kodu; PROMPT_RUNDA_SCHEMAT_10 (12 defektow D1-D12) + AUDYT_SCHEMATOW_OD_ZERA (przyczyny zrodlowe per plik/linia). **Rozjazd miedzywatkowy potwierdzony i naprawiony**: watek SLD (sld-schema-cad-scada) wchloniety w calosci, ale 788-liniowa specyfikacja REDESIGN_SLD_SCADA_CAD_FULL (2026-05-03, galaz redesign-electrical-schema) nigdy nie weszla — SPEC V3 przejal kontrakt LOD, NIE przejal hierarchii obiektow domenowych i jednej gramatyki wizualnej (manifestacja: D1/D6/D8/D9); artefakt przyjety cherry-pickiem. | **Sedno**: „kazdy LOD to kompletny rysunek" (SPEC V3 P6) zaimplementowano jako TRZY OSOBNE RYSUNKI bez wspolnej gramatyki: dwa slowniki LOD (v2 LodPolicy 0-4 w pasku vs v3 SceneLod 0-2), osobne galezie strukturalne buildScene per lod (inna kotwica korytarza), brak rodziny glifow stacji, brak silnika etykiet (kolizje tekst-na-tekscie), enum 'OVERHEAD' surowo w etykiecie, footprint kolumn wg bloku L0 (martwe kadry). Naprawa = malzenstwo kontraktu LOD z V3 + gramatyki obiektowej z majowej specyfikacji: MACIERZ PRAWDY LOD (audyt §3) + fazy S1-S5 (PLAN_SLD_REWORK §0): S1 gramatyka stacji/kotwica/jeden slownik (Opus), S2 silnik etykiet+gestosc+PL (Opus), S3 kolor+sekcje+GPZ (Sonnet), S4 motyw D11 (decyzja wlasciciela)+kadr (Sonnet), S5 goldeny+dowod zoom+macierz parytetu (Fable). | docs/sld/{PROMPT_RUNDA_SCHEMAT_10,AUDYT_SCHEMATOW_OD_ZERA}_2026-07.md, docs/prompts/REDESIGN_SLD_SCADA_CAD_FULL.md (przyjety), docs/plan/PLAN_SLD_REWORK.md §0. Implementacja: ui/sld/v3/scene+canvas, ui/sld/v2/lod (kasacja slownika), engine/sld-layout. | Kryteria odbioru: zoom bez zmiany tozsamosci stacji (zrzuty ≥3 kroki), ZERO kolizji mierzone testem bounding-boxow, gestosc etykiet wg regul, L1 czytelny „na 10 s", 100% PL, parytet funkcji testami, ocena wlasciciela ≥9/10. | wysoki (przebudowa jezyka wizualnego kanwy od zera przy zachowaniu funkcji) |
| V12K-134 | **ROZSTRZYGNIETY (2026-07-22, karta Z-3 (Sonnet) + runda korekt Fable po recenzji wlasciciela („zrzuty znowu zle")).** Dokumentacja wizualna rozplywu zwarciowego (pkt 7) + defekty jakosci scen dowodowych wykryte ogledzinami. | Ogledziny Fable wszystkich dostarczonych PNG (dyrektywa #8: zrzuty sa do OCENY, nie do odhaczenia). | **Z-3:** scena `zwarcia-rozplyw` (RozplywZwarciowy z sentinelem THEVENIN_GRID „siec nadrzedna" + maszyna; liczby przepisane z REALNEGO biegu solvera na sieci testu TH-1 — KCL 1e-15) + scena `zwarcia-schemat` (screenshot-harness `?overlay=faultflow`, fixture gpzFeeder, PRODUKCYJNY buildFaultFlowOverlayForSnapshot: 2 strzalki + znacznik pulse). Zero-Debt Z-3: brak favicon.ico w repo (404 w konsoli kazdej strony) naprawiony; regresja wydajnosci pierwszej kompilacji Vite (statyczny import ciezkiej powloki) → import dynamiczny. **Korekty Fable po ogledzinach:** (1) E-29 — podstawienie kroku Zk bylo ZDEGENEROWANE ('(0.1+j0.4)') a bilans FROZEN (1,3365) PRZECZYL skladowym sladu (0,5+j1,7 → 1,7720) na jednym ekranie; stub przeliczony na komplet spojny (Ik1''=16,128 kA, X/R=3,400, kappa=1,4255) + pelne podstawienie dyplomowe; (2) E-30 — werdykt „4 iteracje" vs 2 wiersze sladu; slad uzupelniony do 4 z norma < 1e-6; (3) spec WZMOCNIONY (test maskujacy defekt = drugi defekt): asercje pelnej sumy podstawienia, zgodnosci |Zk| miedzy sekcjami, liczby wierszy sladu == werdykt; (4) **FIX produkcyjny formatMagnitudeKa**: wklad 0,024 kA renderowal sie jako „0,0 kA" (falszowanie realnego wkladu) — wartosci <0,1 kA prezentowane w amperach („24 A"), +3 testy; (5) kadr schematu bez legendy arkusza (ukrycie czysto prezentacyjne na czas zrzutu) + asercje uczciwych etykiet („5,6 kA", „24 A"). Obserwacja: kanwa v3 ma stale tlo techniczne (decyzja projektowa) — zwarcia-schemat identyczny w obu motywach; fixture gpzFeeder ma niemal wspolliniowe odplywy (etykiety torow blisko siebie). | frontend: creator-harness-main.tsx, screenshot-harness-main.tsx, e2e/{dowody-flow-ekspert,zwarcia-rozplyw}-screenshot.spec.ts, ui/sld-overlay/FaultContributionArrow(+testy), public/favicon.ico, 8 PNG (e29/e30 x2, zwarcia-rozplyw x2, zwarcia-schemat x2). | Po scaleniu i korektach: type-check 0, eslint 0, sld-overlay 15 plikow/208, canvas 43, specy dowodowe 14/14 + zwarcia-rozplyw 4/4 z nowymi asercjami; guardy codenames/forbidden_ui_terms zielone. | wysoki (dowod wizualny pkt 7 + podniesienie rygoru asercji scen dowodowych do poziomu tresci, nie tylko obecnosci) |
| V12K-132 | **ROZSTRZYGNIETY (2026-07-22, karta TH-1 (Opus) — GAP z V12K-120/121: rozplyw pradu od zrodla zastepczego (Thevenin) nieliczony przez kontrakt solvera (tylko superpozycja falownikowa); domkniecie pkt 7 karty wlasciciela; wykonawca w worktree, scalenie Fable).** UI pokazywal uczciwy komunikat „rozplyw od zrodla zastepczego nieliczony". | Fizyka WYLACZNIE w solverze (WHITE BOX, zero heurystyk): standardowy podzial pradow z Z-bus — iniekcja jednostkowa -1 pu w wezle zwarcia → V=Z_bus*i_inj; wspolczynnik galezi f=(V_i−V_j)*y_ij (admitancja szeregowa pu spojna z Y-bus przez AdmittanceMatrixBuilder — warunek domkniecia KCL); I=|f|*Ik''(Thevenin); kierunek fizyczny do wezla zwarcia; wszystkie 4 typy zwarcia toru kanonicznego. | **Konwencja (reuzycie zamiast duplikacji):** TA SAMA lista branch_contributions z sentinelem source_id="THEVENIN_GRID" — caly lancuch downstream (ENM projekcja, RozplywZwarciowy, overlay, strzalki v3) agnostyczny na rodzaj zrodla, tor Thevenina splynal end-to-end bez nowego plumbingu. Slad WHITE BOX w NOWYM addytywnym polu `branch_flow_trace` (wzorzec exclude-None; niezmienialny white_box_trace nietkniety). **Dowod bilansu:** z KCL suma pradow galezi wchodzacych do wezla zwarcia == ik_thevenin DOKLADNIE (rel=1e-9, abs=1e-6; empirycznie 5552,132 A i 7216,877 A = ik_thevenin; testy dla 4 typow). **Dowod addytywnosci:** wpisy falownikowe bajt-w-bajt identyczne; payload sprzed delty niezmieniony; goldeny nietkniete. Sankcja V12K-132 w solver_boundary_guard (do usuniecia po scaleniu do main). Komunikat „nieliczony" usuniety dla nowych biegow. | Backend: solvers/short_circuit_iec60909.py (sankcja), enm/canonical_analysis.py, testy. Front: ui2/wyniki/zwarcia, sld-overlay. 10 plikow, +666/−52. Macierz API bez zmian (pole addytywne istniejacych endpointow). | Wykonawca: partycja SC 633, szersza 1230, black/ruff czyste, 8 guardow, front 3296. Weryfikacja Fable po cherry-picku (na wierzchu SLD-W, bez konfliktow): 634/2 skipped, trace_determinism (poetry) 0, 6 guardow zielonych, type-check 0, eslint 0, front 174 plikow/3310. Odnotowane pre-existing: serialize_value bez adnotacji (mypy, sprzed karty). | wysoki (pkt 7 karty wlasciciela domkniety W CALOSCI: pelny rozplyw pradu zwarciowego — siec nadrzedna + maszyny — w tabeli i na schemacie) |
| V12K-133 | **ROZSTRZYGNIETY (2026-07-22, karta SLD-W (Sonnet) — GAP z V12K-121: strzalki kierunku zwarcia tylko na przeslach jednoprzeskokowych (bramka F-1); wykonawca w worktree, scalenie Fable).** Przeslo wielokawalkowe (>=2 galezie ENM laczone mufa bez wlasciciela) nie dostawalo strzalki, mimo ze §16-v3 nadaje kazdemu czlonowi realny ownerRef. | Recon: F-1 ufa kierunkowi tylko przy jednym wpisie segmentPaths z oboma terminalami stacyjnymi; czlony lancucha maja po jednej stronie stacyjnej. | **`multiHopFaultFlowSegmentRefs`:** dowod lancucha po TOZSAMOSCI WEZLA (toTerminal(i).busRef == fromTerminal(i+1).busRef — fizyczny wezel, nie ownerRef/stacja); odwrotna deklaracja from/to nie przechodzi dopasowania → kierunki mieszane przerywaja lancuch automatycznie (dowiedzione testem). Warunki odmowy (zero strzalki): wezel laczacy uzyty !=2 razy (T-wezel), wezel z wlascicielem-stacja, granice bez realnych stacji, lancuch <2. Przedstawiciel grupy = najdluzszy kawalek; JEDEN wpis do slownika → jedna strzalka na przeslo. **Odkrycie determinizmu miedzy LOD:** rozne LOD-y wybieraly ROZNYCH przedstawicieli tej samej grupy (podzial geometrii proporcjonalny per scena; zmierzone na fixturze 52 stacji) — naprawione: przedstawiciel liczony RAZ na LOD 2, ten sam zbior we wszystkich przebiegach. Chirurgicznie: tylko strzalki zwarciowe; przeplyw mocy nietkniety. Testy na REALNYCH fixturach (openTrunkChain 2-czlonowy, substrate52s grupa 4-czlonowa) + negatywne przez minimalna mutacje (T-wezel, odwrocony czlon → 0). | frontend: sld/v3/canvas/{overlay.ts,SldCanvasV3Workspace.tsx}, overlay.test.ts (+55). Backend nietkniety. | Wykonawca: 168 plikow/3225; weryfikacja Fable po cherry-picku: type-check 0, eslint 0, 168/3225, sld_determinism/overlay_no_physics/no_codenames zielone. **Obserwacja zarejestrowana (poza zakresem):** teoretyczna luka F-1 jednokawalkowej przy odwroconej deklaracji from/to (kontraktowo niewystepujaca na 45 realnych przeslach) — ew. osobna karta. | sredni (strzalki zwarciowe na przeslach wielokawalkowych z dowodem tozsamosci, uczciwa odmowa dla niejednoznacznych) |
| V12K-131 | **ROZSTRZYGNIETY (2026-07-22, karta SLD-P (Sonnet) — dwa GAP-y SLD v3 (V12K-120/121: znacznik punktu zwarcia; P-1: menu kontekstowe DER); wykonawca w worktree, scalenie Fable).** Adapter overlay SC emitowal znacznik punktu zwarcia (CRITICAL+pulse), ale kanwa v3 czytala z kanalu faultFlow tylko flows; legacy klasa `sld-overlay-anim-pulse` NIGDZIE nie zdefiniowana (martwy prymityw). Prawy klik w DER na v3 nie otwieral menu (elementKindForMenu('der')→undefined — scena nie niesie gen_type, wybor konfiguratora per podtyp bylby zgadywaniem). | Recon wykonawcy: grep @keyframes (zero trafien poza LoadingOverlay); akcje subtype-agnostyczne z realnym celem: show-ncrfg (P-1), show-results (D-2). | **(1) Znacznik pulse w v3:** pole `faultPointMarkerRef` w SldV3Overlay, funkcja czysta `computeFaultPointMarkerPlacement` (dopasowanie po meta.ownerRef: symbole, potem odcinki, z pominieciem protectionAnnotation), `SceneFaultPointMarkerNode` — kropka + pulsujacy pierscien NATYWNA animacja SVG (animate na r/opacity) w warstwie fault-flow; zero globalnego CSS. Dowod determinizmu testem: warstwy segmentow/symboli innerHTML-identyczne z i bez znacznika; 2 rendery identyczne; goldeny nietkniete. **(2) Menu DER na v3:** generyczny kind 'der' w rejestrze menu z WYLACZNIE realnymi akcjami: show-ncrfg (zgodnosc przylaczeniowa, preselekcja generatora) + show-results (rozplyw, preselekcja po ref). Pozycje NIE przeniesione swiadomie (wymagalyby zgadywania podtypu lub niezweryfikowanego celu): open-{pv,bess,fw}-config, delete-{pv,bess,fw}, show-frt-hvrt (kandydat na osobna karte po weryfikacji preselekcji E-26). | frontend: sld/v3/canvas/{overlay,SldCanvasV3,SldCanvasV3Workspace}, sld/shared/{SldCommandService,SldContextMenuController,CommandPalette}, testy (7 nowych + przepisany negatywny). Backend nietkniety. | Wykonawca: 175 plikow/3597 zielone + 4 guardy; weryfikacja Fable po cherry-picku: type-check 0, eslint 0, 175/3597, sld_determinism/overlay_no_physics/dead_click/no_codenames zielone. | sredni (punkt zwarcia widoczny na schemacie; DER ma menu z realnymi celami bez fabrykacji podtypu) |
| V12K-130 | **ROZSTRZYGNIETY (2026-07-22, karta ST-1 (Opus) — GAP z V12K-125: kontrakt stabilnosci dynamicznej bez szeregu czasowego; wykonawca w worktree, scalenie Fable).** E-32 mial jawna note „kontrakt bez szeregu czasowego", choc przebieg U(t)/f(t) byl juz deterministycznie liczony (generate_voltage_trajectory, application/stability — istniejacy, przetestowany, uzywany w podgladzie reference_networks; parametry 1:1 ze scenariuszem fault-clear). | ZERO nowej fizyki: wystawienie istniejacego przebiegu; rozmiar odpowiedzi chroniony (szereg TYLKO na zadanie, osobny endpoint). | **Backend:** `_execute_dynamic_stability` zapisuje przebieg w raw_result["time_series"]; builder `build_dynamic_stability_time_series` + endpoint `GET /analysis-runs/{id}/results/dynamic-stability/time-series` (wpis w macierzy API); kontrakt: time_unit "s", quantities voltage_pu/frequency_pu [p.u.], points {t_s,...}. **Dowod addytywnosci:** domyslna odpowiedz results/dynamic-stability bajt-w-bajt identyczna z szeregiem i bez (json.dumps sort_keys); klucz time_series nie wycieka do wiersza FROZEN; determinizm 2x identyczny. **Front E-32:** WykresPrzebieguChart (os czasu s, serie z metadanych backendu, przelacznik, tokeny --mvd-*), hook usePrzebiegStabilnosci laduje NA ZADANIE (klik „Pokaz przebieg"); nota „kontrakt bez szeregu" znika TYLKO dla biegow z szeregiem; starsze biegi → uczciwy stan. Solvery/FROZEN nietkniete. | Backend: enm/canonical_analysis.py, api/analysis_runs.py, application/stability. Front: ui2/wyniki/stabilnosc/**. Macierz API +1 wiersz. | Wykonawca: pytest stability/dynamic 135/1 skip + kontrakt 54, black/ruff czyste, 8 guardow, front 30. Weryfikacja Fable po cherry-picku: 135/1 skip, trace_determinism (poetry) 0, type-check 0, 30/30; Zero-Debt Fable: brakujacy wpis macierzy API dodany (api_lifecycle/v12xx_canon czerwone na HEAD → zielone, amend do commita karty). **GAP zarejestrowany:** podpiecie pelnego solvera RMS (kat wirnika/eigenvalues) do biegu fault-clear = odrebny przebieg (wymaga mapowania modeli dynamicznych z ENM). Dlug pre-existing zmierzony: 19 bledow mypy strict w canonical_analysis.py na HEAD (19→19, zero nowych). | wysoki (nota „bez szeregu" zniesiona dla nowych biegow; przebieg na zadanie zgodnie z zasada sladu) |
| V12K-129 | **ROZSTRZYGNIETY (2026-07-22, karta D-2 (Sonnet) — decyzja wlasciciela (AskUserQuestion 2026-07-22): show-results z kontekstem elementu; wykonawca w worktree, scalenie Fable).** Akcja `show-results` z menu elementu SLD prowadzila do legacy E-24 (profile operatora — nie realny dostawca wynikow elementu). | Recon: `setWynikiTab(tab, element?)` juz niosl kontekst (R2-B); zakladka rozplywu NIE konsumowala elementu; TabelaWynikow miala podswietlenie, brakowalo przewiniecia. | **Deep-link z preselekcja:** `show-results` → `setWynikiTab('rozplyw', ref)` + przestrzen Wyniki; `EkranRozplywu` dopasowuje ref LITERALNIE do `bus_id`/`branch_id` wyniku (szyna → podzakladka Szyny, galaz/linia/kabel/trafo → Galezie; wiersz podswietlony + `scrollIntoView` addytywnie w TabelaWynikow z guardem jsdom); aparat przenosi ref pola macierzystego; brak dopasowania/brak wyniku → uczciwy stan bez zgadywania (zero fabrykacji — swiadomie BEZ tabeli „rodzaj SLD → zakladka"). dead_click special_ids zaktualizowane. Zero-Debt: niejednoznaczny selektor testu (kolizja z etykieta osi Recharts) zawezony `within(tabela)`. | `ui/sld/shared/sldActionExecutor.ts`, `ui2/spaces/wyniki/WynikiWarsztat.tsx`, `ui2/wyniki/rozplyw/{EkranRozplywu,TabelaSzyn,TabelaGalezi}`, `ui2/wyniki/wzorzec/TabelaWynikow.tsx`, `scripts/dead_click_guard.py`. Backend nietkniety. | Wykonawca: 227 plikow/3883 zielone (klik natywny w testach executor); weryfikacja Fable po cherry-picku: type-check 0, eslint 0, 106 plikow/1180, 6 guardow UI zielonych. | sredni (ostatnia martwa akcja wynikowa menu SLD domknieta realnym dostawca) |
| V12K-128 | **ROZSTRZYGNIETY (2026-07-22, karta D-1 (Opus) — DELTA FROZEN ZATWIERDZONA przez wlasciciela (AskUserQuestion 2026-07-22: „Tak, dodaj"); wykonawca w worktree, scalenie Fable).** E-29 czytal skladowe Z1/Z2/Z0 wylacznie ze sladu WHITE BOX (krok „Zk"); wynik FROZEN ich nie eksponowal; GAP passthrough requires_z0/z0_source otwarty (V12K-125). | Zgoda wlasciciela na zmiane wersjonowana FROZEN wykonana ADDYTYWNIE; dowod addytywnosci testem bajt-w-bajt. | **Backend:** pola `z1_ohm/z2_ohm/z0_ohm` (complex|None) w wyniku zwarciowym, `to_dict` addytywnie (pomijanie None — payloady starszych biegow BAJT-W-BAJT identyczne, test `TestSequenceComponentsAdditive`: zbior kluczy legacy == kontrakt sprzed delty, `json.dumps` rowne po zdjeciu kluczy addytywnych); kontrakt bazowy `EXPECTED_SHORT_CIRCUIT_RESULT_KEYS` NIETKNIETY, nowe klucze w `OPTIONAL_...`; wiersze kanoniczne przenosza z1/z2/z0; **GAP domkniety:** passthrough `requires_z0`/`z0_source` do pozycji wyniku i wiersza (bylo tylko w proof_binding). **Parytet konsumentow:** raport PDF/DOCX + resultset_v1 global_results (additionalProperties) addytywnie. **Front:** `skladowePunktu` RESULT-FIRST z fallbackiem na slad dla starszych biegow (uczciwy stan). Sankcja V12K-128 w solver_boundary_guard (do usuniecia po scaleniu do main). Goldeny nietkniete. | Backend: solvers/short_circuit_iec60909.py, enm/canonical_analysis.py, reporting/short_circuit_report_{pdf,docx}.py, application/result_mapping, testy. Front: results-inspector/types, ui2/wyniki/skladowe. Guard: solver_boundary_guard.py. | Wykonawca: pytest celowane 575+781, black/ruff czyste, front 87, 5 guardow. Weryfikacja Fable po cherry-picku: kontrakt+kanoniczne+solver 72/72, type-check 0, skladowe+inspector 87/87, solver_boundary/arch/resultset_v1 zielone, trace_determinism zielony w env poetry (czerwien poza venv = brak pydantic w golym pythonie, nie regresja). **Dlug pre-existing zmierzony:** 20 bledow mypy strict w 2 plikach juz na HEAD (20→20, zero nowych) — poza zakresem karty, odnotowane. | wysoki (skladowe sekwencyjne w kontrakcie wyniku; E-29 czyta wynik, slad zostal fallbackiem) |
| V12K-127 | **ROZSTRZYGNIETY (2026-07-22, karty Z-1 (Opus) + Z-2 (Sonnet) — dokumentacja wizualna fali „POZIOM EKSPERTA" (dyrektywa wlasciciela #8: zrzuty zywej aplikacji po kazdym scalonym etapie); wykonawcy w worktree, scalenie Fable z konfliktem addytywnym w harnessie).** Brak dowodow wizualnych 4 nowych ekranow E-29…E-32 i 9 nowych kreatorow. | Wzorzec dowodowy V12K-119: sceny harnessu na REALNYCH komponentach produkcyjnych, stuby fetch 1:1 z kontraktow backendu (ksztalty jak w testach jednostkowych modulow, zero fizyki, zero fabrykacji), twarde asercje tresci + zero bledow konsoli PRZED zrzutem. | **26 PNG w `docs/audit/visual/flow-ekspert/`**: Z-1 — `e29-skladowe`/`e30-zbieznosc`/`e31-stan-fazowy`/`e32-stabilnosc` ×{light,dark} (wywod/slad renderowany OTWARTY; spec `dowody-flow-ekspert-screenshot.spec.ts` 8/8); Z-2 — `kreator-{zrodlo-dyspozycyjne,odgalezienie,slup-odgalezny,zksn,przekaznik,pomiar,pole-nn,przypisanie-katalogu,edycja-parametrow}` ×{light,dark} (asercje: tytul PL, formularz kroku, PanelTeorii; spec `kreatory-ekspert-screenshot.spec.ts` 18/18). Fixtury bramkowane po `creator` (bez kolizji regul fetch miedzy scenami); dla przekaznik/pomiar snapshot z pelnym kontraktem Bay (fallback useFieldReadModel na synteze z bays — udokumentowany). Obserwacja Z-1: powtorny bieg starszych specow daje niedeterministyczne roznice bajtowe 3 pre-existing PNG (font-hinting; tresc identyczna) — przywrocone, do ew. weryfikacji w watku renderu. Nota: etykiety faz E-31 to A/B/C (kontrakt PhaseValues), asercje dopasowane do realnego renderu. | frontend: creator-harness-main.tsx (+13 scen), 2 nowe specy e2e, 26 PNG. Komponenty produkcyjne i backend NIETKNIETE. | Po scaleniu obu kart (konflikt addytywny else-if w harnessie rozwiazany przez Fable): type-check 0, eslint 0, oba specy 26/26 na scalonym harnessie, guardy codenames/forbidden_ui_terms/docs zielone. | sredni (stala dokumentacja dowodowa nowych ekranow i kreatorow fali) |
| V12K-126 | **ROZSTRZYGNIETY (2026-07-22, fala „POZIOM EKSPERTA" — karty P-4+P-5, wykonawcy Opus w worktree, scalenie Fable).** 10 legacy formularzy kreatorow (`ui/network-build/forms/`) poza frameworkiem `kreatory/rama`: bez kontraktu ekranu prowadzacego, bez PanelTeorii, czesc z phantomami. | Recon wykonawcow per operacja (payload op vs pola formularza); decyzja produktowa w karcie: slup odgalezny to INNA operacja niz odgalezienie (insert_branch_pole vs start_branch) — dwa osobne kreatory. | **9 kreatorow ui2**: `zrodlo-dyspozycyjne` (tory agregat/UPS, retire GensetModal+UPSModal), `odgalezienie` (start_branch), `slup-odgalezny` (insert_branch_pole), `zksn` (insert_zksn), `przekaznik` (add_relay, katalog-first), `pomiar` (add_measurement), `pole-nn` (add_nn_outgoing_field), `przypisanie-katalogu` (assign_catalog), `edycja-parametrow` (update_element_parameters). **`ChooseSnSegmentFamily` WYGASZONY z dowodem parytetu** (konfigurator odcinka pokrywa 100% pol). **2 phantomy ubite:** pole `updates` wysylane zamiast czytanego przez backend `parameters`; `catalog_binding` zbierany a ignorowany. Legacy formularze USUNIETE; barrel `network-build/forms` eksportuje tylko `CableValidationBanner`. | `ui2/kreatory/{zrodlo-dyspozycyjne,odgalezienie,slup-odgalezny,zksn,przekaznik,pomiar,pole-nn,przypisanie-katalogu,edycja-parametrow}/**`, `operationFormRegistry`, `network-build/forms/index.ts`. Backend nietkniety. | Wykonawcy: kreatory+workspace+network-build 179 plikow/1847 zielone; obserwacja 1 flake StationBatchPlanner (zielony w izolacji x2, nie regresja). Weryfikacja Fable po scaleniu fali: type-check czysty, eslint 0, 11 guardow zielonych. | wysoki (koniec legacy formularzy; wszystkie kreatory na ramie ui2) |
| V12K-125 | **ROZSTRZYGNIETY (2026-07-22, fala „POZIOM EKSPERTA" — karty P-2+P-3, wykonawcy Opus w worktree, scalenie Fable).** 4 kody huba analiz (E-29/E-30/E-31/E-32) na ZASTEPCZYM kontrakcie F-E5a mimo istnienia realnych danych w kontraktach backendu. | Recon wykonawcow: PowerFlowTrace (oltc_control/solver_method addytywnie), GET results/phase-state, slad WHITE BOX SC (krok „Zk"), results/dynamic-stability + automation-trace. | **4 realne ekrany ui2**: E-30 `wyniki/zbieznosc/` (werdykt zbieznosci, bilans przebiegu, slad petli OLTC, zalozenia zaczepow), E-31 `wyniki/stan-fazowy/` (napiecia/prady fazowe + asymetrie z werdyktem z flag solvera), E-29 `wyniki/skladowe/` (bilans FROZEN + Z1/Z2/Z0 WYLACZNIE ze sladu WHITE BOX + uziemienie punktu neutralnego z zamrozonej wersji ukladu), E-32 `wyniki/stabilnosc/` (scenariusz → werdykt STABLE/UNSTABLE backendu → wielkosci ze statusami checks → slad automatyki; jawna nota: kontrakt bez szeregu czasowego). **Modul zastepczy `wyniki/kontrakt-analizy` WYGASZONY W CALOSCI** (decyzja Fable przy scaleniu: po P-1…P-3 zaden kod nie pozostal na kontrakcie; usuniety modul+testy, router czyta realnych dostawcow). GAP-y zarejestrowane: z1/z2/z0 addytywnie w FROZEN to_dict (wymaga zgody wlasciciela), requires_z0/z0_source passthrough, dynamic_stability time_series, zaczepy PF poza sladem. | `ui2/wyniki/{zbieznosc,stan-fazowy,skladowe,stabilnosc}/**` (nowe), `ui/workspace/WorkspaceSurfaceRouter.tsx`, `screenCanonRegistry.ts`, `ui/power-flow-results/types.ts` (addytywnie), usuniety `ui2/wyniki/kontrakt-analizy/**`. Backend nietkniety. | Wykonawcy: 44+44 testy kart; weryfikacja Fable po scaleniu fali: type-check czysty, eslint 0, guardy zielone (w tym api_lifecycle, v12xx_canon, docs). | wysoki (koniec zastepczego dostawcy kontraktu analizy; kazdy kod E-29…E-34 ma realnego dostawce) |
| V12K-124 | **ROZSTRZYGNIETY (2026-07-22, fala „POZIOM EKSPERTA" — karta P-1, wykonawca Opus w worktree, scalenie Fable).** E-33 „Wklady zrodel" i E-34 „Weryfikacja cieplna i dynamiczna" na zastepczym kontrakcie, mimo ze program ZWARCIA-PRO dostarczyl realne sekcje (wklady PRO F2, bilans IEC F1, dobor aparatow); `show-ncrfg` prowadzil do legacy E-26. | Recon wykonawcy: zakladka zwarc warsztatu Wynikow zawiera pelny zakres E-33/E-34 (wklady + bilans + I2t); duplikowanie ekranow lamaloby reuse. | **Deep-linki zamiast duplikacji:** E-33/E-34 prowadza `setWynikiTab('zwarcia')` do zakladki zwarc (hub analiz, nawigacja analityczna, raport); `show-ncrfg` → `MacierzNcRfg` z preselekcja generatora (props); `componentKey` E-33/E-34 w rejestrze ekranow zaktualizowany na realnego dostawce `EkranZwarc` (metadana, precedens E-26/V12K-041); tabela audytu pozostalych akcji SLD w raporcie karty. GAP-y zarejestrowane: `show-results`→E-24 (wymaga decyzji produktowej o kontekscie elementu), martwy `ReadOnlyPanelRouter`, menu kontekstowe DER na v3. | `ui2/wyniki/analizy/model.ts`, `ui/sld/shared/sldActionExecutor.ts`, `ui2/zgodnosc/MacierzNcRfg` (props preselekcji), `screenCanonRegistry.ts`, dead_click guard special_ids. Backend nietkniety. | Wykonawca: paczki celowane zielone; weryfikacja Fable po scaleniu fali: type-check czysty, dead_click_guard + pozostale guardy zielone. | sredni (zdolnosci E-33/E-34 dostarczane przez realnego dostawce bez rownoleglej sciezki) |
| V12K-123 | **ROZSTRZYGNIETY (2026-07-22, karta S-C — trzy delty produktowe ZATWIERDZONE przez wlasciciela (AskUserQuestion 2026-07-22: 3x TAK); wykonawca w worktree, scalenie Fable).** Delty z audytu parytetu F5 (V12K-118) wymagajace decyzji: fingerprint dowodu SC3F, werdykty doboru aparatow, zakres porownan. | Zgoda wlasciciela na wszystkie trzy; wykonanie end-to-end z jawnym oznaczeniem zmiany wersjonowanej w commicie. | **(1) Kroki I_b i I2t w dowodzie SC3F:** EQ_SC3F_014 (I_b przy t_b — podstawienie lustrem formuly WHITE BOX solvera, wynik z FROZEN, zero fizyki w proof engine) + EQ_SC3F_015 (I2t=I_th^2*t_k, IEC 60909-0 par. 12); SC3FInput addytywnie ib_ka/tb_s (None -> uczciwy brak kroku dla starszych wejsc, dowod legacy 8 krokow); dowod 9 krokow, determinizm 2x identyczny (sha256 stabilny); reguly jednostek + audyt anti-double-counting. **(2) Porownanie I2t w doborze aparatow:** przy t_th != tk porownanie energii Ith_req^2*tk <= Ithr^2*t_thr (kryterialne, zero nowej fizyki) z krokiem wywodu EQ_P12_006b; pomiar zmiany werdyktow na fixture'ach: 1 (20kA@2s vs 18kA@1s: 324<=648... FAIL->PASS); czasy rowne — zachowanie bez zmian; brak danych katalogowych — FAIL jak dotad. **(3) Delty pelnego bilansu w porownaniach SC:** SC_DERIVED_DELTA_KEYS (stala kolejnosc), addytywne xr_ratio_delta/i2t_delta w P10b (service+api+typ TS), front porownan +15 kolumn eksperckich (Rk/Xk/|Zk|/X/R/I2t x A/B/delta), starsze wyniki -> kreski, delta bez dowodRef (regula R3-C). **Zero-Debt:** naprawa latentnego TypeError (zkk_ohm {re,im} w sc_comparison), zastane F401/UP017/I001/F841 + braki anotacji naprawione. | 26 plikow: proof_engine (equation_registry, unit_verifier, proof_generator, packs), equipment_proof/generator, domain/sc_comparison + results, application/comparison, api/comparison, front ui2/wyniki/porownanie + ui/comparison/types. FROZEN solvery nietkniete. | Wykonawca: celowane 41, pelne suity dotkniete 1575/5 skipped, mypy strict 9 plikow Success, guardy arch/trace_determinism/solver_boundary zielone, front 82+50. Weryfikacja Fable po cherry-picku: proof_engine+equipment+pack 317/317, comparison 150/150, porownanie+comparison 132/132, type-check czysty, 4 guardy zielone. | wysoki (trzy decyzje wlasciciela wdrozone; parytet konsumentow SC domkniety w calosci) |
| V12K-122 | **ROZSTRZYGNIETY (2026-07-22, karta S-A — GAP z audytu F5 (V12K-118): duplikacja fizyki w generate_sc1_proof; wykonawca w worktree, scalenie Fable).** Proof engine SC1 liczyl Z_ekw/prady skladowe/Ik''/kappa/ip/I_th we wlasnym kodzie (lamanie NOT-A-SOLVER). | Recon wykonawcy: jedyny wywolujacy (pack sc_asymmetrical) NIE ma dostepu do wyniku solvera FROZEN, a solver liczy INNE wielkosci brzegowe (I_th=Ik''*sqrt(m+n) vs sqrt(tk), clamp kappa) — odczyt z FROZEN zmienialby liczby dowodu (delta produktowa, NIE wykonana). | **Fizyka przeniesiona VERBATIM (bit-w-bit) do warstwy solverow**: nowy modul `network_model/solvers/short_circuit_asymmetrical_quantities.py` (compute_sc1_asymmetrical_quantities, frozen dataclass WHITE BOX); `generate_sc1_proof` = czysty formatter (wymaga `quantities`); pack wola solver (wzorzec 1:1 z SC3F). **Dowod niezmiennosci: 6 hashow SHA-256 goldenow (proof.json+proof.tex x 1F-Z/2F/2F-Z) identycznych przed/po; goldeny NIETKNIETE.** +test_sc1_not_a_solver (5 testow: brak fizyki w generatorze). **Zero-Debt przy okazji:** flake KreatorStudium ZREPRODUKOWANY (bieg 4/5) i zdiagnozowany jako REALNY DEFEKT PRODUKTU (dwufazowy commit: select pokazywal typ, stan Reacta pusty -> panel danych i przycisk biegu martwe mimo widocznego wyboru); naprawa u zrodla (stan pochodny `efektywnyTyp`, jeden commit); 2 pre-existing bledy mypy naprawione. **Sankcje w solver_boundary_guard (Fable):** guard diffuje origin/main...HEAD i był czerwony przez usankcjonowany FIX V12K-120 — dodany mechanizm SANCTIONED_CHANGES (sciezka -> wpis rejestru z uzasadnieniem, drukowany w wyjsciu; nieusankcjonowane zmiany nadal czerwone; wpis do usuniecia po scaleniu do main). | Backend: proof_generator.py, packs/sc_asymmetrical.py, solvers/short_circuit_asymmetrical_quantities.py (nowy), testy. Frontend: oze/studium/KreatorStudium.tsx. Guard: scripts/solver_boundary_guard.py. | Wykonawca: proof_engine+pack 306, pelna regresja 6470/0, paczka oze 3x436/436. Weryfikacja Fable po cherry-picku: proof_engine+api+golden 434/434, studium 43/43, solver_boundary_guard zielony z jawna sankcja, arch_guard OK. | wysoki (NOT-A-SOLVER przywrocony w torze SC1 bez zmiany ani jednego bajtu dowodow) |
| V12K-121 | **ROZSTRZYGNIETY (2026-07-22, karta S-B — GAP z V12K-120: kierunek przeplywu pradu zwarciowego na SLD; wykonawca w worktree, scalenie Fable).** Kierunek byl w danych (branch_contributions.direction z FROZEN solvera), overlay go nie pokazywal. | Recon: payload overlay SC budowany w calosci po stronie frontu -> sciezka czysto frontowa, lustro OverlayPayloadV1 NIETKNIETE. | **Strzalki kierunku na kanwie v3:** kanal `faultFlow` w useOverlayStore (loadOverlay zeruje kanal — strzalki nie przezywaja podmiany wyniku); `buildFaultFlowOverlayFromScene` + warstwa `sld-v3-fault-flow-overlay`; prymityw FaultContributionArrow (trzon+grot+etykieta kA, format PL) wzdluz najdluzszego biegu polilinii (selekcja jak F9.5); orientacja from_to/to_from wg udowodnionej tozsamosci przesla (bramka F-1 singleHopSegmentRefs — mieszane kierunki/brak pradu -> BRAK strzalki, uczciwe „nie wiem"); grubosc ∝ wadze, kolory z jednej prawdy tercylowej adaptera W-C (rodzina czerwieni, odrebna od energizacji i przeplywu mocy). Determinizm dowiedziony testem: warstwy segmentow/symboli bajt-identyczne ze strzalkami i bez; goldeny layoutu nietkniete. | 12 plikow frontend (sld-overlay, sld/v3/canvas, zwarcia/pokazNaSchemacie) + wyrocznia faultFlowOverlayTracesToInput. Backend nietkniety. | Wykonawca: 454/454 obszar + pelne SLD 166 plikow/3196; weryfikacja Fable po cherry-picku: type-check czysty, 34 pliki/454, guardy overlay_no_physics/sld_determinism/codenames/ui_no_physics zielone. **GAP-y:** rozplyw Thevenina (kontrakt solvera), przesla wielokawalkowe poza bramka F-1, znacznik pulse punktu zwarcia w v3 (mala osobna karta). | wysoki (pkt 7 karty wlasciciela domkniety w granicach kontraktu danych) |
| V12K-120 | **ROZSTRZYGNIETY (2026-07-22, ZWARCIA-PRO faza F4 — karta W-C, pkt 6+7 karty wlasciciela; wykonawca w worktree, scalenie Fable).** Wyniki zwarciowe bez powiazania ze schematem i bez rozplywu pradu w galeziach; GAP wkladow galeziowych z F2 otwarty. | Raport wykonawcy z pelna regresja backendu 6462 passed/0 failed (partycje A/B/C1-C3). | **(delta GAP)** kanoniczny wykonawca SC wola 4 metody FROZEN solvera z include_branch_contributions=True; projekcja `_sc_rozplyw_galeziowy` (A->kA, nazwy z grafu, kierunek z solvera, sort deterministyczny); pole addytywne `branch_contributions` w wierszach (None=starszy wynik, []=policzono bez wkladow falownikowych); kontrakt frontu `ShortCircuitBranchFlow`. Dowod addytywnosci testem (to_dict bez pola bajt-w-bajt identyczny z/bez opcji). **FIX pre-existing u zrodla (Zero-Debt):** short_circuit_iec60909.py:590 — wektor iniekcji mial wymiar node_id_to_index zamiast wymiaru Z-bus; matmul pekal dla KAZDEJ sieci z zamknietym lacznikiem (scalone wezly); dotykalo tez tor scenariuszy; +test regresyjny. **(pkt 2)** sekcja „Rozplyw pradu zwarciowego" (RozplywZwarciowy: galaz, kierunek PL, |I| kA z dowodRef, zrodlo w ekspercie; uczciwe stany). **(pkt 6)** „Pokaz na schemacie": hook usePokazZwarcieNaSchemacie — reuse wzorca V12K-073 (selectElement+centerSldOnElement+navigateToSld). **(pkt 7)** adapter overlay ShortCircuitFlowOverlayAdapter w rodzinie adapterow (znacznik punktu zwarcia CRITICAL+pulse, element per galaz, grubosc/kolor wg skali prezentacyjnej, legenda PL), wpiecie przez produkcyjny useOverlayStore.loadOverlay. **GAP-y zarejestrowane:** strzalki kierunkowe wymagaja kanalu kierunku w OverlayPayloadV1 + renderu scenowego v3 (prymityw FaultContributionArrow istnieje); rozplyw od zrodla Thevenina nieliczony przez kontrakt solvera (tylko superpozycja falownikowa) — komunikat uczciwy w UI. | Backend: enm/canonical_analysis.py, network_model/solvers/short_circuit_iec60909.py (FIX), testy. Frontend: ui2/wyniki/zwarcia/{RozplywZwarciowy,pokazNaSchemacie,zwarciaModel,EkranZwarc,strings}, ui/sld-overlay/ShortCircuitFlowOverlayAdapter, ui/results-inspector/types. | Wykonawca: backend 6462/0 (pelna partycja), frontend wyniki 556/46 plikow, sld-overlay 202, guardy w tym sld_determinism 6/6 i trace_determinism 3/3. Weryfikacja Fable po cherry-picku: canonical+solvery 79/79, wyniki+overlay+inspector 68 plikow/815, 6 guardow (w tym fault_scenarios_determinism). | wysoki (pkt 6+7 domkniete w zakresie kontraktu danych; program ZWARCIA-PRO F1-F5 SCALONY W CALOSCI) |
| V12K-119 | **ROZSTRZYGNIETY (2026-07-22, dyrektywa wlasciciela: „interesuja mnie wszystkie ekrany od systemu do konca wraz z pelnymi dowodami akademickimi — pokaz i udowodnij"; karty V-A + V-B, wykonawcy w worktree, scalenie Fable z rozwiazaniem konfliktu harnessu).** Brak jednolitej, weryfikowalnej dokumentacji wizualnej wywodow na wszystkich ekranach wynikowych. | Sceny harnessu na REALNYCH komponentach ze stubami 1:1 z builderow backendu (liczby spojne miedzy krokami); spec Playwright z twardymi asercjami (werdykt PL + math-rendered + norma + zero bledow konsoli) — zrzut powstaje TYLKO gdy asercje przejda (to jest „udowodnij"). | **12 ekranow z OTWARTYM wywodem** w `docs/audit/visual/dowody/` (24 PNG, oba motywy): LoM (okno normatywne per check), FRT sekwencja (SCR=S_sc/S_n z podstawieniem) + trajektorie (m_U=min(u-u_kr)), OLTC sweep (t(n)=1+(n-n0)*du/100, 5 podstawien 1:1 z tabela), macierz NC RfG (SladTestu T16: wzor->dane->podstawienie->wynik->jednostki), kompensacja po realnym biegu (kandydaci+slad), sila sieci (WSCR z licznikiem/mianownikiem), odbior nN (slad_pl per pomiar), estymacja WLS (chi2>prog, LNR, iteracje J), SSCI (Nyquist white_box), migotanie (P_st=(SUM P_st,i^m)^(1/m) + d=kmax*Sn/Sk''*100%), arc flash (IEEE 1584: I_arc/E/AFB z kotwami napieciowymi). Zero-Debt wykonawcow: fixture FRT (11->10 punktow), kolizja legendy z osia X naprawiona u zrodla w 4 wykresach produkcyjnych, koherencja liczb stuba arc flash (J/cm2<->cal/cm2). Obserwacja: 1 flake KreatorStudium w paczce (zielony w izolacji i w powtorce paczki) — odnotowany, nie regresja. | frontend: creator-harness-main.tsx (+11 scen), e2e/dowody-{oze,wyniki}-screenshot.spec.ts (nowe), 24 PNG; produkcyjne: 4 wykresy (legenda), fixture FRT. Backend nietkniety. | Po scaleniu obu kart: type-check czysty, dowodowe specy 22/22, dotychczasowe specy zrzutow 18/18, oze+oltc vitest 451/451, eslint 0, guardy zielone. | wysoki (dowod wizualny atutu systemu; stala strona dowodowa do oceny wlasciciela) |
| V12K-118 | **ROZSTRZYGNIETY (2026-07-22, ZWARCIA-PRO faza F5 — karta W-D, pkt 13 karty wlasciciela: „raporty PDF/Excel/Word zawieraja identyczny zakres jak UI; bez rozbieznosci miedzy modulami"; wykonawca w worktree, scalenie Fable).** Raporty przebiegu PDF/DOCX prezentowaly z wynikow SC tylko Ik''/ip(/Ith), wywod jako surowy JSON; zabezpieczenia nie mialy dostepu do Ib. | Audyt parytetu 12 konsumentow (tabela §3 w PROGRAM_ZWARCIA_PRO_2026-07.md): raporty przebiegu, eksporty JSON/JSONL, samodzielne eksportery wyniku, pakiet dowodowy SC3F, endpoint wkladow, zabezpieczenia, dobor aparatow, termika, porownania SC, analizy pochodne, ZIP projektu; Excel — brak eksportu wynikow do XLSX w systemie (JSON/JSONL realizuje pozycje, zero fabrykacji). | **WYROWNANE:** (1) raport przebiegu PDF/DOCX — pelny bilans F1 (3 linie/punkt: prady + Rk/Xk/|Zk|/X/R/kappa/c/Un/tk/tb/Ib/Ik/I2t) 1:1 z wierszy kanonicznych + wywod krokowy (Wzor/Podstawienie/Wynik) zamiast zrzutu JSON + naprawa maskowania 0.0; (2) samodzielne eksportery ShortCircuitResult — rozbicie Ik (Thevenin/falowniki/suma) addytywnie + naprawa mojibake (Zero-Debt); (3) zabezpieczenia — `ib_a` dopuszczony w ALLOWED_QUANTITIES (odczyt, zero fizyki). Projekcje |Zk|/X/R/I2t celowo TYLKO w ENM (§0.2 — kierunek importow). Zero-Debt: 16 pre-existing bledow mypy naprawionych u zrodla. | Backend: api/analysis_run_exports.py, network_model/reporting/short_circuit_report_{pdf,docx}.py, domain/protection_current_source.py + testy (+5). Dokument programu §3 (audyt BINDING). FROZEN/solvery/goldeny nietkniete. | Weryfikacja Fable po cherry-picku: eksporty+protection+proof_engine 331/331, pelne API 370/370; docs/utf8/resultset_v1_schema guardy zielone. **GAP-y zarejestrowane w §3:** duplikacja fizyki w generate_sc1_proof (proof_generator.py:711-748 liczy Z_ekw/kappa/ip zamiast czytac wynik solvera — naprawa u zrodla osobna karta); delty produktowe za zgoda wlasciciela: kroki Ib/I2t w dowodzie SC3F (zmiana fingerprintu), porownanie I2t w doborze aparatow (zmiana werdyktow), bilans w deltach porownan. | wysoki (pkt 13 domkniety w zakresie bez decyzji produktowych) |
| V12K-117 | **ROZSTRZYGNIETY (2026-07-22, ZWARCIA-PRO faza F3 — karta W-B, pkt 8-10 karty wlasciciela; wykonawca w worktree, scalenie Fable).** White Box wkladow byl jednym dlugim blokiem; regula malych silnikow bez wartosci (sam werdykt); brak checklisty walidacji metody IEC. | Raport wykonawcy: sekcje budowane w backendzie z TEJ SAMEJ tresci co plaski wywod (gwarancja 1:1 testem); liczby reguly 5% z white_box solvera. | **(pkt 8)** Wspolny `wzorzec/SladSekcyjny.tsx` — akordeon sekcji (domyslnie zwiniete, aria-expanded, norma przy tytule); backend `wywod_sekcje` addytywnie: „Dane wejsciowe i model" (IEC 60909-0:2016) / „Wklad: {maszyna}" (par. 6.6) / „Suma wkladow i reguly" (par. 6.6); plaski `wywod` bez zmian (fallback starszych odpowiedzi). **(pkt 9)** Regula 5% PELNA: tresc + wymaganie + prog (0.05*I''k z liczba) + wartosc obliczona (suma I''k,M) + werdykt + wplyw na Ib; LaTeX podstawienia; solver addytywnie eksponuje `ikss_async_machines_a`, `small_motor_limit_a`. **(pkt 10)** `walidacja_iec` addytywnie: 6 pozycji PASS/FAIL/INFO (norma, c z biegu, metoda Z-bus, maszyny asynchroniczne, regula 5%, determinizm input_hash) — budowana w backendzie z realnych wlasnosci biegu; UI checklista z ikonami w SladSekcyjny. | Backend: api/proof_pack.py, network_model/solvers/machine_sc_iec60909.py (addytywne), testy +. Frontend: wzorzec/SladSekcyjny.tsx (nowy, do reuse), zwarcia/{WkladyZwarciowe,api,EkranZwarc}, testy. | Backend 30/30, black/ruff czyste; frontend po scaleniu z F2: type-check czysty, wyniki 45 plikow/547, pelne ui2 189/2047, eslint 0, 6 guardow. | wysoki (pkt 8-10 karty domkniete; SladSekcyjny = kanoniczny wzorzec sekcyjnego White Box) |
| V12K-116 | **ROZSTRZYGNIETY (2026-07-22, ZWARCIA-PRO faza F2 — karta W-A, pkt 5+12 karty wlasciciela; wykonawca w worktree, scalenie Fable z rozwiazaniem konfliktow z F3).** Sekcja wkladow byla sama tabela; wykres tylko Ik''. | Raport wykonawcy z pomiarem; konflikt F2xF3 w WkladyZwarciowe+tescie rozwiazany przez Fable (filtr/tabela/szczegol/wykres udzialow Z F2 + wywod sekcyjny/walidacja IEC Z F3, sekcyjny ma pierwszenstwo nad plaskim). | **(pkt 5)** Wklady PRO: sortowanie (reuse TabelaWynikow), filtr tekstowy po zrodle (udzialy % liczone PRZED filtrem — filtr nie przeklamuje), wykres udzialow (WykresUdzialowChart, konwencja wykresow wynikow), rozwiniecie wkladu (klik wiersza -> panel szczegolu maszyny: typ PL, Ir, I''k/Ir, mu, q, Ib + wywod dyplomowy TEJ maszyny przez SladWywodu; typ frontu rozszerzony addytywnie o pola maszynowe endpointu; starsza odpowiedz -> uczciwy stan). **(pkt 12)** Wykres przelaczalny: Ik''/ip/Ith/Sk''/I2t (dane z wierszy kanonicznych F1; WykresIkssChart uogolniony addytywnie — domyslne = kontrakt 1:1; wielkosc bez danych -> uczciwy komunikat). **GAP zarejestrowany (pkt 3 karty):** wklady galeziowe branch_contributions nieosiagalne w kontrakcie read-only (kanoniczny wykonawca wola solver bez opcji: canonical_analysis.py:878-909; builder nie przenosi pola: 1902-1941; kontrakt frontu bez pola) — delta backendowa do karty F4/F5, bez fabrykacji. Zero-Debt: nieaktualne TODO-KARTY w zwarciaModel przepisane do stanu faktycznego. | Frontend: ui2/wyniki/zwarcia/** (15 plikow; WykresZwarc, WykresUdzialowChart nowe). Backend nietkniety. | Bramki wykonawcy: 44 pliki/539; po scaleniu z F3: wyniki 45/547, pelne ui2 189/2047, type-check/eslint 0, 6 guardow. | wysoki (pkt 5+12 karty domkniete) |
| V12K-115 | **ROZSTRZYGNIETY (2026-07-22, karta wlasciciela „przebudowa ekranu wynikow zwarciowych IEC 60909" pkt 1-4+11; program ZWARCIA-PRO faza F1, Fable osobiscie).** Tabela zwarc pokazywala tylko Ik''/ip/Ith/Sk'' — a FROZEN solver liczy PELNY bilans (Zk Thevenina complex, R/X, kappa, c, Un, tk, tb, Ib, Ik calkowity/Thevenin/falowniki, wklady). Defekt lancucha: `enm/canonical_analysis.build_short_circuit_results` FILTROWAL pola solvera. | Recon: `ShortCircuitResult.to_dict` (solver FROZEN) niesie zkk_ohm/rx_ratio/kappa/c_factor/un_v/tk_s/tb_s/ib_a/ik_*_a; `payload = result.to_dict()` JEST w raw_result.results — wiec naprawa u zrodla lancucha, solver NIETKNIETY. | **Pelny bilans addytywnie:** (1) ENM `_sc_pelny_bilans` — pola rk/xk/zk_ohm, rx/xr_ratio, kappa, c_factor, un_kv, tk/tb_s, ib/ik/ik_thevenin/ik_inverters_ka, i2t_ka2s; projekcje deterministyczne wielkosci JUZ policzonych (|Zk|=hypot, X/R=1/(R/X), I2t=Ith^2*tk — definicja IEC 60909-0 par. 12; klasa przeksztalcen jak A->kA, komentarz normowy); tables meta +9 kolumn (API). (2) Frontend: `ShortCircuitRow` +15 pol addytywnych; kolumny impedancyjne Rk/Xk/|Zk|/X/R/kappa w tabeli (tryb ekspercki — czytelnosc przy duzych sieciach); panel „Bilans IEC 60909" wybranego punktu (16 pozycji, komplet bez White Box); starsze wyniki -> uczciwe kreski. Program: `docs/uiux/PROGRAM_ZWARCIA_PRO_2026-07.md` (BINDING, fazy F1-F5, rozstrzygniecia par. 0: mu/q sa per maszyna — miejsce w sekcji wkladow, zakaz punktowego mu). | Backend: enm/canonical_analysis.py (+_sc_pelny_bilans, tables meta), tests/test_canonical_analysis_api.py (+asercje bilansu: obecnosc i spojnosc |Zk|=hypot(Rk,Xk), X/R=1/(R/X), I2t=Ith^2*tk, kappa>1, c=1.10). Frontend: ui/results-inspector/types.ts, ui2/wyniki/zwarcia/{zwarciaModel,BilansIEC(nowy),EkranZwarc,strings,zwarcia.css,testy,fixtures}, harness+spec. FROZEN solver nietkniety; kontrakt addytywny. | Backend: canonical API 17/17, black/ruff czyste. Frontend: type-check czysty, zwarcia 45/45, pelne ui2 186 plikow/2006 zielone, 5 guardow (w tym resultset_v1_schema); zrzuty zwarc 2/2 z panelem bilansu. Fazy F2 (wklady PRO) i F3 (White Box sekcyjny + reguly normowe + panel walidacji IEC) delegowane; F4 (SLD sync) i F5 (raporty) w kolejce programu. | wysoki (fundament ZWARCIA-PRO: kazda wielkosc normy w API i na ekranie) |
| V12K-114 | **ROZSTRZYGNIETY (2026-07-22, dyrektywa wlasciciela II: „I''k ma byc pelny kazdy krok, podstawienie i wynik jak w pracy magisterskiej; rozbuduj backend i wszystko inne co trzeba, nic na potem, z propagacja na wszystkie inne obliczenia"; Fable osobiscie + karta T-C delegowana).** Wywod wkladow zwarciowych byl skrotowy (jedna linia I_b = mu*q*I''k bez wywodu skladnikow — skad mu? skad I''k?). | Recon: solver machine_sc_iec60909 liczy wszystkie wielkosci posrednie (Z''m, |Z_mk|/|Z_kk|/|Z_m| pu, I_base, krotnosc, krzywe mu/q per pasmo t_min), ale ich nie eksponowal per krok — luka wzgledem WHITE BOX rule. | **Wywod dyplomowy budowany W SOLVERZE (warstwa fizyki = wlasciciel wielkosci posrednich):** `MachinePartialContribution.wywod` (addytywne pole + to_dict) — kroki {tekst, latex}: impedancja maszyny z modulem -> prad czesciowy wzorem superpozycji Z-bus z PELNYM podstawieniem (c, |Z_mk|, |Z_kk|, |Z_m|, I_base) -> krotnosc I''k/Ir z podstawieniem -> wspolczynnik mu z KRZYWA wlasciwa dla pasma t_min (0.84+0.26e/0.71+0.51e/0.62+0.72e/0.56+0.94e) z podstawieniem i nota przyciecia -> q z krzywa ln(m) per pasmo (asynchroniczne) -> nota crowbar (DFIG) -> I_b z pelnym podstawieniem. Endpoint contributions komponuje: naglowek (model, I''k calkowity, c, t_min) + separator per maszyna + kroki solvera + suma + regula 5%. Zasada par. 1.5 dopisana do dokumentu BINDING (wywod pelny = wzor -> podstawienie -> wynik dla KAZDEJ wielkosci; warstwa liczaca buduje wywod). Propagacja: walidacja energetyczna JUZ zgodna (audyt: 5 kontroli ma wzor+podstawienie); GAP-y OLTC/LoM/trajektorie FRT = karta T-C (wykonawca, w toku). | Backend: network_model/solvers/machine_sc_iec60909.py (+_MU/_Q_CURVE_LATEX, _krzywa, wywod_maszyny, pole wywod), api/proof_pack.py (_wywod_wkladow kompozycja), testy solvera +2 (pelny wywod sync: wzor superpozycji, podstawienie c=1.10, krzywa mu pasma 0.10 s, Ib z podstawieniem, serializacja; async/DFIG: krzywa q ln(m), nota crowbar, determinizm) i API. Frontend: harness (stub 1:1 z nowym ksztaltem). FROZEN nietkniete (pole addytywne, wyniki liczbowe bez zmian). | Solver 17/17, kontrybucje+proof engine 311/311, API proof_pack 7/7, black/ruff czyste; type-check czysty; zrzut zwarc: pelny wywod dyplomowy KaTeX potwierdzony wizualnie (Z''m -> I''k,m -> krotnosc -> mu -> q -> I_b -> suma -> regula 5%). | wysoki (WHITE BOX rule domkniety na poziomie krokow; wzorzec wywod_maszyny kanoniczny dla warstwy solverow) |
| V12K-113 | **ROZSTRZYGNIETY (2026-07-22, dyrektywa wlasciciela: „wzory renderowane KaTeX zapisz jako zasade; do zwarc tez dodaj slad obliczen i wszystkich innych obliczen — glowny atut systemu, uzywalny wszedzie, na zadanie/klik, bez przeladowania ekranu; usystematyzuj czcionki — 3 rodzaje na jednym ekranie to nieprofesjonalne"; fala 1 Fable osobiscie, fale T-A/T-B delegowane).** Trzy defekty systemowe: (a) slady migotania i arc flash renderowaly formula_latex jako SUROWY LaTeX w <code>; (b) wklady zwarciowe bez sladu obliczen (mimo ze solver liczy mu/q/i_b i white_box); (c) chaos typograficzny — Inter (legacy body) + Segoe UI (ui2) + dwa rozne stosy mono na jednym ekranie. | Recon: MathRenderer gotowy (fail-safe, flaga); machine_sc_iec60909 niesie pelny material wywodu; grep font-family: 3 literaly w ui2 (shell/inspector/search) + legacy. | **Zasada zapisana jako BINDING:** `docs/uiux/ZASADA_WYWODOW_KATEX_I_TYPOGRAFII_2026-07.md` (par.1 KaTeX obowiazkowy, par.2 slad na zadanie wszedzie, par.3 dokladnie 2 kroje + font matematyczny KaTeX, par.4 macierz rolloutu). Wdrozenie fali 1: (a) backend `_wywod_wkladow` w proof_pack.py — addytywne pole `wywod` {tekst,latex} odpowiedzi contributions (czysty formatter, ZERO arytmetyki — liczby wylacznie z wyniku solvera); (b) wspolny komponent `wzorzec/SladWywodu.tsx` (zwiniety przycisk -> lista, latex przez MathBlock) + wpiecie w WkladyZwarciowe (hook `useWkladyZwarciowe` zwraca pare wklady+wywod); (c) migotanie + arc flash: formula_latex przez MathBlock zamiast <code>; (d) typografia: token `--mvd-font-sans` w tokens.css, 3 literaly font-family -> var. | Backend: api/proof_pack.py, tests/api/test_proof_pack_api.py. Frontend: wzorzec/{SladWywodu.tsx(nowy),strings,index,wzorzec.css}, zwarcia/{api,EkranZwarc,WkladyZwarciowe,testy}, jakosc/{EkranJakosci,testy mig+af}, theme/tokens.css, shell/inspector/search css, harness+spec. FROZEN nietkniete (pole addytywne). | Backend 7/7 (wywod: wzor normy, podstawienie z liczb solvera, suma, regula 5%, determinizm), black/ruff czyste. Frontend: type-check czysty, ui2 186 plikow/1992 zielone, eslint 0, 8 guardow zielonych; zrzuty 16/16 — wywod KaTeX na scenie zwarc potwierdzony wizualnie. Fale T-A (kreatory) i T-B (pozostale ekrany + sweep fontow) delegowane rownolegle. | wysoki (zasada systemowa; wzorzec SladWywodu do reuse w kazdym ekranie) |
| V12K-112 | **ROZSTRZYGNIETY (2026-07-22, karta recenzyjna wlasciciela R3-D: „Wyniki obliczen wygladaja jak z Commodore — uzyj LaTeX lub czegos lepszego"; Fable osobiscie).** Slad WHITE BOX walidacji energetycznej (R2-A/V12K-105) renderowal sie jako surowe linie ASCII w monospace — wzory ulamkowe nieczytelne, poziom prezentacji ponizej kanonu Proof Engine (matematyka wylacznie LaTeX). | Recon: `ui/proof/MathRenderer.tsx` juz istnieje (KaTeX, testidy math-rendered/math-fallback, flaga ENABLE_MATH_RENDERING domyslnie wlaczona, fail-safe fallback do `<code>`); builder walidacji emitowal czysty tekst. Reuse zamiast duplikacji. | **Strukturalny slad z LaTeX (addytywnie):** backend — `white_box` z `tuple[str,...]` na kroki `{tekst, latex}` (helper `_krok` + `_white_box_progowe`); LaTeX wywodu per kontrola: obciazenie `\varepsilon = |I|/I_n·100%` (+ podstawienie liczbowe), transformator `max(|S_gora|,|S_dol|)/S_n`, napiecie `\delta U = |U-U_n|/U_n·100%`, straty `\Delta P%`, bierna `tan/cos phi`; serializer bez zmian (dicty JSON). Frontend — typ `KrokSladuWalidacji`, `SzczegolWalidacji` renderuje `MathBlock` dla krokow z latex, `<code>` dla tekstowych; scena harnessu zrzutow zaktualizowana do nowego ksztaltu. | Backend: `analysis/energy_validation/{models,builder}.py`, `tests/analysis/test_energy_validation.py`. Frontend: `ui2/wyniki/jakosc/{api.ts,EkranJakosci.tsx}`, `__tests__/{fixtures,sekcje}`, `creator-harness-main.tsx`. FROZEN nietkniete (analiza, kontrakt addytywny — starsze odpowiedzi bez pola nadal poprawne). | Backend 45 zielone (struktura krokow, latex wzoru i podstawienia, kroki tekstowe latex=null, werdykt), black/ruff czyste; frontend jakosc 79 zielone (2x math-rendered z `\delta U` i `= 12.00`), type-check czysty; zrzuty flow-ekspert 10/10 — wzory KaTeX potwierdzone wizualnie na scenie walidacji. | wysoki (prezentacja wywodow na poziomie Proof Engine; wzorzec do reuse w kolejnych sladach) |
| V12K-111 | **ROZSTRZYGNIETY (2026-07-22, FLOW EKSPERT+ runda 3 ALL IN, karta R3-C — GAP K3-G2; wykonawca w worktree, weryfikacja i scalenie Fable).** Porownanie przebiegow A/B pokazywalo liczby obu stron bez drogi do dowodu — dowodRef prowadzil co najwyzej do sladu AKTYWNEGO przebiegu, wiec dla strony B bylby falszywy (regula K3: zero falszywych refow). | Recon wykonawcy PROVED warunek STOP niespelniony: slad jest pobieralny per przebieg (`fetchExtendedTrace(runId)` → `GET /analysis-runs/{id}/results/trace`, `ui/results-inspector/api.ts`), wiec dowod WLASCIWEGO przebiegu jest osiagalny bez rozbudowy backendu. | **Dowody kolumn A/B (addytywnie):** nowy `ui2/wyniki/porownanie/dowodPorownania.ts` (refy `A:<element>`/`B:<element>` + parser); komorki A i B niosa dowodRef, delta CELOWO bez (roznica to arytmetyka porownania, nie wynik solvera); klik → `setWynikiTab('dowod', run_a_id|run_b_id)`; `TrybZwarciowy` trzyma porownywana pare; WynikiWarsztat przechwytuje kontekst dla zakladki dowod; `DowodPrzebiegu` +prop `wskazanyRunId` (null = aktywny, 1:1). | 15 plikow frontend (porownanie/**, spaces/wyniki, shell store, dowod). Backend nietkniety. | Testy wykonawcy +26 (parser refow; klik komorki A → dowod z run_a_id; delta bez refu; kontekst czyszczony). Bramki w worktree: 53 pliki/577 zielone, type-check/eslint/guardy 0. Weryfikacja Fable po cherry-picku (razem z R3-D): type-check czysty, wyniki+shell 53 pliki/600 zielone, 6 guardow UI zielone. | wysoki (K3-G2 domkniety: kazda liczba porownania ma dowod wlasciwego przebiegu) |
| V12K-110 | **ROZSTRZYGNIETY (2026-07-22, FLOW EKSPERT+ runda 3 ALL IN, karta R3-A — GAP K1-G2; wykonawca w worktree, weryfikacja i scalenie Fable).** Tabela galezi rozplywu pokazywala prad bez werdyktu obciazalnosci — inzynier nie widzial na miejscu, czy galaz/transformator przekracza dopuszczalne obciazenie, mimo ze walidacja energetyczna juz to liczy. | Recon: `fetchWalidacjaEnergetyczna` zwraca pozycje BRANCH_LOADING/TRANSFORMER_LOADING kluczowane target_id z realnym procentem obciazenia i statusem progowym z backendu. | **Kolumna „Obciazenie [%]" w tabeli galezi (addytywnie, zero fizyki w UI):** `naMapeObciazen` + `komorkaObciazenia` konsumuja pozycje walidacji per target_id; procent i werdykt WYLACZNIE z backendu (status PASS/WARNING/FAIL); przekroczenie odblokowuje „Popraw w modelu" przez `rodzajPrzekroczeniaWalidacji`; uczciwe „—" gdy walidacja niedostepna; komorka obciazenia CELOWO bez dowodRef (regula K3 — wywod jest przy pozycji walidacji, nie w sladzie LF). | 7 plikow `ui2/wyniki/rozplyw/**`. Backend nietkniety. | Bramki wykonawcy + weryfikacja Fable po cherry-picku: rozplyw 488/488 zielone, type-check czysty, guardy zielone; po scaleniu calego pakietu rundy 3: wyniki+shell 600/600. | wysoki (K1-G2 domkniety: werdykt obciazalnosci przy liczbie, akcja naprawcza 1 klik) |
| V12K-108 | **ROZSTRZYGNIETY (2026-07-22, FLOW EKSPERT+ runda 2 karta R2-E - GAP zbiorczy z raportu R2-C; wykonawca w worktree, weryfikacja i scalenie Fable).** 30 testow w 17 plikach oze/**+kreatory/** emitowalo ostrzezenia act(...) (65 surowych linii) - montaz z asynchronicznym fetchem katalogow bez oczekiwania na stan koncowy. | Pomiar wykonawcy przed/po per plik (tabela w raporcie karty); dwie diagnozy poza mechanizmem karty, potwierdzone stackiem: (a) macierz - afterEach resetowal store przy WCIAZ zamontowanym komponencie (zustand setState poza act) -> naprawa porzadku sprzatania (cleanup() przed resetem); (b) zrodlo - kaskada 5 katalogow -> DRUGI bieg podgladu IEC 60909 z identyczna trescia -> kotwica na liczniku wywolan mocka. | **Naprawa TESTOW u zrodla (wzorce R2-C):** preferencja czekania na REALNY stan koncowy UI (opcje katalogow, komunikaty, chipy); jawny act(async) tylko bez sygnalu UI, ze skomentowanym DLACZEGO; zero wyciszania, zero zmian asercji. **Defekt uboczny naprawiony (Zero-Debt):** wyscig w studium - klik w disabled przycisk uruchomienia przed prefill katalogu (4 miejsca) -> waitFor(toBeEnabled()) przed klikiem (test klika AKTYWNY przycisk jak realny uzytkownik). Defektow komponentow brak (cleanupy poprawne). | 17 plikow testow (174+/37-), zero zmian produkcyjnych. | Pomiar 30->0 testow / 65->0 linii; oze+kreatory 69 plikow/701 zielone; pelna regresja ui2 w worktree 185/1945 zielona; type-check/guardy zielone. Weryfikacja Fable po cherry-picku: 701/701, grep act=0. | sredni (higiena testow domknieta zbiorczo; obserwacja: pre-existing jsdom 'Not implemented: navigation' w EkranWniosku - nie act, test zielony) |
| V12K-109 | **ROZSTRZYGNIETY (2026-07-22, FLOW EKSPERT+ runda 3 ALL IN, karta R3-B - GAP K3-G3; Fable osobiscie, opcja max).** Sekcja 'Wklady' ekranu zwarc byla pass-through bez dostawcy danych (TODO-KARTA 1 w zwarciaModel) - inzynier nie widzial rozbicia pradu zwarciowego na zrodla. | Recon: solver `compute_machine_contributions` (machine_sc_iec60909, IEC 60909-0:2016 par.6.6, WHITE BOX w wyniku) juz liczy rozbicie per maszyna (mu/q/i_b) dla pakietu SC3F; graf z `map_enm_to_network_graph`, id wezla = deterministyczny UUID z ref ENM (`_ref_to_uuid`). | **Dostawca end-to-end (backend pierwszy):** (1) endpoint `POST /api/proof/sc3f/contributions` (proof_pack.py): snapshot ENM + fault_node_id -> JSON `MachineShortCircuitResult.to_dict()` (wklady + white_box); tozsamosc punktu rozwiazywana serwerowo (przyjmuje id wezla grafu LUB ref ENM szyny = target_id wyniku SC); deterministycznie pusta lista bez maszyn. (2) Frontend `zwarcia/api.ts`: fetch + projekcja `naWklady` (JEDYNA operacja: skalowanie prezentacji A->kA) + hook `useWkladyZwarciowe` (cache per punkt, blad -> uczciwy stan 'dane niedostepne'); wpiecie w EkranZwarc addytywnie - props `wklady` (testy/nadpisanie) ma pierwszenstwo, bez propsa hook pobiera dla aktywnego punktu. ZERO fizyki w UI. | Backend: `api/proof_pack.py` (+SCContributionsRequest+endpoint), `tests/api/test_proof_pack_api.py` (+3). Frontend: `ui2/wyniki/zwarcia/{api.ts(nowy),EkranZwarc.tsx}` + `__tests__/ekranZwarc.test.tsx` (+3). FROZEN Result API nietkniete (nowy endpoint czyta snapshot, solver bez zmian). | Backend: 7/7 (rozbicie z WHITE BOX; determinizm odpowiedzi; pusta lista bez maszyn; ref ENM akceptowany), black/ruff czyste. Frontend: zwarcia 3 pliki/41 zielone (dostawca pokazuje zrodlo+prad z backendu, fault_node_id = target_id aktywnego punktu; blad->'dane niedostepne'; props override bez fetch), type-check czysty, guardy zielone. | wysoki (K3-G3 domkniety: wklady zwarciowe realne, ten sam solver co pakiet dowodowy) |
| V12K-107 | **ROZSTRZYGNIETY (2026-07-22, FLOW EKSPERT+ runda 2 karta R2-C — GAP K4-G1; wykonawca w worktree, weryfikacja i scalenie Fable).** Testy integracyjne powloki emitowaly ostrzezenia act(...) (pomiar: integracja 9, wynikiWarsztat 2) — szum maskujacy przyszle realne bledy. | Diagnoza wykonawcy per ostrzezenie: montaz AppRoot planuje realne asynchroniczne aktualizacje (rAF→setAplikacjaGotowa, fetch /api/health), KreatorStudium/MacierzNcRfg pobieraja katalogi przy montazu; NIE defekt komponentow (cleanupy sa; brak setState-po-unmount). | **Naprawa TESTOW u zrodla (bez wyciszania):** helper `renderujPowloke()` = render + `await findByTestId('app-ready')` (realny znacznik gotowosci, ta sama sciezka co e2e) w 9 testach integracji; wynikiWarsztat: `findByTestId('mvd-oze-blad-katalogu')` (realny stan koncowy) + 1 jawny `act(async)` domykajacy efekt bez sygnalu UI (skomentowany DLACZEGO). Zero zmian asercji. | `ui2/__tests__/integracja.test.tsx`, `ui2/spaces/wyniki/__tests__/wynikiWarsztat.test.tsx`. | Pomiar 9→0 i 2→0; celowane 44 zielone; pelna regresja ui2 w worktree 184/1935 zielona; po scaleniu z R2-B obszar 46 zielony, act=0. **GAP zbiorczy zarejestrowany:** ten sam wzorzec w 17 plikach oze/**+kreatory/** (30 ostrzezen) — osobna karta zbiorcza (wzorce przenosne). | sredni (higiena testow; wzorzec renderujPowloke do reuse) |
| V12K-106 | **ROZSTRZYGNIETY (2026-07-22, FLOW EKSPERT+ runda 2 karta R2-B — GAP K1-G1; wykonawca w worktree, weryfikacja i scalenie Fable).** Akcja kontekstowa `bilans-biernej` prowadzila do okna „Dobor kompensacji" bez wezla przekroczenia — deep-link niosl tylko id zakladki, inzynier wybieral wezel recznie. | Recon wykonawcy (dowody plik:linia): okno MA wybor wezla (`EkranKompensacji.tsx:371-386`, select `ref_id` ze snapshotu, konsumowany jako busRef zapytania compensation-sizing); ref akcji = `target_id` walidacji (REACTIVE_BALANCE → wezel); mechanizm = `wynikiTab` + konsument WynikiWarsztat. | **Deep-link z kontekstem elementu (addytywnie, zero fabrykacji):** (1) shell store `wynikiTabElement` + `setWynikiTab(tab, element?)` (stare wywolania 1:1); (2) `usePoprawWModelu` cel wyniki-zakladka niesie ref; (3) WynikiWarsztat konsumuje i czysci OBA pola, kontekst przechwytywany WYLACZNIE dla zakladki kompensacja (zero przeciekow miedzy zakladkami); (4) EkranKompensacji: pre-selekcja po zaladowaniu wezlow, walidacja refu przeciw liscie snapshotu (nieznany → zignorowany, skonsumowany), jednorazowosc (reczny wybor nigdy nie nadpisywany), zastosowanie uniewaznia poprzedni wynik jak reczna zmiana. Wynik liczbowy nadal wylacznie z backendu po jawnym biegu. | `ui2/shell/useShellStore.ts`, `ui2/wyniki/wzorzec/{usePoprawWModelu,akcjeNaprawcze}.ts`, `ui2/spaces/wyniki/WynikiWarsztat.tsx`, `ui2/oze/kompensacja/EkranKompensacji.tsx` + testy (12 plikow). FROZEN/backend nietkniete. | Testy wykonawcy: +9 (deepLink 3; pelny lancuch do wartosci selecta; izolacja kontekstu; znany ref + natywny klik Oblicz → API z busRef; nieznany ref; jednorazowosc; hub bez elementu 1:1). Weryfikacja Fable po cherry-picku: type-check czysty, obszar 654 zielone (drugi bieg — pierwszy byl artefaktem zlego cwd), guardy zielone. | wysoki (K1-G1 domkniety end-to-end: klik akcji → zakladka + wezel gotowy do biegu) |
| V12K-105 | **ROZSTRZYGNIETY (2026-07-22, program FLOW EKSPERT+ runda 2 karta R2-A — GAP K3-G1; Fable osobiscie).** Walidacja energetyczna pokazywala werdykty bez wywodu: `WalidacjaItem` nie niosl sladu WHITE BOX per pozycja, a `observed_value` liczy builder analizy — inzynier nie mogl zweryfikowac liczby 1 klikiem (dowodRef celowo nie nadany w K3: slad przebiegu LF nie jest wywodem tych liczb). | Recon: `analysis/energy_validation/{models,builder,serializer}.py` — 5 kontroli (BRANCH/TRANSFORMER_LOADING, VOLTAGE_DEVIATION, LOSS_BUDGET, REACTIVE_BALANCE), wzorzec sladu-na-miejscu: migotanie (`white_box` per wezel) i odbior (`slad_pl`). | **Slad WHITE BOX per pozycja (addytywnie):** backend — pole `white_box: tuple[str,...] = ()` w `EnergyValidationItem` (default pusty — konstruktory i stare odpowiedzi bez zmian), wywod w 5 kontrolach przez helper `_white_box_progowe` (wzor → dane z realnych wielkosci PF/katalogu → wynik → progi → werdykt; REACTIVE_BALANCE wlasny wywod tan/cos phi; NOT_COMPUTED bez wywodu — powod niesie why_pl), serializer emituje `white_box`. Frontend — `WalidacjaItem.white_box?` (opcjonalne, starsze odpowiedzi bez pola), `SzczegolWalidacji` + zwijany blok sladu (tryb ekspercki, tylko gdy wywod istnieje; reuse klas `mvd-jakosc-slad-*` z migotania). Ciagi deterministyczne (stale formaty), ASCII-PL jak why_pl. | Backend: `analysis/energy_validation/{models,builder,serializer}.py`, `tests/analysis/test_energy_validation.py` (+4). Frontend: `ui2/wyniki/jakosc/{api.ts,EkranJakosci.tsx}`, `__tests__/{fixtures,sekcje}.tsx` (+2). FROZEN Result API nietkniete (analiza, nie solver); kontrakt addytywny. | Testy: backend 45 zielone (w tym: pelny wywod 5 linii przy WARNING z realnymi danymi; NOT_COMPUTED pusty; kazda policzona pozycja ma slad i serializuje sie; determinizm), API quality + service 30 zielone, black/ruff czyste; frontend jakosc 79 zielone (ekspert: wybor pozycji → przycisk → wywod z werdyktem; basic: brak przycisku); guardy codenames/utf8/overlay/ui_phys/arch zielone. | sredni (R2-A — werdykt walidacji weryfikowalny na miejscu; regula K3 zachowana: wywod buildera renderowany przy pozycji, nie falszywy ref do sladu przebiegu) |
| V12K-104 | **ROZSTRZYGNIETY (2026-07-22, program FLOW EKSPERT+ karta K3 — brak C1 audytu; wykonawca-3 w worktree, weryfikacja i scalenie Fable).** Semantyka wzorca (2×klik na liczbie z `dowodRef` → dowod WHITE BOX) nie byla domknieta we wszystkich adapterach — czesc liczb wynikow nie prowadzila do dowodu 1 klikiem. | POMIAR wykonawcy (pelna tabela adapterow × kolumn w raporcie karty): odbiorca ref-a = `WynikiWarsztat.otworzDowod` → zakladka „Dowod obliczen" (slad AKTYWNEGO przebiegu). Stad REGULA DOMKNIECIA: `dowodRef` dostaje WYLACZNIE liczba pochodzaca wprost z wynikow przebiegu; wielkosci liczone builderami on-demand (Pst/Plt, energia Arc Flash, odchylki odbioru, estymaty WLS) maja slad WHITE BOX renderowany NA MIEJSCU — ref do sladu innego przebiegu bylby falszywym dowodem (zero fabrykacji). | **Domkniecie wg reguly:** (1) rozplyw — 10 komorek wynikowych szyn+galezi z ref (`bus_id`/`branch_id`), nadpisana decyzja E8.1 „bez dowodRef" (z komentarzem); (2) jakosc — Ik″ wiarygodnosci (fallback `element_id→target_id`), Sk″ migotania, I_bf Arc Flash (wielkosci z przebiegu SC); (3) odbior — kolumna „model" (wartosc z przebiegu LF, ref=`element_ref`) + USUNIETY MARTWY HANDLER `onOtworzDowod=()=>undefined` (nowy props, realny odbiorca warsztatu — Zero-Debt pkt 5); (4) decyzje jawne BEZ ref: estymacja (wyjscia POST WLS ze sladem na miejscu), porownanie A/B (`BEZ_DOWODU` — wartosci z dwoch przebiegow), walidacja energetyczna (builder bez sladu per pozycja). | `ui2/wyniki/rozplyw/adapters/rozplywAdapter.ts`, `ui2/wyniki/jakosc/jakoscModel.ts`, `ui2/wyniki/odbior/{odbiorModel,EkranOdbioru}.tsx`, `ui2/spaces/wyniki/WynikiWarsztat.tsx` + testy (10 plikow). FROZEN/backend/SLD nietkniete. | Testy wykonawcy: rozplyw +3 (w tym realna sciezka 2×klik→`onOtworzDowod('SZ-ST1')`), jakosc +6, odbior +2 (realna sciezka 2×klik→'BUS-1'). Weryfikacja Fable po cherry-picku na HEAD z K2: type-check czysty, regresja ui2/wyniki+spaces 937 zielone, 6 guardow OK. GAP-y K3-G1 (slad per pozycja walidacji = delta backendu), K3-G2 (dowody porownania A/B), K3-G3 (wklady zwarciowe bez dostawcy) zarejestrowane w programie §5. | sredni (C1 audytu — WHITE BOX 1 klik wszedzie, gdzie kontrakt niesie wywod; rejestr brakow audytu DOMKNIETY W CALOSCI) |
| V12K-103 | **ROZSTRZYGNIETY (2026-07-22, program FLOW EKSPERT+ karta K2 — Fable osobiscie, opcja max; domkniecie GAP-ow B1/B2 audytu zarejestrowanych w V12K-100 zamiast odkladania „na osobna karte" — dyrektywa #4).** Model nie mial pol warunkow przylaczenia OSD (moc przylaczeniowa/limit, wymagany cosφ, tryb pracy) — kafel E1 nie mogl porownac zainstalowanej generacji z limitem umownym, a wniosek OSD nie mial zrodla danych wejsciowych. | Recon: `ENMHeader` (`enm/models.py:126`, wzorzec addytywnych pol V12S-010), dispatch `execute_domain_operation` (`domain_operations.py`), rejestr `ALL_V2_HANDLERS`/`V2_CANONICAL_OPS` + kanon `domain/canonical_operations.py`; sciezka UI = `useSnapshotStore.executeDomainOperation` (ta sama co kreatory, np. lacznik). | **Warunki przylaczenia OSD end-to-end (lancuch: domena → operacja → API → kafel E1):** BACKEND (krok 1/2, commit d7f96937): model `ConnectionConditions` (moc>0, cosφ (0,1], tryb tekst z dokumentu OSD; wszystkie pola opcjonalne — addytywnie, istniejace payloady bez zmian) + `ENMHeader.connection_conditions`; operacja `set_connection_conditions` (scalanie czesciowe, null czysci pole, walidacja zakresow, deepcopy) zarejestrowana w V2 i kanonie (40 ops, kategoria UNIVERSAL). FRONTEND (krok 2/2): `types/enm.ts` (+typ), `pulpitAdapter.mapujPrzylaczenie` (+limit/cosφ/tryb + werdykt `przekroczonoLimit` = generacja znamionowa > limit — porownanie dwoch danych WEJSCIOWYCH, prezentacja nie fizyka), `KafelPrzylaczenia` (sekcja warunkow OSD z uczciwym „nie podano", werdykt tag w limicie/przekracza, formularz „Uzupelnij warunki OSD" wolajacy REALNA operacje przez `executeDomainOperation`; kafel statyczny — bez zagniezdzania button-w-button). | Backend: `enm/models.py`, `enm/domain_operations_v2.py`, `domain/canonical_operations.py`, `tests/enm/test_set_connection_conditions.py` (nowy). Frontend: `types/enm.ts`, `ui2/spaces/projekt/{pulpitAdapter,strings,KafelPrzylaczenia,PulpitProjektu,pulpit.css}` + testy. FROZEN Result API/SLD nietkniete; determinizm: blok addytywny, hash zmienia sie wylacznie gdy warunki ustawione. | Backend: 7 testow operacji + regresja tests/enm 745 zielone; black/ruff czyste; guardy canonical_ops(40)/pcc_zero/domain_no_guessing/readiness_codes/dialog_completeness zielone. Frontend: adapter +3 (warunki+werdykt w limicie/przekroczono; brak bloku→null), pulpit +4→84 zielone w spaces/projekt (w tym REALNA sciezka: klik Uzupelnij→pola→Zapisz→`executeDomainOperation('set_connection_conditions', payload)`; walidacja PL cosφ bez wolania); type-check czysty; 6 guardow UI zielonych. | wysoki (K2 — E1 domkniete od danych OSD do werdyktu bilansu; GAP-y B1/B2 zamkniete end-to-end) |
| V12K-102 | **ROZSTRZYGNIETY (2026-07-22, program FLOW EKSPERT+ karta K4 — brak D1 audytu; wykonawca-2 w worktree, weryfikacja i scalenie Fable).** Inzynier nie widzial „wyniki nieaktualne" w miejscu, gdzie zawsze patrzy — pasek aktywnego przypadku (ui2 CaseBar) mial STATYCZNY chip statusu zasilany z `useAppStateStore.activeCaseResultStatus` zamiast ze store przypadkow. | Recon wykonawcy: `ui2/shell/CaseBar.tsx` (pasek „Zakres obliczen", renderowany zawsze przez AppShell:234); jedyny konsument starego pola = CaseBar + test; id przestrzeni obliczen `'obliczenia'` (`shell/spaces.ts:20,36`). FreshnessBadge ODRZUCONY swiadomie (wymaga pary rewizji, ktorej store nie niesie — numer bylby fabrykacja). | **Znacznik swiezosci w pasku przypadku (zrodlo prawdy = przypadki):** (1) czysty helper `shell/znacznikSwiezosci.ts` (StudyCase|null → model znacznika; niespojna para FRESH+results_valid=false → nieaktualne); (2) `shellStatus.ts` czyta `useStudyCasesStore.activeCase` (stare pole i osierocony typ ResultFreshness usuniete — Zero-Debt); (3) chip „nieaktualne" = BUTTON → `selectSpace('obliczenia')`; „aktualne"/„brak" = statyczny SPAN; (4) stylistyka chipow paska (tokeny, wariant `.mvd-chip-klik`). Etykiety spojne ze STATUS_WYNIKOW_LABEL. | `ui2/shell/{znacznikSwiezosci.ts(nowy),shellStatus.ts,CaseBar.tsx,AppShell.tsx,strings.ts,shell.css}` + 3 pliki testow. FROZEN/backend/SLD nietkniete. | Testy wykonawcy: 15 (helper 6 fixture'ami; CaseBar 6 natywnym klikiem; integracja AppShell realne store'y 3: OUTDATED→obliczenia, FRESH/brak bez akcji). Weryfikacja Fable po scaleniu z K1: type-check czysty, pelna regresja ui2 184 pliki/1918 zielone, 6 guardow OK. GAP zarejestrowany: act(...) w integracja.test.tsx pre-existing (9=9). | sredni (D1 audytu; swiezosc widoczna globalnie w powloce) |
| V12K-101 | **ROZSTRZYGNIETY (2026-07-22, program FLOW EKSPERT+ karta K1 — brak A3 audytu; wykonawca-1 w worktree, weryfikacja i scalenie Fable).** Petla decyzji „Popraw w modelu" (F-E6.1/6.2) miala JEDNA akcje (selekcja+zoom SLD+„Schemat") niezaleznie od rodzaju przekroczenia — inzynier-ekspert oczekuje akcji wlasciwej dla werdyktu. | Recon wykonawcy (dowody plik:linia): powierzchnie osiagalne programowo = selekcja+Schemat (selection store), deep-link zakladki Wynikow (`setWynikiTab`, wzorzec HubDokumentacji:183), `openRouteSurface` (most legacy, uzywany z ui2 E-27→E-28). Dobor odcinka/kabla/transformatora = property-grid po selekcji (BRAK programowego wejscia w konfigurator z ui2 — grep operationSurfaceRegistry/fixActionSurfaceExecutor: wylacznie infrastruktura legacy sterowana fix-akcjami backendu). Zaden konsument petli nie niesie werdyktu miskoordynacji. | **Rejestr akcji naprawczych (addytywny, zero phantom):** (1) `wzorzec/akcjeNaprawcze.ts` — typ `RodzajPrzekroczenia` (napiecie/obciazalnosc-galezi/obciazalnosc-transformatora/migotanie/bilans-biernej) + `akcjaNaprawcza(rodzaj)` (etykieta+opis+kroki nawigacji); (2) `usePoprawWModelu` +opcjonalny 4. parametr `rodzaj` (brak = zachowanie 1:1); (3) KONTEKSTOWA akcja tylko tam, gdzie realna powierzchnia istnieje: `bilans-biernej` → okno „Dobor kompensacji" (`setWynikiTab('kompensacja')`+`setActiveSpace('wyniki')`, selekcja zachowana, bez zoomu SLD; etykieta „Popraw w modelu — dobor kompensacji"); pozostale rodzaje GENERYCZNE z uzasadnieniem (property-grid po selekcji JUZ jest miejscem decyzji); miskoordynacja NIE zmapowana (brak werdyktu u konsumentow = bylby phantom, udokumentowane w naglowku modulu); (4) `TabelaWynikow`/`EkranAnalizy` +prop `rodzajWiersza` (etykieta kontekstowa per wiersz); (5) wpiecia: rozplyw, jakosc walidacja (rodzaj z check_type), migotanie, odbior (U), rejestr „Co wymaga uwagi" (pole `rodzaj` w `Przekroczenie`). | `ui2/wyniki/wzorzec/{akcjeNaprawcze.ts(nowy),usePoprawWModelu,wzorzecModel,TabelaWynikow,EkranAnalizy,index}`, `rozplyw/{TabelaSzyn,TabelaGalezi}`, `jakosc/{jakoscModel,EkranJakosci}`, `odbior/EkranOdbioru`, `co-wymaga-uwagi/{model,EkranCoWymagaUwagi,strings}` (19 plikow). FROZEN/backend/SLD nietkniete. | Testy wykonawcy: registry 6, hook +2 (1:1 bez rodzaju; bilans-biernej→wyniki/kompensacja), realna sciezka +1 (REACTIVE_BALANCE FAIL → natywny klik → etykieta kontekstowa + activeSpace='wyniki' + wynikiTab='kompensacja'), mapowania +3. Weryfikacja Fable po scaleniu z K4: type-check czysty, pelna regresja ui2 184/1918 zielone, 6 guardow OK. GAP-y: deep-link kompensacji bez pre-selekcji wezla; galezie rozplywu bez werdyktu obciazalnosci (kontrakt nie niesie) — zarejestrowane w programie §5. | wysoki (A3 audytu; petla decyzji na poziomie eksperta — kontekst bez fabrykacji) |
| V12K-100 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „B1 i b2" — kolejka audytu `AUDYT_FLOW_INZYNIER_PROJEKTANT_2026-07` §5, braki B1/B2).** E1 (pulpit projektu) zaczynal sie „w srodku" — warunki przylaczenia OSD (Sk″/U) i bilans mocy przylaczeniowej byly kaflami „wkrotce" (`KafelWkrotce`), mimo ze dane sa w modelu. Projektant nie widzial punktu wyjscia FLOW (skad zasilanie, ile generacji vs obciazenia). | Recon modelu (`types/enm.ts`): `Source.sk3_mva/ik3_ka` (warunki OSD od strony sieci), `Bus.voltage_kv` (napiecie przylaczenia po `bus_ref`), `Generator.p_mw`/`Load.p_mw` (moc znamionowa). Recon backendu: `_existing_generation_mw` (hosting_capacity) sumuje SUROWE `p_mw` (bez krotnosci — `p_mw` juz zawiera quantity, `domain_operations_v2.py:2200`). **GAP potwierdzony:** brak pola „moc przylaczeniowa"(limit OSD) i cosφ/tryb pracy w modelu. | **Kafel „Warunki przylaczenia i bilans mocy" (E1, zastepuje wkrotce-bilans, zero phantom):** (1) `pulpitAdapter.mapujPrzylaczenie(snapshot)` — czysta projekcja read-only: `zrodlaSieciowe` (nazwa/U-z-szyny/Sk″/Ik″), `generacjaMw`/`odbioryMw`/`nettoMw` (Σ `p_mw`, rollup ZNAMIONOWY zgodny z backendem, NIE fizyka — analogicznie `naSumeStratGalezi`), + hook `usePrzylaczenieKafel`; (2) `KafelPrzylaczenia.tsx` — U/Sk″/Ik″ glownego zrodla + bilans gen/odb/netto, uczciwy stan zerowy „brak zrodla sieciowego" + instrukcja; klik→„Model sieci"; (3) wpiecie w `PulpitProjektu` (usuniety wkrotce-bilans, `celTytul` zostaje). **GAP-y backendu zarejestrowane (audyt §6, nie fabrykowane):** cosφ wymagany/tryb pracy (B1) + limit mocy OSD (B2) — brak pola w modelu → nie pokazywane; rozbudowa modelu = osobna karta. ZERO fizyki (guard `ui_no_physics` zielony — sumy znamionowe nie sa liczeniem sieci), ZERO mutacji. | `ui2/spaces/projekt/{pulpitAdapter.ts,strings.ts,KafelPrzylaczenia.tsx(nowy),PulpitProjektu.tsx,pulpit.css}`. FROZEN/backend/SLD nietkniete (czyta istniejacy snapshot store). | Testy: `pulpitAdapter` +6 (zrodlo U/Sk/Ik; bilans Σp_mw+netto; brak zrodla→[]; brak Sk→null; szyna nieznana→U null; determinizm), `pulpitProjektu` +4 (1 wkrotce nie 2; realny snapshot Sk/gen/netto; stan zerowy; klik→„model"). Type-check czysty; regresja `spaces/projekt` 78 zielona; guardy zielone. | wysoki (B1/B2 audytu — E1 od punktu przylaczenia; realne pola modelu; GAP-y backendu zarejestrowane, nie fabrykowane) |
| V12K-099 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „A2" — kolejka audytu `AUDYT_FLOW_INZYNIER_PROJEKTANT_2026-07` §5, brak A2).** Petla decyzji „Popraw w modelu" (F-E6.1/V12K-097) byla wpieta TYLKO w rozplyw — Jakosc i Zgodnosc powykonawcza pokazywaly przekroczenia bez prowadzenia z werdyktu do modelu. Rozszerzenie na pozostale ekrany wzorca produkujace realne `ostrzezenie`. | Recon zrodel `ostrzezenie` w ekranach wzorca (`EkranAnalizy`/`TabelaWynikow`): jakosc walidacja (`jakoscModel.ts:mapujWierszWalidacji`, status WARNING/FAIL), jakosc migotanie (pst/plt>limit), zgodnosc powykonawcza (`odbior:mapujWierszZgodnosci`, werdykt „poza tolerancją"). Zwarcia (`zwarciaModel.ts`) **NIE ustawiaja ostrzezenia** (kontrakt SC bez werdyktu przekroczenia) → wpiecie byloby phantomem. Typ elementu gruntowany backendem `analysis/energy_validation/builder.py`: VOLTAGE_DEVIATION/REACTIVE_BALANCE→node(Bus), BRANCH_LOADING→branch(LineBranch), TRANSFORMER_LOADING→branch(TransformerBranch), **LOSS_BUDGET→target_id="network" (agregat systemowy, NIE element)**. | **Wpiecie petli w ekrany z realnym werdyktem (addytywne, zero phantom):** (1) wzorzec `TabelaWynikow`/`EkranAnalizy`/`wzorzecModel` +opcjonalny predykat `wierszDecyzyjny(klucz)` — przycisk „Popraw" pokazuje sie na wierszu z ostrzezeniem TYLKO gdy predykat go dopuszcza (domyslnie: kazdy) — chroni przed martwym przyciskiem na agregacie systemowym; (2) `jakoscModel.typElementuWalidacji(check_type)` — czyste mapowanie na `ElementType|null` (LOSS_BUDGET→null); (3) `EkranJakosci`: walidacja (map compositeKey→item, onPoprawWModelu resolvuje target_id+typ, wierszDecyzyjny wyklucza network) + migotanie (bus_ref→Bus); (4) `EkranOdbioru`: pomiar U→Bus, pomiar P/Q wykluczony predykatem (typ niejednoznaczny w kontrakcie — zero zgadywania). Reuse `usePoprawWModelu`. ZERO fizyki, ZERO mutacji. | `ui2/wyniki/wzorzec/{wzorzecModel,TabelaWynikow,EkranAnalizy}.tsx`, `ui2/wyniki/jakosc/{jakoscModel.ts,EkranJakosci.tsx}`, `ui2/wyniki/odbior/EkranOdbioru.tsx`. FROZEN/backend/SLD nietkniete. | Testy: `jakoscModel` +3 (typElementuWalidacji: Bus/branch/transformer/null), `tabelaWynikow` +2 (wierszDecyzyjny=false ukrywa; selektywny), `sekcje` +3 (2 przyciski element-level; napiecie→Bus+Schemat; LOSS_BUDGET FAIL bez przycisku), `migotanie` +1 (bus→Bus+Schemat), `EkranOdbioru` +2 (P bez przycisku; U→Bus+Schemat). Type-check czysty; regresja ui2/wyniki 441 zielona; guardy zielone. | wysoki (A2 audytu — petla decyzji obejmuje Jakosc+Zgodnosc; zwarcia uczciwie pominiete jako bez werdyktu; predykat chroni przed martwa akcja) |
| V12K-098 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „przeprowadz audyt zespolu ekspertow czego potrzebuje w interfejsie inzynier analiz i projektant w calym flow, uzupelnij" — brak A1 z audytu `AUDYT_FLOW_INZYNIER_PROJEKTANT_2026-07.md`).** Brak KRYTYCZNY A1: przekroczenia rozproszone per zakladka (rozplyw osobno, jakosc osobno...) — inzynier analiz nie widzi WSZYSTKICH problemow sieci w jednym miejscu z akcja naprawcza. Audyt = deliverable poprzedniej mysli; ta tura UZUPELNIA A1 end-to-end (dyrektywa #4 — nie „na potem"), inaczej §4 audytu bylby fabrykacja (dyrektywa #3). | Recon: zrodla werdyktu `WartoscKomorki.ostrzezenie` per adapter — `rozplyw/adapters/rozplywAdapter.ts:94` (napiecePozaZakresem, szyna Bus, store SYNCHRONICZNY `usePowerFlowResultsStore.results`); `jakosc/jakoscModel.ts:208` + `odbior/odbiorModel.ts:261` = werdykty liczone, ale dane pobierane ASYNCHRONICZNIE per ekran (`fetch...` on-demand, brak w store); zwarcia (`zwarciaModel.ts`) NIE ustawiaja ostrzezenia. Reuse `usePoprawWModelu` (F-E6.1, V12K-097). | **Skonsolidowany rejestr „Co wymaga uwagi" (nowa zakladka Wyniki, pierwsza w grupie „Analizy sieci"):** (1) `co-wymaga-uwagi/model.ts` — znormalizowany typ `Przekroczenie` (klucz/analizaPL/elementRef/elementTyp/opis/wartosc) + czysty kolektor `przekroczeniaRozplywu(wynik)` (szyny z napieciem poza przedzialem, ten sam werdykt `napiecePozaZakresem` co adapter — spojnosc ekran↔rejestr) + hook `useRejestrPrzekroczen` (read-only ze store); (2) `EkranCoWymagaUwagi.tsx` — cel jednym zdaniem, lista pozycji z akcja „Popraw w modelu" (reuse `usePoprawWModelu` → selekcja+zoom SLD+„Schemat"), UCZCIWE stany zerowe rozroznione (brak przebiegu ≠ „siec w normie", FLOW §0); (3) wpiecie w `WynikiWarsztat` (ZAKLADKI + GRUPY + render). **Zakres zrodel uczciwy (dyrektywa #3):** konsoliduje analizy trzymajace wynik w SYNCHRONICZNYM store — biezaco rozplyw (napiecia szyn); analizy async per ekran NIE zgadywane — architektura rozszerzalna (kolejny kolektor + konkatenacja) gdy ich wynik trafi do store (A2). ZERO fizyki, ZERO mutacji. | `ui2/wyniki/co-wymaga-uwagi/**` (model/strings/EkranCoWymagaUwagi/coWymagaUwagi.css/index/testy), `ui2/spaces/wyniki/{WynikiWarsztat.tsx,strings.ts}`. FROZEN/backend/SLD nietkniete (reuse publicznego API store'u wynikow + selekcji). Audyt §4 skorygowany do rzeczywistego zakresu (rozplyw, nie „WSZYSTKICH" — uczciwosc). | Testy: `model.test.ts` (6: null→[], w przedziale→[], nisko/wysoko→opis+Bus, filtr+kolejnosc, determinizm) + `EkranCoWymagaUwagi.test.tsx` (4: brak przebiegu vs siec w normie rozroznione, lista+podsumowanie, „Popraw"→selekcja+centrowanie+„Schemat"). Type-check czysty; regresja `co-wymaga-uwagi` (10) + `spaces/wyniki` (34) zielona. | krytyczny (A1 audytu — skonsolidowany rejestr przekroczen z akcja naprawcza; rozszerzalny na kolejne analizy) |
| V12K-097 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Dalej" — F-E6.1, pierwszy plaster petli decyzji wynik→model, priorytet FLOW §2 #3).** Luka FLOW E6: „petla wynik→decyzja→model bez prowadzenia (reczna nawigacja)". Swiezosc/uniewaznienie wynikow (`ui2/freshness/`) i nastawy zabezpieczen (`EkranZabezpieczenAutomatyki`, E-27) JUZ istnialy — realna luka to prowadzenie OD wyniku z przekroczeniem DO miejsca decyzji. | Recon: wspolny wzorzec `EkranAnalizy`/`TabelaWynikow` (`WartoscKomorki.ostrzezenie` = przekroczony prog, `kluczWiersza` = ref elementu); `useSelectionStore.selectElement`/`centerSldOnElement` + `useShellStore.setActiveSpace` (reuse V12K-073) | **„Od wyniku do decyzji" w WSPOLNYM wzorcu (addytywne, high-leverage — lapie wszystkie ekrany wynikow):** (1) hook `usePoprawWModelu(ref, typ, nazwa)` — zaznacza element (property-grid) + centruje SLD + przechodzi do „Schemat"; (2) `TabelaWynikow` +prop `onPoprawWModelu`: kolumna „Decyzja" + przycisk „Popraw w modelu" WYLACZNIE na wierszach z ostrzezeniem (colSpan spacerow wirtualizacji poprawiony); (3) `EkranAnalizy` przekazuje prop; (4) wpiete w rozplyw: `TabelaSzyn` (typ Bus), `TabelaGalezi` (typ LineBranch) — flagowa petla DU/przeciazenie. Brak propa = zero zmiany dla nie-konsumentow. ZERO fizyki (selekcja+nawigacja), ZERO mutacji modelu. | `ui2/wyniki/wzorzec/{usePoprawWModelu.ts(nowy),TabelaWynikow,EkranAnalizy,wzorzecModel,strings,wzorzec.css,index}`, `ui2/wyniki/rozplyw/{TabelaSzyn,TabelaGalezi}.tsx`. FROZEN/backend/SLD nietkniete (reuse publicznego API selekcji). | Testy: `tabelaWynikow` +4 (bez propa brak kolumny; z propem przycisk tylko na wierszu z ostrzezeniem; klik→callback z ref; stopPropagation vs wybor wiersza) + `usePoprawWModelu` +2 (selekcja+centrowanie+przejscie do schematu; typ galezi). Type-check/lint/guardy zielone. | wysoki (petla decyzji wynik→model wlaczona globalnie przez wzorzec; uniewaznienie/nastawy juz byly) |
| V12K-096 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Lec dalej / Kontynuuj" po powtorzonej recenzji — F-E8.2).** Powtorzona recenzja huba (identyczna z V12K-095) JUZ spelniona przez lean redesign; „Kontynuuj" = domkniecie E8 tym, co recenzja stawia najwyzej („skoncentruj interfejs na dokumentach"). Domkniecie DOKUMENTOW: realny skrot do istniejacego generatora STUDIUM PRZYLACZENIOWEGO OZE z huba. | Recon: `oze_analysis_runs.py` (DokumentStudiumRequest, `render_dokument_studium_pdf/docx` — REALNY backend); `WynikiWarsztat` zakladka „studium" (`useState` lokalny, niesterowlny z zewnatrz); zestawienie materiałowe/BOM — **grep pusty, brak backendu** | **Deep-link miedzy-przestrzenny (bounded, addytywny, zero-phantom):** (1) shell store `wynikiTab: string|null` + `setWynikiTab` (jednorazowe zadanie, nie persystowane w intencji); (2) `WynikiWarsztat` konsumuje przez `useEffect` (walidacja id → `setZakladka`, czyszczenie do null); (3) `CelDokumentu` +wariant `wyniki-zakladka`; (4) hub: nowa grupa „Dokumenty przylaczeniowe (OZE)" z JEDNA lean karta „Studium przylaczeniowe OZE" (akcja „Otworz kreator" → Wyniki/studium, formaty PDF/DOCX). Karta w sekcji DOKUMENTY (bez nowej sekcji — lean zachowany). **Zestawienie materiałowe NIE dodane** (brak backendu → phantom; F-E8.3 backend). | `ui2/shell/useShellStore.ts` (wynikiTab), `ui2/spaces/wyniki/WynikiWarsztat.tsx` (konsumpcja), `ui2/spaces/dokumentacja/{model,HubDokumentacji}.tsx` (karta+handler). FROZEN/backend/SLD nietkniete. | Testy: `HubDokumentacji` +1 (studium → wynikiTab='studium'+space='wyniki') = 14; `wynikiWarsztat` +2 (deep-link otwiera zakladke studium + czysci; nieznane id ignorowane) = 18. Type-check/lint/guardy zielone; zrzuty zywej aplikacji potwierdzaja karte studium bez re-bloatu (nadal 3 sekcje). | wysoki (domkniecie dokumentow realnym generatorem studium; zero fabrykacji; BOM odlozony jako backend) |
| V12K-095 | **ROZSTRZYGNIETY (2026-07-21, DRUGA recenzja inzyniera do „Hub Dokumentacji" — 8 uwag: ekran przeladowany, potrzebna HIERARCHIA nie wiecej danych, ~40% krocej; dyrektywa #8).** Swiadome ODWROCENIE V12K-094: poprzednia runda (na prosbe „wiecej informacji inzynierskich") dodala panel statystyk modelu — teraz recenzja ocenia to jako szum o niskiej wartosci decyzyjnej. Nowa zasada: „minimum informacji – maksimum decyzji", ekran podporzadkowany 3 pytaniom (czy obliczenia OK · jakie dokumenty · co dalej). | Druga recenzja wlasciciela (8 pkt); realne selektory runStore/snapshot | **Uproszczenie ~40% (lean):** (1) USUNIETO panel „Analizowany model" (statystyki galezi/odbiorow/zrodel — pkt 1) i rozwlekly 4-krokowy „Tor pracy"; (2) 3 sekcje zamiast 5, eyebrow = 3 pytania: Q1 „OBLICZENIA" (zwiezly pasek statusu: zakonczone + rodzaj/data/wersja albo „brak → Przejdz do obliczen"), Q2 „DOKUMENTY DO WYGENEROWANIA" (sekcja glowna, pkt 2), Q3 „CO DALEJ" (pasek procesu); (3) karty skrocone do 1 zdania (pkt 3/7); (4) ZAWARTOSC jako glowne chipy, formaty drobnym drukiem obok akcji (pkt 4 — zawartosc > formaty); (5/8) usuniete: zrodlo danych, formaty-jako-chipy, panel modelu, tor pracy — mniej tekstu, mniej sekcji. Backend `podsumowanieModelu`/`wykonaneObliczenia` USUNIETE (martwy kod po usunieciu panelu — Zero-Debt); `zawartoscZPrzebiegow` wzbogacona o „Obciazalnosc linii" (realny wynik LF). **ZERO fabrykacji zachowane.** | `ui2/spaces/dokumentacja/{model,strings,HubDokumentacji,dokumentacja.css}` (przepisane lean), `__tests__/HubDokumentacji.test.tsx` (13 testow lean). **Reconciliacja srodowiska:** kontener zresetowany do cc79caac (sprzed V12K-094); `git reset --hard origin` przywrocil round-2 (d80b6d9c) jako baze, na niej nalozono round-3. FROZEN/backend/SLD nietkniete. | Testy: `HubDokumentacji.test.tsx` 13 (cel; Q1 status brak/ok+akcja; Q2 karty ikona/akcent/WHITE BOX, statusy, zawartosc realna, akcje→E-37/E-36/Projekt, formaty drugorzedne; Q3 pasek procesu; selektory) + `MostDokumentacji` 2. Type-check/lint/guardy zielone; zrzuty zywej aplikacji (oba motywy) potwierdzaja lean ~40% krotszy. | wysoki (druga recenzja: hierarchia > ilosc danych; swiadome odwrocenie V12K-094) |
| V12K-094 | **ROZSTRZYGNIETY (2026-07-21, recenzja inzyniera elektroenergetyka do ekranu „Hub Dokumentacji" — 9 uwag; dyrektywa #8: znaleziska z ogledzin = karty poprawek).** Runda poprawek F-E8.1-R1: panel modelu, tozsamosc wizualna dokumentow, jednoznaczne akcje/statusy, zawartosc dokumentu, wyroznienie WHITE BOX, pasek procesu. | Recenzja wlasciciela (9 pkt); realny ksztalt ENM (`types/enm.ts`: buses/branches/transformers/sources/generators/loads); `ANALYSIS_TYPE_LABELS`; realne eksporty E-37/E-36 | **Wdrozone z REALNYCH danych (zero fabrykacji):** (5) panel „Analizowany model" — liczniki ze snapshotu (poziomy napiec distinct, wezly, galezie, transformatory, zrodla, generacja, odbiory) + wykonane obliczenia (distinct etykiety zakonczonych przebiegow) + data ostatniej analizy; (8) zawartosc dokumentu wyprowadzona z REALNIE zakonczonych przebiegow (LOAD_FLOW→rozplyw/spadki/bilans, SC→IEC 60909); (2/7) tozsamosc per typ (ikona SVG + akcent: raport=accent, dowod=formalny/gold z ramka i „DOWOD FORMALNY", archiwum=neutralny); (3) akcje nazywajace skutek (Otworz generator/dowod/archiwum); (4) statusy „Do wygenerowania" / „Wymaga: zakonczony przebieg|otwarty projekt"; (6-czesc realna) formaty (PDF/DOCX/JSON, PDF/LaTeX/JSON, ZIP); (9) pasek procesu Projekt→Obliczenia→Dokumentacja(aktywny)→Eksport→Wniosek OSD; (1) lepsze wykorzystanie przestrzeni. **Naprawa defektu kaskady CSS** (Zero-Debt): klasa `.mvd-dok-sekcja` kolidowala miedzy kontenerem sekcji (radius 10px) a pigulka zawartosci (radius 999px) — pozniejsza regula nadpisywala radius sekcji na 999px → wielkie luki w tle; pigulka przemianowana na `.mvd-dok-tag`. | `ui2/spaces/dokumentacja/model.ts` (+podsumowanieModelu/wykonaneObliczenia/zawartoscZPrzebiegow/pola karty), `HubDokumentacji.tsx` (przebudowa), `dokumentacja.css` (+panel/proces/tozsamosc/naprawa kolizji), `strings.ts`; harness `creator-harness-main.tsx` (+dokumentacja, realistyczna siec). **Cykl zycia dokumentu (status/strony/data/Podglad/Pobierz) = karta F-E8.3 BACKEND — NIE fabrykowac.** | Testy: `HubDokumentacji.test.tsx` 20 (panel modelu realne liczniki + stan zerowy, zawartosc z przebiegow, statusy, akcje→E-37/E-36/Projekt, wyroznienie WHITE BOX, pasek procesu, selektory) + `MostDokumentacji` 2. Type-check/lint czyste; guardy no_codenames/forbidden_ui_terms/ui_terminology/dead_click/utf8 zielone; zrzuty zywej aplikacji (oba motywy) potwierdzaja brak artefaktu. FLOW: F-E8.3/F-E8.2 zarejestrowane. | wysoki (recenzja inzyniera wdrozona end-to-end, zero fabrykacji, defekt CSS u zrodla) |
| V12K-093 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela: „Przejmij dowodzenie i zarzadzaj z planem flow inzyniera" — Fable przejmuje orkiestracje programu wg FLOW_PROJEKTANTA §2).** Priorytet FLOW §2 #2 (F-E8, domkniecie lancucha dokumentem odbiorowym): przestrzen „Dokumentacja" NIE miala powierzchni ui2 — `mostTrasyPrzestrzeni('dokumentacja')` routowal na legacy `navigateToReport` (generator raportu mostu), bez ekranu prowadzacego od flow (wbrew kontraktowi FLOW §0.3). | Rekonesans: `ui2/AppRoot.tsx` (brak slotu dokumentacja, navigateToReport), `shell/spaces.ts` (SpaceId 'dokumentacja' istnieje), realne eksporty backendu (`analysis_runs.py` `/export/report/*` + `/export/proof/*`, `power_flow_runs.py`, `project_archive.py`), wzorzec ekranu prowadzacego `EkranAnalizTechnicznych` + most `MostAnalizTechnicznych` | **Hub Dokumentacji od flow (Fable osobiscie, zadanie jakosciowe/max):** nowa przestrzen `ui2/spaces/dokumentacja/` — `HubDokumentacji` (kontrakt ekranu prowadzacego: cel jednym zdaniem · Tor pracy Projekt→Wariant→Wersja→Obliczenie z akcjami naprawczymi · karty dokumentow w grupach z uczciwym warunkiem dostepnosci · nastepny krok = wniosek OSD) + `MostDokumentacji` (hub ↔ powierzchnia dokumentu E-37/E-36 z paskiem powrotu, wzorzec MostAnalizTechnicznych). **ZERO phantomow**: kazda karta mapuje na REALNEGO dostawce — raport→E-37 (`/analysis-runs/{id}/export/report/*`), pakiet dowodowy→E-36 (`/export/proof/*`), archiwum→przestrzen „Projekt" (`/{id}/export` ZIP). ZERO fizyki w UI (interpretacja stanu store'ow + nawigacja). Wpiecie: `LegacyWarsztat` slot `dokumentacja` + wykluczenie z `mainSurfaceExpanded` (jak „wyniki"); `AppRoot` dostawca + zdjecie auto-nawigacji do legacy. | `ui2/spaces/dokumentacja/**` (model/strings/css/Hub/Most/index/testy), `ui2/legacy/LegacyWarsztat.tsx` (slot), `ui2/AppRoot.tsx` (dostawca + most trasy). Backend/SLD/Result API nietkniete (konsumpcja istniejacych eksportow). | Testy: `HubDokumentacji.test.tsx` (14: cel, tor pracy stany zerowe/realne, akcje naprawcze, karty opis/zrodlo, chipy wymagan przebieg/projekt, „Otworz"→E-37/E-36/przestrzen Projekt realne, nastepny krok, selektory) + `MostDokumentacji.test.tsx` (2: hub gdy brak/obca powierzchnia). Type-check/lint czyste; guardy no_codenames/forbidden_ui_terms/ui_terminology/dead_click/utf8 zielone; integracja AppRoot bez regresji. Kolejka FLOW zaktualizowana (F-E8.2/F-E6/F-E1 zarejestrowane, nie ciche). | wysoki (Fable przejal dowodzenie; domkniecie E8 od flow, zero phantomow) |
| V12K-092 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela: „F4" — kontynuacja fazy F4 SLD rework, badge wynikowy OLTC odlozony w V12K-091).** Domkniecie lancucha OLTC „do ostatniego klika": badge WYNIKOWY (pozycja koncowa zaczepu + liczba przelaczen PO obliczeniu) na glifie transformatora w PRODUKCYJNYM SLD v3. Tabliczka (V12K-091) pokazuje NASTAWE z modelu; badge pokazuje WYNIK load-flow. Rozwiazanie rzekomego mismatchu bayRef↔branch_id z V12K-090: symbol `transformer2W` sceny v3 ma `meta.ownerRef = transformerRef` (NIE bayRef — tamten mismatch dotyczyl aparatow POLA TR, nie samego symbolu transformatora), a `transformerRef = ENM ref_id = element_ref` galezi transformatora w resultset_v1 — tozsamosc ZGODNA. | Recon: `overlay.ts::buildFlowOverlayFromScene` (wzorzec czystego budowniczego per LOD), `scene/buildScene.ts::gpzSymbolToPreview` (ownerRef = `sym.meta.transformerRef` dla transformatora), `canonical_analysis.py::_build_snapshot_graph_element_context` (branch_context[graph_id]["element_id"]=trafo.ref_id), `build_branch_results` (element_id or branch_id), V12K-089 (metryki TAP_POSITION/TAP_SWITCH_COUNT w resultset_v1) | **Badge wynikowy OLTC v3 (READ-ONLY, ZERO fizyki, addytywny/parytetowy):** (1) `overlay.ts`: `TransformerOltcOverlay` + `buildOltcOverlayFromScene(scene, payload)` (allowlista LOAD_FLOW, klucz `ownerRef` symbolu `transformer`, metryki `TAP_POSITION`/`TAP_SWITCH_COUNT`, brak → brak badge nie 0) + wyrocznia `oltcOverlayTracesToPayload` (dowod bajt-rownosci z payloadem); (2) `SldCanvasV3Workspace.tsx`: `buildOltcOverlayForSnapshot` (scalanie 3 LOD, `useRawResultOverlayStore` — ten sam kanal co flow overlay) + wpiecie `oltcByOwnerRef` w overlay; (3) `SldCanvasV3.tsx`: `computeOltcBadgePlacements` + `SceneOltcBadgeNode` (warstwa `sld-v3-oltc-overlay`, badge bursztynowy NA PRAWO od symbolu, filtr `resultOverlays` layer). Dane: solver `oltc_control` → resultset_v1 (V12K-089) → raw store → v3 canvas. PowerFlowResult FROZEN nietkniety (interpretacja gotowego wyniku). | `ui/sld/v3/canvas/overlay.ts` (+budowniczy+wyrocznia+typ), `ui/sld/v3/canvas/SldCanvasV3Workspace.tsx` (+hook+wpiecie), `ui/sld/v3/canvas/SldCanvasV3.tsx` (+placement+render+kolor). Reuse `formatTapPositionLabel` (v2 oltcGlyph, ten sam format co tabliczka V12K-091). | Testy: `overlay.test.ts` +7 (TAP+SWITCH bajt-rowne; pozycja ujemna bez switch; brak TAP→brak badge; nie-LF→pusty; determinizm; wyrocznia negatywna; realne refy transformatorow ze sceny) + `sldCanvasV3.test.tsx` +5 (render badge+tekst „poz. +3 · 4×"; bez overlay warstwa pusta; determinizm innerHTML; formatOltcBadgeLabel znak/switch; computeOltcBadgePlacements ignoruje nie-transformator). Type-check czysty; regresja SLD v3+overlay; guardy `sld_determinism`/`overlay_no_physics`/`trace_ui_leak`/`no_codenames` zielone; FROZEN/determinizm nietkniete. | wysoki (domkniecie lancucha OLTC do wyniku na SLD v3) |
| V12K-091 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela: „OLTC v3 jako osobny, dedykowany przebieg SLD rework").** Domkniecie znaleziska V12K-090: wizual regulacji zaczepow (OLTC/DETC) w PRODUKCYJNYM SLD v3 (compose wlasny), a nie w martwym v2 GpzSwitchgearRenderer. Dotad glif OLTC z V12K-086 zyl tylko w v2 (nierenderowany w produkcji); v3 nie pokazywal stanu regulacji transformatora na tabliczce. | Znalezisko V12K-090; recon: `ui/sld/v3/compose/gpz.ts` (pasmo tabliczki TR `transformerLabels`/`StationNameBandRow`), `CanonicalGpzTransformer` (typ transformatora sceny v3), `enmToCanonicalGpzAdapter.buildTransformers` (ENM→scena), `oltcGlyph.buildOltcAnnotation` (reuzyty z V12K-086, dane z `tap_changer`) | **Wizual OLTC w v3 compose (addytywny/parytetowy, READ-ONLY, ZERO fizyki):** (1) `CanonicalGpzTransformer.oltc?: OltcGlyphAnnotation | null` (typ) + populacja `oltc: buildOltcAnnotation(tr.tap_changer)` w `enmToCanonicalGpzAdapter.buildTransformers` (reuse czystej funkcji V12K-086, dane z modelu); (2) w `compose/gpz.ts` wiersz tabliczki TR `"${kind} ${positionLabel} · ${modeLabel}"` (+ `U_zad` gdy AUTO z nastawa), `labelClass t3`, dokladany WYLACZNIE gdy `oltc` obecny (uczciwy brak regulacji → brak wiersza; fixtury bez OLTC nietkniete → determinizm/parytet); (3) klucz parytetu `gpz.transformer.oltc` dodawany tylko dla transformatorow z regulacja. Dane plyna: model `tap_changer` → adapter ENM→scena → compose v3 → tabliczka. FROZEN/solver nietkniety (prezentacja stanu regulacji z modelu, nie fizyka). | `ui/sld/v2/renderer/GpzCanonicalRenderer.tsx` (pole `oltc` w `CanonicalGpzTransformer` + import typu), `ui/sld/v2/canvas/enmToCanonicalGpzAdapter.ts` (populacja + import `buildOltcAnnotation`), `ui/sld/v3/compose/gpz.ts` (wiersz OLTC + klucz parytetu). V12K-086 (v2 glif) pozostaje jako kontrakt/wzorzec (`oltcGlyph.ts` reuzyty wprost). | Testy: `compose/__tests__/gpz.test.ts` +3 (OLTC AUTO+U_zad → wiersz + klucz parytetu; DETC MAN → wiersz bez U_zad; brak regulacji → ZERO wierszy + brak klucza). Type-check czysty; regresja SLD v3 compose + v2 (124 pliki, 2308 passed/14 todo); guardy `sld_determinism_guards`/`overlay_no_physics`/`no_codenames` zielone; FROZEN/determinizm nietkniete (addytywne, fixtury bez OLTC bez zmian). | wysoki (domkniecie wizualu OLTC do produkcji v3) |
| V12K-090 | **ROZSTRZYGNIETY (2026-07-21 przez V12K-091; pierwotnie OTWARTY/ZNALEZISKO — rekonesans przy ostatniej mili overlay OLTC „Wybierz co lepiej").** Produkcyjny SLD to **v3** (`WorkspaceSurfaceRouter` → `SldCanvasV3Workspace`), sklada scene wlasnym `ui/sld/v3/compose/**`, z v2 importuje TYLKO TYPY (`import type MiniBlockBayDescriptor`). Zaden produkcyjny komponent nie montuje `GpzRenderer`/`GpzSwitchgearRenderer` (v2). **Skutek: glif design-state OLTC z V12K-086 (w `ApparatusTransformerSymbol`, v2 GpzSwitchgearRenderer) NIE renderuje sie w produkcji v3** — jest poprawny i pokryty testami (89 v2), ale v3 go nie uzywa. Analogicznie badge wynikowy OLTC nalezy do v3 compose. Dodatkowo transformator w scenie v3 ma `meta.ownerRef=bayRef`, a metryki overlay (V12K-089) sa kluczowane `branch_id` — niedopasowanie tozsamosci do rozwiazania w v3 (bayRef↔branch_id). | Rekonesans: `WorkspaceSurfaceRouter.tsx:2852` (mount v3), `ui/sld/v3/scene/buildScene.ts` + `compose/gpz.ts` (wlasna scena), `overlay.ts` naglowek („transformer ownerRef=bayRef nie odpowiada kategorii overlay"), brak `<GpzRenderer` poza v2/testami | **Rozwiazanie = implementacja wizualu OLTC w v3 compose** (glif design-state z tap_changer + badge wynikowy z overlay store, z rozwiazaniem bayRef↔branch_id). DANE sa juz produkcyjne (V12K-089 → raw store czytany przez v3). To osobny, wiekszy przebieg SLD v3 (nie last-mile). **Rekomendacja:** wpisac do programu SLD rework (PLAN_SLD_REWORK) jako „wizual OLTC w v3 compose"; nie budowac dalej na v2. | `ui/sld/v3/compose/**` (przyszla implementacja), `docs/plan/PLAN_SLD_REWORK.md`. V12K-086 (v2) pozostaje jako kontrakt/wzorzec + testy; NIE usuwac (moze byc reuzyty przy porcie do v3). | Znalezisko zapisane (nie cicho); wizual OLTC v3 = zadanie do programu SLD. Dane (V12K-089) i logika (adaptery V12K-086/088) gotowe do reuzycia. | wysoki (korekta zakresu: wizual OLTC nalezy do v3) |
| V12K-089 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Kontynuuj addytywne masz zgode na pelne ulepszenia" — Opcja 1 z AskUserQuestion, zgoda na tkniecie kontraktu wynikowego addytywnie).** Overlay wynikowy OLTC „na ekranie" wymagal, by metryki regulacji (pozycja koncowa zaczepu, liczba przelaczen) trafialy do produkcyjnego `overlay_payload` (`resultset_v1`) — dotad `oltc_control` byl tylko w `global_results`, nie per-element, wiec glif transformatora nie mial danych wynikowych. | Rekonesans: `build_execution_result_set` (canonical_analysis) buduje element_results LF z `build_branch_results` rows; `_extract_element_metrics` (result_builder_v1) = whitelista kodow metryk; `oltc_control` (final_positions/switch_counts) w `raw_result`; schemat resultset_v1 — `code`/`format_hint` to WOLNE stringi (bez enuma) | **Surfacing addytywny (FROZEN-safe):** (1) w pętli gałęzi LF `build_execution_result_set` — dla gałęzi z wpisem `oltc_control.final_positions[branch_id]` dopisuje do `values` klucze `tap_position` (+`tap_switch_count` gdy jest) — tylko dla transformatorow z realna regulacja, reszta bez zmian (determinizm; PowerFlowResult FROZEN nietkniety — interpretacja gotowego wyniku); (2) `_extract_element_metrics` whitelista +`tap_position→TAP_POSITION`, `tap_switch_count→TAP_SWITCH_COUNT` (unit "", int). Dane plyna: solver `oltc_control` → resultset_v1 overlay metrics → raw store (v2/v3 canvas) ORAZ typed store przez rekoncyliacje V12K-088 → runtime overlay. Domyka lancuch overlay wynikowego OLTC do danych. | `enm/canonical_analysis.py` (iniekcja tap_* w values gałęzi LF), `domain/result_builder_v1.py` (whitelista metryk). Schemat resultset_v1 BEZ ZMIAN (code/format_hint wolne stringi). exclude_none/addytywne — modele bez OLTC bez zmian (sygnatura stabilna). | Testy: `TestOltcOverlayMetrics` (2: TAP_POSITION/TAP_SWITCH_COUNT surface gdy obecne; brak dla gałęzi bez OLTC). Regresja resultset/result_builder/result_contract (85+44) + canonical/execution/load_flow/oltc (115); ruff/black/mypy; guardy (resultset_v1_schema/trace_determinism[pod poetry]/arch) zielone. | wysoki (domkniecie overlay wynikowego OLTC do danych solvera) |
| V12K-088 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Lecisz" + wybor AskUserQuestion „Wpiecie produkcyjne rodziny adapterow").** Rekonesans overlay OLTC wykryl ROZDWOJENIE kontraktow overlay: (a) produkcyjny backend `/api/execution/runs/{id}/results/v1` zwraca `overlay_payload` z `elements` jako SLOWNIK (RawOverlayPayload) — czytany przez v2/v3 canvas; (b) typowana rodzina adapterow `OverlayPayloadV1` (`elements` tablica; LoadFlow/ZeroSequence/OLTC) konsumowana przez `useOverlayStore` + `V12OverlayModeController` + `useOverlayRuntime` (styleMap), ALE BEZ producenta produkcyjnego — `useLegacyOrchestrator` mial jawny stub `void useOverlayStore; // future PR-16`. Skutek: cala typowana warstwa overlay (w tym nowy adapter OLTC) byla martwa w produkcji. | Rekonesans: `useLegacyOrchestrator` (fetch /results/v1 → `setRawOverlay`), `overlayStore.loadOverlay`, `V12OverlayModeController` (reaguje na `useOverlayStore.overlay` per analysis_type), `RawOverlayElement` (ref_id/kind/metrics/severity) vs `OverlayElement` (element_ref/element_type/numeric_badges/visual_state) | **Adapter rekoncyliacyjny + wpiecie producenta (Opcja 2 wg wlasciciela):** (1) czysty `adaptRawOverlayToTyped(RawOverlayPayload) → OverlayPayloadV1|null` — dict→tablica, metrics→numeric_badges, severity backendu→visual_state (INFO→OK/WARNING/IMPORTANT→WARNING/CRITICAL), sort element_ref ASC, tokeny (zero fizyki/heurystyk), null dla pustego (uczciwy stan zerowy); (2) `useLegacyOrchestrator` — usuniety stub, po `setRawOverlay` karmi TE SAMA odpowiedzia typowana warstwe (`loadTypedOverlay(adaptRawOverlayToTyped(raw))` / `clearTypedOverlay`), obie sciezki (raw dla canvas, typed dla trybu overlay/runtime) z jednego wyniku backendu. Rodzina adapterow (LoadFlow/ZeroSequence/OLTC) ma teraz producenta produkcyjnego; metryki OLTC (gdy backend je wystawi w overlay_payload) plyna automatycznie jako numeric_badges. | `ui/sld-overlay/RawToTypedOverlayAdapter.ts` (nowy) + `index.ts`, `ui2/legacy/useLegacyOrchestrator.ts` (loadOverlay/clearOverlay + deps). Kontrakt backendu BEZ ZMIAN (FROZEN resultset_v1 nietkniety — rekoncyliacja po stronie frontu). | Testy: `RawToTypedOverlayAdapter` (6: dict→tablica sort, mapowanie severity, CRITICAL→thick, null/pusty, metryka bez wartosci→null, determinizm). Regresja sld-overlay+legacy (206); type-check/lint; guardy (overlay_no_physics/sld_determinism/codenames/trace_ui_leak) zielone. | wysoki (ozywienie produkcyjne typowanej warstwy overlay) |
| V12K-087 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Lec oba" — G-OZE-B2, most zgodnosci NC RfG z modelu; domkniecie karty task #50 odblokowanej V12K-084).** Flagi zdolnosci NC RfG (LVRT/HVRT/PF-droop/Q(U)) konfigurowalo sie tylko w matrycy NcRfgTestsTab (station-der, client-side) i wysylalo w ULOTNYM body do `/api/ncrfg-tests/run` — nie zyly na modelu. Rownolegle `NcRfgComplianceChecker` + `DerDataForCompliance` istnialy, ale BEZ PRODUCENTA z modelu (martwy konsument). Phantom: deklaracja FRT projektanta nie mapowala na zadne pole modelu. | Rekonesans: `add_converter_source` utrwalal `frequency_droop_percent` (G-OZE-B) i `qu_slope_pu_per_pu` (G-OZE-B3/B4) na `generator.meta`, ale NIE flagi FRT; `DerDataForCompliance` (has_lvrt/has_hvrt/has_pf_droop/has_qu_curve) bez konstruktora w src; karta katalogowa ConverterType nie ma zdolnosci FRT (tylko envelope Q) | **Utrwalenie + most (reuzycie, zero rownoleglej sciezki):** (1) `AddConverterSourcePayload.has_lvrt_curve/has_hvrt_curve` (jawna deklaracja FRT) → `add_converter_source` zapisuje na `generator.meta` (3 galezie PV/BESS/FW, helper `_as_bool`); (2) most `build_der_compliance_from_generator` / `build_der_compliance_list_from_enm` (`ncrfg_compliance/model_bridge.py`) → `DerDataForCompliance` z modelu: FRT z jawnych flag, `has_pf_droop` wyprowadzone z `frequency_droop_percent>0`, `has_qu_curve` z `qu_slope_pu_per_pu>0` lub trybu Q(U) (Q_OD_U), cosφ/moc/napiecie z generatora+szyny (bez fabrykacji: brak szyny/nie-falownik → pominiety); ozywia checker (realny konsument z modelu). (3) UI: kreator OZE krok Regulacja zyskuje przelaczniki „Charakterystyka LVRT/HVRT" (PoleWyboru tak/nie → payload; false→undefined). (4) Endpoint uruchamialny (follow „Kontynuuj"): `GET /api/ncrfg-tests/cases/{case_id}/compliance?operator_id=` — buduje wejscia DER z committed ENM (most) i uruchamia checker per zrodlo; uczciwy stan zerowy (brak DER → pusta lista), 404 dla nieznanego operatora. Lancuch domkniety: model → most → checker → API. | backend: `enm/domain_ops_models.py` (payload), `enm/domain_operations_v2.py` (`_as_bool` + meta ×3), `application/ncrfg_compliance/model_bridge.py` (nowy) + `__init__.py`; front: `ui2/kreatory/zrodlo-oze/{zrodloOzeModel,strings,KreatorZrodlaOze}`. FROZEN/solver/PF nietkniete; flagi addytywne (brak → False/undefined, determinizm zachowany). Odnoga matrycy NcRfgTestsTab (station-der) BEZ ZMIAN — reuzyto pola modelu, nie ruszano rownoleglej persystencji. | Testy: backend `test_ncrfg_model_bridge` (9: payload FRT, most FRT/PF/Q(U)/cosφ, lista z ENM pomija nie-falownik/bez szyny, checker liczy na danych z modelu, determinizm); front `zrodloOzeModel` +1 (FRT tylko gdy zadeklarowane). Regresja backend converter/oze/ncrfg (141) + front OZE (24); ruff/black/mypy; guardy (arch/domain_no_guessing/canonical_ops/utf8/codenames/forbidden_terms/dialog_completeness) zielone. | wysoki (domkniecie phantomu FRT + ozywienie checkera z modelu) |
| V12K-086 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Lec oba" — glif OLTC na SLD, karta koordynacyjna SLD-02, odblokowana zniesieniem granicy watkow V12K-084).** Program OLTC (V12K-045) dostarczyl kanoniczny `Transformer.tap_changer` + wynik regulacji z LF, ale SCHEMAT pokazywal tylko booleanowski grot zaczepu (`hasTapChanger`), i to niepodpiety w GPZ (call site bez propsow). Karta SLD-02 wymagala adnotacji stanu regulacji (OLTC/DETC, pozycja, AUTO/MAN, U_zad). Wczesniej odlozone do „watku SLD" — po V12K-084 realizowane inline. | Karta `docs/uiux/KARTA_KOORDYNACJI_SLD_02_OLTC_GLIF.md` (kontrakt danych READ-ONLY: pkt 2 tap_changer); recon: front `types/enm.ts` (brak typu TapChanger — tylko plaskie tap_*), `enmToSldAdapter` trBays (map tr→GpzBayDescriptor), `GpzApparatusSymbols.ApparatusTransformerSymbol`, `GpzSwitchgearRenderer` (call site bez propsow) | **Glif OLTC design-state (READ-ONLY, ZERO fizyki w SLD):** (1) typ `TapChanger` na froncie (mirror ENM) + `Transformer.tap_changer`; (2) czysty helper `buildOltcAnnotation(tap_changer)` → `OltcGlyphAnnotation|null` (kind OLTC/DETC, pozycja „poz. ±N", tryb AUTO/MAN wg control_mode, U_zad tylko AUTO+dodatni setpoint — zero fabrykacji); (3) `GpzBayDescriptor.oltc` populowane w adapterze z `tr.tap_changer`; (4) `ApparatusTransformerSymbol` renderuje adnotacje (etykieta+pozycja, tryb·U_zad) po lewej stronie symbolu, tokeny SLD (`COLOR_LINE_PRIMARY`), obecnosc oltc implikuje grot zaczepu; (5) call site GPZ przekazuje `oltc={bay.oltc}`. Brak tap_changer/NONE → brak adnotacji (jak dzis). | `types/enm.ts`, `ui/sld/v2/canvas/oltcGlyph.ts` (nowy helper), `enmToSldAdapter.ts` (trBays + import), `GpzSwitchgearTypes.ts` (OltcGlyphAnnotation + pole), `GpzApparatusSymbols.tsx` (render), `GpzSwitchgearRenderer.tsx` (prop). Kontrakt danych OLTC BEZ ZMIAN (tap_changer istnieje). **Overlay wynikowy (karta §3.5) DOSTARCZONY (follow „Lecisz"):** czysty adapter `OltcOverlayAdapter.adaptOltcControlToOverlay(oltc_control) → OverlayPayloadV1` (wzorzec ZeroSequence/LoadFlow adapterow) — jeden element per transformator z badge `tap_position` (pozycja koncowa) + `switch_count` (liczba przelaczen), werdykt wizualny z `converged` (OK / brak zbieznosci→WARNING), sort element_id ASC (determinizm), tokeny semantyczne (bez fizyki, bez hex — `overlay_no_physics_guard` zielony). Produkuje kontrakt konsumowany przez istniejacy OverlayEngine (element_ref = branch_id transformatora). Eksport z `sld-overlay/index`. Testy: `OltcOverlayAdapter` (7: badge pozycji/przelaczen, sort, brak zbieznosci→WARNING, brak switch_counts→bez fabrykacji, pusty→uczciwy stan zerowy, determinizm). design-state (§3.1-3.4) + overlay (§3.5) — karta SLD-02 domknieta. | Testy: `oltcGlyph` (8: format pozycji ±, mapowanie OLTC/DETC/AUTO/MAN/U_zad, guardy braku danych, determinizm) + `gpzSwitchgearScada` +2 (symbol z adnotacja: testid/kind/mode/tekst/grot; bez oltc→brak). Regresja SLD: 417 (canvas+determinizm) + 162 symbole. type-check/lint/guardy (no_codenames/sld_determinism/forbidden_terms) zielone. Determinizm zachowany (fixtures bez tap_changer → brak zmiany). | wysoki (domkniecie glifu OLTC design-state) |
| V12K-085 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Kontynuuj krok po kroku" — G-STK-8, strona KONFIGURACYJNA GAP-2).** Domkniecie producenta SPD ustalonego w nocie dlugu V12K-083: `bays[].primary_devices` kind=SURGE_ARRESTER nie mial kanonicznego producenta — most G-STK-7 czytal pole, ktorego nikt nie zapisywal kanonicznie (poza fixture SLD i recznym modelem). Inzynier nie mogl postawic ogranicznika, choc glif SLD i koordynacja izolacji IEC 60071 czekaly. | Rekonesans enduro: `add_sn_bay` pisze do `substation.meta.field_specs` (kanal read-model), `_collect_bays`/`_build_primary_devices` projektuja field_specs→Bay (primary_devices z equipment_refs/pomiarow — bez zrodla ogranicznika); wzorzec op G-KOMP (`add_shunt_compensator_sn`); katalog `mv_surge_arrester_catalog` (12 typow, namespace OGRANICZNIK_SN, `get_surge_arrester_type`) | **Operacja `add_surge_arrester_sn`** (domain_operations_v2): katalog-first (walidacja typu OGRANICZNIK_SN), materializuje aparat na `field_spec["surge_arresters"]` wskazanego pola (field_ref lub bus_ref→pierwsze pole), deterministyczny device_ref, dedup per catalog_ref, walidacje (spd.bus_missing/catalog_required/catalog_not_found/field_not_found/already_present). **Storage = field_spec** (jeden kanal, zero rownoleglego store) czytany przez OBAJ konsumenci: (1) read-model `_build_primary_devices` projektuje `Bay.meta["surge_arresters"]`→`BayPrimaryDevice(kind=SURGE_ARRESTER, GROUND_BRANCH, earthing_role=surge_ground, symbol surge_arrester_10ka)` → glif SLD; (2) most `build_v126_insulation_from_enm` czyta field_specs (kanal 2, parytet z `bays[].primary_devices`) → koordynacja izolacji IEC 60071. Op zarejestrowana (op-name set + ALL_V2_HANDLERS). | `enm/domain_operations_v2.py` (op + rejestracja), `solver_input/v126_contracts.py` (most: kanal field_specs), `application/field_read_model.py` (`_collect_bays` przenosi surge_arresters na Bay.meta, `_build_primary_devices` projektuje aparat). FROZEN/SC nietkniete; addytywne (modele bez SPD → bez zmian). Bez edycji ui/sld/** (glif renderuje istniejacy symbol z primary_devices). | Testy: `test_add_surge_arrester_sn` (9: materializacja katalog-first, resolve po bus, walidacje, dedup, most czyta kanal field_spec z realnymi parametrami POLIM-D 24kV, read-model projektuje SURGE_ARRESTER, determinizm) + regresja read-model/field-view (18) + solver_input/v126 (52). ruff/black/mypy (nowy kod czysty; 10 bledow mypy pre-existing przed l.2579), guardy (canonical_ops/dialog_completeness/arch/solver_boundary/domain_no_guessing) zielone. **Kreator ui2 DOSTARCZONY (domkniecie „do ostatniego klika"):** `ui2/kreatory/ogranicznik/**` (KreatorOgranicznikaSn, katalog-first OGRANICZNIK_SN przez istniejacy `fetchSurgeArresterTypes`, odczyt karty U_m/MCOV/U_res/klasa/BIL, PanelTeorii IEC 60071 must-have, uczciwy stan zerowy bez pola/szyny, sekcja downstream „co to uruchamia", selekcja po operacji na pole BaySN); wpiecie: `operationFormRegistry`, `operationSurfaceRegistry` (expand_workspace), `catalogGate` (OGRANICZNIK_SN + topology_allowed_without_catalog), `operationContext` (BaySN→field_ref / Bus→bus_ref), `actionMenuBuilders` + `EngineeringContextMenu` (akcja „Dodaj ogranicznik przepiec SN..." w menu szyny SN), `types/domainOps`. Testy front: `ogranicznikModel` (6) + `KreatorOgranicznikaSn` realna sciezka (3: zapis add_surge_arrester_sn z field_ref+katalog, stan zerowy blokuje, selekcja BaySN). Type-check/lint/guardy (dead_click/interaction_matrix/dialog_completeness/codenames/terminology) zielone; regresja rejestrow/menu (418). ZERO fizyki w UI (margines BIL z solvera koordynacji). | wysoki (domkniecie config side GAP-2 do ostatniego klika) |
| V12K-084 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „twarda granica watkow usunieta zapamietaj i zapisz do planow dzialaj enduro end").** Trzy programy (UI/UX, SLD rework, Engineering 10x) mialy TWARDA granice watkow: „Cross-thread file collisions are forbidden; coordination only via cards" — SLD rework i 10x jako OSOBNE sesje, zakaz edycji `ui/sld/**`/`sld-editor/**`/`engine/sld-layout/**` i warstw 10x z watku UI/UX; domykanie lancucha do SLD odkladane do „karty cross-thread". | Dyrektywa wlasciciela 2026-07-21; `CLAUDE.md` „Active programs"; liczne rejestry V12K z klauzula „bez edycji ui/sld/** (watek SLD V12K-060)" | **Granica ZNIESIONA.** Jeden watek prowadzi wszystkie trzy programy end-to-end w tej samej sesji/PR; wolno edytowac `ui/sld/**`, `sld-editor/**`, `engine/sld-layout/**` oraz warstwy 10x. Znika zakaz kolizji cross-thread i obowiazek kart koordynacyjnych miedzy watkami. Lancuch domyka sie do ostatniego klika (w tym glify/rendering SLD) bez odkladania do osobnego watku. Rygor jakosci BEZ ZMIAN: pelna regresja wlasciwej warstwy + guardy + determinizm + FROZEN/golden nietkniete przed scaleniem. Klauzule „watek SLD V12K-060" w starszych rejestrach staja sie bezprzedmiotowe (mozna realizowac tu). | `CLAUDE.md` (sekcja „Active programs" — granica zniesiona), `mv-design-pro/PLANS.md`. Wszystkie przyszle karty: dozwolona edycja SLD/10x w tym watku. | Meta-decyzja procesowa (bez zmiany kodu produktu); brak wplywu na determinizm/FROZEN. Odblokowuje: G-OZE-B2 (most NC RfG audit2), wzbogacenie glifow SLD z modelu, karty koordynacyjne SLD/OLTC realizowane inline. | wysoki (proces) |
| V12K-083 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Kontynuuj" — ostatnia realna faza programu stacji G-STK-7).** GAP-2 (ogranicznik przepiec) domkniety: aparat pierwotny pola `kind=SURGE_ARRESTER` istnial w modelu (materializowany przez `station_archetype_substrate`) i renderowal sie na SLD (glif surge_arrester_10ka), ale koordynacja izolacji IEC 60071 (`_insulation` w v126_academic) czytala WYLACZNIE `parameters["insulation"]` — reczna kolekcje z UI (V126AcademicSurface wysylalo FABRYKOWANY ogranicznik: `BUS_SN`/`70kV`). Element modelu byl WYSPA (rysowany, nie liczony); UI wysylalo phantom (kontrolka bez pokrycia w modelu). | Recon: `V126InsulationInput` (u_m/network_neutral/mcov/residual/tov/energia), `_insulation` (bil_table 12/17.5/24/36 kV, margines BIL), `mv_surge_arrester_catalog` (12 typow PN-EN 60099-4: POLIM-D/3EK7/UltraSIL/SB/ASA), `BayPrimaryDevice.kind=SURGE_ARRESTER`+catalog_ref, `Bus.grounding`/`Transformer.hv_neutral/lv_neutral` (GroundingConfig), `station_archetype_substrate` (producent ogranicznikow) | **Most `build_v126_insulation_from_enm(enm)`** (solver_input/v126_contracts): dla kazdego SURGE_ARRESTER w polach modelu buduje `V126InsulationInput` — MCOV/U_res/TOV/energia z rekordu katalogu po `catalog_ref` (gdy brak karty → dobor wstepny: U_m z typoszeregu IEC wg napiecia szyny, mcov None → solver liczy udokumentowanie); `network_neutral` z uziemienia punktu neutralnego (szyna→transformator, isolated/petersen→"isolated", rezystor/bezposrednie→"earthed", brak→kontraktowy default); deduplikacja per (szyna, catalog_ref) — ograniczniki fazowe = jedno wejscie; kolejnosc deterministyczna. Wpiete w `build_v126_input_from_enm` (model napedza; `parameters` dalej nadrzedne — wsteczna zgodnosc). UI: `V126AcademicSurface` insulation_coordination przestaje wysylac fabrykowany ogranicznik (pusty `parameters` → model napedza, uczciwy stan zerowy „brak ograniczników"). ZERO fizyki w UI — margines BIL z solvera. | `solver_input/v126_contracts.py` (most + wpiecie), `ui/workspace/surfaces/V126AcademicSurface.tsx` (usuniety phantom insulation). FROZEN solver `_insulation` + SC nietkniete; `insulation` addytywne (modele bez ogranicznikow → [] jak dotad, determinizm/golden zachowany). Bez edycji ui/sld/**. | Testy: `test_v126_insulation_bridge` (11: mapowanie karty katalogu, network_neutral z szyny petersen/rezystor, brak danych→default, brak ogranicznika→[], dedup fazowy, brak karty→U_m z szyny, ogranicznik nN→pominiety, wpiecie w build_input, determinizm, e2e run przez API z pustymi parametrami). Regresje: solver_input + v126 (52) + reference_packs zielone; ruff/black/mypy; guardy (arch/solver_boundary/codenames/forbidden_terms/utf8) zielone; frontend type-check. **Nota dlugu (nie-cichy, doprecyzowana rekonesansem enduro 2026-07-21):** strona KONFIGURACYJNA GAP-2 pozostaje otwarta — brak KANONICZNEGO producenta `bays[].primary_devices` kind=SURGE_ARRESTER. `station_archetype_substrate` to SPEC fixture SLD (nie materializuje ENM), a `add_sn_bay` pisze do `field_specs` (meta stacji, kanal read-model), nie do `bays[].primary_devices`. Most G-STK-7 (odczyt `bays[].primary_devices`) jest poprawny i przetestowany dla obu sciezek (karta/dobor wstepny), ale realny producent modelowy ogranicznika (operacja domenowa `add_surge_arrester`/rozszerzenie pola o aparat SPD z catalog_ref + kreator ui2) = ODREBNY dedykowany przebieg (SPD config side). Do zlecenia jako G-STK-8 (config SPD end-to-end: op → primary_devices+catalog_ref → SLD glif + most G-STK-7 → koordynacja izolacji). | wysoki (domkniecie GAP-2, ostatnia realna faza stacji) |
| V12K-082 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „zbadaj doglebnie wielowarstwowo pole sprzegla w powiazaniu z szablonami pol i schematem i wszystkim o czym zapominasz — wymagany audyt do ostatniego klika end to end").** Wielowarstwowy audyt pola SPRZEGLO po G-STK-5. Znaleziono realna luke: `insert_station_on_segment_sn` (GLOWNA sciezka, swiadomy podzial) czytala z sn_fields tylko field_role+apparatus — GUBILA wiazanie szablonu producenta (bay_template_ref/family/manufacturer) i NIE materializowala protection_codes. Phantom „w druga strone": kreator wybiera szablon SPRZEGLA, backend wyrzuca → read-model (konsumuje spec.protection_codes/bay_template_ref) i SLD bez wiazania/glifow. G-POLE-R + kody zrobiono dla add_sn_bay/append, NIE dla insert. | **Kanon `docs/uiux/AUDYT_POLE_SPRZEGLA_2026-07.md`** (trasa pola przez warstwy: kreator→katalog→insert→append→read-model→SLD); recon `field_read_model` (l.318/319 konsumuje codes/template, `_assign_coupler_sides`), `BAY_TEMPLATE_COUPLER`, `_resolve_bay_template_protection_codes` | **PS-1/PS-2 (parytet z add_sn_bay/append):** petla pol insert czyta refy producenta per pole (fallback na station.switchgear) + materializuje protection_codes wspolnym resolverem (szablon producenta > kanoniczna tablica roli, zero fabrykacji); wszystkie klucze na field_spec. Domkniecie „do ostatniego klika": kreator→field_spec(szablon+kody)→read-model→SLD. **PS-3** (rendering SLD 2 sekcji + glif sprzegla) — **WERYFIKACJA (dyrektywa „Ps 4 oraz SLD"): NIE byla luka.** Stacja renderuje sie przez `MiniBlockRmuRenderer` (nie GPZ `SectionRenderer`), ktory jest w pelni sekcyjno-swiadomy: `deriveFootprintType('sectional')→mv_lv_sectional` → szyna cellular (IEC 60617 segregated) + kolumna pola sprzegla „SPR"; napedzane `station_type='sectional'` z G-STK-5. Potwierdzone 89 testami SLD. **PS-4** ✅ NAPRAWIONE — append liczy protection_codes wspolnym resolverem dla wszystkich rol (test: LINIA_IN→51/50/51N, TR→87T/…). | `enm/domain_operations.py` (petla pol insert: refy + resolver protection_codes), `docs/uiux/AUDYT_POLE_SPRZEGLA_2026-07.md`. FROZEN nietkniety; klucze field_spec addytywne (determinizm 100x zachowany). | Testy: TestInsertStationFieldTemplateBinding (SPRZEGLO→COUPLER 51/50 + bay_template_ref/manufacturer; TR→transformatorowe; IN→liniowe). Regresje 66 domain (determinizm/permutacja) + 10 field/protection read-model; guardy/ruff/black zielone. | wysoki (domkniecie pola do ostatniego klika) |
| V12K-081 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Lec").** G-STK-5: realna dwusekcyjna stacja sekcyjna (P5 z audytu). Stacja typu D dokladala pole SPRZEGLO, ale tworzyla JEDNA szyne SN — sprzeglo „w powietrzu" (brak II sekcji). | Karta G-STK-5; recon `insert_station_on_segment_sn` (jedna sn_bus), `bus_coupler` (models Literal + mapping LOAD_SWITCH zamkniety→w topologii), GPZ coupler jako wzor | **Wdrozenie (gated na station_type=="D", pozostale typy nietkniete → determinizm zachowany):** insert tworzy DWIE sekcje szyny SN (A=WE+trafo, B=WY) + realne sprzeglo miedzysekcyjne (bus_coupler, normalnie zamkniete → ciaglosc magistrali); prawy odcinek magistrali wychodzi z sekcji B; pole SPRZEGLO wiazane z REALNYM sprzeglem (equipment_refs, nie zdublowany aparat); pole WY na sekcji B; bus_refs=[A,B]. Front bez zmian (opcja „Sekcyjna" juz istnieje → backend D). | `enm/domain_operations.py` (insert: 2 sekcje + coupler + routing + patch pol). FROZEN solver/PF nietkniety (topologia w warstwie domenowej). | Testy: TestStationTypeDSectional wzmocniony (2 sekcje SN 15 kV + sprzeglo bus_coupler zamkniete + prawy odcinek z sekcji B + pole SPRZEGLO→realne sprzeglo). Determinizm 100x + permutacja + type-B passthrough nietkniete; 98 domain/mapping/PF; guardy (arch/domain_no_guessing/pcc_zero/canonical_ops/solver_boundary)/ruff/black zielone. | wysoki |
| V12K-080 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Lec").** G-STK-4: petla zwarcia nN u zrodla stacji budowana Z MODELU (domkniecie lancucha uziemienia G-STK-1 „do ostatniego klika"). Konsument (solver `fault_loop_iec60364` + endpoint) istnial, ale bral skladowe R/X recznie (scaffolding P0.5b) — brak mostu model→solver. | Karta G-STK-4 (`KARTY_WYKONAWCZE_STACJA_GSTK_2026-07.md`); recon `fault_loop_builder` (scaffolding), `fault_loop_iec60364.compute_fault_loop`, `Transformer.uk_percent/pk_kw/sn_mva/ulv_kv`, `substation.meta.nn_earthing_system` (z G-STK-1) | **Wdrozenie end-to-end (Fable):** (solver) `transformer_lv_impedance_ohm` — Z transformatora sprowadzona do nN z uk%/Sn/Ulv/Pk (Z=uk%·Un²/Sn, R z Pk, X=√(Z²−R²); brak Pk → R=0,X=Z jawnie). (analiza, ZERO fizyki) `application/analyses/fault_loop/service.build_station_fault_loop_view` — wylawia trafo/szyne nN, mapuje uklad (TN-*→solver; TT/IT→uczciwe „nie dotyczy"), petla u zrodla (przewody≈0) przez `compute_fault_loop`; uczciwy `missing_data`. (API) `GET /api/cases/{id}/enm/station-fault-loop`. (UI) sekcja inspektora „Petla zwarcia u zrodla (nN)" w zakladce Wyniki dla stacji — Z_s/Ik/Z trafo + uczciwe stany. **ZERO fizyki w UI, ZERO fabrykacji.** | `network_model/solvers/fault_loop_builder.py`, `application/analyses/fault_loop/service.py` (nowy), `api/enm.py` (endpoint), `ui2/inspector/SekcjaPetlaZwarcia.tsx` (nowy) + mount w InspectorPanel. FROZEN solver `compute_fault_loop` nietkniety. | Testy: serwis 5 (TN Ik~24 kA dla 630 kVA/uk 4%, IT „nie dotyczy", brak uk, czysto reaktancyjny bez Pk, brak stacji) + sekcja frontend 3 (realna sciezka fetch OK/IT/brak). Regresje: fault_loop 69 backend, inspector 62 frontend; guardy/ruff/black/type-check/lint zielone. | wysoki (domkniecie lancucha uziemienia) |
| V12K-079 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „napisz prompt wykonawczy i Lec po kolei i nie pytaj").** Kontynuacja programu braków stacji (po G-STK-1). Karty wykonawcze G-STK-2..7 (§0 rozstrzygnięć + bramki). | **Kanon `docs/uiux/KARTY_WYKONAWCZE_STACJA_GSTK_2026-07.md`**; recon per faza (add_nn_load, mapper transformatora, v126 `_insulation`, sectional 1-szyna) | **Wdrożone end-to-end (Fable osobiscie):** **G-STK-3** potrzeby własne stacji — helper `_materialize_station_auxiliary_load` (parytet insert/append) czyta `station_auxiliary` (P + cosφ) → odbiór nN, Q z cosφ (reużycie add_nn_load), konsument PF; UI sekcja w kroku nN. **G-STK-6** praca równoległa transformatorów — `Transformer.n_parallel` (addytywne, exclude_none, GOLDEN-SAFE) + mapper agreguje Sn×n (Z/n bez zmiany solvera) + allowlist create/update + station op (`_apply_transformer_parallelism`, parytet); UI pole 1-4 + PanelTeorii. **Recon rozstrzygający:** G-STK-5 (dwusekcyjna) — potwierdzony realny brak (sectional = pole SPRZĘGŁO + 1 szyna SN); G-STK-7 (SPD) — GO z mostem (konsument `_insulation` IEC 60071 istnieje, brak mostu model→V126InsulationInput). **ZERO fizyki w UI, ZERO fabrykacji, ZERO wysp.** | `enm/domain_operations.py` (helpery + insert/append), `enm/models.py` (n_parallel), `enm/mapping.py` (agregacja), `enm/topology_ops.py` (allowlist), `ui2/kreatory/stacja/**` (2 pola + testy), `docs/uiux/KARTY_WYKONAWCZE_STACJA_GSTK_2026-07.md`. FROZEN solver nietkniety (zmiany w input-prep/mapperze). | Testy realnej ścieżki: append potrzeby własne (Q z cosφ 0,0075 MVAr); append n_parallel=2 (dowód konsumenta przez map_enm_to_network_graph → Sn×2); payloady warunkowe. Regresje: 98 domain/mapping/PF (determinizm+golden zachowane, n=1 → bez zmian), 57 stacja; guardy/ruff/black/type-check/lint zielone. Pozostaje KARTA: G-STK-2/4/5/7 (dedykowane rundy). | wysoki (2 fazy end-to-end + recon) |
| V12K-078 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „opcja MAX — realne braki").** Sekcja E audytu eksperckiego (realne braki funkcji stacji): uziemienie/punkt neutralny, SPD, uklad pomiarowy, sekcjonowanie nN, dwusekcyjna stacja, praca rownolegla trafo, potrzeby wlasne. Rekonesans: model domenowy MA bogate pokrycie (GroundingConfig na trafo, SURGE_ARRESTER apparatus, Measurement CT/VT), ale kreator stacji ich NIE konfiguruje — wyspy. | Dyrektywa wlasciciela 2026-07-21; **kanon `docs/uiux/AUDYT_STACJA_BRAKI_MAX_2026-07.md`** (7 soczewek: pokrycie backendu + ryzyko wyspy per brak + plan fazowy G-STK-1..7); recon `enm/models.GroundingConfig`, `eligibility_service.has_grounding`, `proof_engine/packs/earthing_ground_fault_sn`, `fault_loop_iec60364` | **FLAGSHIP G-STK-1 (Fable osobiscie, zero wyspy):** uziemienie punktu neutralnego. Konsumenci JUZ istnieli (eligibility has_grounding, pakiet dowodowy earthing, field read model) — dobudowano TYLKO konfiguracje w kreatorze. Backend: `_build_neutral_grounding` + `_apply_station_neutral_grounding` czyta `payload.nn_earthing` (uklad TN/TT/IT + typ punktu neutralnego + R/X) → GroundingConfig na lv_neutral/hv_neutral + uklad na substation.meta; PARYTET insert/append; addytywne. UI: krok „Uziemienie i punkt neutralny" (uklad + punkt + rezystancja tylko impedancyjna + PanelTeorii PN-HD 60364-4-41). **ZERO fizyki w UI, ZERO fabrykacji** (R tylko gdy podane). Pozostale braki G-STK-2..7 (pomiary/potrzeby wlasne/petla z modelu/II sekcja/rownolegle trafo/SPD) = plan w dokumencie z ocena ryzyka wyspy (SPD warunkowy — konsument liczbowy niepewny). | `enm/domain_operations.py` (helpery + insert/append), `ui2/kreatory/stacja/**` (krok + model + strings + testy), `docs/uiux/AUDYT_STACJA_BRAKI_MAX_2026-07.md`. FROZEN Result API + solvery nietkniete; GroundingConfig to istniejacy kontrakt. | Testy realnej sciezki: append→GroundingConfig+uklad w meta; insert→dowod konsumenta (EnergyNetworkModel + has_grounding); brak bloku→unset; payload nn_earthing; R tylko impedancyjny; natywny wybor IT+izolowany w kroku uziemienia. Regresje: 63 domain + 55 stacja; guardy/ruff/black/type-check/lint zielone. | wysoki (flagship z 7-fazowego programu) |
| V12K-077 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Lec nastepnie przepytaj szerokie grono ekspertow o braki i poprawki i kod reviev").** Audyt ekspercki (7 soczewek, 3 recenzje read-only nad dorobkiem sesji: arc flash / KreatorStacji / protection I-t / WLS / SSCI). Wykryto defekt KRYTYCZNY K1/K2 (cichy blad danych: `append_station_on_endpoint` calkowicie ignorowal `nn_block` — gubione zrodlo OZE/odplywy nN/wylacznik glowny nN) + defekty WYSOKIE/SREDNIE warstwy interpretacji-prezentacji (W-1 clamp LTI bez sladu, P1 brak typu terminal, P2/P3 zgadywanie napiecia SN, S-1 bezzwloczna „brak danych", WLS-S1/S2 rezyduum ze sladu, WLS-S3/S4/D1, B1 SSCI bez UI, D-1 log10). | Dyrektywa wlasciciela 2026-07-21; **kanon `docs/uiux/AUDYT_EKSPERCKI_2026-07-21.md`** (znaleziska + dyspozycje NAPRAWA/KARTA, kazde zweryfikowane niezaleznie przez Fable); recon append↔insert (0 wystapien nn_block w append), `deriveSnVoltageKv`, `iec_curves`, `state_estimation_wls`, `arc_flash/builder` | **Swarm (K1) + Fable osobiscie (fizyka/phantom/quality):** K1/K2 parytet append↔insert (ekstrakcja `_build_nn_field_specs`/`_materialize_nn_source`, test reprodukujacy PV+LOAD). W-1 `MAX_TRIPPING_TIME_S` udokumentowany + `unclamped_tripping_time_s`/`clamp_applied` w sladzie WHITE BOX. S-1 bezzwloczna = podloga czasowa solvera (nie „brak danych"/t_s=0). WLS-S1/S2 addytywne `final_residuals` (r i r_N z tego samego iteratu, niezaleznie od sladu). WLS-S3 baner walidacji syntetycznej niezalezny od trybu. WLS-S4 reuzycie `przebiegRozplywuEstymacji`. WLS-D1 etykieta PL podejrzanego. B1 ekran SSCI `ui2/wyniki/ssci`. D-1 guard `log10`. **P1** typ `terminal` w kreatorze ui2 + domyslny terminal w append. **P2/P3** `deriveSnVoltageKv` z rzeczywistej szyny (odcinek/terminal), nieznane→pominiecie `sn_voltage_kv` (backend ustala z szyny), usuniety fallback 15 kV. **ZERO fizyki w UI, ZERO fabrykacji** (napiecie z szyny, TMS/energia z solvera). | (K1) `enm/domain_operations.py` + `tests/enm/test_append_station_on_endpoint.py`; (W-1/S-1/D-1) `protection/curves/iec_curves.py`, `application/protection_read_model.py`, `analysis/arc_flash/builder.py` + testy; (WLS) `network_model/solvers/state_estimation_wls.py`, `application/analyses/state_estimation/service.py`, `ui2/wyniki/estymacja/**` + testy; (SSCI) `ui2/wyniki/ssci/**`; (P1/P2/P3) `ui2/kreatory/stacja/**`, `ui/network-build/forms/InsertStationFormHelpers.ts` + testy. FROZEN API addytywnie (defaulty), determinizm zachowany. | Testy realnej sciezki: append PV+LOAD; instant bez zwloki→krzywa; WLS rezyduum bez sladu; SSCI 44; terminal→WE+TR; append→terminal; snVoltageKv=0→pominiecie. Regresje: protection/arc-flash/iec 132, WLS 37, estymacja 25, stacja 52, network-build 336; guardy (ui_no_physics/terminology/arch/trace_ui_leak/dead_click/dialog_completeness), ruff/black/type-check/lint zielone. Pozostaje KARTA: S-2/S-5/P4/P5 + realne braki funkcji stacji (uziemienie/SPD/pomiary/sekcjonowanie). | krytyczny→rozwiazany |
| V12K-076 | **ROZSTRZYGNIETY (2026-07-21, dyrektywa wlasciciela „Lec" — domkniecie luk ❌ zero-UI z inwentarza).** Dwie zdolnosci obliczeniowe mialy solver/analize, ale ZERO ekspozycji (API+UI): Estymacja stanu WLS (`state_estimation_wls.py`) i Stabilnosc SSCI (`analysis/ssci_stability` + v126 `_ssci_impedance`). Dodatkowo E-3: krzywe odwrotne I-t renderowaly „brak TMS" bo model ENM nie mial mnoznika czasowego. | Dyrektywa wlasciciela 2026-07-21; inwentarz `INWENTARZ_FUNKCJI_2026-07.md` (Audyt F: luki ❌); recon solverow + wzorca `quality_analysis_runs.py`/`v126_academic.py` | **Swarm (weryfikacja niezalezna Fable + cherry-pick + bramki + push):** (E-3) `ProtectionSetting.time_multiplier` addytywnie w ENM → read model liczy krzywe odwrotne solverem IEC 60255 z realnym TMS (brak TMS → uczciwy `missing_data`), panel I-t (E-4/E-5) renderuje pelne krzywe; UI edytora NIE fabrykowany (brak edytora nastaw = brak phantomu). (WLS) `POST /api/quality/state-estimation` + `/requirements` (Y-bus z grafu przez zamrozony builder → `estimate_wls`; bez pomiarow → 422, zero pseudo-pomiarow) + ekran `ui2/wyniki/estymacja` (wejscie pomiarow → |V|/kat/rezydua/χ²/LNR + WHITE BOX). (SSCI) `GET .../results/v126/ssci_impedance/stability` (lineage: przebiegi SSCI to run v126) + serwis werdyktu (max|L|/margines fazy/f winna/blyskosc −1 + WHITE BOX). **ZERO fizyki poza solverem, zero fabrykacji**. | (E-3) `enm/models.py`, `application/protection_read_model.py`, `frontend/types/enm.ts`, `ItCurvePanel.test`; (WLS) `application/analyses/state_estimation/**`, `api/quality_analysis_runs.py`, `ui2/wyniki/estymacja/**` + zakladka WynikiWarsztat; (SSCI) `application/analyses/ssci_stability/**`, `api/v126_academic.py`. FROZEN API addytywnie, determinizm, macierz API + inwentarz zaktualizowane. | Testy: E-3 protection-view+ItCurvePanel; WLS 34 quality API + 24 UI; SSCI 6 (realny solver D-03). Pelna regresja backendu **6372 passed** po zmianie ENM; pelna regresja frontendu (WLS UI nav). **Inwentarz: 0 funkcji ❌ zero-UI** — kazda zdolnosc backendu ma ≥◐ ujscie w UI. | wysoki |
| V12K-075 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela „arc flash (front potem) architektujesz kolejne kroki agent swarm" + „Lec").** Audyt D: kreator stacji SN/nN byl legacy god-file `InsertStationForm.tsx` (1884 w.) — bez PanelTeorii (must-have V12K-054/066), bez wspolnego wiazania V12K-073, monolit. Migracja do `ui2/kreatory/stacja` na wzorcu rama, fazowo, RÓWNOLEGLE do legacy (bez regresji przeplywu budowy), cutover na koncu. | Dyrektywa wlasciciela 2026-07-20; **kanon `docs/uiux/AUDYT_D_KREATOR_STACJI_2026-07.md`** (audyt + kontrakt + fazy D1-D5 + kryteria odbioru); recon powierzchni legacy + kontraktu operacji `insert_station_on_segment_sn`/`append_station_on_endpoint` | **Swarm (worktree, commit bez push; Fable weryfikuje niezaleznie + cherry-pick + bramki + push):** D2 rdzen (typ/umiejscowienie koniec-vs-podzial + transformator TRAFO_SN_NN + zapis + `useSelekcjaPoOperacji`), D3 rozdzielnica SN (producent/rodzina/kompletne szablony pol + podglad SVG + `sn_fields`), D4 blok nN + PV, **D4.1 pelny parytet nN 5/5** (LOAD/CUSTOM/PV/BESS/FW — dobor falownika per kind + feeder role + `namespaceZrodlaNn`), D5 cutover (Fable osobiscie). Payload `nn_block`/`sn_fields`/`station.switchgear` mapowany 1:1 na kontrakt backendu — **zero phantom**. **ZERO fizyki w UI** (dobor = porownanie napiec katalogowych; wynik z solvera). | `frontend/src/ui2/kreatory/stacja/**` (nowy kreator + testy), `ui/workspace/operationFormRegistry.tsx` + `ui/topology/modals/operationSurfaceRegistry.ts` (cutover na `KreatorStacjiSnNn`), USUNIETE `InsertStationForm.tsx` (1884 w.) + jego test; ZACHOWANE helpery `InsertStationFormHelpers`/`catalogPayload` (reuzywa KreatorStacji). Legacy/backend/`ui/sld/**` nietkniete do cutoveru. FROZEN API/semantyka podzialu/determinizm nietkniete. | Testy: `stacjaModel.test.ts` + `KreatorStacjiSnNn.test.tsx` (49, realna sciezka natywna per faza), registry-completeness/operationFormRegistry/coverage-matrix zielone (71), helpery legacy zachowane (50). Pelna regresja frontendu po cutoverze. Program D1-D5 zamkniety: god-file zastapiony kreatorem ui2 z pelnym parytetem + PanelTeorii + wiazanie. | wysoki |
| V12K-074 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „od scalenia watkow nie ma juz twarda granica miedzy watkami, jedziesz wszystko, all in max").** Audyt A2: analiza arc flash (IEEE 1584) liczyla per-szyna energie incydentu/AFB/kategorie SOI (endpoint `/api/quality/arc-flash`), ale BRAK bylo raportu eksportowalnego (JSON/tekst/PDF/DOCX/LaTeX) — luka „do ostatniego klika" (wynik analizy → dokument dla inzyniera/audytora). | Dyrektywa wlasciciela 2026-07-20; recon: `arc_flash/serializer.view_to_dict` (per szyna: incident_energy_cal_cm2/arc_flash_boundary_mm/ppe_category/voltage/arc_time/status/missing_data), wzorzec `analysis/reporting/audit2_report.py`, endpoint `build_arc_flash_view` | Backend: nowy `analysis/reporting/arc_flash_report.py` (`ArcFlashReportContext` + render json/text/pdf/docx/latex/all_formats) — deterministyczny (sort po bus_ref), polskie etykiety, uczciwy stan zerowy, podsumowanie najgorszego przypadku (max E) + rozklad kategorii SOI + braki danych. Endpointy: `POST /api/quality/arc-flash/report` (json/text/latex) + `.pdf` + `.docx` (Response z Content-Disposition). **ZERO fizyki**: raport TYLKO interpretuje policzony widok arc flash (solver liczy E/AFB/SOI). | `backend/src/analysis/reporting/arc_flash_report.py` (nowy), `backend/src/api/quality_analysis_runs.py` (3 endpointy + `ArcFlashReportZadanie` + `_arc_flash_report_context`). FROZEN Result API + solver arc flash nietkniete. Frontend arc flash surface = brak (osobna karta UI, do zaplanowania). | Testy: `test_arc_flash_report.py` (6: podsumowanie/najgorszy przypadek/rozklad SOI/sort determ./stan zerowy/latex escape/all-formats) + `test_quality_analysis_runs_api.py` +5 (report json/pdf/docx/determinizm/404). Arch guard (analiza bez fizyki), ruff/black/mojibake zielone; slice 35 + arc flash analysis 52 passed. | sredni |
| V12K-072 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela „Kontynuuj i nie zatrzymuj sie... przewiduj klik do przodu" — M3 programu magistrali, domkniecie).** Asystent doboru przekroju: interpretacja Iz (katalog) i ΔU (backend) vs kryteria + skumulowany spadek ciagu. | Program `docs/uiux/PROGRAM_MAGISTRALA_MAX_2026-07.md` (M3); recon podglad R1 (delta_u_pct), `iznamPrzekroczony` | Model `ocenaDoboru(params, ΔU%, prad, limit=5%)` → stany obciazalnosc (prad ≤ Iz) i spadek (ΔU ≤ limit) z komunikatem naprawczym; `lacznySpadekPct` (suma ΔU odcinkow, radialny ciag); `OdcinekBudowy.delta_u_pct` zapamietany przy dodaniu. UI: panel „Ocena doboru przekroju" (obciazalnosc/spadek + wskazowka Ith ≥ Ik·√tk) + skumulowany ΔU w „Magistrala w budowie" z ostrzezeniem po przekroczeniu limitu. **ZERO fizyki**: UI porownuje policzone przez solver ΔU i katalogowe Iz z kryteriami, nic nie liczy. | `ui2/kreatory/magistrala/magistralaModel.ts` (ocenaDoboru/lacznySpadekPct/LIMIT_SPADKU_PCT/OdcinekBudowy.delta_u_pct), `strings.ts` (ocena), `KreatorMagistralaSn.tsx` (ocena panel + skumulowany). FROZEN API/backend/ui/sld/** nietkniete. | Testy: `magistralaModel` +4 (obciazalnosc ok/ostrzezenie/brak, spadek vs limit, skumulowany spadek, zwracane wartosci). Guardy/type-check/lint zielone. Regresja frontendu pelna. Zrzut: asystent lapie prad 300 A > Iz 255 A. **Program MAGISTRALA MAX zamkniety: M1 (V12K-070) + M2 (V12K-071) + M3 (V12K-072) — wszystkie 3 dyrektywy IMG_8271.** | wysoki |
| V12K-071 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela „Lec i rozwin skrzydla" — M2 programu magistrali).** Kreator budowal POJEDYNCZY odcinek i zamykal sie (lancuchowanie „Kolejny odcinek" przez close→reopen modalu); brak spojnego buildera realnej sieci (dyrektywa #3 z IMG_8271). | Program `docs/uiux/PROGRAM_MAGISTRALA_MAX_2026-07.md` (M2); recon `onZapisz`, `trunkContinuation` (buildNextContext/createdEndpointBusRef juz wspieraly kontynuacje z konca) | Builder realnej sieci: „Kolejny odcinek" (next_step='continue') NIE zamyka okna — koniec dodanego odcinka staje sie startem nastepnego (`kontekstKontynuacji` w stanie `nadpisanyKontekst`), formularz resetuje dlugosc/nazwe (typ zostaje), krok=parametry. Panel „Magistrala w budowie" (aside) pokazuje rosnaca liste odcinkow (rodzaj·typ·przekroj·dlugosc) + laczna dlugosc. Akcja glowna „Dodaj odcinek"; „Anuluj" po 1. odcinku → „Zakoncz budowe" (close+SLD). Elementy na koncu (stacja/ZK/slup) nadal lancuchuja realna operacje. Kazdy odcinek = realna `continue_trunk_segment_sn` w modelu (bez shadow-modelu, bez zmiany kontraktow FROZEN). | `ui2/kreatory/magistrala/magistralaModel.ts` (OdcinekBudowy/podsumujOdcinek/lacznaDlugosc/kontekstKontynuacji/fmtDlugosc), `strings.ts` (builder), `KreatorMagistralaSn.tsx` (stan buildera + onZapisz stay-open + panel + akcje). ZERO fizyki w UI; FROZEN API/backend/ui/sld/** nietkniete. | Testy: `magistralaModel` +4 (podsumowanie/laczna/format/kontekst kontynuacji), `KreatorMagistralaSn` +2 (real path: „Kolejny odcinek" NIE zamyka + drugi odcinek from_terminal_id=koniec; „Zakoncz budowe" zamyka+SLD). Guardy/type-check/lint zielone. Regresja frontendu pelna. Zrzut z panelem „Magistrala w budowie". **Pozostaje M3 (asystent doboru przekroju: Iz/ΔU/Ith).** | wysoki |
| V12K-070 | **W REALIZACJI (2026-07-20, dyrektywa wlasciciela, zrzut IMG_8271: „magistrale potrzebuja rozbudowy o wykorzystanie katalogow bazy typow kabli i przekrojow; przetestuj wszystkie parametry normowe do obliczen w zaleznosci od kabel czy napowietrzna; kreator musi umozliwiac budowanie realnej sieci a nie pojedynczego odcinka; dodaj do kanonu, opcja MAX").** Audyt wielosoczewkowy: BACKEND kompletny i normowo rozniceujacy (`_apply_materialized_branch_fields`: kabel c_nf→b_siemens=2πf·C, linia b_us, skladowa zerowa r0/x0/b0, przekroj + zyla powrotna Jth/Ith zwarcia doziemnego, liczba zyl, material). BRAKI frontowe: (GAP-MAG-1) `parametryZKatalogu` gubil c_nf/b_us/przekroj/material/izolacje/norme/temp/Ith — podglad pokazywal 3 pola (R/X/Iznam); (GAP-MAG-2) brak jawnego doboru przekroju; (GAP-MAG-3) pojedynczy odcinek zamiast buildera realnej sieci. | Dyrektywa wlasciciela 2026-07-20; zrzut IMG_8271; recon `CableType`/`LineType`, `_apply_materialized_branch_fields`, `continue_trunk_segment_sn`, `magistralaModel.parametryZKatalogu` | **Kanon: `docs/uiux/PROGRAM_MAGISTRALA_MAX_2026-07.md`** (audyt + tabela parametrow normowych kabel/napowietrzna + fazy M1-M3). **M1 wdrozone (ta runda):** `ParametryOdcinka` niesie WSZYSTKIE pola normowe per typ (kabel: c_nf/izolacja/Ith zyly powrotnej/temp/material/przekroj; linia: b_us/przekroj/material/temp — bez zyly powrotnej); `paramReadout` pokazuje sekcje „Parametry normowe do obliczen" warunkowo per rodzaj; panel teorii rozbudowany o roznice kabel↔napowietrzna (C vs B, zyla powrotna, izolacja/temp). Harness + zrzut z realnym kablem. **M2 (builder wielo-odcinkowy) i M3 (asystent doboru przekroju) = kolejne rundy (zapisane w programie).** | `docs/uiux/PROGRAM_MAGISTRALA_MAX_2026-07.md` (kanon), `ui2/kreatory/magistrala/magistralaModel.ts|strings.ts|KreatorMagistralaSn.tsx`, `creator-harness-main.tsx` (fixtury cable/line), `e2e/creator-screenshot.spec.ts`. **ZERO fizyki w UI** (wartosci z katalogu, wynik z solvera); FROZEN API/backend/ui/sld/** nietkniete. | Testy: `magistralaModel` +2 (kabel niesie C/izolacja/Ith bez B; linia niesie B bez C/izolacji/zyly powrotnej). Backend juz pokryty (`test_mv_cable_line_catalog`, `test_catalog_materialization_persistence`). Guardy zielone; type-check+lint zielone. Regresja frontendu pelna. Zrzut magistrali z parametrami normowymi kabla. | wysoki |
| V12K-069 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela „Kontynuuj" — domkniecie rolloutu wykresow).** Ostatnie dwa wykresy „(plan)": odbior (trojkat mocy) i GPZ (sztywnosc sieci) na wspolnej bazie. | Standard `docs/uiux/STANDARD_PANEL_TEORII_2026-07.md`; baza `rama/wykresPomoc`; modele odbioru (cos_phi) i GPZ (Sk″/RX) | `odbior/WykresTrojkatMocy.tsx`: trojkat mocy S/P/Q (S=1) parametryzowany ZYWO przez cos_phi — nizszy cosφ → dluzsza przyprostokatna Q (Q=P·tan(arccos cosφ)). `zrodlo/WykresSztywnosci.tsx`: hiperbola Z/Zn=Skn/Sk″ z punktem odniesienia (1;1) — ilustruje sztywnosc sieci (wieksze Sk″ → mniejsze Z, wyzsze prady zwarciowe). Oba wpiete jako children PanelTeorii. Harness rozszerzony o odbior (zrzut) i GPZ (dispatch). **ZERO fizyki sieci**: wykresy = ksztalt prawa/trend wg nastaw; rzeczywiste P/Q/Ik″/Z liczy solver (rozplyw / IEC 60909). | `ui2/kreatory/odbior/WykresTrojkatMocy.tsx|strings|Kreator`, `zrodlo/WykresSztywnosci.tsx|strings|Kreator`, `creator-harness-main.tsx`, `e2e/creator-screenshot.spec.ts`, docs. FROZEN API/backend/ui/sld/** nietkniete. | Testy: `WykresTrojkatMocy` +2 (trzy boki+punkt; nizszy cosφ→wieksze Q), `WykresSztywnosci` +1 (hiperbola malejaca). Guardy zielone; type-check+lint zielone. Regresja frontendu pelna. Zrzut odbioru oba motywy. **Dlug: zrzut GPZ w harnessie wymaga pelniejszego kontekstu 7-krokowego kreatora — wpis w backlogu standardu (WykresSztywnosci pokryty testem jedn.).** Domkniecie rolloutu wykresow (6/6 kreatorow z charakterystyka). | wysoki |
| V12K-068 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela „Kontynuuj" — rollout wykresow wg standardu).** Kolejne zywe wykresy teorii na wspolnej bazie (V12K-067): kompensator i magistrala. | Standard `docs/uiux/STANDARD_PANEL_TEORII_2026-07.md` (depth backlog); baza `rama/wykresPomoc`; modele kompensatora (Q katalogowe) i magistrali (cos_phi, dlugosc_m) | `kompensator/WykresQU.tsx`: parabola Q/Qn=(U/Un)² z punktem znamionowym (1;1) — ilustruje Q∝U² (bateria oddaje mniej Q przy nizszym napieciu). `magistrala/WykresSpadku.tsx`: poglądowy profil napiecia U(x) od zrodla (1,0 pu) do konca odcinka + limit dopuszczalny + pasmo poza zakresem; nachylenie zalezne ZYWO od cos_phi (nizszy cosφ → stromszy spadek, wieksza skladowa bierna na reaktancji; skala poglądowa dla R≈X). Oba wpiete jako children PanelTeorii (krok typ/parametry). Harness + screenshot spec rozszerzone o kompensator+magistrala (kontekst: bus_ref, from_terminal_id, voltage_kv, length_m). **ZERO fizyki sieci**: wykresy = ksztalt prawa/trend wg nastaw, NIE wynik solvera; rzeczywiste Q/ΔU liczy rozplyw. | `ui2/kreatory/kompensator/WykresQU.tsx|strings|Kreator`, `magistrala/WykresSpadku.tsx|strings|Kreator`, `creator-harness-main.tsx`, `e2e/creator-screenshot.spec.ts`, docs. FROZEN API/backend/ui/sld/** nietkniete. | Testy: `WykresQU` +1, `WykresSpadku` +2 (profil+limit+pasmo; nizszy cosφ→glebszy spadek). Guardy ui_no_physics/terminology/forbidden/utf8/docs/canon zielone; type-check+lint zielone. Regresja frontendu pelna. Zrzuty oba motywy: `docs/audit/visual/kreatory/kreator_{kompensator,magistrala}_teoria_*`. | wysoki |
| V12K-067 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela „Kontynuuj" — pogłebienie standardu V12K-066).** Rollout wg standardu PanelTeorii: (a) wspolna baza wykresow dla wszystkich kreatorow (zamiast bespoke per-kreator), (b) najbogatszy inzyniersko kreator transformatora dostaje zywy wykres charakterystyki AVR + pelne pokrycie krokow. | Standard `docs/uiux/STANDARD_PANEL_TEORII_2026-07.md`; wzorzec `WykresyNcRfg` (OZE); model transformatora (tap_min/max/neutral, step_percent, setpoint_kv, deadband_kv, control_mode) | Nowa wspoldzielona baza wykresow `rama/wykresPomoc.tsx` (VBW/VBH/PAD/px/py/`RamkaWykresu`) + `rama/wykresy.css` (klasy `mvd-wykres-*`) — jeden uklad wspolrzednych/rama/arkusz stylow dla WSZYSTKICH wykresow. OZE `WykresyNcRfg` zmigrowany na baze (usuniete lokalne px/py/Ramka + `wykresyNcRfg.css`; klasy `mvd-ncrfg-*`→`mvd-wykres-*`). Nowy `transformator/WykresAvr.tsx`: charakterystyka regulacji — kropki napiecia szyny per zaczep (odstep = krok zaczepu) + pasmo nieczulosci wokol napiecia zadanego (tylko OLTC+AVR); wizualizuje wymog „pasmo szersze niz skok zaczepu". Panel teorii transformatora dla kroku szyny (przekladnia/uk/grupa polaczen) + regulacji (z wykresem). Panele zwiniete domyslnie. **ZERO fizyki sieci** (ksztalt specyfikacji regulacji, nie wynik solvera; rzeczywiste napiecie/zaczep liczy petla OLTC rozplywu). | `ui2/kreatory/rama/wykresPomoc.tsx|wykresy.css`, `rama/index.ts`, `zrodlo-oze/WykresyNcRfg` (migracja), usuniety `wykresyNcRfg.css`, `transformator/WykresAvr.tsx|strings|Kreator`, `creator-harness-main.tsx` (+transformator), `e2e/creator-screenshot.spec.ts` (+transformator), docs. FROZEN API/backend/ui/sld/** nietkniete. | Testy: `WykresAvr` +4 (kropki per zaczep, pasmo tylko przy AUTO, przerzedzanie), `rama` (PanelTeorii) bez regresji, OZE bez regresji po migracji bazy. Guardy ui_no_physics/terminology/forbidden/utf8/docs/canon zielone; type-check+lint zielone. Regresja frontendu pelna. Zrzuty AVR (oba motywy): `docs/audit/visual/kreatory/kreator_transformator_regulacja_*`. | wysoki |
| V12K-066 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „tworzymy teorie do kazdego panelu konfiguracji, dodaj do must-have, zaprojektuj i dodaj do kazdego nastepnego kroku oraz wszystkich dotychczas").** Panel teorii NC RfG (V12K-065) mial trafic do KAZDEGO kreatora ui2 jako standard „must-have", nie tylko OZE. | Dyrektywa wlasciciela 2026-07-20; wzorzec `zrodlo-oze/WykresyNcRfg` (panel + wykresy); inwentarz 9 kreatorow ui2 (kompensator/lacznik/magistrala/odbior/pierscien/pole/transformator/zrodlo/zrodlo-oze) | Nowy wspoldzielony prymityw `rama/PanelTeorii` (uogolnienie z panelu NC RfG — reuse, nie duplikacja): rozwijany `<details>`, propsy tytul/opis/wymog/wymogPrefix/podstawa/domyslnieOtwarty/children(wykres)/testid; style `rama/panelTeorii.css` (tokeny --mvd-*, theme-aware). OZE zrefaktoryzowany na PanelTeorii (charakterystyki SVG jako children) + panele teorii dla krokow 1 (technologia) i 2 (falownik). Panele teorii dodane do WSZYSTKICH 9 kreatorow (transformator: regulacja/OLTC; kompensator: Q∝U²; lacznik: NOP/sekcja; magistrala: ΔU/straty; odbior: cosφ/ZIP; pierscien: NOP; pole: sklad aparatury/IEC 62271; zrodlo/GPZ: moc zwarciowa/IEC 60909). Tresc: po co / z czego / co daje + wymog normy + podstawa. Kontrakt „must-have" spisany w `docs/uiux/STANDARD_PANEL_TEORII_2026-07.md` (rejestr pokrycia + depth backlog jawny — depth pozostalych krokow GPZ/transformatora i wykresy „(plan)" jako kolejne rundy, NIE odkladane cicho). | `ui2/kreatory/rama/panelTeorii.tsx|css`, `rama/index.ts`, `zrodlo-oze/WykresyNcRfg` (refaktor na PanelTeorii), `wykresyNcRfg.css` (trim do elementow SVG), 9× `Kreator*.tsx` + `strings.ts`, `docs/uiux/STANDARD_PANEL_TEORII_2026-07.md`. **ZERO fizyki sieci** (panel = teoria + ksztalt specyfikacji, nie wynik solvera); FROZEN API / backend / ui/sld/** nietkniete. | Testy: `rama` +2 (PanelTeorii renderuje/rozwijalnosc/pomija puste bloki), OZE panel bez regresji. Guardy: ui_no_physics/ui_terminology/forbidden/utf8 zielone; type-check + lint zielone. Regresja frontendu pelna. Zrzuty: `docs/audit/visual/kreatory`. | wysoki |
| V12K-065 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „dodaj pole z pomocy z pelnym wytlumaczeniem teorii i wykresami z NC RfG, wymysl jak to intuicyjnie zintegrowac").** G-OZE-B5: krok „Regulacja" kreatora OZE mial opisy pol, ale brak calosciowego wykladu teorii NC RfG i wizualizacji charakterystyk — projektant nie widzial ksztaltu prawa sterowania, ktore konfiguruje. | Dyrektywa wlasciciela 2026-07-20; recon `ui_no_physics_guard` (blokuje TYLKO fizyke sieci: √3/impedancja/dUdP — nie ksztalt charakterystyki sterowania), istniejacy precedens wykresu OZE `ui2/oze/zdolnosc/WykresZdolnosciChart` (Recharts, tokeny --mvd-*, determinizm) | Nowy komponent `WykresyNcRfg.tsx`: kontekstowy, rozwijany (`<details>`) panel „Teoria i charakterystyka NC RfG" wpiety w sekcje Regulacja. Zawiera: (a) pelny wyklad teorii per tryb (cosφ/Q(U)/P(f)-LFSM/bez regulacji) — po co, wymog NC RfG (typ B/C/D), jak czytac; (b) ZYWE wykresy SVG parametryzowane biezacymi nastawami — Q(U) volt-var (pasmo nieczulosci + ramiona statyzmu + plateau Qmin/Qmax), P(f)/LFSM (pasmo + opadajace ramie LFSM-O z progiem 50.2 Hz + rosnace LFSM-U dla BESS), cosφ (linia robocza Q=P·tan(arccos cosφ) + klin zdolnosci ±0,95); (c) podstawa prawna (Rozporzadzenie UE 2016/631 + PTPiREE). Krzywa pusta → linia przerywana z wartosciami sugerowanymi (dydaktyka przed wpisem). **ZERO fizyki sieci:** wykres = ksztalt SPECYFIKACJI prawa sterowania (nastawy uzytkownika + normatywny ksztalt), NIE wynik solvera; rzeczywiste Q/redukcja P w punkcie pracy z kanonicznego PF. Kolory tokeny --mvd-* (theme-aware light/dark), deterministyczne (stale wymiary, brak animacji/losowosci), role=img+aria-label. | `ui2/kreatory/zrodlo-oze/WykresyNcRfg.tsx` (nowy), `wykresyNcRfg.css` (nowy), `strings.ts` (NCRFG_TEORIA + teksty teorii), `KreatorZrodlaOze.tsx` (wpiecie panelu). Bez zmian backendu, FROZEN API, ui/sld/**. | Guardy: `ui_no_physics_guard` PASS (charakterystyka sterowania ≠ fizyka sieci), `ui_terminology`/`forbidden_ui_terms`/`utf8` zielone, type-check + lint zielone. Test: `KreatorZrodlaOze` (panel obecny + wykres SVG w kroku Regulacja). Regresja frontendu pelna. Zrzuty oba motywy: `docs/audit/visual/kreatory/kreator_oze_krok3_*`. | wysoki |
| V12K-064 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „po kazdej rundzie kod review i audyt zespolu ekspertow i poprawki end to end").** Runda przegladowa po V12K-063. **Code review** (2 znaleziska): (1) brak walidacji zakresu cosφ (0;1] — solver klampuje cosφ>1 do 1.0 (pasywne), wiec kontrolka bez walidacji przyjmowala wartosc cicho neutralizowana (phantom czesciowy); (2) drobna niespojnosc: pole pasma P(f) widoczne przy statyzmie=0, a payload je pomijal. **Audyt ekspercki** (soczewka przylaczeniowca/NC RfG): forward-phantom w trybie Q(U) — solver `q_from_voltage` konsumuje napieciowe pasmo nieczulosci `qu_deadband_low_pu`/`qu_deadband_high_pu` (zakres Q=0), ale ani op ani UI go nie ustawialy → domyslny punkt 1.0/1.0 (reakcja natychmiastowa), niezgodny z charakterystyka NC RfG Q(U) (pasmo np. 0.95–1.05 pu). Ta sama klasa defektu co pasmo P(f) z V12K-063. | Code review HEAD~2..HEAD; recon `power_flow_inverter.q_from_voltage` (l.96–116), `inverter_control_from_params` (l.297–298 default 1.0/1.0), `_build_converter_control_by_node` (nie ustawial pasma), walidacja mappera (low>high → ValueError) | Poprawki end-to-end: (1) walidacja cosφ∈(0;1] + pole `max={1}` (dodano prop `max` do wspoldzielonego `PoleLiczbowe` — addytywnie, reuse); (2) widocznosc pola pasma P(f) zgodna z bramka payloadu (statyzm truthy); (3) napieciowe pasmo Q(U) end-to-end: payload `qu_deadband_low_pu`/`qu_deadband_high_pu`, utrwalanie na meta (PV/BESS/FW), wpiecie w `_build_converter_control_by_node` (przy aktywnym Q(U)), 2 pola UI (dolne/gorne, sugerowane 0.95/1.05 pu) + opis inzynierski, walidacja low≤high (solver odrzuca low>high). | `enm/domain_ops_models.py`, `enm/domain_operations_v2.py`, `enm/canonical_analysis.py`, `ui2/kreatory/rama/pola.tsx` (prop max), `ui2/kreatory/zrodlo-oze/**`. FROZEN Result API + ui/sld/** + station-der nietkniete; pola addytywne, brak pasma → domyslny punkt 1.0/1.0 (determinizm). | Testy: `zrodloOzeModel` +2 (walidacja cosφ + pasmo Q(U) low>high), payload Q(U) deadband; `KreatorZrodlaOze` (pola pasma w trybie Q(U)); backend `test_qu_deadband_reaches_control_from_meta` + `_defaults_to_point_without_meta` + persist na meta. Regresje: backend enm 708 + slice converter/inverter/PF 782 passed; frontend pełna. | wysoki |
| V12K-063 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „Rozbuduj opisy i wyjasnienia dla kazdej funkcji daj sugerowane nastawy pomysl co jeszcze brakuje").** G-OZE-B3: rozbudowa opisow/podpowiedzi kroku „Regulacja" kreatora OZE ujawnila PHANTOM — model frontendu NIGDY nie wysylal `cos_phi` (tryb STALY_COS_PHI) ani `qu_slope_pu_per_pu` (tryb Q_OD_U), mimo ze kanoniczny PF falownika je czyta (V12K-052). Wybor trybu regulacji Q byl wiec bez wartosci rzadzacej = zrodlo pracowalo pasywnie (Q=0), a wybor trybu nie wplywal na rozplyw. Dodatkowo pasmo nieczulosci P(f)/LFSM (`lfsm_deadband_hz`) nie mialo dostawcy w op. | Dyrektywa wlasciciela 2026-07-20; recon: `zbudujPayload` (brak cos_phi/qu_slope w payloadzie), `_build_converter_control_by_node` (czyta cos_phi/qu_slope_pu_per_pu/lfsm_deadband_hz z meta), `inverter_control_from_params` (deadband LFSM juz wspierany) | Frontend: pola rzadzace trybem Q wystawione WARUNKOWO (cosφ tylko dla STALY_COS_PHI, nachylenie Q(U) tylko dla Q_OD_U — zero fabrykacji), `zbudujPayload` wysyla je tylko dla wlasciwego trybu; pole „Pasmo nieczulosci P(f)" pojawia sie po podaniu statyzmu; ostrzezenie gotowosci „tryb pasywny" gdy tryb Q bez wartosci rzadzacej (`trybQWymagaWartosci`); sugerowane nastawy (SUGEROWANE: cosφ 0,95, Q(U) 4, statyzm 5%, pasmo 0,2 Hz) jako placeholdery + rozbudowane opisy inzynierskie (po co/z czego/co daje, odwolania NC RfG). Backend: `AddConverterSourcePayload` + 3 bloki meta generatora (PV/BESS/FW) utrwalaja `qu_slope_pu_per_pu`+`lfsm_deadband_hz`; `_build_converter_control_by_node` wpina deadband do params LFSM. | `ui2/kreatory/zrodlo-oze/**` (zrodloOzeModel/strings/KreatorZrodlaOze), `enm/domain_ops_models.py`, `enm/domain_operations_v2.py`, `enm/canonical_analysis.py`. FROZEN Result API + ui/sld/** + station-der nietkniete; pola addytywne (exclude_none, seed bez zmian dla istniejacych payloadow). | Testy: `zrodloOzeModel` +3 (cos_phi/qu_slope per tryb, deadband tylko przy statyzmie, trybQWymagaWartosci) + `KreatorZrodlaOze` +1 (realna sciezka: cosφ↔Q(U) po zmianie trybu, deadband po statyzmie) + backend `test_add_converter_source_persists_reactive_regulation_on_generator_meta`. Regresje: frontend 9049 passed, backend enm 706 passed; determinizm przy 50 Hz. | wysoki |
| V12K-061 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „C wersja maksymalna przebudowa").** Audyt poz. C: kreator OZE `add_converter_source` byl legacy `AddConverterSourceForm` (1365 w.) — fizyka regulacji domknieta (G-OZE-PF/V12K-052), ale UI poza kanonem FLOW. Przebudowa opcja MAX na ui2 (poprzedzona audytem eksperckim V12K-051, zweryfikowanym wzgledem kodu: realne pola op vs konsumenci). | Dyrektywa wlasciciela 2026-07-20; `docs/uiux/AUDYT_OZE_DER_MAX_2026-07.md` (§4 projekt, §5 fazy); recon `AddConverterSourcePayload` (pola realnie utrwalane), ConverterType katalog + PTPiREE, block-transformer ze snapshotu, NcRfgPtpireeModuleInput (z body, nie z modelu) | Kreator ui2 `KreatorZrodlaOze` (kreatory/rama, 4 kroki) zastepuje legacy. Pokrycie WSZYSTKICH utrwalanych pol (1:1): technologia PV/BESS/FW, wariant nn_side/block_transformer + picker TR blokowego, umiejscowienie NEW/EXISTING + aparat nN, katalog falownika (ZRODLO_NN_PV/BESS/CONVERTER) + liczba + tabliczka, tryb regulacji (STALY_COS_PHI/Q_OD_U/P_OD_U/WYLACZONE — realnie w PF), Q min/max, moc robocza P, tryb BESS + SOC, PTPiREE z katalogu → materialized_params. Downstream (machine SC/inverter PF/NC RfG/grid strength/reactive adequacy). Zero-fabrykacja: pola NC RfG (krzywe/flagi) NIE utrwalane przez op → **G-OZE-B karta** (lancuch backendowy: op persist + runner NC RfG czyta z modelu), nie phantom UI. | `ui2/kreatory/zrodlo-oze/**`, `operationFormRegistry`, `operationSurfaceRegistry`, `forms/index.ts`. Retire `AddConverterSourceForm` + test. Bez edycji backendu i ui/sld/**. | Testy: `zrodloOzeModel` (13: nazewnictwo/namespace/walidacja/payload nn_side+block+BESS/materialized) + `KreatorZrodlaOze` (3, realna sciezka) + operationFormRegistry (11) + modalRegistryCompleteness (8). Type-check + guardy UI zielone. Pelna regresja frontendu. | wysoki |
| V12K-059 | **W REALIZACJI (2026-07-20, dyrektywa wlasciciela: „wymagany audyt powykonawczy zespolu ekspertow — czego brakuje, co nie powiazane miedzy modulami i warstwami; audyt globalny, najpierw uwagi potem poprawki").** Audyt globalny wielosoczewkowy (rekonesans kodu, nie z pamieci). Znaleziska: **A** [KRYTYCZNE] Arc Flash = kompletna wyspa (IEEE 1584-2018 gotowy, 0 uzyc symboli publicznych poza modulem — brak dispatch/API/UI/raport); **B** [WYSOKIE] pole SN → materializacja zabezpieczen niedomknieta (G-POLE-R zwiazal refy danych, ale add_sn_bay nie wyprowadzal protection_codes z szablonu/roli); **C** [WYSOKIE] kreator OZE `add_converter_source` wciaz legacy (fizyka PF domknieta w V12K-052, UI regulacji poza kanonem FLOW); **D** [SREDNIE] 11/20 operacji poza kanonem kreatorow ui2; **E** [SREDNIE, do weryfikacji] protection_ref pola moze nie wplywac na koordynacje I-t; **F** [NISKIE] rozjazd inwentarz vs kod. | Dyrektywa wlasciciela 2026-07-20; `docs/uiux/AUDYT_GLOBALNY_POWYKONAWCZY_2026-07.md` (binding); rekonesans: uzycia symboli arc_flash (0), field_read_model/Bay.protection_codes, station_templates protection derivation, operationFormRegistry legacy providers | **Poz. B ZREALIZOWANA W PELNI (ta runda, rozbudowa na zadanie wlasciciela „musisz rozbudowac jeszcze najpierw B"):** `_resolve_bay_template_protection_codes(manufacturer, template, role)` — reuzycie kanonicznego resolvera Reference Engine; kolejnosc zero-fabrykacja: `template.protection_requirements` (dane producenckie, pierwszenstwo) → kanoniczna tablica per rola `BAY_PROTECTION_CODES_BY_ROLE` (PTPiREE/IRiESD + IEC 60255) dla WSZYSTKICH rol: IN 51/50/51N, OUT/FEEDER 51/50/51N/67N, TR (reuzycie TRANSFORMER_BAY_PROTECTION_CODES), COUPLER 51/50, OZE (reuzycie interfejsu NC RfG OZE_INTERFACE_BAY_PROTECTION_CODES), MEASUREMENT puste (uczciwy brak). `add_sn_bay` zapisuje `protection_codes` na field_spec; `field_read_model` projektuje na `Bay.protection_codes`; frontend glify SLD v2 juz konsumuja `bay.protectionCodes`. **Poz. A ZREALIZOWANA (ta runda):** wyspa Arc Flash (IEEE 1584-2018) wpieta end-to-end — `application/analyses/arc_flash_view.py::build_arc_flash_view` (mapowanie SC run→ArcFlashInput, ZERO fizyki) → `POST /api/quality/arc-flash` → sekcja UI `SekcjaArcFlash` w oknie „Jakosc wynikow" (formularz parametrow projektowych → przelicz → tabela energii/AFB/PPE + szczegol z proweniencja + slad WHITE BOX ekspercki). Brak wejscia projektowego ⇒ uczciwe „dane niekompletne". Testy: backend 6 (API), frontend 5 (realna sciezka). **Poz. B2 zwiniete do B (nie karta). Poz. A2 (raport PDF/DOCX Arc Flash), C/D/E/F = karty planu (§3 audytu), nie odkladane cicho.** | `enm/domain_operations.py` (_build_field_spec), `enm/domain_operations_v2.py` (resolver + add_sn_bay), `network_model/catalog/bay_templates.py` (BAY_PROTECTION_CODES_BY_ROLE + OZE_INTERFACE), `application/field_read_model.py`. FROZEN Result API + ui/sld/** nietkniete. | Poz. B: `test_add_sn_bay_tr_materializes_protection_codes` + `_feeder_role_materializes_canonical` + `_oze_role_materializes_nc_rfg_interface` + backward-compat MEASUREMENT (7/7 sn_bay). Pelne regresje backend+frontend. Poz. A/C/D/E/F: kolejne rundy. | wysoki |
| V12K-058 | **ROZSTRZYGNIETY (2026-07-20, dyrektywa wlasciciela: „G-POLE musisz zrobic ponowny audyt end-to-end i nie tworzyc rownoleglych sciezek — pole SN musi byc powiazane z tym co juz mamy, czyli szablonami pol producentow; przeprojektuj to globalnie i powiaz wielowarstwowo").** G-POLE-R: audyt wykryl, ze G-POLE utworzyl RÓWNOLEGLĄ, plytka sciezke (surowy aparat + bay_role) ignorujaca istniejaca infrastrukture szablonow pol producentow (Reference Engine: SwitchgearFamily/CompleteMvBayTemplate, pickery, `_build_field_spec` z protection_ref/bay_template_ref/switchgear_family_ref/manufacturer_ref). `add_sn_bay` JUZ uzywal `_build_field_spec`, ale operacja/payload nie przenosily referencji producenckich — powiazanie bylo martwe (backend-capability bez punktu wejscia = defekt, „no islands"). | Dyrektywa wlasciciela 2026-07-20; `docs/uiux/AUDYT_POLE_SN_SZABLONY_2026-07.md` (binding); recon `_build_field_spec` (protection_ref/bay_template_ref/switchgear_family_ref/manufacturer_ref), `fetchSwitchgearFamilies`/`fetchCompleteBayTemplates`, `SwitchgearFamily`/`CompleteMvBayTemplateSummary`, BayKind↔bay_role | Backend: `AddSnBayPayload` + `add_sn_bay` przenosza `switchgear_family_ref`/`bay_template_ref`/`manufacturer_ref`/`protection_ref` do `_build_field_spec` (addytywnie, tylko truthy → `producer_refs`; wsteczna zgodnosc: brak → jak dotad). Frontend: `KreatorPolaSn` reuzywa Reference Engine — `bayKindZRoli(rola)` mapuje role na BayKind, `fetchSwitchgearFamilies` (rodzina→producent) + `fetchCompleteBayTemplates(manufacturer, bayKind)` (szablon pola); wybor rodziny ustawia producenta i filtruje szablony; wiersz gotowosci „Szablon" (zalecany/powiazany); sekcja „Aparat glowny (konfiguracja ekspercka)" jako fallback gdy brak szablonu. Zero rownoleglej sciezki — jedna warstwa pol producenckich. | `enm/domain_ops_models.py`, `enm/domain_operations_v2.py`, `types/domainOps.ts`, `ui2/kreatory/pole/**` (polaSnModel/KreatorPolaSn/strings + testy). Bez edycji ui/sld/**; FROZEN Result API nietkniety. | Testy: backend `test_add_sn_bay_binds_producer_bay_template` + `..._stays_backward_compatible` (2, 4/4 sn_bay); frontend polaSnModel (11: bayKindZRoli/maSzablonProducenta/refy addytywne) + KreatorPolaSn (4, realna sciezka: rodzina→BayKind→szablon→payload). Type-check + guardy UI zielone. Pelne regresje backend+frontend. | wysoki |
| V12K-057 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „kontynuuj").** G-POLE: legacy `AddSnBayForm` (491 w., self-contained, bez sprzezenia SLD-nav) przebudowany na kreator ui2. Kolejne ogniwo programu przebudowy kreatorow (rekomendacja z chartera V12K-056: wysoka czestosc, czysty port). | Dyrektywa wlasciciela 2026-07-19; `KREATORY_STANDARD_2026-07.md`; recon add_sn_bay (bus_ref/station_ref z kontekstu przez enmResolvers, bay_role, apparatus_kind, APARAT_SN), MVApparatusCatalogType | Kreator ui2 `KreatorPolaSn` (kreatory/rama, 2 kroki: rola+aparat+katalog+nazwa → podsumowanie/zapis): rola pola (IN/OUT/FEEDER/TR/COUPLER/MEASUREMENT/OZE), rodzaj aparatu (BREAKER/DISCONNECTOR/LOAD_SWITCH/MEASUREMENT), katalog-first APARAT_SN (fetchMvApparatusTypes), nazwa, uczciwy stan zerowy (brak szyny/stacji), sekcja downstream (topologia pola, SLD, zabezpieczenia polowe). Kontrakt payloadu 1:1 z legacy. Retire AddSnBayForm + AddSnBayForm.test (intencja resolvera juz w enmResolvers.test; payload/realna sciezka w polaSnModel.test + KreatorPolaSn.test). | `ui2/kreatory/pole/**`, `operationFormRegistry`, `operationSurfaceRegistry`, `forms/index.ts`, `modalRegistryCompleteness.test`. Bez edycji ui/sld/**. | Testy: polaSnModel (5) + KreatorPolaSn (3, realna sciezka) + completeness (8) + registry (11). Type-check + guardy UI zielone. Pelna regresja frontendu. | sredni |
| V12K-056 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „kontynuuj").** G-RING: legacy `ConnectRingForm` (142 w., wrapper nad RingCloseModal+NOPModal, scada-style) przebudowany na kreator ui2. Kontynuacja programu przebudowy kreatorow (KROK 3 promptu V12K-055). | Dyrektywa wlasciciela 2026-07-19; `KREATORY_STANDARD_2026-07.md`; recon ConnectRingForm (2-stage: connect_secondary_ring_sn → set_normal_open_point), RingCloseFormData/NOPFormData, reuzycie RingModal/NOP tylko w ConnectRingForm | Kreator ui2 `KreatorPierscienia` (kreatory/rama, 2 kroki): domkniecie (segment katalog-first KABEL/LINIA + dlugosc + zaciski z kontekstu) → `connect_secondary_ring_sn`; punkt normalnie otwarty (wybor lacznika z kandydatow) → `set_normal_open_point`. Sekcja downstream (PF pierscieniowy/NOP, pewnosc zasilania), uczciwy stan zerowy (brak zaciskow). Kontrakt payloadow 1:1 z legacy. Retire ConnectRingForm + RingCloseModal + NOPModal + nopModal.test (intencja przeniesiona do testow modelu + realnej sciezki). | `ui2/kreatory/pierscien/**`, `operationFormRegistry`, `operationSurfaceRegistry`, `forms/index.ts`, `modals/index.ts`, `modalRegistryCompleteness.test`, `operationFormRegistry.test`, `dialog_completeness_guard.py`. Bez edycji ui/sld/**. | Testy: pierscienModel (7) + KreatorPierscienia (3, realna sciezka 2-krokowa) + registry (11) + completeness (8). Type-check + guardy UI zielone. Pelna regresja frontendu. | sredni |
| V12K-055 | **ROZSTRZYGNIETY (2026-07-19, dyrektywa wlasciciela: „zaprojektuj nastepny prompt kontynuacji i wykonaj bez zatrzymywania"; Opcja A dla F2 wybrana przez wlasciciela).** Runda kontynuacyjna DER: (F2) pakiet dowodowy SC3F + endpoint — pierwszy pakiet dla zwarcia 3-fazowego (domyka luke: 3F nie mial pakietu; generate_sc3f_proof byl osierocony), fizyka serwerowa z snapshotu ENM, rozbicie maszynowe μ/q/i_b dla maszyn wirujacych; (F-follow-1) defekt walidacji: add_genset_nn/add_ups_nn zapisywaly gen_type „GENSET"/„UPS" ODRZUCANE przez model Generator → agregat/UPS nie przechodzily walidacji ENM (caly lancuch SC maszyn zablokowany dla agregatow). | Dyrektywa wlasciciela 2026-07-19; `docs/uiux/PROMPT_RUNDA_DER_DOKONCZENIE_2026-07.md`; `PLAN_SC_MASZYNY_DER_2026-07.md`; recon: sc_asymmetrical pack + api/proof_pack (wzorzec), SC3FInput.machine_result, GensetSpec (rated_voltage_kv/power_factor), Generator.gen_type enum | F2: `packs/sc_symmetrical.py::SC3FProofPack` + `POST /api/proof/sc3f/pack` (fizyka serwerowa map_enm→compute_3ph→compute_machine_contributions; rozbicie μ/q/i_b tylko dla maszyn wirujacych; brak maszyn→dowod bez sekcji, determinizm). F-follow-1: add_genset_nn→gen_type=„synchronous"+tabliczka (sn_mva=P/cosφ, un_kv, cos_phi; x″d domyslne IEC), add_ups_nn→„bess" (InverterSource §6.7, tozsamosc w name+meta.source_kind). | `application/proof_engine/packs/sc_symmetrical.py`, `packs/__init__.py`, `api/proof_pack.py`, `enm/domain_operations_v2.py`. FROZEN Result API + solver SC nietkniete; bez edycji ui/sld/**. | Testy: `test_sc3f_pack` (4), `test_proof_pack_api` (endpoint SC3F), `test_genset_ups_sc` (2, realna sciezka: operacja→walidacja ENM→graf→zrodlo SC). Determinizm: dowod bez maszyn bajt-identyczny; agregat/UPS bez tabliczki→domyslne IEC. Pelna regresja backendu per faza. | wysoki |
| V12K-054 | **ROZSTRZYGNIETY (audyt+plan, 2026-07-19, dyrektywa wlasciciela: „kontynuuj zgodnie bez zatrzymywania" + Zero-Debt + phantom rule w obie strony).** G-SCM forward-phantom (istotny dla bezpieczenstwa): kanoniczny bieg zwarciowy NIE uwzglednia zadnego wkladu prad zwarciowego od `enm.generators` (gensety §6.3, PV/BESS/FW §6.7, UPS) — I″k/ip/Ith zanizone o caly udzial maszynowy/DER. Caly lancuch (solver, ybus bocznik maszyny, superpozycja falownika, modele Synchronous/Asynchronous/InverterSource, analiza μ/q §6.6) ISTNIEJE i jest testowany na sieciach referencyjnych; zerwane JEDNO ogniwo — `map_enm_to_network_graph` nie buduje zrodel maszynowych/falownikowych z `enm.generators`. | Dyrektywa wlasciciela 2026-07-19; `docs/plan/PLAN_SC_MASZYNY_DER_2026-07.md`; recon: `enm/mapping.py:241-243/379-439` (generatory tylko P/Q; zrodla SC tylko z `enm.sources`), `canonical_analysis.py:878-908`, `short_circuit_iec60909.py:221/462` (ik_total=thevenin+inverters), `ybus.py:132-168` (bocznik maszyny SC-only, no-op bez maszyn), `core/machine.py`/`core/inverter.py` (modele + domyslne IEC), `reference_networks/station_archetype_substrate.py` (jedyny builder maszyn) | Plan fazowy: F1 most `map_enm_to_network_graph` (gen_type→zrodlo: pv_inverter/bess/wind_inverter→InverterSource I_k=k_sc·I_n; GENSET→SynchronousMachineSource sr=P/cosφ, ur=napiecie szyny, x″d domyslne IEC modelu; UPS→F-follow) — zero fabrykacji (domyslne IEC-typowe w modelach domenowych, WHITE BOX, jak istniejacy rx_ratio=0.1). F2 wpiecie interpretacji maszynowej w kanoniczny wynik (osobny artefakt, has_machines). F3 sekcja downstream w kreatorach (wplyw na I″k). F-follow: katalog x″d/k_sc, DFIG Typ3 vs Typ4, UPS (decyzja modelowa), silniki-odbiory. | `enm/mapping.py` (sedno F1), `enm/canonical_analysis.py` (F2), `ui2/kreatory/*` (F3). SHORT_CIRCUIT SOLVER + FROZEN Result API NIETKNIETE (zmiana WEJSCIA grafu, nie schematu wyniku; pola `ik_inverters_a` juz istnieja). PF nietkniety (bocznik maszyn SC-only). Bez edycji `ui/sld/**`. | Determinizm: sieci bez generatorow → Y-bus bajt-identyczny (no-op potwierdzony ybus.py:161); sieci z generatorami → wynik zmienia sie (I″k rosnie — poprawnie), golden przepisywane do nowej prawdy z intencja + pomiarem (Zero-Debt §2). Pelna regresja backendu per faza. | wysoki |
