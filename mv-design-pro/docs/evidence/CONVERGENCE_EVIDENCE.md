# MV-DESIGN-PRO — CONVERGENCE EVIDENCE (dowody konwergencji; dokument żywy)

**Status:** KANONICZNY, ŻYWY (kontrakt MAX PLATFORM 2026-09-04, §34–§36, §40, §42). Jedyne miejsce, w którym stan programu jest raportowany z dowodem (CI, testy, pomiary, wyrocznie). Zasada: „zielone przez pominięcie" nie jest dowodem; test snapshotowy nie jest wyrocznią fizyki.

---

## 0. Stan faktyczny repozytorium (konstytucja §28 — pomiar, nie deklaracja)

| Element | Pomiar (2026-09-04) |
|---|---|
| Gałąź / HEAD | `claude/mv-design-pro-twin-audit-u4lhy0` @ `c8331423` (konstytucja); `main` @ `7e84753a` (#473, 2026-09-04); `main` **bez ochrony gałęzi** (`protected: false`, >400 gałęzi `claude/*`, `codex/*`, `kopia/*`) |
| Drzewo robocze | czyste poza plikami karty CV-1-G (guard klucza, test, krok CI) w trakcie odbioru |
| Punkt wejścia aplikacji | `backend/src/api/main.py` (`app = FastAPI(...)`, 48 routerów); OpenAPI: **319 ścieżek, 227 schematów** (snapshot `backend/schemas/openapi_snapshot.json`); audyt A9 liczył 334 tras zamontowanych (różnica = trasy wyłączone produkcyjnie / duplikaty metod) |
| Persystencja | ENM: plik JSON per klucz (`enm/store.py`; do CV-1 klucz = `sha256(case_id)`, od `9667235a` klucz twin projektu przez fasadę); SQL (`DATABASE_URL`, domyślnie SQLite `./mv_design_pro.db`, `create_all`): `projects`, `study_cases`, `operating_cases` (legacy), `network_*` (legacy IR), `canonical_runs` (silnik #2), `analysis_runs` (legacy), `study_runs/results`, `*_types`, `document_records`; in-memory: scenariusze zakłóceniowe, serie, nadpisania SLD, konfiguracje, biegi V12.6; pliki: `.station_templates/`, archiwum ZIP; zadeklarowane, nieużywane: MongoDB, Redis, Celery |
| Własność modelu (runtime) | do CV-1: `StudyCase` de facto posiada ENM (plik per przypadek, model domyślny dla dowolnego `case_id`); po CV-1 rdzeń: klucz projektu + tłumacz (`application/twin_key.py`), wiring konsumentów w toku (karta CV-1-W) |
| Konsumenci magazynu ENM | 21 plików `src` (35 wywołań w `api/enm.py`), 80 plików testów (klucz surowy — dopuszczalny) |
| Tor biegów | produkcyjny: `api/execution_runs.py` → `enm/canonical_analysis.create_run/execute_run` (P1/S1); równoległe: `enm.py /runs`, `power_flow_runs/execute`, `unified_runs` (legacy ORM), `v126_academic` (in-memory); 4 rejestry biegów |
| Duplikaty reprezentacji | 6 modeli scenariusza + 4 delty in-memory; 10 builderów PF / 7 ścieżek SC; 20 implementacji topologii (14 backend + 6 klient); 8 reprezentacji stanu łącznika; 6 reprezentacji uziemienia; dialekt słownikowy benchmarków (12 + 1) obok ENM; `meta.field_specs` obok typowanego `Bay` |
| Systemy martwe/legacy | `study_case_engine.py` (test-only), P23 `study_scenario` (bez API), `execution_engine` (test-only), `network_wizard` (deprecated), ADR-008 `operating_cases`/`switching_states`, `trace_v2` (bez konsumenta), `enm/migrations/v_ports_001.py` (niewpięte), `sldNetwork53` (ręczny model w kliencie) |
| Aktywne migracje | CV-0 (zaufanie CI, fabrykacje, rejestry) i CV-1 (własność projektu) — `docs/architecture/CONVERGENCE_ROADMAP.md` §1 |

## 0a. Weryfikacja kierunku z audytu (konstytucja §29 — klasyfikacja per ustalenie)

| # | Ustalenie audytu | Klasyfikacja | Dowód na HEAD |
|---|---|---|---|
| 1 | ENM powinien stać się rdzeniem Canonical Project Twin | **CONFIRMED** | ocena 13 kryteriów: `CANONICAL_TWIN_ARCHITECTURE.md` §A.1; każda luka addytywna |
| 2 | Nie tworzyć drugiego trwałego `TwinModel` | **CONFIRMED** (i wpisane jako decyzja DT-1; ADR-012 skorygowany) | `DECISION_FREEZE_REGISTER.md` DT-1 |
| 3 | `network_model` → pochodny niemutowalny Computational IR | **CONFIRMED + NEW ISSUE** | tor kanoniczny już buduje `NetworkGraph` z ENM (`enm/mapping.py`); NEW: 12 benchmarków + `oze_pv_bess` istnieją tylko w dialekcie słownikowym liczonym własnym NR (P9) — druga prawda o sieci (pomiar `tests/golden/test_registry.py`) |
| 4 | Persystencja ENM per przypadek → per projekt | **CONFIRMED, PARTIALLY FIXED** | `enm/store.py:82` (`sha256(case_id)`); rdzeń CV-1 `9667235a`; wiring CV-1-W w toku |
| 5 | `StudyCase` odwołuje się do rewizji, nie posiada sieci | **CONFIRMED** (kierunek) | `domain/study_case.py` trzyma tylko konfigurację (C1) — brak pola modelu; magazyn był kluczowany przypadkiem (pkt 4) |
| 6 | Wiele form scenariusza — konsolidować, nie pisać kolejnego silnika | **CONFIRMED** | inwentarz 6 form + 4 delt in-memory: `CANONICAL_TWIN_ARCHITECTURE.md` §B.2 |
| 7 | Istnieją centralny solver-input builder i `ExecutionEngineService` — konsolidować wokół nich | **PARTIAL / MISDIAGNOSED** | `solver_input/builder` emituje KONTRAKT JSON (P11) nieczytany przez solver — nie jest assemblerem toru; `ExecutionEngineService` jest TEST-ONLY (5 plików testów, 0 wywołań z API) — produkcyjny tor to `canonical_analysis` (E1). Konsolidacja idzie wokół E1/P1, kontrakt `solver_input` pozostaje kontraktem wypełnianym przez assembler |
| 8 | `LvDomainProjectionV1` — silny wzorzec projekcji atomowej | **CONFIRMED** | 18 fixtur, hash identyczny cross-platform po kwantyzacji (`ee0ec472`, CI); wzorzec dla sceny SN |
| 9 | Frontend SLD SN posiada za dużo autorytetu semantycznego | **CONFIRMED** | `enmToSldAdapter.ts` 6 585 LOC, 5 implementacji topologii w kliencie (A2-08), `sldNetwork53` ręczny |
| 10 | Rozproszone ownership modeli/nastaw zabezpieczeń | **CONFIRMED** | nastawy per case (`ProtectionConfig`) i w modelu (`ProtectionAssignment`); fantomy nastaw w UI usunięte (FAB-B `021423bf`) |
| 11 | `meta.field_specs` niesie znaczenie inżynierskie → typowane obiekty | **CONFIRMED** | A1-02; typowany `Bay` istnieje, zapis kolekcji wyłączony (`LEGACY_FIELD_COLLECTIONS`) |
| 12 | `main` CI z wieloma niezależnymi awariami; ochrona gałęzi wyłączona | **CONFIRMED** | `main` @ a1ab2959/7e84753a: pytest/vitest/V12K czerwone (środowisko `networkx`), SLD determinism i E2E full czerwone od #472; `protected: false` |
| 13 | Commit M0 (kanonizacja liczb, guardy) — przejrzeć niezależnie | **CONFIRMED z korektą → ALREADY FIXED** | przegląd §36: kwantyzacja 9 cyfr poprawna (klasa równoważności 1e-12 vs 1e-8 zmierzona), ale NaN/Inf przechodziły do JSON (defekt maskowany) → tryb ścisły `KontraktNiefinitowyError` (`e6f11de7`); -0.0, porządek, jednostki, complex przypięte testami |

## A. Stan faktyczny CI (§34) — gałąź `claude/mv-design-pro-twin-audit-u4lhy0`

Klasyfikacja czerwonych bramek na szczycie `c5ebde3f` (pomiar z logów GitHub Actions, 2026-09-02) i ich los:

| Workflow | Stan @ c5ebde3f | Klasa (§34) | Przyczyna źródłowa | Naprawa | Stan po naprawie |
|---|---|---|---|---|---|
| Docs Integrity Guard | zielony | — | — | — | zielony |
| Physics Label Guard | zielony | — | — | — | zielony |
| Architectural And Repo Hygiene | zielony | — | — | — | zielony |
| Frontend E2E smoke | zielony | — | — | — | zielony |
| Python tests | czerwony (2 testy) | **stale test / policy guard** | `scripts/guardy_z_ci.py` nie rozpoznawał `$GUARD_PY` po ujednoliceniu środowiska guardów | regex `(?:python3?|\$\{?GUARD_PY\}?)` (`cfbc75fb`) | krok pytest **zielony** @ e6f11de7 (10 568 testów); czerwony pozostał krok „Testy guardów": 2 testy własne `test_solver_input_substitute_guard.py` (11 naruszeń + 2 moduły bez decyzji w mapie: `lv_temperature_correction.py`, `lv_mcb_bands_iec60898.py`) → karta CI-A → @ 930f1ada: pytest (10 5xx), testy guardów, audit2, 18 guardów **zielone**; czerwony wyłącznie ostatni krok „Format i lint backendu”: black na `scripts/` (3 pliki tej gałęzi: `guardy_z_ci.py`, `enm_store_key_guard.py`, `test_enm_store_key_guard.py` — **source regression (format)**, moje pominięcie osobnego wywołania `black --config pyproject.toml ../scripts`); sformatowane (`ff6262bc`), 396 testów własnych guardów zielone |
| Frontend checks | czerwony (1 test vitest + zapadka tsconfig) | **source regression** (komunikat nie w formie dokonanej — moja zmiana) + **policy guard** (dług typów 658 > 531, wyciszenia 39 > 35 — pre-existing) | `operationSuccessMessages.ts`; `tsconfig_gate_guard` | komunikaty poprawione (`cfbc75fb`); karta CI-B (agent, worktree) | @ e6f11de7: type-check, lint, vitest, audit2, 5 guardów **zielone**; czerwony wyłącznie `TSConfig Gate Guard` → CI-B |
| P0 Extended Guards | czerwony (11 naruszeń) | **environment/dependency** (naprawione w `c5ebde3f`: jedno środowisko poetry) → odsłoniło **source regression** z #472 (podstawianie liczb za brakujące dane w `domain_operations_v2.py`, `mapping.py`) + zapadka `c_factor` do obniżenia | `solver_input_substitute_guard` | karta CI-A (agent, worktree; §0: brak podstawień, jawna odmowa, jedna definicja `n_parallel`) | w toku |
| SLD Determinism Guards | czerwony | **nowa treść sceny (portal LV nN, #472), nie regresja trasowania** — dowód: atrybucja per przyczyna (footprint `#lv-portal-drop` +848 px na L1/L2; kaskada rezerwacji kanału i jogu trasy z wyższego footprintu stacji: 29/53 stacji 0→48 px), ablacja 2×2 = stan sprzed #472 co do piksela | `frontend/scripts/sld_v3_acceptance.mjs` baseline | karta CI-C: baseline z komentarzem dowodowym + bramka per przyczyna `VERTICAL_LENGTH_BY_CAUSE_BASELINE` + test negatywny (`ee0ec472`) | **zielony** @ ee0ec472 (run 33925212895) |
| Frontend E2E full | czerwony (12 speców) | **source regression na `main` od #472** (identyczna lista na `main` @ a1ab2959; poprzedni zielony bieg: 2031fc75, 2026-08-15) | m.in. chip `mvd-casebar-model` „Model: w budowie" przy `ready: true` z serwera | karta CI-D (agent, realny backend, przyczyna źródłowa w produkcie) | w toku → @ 930f1ada: 407 passed, 1 failed — `industrial-template-mass-flow` NIE na timeout, lecz na bramce rozmiaru: odpowiedź `POST /api/cases/{id}/runs/short-circuit` = 105 289 825 B przy limicie 60 MB. Klasa (§34: **source regression, KLASA NIE INSTANCJA**): odchudzenie V12K-284 i rozdzielenie K14 obsłużyły wyłącznie `branch_contributions`, a `branch_flow_trace` (ślad WHITE BOX podziału prądu, TH-1, ten sam iloczyn punkty×gałęzie, ~5× większy od wkładów) został w wierszu; po #472 (więcej węzłów raportowalnych nN) odpowiedź urosła 4,4×. Naprawa u źródła: klasa `KLUCZE_ROZPLYWU` = (wkłady, ślad) w JEDNYM miejscu (`canonical_run_repository.py`), wycinana ze świeżych wierszy POST i z artefaktu przy zapisie (kolumna addytywna `branch_flow_trace_json` w tabeli rozpływu), oddawana na żądanie przez tę samą końcówkę rozpływu punktu; `db.init_db` dokłada kolumny addytywne do istniejących baz (`_dolacz_kolumny_addytywne`, `create_all` tworzy tylko tabele); 8 testów klasy (klucz × zapis rozdzielony × świeże wiersze × widok punktu × baza sprzed kolumny × kolumna NOT NULL odmawia) |

Zakazy respektowane: brak `skip`, brak podnoszenia tolerancji, brak aktualizacji goldenów bez dowodu semantycznego, brak kasowania testów.

**Stan końcowy (2026-09-05, szczyt `ef9d6790`): 9/9 workflowów zielonych** — Python tests (`33932904372`), Frontend checks (`33932904356`), Docs Integrity (`33932904335`), Physics Label (`33932904432`), Architectural And Repo Hygiene (`33932904362`), SLD Determinism (`33932904347`), P0 Extended (`33932904357`) na `ef9d6790`; Frontend E2E full (`33932883655`: 408 speców, bramka 60 MB przeszła po naprawie klasy `KLUCZE_ROZPLYWU`) i E2E smoke (`33932883696`) na `e2a0dc17` (workflowy path-filtered; `ef9d6790` zmienia wyłącznie dokumentację, treść kodu identyczna). Dla porównania `main` (7e84753a) pozostaje czerwony na E2E full i SLD Determinism od #472. Żaden test nie został pominięty, wyłączony ani przeniesiony do `continue-on-error`; żaden próg nie został podniesiony (bramka 60 MB bez zmian, budżety zapadek obniżone, nie podniesione).

**Stan bieżący (2026-09-05 ~05:10 UTC, szczyt wypchnięty `3499df8c`):** E2E full **zielony** na `8bb6589a` (run `33944433207`, 408 speców; czerwony od `52df796a` do `814443c5` wyłącznie na `critical-der-config` — przyczyna FAB-F, naprawa E2E-DER), Python tests (`33944799463`) i Frontend checks (`33944799461`) zielone na `3499df8c`, Physics Label / Docs / Arch / P0 Extended / SLD Determinism / E2E smoke zielone na `814443c5` i `8bb6589a`; E2E full na `3499df8c` w toku w chwili wpisu. Żaden test nie został pominięty, wyłączony ani przeniesiony do `continue-on-error`; żaden próg nie został podniesiony.

## B. Ochrona gałęzi `main` (§36) — **OWNER ACTION P0**

Pomiar (GitHub API `list_branches`, 2026-09-04): `main` → `protected: false`; łącznie ponad 400 gałęzi (`claude/*`, `codex/*`, `kopia/*`), żadna chroniona. Sesja agenta nie ma uprawnień administracyjnych do włączenia ochrony (brak narzędzia w MCP; wymaga roli admin repozytorium).

**Dokładne działanie właściciela (Settings → Branches → Add branch ruleset / protection rule dla `main`):**
1. Require a pull request before merging (min. 1 approval; dismiss stale approvals on new commits).
2. Require status checks to pass before merging — wymagane checki (nazwy jobów): `pytest` (Python tests), `frontend` (Frontend checks), `V12K Extended Invariant Guards` (P0 Extended Guards), `SLD Guards (Python)` i `SLD Contract Tests (Vitest)` (SLD Determinism), `Docs Integrity Guard`, `Architectural And Repo Hygiene Guard`, `Physics Label Guard`, `full-real-backend-e2e` (Frontend E2E full) oraz smoke e2e; „Require branches to be up to date before merging".
3. Require conversation resolution before merging.
4. Block force pushes; block deletions; „Do not allow bypassing the above settings" (dotyczy także administratorów).
5. Opcjonalnie: require linear history.
Do czasu wykonania: każdy merge do `main` bez zielonego kompletu bramek jest naruszeniem kontraktu (§36) — odnotowane jako P0 w §H.

## C. Determinizm numeryczny (§35)
- Polityka: `docs/architecture/CANONICAL_TWIN_ARCHITECTURE.md` §C.5. Kod: `backend/src/application/analyses/kontrakt_liczb.py` (tryb ścisły: NaN/±inf/complex → `KontraktNiefinitowyError` ze ścieżką pola).
- Dowód: `tests/application/analyses/lv_domain/test_kwantyzacja_kontraktu.py` — fixtury × ±1 ULP (18 fixtur), własności, klasa równoważności 1e-12 vs 1e-8, porządek kluczy/list, niezależność od skali jednostki, `int`/`bool` nietknięte; pakiet `lv_domain` zielony lokalnie (wynik w §F). 18/18 fixtur nN: 0 wartości niefinitowych.

## D. Definition of Done — program (§40)

| # | Kryterium | Stan | Dowód / wycinek |
|---|---|---|---|
| 1 | Project posiada jeden Canonical Twin | NIE — ENM per `case_id` | CV-1 |
| 2 | ENM nie jest kopiowany per StudyCase | NIE | CV-1 |
| 3 | Assets mają stabilną tożsamość | CZĘŚCIOWO — `ref_id` stabilne; tłumaczenia uuid5 w 4 przestrzeniach | CV-4 (T-4) |
| 4 | Terminal/connectivity model jest fundamentem | NIE — `Port*` metadane bez egzekwowania | CV-5 (T-2) |
| 5 | Phase model nie blokuje ABCN ani earth-fault | NIE — brak faz, N/PEN/PE | CV-5 (F-1, F-2) |
| 6 | Grounding jest first-class | NIE — 6 reprezentacji, fizyka nie czyta | CV-5 (F-3) |
| 7 | Revision / Variation / Scenario / StudyCase rozdzielone | NIE — 6 modeli scenariusza, 4 delty in-memory | CV-3 |
| 8 | EffectiveSnapshot immutable i reprodukowalny | CZĘŚCIOWO — `CanonicalRun.snapshot` zamrożony, bez scenariusza | CV-3 |
| 9 | Topology derived | CZĘŚCIOWO — union-find w Ybus; 20 implementacji | CV-4 |
| 10 | Jeden canonical computational boundary | NIE — 10 builderów PF / 7 SC | CV-4 |
| 11 | `network_model` nie konkuruje z ENM | NIE — legacy ORM `network_*` żywy (wizard, XLSX) | CV-4 (procedura kasacji) |
| 12 | Solver input ma pełny provenance | CZĘŚCIOWO — `input_hash`, `snapshot_hash`; pola stałe w `build_analysis_run_reproducibility` | CV-2 |
| 13 | ResultSet posiada RevisionEnvelope | NIE | CV-2 |
| 14 | Protection settings — jedno ownership | NIE — nastawy per case i w modelu | po CV-5 (ADR-022) |
| 15 | SLD jest projekcją tego samego stanu | CZĘŚCIOWO — nN 3.0.0 tak; SN: klient liczy topologię | CV-4 → SLD |
| 16 | SN i nN bez konkurencyjnych modeli | CZĘŚCIOWO — jeden graf; nN miało równoległą prawdę (A1-08) | CV-5 |
| 17 | White Box odtwarza pełną drogę wyniku | CZĘŚCIOWO — ślady inline; `trace_v2` martwe | CV-4 |
| 18 | Reference Networks mają niezależne wyrocznie | CZĘŚCIOWO — 12 benchmarków IEEE/CIGRE (pandapower) tak; reszta REGRESSION_ONLY | rejestr §2 |
| 19 | CI wiarygodne | **DONE** — 9/9 zielone na `ef9d6790`/`e2a0dc17` (§A stan końcowy); każdy wcześniejszy czerwony sklasyfikowany i naprawiony u źródła (stale parser, martwy test, zapadka typów 658 → 137, 11 podstawień, regresja gotowości po #472, klasa ładunku per gałąź 105 MB), zero pominięć | §A |
| 20 | Main chroniony lub jednoznaczne owner action | OWNER ACTION zapisane | §B |
| 21 | G01 przechodzi end-to-end | NIE — G01 nie istnieje jako jedna sieć | CV-6 |
| 22 | Future Capability Review potwierdza fundament | TAK (projektowo) — 16 decyzji, 4 z warunkiem | `docs/architecture/DECISION_FREEZE_REGISTER.md` |
| 23 | Brak równoległych trwałych subsystemów legacy | NIE — inwentarz w `CANONICAL_TWIN_ARCHITECTURE.md` §B.2, `CANONICAL_TWIN_ARCHITECTURE.md` §C.2.3 | CV-1…CV-4 |
| 24 | Każda usunięta ścieżka ma guard przeciw wskrzeszeniu | CZĘŚCIOWO — `legacy_public_path_guard`, `verification_phantom_paths_guard` istnieją | per wycinek |

## E. Karty w toku (delegowane; model wykonawczy: `AGENTS.md` §8)
| Karta | Zakres | Wykonawca | Stan |
|---|---|---|---|
| CI-A | `solver_input_substitute_guard` 11 naruszeń u źródła | agent (worktree) | **odebrana** (`06a9e0f1`): pola wymagane odcinka nN przez `_wymagane_pola_odcinka` (jawny błąd `nn.segment_field_missing`), merge odrzuca różne parametry na km (`nn.merge_segments_type_mismatch`), `order` sekcji wymagany, `liczba_torow()` jako jedna definicja neutralnego elementu (Cable/TR/Generator/fault_loop), zapadka `c_factor` usunięta, 2 moduły dopisane do `CONTRACT_SOURCES`; guard RC=0 (1868 pól), testy własne 44, pełny pytest 10538 w worktree |
| CI-B | tsconfig gate 658 → ≤ 531, wyciszenia 39 → ≤ 35 | agent (worktree) | **odebrana** (`9b96ebcc`): dług typów poza bramką 658 → **137** (budżet 137), wyciszenia 39 → **1** (budżet 1; jedyne pozostałe dokumentuje, że format spoza rejestru eksportu nie przechodzi ani typu, ani wykonania), 0 błędów w bramce — wszystko u źródła: brak `import { describe, it, expect } from 'vitest'` (259 błędów w 3 plikach), martwy `@ts-expect-error` na shimie `URL.createObjectURL` (38 błędów = 38 wyciszeń, TS DOM lib zna te metody), fikstury niezgodne z kanonem typu (`EnmFragment.sources`, `StationTransformerUnit` 7 pól tabliczki, `ReadinessEntry.severity`, `KontekstJakosci` → `case_id`+`snapshot_hash`+`run_id`, sygnatura `derFixture`), `Array.prototype.at()` pod zamrożonym `lib: ES2020` (`src/test/arrayAt.ts`), 1 addytywne poszerzenie typu produkcyjnego `TraceValue` (wariant zespolony `re`/`im`, który `ElementCalculationProofPanel` już konsumował) z jawnym guardem `undefined` w `dowodModel.ts`; weryfikacja niezależna Fable: vitest 48 plików / 1039 testów zielone, guard RC=0 na gałęzi |
| CI-C | SLD `vertical_length_probe` z dowodem semantycznym | agent (worktree) | **odebrana** (`ee0ec472`), CI zielone |
| CI-D | 12 czerwonych speców e2e (pre-existing od #472) | agent (worktree) | **odebrana** (`b349377b`): 11/12 — jedna przyczyna produktowa (`enm/domain_operations.py:2141` `_build_readiness` nie znał wyjątku migracji nN, który `ENMValidator` liczy jako W061) + 2 testy regresyjne; 3 specy z przestarzałymi założeniami fixture/kanonu (opt-in `station_auxiliary`, `nn_earthing` przed LF, KD-8 zależność kanwy od motywu) przepisane z dowodem; 12. spec = P0-5 (wydajność zwarć), nie naprawiony i nie maskowany |
| FAB-A | fikcyjny katalog przekaźników `ACME_REX*` z `vendor: ABB` serwowany jako katalog (A6-17, D-33) | agent (worktree) | **odebrana** (`f49d7f18`): 5 urządzeń → identyfikatory referencyjne bez marki `REF-OC-100/200`, `REF-OC-EF-300/500/700`, szablony `template_ref_oc_*`, `vendor`/`series` = `None` (pole `DeviceCapability.vendor` → `str | None`, wyrównanie do istniejącego `ProtectionDeviceType.vendor`), `name_pl` „Profil referencyjny (nie produkt producenta)…" zasila wszystkie UI; proweniencja przez istniejące `REFERENCYJNY`/`REFERENCYJNY_V1` (bez nowego mechanizmu); `devices_v0.json` przepisany skryptem na strukturze JSON z dowodem semantycznym (pola liczbowe 5 rekordów identyczne, 46 rekordów bit-identycznych); 39 plików; luka wzorca karty (`rex100` bez granicy słowa) wykryta czerwonym testem i domknięta; PTPiREE `T-REX-*` = realny produkt (nietknięte). Weryfikacja: pełny pytest 10513 passed w worktree (agent); na gałęzi (Fable): pytest `network_model/catalog`+`protection`+`api`+`golden`+`proof_engine`+6 plików `enm` 2389 passed, vitest 3 pliki 50 passed, eslint, ruff, black, 14 guardów RC=0, `test_no_codenames_guard` 37 passed. Znaleziska innej klasy: (1) picker `KreatorPrzekaznika.tsx` oferuje katalog analityczny (51), a `add_relay` przyjmuje tylko kanoniczny (12) → **FAB-F F6** (zawężenie pickera do czasu konwergencji katalogów); (2) `devices_v0.json` niesie DWA schematy ID dla realnej rodziny Elektrometal e2TANGO (`EM_ETANGO_*` 7 i `EM_E2TANGO_*` 5, nakładające się 600/800/1000) → §H P1-5 |
| FAB-B | fantom nastaw `SldDetailDrawer` → dane z modelu / uczciwy stan zerowy | agent (worktree) | **odebrana** (`021423bf`): 3 bloki (DER 6 fikcyjnych progów ANSI, pole 5 wierszy, aparat 3 nastawy) → `ElementProtectionFunctionsPanel` na tej samej ścieżce danych co widok zabezpieczeń (`useProtectionAssignment` → `GET /enm/protection-view`); 6 testów klasy (pozytywne + negatywne bez liczb z jednostkami); 72/72; guardy zielone |
| CV-1-G | guard `enm_store_key_guard` (klucz przypadku w API/aplikacji = naruszenie; zapadka mierzona) + krok CI | agent (główny checkout, bez commitu) | w toku |
| FAB-C | zaszyte dane katalogowe w zakładce „parametry” kabla (`XRUHKXS 1×120`, `120 mm²`, `Al`, `270 A`, `PN-HD 620 S2`) | agent (worktree) | **odebrana** (`ff394efb`): 5 literałów → pola modelu (`catalog_ref`, `cross_section_mm2`, `conductor_material`, `rating.in_a`, `insulation`) z jawnym „Brak w modelu”; wiersz „Norma” usunięty (ENM nie niesie normy konstrukcyjnej kabla — fabrykacja, nie luka); izolacja linii napowietrznej „Nie dotyczy (przewód goły)”; oba miejsca budowy `cableRunSpec` naprawione jedną funkcją (`extractCableCatalogSpec`), v3 reużywa ten sam komponent; parytet pól TS↔backend potwierdzony; 3 testy (komplet danych / brak danych / linia napowietrzna); weryfikacja: vitest `sld`+`sld-editor` 263 plików / 4988 testów, type-check, lint, 8 guardów UI RC=0 (agent) + vitest 75, eslint, 5 guardów, gate RC=0 na gałęzi (Fable). Znaleziska innej klasy → karta FAB-F: `SldTitleBlock.tsx` `DEFAULTS` z kryptonimem „K30” i zmyślonymi danymi projektu (guard kryptonimów łapie tylko `P<cyfry>`), `DER_CATALOG_OPTIONS` 18 zaszytych `catalog_ref` poza katalogiem backendu, unia `ParameterSource` bez `MANUAL_EQUIVALENT` (guard parytetu nie porównuje literałów unii) |
| PERF-0 | baseline wydajności B1–B10 na sieciach rejestru (S: G02/G03/G08, M: G13/G00; L brak) — pomiar albo NIEMIERZALNE z powodem | agent (główny checkout, bez commitu) | **odebrana** (`47d809a9`): `backend/scripts/benchmark_baseline.py` + `tests/perf` (7 testów dymnych, zero asercji na czasy) + `docs/evidence/PERFORMANCE_BASELINE.md` / `performance_baseline.json` z realnego biegu (5 powtórzeń, G00: 1); przekroczenia: **B4 zwarcia G00 376 794 ms przy budżecie 1 s (377×)**, B3 rozpływ G00 5 100 ms przy 200 ms, B1 topologia G00 110 ms przy 30 ms, B8 projekcja nN 18–875 ms przy 15 ms (18/18 scenariuszy), B2 G03 30 ms/20 ms, B3 G02[1] 55 ms/50 ms, B4 G08 168 ms/100 ms; NIEMIERZALNE z powodem: B5 (solver 4-przewodowy nN nie istnieje, ADR-021 PROPOSED), B7/B9 (kod klienta), B2 na G00 (`create_run` odmawia — 21 BLOCKER); sieć L NIE ISTNIEJE (jawnie w JSON i MD); weryfikacja Fable: ruff/black RC=0, pytest `tests/perf` 7 passed |
| SUB-52s | obliczalność substratu 52 stacji (G00): 20× E063 (brak układu uziemienia nN), 1× E003 (wyspa po otwartym NOP na promieniowym odgałęzieniu) | agent (worktree) | **wykonana, wstrzymana w worktree** (`42649500`): E063 przez istniejący kanał `nn_earthing.lv_system` (TN-C-S) w obu ładunkach stacji; E003 przez `connect_secondary_ring_sn` między końcówkami dwóch odgałęzień (NOP zostaje otwarty jako rezerwa — wykrywanie osieroconego odgałęzienia z grafu, nie z UUID); 21 → 0 blokerów, hash G00 i fikstury zregenerowane skryptami repo (`sldNetwork53.ts`, `sldSubstrate52s.enm.json/.powerflow.json`), atrybucja geometrii 100% do gałęzi pierścienia; backend 147 passed, `accept:sld-v3` RC=0, vitest 1552/1553 — 1 test celowo czerwony: `buildScene.p1Recenzja` kontrakt lokalności (dodanie 1 stacji przesuwa PIONOWO 94/106 kotwic przy niezmienionym podziale wierszy — defekt SILNIKA układu przy pierścieniu) → karta SLD-LOC; scalenie SUB-52s po SLD-LOC, żeby gałąź nie była czerwona. Znaleziska innej klasy: P1-6 (GPZ `hv_neutral` gubione w budowniczym), P1-7 (walidator O(N³)) |
| FAB-D1 | ciche podstawienia liczb na ścieżce operacji domenowych ENM (klasa A6-12 cz. 1) | agent (worktree) | **odebrana** (`ab0c5e93`): naprawione u źródła D2 (`add_transformer_sn_nn` i TR blokowy DER przez wspólny `_require_transformer_fields` → `transformer.field_missing`), D3 (`_resolve_converter_defaults`, genset, UPS → `generator.power_missing`, `power_factor or 0.8` → `generator.power_factor_invalid`), D4 (LDC: blok tylko przy obu wartościach), D5 (`add_nn_load`, potrzeby własne → `load.q_missing`), D6 (`length_m` → `nn.segment_field_missing`), D8 (`DEFAULT_LOAD_KW` usunięte → `load.p_missing`, `except Exception` zawężony, porty z typu → `catalog.ports_missing`), D9 (`BayMeasurements(frequency_hz=50.0)` → pusty zestaw), `mapping.py:283` sentinel → jawny warunek + ślad; kody zarejestrowane; zapadka `solver_input_substitute_guard` obniżona o 6 pozycji; 20 plików, nowy `tests/enm/test_fab_d1_ciche_podstawienia.py`; agent: pytest enm/api/application/invariants/golden **4804 passed**, 9 guardów RC=0; fikstura `sldNetwork53.ts` zregenerowana (`q_mvar 0.0 → null` bez `q_min_mvar`, dowód dump przed/po). **Dwie klasy świadomie zostawione z pomiarem** → karty: (1) **FAB-G** tabliczka GPZ WN/SN z literałów (`hv_voltage_kv or 110`, `25 MVA`, `12 %`, `120 kW`, `25 kW`, `0,2 %`, `YNd11`) — proste odrzucenie = 517 czerwonych testów (każda sieć testowa zaczyna od `add_grid_source_sn`); decyzja: typ katalogowy obowiązkowy jak w PowerFactory, testy przez wspólne fabryki ładunku; (2) **FAB-H** `k_sc` falownika 1,1 (katalog nie niesie pola żadnym torem; `InverterSource.k_sc` w `core` z własnym 1,1) — decyzja: pole katalogowe + 1,1 jako ZAREJESTROWANE założenie (ślad White Box + WARNING `inverter.k_sc_assumed`), plus klasa „Q wytwórcy → 0" w trzech plikach (`mapping.py:589`, `canonical_analysis.py:1637`, `v126_contracts.py`) — jeden predykat, jeden kod `generator.q_missing` |
| FAB-D2 | ciche podstawienia na ścieżce katalog → materializacja → kontrakt (klasa A6-12 cz. 2) | agent (worktree) | **odebrana** (`eefab9a0`, cherry-pick z jednym konfliktem w `api/catalog.py` złożonym z FAB-A: producent pusty = profil referencyjny + `dopasowanie`/`certyfikat_ptpiree` zamiast `confidence`): D2 `i0_percent/p0_kw/vector_group` Optional (WARNING `transformer.no_load_params_missing`, BLOCKER `transformer.vector_group_missing` dla SC_1F), `V126TransformerInput.p0_kw` Optional z bramką w `api/v126_academic.py` (solver FROZEN nietknięty), D3 `_generator_q_mvar_jawne` → BLOCKER `generator.q_missing` (chyba że karta niesie Q-set-point), D4 jeden wyjątek `BrakujacePoleIRError` + moduł `network_model/ir_fields.py` dla `from_dict` w `core/*` i `catalog/types.py` (81 trafień), D5 `impedance_override.incomplete`, D6 `catalog.type_incomplete` + `control_mode` → `None` (emiter dla wcześniej zarezerwowanego `pv.control_mode_missing` — rezerwacja zdjęta), D7 nieznany status → `ValueError`, D8 profil DER: nieznany rodzaj → `der.dynamic_profile_missing` (BLOCKER), domyślny → `der.dynamic_profile_default` (WARNING z proweniencją), D9 API `dopasowanie: PELNE|CZESCIOWE` + `certyfikat_ptpiree` (snapshot OpenAPI przeliczony), uzupełnienie #1 `semantic.transformer_bus_voltage_missing`; dwie regresje wykryte i naprawione w karcie (`CatalogBinding.from_dict` łagodny parsing zachowany — walidacja powiązania ma własny mechanizm; `TransformerBranch.validate` z `None`); 33 fikstury uzupełnione realnymi danymi (w tym V12.6 przeliczone z tabelą przed/po). Świadomie zostawione z uzasadnieniem: `v126_contracts.py:425` `generator.q_mvar or 0.0` (konsument w solverze FROZEN zasilającym wiele analiz — B-01, dług nazwany w zapadce), `der_dynamic/models.py:125` (0 s inercji wirtualnej = fizycznie poprawne dla GFL), `mv_cable_line_catalog.py` (martwe: filtr przed użyciem), `ParameterSource.INSTANCE` (2 konsumenci produkcyjni, jawna proweniencja). Agent: pełny pytest **10711 passed**, mypy 0 (799 plików), guardy RC=0; na gałęzi: pełna weryfikacja (guardy CI komplet, pełny pytest, mypy, frontend) — liczby w §F |
| FAB-E | brak wyniku solvera pokazywany jako 0 (A6-12 część 3): 345 trafień `.get(pole, 0.0)`/`or 0.0` w API, raportach PDF/DOCX, dowodach, porównaniach, emiterach śladu, fikstur sieci referencyjnych | agent (worktree na `ff394efb`, `karta_fab_e.md`) | **odebrana** (`27e8a44b` cherry-pick `d1493111` z 5 konfliktami: 2 guardy = wersja gałęzi, 2 importy = unia, 1 test przepięty na realny projekt + klucz twin; `0d549d4a` korekta scalenia): 345 + dodatkowe trafienia tej samej klasy w ~60 plikach zaklasyfikowane per trafienie (a: fabrykowane zero → naprawione; b: zweryfikowany bezpieczny domysł; c: fałszywy pozytyw skanera; E6: rdzeń zamrożony/legacy do kasacji w CV-4 — policzone); nowe moduły `network_model/reporting/missing_value.py` (`format_wynik` „brak danych" w PDF/DOCX), `application/trace_emitters/wynik.py` (`BRAK_DANYCH`), `application/reference_networks/wymagane.py` (`pole_wymagane`, `ReferenceFixtureError`); komparator: nowy status **NIEPORÓWNYWALNY** zamiast FAIL od fabrykowanego 0,0; 5 rodzin dataclass wyników i 5 modeli API porównań → `| None`; mapy `ISSUE_SEVERITY_MAP.get(code, INFORMATIONAL)` → subskrypcja wprost + test kompletności; dowód PF: `ValueError` zamiast fabrykowanego `1.0 pu`. **Korekta klasyfikacji agenta (Fable):** 8 trafień `api/catalog.py` (auto-populate: `params.get(pole, 0.0)` dla mocy/napięć/przekroju/prądu) zakwalifikowanych jako „rdzeń zamrożony" to końcówka API — pozycja katalogu bez pola liczbowego była traktowana jak pozycja o wartości 0 („Sn=0 kVA" w propozycjach); naprawione w `0d549d4a` (pozycja pomijana z nazwanym powodem w logu, test iloczynu cech transformator×kabel×aparat). Pozostałe E6 policzone przez agenta (`power_flow_input_builder.py` 4, `network_wizard/service.py` 7, `analysis_run/service.py` 12, `results_inspector.py` 1) = tor legacy P2–P12 do kasacji (CV-4). Weryfikacja: agent pytest 5409 passed / 7 skipped; Fable w worktree (na `26542b66`): black/ruff src+tests RC=0, mypy 0 (804 pliki), pytest 11 katalogów **7319 passed** (6 czerwonych = `create_run` bez `klucz_twin` w testach karty sprzed CV-1-W → naprawione; 1 = rozjazd fixtury `sldNetwork53.ts` naprawiony na gałęzi w `2024fb4b`), snapshot OpenAPI przeliczony (poszerzenie pól delt porównań do nullable — świadoma zmiana kontraktu, zero zmian ścieżek). **Domknięcie na gałęzi (`98ad6b6a`):** pełny zestaw guardów CI dał dwie czerwienie po scaleniu — (1) `solver_input_substitute_guard`: 259 wpisów „dług ZMALAŁ" (karta usunęła podstawienia, nie obniżyła zapadki, bo jej baza była sprzed GUARD-SUB) → zapadka przeliczona programowo z zachowaniem powodów per plik: **89 plików / 592 → 61 / 320**, wykluczenia 24/64 → 15/46, pin testu własnego przepisany; po obniżce **3 realne naruszenia** w `api/canonical_run_views.py`: karta zamieniła `.get(k, 0)` na `.get(k) or 0` / `or 100.0` (moc bazowa 100 MVA fabrykowana w widoku interpretacji rozpływu) i zostawiła `max_mismatch_pu=0.0` („idealna zbieżność"), a ta sama forma w `energy_validation/service.py` (odczyt do zmiennej lokalnej, poza zasięgiem guarda) — naprawione jedną klasą: skalary biegu z artefaktu przez istniejącą warstwę wiązania (`power_flow_binding.skalary_wyniku_rozplywu` / `_lub_brak`), niedopasowanie z kroku końcowego śladu (`max_mismatch_ze_sladu`, upubliczniona) albo jawny brak, `analysis.PowerFlowResult` skalary `| None`; (2) `ui_production_codes_guard`: komunikat adaptera konwencji mocy niósł tokeny kontraktu (`branch_results`, `branch_id=`) → semantyka po polsku. Weryfikacja: oba guardy PASS, 54 testy własne, mypy src 0 (807 plików), pytest widok/walidacja energii/proof_engine/analysis/porównania/wiązanie 998 passed, 12 guardów generycznych RC=0. **Znalezisko klasy:** guard podstawień nie widzi wzorca „odczyt pola do zmiennej lokalnej → podstawienie" (`x = d.get("pole"); int(x) if x is not None else 0`) → karta GUARD-SUB-2 (zlecona) |
| FAB-F | metryka rysunku `SldTitleBlock` v2 z kryptonimem i zmyślonymi danymi projektu; guard kryptonimów bez klasy `K<cyfry>`; picker DER z 18 zaszytymi `catalog_ref`; parytet literałów unii; picker przekaźników poza bramą `add_relay` | agent (worktree, przebazowana na `92310661`) | **odebrana** (cherry-pick `bbe3ea9f` — weryfikacja na gałęzi: patrz §F): F1 `SldTitleBlock` v2 USUNIĘTY (zero konsumentów produkcyjnych; typ przycięty do 5 realnie używanych pól jako `OsdDrawingMetadata` w `ui/reports`); F2 `no_codenames_guard` + `export_codenames_guard` łapią `K<cyfry>` (K0 bez wykluczenia — pomiar), 8 trafień backendu (`step_controller` mapa etykiet PL, `generator_validation`) + ~5 frontu + 128 opisów testów z prefiksem „K30" naprawione; cytowania bramek K1–K13 w `describe/it` = jawny wyjątek `GATE_CITATION_TOKENS` (ta sama konwencja co F6d/S9-6/M0-1; pomiar 305 wystąpień w 109 plikach, tylko skan frontu); testy własne guarda 24 → 55; F3 `DER_CATALOG_OPTIONS` → katalog backendu (`fetchDerConverterTypes`, filtr nN/SN po `un_kv`), **9/18 zaszytych `catalog_ref` nie istniało** (backend nie ma przekształtników poniżej 1 kV); regresja własna wykryta pełnym vitestem (zapytanie katalogu przy każdym renderze) naprawiona (`enabled` po zakładce); F4 `enm.ts`: `MANUAL_EQUIVALENT`, 6 pól `ShuntCapacitor`, 4 wartości D10 `function_type`; **guard parytetu rozszerzony o literały unii i naprawiony** (12 pól dziedziczonych z `BranchBase` było cicho pomijanych — nowy `tests/ci/test_enm_contract_parity_guard.py` 18 testów; 115 pól Literal, 0 rozjazdów) + 4 błędy konsumenckie (`PARAMETER_SOURCE_LABELS_PL`, `REQUIRED_INPUTS_BY_FUNCTION` per funkcja zamiast heurystyki `startsWith`, `insulationLabel` bez `EPR`); F6 `KreatorPrzekaznika` → katalog kanoniczny (12) z testem parytetu z bramą `add_relay` i stanem pustym. Agent: type-check, lint, `guard:codenames`, 9 guardów, `tests/ci` 222, black/ruff (src, tests, scripts) RC=0; vitest 365 plików / 5940 testów — 1 czerwony pre-existing `kosztSceny.test.ts` (próg 3,05 s vs < 3 s pod obciążeniem; izolowany rerun 8/8) → **P1-8**. **KOREKTA 2026-09-05 (E2E-DER):** twierdzenia F3 „9/18 zaszytych `catalog_ref` nie istniało” i „backend nie ma przekształtników poniżej 1 kV” są **fałszywe** — pomiar `get_default_mv_catalog()`: 18/18 identyfikatorów istnieje, katalog `converter-types` ma 66 pozycji PV (w tym `conv-pv-nn-*` 0,4–0,8 kV), a zbiory PV/BESS są identyczne z `pv-inverter-types`/`bess-inverter-types`; usunięcie listy było słuszne z powodu DUPLIKACJI katalogu, nie fantomów. „Regresja własna naprawiona (`enabled` po zakładce)” wprowadziła regresję krytycznej ścieżki (zapis DER bez wejścia na „Falownik” niemożliwy) — naprawiona klasowo w `8bb6589a`; wniosek metodyczny: agent nie uruchomił e2e i przyjął pomiar katalogu bez dowodu |
| SLD-LOC | lokalność pionowa kotwic stacji przy sieci z pierścieniem: +1 stacja na ogonie magistrali przesuwa Δy 94/106 kotwic przy identycznym podziale wierszy (kontrakt WYTYCZNE §9) | agent (worktree na gałęzi + SUB-52s, karta w prompcie) | **scalona** (`536b0e11` SUB-52s cherry-pick `baf4183e`, `caa92911` SLD-LOC cherry-pick `098f76ab`, `2024fb4b` fixtury substratu 52s zregenerowane na scalonej gałęzi — konflikt `source_hash` w `sldNetwork53.ts`: FAB-D1 i FAB-G zmieniły materializację po bazie karty; hash 14047bcd → **bc10e5ac**, rozpływ zbieżny, 315 szyn zasilonych, 0 bez zasilania, 1 punkt otwarty); przyczyna wg agenta: JEDEN globalny licznik kodów zastępczych stacji dzielony między ciągami (`enmToSldAdapter.buildStations`), naprawa: baza numeracji per ciąg z indeksu strukturalnego × rozmiar bloku (`max` liczby stacji ciągu, podłoga 10) — macierz L4 9/9 kombinacji `pionowe=0`, przed naprawą 94/106 pionowych; baseline z atrybucją: `totalVerticalSegmentLength` 20936/43912/43912 → 21040/44016/44016, 6 odcisków `kosztSceny`, `obrysArkusza` S51–S53 → S109–S111, asercja S9-1 poprawiona (rozróżnia przepakowanie WEWNĄTRZ edytowanego ciągu od wycieku do innego). Agent: vitest `v3`+`engine/sld-layout`+`v2` 246 plików / 4755, `accept:sld-v3` ALL PASS, type-check, lint, `sld_determinism_guards` 0 naruszeń, `kosztSceny` izolowany 8/8 (próg P1-8 NIE podniesiony). Fable na gałęzi (fixtury bc10e5ac): vitest `v3`+`engine/sld-layout`+`v2` **245 plików / 4746 passed** (macierz L4 9/9 `pionowe=0` na zregenerowanych fixturach, pinowane baseline'y agenta trzymają bez zmian), `accept:sld-v3` ALL PASS, tsc 0, eslint 0, `sld_determinism_guards` 0 naruszeń; backend `enm`+`invariants`+`api`+`application`+`golden`+`reference_networks`+`infrastructure` 5003 passed. Znalezisko poza kartą: drugie odgałęzienie na stacji z jednym daje `crossingCount=2` niezależnie od hosta (`computeRowChannelPlan` per wiersz, nie per stacja) — osobna karta routingu pól |
| CV-1-W | wiring: wszyscy konsumenci `get_enm/set_enm/has_enm/restore_enm` w `api/` i `application/` na klucz twin projektu przez JEDEN tłumacz (`api/klucz_twin_dep.py`) | agent (worktree, 121 plików) | **odebrana** (`a71bd91c` cherry-pick z 1 konfliktem: identyfikator referencyjny FAB-C vs sygnatura helpera; `4ab4baec` korekty scalenia: 5 czerwonych po pełnym pytest na gałęzi — snapshot OpenAPI po `KluczTwin` przeliczony skryptem, test FAB-D1 i 3 testy FAB-D2 na realny projekt+przypadek; zapadka `enm_store_key_guard` 18 plików/74 → 1/1 (jedyny zastany: zapis tymczasowy importu archiwum ZIP z wieloma snapshotami), test własny guarda przypięty). Decyzje agenta przyjęte: `analysis_dispatch` klucz z `project_id` (id przypadku operacyjnego ≠ StudyCase — realny defekt), archiwum ZIP: eksport modelu projektu pod każdym `case_id` (schemat addytywny), import: pierwszy wpis = model projektu, pozostałe przez `migruj_klucz_przypadku_do_projektu` do `legacy_przypadki/`; `case_id` niepoprawny → 404 (jedno miejsce tłumaczenia). Agent: pełny pytest 10590 passed; na gałęzi 10759+5 naprawionych → §F |
| WB-2 | jedna funkcja rozpakowania wartości śladu WHITE BOX (trzy niezależne kopie) | agent (worktree, `karta_wb2.md`) | **odebrana** (`08ba0f4e`): inwentarz klasy wykazał **5** miejsc, nie 3 (`skladowe/model.ts::naZespolona`, `proof/export/exportTracePdf.ts::formatValue`) — wszystkie na `results-inspector/traceValue.ts::rozpakujWartoscSladu`, 32 testy iloczynu cech; na gałęzi vitest 35 plików / 457 testów, type-check, lint RC=0 |
| E2E-FIX | `critical-engineer-flow.spec.ts` asertował usunięte przez FAB-D2 pole `confidence` (E2E full czerwony na `92310661`) | Fable | **naprawione** (`40fa31bf`): ranking sprawdzany kluczem sortowania backendu (`dopasowanie` PELNE/CZESCIOWE, `certyfikat_ptpiree`); lokalnie 8/8 z realnym backendem. Wniosek metodyczny: karty zmieniające kontrakt API muszą grepować `frontend/e2e/**` (e2e full nie biegnie u agentów); lokalny e2e wymaga `PYTHONPATH=<backend>/src` i `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH`, a PRZED uruchomieniem ubicia zastanych serwerów (`uvicorn`/`vite` z cudzych worktree na 8000/5173 — Playwright `reuseExistingServer` testował STARY kod) |
| E2E-DER | E2E full czerwony od FAB-F (`2239ce9c`, biegi 304–307): `critical-der-config.spec.ts` — zapis DER z zakładki „Moc” nigdy nie wysyłał `POST …/generators` (15 s bez żądania w logu backendu) | Fable | **naprawione** (`8bb6589a`) — przyczyna: szuflada pobierała katalog przekształtników leniwie (tylko na zakładce „Falownik”) i CICHO podstawiała pierwszą pozycję, więc wartość była pusta, a komunikat wskazywał niewidoczną zakładkę; CV-1-W niewinna. KLASA „cichy domyślny `catalog_ref`” domknięta w 4 miejscach: szuflada (wybór JAWNY z pozycją zastępczą, stan haka kluczowany parą technologia×wariant, zmiana pary czyści wybór, błąd prowadzi do zakładki z polem — reguła dla całej klasy pól), backend `api/generators.py` (`catalog_ref` WYMAGANY → 422, mapa `_DEFAULT_CATALOG_BY_VARIANT` usunięta, pola wymagane z białych znaków = 422 zamiast cichego `None`), `AddDerWizard` (mapa zapasowych identyfikatorów usunięta), spec e2e (realna ścieżka z jawnym wyborem). Test maskujący (klikał „Falownik” przed zapisem) przepisany + test iloczynu cech. **Korekta FAB-F** (pomiar `get_default_mv_catalog()`): wszystkie 18 dawnych identyfikatorów istnieje w katalogu, katalog MA przekształtniki nN — twierdzenia „9/18 nie istniało” i „brak przekształtników < 1 kV” były fałszywe (zob. wiersz FAB-F). Dowód: pytest 26 (nowy test 422), snapshot OpenAPI przeliczony, vitest szuflada 77 + `sld/v3/canvas`+`sld/v2/canvas`+`network-build` 167 plików / 2196, tsc/eslint RC=0, e2e z realnym backendem 1 passed (POST 201, generator w ENM). Pozostałość tej samej rodziny: statyczny katalog urządzeń DER w `AddDerWizard` (fallback lokalny 3×97 pozycji) → karta **FAB-I** (zlecona, §H P1-9) |
| GUARD-SUB | `solver_input_substitute_guard` skanuje tylko `network_model/solvers`, `solver_input`, `enm` — 560 trafień klasy poza zasięgiem (66 w legacy budowniczych) | agent (worktree, `karta_guard_sub.md`) | **odebrana** (`0f4c3a59`, cherry-pick `a201b8f6` z 1 konfliktem w zapadce): zakres `network_model` (całość), `solver_input`, `enm`, `application`, `api` — 597 plików, zapadka fizyczna **89 plików / 592**, wykluczenia niefizyczne 24 pliki / 64 (liczniki rekordów archiwum, iteracji, rangi, geometria SLD — każde z uzasadnieniem, mechanizm dwukierunkowy jak zapadka); 3 sygnatury FAB-H, które karta świadomie zostawiła poza zapadką, na gałęzi już nie istnieją (domknięcie FAB-H `d58b949e`/`54cb5356`) — test własny pinujący „dokładnie 3 naruszenia” przepisany na stan faktyczny (RC=0, sumy per korzeń); guard na gałęzi PASS, 54 testy własne; TOP-10 plików wg trafień (`reference_networks/computation.py` 33, `domain_operations.py` 29, `analysis_run/service.py` 28, `power_flow_comparison/service.py` 27, `canonical_analysis.py` 25) = kandydaci kart; zauważona KLASA: most „wynik zwarcia → wejście oceny zabezpieczeń” zduplikowany w `protection/overcurrent/pipeline.py` i `wytrzymalosc_cieplna_przewodow.py`, para `m_factor`/`n_factor` (IEC 60909) w trzech proof-packach → karta konsolidacyjna po CV-2 |
| ADV-CV1 | przegląd adwersaryjny §38 „PROJECT owns ENM” (7 hipotez) | agent opus (worktree, `karta_adv_cv1.md`) | **odebrana** (`26542b66`, cherry-pick `b4d391bf` z 6 konfliktami): 14 prób obalenia, **3 udane i naprawione u źródła** (eksport archiwum omijał migrację → `klucz_twin_dla_projektu`/`migruj_projekt_z_legacy_z_repozytorium`; dispatch nN fabrykował pusty model projektu; dziennik promowanego przypadku szedł do legacy), 11 nieudanych zostaje jako inwarianty I-5…I-13 (`tests/invariants/test_wlasnosc_modelu_projektu_adwersaryjne.py`, 15 testów); guard REGUŁA 2 (`klucz_twin_projektu` niedostępny poza tłumaczem) + zakres `enm/**`. **Uzgodnienie z CV-2 w JEDEN mechanizm** (Fable): dwa równoległe mechanizmy „historia idzie za modelem” (`skopiuj_dziennik`+`skopiuj_katalog_rewizji` z CV-2 vs `przenies_dziennik` z ADV-CV1) scalone w `przenies_dziennik` + `przenies_katalog_rewizji_pod_klucz` (semantyka PRZENIESIENIA, odmowa gdy cel ma historię) z JEDNYM predykatem (`ma_historie` ∨ `dostepne_rewizje`) dla dziennika i migawek, manifest `"dziennik": ZA_MODELEM/ODLOZONY/BRAK`, wpis przeniesienia tylko gdy rewizja HEAD nie ma wpisu (bez dublowania rewizji); test CV-2 przepisany na semantykę przeniesienia. Weryfikacja: worktree pytest `enm`+`invariants`+`api`+`application` 4739 passed; na gałęzi (po SUB-52s/SLD-LOC/FAB-G) `enm`+`invariants`+`api`+`application`+`golden`+`reference_networks`+`infrastructure` **5003 passed, 5 skipped** (445 s), `enm_store_key_guard` PASS (442 pliki, zapadka 1/1), 34 testy własne, snapshot OpenAPI aktualny. Nierozstrzygnięte przez agenta (uczciwie): blokada międzyprocesowa (decyzja wdrożeniowa), druga prawda o świeżości `StudyCase.result_status` (→ CV-2-W, w toku), tożsamość modelu w dedupie legacy `_get_enm_hash` (tor E2 do kasacji), sieroty plików zastanych przypadków skasowanych z bazy (raport wykonalny, klucz w treści pliku) |
| FAB-G | tabliczka GPZ WN/SN wyłącznie z typu katalogowego (klasa D2 odłożona przez FAB-D1: 517 testów) | agent (worktree, `karta_fab_g.md`) | **odebrana** (`670c77c5` cherry-pick `625427a7` bez konfliktów, 98 plików; `3499df8c` korekta scalenia): `add_grid_source_sn` wymaga `transformer_catalog_ref`/`transformer_catalog_binding` albo pary `hv_voltage_kv`+`transformer_sn_mva` rozstrzygalnej w mapie (inaczej `catalog.ref_required` z listą dostępnych par), literały `or 12.0/120.0/0.2/"YNd11"` usunięte (były martwe — materializacja i tak nadpisywała, dowód G4: żaden hash ani fikstura nie zmieniły się przez tę kartę), katalog materializowany PRZED szyną 110 kV; testy przez wspólne fabryki ładunku (`catalog_test_helpers.gpz_payload` + lokalne wrappery), 12 testów iloczynu cech, 43/44 speców e2e z parą (44. mockuje żądanie), kreator GPZ z pickerem typu (`PoleKatalogu`); na gałęzi 25 testów CV-2 padło na `catalog.ref_required` (karta nie znała testów powstałych po jej bazie) → para dopisana w dwóch plikach, inwentarz klasy potwierdził brak innych trafień. Weryfikacja: agent pytest 4859 passed (5 pre-existing = 4ab4baec), 12 guardów RC=0, vitest 132; Fable na gałęzi pytest `enm`+`e2e`+`golden`+`reference_networks`+rejestr+guardy CI 2117 passed (+66 po korekcie), tsc 0, vitest `ui2/kreatory/zrodlo` 11 plików / 132, 12 guardów RC=0. Zgłoszone przez agenta, nie naprawione: brama HTTP (`domain_ops_policy`) nie ma pojęcia „pole wymagane” dla transformatora (błąd domenowy w treści 200 zamiast 422) — kandydat na rozszerzenie `PozycjaBramyApi`; brak krzyżowej walidacji typu 20 kV vs sieć 15 kV (luka przedistniejąca) |
| CV-2 (rdzeń) | `ModelRevision` przy magazynie ENM (migawka każdej rewizji, `checkout`), `RevisionEnvelope` na biegu, świeżość wyprowadzana z koperty, kasacja H1/H2/H3, `provenance_constant_guard` | Fable (worktree `fable-cv2`) | **scalony** (`9f5fe90e`, 31 plików): `enm/rewizje.py` (migawka `<digest>.rev/<n>.json.gz`, HEAD autorytatywny, uzgodnienie przy wczytaniu), `enm/store.py::checkout`, dziennik = rejestr rewizji (`hash_sha256`, `rodzic`, pełny `ladunek`, bez limitu), migracja CV-1 kopiuje historię pod klucz projektu (wcześniej lądowała w `legacy_przypadki/` — luka CV-1 zamknięta), `enm/envelope.py` + `network_model/catalog/odcisk.py` (odcisk biblioteki typów deterministyczny między procesami), `canonical_runs.envelope_json` (addytywna, dokładana do istniejących baz), `result_freshness.evaluate_envelope_freshness` (lista zmian z dziennika, „katalog zmieniony”) i `status_wynikow_przypadku`; H1 (5 pól nagłówka + lustro TS), H2 (`solver_version`/`catalog_schema_version` bez stałych; V12.5: `catalog_fingerprint`, `model_revision`), H3 (stałe „układ normalny” usunięte, `switching_state_ref` bez etykiety zastępczej — guard znalazł 4. instancję w `analysis_case_context.py`); `provenance_constant_guard` w `python-tests.yml`. Testy: 28 (rewizje: awaria × 5 kroków × 2 sieci, checkout × komendy, sieroty, magazyn zastany, migracja, import) + 19 (koperta, odcisk, świeżość iloczyn cech, `create_run`) + 16 (persystencja). Weryfikacja: §F. **Wiring (status przypadku z biegów w API, kasacja pisarzy `ResultInvalidator`/`mark_*`/`invalidate*`, ładunek komendy w `api/enm.py`, ekran świeżości z listą zmian, `GET .../rewizje/{n}`) — karta CV-2-W (agent)** |
| FAB-H | `k_sc` falownika: pole katalogowe `ConverterType.k_sc` + 1,1 jako zarejestrowane założenie (`k_sc_zrodlo`, ślad White Box, WARNING `inverter.k_sc_assumed` z emiterem, tożsamość liczb dla sieci bez karty); klasa „Q wytwórcy → 0” w 3 plikach — jedna funkcja `moc_bierna_wytworcy`, `generator.q_missing` dla rozpływu, V12.6 przez bramkę API (solver FROZEN nietknięty) | agent (worktree; karta `karta_fab_h.md`) | **odebrana** (`4ee77f56` cherry-pick bez konfliktów; inwentarz klasy: 3 przestrzenie katalogu z `k_sc` (`ConverterType`, `PVInverterType`, `BESSInverterType` — druga ścieżka katalogowa PV/BESS, której FAB-D1 nie objął), IR `InverterSource.k_sc_zrodlo`, `graph.k_sc_assumptions_trace`, 2 kody gotowości z emiterem, Q przez `solver_input/moc_bierna_wytworcy.py` w bramce gotowości/mapping/canonical_analysis/v126; agent: pełny pytest 10773 passed, mypy 0). **Odbiór wykazał niedomknięcie klasy**: `solver_input_substitute_guard` (którego agent nie uruchomił) złapał 3 NOWE podstawienia `q_mvar or 0.0`/`if-else 0.0` uzasadniane BLOCKER-em gotowości (dwa niezależne predykaty — KLASA §3) → domknięte przez Fable `d58b949e` (Q nieznane = wkład pominięty; zapadka `gen.q_mvar`/`generator.q_mvar` 1→0) i `54cb5356` (brama SSCI w API dla `_z_conv_components` solvera FROZEN — B-01; `V126ConverterInput.q_mvar` z tego samego źródła; test API przeniesiony na realny projekt+przypadek, bo FAB-H bazowała sprzed CV-1-W: 5 czerwonych → 0). Świadomie zostawione: `GeneratorSN/NN.k_sc` (`core/generator.py`, konsument wyłącznie `oze_validators.py` + 1 test — martwa wyspa toru legacy, kasacja z P2–P12 w CV-4; P2-1) |
| WB-ROZPLYW | ślad WHITE BOX podziału prądu zwarciowego (TH-1) na ścieżce użytkownika | agent (worktree) | **odebrana** (`acd2862c`): sekcja „Podział prądu zwarciowego — ślad obliczeń” w `ui2/wyniki/zwarcia/EkranZwarc.tsx` pod tabelą rozpływu, zasilana z tego samego wywołania końcówki rozpływu punktu (`RozplywOdpowiedz.branch_flow_trace`, hook zwraca `{flows, trace, blad}` — błąd HTTP odróżniony od braku danych); kroki renderuje istniejący `KrokDowodu`+`mapujKroki` (zero drugiego renderera); trzy uczciwe stany (błąd / niedostępny / pusty); fixture TH-1 skopiowana z realnego biegu solvera. **Defekt klasy naprawiony przy okazji**: `dowodModel.ts::mapujWielkosci` zakładał opakowany `TraceValue {value,unit,label}`, a solver emituje skalar wprost albo `{re,im}` — KAŻDA wartość realnego śladu w oknie „Dowód obliczeń” renderowała się jako kreska; naprawa `rozpakujWartosc` (oba kształty) + testy regresji. Testy: 14 plików / 172 passed (`zwarcia`+`dowod`), type-check, lint, 6 guardów UI RC=0 (agent); weryfikacja Fable na gałęzi w toku. Obserwacja → karta **WB-2**: trzy niezależne implementacje rozpakowania wartości śladu (`ElementCalculationProofPanel.unwrapTraceValue`, `TraceStepView.formatValue`, `dowodModel.rozpakujWartosc`) — konsolidacja do jednej funkcji współdzielonej |

## F. Dowody inżynierskie (testy, wyrocznie, przebiegi) — uzupełniane po każdym wycinku
- 2026-09-04: `pytest tests/application/analyses/lv_domain/` — komplet zielony po wdrożeniu trybu ścisłego (liczby w commicie).
- 2026-09-04: `tests/ci/test_guardy_z_ci.py` 4 passed; `snapshotStore.successToast.test.ts` 8 passed; guardy: `success_toast`, `claude_md_struktura`, `docs_guard`, `docs_archive_guard` zielone.
- 2026-09-04: CV-1 rdzeń (`9667235a`): `tests/invariants` + `tests/enm` + magazyn 1682 passed; `tests/api` + `station_templates` + `project_archive` 1086 passed po zmianie klucza magazynu (semantyka klucza surowego w testach niezmieniona).
- 2026-09-04: rejestr sieci wzorcowych w kodzie (`0c506744`): `tests/golden/test_registry.py` 14 passed (budowniczowie G02/G03/G04/G05/G07/G08/G13/G00/B-BENCH wykonalni; tabela generowana aktualna; zapadka pokrycia rodzin).
- 2026-09-04: SLD acceptance `npm run accept:sld-v3` RC=0 lokalnie (410 PASS) i w CI @ ee0ec472.
- 2026-09-04: snapshot OpenAPI (`0cb1c8d0`): 319 ścieżek, 227 schematów; test aktualności i determinizmu 2 passed — kontrakt HTTP jest odtąd diffowalny (M0-6).
- 2026-09-04: CI-B (`9b96ebcc`): `tsconfig_gate_guard` RC=0 na gałęzi (137 poza bramką / 1 wyciszenie / 0 w bramce); vitest na 48 zmienionych plikach testów: 1039 passed (odbiór niezależny Fable); w worktree agenta 70 plików / 1385 passed, type-check i lint RC=0.
- 2026-09-04: PERF-0 (`47d809a9`): pełny bieg `benchmark_baseline.py --powtorzenia 5 --sieci S,M` 435,6 s, RC=0; `tests/perf` 7 passed (bieg dymny na sieciach S, 1 powtórzenie); mtime bazy `mv_design_pro.db` niezmieniony (izolacja środowiska pomiaru).
- 2026-09-05: klasa `KLUCZE_ROZPLYWU` (wkłady + ślad podziału prądu): `tests/infrastructure/persistence/test_canonical_run_rozplyw.py` 15 passed (8 nowych), `test_enm_api` świeży bieg + końcówka rozpływu, `test_canonical_analysis_api` — 17 passed celowanych; guardy trace_ui_leak/resultset_v1/audit_contract/api_lifecycle/arch/vulture/solver_boundary/severity_contract RC=0; mypy_ratchet 0 błędów; regresja backendu (infrastructure, enm, api, e2e, analyses, proof_engine): **4206 passed, 2 skipped** (239 s) na `1e9f21c5`.
- 2026-09-05: lokalny komplet 82/82 guardów wołanych przez CI zielony na `f3b634d6` (`guardy_z_ci.py` interpreterem poetry) + 396 testów własnych guardów.
- 2026-09-05: FAB-D1 na gałęzi (`ab0c5e93`): pytest enm/api/application/invariants/golden **4804 passed, 5 skipped** (328 s); ruff/black (src, tests, scripts) RC=0; 11 guardów RC=0 (catalog_metadata/enforcement pod interpreterem poetry — pod systemowym python3 czerwone wyłącznie z braku `networkx`, artefakt środowiska); testy własne guarda podstawień 44 passed; vitest konsumentów `sldNetwork53.ts` 11 passed.
- 2026-09-05: CI `Python tests` na `f4e392f9` czerwony w kroku `readiness_consumption_guard` (F-K6): FAB-D1 zarejestrowała 3 kody gotowości bez emitera (`inverter.k_sc_missing`, `load.p_missing`, `catalog.ports_missing`) — klasa „deklaracja bez dostawcy”; naprawa u źródła `38cee389`: kody usunięte (punkt rozgałęźny bez portów zgłasza istniejąca brama `catalog.ref_required`; odbiór legacy bez mocy nie powstaje; `k_sc` wraca w FAB-H razem z emiterem); guardy readiness_codes/consumption/fix_action/domain_no_guessing/canonical_ops RC=0, 532 + 3 testy zielone. Wniosek metodyczny: łańcuch odbioru kart uruchamia odtąd KOMPLET guardów CI (`guardy_z_ci.py` interpreterem poetry), nie wybór.
- 2026-09-05: WB-ROZPLYW na gałęzi (`acd2862c`): vitest `ui2/wyniki/zwarcia`+`dowod` 14 plików / 172 passed, eslint zmienionych, `npm run type-check`, guardy ui_no_physics/trace_ui_leak/forbidden_ui_terms/no_codenames/dead_click/tsconfig_gate RC=0.
- 2026-09-05: FAB-D2 na gałęzi (`eefab9a0` + `d1229902`/`c71282e2`): mypy src 0 błędów na czystym cache (799 plików; 4 błędy zawężenia Optional w tabliczce agregatu z FAB-D1 naprawione, `has-type` w cyklu importów `domain_operations` ↔ `v2` domknięty jawną adnotacją), `guardy_z_ci.py` 82/82 (mypy_ratchet po naprawie RC=0), vitest `akademickie` 108 passed, type-check RC=0; **pełny pytest backendu 10742 passed, 11 skipped** (644 s) na `92310661`.
- 2026-09-05: CV-1-W na gałęzi (`a71bd91c`+`4ab4baec`): pełny pytest **10759 passed + 5 naprawionych w `4ab4baec`, 11 skipped** (779 s), mypy 0, black/ruff (src, tests, scripts) RC=0, `guardy_z_ci.py` komplet po obniżeniu zapadki `enm_store_key` (414 testów własnych guardów); WB-2 (`08ba0f4e`): vitest 35 plików / 457 testów, type-check, lint RC=0; E2E `critical-engineer-flow.spec.ts` lokalnie 8/8 z realnym backendem. CI na `4ab4baec`: Python tests zielone (run 33941020675); Frontend checks / E2E full w toku w chwili wpisu.
- 2026-09-05: FAB-H na gałęzi (`4ee77f56`): black/ruff/mypy RC=0; pytest zakres 10 katalogów **6895 passed, 5 failed** — wszystkie 5 w `test_v126_generator_q_missing_api.py` (seeding surowym kluczem sprzed CV-1-W) → naprawione w `54cb5356`; `guardy_z_ci.py` czerwony wyłącznie na `solver_input_substitute_guard` (3 nowe podstawienia FAB-H + 2 wpisy do obniżenia) → naprawione w `d58b949e`. Po domknięciu (`d58b949e`+`54cb5356`+port testu „strukturalnego zera”): `solver_input_substitute_guard` RC=0 (zapadka w obie strony, test własny zielony), `readiness_consumption_guard` RC=0, black/ruff/mypy RC=0, pytest `tests/enm tests/solver_input tests/api/test_v126_* tests/application/calculation_readiness tests/test_canonical_analysis_api.py tests/golden` **2010 passed** (1 test FAB-H przepisany do domkniętego kanonu: Q nieznane w powiązaniu falownika = `None`, nie 0,0).
- 2026-09-05 (~05:35 UTC, push `98ad6b6a`, 12 commitów): SUB-52s + SLD-LOC + fixtury (`536b0e11`/`caa92911`/`2024fb4b`), ADV-CV1 (`26542b66`), FAB-G korekta, FAB-E (`27e8a44b`/`0d549d4a`/`98ad6b6a`) — backend `enm`+`invariants`+`api`+`application`+`golden`+`reference_networks`+`infrastructure` **5003 passed / 5 skipped** (445 s); FAB-E w worktree 11 katalogów **7319 passed** + na gałęzi 998; frontend SLD **245 plików / 4746**, `accept:sld-v3` ALL PASS, tsc 0, eslint 0, `sld_determinism_guards` 0; `guardy_z_ci.py` 83 guardy: 81 zielone + 2 czerwone naprawione u źródła w `98ad6b6a` (`solver_input_substitute_guard`, `ui_production_codes_guard`), 460 testów własnych; 12 guardów generycznych RC=0 przed pushem; mypy 0 (807); black/ruff 0.
- 2026-09-05: GUARD-SUB (`0f4c3a59`) i E2E-DER (`8bb6589a`) na gałęzi: `solver_input_substitute_guard` PASS (597 plików, zapadka 89/592, wykluczenia 24/64), 54 testy własne; pytest `test_generators_api`+`test_openapi_snapshot`+`test_wspolbieznosc_koncowek_modelu` 26 passed; vitest `sld/v3/canvas`+`sld/v2/canvas`+`network-build` **167 plików / 2196 testów**, `SldDetailDrawer` 77; `tsc --noEmit`, eslint (`--max-warnings 0`) RC=0; e2e `critical-der-config` z realnym backendem 1 passed (44 s). CV-2 (rdzeń) na gałęzi (`9f5fe90e`): `guardy_z_ci.py` 83/83 + 445 testów własnych, mypy 0 (804 pliki), pytest `enm`+`api`+`application`+`invariants`+`e2e`+`infrastructure`+`cgmes`+`solver_input` **5044 passed, 7 skipped** (418 s). CI na `814443c5`: Python tests, Physics Label, Docs, Arch, P0 Extended, SLD Determinism, E2E smoke zielone; E2E full czerwony (DER — naprawa `8bb6589a`), Frontend checks w toku.
- 2026-09-05: FAB-F na gałęzi (`2239ce9c`): `npm run type-check`, `npm run lint`, `guard:codenames` RC=0; vitest zakres dotknięty (sld/v2/canvas, reports, ui2/kreatory/przekaznik, network-build/cards, sld/v3/export, station-der) **60 plików / 918 testów**; guardy no_codenames, export_codenames, tsconfig_gate (137/1), dead_click, forbidden_ui_terms, ui_terminology, vulture, enm_contract_parity, catalog_metadata RC=0; pytest `tests/ci` + `test_wizard_step_controller` + `test_station_field_validation` + `test_generator_validation` + `test_e2e_determinism` **309 passed**; testy własne guardów kryptonimów (`scripts/test_no_codenames_guard.py`, `scripts/test_export_codenames_guard.py`) **60 passed**; black/ruff (src, tests, scripts) RC=0.

## G. Ustalenia adwersaryjne (§38) — po każdej granicy
| Data | Granica | Próba obalenia | Wynik |
|---|---|---|---|
| 2026-09-05 | CV-2: „każda rewizja ma migawkę, `checkout(n)` odtwarza hash z dziennika, awaria nie zostawia śladu” | iniekcja awarii po KAŻDYM z 5 kroków zapisu (dziennik roboczy, migawka robocza, podmiana HEAD, podmiana migawki, podmiana dziennika) × 2 sieci z prawdziwymi komendami domenowymi; sierota powyżej HEAD; migawka bieżącej o innym hashu; magazyn sprzed rejestru (bez `.rev/`, dziennik obcięty); import archiwum; migracja klucza | OBALONA w każdym wariancie: stan = ostatnia rewizja spójna (pamięć, HEAD co do bajtu, brak migawki n, brak wpisu n), po ponownym wczytaniu `checkout(n).hash == dziennik[n].hash` dla każdego n; sierota usunięta i nigdy promowana; migawka obca zastąpiona treścią HEAD; wpis odtworzony nazywa brak wprost. **Jedna hipoteza POTWIERDZONA po drodze**: test wycofania iniektował awarię w `Path.write_bytes` globalnie, więc po CV-2 trafiał w PRZYGOTOWANIE migawki zamiast w wycofanie (test mierzył co innego niż deklarował) — iniekcja zawężona do plików poza `.rev/` (`tests/enm/test_store_concurrency.py`) |
| 2026-09-05 | CV-2: „świeżość wyprowadzana z koperty nie może uznać wyniku za aktualny bez dowodu” | koperta niespójna z własnym odciskiem (edycja zapisu poza systemem); bieg bez koperty (sprzed CV-2); ta sama rewizja, inny hash; katalog inny przy tym samym modelu; brak modelu bieżącego | OBALONA: każdy wariant daje OUTDATED z nazwaną przyczyną (`koperta-rewizji-niespojna`, kotwice hashowe, `model-zmieniony`, `katalog-zmieniony`, `brak-modelu-biezacego`); FRESH wyłącznie przy zgodnej rewizji, hashu i odcisku katalogu; każdy kod przyczyny ma zdanie PL (test przypięty) |
| 2026-09-05 | CV-1 „PROJECT owns ENM" — ścieżka adresowana PROJEKTEM (nie przypadkiem) | eksport archiwum projektu w świeżym procesie, model nadal pod kluczem przypadku (`tests/invariants/test_wlasnosc_modelu_projektu_adwersaryjne.py::test_i5_eksport_archiwum_nie_gubi_modelu_zastanego_per_przypadek`) | **TWIERDZENIE OBALONE — defekt realny, naprawiony u źródła.** `project_archive/service.py::_collect_enm` budowało klucz przez `enm.klucz_twin.klucz_twin_projektu` (czysta funkcja, ZERO migracji), więc `has_enm` = `False` i archiwum ZIP wychodziło BEZ sekcji ENM (import takiego archiwum = projekt bez sieci). Naprawa: `application/twin_key.migruj_projekt_z_legacy_z_repozytorium(project_id, CaseRepository(session))` przed odczytem; test zielony |
| 2026-09-05 | CV-1 — ta sama klasa na drugiej ścieżce projektowej | dispatch analiz nN (`FAULT_LOOP_NN`/`SWZ_NN`) w świeżym procesie, model zastany per przypadek (`…::test_i7_dispatch_nn_nie_fabrykuje_pustego_modelu_projektu`) | **TWIERDZENIE OBALONE — defekt realny, naprawiony u źródła.** `analysis_dispatch/service.py` budowało klucz tą samą czystą funkcją; `get_enm` FABRYKOWAŁO pusty model domyślny i go ZAPISYWAŁO, więc realny model projektanta przy pierwszym wejściu przez `/api/cases/...` lądował w `legacy_przypadki/` jako ROZBIEZNY (podmiana sieci na pustą). Naprawa: `application/twin_key.klucz_twin_dla_projektu` (migracja + klucz) w obu dispatcherach; klasa domknięta bramką (`enm_store_key_guard` REGUŁA 2: `klucz_twin_projektu` niedostępne w `api/**` i `application/**`) |
| 2026-09-05 | CV-1 — migracja „nic nie gubi" (dziennik zmian) | promocja modelu przypadku aktywnego na model projektu z zachowaniem licznika rewizji (`…::test_i6_dziennik_promowanego_przypadku_idzie_za_modelem`) | **TWIERDZENIE OBALONE — defekt realny, naprawiony u źródła.** `store._odloz_do_legacy` odkładał dziennik KAŻDEGO przypadku (także promowanego) do `legacy_przypadki/`; projekt startował z historią pustą przy modelu w rewizji N, a `GET /enm/dziennik-zmian?od_rewizji=R` oddawał listę Z DZIURĄ wyglądającą na kompletną. Naprawa: `dziennik_zmian.przenies_dziennik` (dziennik idzie ZA modelem, odmawia nadpisania cudzej historii) + manifest niesie los dziennika (`ZA_MODELEM`/`ODLOZONY`/`BRAK`) |
| 2026-09-05 | CV-1 — bramka własna (`enm_store_key_guard`, krok CI `p0-extended-guards.yml`) | pomiar zapadki na HEAD po karcie wiring CV-1-W | **BRAMKA CZERWONA NA HEAD — naprawione.** CV-1-W przepisała konsumentów, ale nie obniżyła zapadki: 18 plików meldowało „Dług ZMALAŁ — obniż budżet", czyli krok CI przewracał się na własnej naprawie. Stan zmierzony: 1 plik / 1 wywołanie (`project_archive/service.py` — zapis TYMCZASOWY pod kluczem przypadku w imporcie archiwów legacy, droga poprawna, nie dług); zapadka obniżona 18/74 → 1/1. Zakres skanu rozszerzony o `enm/**` (440 plików zamiast 408) — warstwa, w której magazyn i klucz MIESZKAJĄ, weszła CZYSTA (zero naruszeń obu reguł, zapadka bez zmian); testy własne bramki 28 → 34 |
| 2026-09-05 | CV-1 — współbieżność i rozdzielność (hipotezy 1, 2, 3, 6, 7) | 11 prób: równoległe pierwsze tłumaczenie dwóch przypadków jednego projektu; dwa RÓŻNE przypadki tego samego projektu zapisujące równolegle; świeżość wyniku przypadku A po edycji przez przypadek B; wspólny `ENM_STORE_DIR` dla wielu projektów; plik zastany przypadku SKASOWANEGO z bazy; projekt bez przypadków; przypadek spoza bazy × brak warstwy DB × nie-UUID | **TWIERDZENIE SIĘ BRONI** — wszystkie próby nieudane, testy zostają jako inwarianty I-8…I-13 w `tests/invariants/test_wlasnosc_modelu_projektu_adwersaryjne.py` (19 testów w `tests/invariants`, RC=0). Nierozstrzygnięte i nazwane: blokada `blokada_twin` jest W PROCESIE (wiele workerów uvicorna = osobna decyzja wdrożeniowa, zapisana w docstringu `enm/store.blokada_twin`) |
| 2026-09-05 | CV-1 — dług wykryty przy okazji (Zero-Debt, nie własny) | pełna regresja `tests/api` na HEAD | **3 czerwone testy pre-existing, naprawione u źródła:** `tests/enm/test_f10_6_domain_fields.py` (jeden test klasy został przy dawnym podpisie `_seed`/`get_enm_field_view` → `TypeError`), `tests/api/test_v126_opf_loss_lcc_api.py` ×3 (literał `case_id` bez wiersza w bazie → 404 zamiast 422/200), `tests/api/test_openapi_snapshot.py` (snapshot nieodświeżony po CV-1-W: `case_id` traci `format: uuid` na 2 trasach, bo sub-zależność `KluczTwin` deklaruje `case_id: str` — runtime zweryfikowany: nie-UUID daje 404 z tłumacza, więc dokument mówi prawdę; snapshot wygenerowany ponownie) |

## H. Pozostałe P0/P1 (tylko realne)
| ID | Ryzyko | Klasa | Działanie |
|---|---|---|---|
| P0-1 | `main` bez ochrony; merge #472 z czerwonym CI (P0/vitest/pytest maskowane przez środowisko) | governance | owner action §B |
| P0-2 | ENM per `case_id` — kopia sieci per przypadek | architektura | CV-1 |
| P0-3 | fabrykacje użytkowe: fikcyjny katalog, fantom nastaw, `c_factor=1.0` domyślne w torze legacy | zaufanie | FAB-A/B; kasacja toru legacy (CV-4) |
| P0-4 | 12 czerwonych e2e na `main` od #472 | regresja produktu | **ZAMKNIĘTE**: 11/12 przez CI-D (`b349377b`), 12. (`industrial-template-mass-flow`) przez naprawę klasy `KLUCZE_ROZPLYWU` (`1e9f21c5`) — E2E full 408 speców zielone na `e2a0dc17`; wydajność zwarć pozostaje otwarta jako P0-5 (pomiar PERF-0), niezależnie od zielonej bramki |
| P1-1 | 4 rejestry biegów, 5 sposobów uruchomienia | spójność | CV-3.3 |
| P1-2 | uziemienie: 6 reprezentacji, fizyka nie czyta żadnej | fizyka doziemna | CV-5 |
| P1-3 | 12 benchmarków IEEE/CIGRE/IEC + `oze_pv_bess` istnieją tylko w dialekcie słownikowym P9 (nie walidują się jako ENM) — jedyna niezależna wyrocznia LF/SC liczy się poza torem kanonicznym | spójność / dowodowość | CV-4 (benchmarki jako ENM przez assembler; zapadka `BENCHMARK_DICT_ZASTANE`) |
| P1-4 | substrat 52 stacji (G00): 21 BLOCKER walidatora, budowa ≈ 40 s — sieć skali nieobliczalna | wydajność / rejestr | CV-4/CV-6 (naprawa u źródła; zapadka `BLOKERY_ZASTANE`) → SUB-52s (`42649500`, worktree): 21 → 0 BLOCKER (E063 ×20 przez kanał `nn_earthing.lv_system`, E003 przez pierścień `connect_secondary_ring_sn`); budowa ≈ 10–15 s (profil: walidator O(N³) = P1-7); scalenie po SLD-LOC |
| P1-5 | trzy prawdy o przekaźnikach (A6-17): `ProtectionDeviceType` (12, kanoniczny, jedyny przyjmowany przez `add_relay`), `devices_v0.json` (51, analityczny — z DUPLIKACJĄ schematów ID `EM_ETANGO_*`/`EM_E2TANGO_*` dla jednej realnej rodziny e2TANGO, warianty 600/800/1000 podwójnie), `protection_vendors.py` | dane / drugie prawdy | konwergencja do JEDNEGO katalogu zabezpieczeń z `ProtectionCapabilityRegistry` (PZ-07, ADR-022) — karta architektoniczna po CV-1; do tego czasu picker zawężony do katalogu kanonicznego (FAB-F F6); scalenie schematów e2TANGO = decyzja danych (jeden schemat ID, warianty z karty producenta) w tej samej karcie |
| P1-6 | GPZ: regeneracja fikstury substratu 52s pokazuje `hv_neutral: directly_grounded → null` i `i0_percent: 0.2 → 0.35` dla `tr-wn-sn-110-15-25mva-yd11`; katalog niesie 0.35 (fikstura była nieaktualna — katalog jest prawdą), ale `hv_neutral` GPZ dziś NIE jest ustawiane przez `add_grid_source_sn` (jedyny pisarz `_apply_station_neutral_grounding`, `domain_operations.py:4605`, woła tylko wstawianie stacji) i walidator tego nie zgłasza (brak kodu dla braku uziemienia punktu neutralnego WN/SN) | kompletność danych / uziemienie punktu neutralnego | CV-5 F-3 `NeutralGrounding` typowane dla KAŻDEGO transformatora (GPZ włącznie) + kod walidatora + ustawienie w `add_grid_source_sn` z karty typu; do tego czasu analizy doziemne WN nie mają danych GPZ (uczciwy brak, nie 0) |
| P1-7 | walidator: `pole_transformatorowe._szyny_stacji` O(stacje×szyny) wołane 5 287× i `_tekst` 4 451 480× przy budowie substratu 52s (każda ze 151 operacji domenowych uruchamia pełny `ENMValidator().validate()` przez `_build_readiness`) — łącznie O(N³) na rosnącej sieci; profil cProfile z karty SUB-52s | wydajność / walidator | indeks szyn per stacja budowany raz na walidację (słownik), nie na każdy transformator; pomiar B1/B2 po naprawie; osobna karta po CV-1-W (walidator jest współdzielony przez wszystkie operacje) |
| P2-1 | `network_model/core/generator.py::GeneratorSN/GeneratorNN.k_sc = 1.1` — trzecie miejsce zaszytego 1,1 (FAB-H): zero konsumentów produkcyjnych (`oze_validators.py` importowany wyłącznie przez `tests/test_oze_generators.py`) — martwa wyspa toru legacy P2–P12 | martwy kod / legacy | kasacja razem z torem legacy (CV-4, procedura 7 kroków); nie „naprawiać” martwego kodu |
| P1-8 | `frontend/src/ui/sld/v3/scene/__tests__/kosztSceny.test.ts`: próg czasowy (< 3 s) zależny od obciążenia maszyny — czerwony przy równoległych agentach (3,05 s), zielony w izolacji; test jednostkowy z bramką wall-clock = niedeterministyczna bramka | jakość testów / determinizm | próg czasowy → pomiar operacji (licznik) albo benchmark poza suitą jednostkową (`tests/perf` wzorzec PERF-0 z medianą i p95); naprawa po scaleniu SLD-LOC (ten sam plik) |
| P1-9 | `AddDerWizard` (`network-build/station-der`): statyczne listy `PV_INVERTER_CATALOG`/`BESS_PCS_CATALOG`/`WIND_TURBINE_CATALOG` (`catalogs.ts`, po ~97 pozycji) podstawiane jako „fallback lokalny”, gdy backend zwróci pustą listę albo błąd — użytkownik wybiera urządzenie, którego backend nie zna (zapis 422 albo zbieżny identyfikator o innych parametrach); ta sama rodzina co `DER_CATALOG_OPTIONS` (FAB-F) i `_DEFAULT_CATALOG_BY_VARIANT` (E2E-DER) | fabrykacja / drugie źródło prawdy o katalogu | karta **FAB-I** zlecona (2026-09-05): katalog wyłącznie z backendu, stan pusty/błąd uczciwy i blokujący krok, kasacja statyków bez konsumentów, inwentarz pozostałych statycznych katalogów tego pliku (baterie, krzywe LVRT/HVRT, profile NC RfG, poziomy nN, transformatory blokowe) z pomiarem |
| P0-5 | **wydajność zwarć na sieci M**: `POST /api/execution/runs/{id}/execute` (IEC 60909, 50 stacji) kończy się poprawnie (HTTP 200), ale w 297 s / 1 581 s pod obciążeniem (pomiar karty CI-D) i ponad budżetem klienta także w CI (spec `industrial-template-mass-flow.spec.ts` czerwony na `main` od #472); budżet B4 dla M = 1 s, L = 10 s (plan wydajności §1a) — przekroczony o 2–3 rzędy | wydajność / tor kanoniczny | **pomiar PERF-0 (`47d809a9`): B4 na G00 (315 szyn, 54 transformatory) = 376 794 ms (6,3 min) przy budżecie 1 s — 377×; B3 rozpływ 5 100 ms przy 200 ms (25×); B1 topologia 110 ms przy 30 ms**; przyczyna: pętla po wszystkich węzłach z gęstą algebrą (A3-10: SC O(N·n³)); naprawa = faktoryzacja rzadka + kolumny selektywne (CV-4, §C.3) — zmiana rdzenia wymaga B-01; **zakaz** podnoszenia timeoutu testu bez naprawy |

## I. Decyzje właściciela (tylko nierozstrzygalne z repo/norm/danych)
| ID | Pytanie | Dlaczego nie da się rozstrzygnąć samodzielnie |
|---|---|---|
| OD-1 | Ochrona `main` (§B) | wymaga uprawnień administratora |
| OD-2 | Czy istnieją projekty użytkowników w legacy ORM `network_*` (XLSX/wizard) wymagające eksportu przed kasacją (D-03 krok 3) | dane produkcyjne poza repozytorium |
| OD-3 | Publikowane wyrocznie dla G01 (sieć kompensowana): wskazanie źródła literaturowego/normowego akceptowanego przez właściciela jako `PUBLISHED_BENCHMARK` | wybór źródła normatywnego/literaturowego jest decyzją właściciela produktu |
| OD-4 | Retencja rewizji modelu (CV-2): rewizje są append-only, każda z pełną migawką (gzip ≈ 80 kB przy 54 stacjach); rewizje referowane przez bieg lub przypadek są nieusuwalne; pytanie właściciela: czy dopuścić operację pruningu rewizji NIEREFEROWANYCH (jawną, na żądanie, z manifestem) czy trzymać pełną historię bez wyjątku | polityka retencji danych projektu (koszt nośnika vs kompletność audytu) |

## J. Następny vertical slice
**CV-1 — Project owns ENM** (`docs/architecture/CANONICAL_TWIN_ARCHITECTURE.md` §2): magazyn per projekt, fasada `case_id → project_id`, migracja per-case ENM → warianty, guard przeciw zapisowi kluczem przypadku, inwarianty I-1…I-4 w `tests/invariants/`.
