# MV-DESIGN-PRO — CONVERGENCE EVIDENCE (dowody konwergencji; dokument żywy)

**Status:** KANONICZNY, ŻYWY (kontrakt MAX PLATFORM 2026-09-04, §34–§36, §40, §42). Jedyne miejsce, w którym stan programu jest raportowany z dowodem (CI, testy, pomiary, wyrocznie). Zasada: „zielone przez pominięcie" nie jest dowodem; test snapshotowy nie jest wyrocznią fizyki.

---

## 0. Stan faktyczny repozytorium (konstytucja §28 — pomiar, nie deklaracja)

| Element | Pomiar (2026-09-04) |
|---|---|
| Gałąź / HEAD | `claude/mv-design-pro-twin-audit-u4lhy0` @ `c8331423` (konstytucja); `main` @ `7e84753a` (#473, 2026-09-04); `main` **bez ochrony gałęzi** (`protected: false`, >400 gałęzi `claude/*`, `codex/*`, `kopia/*`) |
| Drzewo robocze | czyste poza plikami karty CV-1-G (guard klucza, test, krok CI) w trakcie odbioru |
| Punkt wejścia aplikacji | `backend/src/api/main.py` (`app = FastAPI(...)`, 48 routerów); OpenAPI: **319 ścieżek, 227 schematów** (snapshot `backend/schemas/openapi_snapshot.json`); audyt A9 liczył 334 tras zamontowanych (różnica = trasy wyłączone produkcyjnie / duplikaty metod) |
| Persystencja | ENM: plik JSON per klucz (`enm/store.py`; do CV-1 klucz = `sha256(case_id)`, od `9667235a` klucz twin projektu przez fasadę); SQL (`DATABASE_URL`, domyślnie SQLite `./mv_design_pro.db`, `create_all`): `projects`, `study_cases`, `operating_cases` (legacy), `network_*` (legacy IR), `canonical_runs` (silnik #2), `analysis_runs` (legacy), `study_runs/results`, `*_types`, `document_records`; in-memory: scenariusze zakłóceniowe, serie, nadpisania SLD, konfiguracje, biegi V12.6; pliki: `.station_templates/`, archiwum ZIP; zadeklarowane, nieużywane: MongoDB, Redis, Celery |
| Własność modelu (runtime) | do CV-1: `StudyCase` de facto posiada ENM (plik per przypadek, model domyślny dla dowolnego `case_id`); po CV-1 rdzeń: klucz projektu + tłumacz (`application/twin_key.py`), wiring konsumentów w toku (karta CV-1-W) |
| Konsumenci magazynu ENM | 21 plików `src` (35 wywołań w `api/enm.py`), 80 plików testów (klucz surowy — dopuszczalny) |
| Tor biegów | produkcyjny: `api/execution_runs.py` → `enm/canonical_analysis.create_run/execute_run` (P1/S1); równoległe: `enm.py /runs`, `power_flow_runs/execute`, `unified_runs` (legacy ORM), `v126_academic` (in-memory); 4 rejestry biegów |
| Duplikaty reprezentacji | 6 modeli scenariusza + 4 delty in-memory; 10 builderów PF / 7 ścieżek SC; 20 implementacji topologii (14 backend + 6 klient); 8 reprezentacji stanu łącznika; 6 reprezentacji uziemienia; dialekt słownikowy benchmarków (12 + 1) obok ENM; `meta.field_specs` obok typowanego `Bay` |
| Systemy martwe/legacy | `study_case_engine.py` (test-only), P23 `study_scenario` (bez API), `execution_engine` (test-only), `network_wizard` (deprecated), ADR-008 `operating_cases`/`switching_states`, `trace_v2` (bez konsumenta), `enm/migrations/v_ports_001.py` (niewpięte), `sldNetwork53` (ręczny model w kliencie) |
| Aktywne migracje | CV-0 (zaufanie CI, fabrykacje, rejestry) i CV-1 (własność projektu) — `docs/architecture/CONVERGENCE_ROADMAP.md` §1 |

## 0a. Weryfikacja kierunku z audytu (konstytucja §29 — klasyfikacja per ustalenie)

| # | Ustalenie audytu | Klasyfikacja | Dowód na HEAD |
|---|---|---|---|
| 1 | ENM powinien stać się rdzeniem Canonical Project Twin | **CONFIRMED** | ocena 13 kryteriów: `CANONICAL_TWIN_ARCHITECTURE.md` §A.1; każda luka addytywna |
| 2 | Nie tworzyć drugiego trwałego `TwinModel` | **CONFIRMED** (i wpisane jako decyzja DT-1; ADR-012 skorygowany) | `DECISION_FREEZE_REGISTER.md` DT-1 |
| 3 | `network_model` → pochodny niemutowalny Computational IR | **CONFIRMED + NEW ISSUE** | tor kanoniczny już buduje `NetworkGraph` z ENM (`enm/mapping.py`); NEW: 12 benchmarków + `oze_pv_bess` istnieją tylko w dialekcie słownikowym liczonym własnym NR (P9) — druga prawda o sieci (pomiar `tests/golden/test_registry.py`) |
| 4 | Persystencja ENM per przypadek → per projekt | **CONFIRMED, PARTIALLY FIXED** | `enm/store.py:82` (`sha256(case_id)`); rdzeń CV-1 `9667235a`; wiring CV-1-W w toku |
| 5 | `StudyCase` odwołuje się do rewizji, nie posiada sieci | **CONFIRMED** (kierunek) | `domain/study_case.py` trzyma tylko konfigurację (C1) — brak pola modelu; magazyn był kluczowany przypadkiem (pkt 4) |
| 6 | Wiele form scenariusza — konsolidować, nie pisać kolejnego silnika | **CONFIRMED** | inwentarz 6 form + 4 delt in-memory: `CANONICAL_TWIN_ARCHITECTURE.md` §B.2 |
| 7 | Istnieją centralny solver-input builder i `ExecutionEngineService` — konsolidować wokół nich | **PARTIAL / MISDIAGNOSED** | `solver_input/builder` emituje KONTRAKT JSON (P11) nieczytany przez solver — nie jest assemblerem toru; `ExecutionEngineService` jest TEST-ONLY (5 plików testów, 0 wywołań z API) — produkcyjny tor to `canonical_analysis` (E1). Konsolidacja idzie wokół E1/P1, kontrakt `solver_input` pozostaje kontraktem wypełnianym przez assembler |
| 8 | `LvDomainProjectionV1` — silny wzorzec projekcji atomowej | **CONFIRMED** | 18 fixtur, hash identyczny cross-platform po kwantyzacji (`ee0ec472`, CI); wzorzec dla sceny SN |
| 9 | Frontend SLD SN posiada za dużo autorytetu semantycznego | **CONFIRMED** | `enmToSldAdapter.ts` 6 585 LOC, 5 implementacji topologii w kliencie (A2-08), `sldNetwork53` ręczny |
| 10 | Rozproszone ownership modeli/nastaw zabezpieczeń | **CONFIRMED** | nastawy per case (`ProtectionConfig`) i w modelu (`ProtectionAssignment`); fantomy nastaw w UI usunięte (FAB-B `021423bf`) |
| 11 | `meta.field_specs` niesie znaczenie inżynierskie → typowane obiekty | **CONFIRMED** | A1-02; typowany `Bay` istnieje, zapis kolekcji wyłączony (`LEGACY_FIELD_COLLECTIONS`) |
| 12 | `main` CI z wieloma niezależnymi awariami; ochrona gałęzi wyłączona | **CONFIRMED** | `main` @ a1ab2959/7e84753a: pytest/vitest/V12K czerwone (środowisko `networkx`), SLD determinism i E2E full czerwone od #472; `protected: false` |
| 13 | Commit M0 (kanonizacja liczb, guardy) — przejrzeć niezależnie | **CONFIRMED z korektą → ALREADY FIXED** | przegląd §36: kwantyzacja 9 cyfr poprawna (klasa równoważności 1e-12 vs 1e-8 zmierzona), ale NaN/Inf przechodziły do JSON (defekt maskowany) → tryb ścisły `KontraktNiefinitowyError` (`e6f11de7`); -0.0, porządek, jednostki, complex przypięte testami |

## A. Stan faktyczny CI (§34) — gałąź `claude/mv-design-pro-twin-audit-u4lhy0`

Klasyfikacja czerwonych bramek na szczycie `c5ebde3f` (pomiar z logów GitHub Actions, 2026-09-02) i ich los:

| Workflow | Stan @ c5ebde3f | Klasa (§34) | Przyczyna źródłowa | Naprawa | Stan po naprawie |
|---|---|---|---|---|---|
| Docs Integrity Guard | zielony | — | — | — | zielony |
| Physics Label Guard | zielony | — | — | — | zielony |
| Architectural And Repo Hygiene | zielony | — | — | — | zielony |
| Frontend E2E smoke | zielony | — | — | — | zielony |
| Python tests | czerwony (2 testy) | **stale test / policy guard** | `scripts/guardy_z_ci.py` nie rozpoznawał `$GUARD_PY` po ujednoliceniu środowiska guardów | regex `(?:python3?|\$\{?GUARD_PY\}?)` (`cfbc75fb`) | krok pytest **zielony** @ e6f11de7 (10 568 testów); czerwony pozostał krok „Testy guardów": 2 testy własne `test_solver_input_substitute_guard.py` (11 naruszeń + 2 moduły bez decyzji w mapie: `lv_temperature_correction.py`, `lv_mcb_bands_iec60898.py`) → karta CI-A → @ 930f1ada: pytest (10 5xx), testy guardów, audit2, 18 guardów **zielone**; czerwony wyłącznie ostatni krok „Format i lint backendu”: black na `scripts/` (3 pliki tej gałęzi: `guardy_z_ci.py`, `enm_store_key_guard.py`, `test_enm_store_key_guard.py` — **source regression (format)**, moje pominięcie osobnego wywołania `black --config pyproject.toml ../scripts`); sformatowane (`ff6262bc`), 396 testów własnych guardów zielone |
| Frontend checks | czerwony (1 test vitest + zapadka tsconfig) | **source regression** (komunikat nie w formie dokonanej — moja zmiana) + **policy guard** (dług typów 658 > 531, wyciszenia 39 > 35 — pre-existing) | `operationSuccessMessages.ts`; `tsconfig_gate_guard` | komunikaty poprawione (`cfbc75fb`); karta CI-B (agent, worktree) | @ e6f11de7: type-check, lint, vitest, audit2, 5 guardów **zielone**; czerwony wyłącznie `TSConfig Gate Guard` → CI-B |
| P0 Extended Guards | czerwony (11 naruszeń) | **environment/dependency** (naprawione w `c5ebde3f`: jedno środowisko poetry) → odsłoniło **source regression** z #472 (podstawianie liczb za brakujące dane w `domain_operations_v2.py`, `mapping.py`) + zapadka `c_factor` do obniżenia | `solver_input_substitute_guard` | karta CI-A (agent, worktree; §0: brak podstawień, jawna odmowa, jedna definicja `n_parallel`) | w toku |
| SLD Determinism Guards | czerwony | **nowa treść sceny (portal LV nN, #472), nie regresja trasowania** — dowód: atrybucja per przyczyna (footprint `#lv-portal-drop` +848 px na L1/L2; kaskada rezerwacji kanału i jogu trasy z wyższego footprintu stacji: 29/53 stacji 0→48 px), ablacja 2×2 = stan sprzed #472 co do piksela | `frontend/scripts/sld_v3_acceptance.mjs` baseline | karta CI-C: baseline z komentarzem dowodowym + bramka per przyczyna `VERTICAL_LENGTH_BY_CAUSE_BASELINE` + test negatywny (`ee0ec472`) | **zielony** @ ee0ec472 (run 33925212895) |
| Frontend E2E full | czerwony (12 speców) | **source regression na `main` od #472** (identyczna lista na `main` @ a1ab2959; poprzedni zielony bieg: 2031fc75, 2026-08-15) | m.in. chip `mvd-casebar-model` „Model: w budowie" przy `ready: true` z serwera | karta CI-D (agent, realny backend, przyczyna źródłowa w produkcie) | w toku → @ 930f1ada: 407 passed, 1 failed — `industrial-template-mass-flow` NIE na timeout, lecz na bramce rozmiaru: odpowiedź `POST /api/cases/{id}/runs/short-circuit` = 105 289 825 B przy limicie 60 MB. Klasa (§34: **source regression, KLASA NIE INSTANCJA**): odchudzenie V12K-284 i rozdzielenie K14 obsłużyły wyłącznie `branch_contributions`, a `branch_flow_trace` (ślad WHITE BOX podziału prądu, TH-1, ten sam iloczyn punkty×gałęzie, ~5× większy od wkładów) został w wierszu; po #472 (więcej węzłów raportowalnych nN) odpowiedź urosła 4,4×. Naprawa u źródła: klasa `KLUCZE_ROZPLYWU` = (wkłady, ślad) w JEDNYM miejscu (`canonical_run_repository.py`), wycinana ze świeżych wierszy POST i z artefaktu przy zapisie (kolumna addytywna `branch_flow_trace_json` w tabeli rozpływu), oddawana na żądanie przez tę samą końcówkę rozpływu punktu; `db.init_db` dokłada kolumny addytywne do istniejących baz (`_dolacz_kolumny_addytywne`, `create_all` tworzy tylko tabele); 8 testów klasy (klucz × zapis rozdzielony × świeże wiersze × widok punktu × baza sprzed kolumny × kolumna NOT NULL odmawia) |

Zakazy respektowane: brak `skip`, brak podnoszenia tolerancji, brak aktualizacji goldenów bez dowodu semantycznego, brak kasowania testów.

**Stan końcowy (2026-09-05, szczyt `ef9d6790`): 9/9 workflowów zielonych** — Python tests (`33932904372`), Frontend checks (`33932904356`), Docs Integrity (`33932904335`), Physics Label (`33932904432`), Architectural And Repo Hygiene (`33932904362`), SLD Determinism (`33932904347`), P0 Extended (`33932904357`) na `ef9d6790`; Frontend E2E full (`33932883655`: 408 speców, bramka 60 MB przeszła po naprawie klasy `KLUCZE_ROZPLYWU`) i E2E smoke (`33932883696`) na `e2a0dc17` (workflowy path-filtered; `ef9d6790` zmienia wyłącznie dokumentację, treść kodu identyczna). Dla porównania `main` (7e84753a) pozostaje czerwony na E2E full i SLD Determinism od #472. Żaden test nie został pominięty, wyłączony ani przeniesiony do `continue-on-error`; żaden próg nie został podniesiony (bramka 60 MB bez zmian, budżety zapadek obniżone, nie podniesione).

**Stan bieżący (2026-09-05 ~05:10 UTC, szczyt wypchnięty `3499df8c`):** E2E full **zielony** na `8bb6589a` (run `33944433207`, 408 speców; czerwony od `52df796a` do `814443c5` wyłącznie na `critical-der-config` — przyczyna FAB-F, naprawa E2E-DER), Python tests (`33944799463`) i Frontend checks (`33944799461`) zielone na `3499df8c`, Physics Label / Docs / Arch / P0 Extended / SLD Determinism / E2E smoke zielone na `814443c5` i `8bb6589a`; E2E full na `3499df8c` w toku w chwili wpisu. Żaden test nie został pominięty, wyłączony ani przeniesiony do `continue-on-error`; żaden próg nie został podniesiony.

**Stan bieżący (2026-09-05 ~06:05 UTC, szczyt wypchnięty `3c4b3c13`):** na `24d2257b` 8/9 zielone (Python tests `33947416379`, Frontend checks `33947416373`, P0 Extended `33947416410`, Arch `33947416365`, SLD Determinism `33947416479`, Docs `33947416432`, Physics Label `33947416377`, E2E smoke `33947416391`); **Frontend E2E full czerwony od FAB-G** (`3499df8c` run 309, `98ad6b6a` run 310, `24d2257b` run 311: 404 passed / 4 failed / 2 skipped, 28 min): `ux-feedback-loop.spec.ts` C2+C3 (mock backendu bez `GET /api/catalog/transformer-types`, którego kreator GPZ wymaga po FAB-G) i `sld-pa-powerflow-tor.spec.ts` N-2+screenshot (asercja „≥1 przyciemniona stacja" opierała się na wyspie E003 substratu, naprawionej w SUB-52s — `de_energized_bus_refs: []`); przyczyna = KLASA „karta zmienia kontrakt/fiksturę, nie grepuje `frontend/e2e/**`" (wniosek E2E-FIX z `40fa31bf` powtórzony); naprawa u źródła zlecona (karta E2E-FIX (2), §E). Runy na `3c4b3c13` (CV-3.1 rdzeń + PARITY-CV3 + GUARD-SUB-2) w toku w chwili wpisu. Żaden test nie został pominięty, wyłączony ani przeniesiony do `continue-on-error`; żaden próg nie został podniesiony.
**Stan bieżący (2026-09-05 ~09:05 UTC, szczyt wypchnięty `e82ee602`): 9/9 zielone na `325d7994`** — Python tests (`33955141884`), Frontend checks (`33955141900`), Frontend E2E full (`33955141911`, run 319; poprzedni zielony bieg E2E full: `6b27554a` run 318 `33954747206` — pierwszy po naprawie E2E-FIX, czerwony od FAB-G przez runy 309–317), E2E smoke (`33955141898`), Docs (`33955141899`), Physics Label (`33955141869`), Arch (`33955141893`), P0 Extended (`33955141895`), SLD Determinism (`33955141902`). Przywrócenie zieleni E2E full = naprawa u źródła czterech testów (mock katalogu GPZ odzwierciedlający kontrakt `catalog.ref_required`, drugi companion konserwacyjny przez `apply_scenario`, równość zbiorów zamiast progu „≥1") plus usunięcie klasy „reoptymalizacja Vite w trakcie biegu" (`optimizeDeps.entries`); jedyny test flaky z runu 317 (S9-5 zapis magistrali) ma kartę E2E-S95 u agenta. Runy na `e82ee602` (CV-3.2 C4 + P24+) w toku w chwili wpisu. Żaden test nie został pominięty, wyłączony ani przeniesiony do `continue-on-error`; żaden próg ani limit czasu nie został podniesiony. **Korekta (2026-09-05 ~09:10 UTC):** P0 Extended CZERWONY na `e82ee602`, `45af111d`, `58b55777` — `grep_zero_guard`: banery historyczne dwóch dokumentów zastąpionych (kasacja C4/P24+) cytowały literalne nazwy dawnych plików etapowych; lokalna lista odbioru CV-3.2 nie obejmowała tego guarda (dopisany do stałej listy odbioru). Naprawa u źródła `af752b0d` (banery bez literalnych nazw; `grep_zero`, `docs`, `docs_archive` rc=0 lokalnie).

**Korekta (2026-09-05 ~06:15 UTC):** CI na `3c4b3c13`: Physics Label, Docs, Arch, E2E smoke, SLD Determinism zielone; **P0 Extended czerwony** (run `33948970798`) na `port_binding_guard --strict` — nowy harness parytetu (`tests/golden/parytet_scenariuszy/harness.py`) wszedł w zakres skanu `tests/golden/**` z 7 połączeniami Cable/OverheadLine bez `endpoint_a_port`/`endpoint_b_port` (fikstury skopiowane z `tests/application/*`, które są poza zakresem guarda) → naprawione u źródła w `54071d6d` (porty jawne `PortRef("<szyna>:sn")`; to metadane wiązania SLD, nie fizyka — złote hashe parytetu 30/30 bez zmian, guard RC 0). Wniosek metodyczny (klasa): karta tworząca fiksturę w `tests/golden/**` musi uruchomić `port_binding_guard --strict` (agent PARITY-CV3 nie miał tego guarda w definicji ukończenia — dopisany do szablonu kart fikstur).

**Stan bieżący (2026-09-05 ~14:20 UTC, szczyt wypchnięty `51cdb9df`):** na `1fdeec44` Python tests, Frontend checks, P0 Extended, Arch, Docs, Physics Label, SLD Determinism zielone (E2E smoke/full bez biegu — commity `24b045ce`/`1fdeec44` tylko dokumentacja); **Frontend E2E full czerwony w runach 325–330** (`7732c8be` → `92b63674`; ostatni zielony run 324 na `9b78d322`): 5 testów `creator-screenshot` (oze light/dark, wiazania ×3) — trzy klasy naprawione w `51cdb9df` (wiersz E2E-FULL-FIX w §E; lokalnie 99/99 na własnych portach), bieg CI na `51cdb9df` w toku.

**Stan bieżący (2026-09-05 ~15:05 UTC): 9/9 zielone na `51cdb9df`** — Python tests (`33971230192`), Frontend checks (`33971230324`), **Frontend E2E full (`33971230270`, run 331 — pierwszy zielony po naprawie E2E-FULL-FIX, czerwony w runach 325–330)**, E2E smoke (`33971230289`), Docs (`33971230174`), Physics Label (`33971230215`), Arch (`33971230305`), P0 Extended (`33971230274`), SLD Determinism (`33971230188`); na `f85ef79f` (tylko dokumentacja) 7/7 zielone (`33971282500`, `33971282499`, `33971282583`, `33971282555`, `33971282540`, `33971282564`, `33971282572`).

**Stan bieżący (2026-09-05 ~16:55 UTC):** na `fd7cc0e5` (odbiór CV-3.3-B) 8/9 zielone, **Python tests czerwony** (`33974711179`, run 4868: **47 failed / 10895 passed** — `tests/test_archive_diff.py` + `tests/test_incremental_archive.py`, `RunsSection.__init__() got an unexpected keyword argument 'analysis_runs'`): CV-3.3-B skasowała `analysis_runs`/`study_runs`/`study_results` z sekcji archiwum (R2/R3), a dwa pliki testów W KORZENIU `tests/` nadal budowały archiwum starym kształtem. Przyczyna KLASY, nie instancji: regresja lokalna kart CV-3.3-A/A2/B i odbiorów koordynatora biegła na 9 katalogach (`tests/api … tests/ci`) i była w wierszach §E/§F opisywana jako „parytet z CI”, podczas gdy `python-tests.yml` uruchamia `poetry run pytest -q -m "not pandapower"` = CAŁY `tests/` (`testpaths`), łącznie z plikami w korzeniu — deklaracja parytetu bez pomiaru (reguła KLASA NIE INSTANCJA pkt 4). Naprawa u źródła (commit „CI: 47 czerwonych testow archiwum…”): budowniczowie obu testów przepisani na kanon R1 (`runs = {canonical_runs, analysis_runs_index}`, `results` = pusty kontener; testy „zmiana wyników” mierzą odtąd sekcję `runs` — intencja zachowana, komentarz w fikstuze); **117 passed** (oba pliki + `tests/application/project_archive`). Odtąd regresja przedpushowa backendu = pełny `tests/` z `-m "not pandapower"` (jak CI); wpisy „parytet z CI” sprzed tej korekty należy czytać jako „9 katalogów”. Na `c953e831` (FAB-K) biegi CI w toku (Python tests, Frontend checks, E2E full).

**Stan bieżący (2026-09-05 ~17:30 UTC):** `15e8cb0d` **9/9 zielone** (Python tests `33978421691` po naprawie CI-ARCHIWUM, Frontend checks `33978421800`, E2E full run 334 `33978421714`). Na `3d47c275` (CV-4.1) **Python tests czerwony** (`33979330167`, run 4871: **1 failed / 10980 passed** — `tests/golden/parytet_assemblera/test_parytet_assemblera.py::test_parytet_bit_w_bit`: 252/252 hashy INNE niż lokalnie, zero różnic w odmowach); 8/8 pozostałych zielone. Klasa (zmierzona sondą surowego wyniku, nie zgadnięta): `raw_result` niesie liczby, które FIZYCZNIE są zerem albo resztą poniżej tolerancji (`p_from_mw` −6,9·10⁻¹⁶, `branch_current_ka` 2,7·10⁻¹⁷, `i_contrib_a` 9·10⁻¹⁴, `p_injected_mw` 4,7·10⁻⁸ na szynie PQ po zbieżności NR) — ich „9 cyfr znaczących” to w całości szum sumowania BLAS/CPU, inny na każdej maszynie; kwantyzacja do cyfr znaczących (`kwantyzuj_kontrakt`, ADR-018) jest przenośna wyłącznie dla wartości o błędzie względnym rzędu epsilona — założenie M0-2 nie obejmuje zer numerycznych. Harness parytetu scenariuszy (`parytet_scenariuszy`, 28 hashy) hashuje WIDOKI analiz, nie surowy wynik — i przechodzi na CI (runy 4870/4871): tam klasa się nie ujawnia, co jest pomiarem, nie założeniem. Naprawa u źródła (commit „CI: parytet assemblera zalezny od platformy…”): widok parytetu `widok_parytetu` = 9 cyfr znaczących ∧ 9 miejsc dziesiętnych w jednostkach kontraktu + zero bez znaku (parametry w rodzaju `tolerance_used = 1e-8` nietknięte; wartości dużych — bez zmian po kroku cyfr znaczących); test klasy (iloczyn: zero fizyczne × reszta NR × parametr × wartość duża × znak; hash niezmienniczy pod zaburzeniem 3·10⁻¹⁶ względnie); złote hashe przeliczone i **parytet PRZED/PO ponownie udowodniony pod nowym hashem** (zbiór zebrany na `4b65cb14` — krok 0, sprzed wycięcia — z tym samym harnessem == zbiór na szczycie: 252/252, w tym 42 odmowy). Na `68cafc9b` (CV-3.3-B2) Python tests spodziewanie czerwony na tym samym teście — zastąpiony szczytem z naprawą.

**Stan bieżący (2026-09-05 ~18:00 UTC):** na `5793a3e0` Python tests **nadal czerwony** (`33981009770`, run 4873: 1 failed / 10981 passed — ten sam test parytetu, wiele wpisów innych niż lokalnie także pod widokiem 9 cyfr znaczących ∧ 9 miejsc dziesiętnych); pozostałe 8/8 zielone; na `90e3237c` (CV-3.3-C) ten sam stan spodziewany. Wniosek (klasa, nie instancja): surowy wynik solvera NIE jest przenośny między maszynami przy ŻADNEJ kwantyzacji do siatki — układy o złym uwarunkowaniu (Ybus/Zbus z bardzo małymi impedancjami) wzmacniają szum sumowania BLAS/CPU do 10⁻⁸…10⁻⁹ względnie, a każda siatka ma granice; przy tysiącach wartości na bieg jakaś zawsze leży przy granicy. Naprawa u źródła (commit „CI: parytet assemblera - szkielet dokladnie, liczby kontraktu z tolerancja…”): złoty wpis = odmowa (tekst, dokładnie) + hash SZKIELETU wyniku (cały `raw_result` z liczbami zastąpionymi znacznikiem — klucze, statusy, ograniczenia raportowe, identyfikatory, `proof_ref`, długości list; dokładnie) + LICZBY kontraktu (wszystkie liczby zmiennoprzecinkowe poza śladem White Box per węzeł zwarcia: `branch_contributions`, `branch_flow_trace`, `white_box_trace`) porównywane z tolerancją `|a−b| ≤ 10⁻⁹ + 2·10⁻⁶·|a|` względem wartości zapisanych (7 cyfr znaczących). Pomiar: 95 553 liczb kontraktu w 252 wpisach, złoty plik 835 KiB; ślad per węzeł wykluczony z porównania LICZBOWEGO z pomiaru (52 stacje, SC 3F: 549 k liczb / 108 MB JSON na jeden bieg — `branch_contributions` 2 906 liczb/węzeł, `branch_flow_trace` 1 579, `white_box_trace` 628), ale objęty porównaniem STRUKTURALNYM (długości i klucze). Lokalnie determinizm nadal DOKŁADNY (`test_harness_jest_deterministyczny`: szkielet, liczby i ścieżki równe bit w bit; PRZED `4b65cb14` == PO: 252 wpisów identycznych dokładnie, 252/252 w tolerancji). Testy klasy: rozdział szkielet/liczby (iloczyn typów × dict/lista), wykrycie zmiany 10⁻⁴ względnie ze ścieżką, tolerancja szumu 10⁻⁸ i zera → 10⁻¹⁷, inna odmowa, inna struktura, granica tolerancji dokładnie.

**Stan bieżący (2026-09-05 ~18:30 UTC):** na `158d9831` Python tests **czerwony** (`33982599339`, run 4875: 1 failed / 10931 passed — ten sam test), ale meldunek `porownaj_wpis` ROZDZIELIŁ klasy: (a) każdy wpis SC — `szkielet` inny niż lokalnie; (b) PF 52 stacji — 14 liczb ponad tolerancję. Sonda (a): szkielet nie był czysto strukturalny — kroki śladu White Box niosą liczby SFORMATOWANE W NAPISACH (`substitution_latex`: `0.6 \cdot 0.0483606`, `z_tk_formula_latex`, 6 cyfr — ten sam szum w innej postaci), a `proof_ref` = `proof:short-circuit:<sha256 wyniku>` (i `proof_binding.proof_ref`) to skrót pochodny od WYNIKU, więc różny między maszynami dokładnie dlatego, że wynik różni się szumem. Sonda (b): 324 wartości gałęzi PF 52 stacji leżą w przedziale 10⁻¹⁴…10⁻⁶ MW/Mvar (przepływy gałęzi nieobciążonych) — czysty szum, a ATOL 10⁻⁹ leżał poniżej szumu. Naprawa u źródła (commit „CI: parytet assemblera - szkielet bez napisow sladu i skrotow wyniku, ATOL 1e-6…”): w poddrzewach śladu napis → `"<s>"` (klucze, `int`, `bool`, długości list zostają), 64-znakowy heks w napisie → `"<sha256>"` (prefiks `proof:short-circuit:` zostaje — rodzaj i obecność dowodu nadal porównywane dokładnie), ATOL = 10⁻⁶ jednostki kontraktu (1 W / 1 mV / 1 mA; RTOL 2·10⁻⁶ bez zmian); testy klasy rozszerzone (napis w śladzie, skrót w `proof_ref` i w `proof_binding`, szum 3·10⁻¹⁴ na zerze, 99 %/101 % marginesu tolerancji). Złoty plik przeliczony (835 KiB, 95 553 liczb, 252 wpisy / 42 odmowy); PRZED `4b65cb14` == PO (252 dokładnie, 252/252 w tolerancji); gate lokalny zielony. Porównanie liczbowe śladu per węzeł pozostaje poza CI (P1-10: 108 MB na bieg) — parytet śladu jest strukturalny, agregaty kontraktu liczbowe.

**Stan bieżący (2026-09-05 ~19:45 UTC):** na `40e49c22` Frontend checks (`33985176362`) i E2E full (`33985176387`, run 340) **zielone**, Python tests **czerwony** (`33985176363`, run 4876: 1 failed / 10931 passed — ten sam test): **155 wpisów SC** (G00 3, G03 1, G04 74, G05 74, G08 3; PF 0) z innym szkieletem przy ZEROWEJ rozbieżności liczb kontraktu. Sonda lokalna (szum 10⁻¹² względnie + 10⁻¹⁵ addytywnie na `np.linalg.inv`, jedyna algebra liniowa toru SC: `short_circuit_core.build_zbus`, `enm/mapping.py` Z0) ODTWARZA klasę bez zgadywania: G04/00 `branch_contributions` 9 vs 10 i `branch_flow_trace` 6 vs 7 (wpis bilansu KCL przesunięty, `notes` `None`/napis), G00/G08 to samo; poza poddrzewami śladu ZERO różnic. Przyczyna w solverze (FROZEN, B-01 — nietknięty): `_build_branch_contributions_for_*` buduje ślad progiem zerowym na liczbie (`if i_a <= 0: continue`, `if i_mag <= 0: continue`) — gałąź o prądzie DOKŁADNIE 0 na jednej maszynie i 10⁻¹⁷ na drugiej wchodzi do listy albo nie; długość listy, pozycja wpisu KCL i obecność notatek są funkcją szumu, więc zdanie z CI-PARYTET-3 „parytet śladu jest strukturalny” było twierdzeniem bez pomiaru — KOREKTA: struktura śladu też nie jest przenośna. Naprawa u źródła (commit „CI: parytet assemblera - poddrzewa sladu poza porownaniem miedzy maszynami, mapa skrotow poddrzew…”): w szkielecie poddrzewo śladu (`branch_contributions`, `branch_flow_trace`, `white_box_trace`) → `"<slad>"` (`None` zostaje — obecność śladu jest funkcją opcji biegu), maskowanie napisów śladu (`"<s>"`) skasowane jako martwe; lokalnie ślad NADAL porównywany dokładnie (`slad_sha256` — skrót pełnych poddrzew, tylko w pamięci, `test_harness_jest_deterministyczny`). Diagnostyka zamiast zgadywania: złoty wpis niesie MAPĘ SKRÓTÓW poddrzew szkieletu do głębokości 2 (`$.results[7]`, `$.graph.nodes`; 16 hex), `porownaj_wpis` nazywa liście rozbieżności, a test dopisuje do meldunku szkielet pierwszego rozbieżnego poddrzewa (≤ 16 kB) — kolejny czerwony CI mówi CO i GDZIE, nie tylko „hash inny”. Testy klasy: iloczyn {długość listy, notatka `None`/napis, pozycja wpisu KCL} × {ślad, kontrakt} — ślad: szkielet, mapa i liczby równe, `slad_sha256` inny; kontrakt (`contributions` +1, `dopuszczalnosc_raportowa`, ślad `None`): szkielet inny ze ścieżką `$.results[0]`; wpis bez mapy → „brak mapy skrótów”. Złoty plik przeliczony (1213 KiB, 95 553 liczb, 8 836 skrótów, 252 wpisy / 42 odmowy); PRZED `4b65cb14` == PO (252 dokładnie, 252/252 w tolerancji); gate lokalny zielony. Zakres regresji tego commitu: `tests/golden` + testy własne (harness importowany wyłącznie przez własne testy, `regeneruj.py` i `tests/enm/test_wyniki_finitowe.py::_bieg` — pomiar grepem), pełny `tests/` biegnie na kolejnym szczycie (odbiór CV-4.2).

## B. Ochrona gałęzi `main` (§36) — **OWNER ACTION P0**

Pomiar (GitHub API `list_branches`, 2026-09-04): `main` → `protected: false`; łącznie ponad 400 gałęzi (`claude/*`, `codex/*`, `kopia/*`), żadna chroniona. Sesja agenta nie ma uprawnień administracyjnych do włączenia ochrony (brak narzędzia w MCP; wymaga roli admin repozytorium).

**Dokładne działanie właściciela (Settings → Branches → Add branch ruleset / protection rule dla `main`):**
1. Require a pull request before merging (min. 1 approval; dismiss stale approvals on new commits).
2. Require status checks to pass before merging — wymagane checki (nazwy jobów): `pytest` (Python tests), `frontend` (Frontend checks), `V12K Extended Invariant Guards` (P0 Extended Guards), `SLD Guards (Python)` i `SLD Contract Tests (Vitest)` (SLD Determinism), `Docs Integrity Guard`, `Architectural And Repo Hygiene Guard`, `Physics Label Guard`, `full-real-backend-e2e` (Frontend E2E full) oraz smoke e2e; „Require branches to be up to date before merging".
3. Require conversation resolution before merging.
4. Block force pushes; block deletions; „Do not allow bypassing the above settings" (dotyczy także administratorów).
5. Opcjonalnie: require linear history.
Do czasu wykonania: każdy merge do `main` bez zielonego kompletu bramek jest naruszeniem kontraktu (§36) — odnotowane jako P0 w §H.

## C. Determinizm numeryczny (§35)
- Polityka: `docs/architecture/CANONICAL_TWIN_ARCHITECTURE.md` §C.5. Kod: `backend/src/application/analyses/kontrakt_liczb.py` (tryb ścisły: NaN/±inf/complex → `KontraktNiefinitowyError` ze ścieżką pola).
- Dowód: `tests/application/analyses/lv_domain/test_kwantyzacja_kontraktu.py` — fixtury × ±1 ULP (18 fixtur), własności, klasa równoważności 1e-12 vs 1e-8, porządek kluczy/list, niezależność od skali jednostki, `int`/`bool` nietknięte; pakiet `lv_domain` zielony lokalnie (wynik w §F). 18/18 fixtur nN: 0 wartości niefinitowych.

## D. Definition of Done — program (§40)

| # | Kryterium | Stan | Dowód / wycinek |
|---|---|---|---|
| 1 | Project posiada jeden Canonical Twin | NIE — ENM per `case_id` | CV-1 |
| 2 | ENM nie jest kopiowany per StudyCase | NIE | CV-1 |
| 3 | Assets mają stabilną tożsamość | CZĘŚCIOWO — `ref_id` stabilne; tłumaczenia uuid5 w 4 przestrzeniach | CV-4 (T-4) |
| 4 | Terminal/connectivity model jest fundamentem | NIE — `Port*` metadane bez egzekwowania | CV-5 (T-2) |
| 5 | Phase model nie blokuje ABCN ani earth-fault | NIE — brak faz, N/PEN/PE | CV-5 (F-1, F-2) |
| 6 | Grounding jest first-class | NIE — 6 reprezentacji, fizyka nie czyta | CV-5 (F-3) |
| 7 | Revision / Variation / Scenario / StudyCase rozdzielone | NIE — 6 modeli scenariusza, 4 delty in-memory | CV-3 |
| 8 | EffectiveSnapshot immutable i reprodukowalny | CZĘŚCIOWO — `CanonicalRun.snapshot` zamrożony, bez scenariusza | CV-3 |
| 9 | Topology derived | CZĘŚCIOWO — union-find w Ybus; 20 implementacji | CV-4 |
| 10 | Jeden canonical computational boundary | NIE — 10 builderów PF / 7 SC | CV-4 |
| 11 | `network_model` nie konkuruje z ENM | NIE — legacy ORM `network_*` żywy (wizard, XLSX) | CV-4 (procedura kasacji) |
| 12 | Solver input ma pełny provenance | CZĘŚCIOWO — `input_hash`, `snapshot_hash`; pola stałe w `build_analysis_run_reproducibility` | CV-2 |
| 13 | ResultSet posiada RevisionEnvelope | NIE | CV-2 |
| 14 | Protection settings — jedno ownership | NIE — nastawy per case i w modelu | po CV-5 (ADR-022) |
| 15 | SLD jest projekcją tego samego stanu | CZĘŚCIOWO — nN 3.0.0 tak; SN: klient liczy topologię | CV-4 → SLD |
| 16 | SN i nN bez konkurencyjnych modeli | CZĘŚCIOWO — jeden graf; nN miało równoległą prawdę (A1-08) | CV-5 |
| 17 | White Box odtwarza pełną drogę wyniku | CZĘŚCIOWO — ślady inline; `trace_v2` martwe | CV-4 |
| 18 | Reference Networks mają niezależne wyrocznie | CZĘŚCIOWO — 12 benchmarków IEEE/CIGRE (pandapower) tak; reszta REGRESSION_ONLY | rejestr §2 |
| 19 | CI wiarygodne | **DONE** — 9/9 zielone na `ef9d6790`/`e2a0dc17` (§A stan końcowy); każdy wcześniejszy czerwony sklasyfikowany i naprawiony u źródła (stale parser, martwy test, zapadka typów 658 → 137, 11 podstawień, regresja gotowości po #472, klasa ładunku per gałąź 105 MB), zero pominięć | §A |
| 20 | Main chroniony lub jednoznaczne owner action | OWNER ACTION zapisane | §B |
| 21 | G01 przechodzi end-to-end | NIE — G01 nie istnieje jako jedna sieć | CV-6 |
| 22 | Future Capability Review potwierdza fundament | TAK (projektowo) — 16 decyzji, 4 z warunkiem | `docs/architecture/DECISION_FREEZE_REGISTER.md` |
| 23 | Brak równoległych trwałych subsystemów legacy | NIE — inwentarz w `CANONICAL_TWIN_ARCHITECTURE.md` §B.2, `CANONICAL_TWIN_ARCHITECTURE.md` §C.2.3 | CV-1…CV-4 |
| 24 | Każda usunięta ścieżka ma guard przeciw wskrzeszeniu | CZĘŚCIOWO — `legacy_public_path_guard`, `verification_phantom_paths_guard` istnieją | per wycinek |

## E. Karty w toku (delegowane; model wykonawczy: `AGENTS.md` §8)
| Karta | Zakres | Wykonawca | Stan |
|---|---|---|---|
| CI-A | `solver_input_substitute_guard` 11 naruszeń u źródła | agent (worktree) | **odebrana** (`06a9e0f1`): pola wymagane odcinka nN przez `_wymagane_pola_odcinka` (jawny błąd `nn.segment_field_missing`), merge odrzuca różne parametry na km (`nn.merge_segments_type_mismatch`), `order` sekcji wymagany, `liczba_torow()` jako jedna definicja neutralnego elementu (Cable/TR/Generator/fault_loop), zapadka `c_factor` usunięta, 2 moduły dopisane do `CONTRACT_SOURCES`; guard RC=0 (1868 pól), testy własne 44, pełny pytest 10538 w worktree |
| CI-B | tsconfig gate 658 → ≤ 531, wyciszenia 39 → ≤ 35 | agent (worktree) | **odebrana** (`9b96ebcc`): dług typów poza bramką 658 → **137** (budżet 137), wyciszenia 39 → **1** (budżet 1; jedyne pozostałe dokumentuje, że format spoza rejestru eksportu nie przechodzi ani typu, ani wykonania), 0 błędów w bramce — wszystko u źródła: brak `import { describe, it, expect } from 'vitest'` (259 błędów w 3 plikach), martwy `@ts-expect-error` na shimie `URL.createObjectURL` (38 błędów = 38 wyciszeń, TS DOM lib zna te metody), fikstury niezgodne z kanonem typu (`EnmFragment.sources`, `StationTransformerUnit` 7 pól tabliczki, `ReadinessEntry.severity`, `KontekstJakosci` → `case_id`+`snapshot_hash`+`run_id`, sygnatura `derFixture`), `Array.prototype.at()` pod zamrożonym `lib: ES2020` (`src/test/arrayAt.ts`), 1 addytywne poszerzenie typu produkcyjnego `TraceValue` (wariant zespolony `re`/`im`, który `ElementCalculationProofPanel` już konsumował) z jawnym guardem `undefined` w `dowodModel.ts`; weryfikacja niezależna Fable: vitest 48 plików / 1039 testów zielone, guard RC=0 na gałęzi |
| CI-C | SLD `vertical_length_probe` z dowodem semantycznym | agent (worktree) | **odebrana** (`ee0ec472`), CI zielone |
| CI-D | 12 czerwonych speców e2e (pre-existing od #472) | agent (worktree) | **odebrana** (`b349377b`): 11/12 — jedna przyczyna produktowa (`enm/domain_operations.py:2141` `_build_readiness` nie znał wyjątku migracji nN, który `ENMValidator` liczy jako W061) + 2 testy regresyjne; 3 specy z przestarzałymi założeniami fixture/kanonu (opt-in `station_auxiliary`, `nn_earthing` przed LF, KD-8 zależność kanwy od motywu) przepisane z dowodem; 12. spec = P0-5 (wydajność zwarć), nie naprawiony i nie maskowany |
| FAB-A | fikcyjny katalog przekaźników `ACME_REX*` z `vendor: ABB` serwowany jako katalog (A6-17, D-33) | agent (worktree) | **odebrana** (`f49d7f18`): 5 urządzeń → identyfikatory referencyjne bez marki `REF-OC-100/200`, `REF-OC-EF-300/500/700`, szablony `template_ref_oc_*`, `vendor`/`series` = `None` (pole `DeviceCapability.vendor` → `str | None`, wyrównanie do istniejącego `ProtectionDeviceType.vendor`), `name_pl` „Profil referencyjny (nie produkt producenta)…" zasila wszystkie UI; proweniencja przez istniejące `REFERENCYJNY`/`REFERENCYJNY_V1` (bez nowego mechanizmu); `devices_v0.json` przepisany skryptem na strukturze JSON z dowodem semantycznym (pola liczbowe 5 rekordów identyczne, 46 rekordów bit-identycznych); 39 plików; luka wzorca karty (`rex100` bez granicy słowa) wykryta czerwonym testem i domknięta; PTPiREE `T-REX-*` = realny produkt (nietknięte). Weryfikacja: pełny pytest 10513 passed w worktree (agent); na gałęzi (Fable): pytest `network_model/catalog`+`protection`+`api`+`golden`+`proof_engine`+6 plików `enm` 2389 passed, vitest 3 pliki 50 passed, eslint, ruff, black, 14 guardów RC=0, `test_no_codenames_guard` 37 passed. Znaleziska innej klasy: (1) picker `KreatorPrzekaznika.tsx` oferuje katalog analityczny (51), a `add_relay` przyjmuje tylko kanoniczny (12) → **FAB-F F6** (zawężenie pickera do czasu konwergencji katalogów); (2) `devices_v0.json` niesie DWA schematy ID dla realnej rodziny Elektrometal e2TANGO (`EM_ETANGO_*` 7 i `EM_E2TANGO_*` 5, nakładające się 600/800/1000) → §H P1-5 |
| FAB-B | fantom nastaw `SldDetailDrawer` → dane z modelu / uczciwy stan zerowy | agent (worktree) | **odebrana** (`021423bf`): 3 bloki (DER 6 fikcyjnych progów ANSI, pole 5 wierszy, aparat 3 nastawy) → `ElementProtectionFunctionsPanel` na tej samej ścieżce danych co widok zabezpieczeń (`useProtectionAssignment` → `GET /enm/protection-view`); 6 testów klasy (pozytywne + negatywne bez liczb z jednostkami); 72/72; guardy zielone |
| CV-1-G | guard `enm_store_key_guard` (klucz przypadku w API/aplikacji = naruszenie; zapadka mierzona) + krok CI | agent (główny checkout, bez commitu) | **odebrana** (`4ab4baec` CV-1-W na gałęzi: zapadka 18 plików/74 → **1/1**; `26542b66` ADV-CV1; krok w `p0-extended-guards.yml`; stan na `24b045ce`: 425 plików w zakresie api/application/enm, PASS, zapadka 1 plik / suma 1). Korekta wiersza 2026-09-05 ~11:30 UTC — stan „w toku" był nieaktualny od pushu `98ad6b6a` |
| FAB-C | zaszyte dane katalogowe w zakładce „parametry” kabla (`XRUHKXS 1×120`, `120 mm²`, `Al`, `270 A`, `PN-HD 620 S2`) | agent (worktree) | **odebrana** (`ff394efb`): 5 literałów → pola modelu (`catalog_ref`, `cross_section_mm2`, `conductor_material`, `rating.in_a`, `insulation`) z jawnym „Brak w modelu”; wiersz „Norma” usunięty (ENM nie niesie normy konstrukcyjnej kabla — fabrykacja, nie luka); izolacja linii napowietrznej „Nie dotyczy (przewód goły)”; oba miejsca budowy `cableRunSpec` naprawione jedną funkcją (`extractCableCatalogSpec`), v3 reużywa ten sam komponent; parytet pól TS↔backend potwierdzony; 3 testy (komplet danych / brak danych / linia napowietrzna); weryfikacja: vitest `sld`+`sld-editor` 263 plików / 4988 testów, type-check, lint, 8 guardów UI RC=0 (agent) + vitest 75, eslint, 5 guardów, gate RC=0 na gałęzi (Fable). Znaleziska innej klasy → karta FAB-F: `SldTitleBlock.tsx` `DEFAULTS` z kryptonimem „K30” i zmyślonymi danymi projektu (guard kryptonimów łapie tylko `P<cyfry>`), `DER_CATALOG_OPTIONS` 18 zaszytych `catalog_ref` poza katalogiem backendu, unia `ParameterSource` bez `MANUAL_EQUIVALENT` (guard parytetu nie porównuje literałów unii) |
| PERF-0 | baseline wydajności B1–B10 na sieciach rejestru (S: G02/G03/G08, M: G13/G00; L brak) — pomiar albo NIEMIERZALNE z powodem | agent (główny checkout, bez commitu) | **odebrana** (`47d809a9`): `backend/scripts/benchmark_baseline.py` + `tests/perf` (7 testów dymnych, zero asercji na czasy) + `docs/evidence/PERFORMANCE_BASELINE.md` / `performance_baseline.json` z realnego biegu (5 powtórzeń, G00: 1); przekroczenia: **B4 zwarcia G00 376 794 ms przy budżecie 1 s (377×)**, B3 rozpływ G00 5 100 ms przy 200 ms, B1 topologia G00 110 ms przy 30 ms, B8 projekcja nN 18–875 ms przy 15 ms (18/18 scenariuszy), B2 G03 30 ms/20 ms, B3 G02[1] 55 ms/50 ms, B4 G08 168 ms/100 ms; NIEMIERZALNE z powodem: B5 (solver 4-przewodowy nN nie istnieje, ADR-021 PROPOSED), B7/B9 (kod klienta), B2 na G00 (`create_run` odmawia — 21 BLOCKER); sieć L NIE ISTNIEJE (jawnie w JSON i MD); weryfikacja Fable: ruff/black RC=0, pytest `tests/perf` 7 passed |
| SUB-52s | obliczalność substratu 52 stacji (G00): 20× E063 (brak układu uziemienia nN), 1× E003 (wyspa po otwartym NOP na promieniowym odgałęzieniu) | agent (worktree) | **odebrana** (`536b0e11` = cherry-pick `42649500`, fikstury zregenerowane na scalonej gałęzi w `2024fb4b` SLD-LOC, hash `bc10e5ac`; push `98ad6b6a`; korekta wiersza 2026-09-05 ~11:30 UTC — stan „wstrzymana" był nieaktualny): E063 przez istniejący kanał `nn_earthing.lv_system` (TN-C-S) w obu ładunkach stacji; E003 przez `connect_secondary_ring_sn` między końcówkami dwóch odgałęzień (NOP zostaje otwarty jako rezerwa — wykrywanie osieroconego odgałęzienia z grafu, nie z UUID); 21 → 0 blokerów, hash G00 i fikstury zregenerowane skryptami repo (`sldNetwork53.ts`, `sldSubstrate52s.enm.json/.powerflow.json`), atrybucja geometrii 100% do gałęzi pierścienia; backend 147 passed, `accept:sld-v3` RC=0, vitest 1552/1553 — 1 test celowo czerwony: `buildScene.p1Recenzja` kontrakt lokalności (dodanie 1 stacji przesuwa PIONOWO 94/106 kotwic przy niezmienionym podziale wierszy — defekt SILNIKA układu przy pierścieniu) → karta SLD-LOC; scalenie SUB-52s po SLD-LOC, żeby gałąź nie była czerwona. Znaleziska innej klasy: P1-6 (GPZ `hv_neutral` gubione w budowniczym), P1-7 (walidator O(N³)) |
| FAB-D1 | ciche podstawienia liczb na ścieżce operacji domenowych ENM (klasa A6-12 cz. 1) | agent (worktree) | **odebrana** (`ab0c5e93`): naprawione u źródła D2 (`add_transformer_sn_nn` i TR blokowy DER przez wspólny `_require_transformer_fields` → `transformer.field_missing`), D3 (`_resolve_converter_defaults`, genset, UPS → `generator.power_missing`, `power_factor or 0.8` → `generator.power_factor_invalid`), D4 (LDC: blok tylko przy obu wartościach), D5 (`add_nn_load`, potrzeby własne → `load.q_missing`), D6 (`length_m` → `nn.segment_field_missing`), D8 (`DEFAULT_LOAD_KW` usunięte → `load.p_missing`, `except Exception` zawężony, porty z typu → `catalog.ports_missing`), D9 (`BayMeasurements(frequency_hz=50.0)` → pusty zestaw), `mapping.py:283` sentinel → jawny warunek + ślad; kody zarejestrowane; zapadka `solver_input_substitute_guard` obniżona o 6 pozycji; 20 plików, nowy `tests/enm/test_fab_d1_ciche_podstawienia.py`; agent: pytest enm/api/application/invariants/golden **4804 passed**, 9 guardów RC=0; fikstura `sldNetwork53.ts` zregenerowana (`q_mvar 0.0 → null` bez `q_min_mvar`, dowód dump przed/po). **Dwie klasy świadomie zostawione z pomiarem** → karty: (1) **FAB-G** tabliczka GPZ WN/SN z literałów (`hv_voltage_kv or 110`, `25 MVA`, `12 %`, `120 kW`, `25 kW`, `0,2 %`, `YNd11`) — proste odrzucenie = 517 czerwonych testów (każda sieć testowa zaczyna od `add_grid_source_sn`); decyzja: typ katalogowy obowiązkowy jak w PowerFactory, testy przez wspólne fabryki ładunku; (2) **FAB-H** `k_sc` falownika 1,1 (katalog nie niesie pola żadnym torem; `InverterSource.k_sc` w `core` z własnym 1,1) — decyzja: pole katalogowe + 1,1 jako ZAREJESTROWANE założenie (ślad White Box + WARNING `inverter.k_sc_assumed`), plus klasa „Q wytwórcy → 0" w trzech plikach (`mapping.py:589`, `canonical_analysis.py:1637`, `v126_contracts.py`) — jeden predykat, jeden kod `generator.q_missing` |
| FAB-D2 | ciche podstawienia na ścieżce katalog → materializacja → kontrakt (klasa A6-12 cz. 2) | agent (worktree) | **odebrana** (`eefab9a0`, cherry-pick z jednym konfliktem w `api/catalog.py` złożonym z FAB-A: producent pusty = profil referencyjny + `dopasowanie`/`certyfikat_ptpiree` zamiast `confidence`): D2 `i0_percent/p0_kw/vector_group` Optional (WARNING `transformer.no_load_params_missing`, BLOCKER `transformer.vector_group_missing` dla SC_1F), `V126TransformerInput.p0_kw` Optional z bramką w `api/v126_academic.py` (solver FROZEN nietknięty), D3 `_generator_q_mvar_jawne` → BLOCKER `generator.q_missing` (chyba że karta niesie Q-set-point), D4 jeden wyjątek `BrakujacePoleIRError` + moduł `network_model/ir_fields.py` dla `from_dict` w `core/*` i `catalog/types.py` (81 trafień), D5 `impedance_override.incomplete`, D6 `catalog.type_incomplete` + `control_mode` → `None` (emiter dla wcześniej zarezerwowanego `pv.control_mode_missing` — rezerwacja zdjęta), D7 nieznany status → `ValueError`, D8 profil DER: nieznany rodzaj → `der.dynamic_profile_missing` (BLOCKER), domyślny → `der.dynamic_profile_default` (WARNING z proweniencją), D9 API `dopasowanie: PELNE|CZESCIOWE` + `certyfikat_ptpiree` (snapshot OpenAPI przeliczony), uzupełnienie #1 `semantic.transformer_bus_voltage_missing`; dwie regresje wykryte i naprawione w karcie (`CatalogBinding.from_dict` łagodny parsing zachowany — walidacja powiązania ma własny mechanizm; `TransformerBranch.validate` z `None`); 33 fikstury uzupełnione realnymi danymi (w tym V12.6 przeliczone z tabelą przed/po). Świadomie zostawione z uzasadnieniem: `v126_contracts.py:425` `generator.q_mvar or 0.0` (konsument w solverze FROZEN zasilającym wiele analiz — B-01, dług nazwany w zapadce), `der_dynamic/models.py:125` (0 s inercji wirtualnej = fizycznie poprawne dla GFL), `mv_cable_line_catalog.py` (martwe: filtr przed użyciem), `ParameterSource.INSTANCE` (2 konsumenci produkcyjni, jawna proweniencja). Agent: pełny pytest **10711 passed**, mypy 0 (799 plików), guardy RC=0; na gałęzi: pełna weryfikacja (guardy CI komplet, pełny pytest, mypy, frontend) — liczby w §F |
| FAB-E | brak wyniku solvera pokazywany jako 0 (A6-12 część 3): 345 trafień `.get(pole, 0.0)`/`or 0.0` w API, raportach PDF/DOCX, dowodach, porównaniach, emiterach śladu, fikstur sieci referencyjnych | agent (worktree na `ff394efb`, `karta_fab_e.md`) | **odebrana** (`27e8a44b` cherry-pick `d1493111` z 5 konfliktami: 2 guardy = wersja gałęzi, 2 importy = unia, 1 test przepięty na realny projekt + klucz twin; `0d549d4a` korekta scalenia): 345 + dodatkowe trafienia tej samej klasy w ~60 plikach zaklasyfikowane per trafienie (a: fabrykowane zero → naprawione; b: zweryfikowany bezpieczny domysł; c: fałszywy pozytyw skanera; E6: rdzeń zamrożony/legacy do kasacji w CV-4 — policzone); nowe moduły `network_model/reporting/missing_value.py` (`format_wynik` „brak danych" w PDF/DOCX), `application/trace_emitters/wynik.py` (`BRAK_DANYCH`), `application/reference_networks/wymagane.py` (`pole_wymagane`, `ReferenceFixtureError`); komparator: nowy status **NIEPORÓWNYWALNY** zamiast FAIL od fabrykowanego 0,0; 5 rodzin dataclass wyników i 5 modeli API porównań → `| None`; mapy `ISSUE_SEVERITY_MAP.get(code, INFORMATIONAL)` → subskrypcja wprost + test kompletności; dowód PF: `ValueError` zamiast fabrykowanego `1.0 pu`. **Korekta klasyfikacji agenta (Fable):** 8 trafień `api/catalog.py` (auto-populate: `params.get(pole, 0.0)` dla mocy/napięć/przekroju/prądu) zakwalifikowanych jako „rdzeń zamrożony" to końcówka API — pozycja katalogu bez pola liczbowego była traktowana jak pozycja o wartości 0 („Sn=0 kVA" w propozycjach); naprawione w `0d549d4a` (pozycja pomijana z nazwanym powodem w logu, test iloczynu cech transformator×kabel×aparat). Pozostałe E6 policzone przez agenta (`power_flow_input_builder.py` 4, `network_wizard/service.py` 7, `analysis_run/service.py` 12, `results_inspector.py` 1) = tor legacy P2–P12 do kasacji (CV-4). Weryfikacja: agent pytest 5409 passed / 7 skipped; Fable w worktree (na `26542b66`): black/ruff src+tests RC=0, mypy 0 (804 pliki), pytest 11 katalogów **7319 passed** (6 czerwonych = `create_run` bez `klucz_twin` w testach karty sprzed CV-1-W → naprawione; 1 = rozjazd fixtury `sldNetwork53.ts` naprawiony na gałęzi w `2024fb4b`), snapshot OpenAPI przeliczony (poszerzenie pól delt porównań do nullable — świadoma zmiana kontraktu, zero zmian ścieżek). **Domknięcie na gałęzi (`98ad6b6a`):** pełny zestaw guardów CI dał dwie czerwienie po scaleniu — (1) `solver_input_substitute_guard`: 259 wpisów „dług ZMALAŁ" (karta usunęła podstawienia, nie obniżyła zapadki, bo jej baza była sprzed GUARD-SUB) → zapadka przeliczona programowo z zachowaniem powodów per plik: **89 plików / 592 → 61 / 320**, wykluczenia 24/64 → 15/46, pin testu własnego przepisany; po obniżce **3 realne naruszenia** w `api/canonical_run_views.py`: karta zamieniła `.get(k, 0)` na `.get(k) or 0` / `or 100.0` (moc bazowa 100 MVA fabrykowana w widoku interpretacji rozpływu) i zostawiła `max_mismatch_pu=0.0` („idealna zbieżność"), a ta sama forma w `energy_validation/service.py` (odczyt do zmiennej lokalnej, poza zasięgiem guarda) — naprawione jedną klasą: skalary biegu z artefaktu przez istniejącą warstwę wiązania (`power_flow_binding.skalary_wyniku_rozplywu` / `_lub_brak`), niedopasowanie z kroku końcowego śladu (`max_mismatch_ze_sladu`, upubliczniona) albo jawny brak, `analysis.PowerFlowResult` skalary `| None`; (2) `ui_production_codes_guard`: komunikat adaptera konwencji mocy niósł tokeny kontraktu (`branch_results`, `branch_id=`) → semantyka po polsku. Weryfikacja: oba guardy PASS, 54 testy własne, mypy src 0 (807 plików), pytest widok/walidacja energii/proof_engine/analysis/porównania/wiązanie 998 passed, 12 guardów generycznych RC=0. **Znalezisko klasy:** guard podstawień nie widzi wzorca „odczyt pola do zmiennej lokalnej → podstawienie" (`x = d.get("pole"); int(x) if x is not None else 0`) → karta GUARD-SUB-2 (zlecona) |
| FAB-F | metryka rysunku `SldTitleBlock` v2 z kryptonimem i zmyślonymi danymi projektu; guard kryptonimów bez klasy `K<cyfry>`; picker DER z 18 zaszytymi `catalog_ref`; parytet literałów unii; picker przekaźników poza bramą `add_relay` | agent (worktree, przebazowana na `92310661`) | **odebrana** (cherry-pick `bbe3ea9f` — weryfikacja na gałęzi: patrz §F): F1 `SldTitleBlock` v2 USUNIĘTY (zero konsumentów produkcyjnych; typ przycięty do 5 realnie używanych pól jako `OsdDrawingMetadata` w `ui/reports`); F2 `no_codenames_guard` + `export_codenames_guard` łapią `K<cyfry>` (K0 bez wykluczenia — pomiar), 8 trafień backendu (`step_controller` mapa etykiet PL, `generator_validation`) + ~5 frontu + 128 opisów testów z prefiksem „K30" naprawione; cytowania bramek K1–K13 w `describe/it` = jawny wyjątek `GATE_CITATION_TOKENS` (ta sama konwencja co F6d/S9-6/M0-1; pomiar 305 wystąpień w 109 plikach, tylko skan frontu); testy własne guarda 24 → 55; F3 `DER_CATALOG_OPTIONS` → katalog backendu (`fetchDerConverterTypes`, filtr nN/SN po `un_kv`), **9/18 zaszytych `catalog_ref` nie istniało** (backend nie ma przekształtników poniżej 1 kV); regresja własna wykryta pełnym vitestem (zapytanie katalogu przy każdym renderze) naprawiona (`enabled` po zakładce); F4 `enm.ts`: `MANUAL_EQUIVALENT`, 6 pól `ShuntCapacitor`, 4 wartości D10 `function_type`; **guard parytetu rozszerzony o literały unii i naprawiony** (12 pól dziedziczonych z `BranchBase` było cicho pomijanych — nowy `tests/ci/test_enm_contract_parity_guard.py` 18 testów; 115 pól Literal, 0 rozjazdów) + 4 błędy konsumenckie (`PARAMETER_SOURCE_LABELS_PL`, `REQUIRED_INPUTS_BY_FUNCTION` per funkcja zamiast heurystyki `startsWith`, `insulationLabel` bez `EPR`); F6 `KreatorPrzekaznika` → katalog kanoniczny (12) z testem parytetu z bramą `add_relay` i stanem pustym. Agent: type-check, lint, `guard:codenames`, 9 guardów, `tests/ci` 222, black/ruff (src, tests, scripts) RC=0; vitest 365 plików / 5940 testów — 1 czerwony pre-existing `kosztSceny.test.ts` (próg 3,05 s vs < 3 s pod obciążeniem; izolowany rerun 8/8) → **P1-8**. **KOREKTA 2026-09-05 (E2E-DER):** twierdzenia F3 „9/18 zaszytych `catalog_ref` nie istniało” i „backend nie ma przekształtników poniżej 1 kV” są **fałszywe** — pomiar `get_default_mv_catalog()`: 18/18 identyfikatorów istnieje, katalog `converter-types` ma 66 pozycji PV (w tym `conv-pv-nn-*` 0,4–0,8 kV), a zbiory PV/BESS są identyczne z `pv-inverter-types`/`bess-inverter-types`; usunięcie listy było słuszne z powodu DUPLIKACJI katalogu, nie fantomów. „Regresja własna naprawiona (`enabled` po zakładce)” wprowadziła regresję krytycznej ścieżki (zapis DER bez wejścia na „Falownik” niemożliwy) — naprawiona klasowo w `8bb6589a`; wniosek metodyczny: agent nie uruchomił e2e i przyjął pomiar katalogu bez dowodu |
| SLD-LOC | lokalność pionowa kotwic stacji przy sieci z pierścieniem: +1 stacja na ogonie magistrali przesuwa Δy 94/106 kotwic przy identycznym podziale wierszy (kontrakt WYTYCZNE §9) | agent (worktree na gałęzi + SUB-52s, karta w prompcie) | **scalona** (`536b0e11` SUB-52s cherry-pick `baf4183e`, `caa92911` SLD-LOC cherry-pick `098f76ab`, `2024fb4b` fixtury substratu 52s zregenerowane na scalonej gałęzi — konflikt `source_hash` w `sldNetwork53.ts`: FAB-D1 i FAB-G zmieniły materializację po bazie karty; hash 14047bcd → **bc10e5ac**, rozpływ zbieżny, 315 szyn zasilonych, 0 bez zasilania, 1 punkt otwarty); przyczyna wg agenta: JEDEN globalny licznik kodów zastępczych stacji dzielony między ciągami (`enmToSldAdapter.buildStations`), naprawa: baza numeracji per ciąg z indeksu strukturalnego × rozmiar bloku (`max` liczby stacji ciągu, podłoga 10) — macierz L4 9/9 kombinacji `pionowe=0`, przed naprawą 94/106 pionowych; baseline z atrybucją: `totalVerticalSegmentLength` 20936/43912/43912 → 21040/44016/44016, 6 odcisków `kosztSceny`, `obrysArkusza` S51–S53 → S109–S111, asercja S9-1 poprawiona (rozróżnia przepakowanie WEWNĄTRZ edytowanego ciągu od wycieku do innego). Agent: vitest `v3`+`engine/sld-layout`+`v2` 246 plików / 4755, `accept:sld-v3` ALL PASS, type-check, lint, `sld_determinism_guards` 0 naruszeń, `kosztSceny` izolowany 8/8 (próg P1-8 NIE podniesiony). Fable na gałęzi (fixtury bc10e5ac): vitest `v3`+`engine/sld-layout`+`v2` **245 plików / 4746 passed** (macierz L4 9/9 `pionowe=0` na zregenerowanych fixturach, pinowane baseline'y agenta trzymają bez zmian), `accept:sld-v3` ALL PASS, tsc 0, eslint 0, `sld_determinism_guards` 0 naruszeń; backend `enm`+`invariants`+`api`+`application`+`golden`+`reference_networks`+`infrastructure` 5003 passed. Znalezisko poza kartą: drugie odgałęzienie na stacji z jednym daje `crossingCount=2` niezależnie od hosta (`computeRowChannelPlan` per wiersz, nie per stacja) — osobna karta routingu pól |
| CV-1-W | wiring: wszyscy konsumenci `get_enm/set_enm/has_enm/restore_enm` w `api/` i `application/` na klucz twin projektu przez JEDEN tłumacz (`api/klucz_twin_dep.py`) | agent (worktree, 121 plików) | **odebrana** (`a71bd91c` cherry-pick z 1 konfliktem: identyfikator referencyjny FAB-C vs sygnatura helpera; `4ab4baec` korekty scalenia: 5 czerwonych po pełnym pytest na gałęzi — snapshot OpenAPI po `KluczTwin` przeliczony skryptem, test FAB-D1 i 3 testy FAB-D2 na realny projekt+przypadek; zapadka `enm_store_key_guard` 18 plików/74 → 1/1 (jedyny zastany: zapis tymczasowy importu archiwum ZIP z wieloma snapshotami), test własny guarda przypięty). Decyzje agenta przyjęte: `analysis_dispatch` klucz z `project_id` (id przypadku operacyjnego ≠ StudyCase — realny defekt), archiwum ZIP: eksport modelu projektu pod każdym `case_id` (schemat addytywny), import: pierwszy wpis = model projektu, pozostałe przez `migruj_klucz_przypadku_do_projektu` do `legacy_przypadki/`; `case_id` niepoprawny → 404 (jedno miejsce tłumaczenia). Agent: pełny pytest 10590 passed; na gałęzi 10759+5 naprawionych → §F |
| WB-2 | jedna funkcja rozpakowania wartości śladu WHITE BOX (trzy niezależne kopie) | agent (worktree, `karta_wb2.md`) | **odebrana** (`08ba0f4e`): inwentarz klasy wykazał **5** miejsc, nie 3 (`skladowe/model.ts::naZespolona`, `proof/export/exportTracePdf.ts::formatValue`) — wszystkie na `results-inspector/traceValue.ts::rozpakujWartoscSladu`, 32 testy iloczynu cech; na gałęzi vitest 35 plików / 457 testów, type-check, lint RC=0 |
| E2E-FIX | `critical-engineer-flow.spec.ts` asertował usunięte przez FAB-D2 pole `confidence` (E2E full czerwony na `92310661`) | Fable | **naprawione** (`40fa31bf`): ranking sprawdzany kluczem sortowania backendu (`dopasowanie` PELNE/CZESCIOWE, `certyfikat_ptpiree`); lokalnie 8/8 z realnym backendem. Wniosek metodyczny: karty zmieniające kontrakt API muszą grepować `frontend/e2e/**` (e2e full nie biegnie u agentów); lokalny e2e wymaga `PYTHONPATH=<backend>/src` i `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH`, a PRZED uruchomieniem ubicia zastanych serwerów (`uvicorn`/`vite` z cudzych worktree na 8000/5173 — Playwright `reuseExistingServer` testował STARY kod) |
| E2E-DER | E2E full czerwony od FAB-F (`2239ce9c`, biegi 304–307): `critical-der-config.spec.ts` — zapis DER z zakładki „Moc” nigdy nie wysyłał `POST …/generators` (15 s bez żądania w logu backendu) | Fable | **naprawione** (`8bb6589a`) — przyczyna: szuflada pobierała katalog przekształtników leniwie (tylko na zakładce „Falownik”) i CICHO podstawiała pierwszą pozycję, więc wartość była pusta, a komunikat wskazywał niewidoczną zakładkę; CV-1-W niewinna. KLASA „cichy domyślny `catalog_ref`” domknięta w 4 miejscach: szuflada (wybór JAWNY z pozycją zastępczą, stan haka kluczowany parą technologia×wariant, zmiana pary czyści wybór, błąd prowadzi do zakładki z polem — reguła dla całej klasy pól), backend `api/generators.py` (`catalog_ref` WYMAGANY → 422, mapa `_DEFAULT_CATALOG_BY_VARIANT` usunięta, pola wymagane z białych znaków = 422 zamiast cichego `None`), `AddDerWizard` (mapa zapasowych identyfikatorów usunięta), spec e2e (realna ścieżka z jawnym wyborem). Test maskujący (klikał „Falownik” przed zapisem) przepisany + test iloczynu cech. **Korekta FAB-F** (pomiar `get_default_mv_catalog()`): wszystkie 18 dawnych identyfikatorów istnieje w katalogu, katalog MA przekształtniki nN — twierdzenia „9/18 nie istniało” i „brak przekształtników < 1 kV” były fałszywe (zob. wiersz FAB-F). Dowód: pytest 26 (nowy test 422), snapshot OpenAPI przeliczony, vitest szuflada 77 + `sld/v3/canvas`+`sld/v2/canvas`+`network-build` 167 plików / 2196, tsc/eslint RC=0, e2e z realnym backendem 1 passed (POST 201, generator w ENM). Pozostałość tej samej rodziny: statyczny katalog urządzeń DER w `AddDerWizard` (fallback lokalny 3×97 pozycji) → karta **FAB-I** (zlecona, §H P1-9) |
| FAB-I | statyczny katalog urządzeń DER w kreatorze `AddDerWizard` jako „fallback lokalny” (3×97 pozycji) — pozostałość rodziny E2E-DER/FAB-F | agent (worktree, `karta_fab_i.md`) | **odebrana** (`a16b8e26`, cherry-pick `11510a9e` bez konfliktów): szczegóły i pomiar pozostałości klasy → §H P1-9; weryfikacja na gałęzi w toku (vitest `network-build`+`workspace`, tsc, eslint, guardy UI) — patrz §F |
| GUARD-SUB | `solver_input_substitute_guard` skanuje tylko `network_model/solvers`, `solver_input`, `enm` — 560 trafień klasy poza zasięgiem (66 w legacy budowniczych) | agent (worktree, `karta_guard_sub.md`) | **odebrana** (`0f4c3a59`, cherry-pick `a201b8f6` z 1 konfliktem w zapadce): zakres `network_model` (całość), `solver_input`, `enm`, `application`, `api` — 597 plików, zapadka fizyczna **89 plików / 592**, wykluczenia niefizyczne 24 pliki / 64 (liczniki rekordów archiwum, iteracji, rangi, geometria SLD — każde z uzasadnieniem, mechanizm dwukierunkowy jak zapadka); 3 sygnatury FAB-H, które karta świadomie zostawiła poza zapadką, na gałęzi już nie istnieją (domknięcie FAB-H `d58b949e`/`54cb5356`) — test własny pinujący „dokładnie 3 naruszenia” przepisany na stan faktyczny (RC=0, sumy per korzeń); guard na gałęzi PASS, 54 testy własne; TOP-10 plików wg trafień (`reference_networks/computation.py` 33, `domain_operations.py` 29, `analysis_run/service.py` 28, `power_flow_comparison/service.py` 27, `canonical_analysis.py` 25) = kandydaci kart; zauważona KLASA: most „wynik zwarcia → wejście oceny zabezpieczeń” zduplikowany w `protection/overcurrent/pipeline.py` i `wytrzymalosc_cieplna_przewodow.py`, para `m_factor`/`n_factor` (IEC 60909) w trzech proof-packach → karta konsolidacyjna po CV-2 |
| ADV-CV1 | przegląd adwersaryjny §38 „PROJECT owns ENM” (7 hipotez) | agent opus (worktree, `karta_adv_cv1.md`) | **odebrana** (`26542b66`, cherry-pick `b4d391bf` z 6 konfliktami): 14 prób obalenia, **3 udane i naprawione u źródła** (eksport archiwum omijał migrację → `klucz_twin_dla_projektu`/`migruj_projekt_z_legacy_z_repozytorium`; dispatch nN fabrykował pusty model projektu; dziennik promowanego przypadku szedł do legacy), 11 nieudanych zostaje jako inwarianty I-5…I-13 (`tests/invariants/test_wlasnosc_modelu_projektu_adwersaryjne.py`, 15 testów); guard REGUŁA 2 (`klucz_twin_projektu` niedostępny poza tłumaczem) + zakres `enm/**`. **Uzgodnienie z CV-2 w JEDEN mechanizm** (Fable): dwa równoległe mechanizmy „historia idzie za modelem” (`skopiuj_dziennik`+`skopiuj_katalog_rewizji` z CV-2 vs `przenies_dziennik` z ADV-CV1) scalone w `przenies_dziennik` + `przenies_katalog_rewizji_pod_klucz` (semantyka PRZENIESIENIA, odmowa gdy cel ma historię) z JEDNYM predykatem (`ma_historie` ∨ `dostepne_rewizje`) dla dziennika i migawek, manifest `"dziennik": ZA_MODELEM/ODLOZONY/BRAK`, wpis przeniesienia tylko gdy rewizja HEAD nie ma wpisu (bez dublowania rewizji); test CV-2 przepisany na semantykę przeniesienia. Weryfikacja: worktree pytest `enm`+`invariants`+`api`+`application` 4739 passed; na gałęzi (po SUB-52s/SLD-LOC/FAB-G) `enm`+`invariants`+`api`+`application`+`golden`+`reference_networks`+`infrastructure` **5003 passed, 5 skipped** (445 s), `enm_store_key_guard` PASS (442 pliki, zapadka 1/1), 34 testy własne, snapshot OpenAPI aktualny. Nierozstrzygnięte przez agenta (uczciwie): blokada międzyprocesowa (decyzja wdrożeniowa), druga prawda o świeżości `StudyCase.result_status` (→ CV-2-W, w toku), tożsamość modelu w dedupie legacy `_get_enm_hash` (tor E2 do kasacji), sieroty plików zastanych przypadków skasowanych z bazy (raport wykonalny, klucz w treści pliku) |
| FAB-G | tabliczka GPZ WN/SN wyłącznie z typu katalogowego (klasa D2 odłożona przez FAB-D1: 517 testów) | agent (worktree, `karta_fab_g.md`) | **odebrana** (`670c77c5` cherry-pick `625427a7` bez konfliktów, 98 plików; `3499df8c` korekta scalenia): `add_grid_source_sn` wymaga `transformer_catalog_ref`/`transformer_catalog_binding` albo pary `hv_voltage_kv`+`transformer_sn_mva` rozstrzygalnej w mapie (inaczej `catalog.ref_required` z listą dostępnych par), literały `or 12.0/120.0/0.2/"YNd11"` usunięte (były martwe — materializacja i tak nadpisywała, dowód G4: żaden hash ani fikstura nie zmieniły się przez tę kartę), katalog materializowany PRZED szyną 110 kV; testy przez wspólne fabryki ładunku (`catalog_test_helpers.gpz_payload` + lokalne wrappery), 12 testów iloczynu cech, 43/44 speców e2e z parą (44. mockuje żądanie), kreator GPZ z pickerem typu (`PoleKatalogu`); na gałęzi 25 testów CV-2 padło na `catalog.ref_required` (karta nie znała testów powstałych po jej bazie) → para dopisana w dwóch plikach, inwentarz klasy potwierdził brak innych trafień. Weryfikacja: agent pytest 4859 passed (5 pre-existing = 4ab4baec), 12 guardów RC=0, vitest 132; Fable na gałęzi pytest `enm`+`e2e`+`golden`+`reference_networks`+rejestr+guardy CI 2117 passed (+66 po korekcie), tsc 0, vitest `ui2/kreatory/zrodlo` 11 plików / 132, 12 guardów RC=0. Zgłoszone przez agenta, nie naprawione: brama HTTP (`domain_ops_policy`) nie ma pojęcia „pole wymagane” dla transformatora (błąd domenowy w treści 200 zamiast 422) — kandydat na rozszerzenie `PozycjaBramyApi`; brak krzyżowej walidacji typu 20 kV vs sieć 15 kV (luka przedistniejąca) |
| CV-2 (rdzeń) | `ModelRevision` przy magazynie ENM (migawka każdej rewizji, `checkout`), `RevisionEnvelope` na biegu, świeżość wyprowadzana z koperty, kasacja H1/H2/H3, `provenance_constant_guard` | Fable (worktree `fable-cv2`) | **scalony** (`9f5fe90e`, 31 plików): `enm/rewizje.py` (migawka `<digest>.rev/<n>.json.gz`, HEAD autorytatywny, uzgodnienie przy wczytaniu), `enm/store.py::checkout`, dziennik = rejestr rewizji (`hash_sha256`, `rodzic`, pełny `ladunek`, bez limitu), migracja CV-1 kopiuje historię pod klucz projektu (wcześniej lądowała w `legacy_przypadki/` — luka CV-1 zamknięta), `enm/envelope.py` + `network_model/catalog/odcisk.py` (odcisk biblioteki typów deterministyczny między procesami), `canonical_runs.envelope_json` (addytywna, dokładana do istniejących baz), `result_freshness.evaluate_envelope_freshness` (lista zmian z dziennika, „katalog zmieniony”) i `status_wynikow_przypadku`; H1 (5 pól nagłówka + lustro TS), H2 (`solver_version`/`catalog_schema_version` bez stałych; V12.5: `catalog_fingerprint`, `model_revision`), H3 (stałe „układ normalny” usunięte, `switching_state_ref` bez etykiety zastępczej — guard znalazł 4. instancję w `analysis_case_context.py`); `provenance_constant_guard` w `python-tests.yml`. Testy: 28 (rewizje: awaria × 5 kroków × 2 sieci, checkout × komendy, sieroty, magazyn zastany, migracja, import) + 19 (koperta, odcisk, świeżość iloczyn cech, `create_run`) + 16 (persystencja). Weryfikacja: §F. **Wiring (status przypadku z biegów w API, kasacja pisarzy `ResultInvalidator`/`mark_*`/`invalidate*`, ładunek komendy w `api/enm.py`, ekran świeżości z listą zmian, `GET .../rewizje/{n}`) — karta CV-2-W (agent)** |
| FAB-H | `k_sc` falownika: pole katalogowe `ConverterType.k_sc` + 1,1 jako zarejestrowane założenie (`k_sc_zrodlo`, ślad White Box, WARNING `inverter.k_sc_assumed` z emiterem, tożsamość liczb dla sieci bez karty); klasa „Q wytwórcy → 0” w 3 plikach — jedna funkcja `moc_bierna_wytworcy`, `generator.q_missing` dla rozpływu, V12.6 przez bramkę API (solver FROZEN nietknięty) | agent (worktree; karta `karta_fab_h.md`) | **odebrana** (`4ee77f56` cherry-pick bez konfliktów; inwentarz klasy: 3 przestrzenie katalogu z `k_sc` (`ConverterType`, `PVInverterType`, `BESSInverterType` — druga ścieżka katalogowa PV/BESS, której FAB-D1 nie objął), IR `InverterSource.k_sc_zrodlo`, `graph.k_sc_assumptions_trace`, 2 kody gotowości z emiterem, Q przez `solver_input/moc_bierna_wytworcy.py` w bramce gotowości/mapping/canonical_analysis/v126; agent: pełny pytest 10773 passed, mypy 0). **Odbiór wykazał niedomknięcie klasy**: `solver_input_substitute_guard` (którego agent nie uruchomił) złapał 3 NOWE podstawienia `q_mvar or 0.0`/`if-else 0.0` uzasadniane BLOCKER-em gotowości (dwa niezależne predykaty — KLASA §3) → domknięte przez Fable `d58b949e` (Q nieznane = wkład pominięty; zapadka `gen.q_mvar`/`generator.q_mvar` 1→0) i `54cb5356` (brama SSCI w API dla `_z_conv_components` solvera FROZEN — B-01; `V126ConverterInput.q_mvar` z tego samego źródła; test API przeniesiony na realny projekt+przypadek, bo FAB-H bazowała sprzed CV-1-W: 5 czerwonych → 0). Świadomie zostawione: `GeneratorSN/NN.k_sc` (`core/generator.py`, konsument wyłącznie `oze_validators.py` + 1 test — martwa wyspa toru legacy, kasacja z P2–P12 w CV-4; P2-1) |
| WB-ROZPLYW | ślad WHITE BOX podziału prądu zwarciowego (TH-1) na ścieżce użytkownika | agent (worktree) | **odebrana** (`acd2862c`): sekcja „Podział prądu zwarciowego — ślad obliczeń” w `ui2/wyniki/zwarcia/EkranZwarc.tsx` pod tabelą rozpływu, zasilana z tego samego wywołania końcówki rozpływu punktu (`RozplywOdpowiedz.branch_flow_trace`, hook zwraca `{flows, trace, blad}` — błąd HTTP odróżniony od braku danych); kroki renderuje istniejący `KrokDowodu`+`mapujKroki` (zero drugiego renderera); trzy uczciwe stany (błąd / niedostępny / pusty); fixture TH-1 skopiowana z realnego biegu solvera. **Defekt klasy naprawiony przy okazji**: `dowodModel.ts::mapujWielkosci` zakładał opakowany `TraceValue {value,unit,label}`, a solver emituje skalar wprost albo `{re,im}` — KAŻDA wartość realnego śladu w oknie „Dowód obliczeń” renderowała się jako kreska; naprawa `rozpakujWartosc` (oba kształty) + testy regresji. Testy: 14 plików / 172 passed (`zwarcia`+`dowod`), type-check, lint, 6 guardów UI RC=0 (agent); weryfikacja Fable na gałęzi w toku. Obserwacja → karta **WB-2**: trzy niezależne implementacje rozpakowania wartości śladu (`ElementCalculationProofPanel.unwrapTraceValue`, `TraceStepView.formatValue`, `dowodModel.rozpakujWartosc`) — konsolidacja do jednej funkcji współdzielonej |
| CV-3.1 (rdzeń) | `OperatingScenario` + `apply_scenario` → `EffectiveNetworkSnapshot`, magazyn scenariuszy nazwanych, koperta v2, świeżość scenariusza, `bieg_wariantu`, `scenario_copy_guard` | Fable (worktree `fable-cv2`) | **scalony** (`e1bc25f1`, 14 plików): `enm/scenariusze.py` (pola WYŁĄCZNIE z dostawcą: `out_of_service`, `setpoints`, `gen_scaling`, `injections`, `probe_shunts`, `fault_spec`; kolejność nadpisań stała; `ref_id` spoza modelu = błąd z nazwą; sondy bit w bit równe elementom pomocników D2/D3/D5 — pin; magazyn `<digest>.scen/<id>.json` append-only + nagrobek + migracja klucza CV-1 za modelem z manifestem `scenariusze`), `enm/hash.py::hash_migawki_enm` (hash ze słownika == hash z obiektu, jedna lista wykluczeń nagłówka), `enm/envelope.py` v2 (`scenario_ref`+`scenario_hash`; v1 = stan normalny bit w bit; `snapshot_hash` koperty = hash BAZY), `create_run(scenariusz=)` z walidacją migawki efektywnej i projekcją scenariusza zwarciowego na opcje, `bieg_wariantu` (jedyna fabryka wariantu; odmowa składania scenariuszy), `result_freshness` `SCENARIUSZ_ZMIENIONY`/`SCENARIUSZ_USUNIETY`; guard R1–R4 z zapadką 19 zastanych trafień w 6 rodzinach (D1–D6) + 18 testów własnych + krok CI; docs B.3/roadmap/OW-9. Testy: 38 (`test_scenariusze.py`) + 14 (`test_scenariusz_i_bieg.py`, I-S1…I-S5). Weryfikacja: §F |
| CV-3.2 (C2/C3) | kasacja C2 (`domain/study_case_engine.py` 623 + test 1163 — silnik biegów tylko w testach) i C3 (9 operacji `study_cases` v2 zapisujących w próżnię: 266 linii handlerów, 78 rejestru `OperationSpec`, 52 `CANONICAL_OPS` bez importerów, 9 wpisów `NO_MODAL_NEEDED`, 11 komunikatów FE); guard wskrzeszenia; intencja „dwa biegi porównywalne" na kanon | agent (worktree na `3c4b3c13`, `karta_cv32.md`) + domknięcie Fable | **odebrana** (agent `a1a703fd`, cherry-pick bez konfliktów, 27 plików +500/−2531; domknięcie Fable `f4f020f4`): grep skasowanych symboli = 0 poza guardem wskrzeszenia i notami korygującymi; kolizje nazw żywego kodu nietknięte (`api/study_cases.py::create_study_case/compare_study_cases`, `api/enm.py::run_short_circuit/run_power_flow`, `designer/actions.py::ActionType`, `network_wizard/service.py::create_study_case` — dwie ostatnie nienazwane w karcie, znalezione przez agenta); guard `legacy_public_path_guard` rozszerzony o C2 (moduł/AST klas) i C3 (rejestr + handlery + `NO_MODAL_NEEDED`, reużycie `canonical_ops_guard`), 9 testów własnych; I-S6: dwa biegi tego samego scenariusza bit w bit, bieg bazowy vs N-1 z wykrywalną deltą (realny solver PF). Weryfikacja Fable na `fable-cv3`: pytest enm/invariants/api/application/analysis/domain/ci + 3 pliki = 5785 passed / 2 failed → **znalezisko odbioru:** `tests/ci/test_kontrakt_routerow_miedzy_straznikami.py` (2 testy) — top-level `import canonical_ops_guard` nie ładował się przez `spec_from_file_location` (lista weryfikacji karty nie obejmowała `tests/ci`); naprawa wg konwencji `route_prefix_guard` (katalog skryptu na sys.path) → 3 + 12 testów zielone; 14 guardów rc=0 (canonical_ops 49 operacji, dialog_completeness, single_entry_point, api_lifecycle, legacy_public_path, docs, docs_archive, vulture, import_graph, arch, scenario_copy, solver_input_substitute, readiness_codes, no_codenames) + 3 testy własne guardów, mypy 809, snapshot OpenAPI po regeneracji bez zmian (318/228), ruff/black 0, tsc/eslint 0, vitest topology + obliczenia 29 plików / 364. **C4 zatrzymana przez agenta (STOP §0.3):** `analysis/reporting/pdf/p24_plus_report.py:45` konsumuje `ScenarioComparisonView`; pomiar Fable: `export_p24_plus_report_pdf` ma ZERO wołających w `backend/src` poza re-eksportem pakietu, ZERO tras HTTP, ZERO wzmianek w INWENTARZ_FUNKCJI/KANON → decyzja: kasacja C4 RAZEM z P24+ (825 linii; `test_koperta_kontekstu` traci tylko ogniwo `ReportContext`, `analysis/koperta_kontekstu.py` żywy) — drugi commit agenta odebrany (`d5c45fb2`, 2 konflikty w docs rozwiązane: wiersz CV-3.1 z HEAD, CV-3.2 z agenta; atrybucja decyzji skorygowana na Fable): 14 plików skasowanych (2464 linie: study_scenario 611, scenario_comparison 345, P24+ 830, 3 pliki testów 678), `test_koperta_kontekstu` bez ogniwa `ReportContext`, guard wskrzeszenia C4/P24+ (katalogi, `ScenarioComparisonBuilder`, `export_p24_plus_report_pdf`; 6 testów), 0 intencji testowych do przeniesienia (każdy z 8 testów dowodził wyłącznie własnej, kasowanej funkcji — uzasadnienie per test w meldunku). Weryfikacja Fable: pytest analysis/application/enm/invariants/api/ci/domain 5742 passed / 5 skipped (pandapower), 15 guardów + 4 testy własne rc=0 (pin guarda podstawień przeliczony: 3652 → 3637 pól, 602 → 597 plików, application 346 → 341 — `9fc5ff56`), mypy 797 plików 0, snapshot OpenAPI 318/228 bez zmian, ruff/black 0 |
| PARITY-CV3 | złote hashe parytetu rodzin D1–D6 zebrane PRZED migracją (`tests/golden/parytet_scenariuszy/`) | agent (worktree na `24d2257b`, `karta_parity_cv3.md`) | **odebrana** (`1cae2f84`, cherry-pick bez konfliktów): 28 hashy (N-1 4, hosting 3, P-Q 3, odpowiedź OSD 10, kompensacja 4, nastawy 4) na sieci złotej, pierścieniu, sieciach promieniowych/rozgałęzieniu; 30 testów w ~5 s; zero zmian w `src/**`. Uczciwe odstępstwo: D6 nie działa na sieci złotej (jedyna gałąź z kompletem danych katalogowych jest ślepym końcem selektywności — brak `cross_section_mm2` na sąsiedniej), więc D6 mierzy dwie sieci z testu batch_run; harness wykrył własny defekt (dwa warianty `next_bus` dawały identyczny hash przy równych impedancjach — sieć rozgałęzienia poprawiona w harnessie z opisem). Decyzja agenta przyjęta: biegi bazowe budowane syntetycznie (`CanonicalRun` bez `set_enm`/`execute_run`) po zweryfikowaniu, że żadna rodzina nie czyta `raw_result`/`validation` biegu bazowego |
| GUARD-SUB-2 | guard podstawień śledzi zmienne lokalne (forma H: odczyt pola → podstawienie liczby) | agent (worktree na `1b705f66`) | **odebrana** (`22cd5f49` bez konfliktów; pin przeliczony na gałęzi w `3c4b3c13`: 3653 pól / 601 plików po `enm/scenariusze.py`): forma H wykryła 30 trafień w 12 plikach (naprawa własnego fałszywego alarmu: nośnik jako baza dereferencji dublował formę B — 34 → 30, test regresyjny); klasyfikacja per trafienie: dług 27 (CT/kc/Ik w nastawach nadprądowych, `swz/werdykt`, klucze sortowania werdyktu projektowego, wartości w dowodzie LaTeX i Proof Inspectorze, `engine.py`, wzorzec C generacji lokalnej, `rated_power_mva`/`nn_voltage_kv` w operacjach domenowych, `payload.quantity` v2), wykluczenia 3 (klucz sortowania z flagą logiczną w raporcie DOCX, reguła walidacji semantycznej, zawężenie typu dopasowania wzorca); trzy pliki rodziny odtwarzaczy rozpływu (§0.4) — zero trafień H (czyste po `98ad6b6a`); zapadka 66/346, wykluczenia 18/50; 67 testów własnych; czas skanu ~6 s (budżet 20 s). Nienazwane ograniczenie (w docstringu): aliasowanie nośnika przez inną nazwę (`y = x`) poza formą H |
| C6-PERSIST | scenariusze zwarciowe trwałe w magazynie scenariuszy (`kind=FAULT_STUDY`, `fault_spec`), „ma biegi" z kopert, `create_run_from_scenario` przez `create_run(scenariusz=)`, lokalizacja zwarcia BUS/NODE honorowana (jeden węzeł), BRANCH/BRANCH_POINT = kod gotowości + 409 | agent (worktree na `3c4b3c13`, `karta_c6_persist.md`) | **odebrana** (`f53b780a`, cherry-pick bez konfliktów na `c291b692`; 15 plików, +1109/−213): `FaultScenarioService` bezstanowy — `klucz` (Canonical Project Twin) argumentem KAŻDEJ metody, tłumaczenie `case_id → klucz` wyłącznie na granicy API; końcówki adresowane samym `scenario_id` (get/put/delete/eligibility/sld-overlay/runs) odnajdują klucz przeglądaniem magazynu (`enm.scenariusze.znajdz_klucz_scenariusza` — plik `<id>.json` niesie własny `klucz`, bez drugiej prawdy w osobnym indeksie; uszkodzony plik = jawny `ScenariuszUszkodzonyError`); `register_run` i rejestr `_scenario_runs` skasowane — „ma powiązane biegi" wyprowadzane z `koperta.scenario_ref` biegów przypadku dla OBU ścieżek (pojedynczy bieg i seria; rozszerzenie ponad literę karty: żadna z dwóch ścieżek nie przekazywała `scenariusz=` do `create_run`, więc naprawa tylko jednej pozwoliłaby usunąć scenariusz z aktywnym biegiem serii); `solver_input_for_scenario` = cienki alias `opcje_biegu_ze_scenariusza` (jedna projekcja); pole addytywne `revision` na `FaultScenarioResponse` (snapshot OpenAPI: diff wyłącznie addytywny); `_execute_short_circuit` honoruje `options["location"]` (BUS/NODE → jeden węzeł, parytet fizyki z biegiem bez lokalizacji; BRANCH/BRANCH_POINT → `ValueError` z kodem kanonu `fault.location_on_branch_requires_assembler`, tym samym, którym eligibility blokuje przed utworzeniem biegu — jedno źródło komunikatu). Weryfikacja niezależna Fable na `fable-cv2` (`cffe90ca` = `f53b780a`): pytest 4064 passed (suity zwarciowe + `tests/enm` + inwarianty + `tests/api` + parytet + analizy + nastawy wsadowe), guardy `enm_store_key`/`no_direct_fault_params`/`scenario_copy` (0)/`readiness_codes` (108 kodów)/`api_lifecycle`/`solver_input_substitute`/`vulture` rc=0, mypy 809 plików 0, snapshot OpenAPI po regeneracji bez zmian (318 ścieżek, 228 schematów), ruff/black 0. Agent: 2961 passed w zakresie karty + 2545 poza + 165 testów własnych guardów. **Znalezisko uboczne z odbioru (klasa, nie instancja):** `fix_action_id` w `READINESS_CODES` — 103/108 kodów niesie identyfikator akcji naprawczej, którego NIKT nie wykonuje (zero dyspozytorów we frontendzie, zero rejestru w backendzie; 76 z 83 identyfikatorów nie występuje nigdzie poza definicją); jedyną realną akcją jest `fix_navigation` (obecna na wszystkich 108) → karta FIX-ACTION-KASACJA (§E niżej) |
| FIX-ACTION-KASACJA | fantom `fix_action_id` w `READINESS_CODES` (103/108 kodów, 83 identyfikatory, 76 bez żadnego wystąpienia poza definicją, zero dyspozytorów FE/BE) → kasacja pola z `ReadinessCodeSpec`, mostu (`canonical_fix_action_id`/`fix_action_id`), prezentacji nastaw, typów FE; `fix_navigation` obowiązkowa (jeden predykat guarda) | agent (worktree na `a5a68bf3`, `karta_fix_action.md`) + domknięcie Fable | **odebrana** (agent `efa19e09`, cherry-pick bez konfliktów, 28 plików +136/−197; pin guarda podstawień `b5e53a37`: 3637 → 3636 pól): pole usunięte z `ReadinessCodeSpec` i 103 wpisów (treść kodów nietknięta, diff mechaniczny), `fix_navigation` OBOWIĄZKOWA dla każdego kodu (poprzedni predykat wymagał ścieżki naprawczej tylko od BLOCKER-ów — WARNING/INFO bez nawigacji były furtką tej samej klasy) z `panel` z zamkniętego zbioru 10 paneli spisanych z rejestru, most i prezentacja nastaw bez kluczy (usunięte, nie `None`), `catalog_enforcement_guard` przepisany na `fix_navigation.panel` z tym samym rygorem, 8 plików testów backendu + 9 plików FE (typy, fixtury, mock harnessu) z zachowaniem intencji; wpis rejestru V12K-338 z korektą liczb karty w obie strony (identyfikatory bez wystąpienia 76 → 78, pliki testów 15 → 8). Zmierzone przez Fable: `fix_navigation.panel` MA realnych konsumentów (`SekcjaNastaw.tsx::przestrzenDlaPanelu`, adapter gotowości) — pozostawiona ścieżka naprawcza nie jest fantomem. Weryfikacja Fable na `fable-cv3` (baza `45af111d`): pytest domain/api/ci/enm/invariants/protection 3635 passed, 18 guardów rc=0 (readiness_codes 108 kodów, catalog_enforcement, canonical_ops, domain_no_guessing, api_lifecycle, docs, docs_archive, vulture, catalog_binding, catalog_gate, legacy_public_path, dialog_completeness, arch, no_codenames, readiness_consumption, fix_action_completeness, forbidden_ui_terms, ui_terminology) + 3 testy własne, mypy 797, snapshot OpenAPI bez zmian (318/228), ruff/black 0, tsc/eslint 0, vitest kryteria/koordynacja/oltc/jakość 13 plików / 160; agent dodatkowo pełna regresja backendu 11060 passed / 11 skipped i `src/ui2` 289 plików / 3381. Znalezisko uboczne agenta naprawione przez Fable: kanon §10.2 odsyłał do V12K-337, którego rejestr nie miał (wiersz pod kluczem opisowym) → `58b55777`. Znalezisko z pełnej regresji agenta: 11 skipów (5 pandapower + 6 innych) → karta SKIP-INWENTARZ |
| CV-3-W | migracje D1–D3 (A) i D4–D6 (B) na `apply_scenario` + `bieg_wariantu`, parytet bit w bit ze złotymi hashami, zapadka guarda 19 → 0 | 2 agenci (worktree na `3c4b3c13`, `karta_cv3w.md`) | **odebrane** (B `dc5fedd0` bez konfliktów; A `9faa78c7` z 3 konfliktami A↔B w zapadce/pinie/wyroczniach — scalone do zapadki **0** i jawnych słowników wyroczni; domknięcie `498f4260`): sześć rodzin bez prywatnych pomocników (`_probe_generator`, `_probe_capacitor`, `_night_generators`, `_snapshot_with_*`, `_scenario_run`, `_bieg_wariantu`, `_wariant_*` usunięte), złote hashe 30/30 bez zmian po obu kartach; **znalezisko przy odbiorze A (naprawione u źródła w rdzeniu):** migracja D1 straciła optymalizację „jeden graf na kontyngencję" (dyspozytor nie przyjmował gotowego grafu → drugi graf na każdą z N kontyngencji) → `wykonaj_bieg_w_pamieci(run, graf=)`/`_wykonaj_analize_biegu(run, graf)` oddają gotowy graf do rozpływu (graf poza rozpływem = jawny błąd kontraktu), piny: wynik z gotowym grafem bit w bit równy, liczba budów grafu = kontyngencje + baza; uczciwe rozstrzygnięcie testu D3 (rekord generatora bez `p_mw` → `ValidationError` całego modelu zamiast cichego pominięcia — silniejsza forma FAB-E); test D6 pinujący literalną równość `snapshot_hash`/`input_hash` z kotwicą przepisany na intencję (brak mutacji kotwicy) — `bieg_wariantu` liczy hashe uczciwie. Weryfikacja na gałęzi: parytet 30/30, suity rodzin + dyspozytor + scenariusze + inwarianty + API 217 passed, `scenario_copy_guard` 0/0 (446 plików) + 18 testów własnych, `no_direct_fault_params`/`solver_input_substitute`/`vulture` RC 0, mypy 809 plików 0, black/ruff 0 |
| E2E-FIX (2) | E2E full czerwony od FAB-G/SUB-52s: `ux-feedback-loop` C2/C3 (mock bez `GET /api/catalog/transformer-types`, którego wymaga kreator GPZ po FAB-G), `sld-pa-powerflow-tor` N-2/screenshot (asercja „≥1 przyciemniona stacja" opierała się na wyspie E003 substratu naprawionej w SUB-52s; `de_energized_bus_refs: []`) | agent (worktree na `3c4b3c13`, `karta_e2e_fix.md`) + domknięcia Fable | **odebrana** (agent `314c644b`, cherry-pick bez konfliktów, 12 plików; domknięcia Fable `cd1547b2`, `56025be6`): mock C2/C3 serwuje `transformer-types` (jedna pozycja z fixtury kreatora) i `tap-changers`, a operacja `add_grid_source_sn` bez `transformer_catalog_ref` odpowiada TYM SAMYM kształtem `catalog.ref_required`, co `_error_response` backendu — C2 asertuje realny ładunek (`transformer_catalog_ref`, `hv_voltage_kv`, `transformer_sn_mva`); drugi companion konserwacyjny substratu przez `apply_scenario` (`select_ring_maintenance_scenario`: żadna stacja nie jest izolowalna dwiema gałęziami, bo wyłączniki pól SN są gałęziami zamkniętymi — §0.C: stacja o najmniejszym stopniu, L7-1, wszystkie 5 gałęzi; kaskada 4 stacji laterału udokumentowana w polu `scenario`; 28 szyn bez zasilania), N-2 = RÓWNOŚĆ zbiorów dla `normal` i `maintenance`, 2 PNG, `route_prefix_guard` wpis szablonowy. Domknięcie 1: predykaty parami — szyna SN wybranej stacji musi być w `de_energized_bus_refs` po biegu (rozjazd prognozy z grafu gałęzi i prawdy solvera = jawny `ValueError`, nie cichy companion; `MultiGraph` dla gałęzi równoległych; 2 testy rozjazdu: bez nadpisań, tylko wyłączniki pól). Domknięcie 2: bieg e2e 14/15 — jedyna czerwień to `page.goto` harnessu SLD (limit 30 s), choć harness się wyrenderował: Vite pre-bundlował tylko `index.html`, pierwsze wejście na `*-harness.html` uruchamiało reoptymalizację + pełne przeładowanie; `optimizeDeps.entries` = wszystkie punkty wejścia (wzorzec), limity Playwrighta bez zmian, ten sam spec 8/8 na świeżym serwerze. Weryfikacja Fable: backend 441 passed (reference_networks + scenariusze + ci; 5 skipped = pandapower, karta PANDAPOWER-DEP), 10 guardów + 2 piny rc=0, mypy 809, ruff/black 0, companiony po regeneracji bit w bit (normal `bc10e5ac`, maintenance `0613eacd`); frontend tsc 0, eslint 0, vitest 145 plików / 2904; e2e realny backend: 14/15 → 8/8 (spec SLD po naprawie) + 7/7 ux-feedback. CI E2E full na `a5a68bf3` (przed scaleniem): dokładnie te 4 testy czerwone, 403 passed, 2 skipped, 1 flaky (S9-5 zapis magistrali: `toBeEnabled` 30 s, retry zielony → karta E2E-S95, klasa „enable bez sygnału gotowości") |
| CV-3.2 (C4) | kasacja C4 (`application/study_scenario/**`, `analysis/scenario_comparison/**`) RAZEM z P24+ (`analysis/reporting/pdf/**`) po decyzji architektonicznej Fable (architekt prowadzący; pomiar wołających P24+) (2026-09-05, wariant c): P24+ jest tym samym bytem co C4 („raport bez trasy") — `export_p24_plus_report_pdf` miał 0 wołających w `backend/src` poza re-eksportem własnego `__init__.py`, 0 tras HTTP (żaden router `api/` go nie importował), 0 wzmianek w `INWENTARZ_FUNKCJI_2026-07.md`/`KANON_V12_XX.md` — zmierzone grepem przed kasacją | agent (ten sam worktree, drugi commit na `41a00e01`) | **odebrana**, SHA `ec0c20dc`: usunięte `application/study_scenario/**` (5 plików, 611 linii), `analysis/scenario_comparison/**` (4 pliki, 345 linii), `analysis/reporting/pdf/**` (2 pliki, 830 linii — P24+ w całości), testy `tests/analysis/test_pdf_report_p24_plus.py` (3 testy), `tests/analysis/test_scenario_comparison.py` (2 testy), `tests/application/test_study_scenario_workflow_p23.py` (3 testy); `analysis/__init__.py::__all__` — zdjęty wpis `"scenario_comparison"`; `tests/analysis/test_koperta_kontekstu.py` — zdjęte ogniwo `ReportContext` z `OGNIWA_LANCUCHA` (2 testy łańcucha nadal zielone na 8 żywych ogniwach zamiast 9). Żadna z 8 usuniętych testowych intencji nie dotyczyła żywego modułu (wszystkie wyłącznie własnej, skasowanej implementacji: determinizm/sekcje P24+ własnego renderu, porządkowanie/determinizm `ScenarioComparisonBuilder.build()`, deterministyczne hashe/NOT_COMPUTED/serializacja P23 własnych modeli) — 0 migracji intencji, uzasadnienie per test w meldunku. Guard wskrzeszenia: `scripts/legacy_public_path_guard.py::check_c4_and_p24_plus_resurrection` (3 katalogi + klasa `ScenarioComparisonBuilder` + funkcja `export_p24_plus_report_pdf`, skan całego `src` żeby złapać wskrzeszenie pod inną ścieżką; katalog sprawdzany po obecności `*.py`, nie po `Path.exists()`, żeby osierocony `__pycache__` z sesji sprzed kasacji nie fałszował wyniku) + 6 nowych testów własnych. Weryfikacja: pytest `tests/analysis tests/application tests/enm tests/invariants tests/api` — patrz §F; mypy/black/ruff 0; guardy `docs_guard`/`vulture_guard`/`import_graph_guard`/`arch_guard`/`legacy_public_path_guard`(+18 testów własnych łącznie)/`canonical_ops_guard`/`dialog_completeness_guard`/`single_entry_point_guard`/`api_lifecycle_guard` RC=0; snapshot OpenAPI — bez zmian (0 tras C4/P24+ istniało). 5 pominiętych testów w zakresie karty (środowiskowe, `pandapower` brak w środowisku, osobna karta — bez naprawy): `tests/application/reference_networks/test_pandapower_cross_validation.py` (cały moduł, `pytest.importorskip` linia 17, 6 testów zwiniętych w 1 zdarzenie kolekcji) + `test_pandapower_bridge.py::TestEnmConversion::test_ieee_4bus_converts_without_error`/`test_pp_simple_4bus_converts` + `TestPandapowerPowerflow::test_runs_powerflow_returns_buses`/`test_voltages_drop_from_slack` (4 × `@pytest.mark.skipif`) |
| PANDAPOWER-DEP | 5 „skipped" = 10 testów walidacji krzyżowej z pandapower NIGDY niewykonanych (brak zależności w pyproject/lock) | agent (worktree na `e82ee602`, `karta_pandapower.md`) | **odebrana** (`7c9ab168`): bramka §0.1 ZADZIAŁAŁA — każda wersja pandapower (3.0.0–3.5.4) pinuje `scipy<1.17` na Pythonie 3.11 (metadane wheel: `scipy~=1.18; py>=3.12` / `scipy<1.17; py<3.12`), repo ma scipy 1.17.0; `poetry add` rozwiązałby się CICHO obniżając lock (pin `^1.12.0`) — agent zatrzymał się zgodnie z bramką i zameldował. Decyzja Fable: opcja 2 (izolacja): marker pytest `pandapower` zamiast `skipif`/`importorskip`/`is_pandapower_available` (skasowane), główny bieg CI `-m "not pandapower"` (DESELEKCJA, 0 skipped; import pandapower leniwy w fixture — twardy import na poziomie modułu wywalał kolekcję całego biegu, zmierzone), nowy równoległy job `pandapower-cross-validation` w `python-tests.yml` (własny klucz cache `poetry-pandapower-*`, `poetry run pip install pandapower==3.5.4` obniża scipy do 1.16.3 WYŁĄCZNIE w efemerycznym venv joba, `fetch-depth: 0` — własna regresja złapana przez `test_guard_diff_base`); lokalnie izolowany venv `/root/.cache/pp-venv` z 94 pakietami z `poetry.lock` (brak pluginu export). **Znalezisko fizyczne:** pierwszy bieg 4/10 czerwone — most `enm_to_pandapower_dict` wymagał `sk_max_mva`/`rx_ratio` bezwarunkowo, choć `pp.create_ext_grid` ma je opcjonalne (potrzebne tylko `pp.shortcircuit`, którego most nie woła) → naprawa mostu, zero zmian solverów/tolerancji; po naprawie 10/10: IEEE 4-bus Stevenson NR vs pandapower NR |ΔV| ≤ 2,1e-11 pu, |Δkąt| ≤ 1,5e-9°. Główny venv nietknięty (numpy 1.26.4 / scipy 1.17.0 / pandas 2.3.3 / networkx 3.6.1 przed i po; `poetry.lock` bez zmian). Weryfikacja Fable na `fable-cv5`: cherry-pick bez konfliktów na `58367464`; główny venv `-m "not pandapower"` `tests/application/reference_networks` + `tests/ci` 400 passed / 10 deselected / 0 skipped (125 s); izolowany venv `/root/.cache/pp-venv` `-m pandapower` 10 passed / 178 deselected (7 s); wersje przed/po: główny numpy 1.26.4 / scipy 1.17.0 / pandas 2.3.3 / networkx 3.6.1, izolowany scipy 1.16.3 + pandapower 3.5.4; guardy arch, vulture, repo_hygiene, verification_phantom_paths, solver_boundary, grep_zero, docs rc=0; mypy 797, ruff/black 0; nowy job CI z osobnym kluczem cache i `fetch-depth: 0` przejrzany |
| E2E-S95 | flaky zapis kreatora magistrali (CI run 317: `toBeEnabled` 30 s, retry zielony) | agent (worktree na `325d7994`, `karta_e2e_s95.md`) | **odebrana** (`f62f7891`, cherry-pick bez konfliktów): przyczyna w kodzie — `KreatorMagistralaSn.tsx:565` `zapisZablokowany = !hasStart || !activeCaseId` (bramka nie zależała ani od poprawnej długości, ani od stanu ładowania katalogu kabli/linii; spec zakładał przyczynowość „wypełnij → włącz", której produkt nie deklarował); naprawa: `katalogLadowanie` jawny (`.finally`, nie z długości tablic), `stanGotowosci: ladowanie | zablokowany | gotowy` = JEDNO źródło dla `disabled` i `data-status` na `KreatorRama` (predykaty parami), długość niedodatnia blokuje z komunikatem, katalog w trakcie ładowania blokuje z komunikatem; spec czeka na `data-status="gotowy"` PRZED `toBeEnabled` (limit nie podniesiony — `toBeEnabled` na limicie domyślnym). KLASA: 24 kreatory przejrzane, ten sam defekt naprawiony w 12 (magistrala, ogranicznik, kompensator, transformator, pierścień, ZKSN, słup rozgałęźny, odgałęzienie, źródło dyspozycyjne, pole SN, łącznik sekcyjny, odbiór nN — tryb ręczny świadomie bez bramki katalogu, test pokrywa), 9 zgodnych bez zmian; +4 testy (transition disabled→enabled natywnym `userEvent`, iloczyn cech ładowanie×pole poprawne) — `src/ui2/kreatory` 693 → 697. Uczciwie: 30-sekundowego zawieszenia z CI agent nie odtworzył lokalnie (3× pod obciążeniem: asercja 14–83 ms) — przyczyna wskazana w kodzie, nie zgadnięta; mechanizm eliminuje klasę „ślepe odpytywanie disabled". Pominięte świadomie (obszar DER równolegle w FAB-J): `zrodlo-oze` (ten sam defekt) i `zrodlo` GPZ (brak bramki w ogóle) → FAB-K(c). Weryfikacja Fable na `fable-cv3`: tsc 0, eslint 0, vitest `src/ui2/kreatory` 54 plików / 697, 9 guardów (dead_click, dialog_completeness, no_codenames, forbidden_ui_terms, ui_terminology, ui_no_physics, repo_hygiene, grep_zero, verification_phantom_paths) rc=0; e2e s95 na realnym backendzie (własne serwery na 8012/5185, świeża baza) 2/2 passed w 1,4 min; rozgrzewka aplikacji 46 s poza limitami testów |
| FE-HIGIENA | ostrzeżenie React „same key" w SLD v3 (klasa `key=` w `sld/v3/**`) + 3 bezwarunkowe `test.skip` w e2e | agent (worktree na `a5a68bf3`, `karta_fe_higiena.md`) + domknięcie Fable (własność serwerów e2e) | **odebrana** (agent `cda1878a`, cherry-pick bez konfliktów, 4 pliki; domknięcie Fable `9756f445, spec `87584ddc``): przyczyna źródłowa — `ownerRef` symbolu sceny jest CELOWO tożsamością POLA (nakładka energizacji kluczuje po polu; pole liniowe = odłącznik + wyłącznik o jednym `ownerRef`), a `computeSwzBadgePlacements`/`computeOltcBadgePlacements` w `SldCanvasV3.tsx` tworzyły odznakę PER SYMBOL zamiast PER WPIS nakładki → dedup po `ownerRef` (pierwsze trafienie w kolejności sceny, ta sama reguła co `computeFaultPointMarkerPlacement`); pierwsza próba agenta (osobny `ownerRef` per instancja stosu pola GPZ) ODRZUCONA przez samego agenta po rozszerzeniu testu na cały substrat — łamałaby `energizedByOwnerRef` dla 2. aparatu pola; inwentarz 72 miejsc `key=` w `sld/v3/**` sklasyfikowany (index-w-jednym-map, testId, ref, ownerRef); test przypinający na pełnym substracie (LOD 1/2, nakładka SWZ na każdym `ownerRef` aparatu, OLTC na każdym transformatorze) czerwieni się na cofnięciu (2/4) i jest zielony po naprawie. Skipy: `critical-engineer-flow:141` — końcówka `GET /api/catalog/protection/device-types` ISTNIEJE (`api/catalog.py:624`; 51 urządzeń / 11 producentów zmierzone na żywym backendzie) → test realny; `sld-supply-path-visibility:215,235` → bramka warunkowa wzorcem `catalog-enforcement`, wyrocznie z realnego backendu: 5 producentów (nie 4 — Schneider dołączony po powstaniu tytułu), 85 kompletów pól `repo_verified` (nie „10 fallbacków"); `grep "test.skip(true\|test.skip('"` = 0. Weryfikacja Fable na `fable-cv2`: tsc 0, eslint 0, vitest `sld/v3` + `sld/v2/geometry` 135 plików / 2776 (0 wystąpień „same key"), 7 guardów (sld_determinism, no_codenames, forbidden_ui_terms, dead_click, verification_phantom_paths, local_truth, grep_zero) rc=0; e2e realny backend 24/24 passed (critical-engineer-flow 8, sld-supply-path-visibility 9, ux-feedback-loop 7) na WŁASNYCH serwerach 8011/5184 ze świeżą bazą po naprawie runnera; wcześniejsze biegi tej samej trójki: 9/17 (adopcja cudzych serwerów), 23/24 (zimny start Vite 31 s → rozgrzewka `globalSetup`), 22/24 (spec z własną zmienną `API_BASE` trafiał w zastany backend 8000 — ujednolicony na `PLAYWRIGHT_BACKEND_URL`, kontrola klasy: jedyny taki spec). **Znalezisko klasy przy odbiorze (Fable):** mój pierwszy bieg e2e = 9/17 czerwonych przez `ERR_CONNECTION_REFUSED` — `reuseExistingServer: true` adoptował cudze serwery z 8000/5173 (zastany backend innego worktree, cudza baza z projektami; po ubiciu cudzego Vite specy padały), a `poetry run` w worktree trafia w pusty venv bez uvicorna (maskowane przez adopcję). Naprawa u źródła w `playwright.config.ts`/`playwright-run-real.mjs`: porty wyprowadzane z adresów (`PLAYWRIGHT_BACKEND_PORT`/`FRONTEND_PORT`), reuse WYŁĄCZNIE przez `PLAYWRIGHT_REUSE_SERVER=1`, świeża baza i świeży magazyn ENM w katalogu tymczasowym biegu, `PLAYWRIGHT_BACKEND_PYTHON` + `PYTHONPATH=src` dla spawnowanego backendu; w CI zachowanie bez zmian |
| CV-3.3-A | kasacja E3 (`ExecutionEngineService`), E2-widmo (`unified_runs` + `unified_run_dispatch` + `analysis_dispatch`), martwych podmodułów R2 (`analysis_run/{export_service,results_inspector,orchestrator,catalog_context,dtos}`); testy fizyki na kanon | agent (worktree na `a5a68bf3`, `karta_cv33a.md`) | **odebrana** (`585d10f1`, cherry-pick bez konfliktów): 12 plików produkcji / 3723 linie (E3 1189, E2-widmo 916 — dyspozytor `analysis_dispatch` okazał się wyłącznym konsumentem, więc poszedł razem; R2 martwe 1618), `api/execution_runs.py::get_engine` i router `unified_runs` zdjęte, `analysis_run/__init__` eksportuje tylko żywe; testy: 3 pliki skasowane (1894 linii), 5 plików R2/E2 z katalogów testowych, 21 edytowanych chirurgicznie, 3 NOWE pliki kanonu (1114 linii, 33 testy: rozpływ promieniowy/pierścień z determinizmem, znak mocy węzłowej vs niezależny bilans na 4 sieciach, suma wstrzyknięć = straty, szyna tranzytowa = 0 w 3 topologiach, golden SC3F/SC2F/SC1F z 3F>2F, determinizm dwóch biegów per typ zwarcia, wkład sieci nadrzędnej > 0, ślad WHITE BOX niepusty, kształt kontraktu + persystencja bez zmiany wartości); świadomie NIE migrowane z uzasadnieniem: fizyka PV-bus (kanon nie buduje `PVSpec` — dług A3-04 → CV-4; dowód na solverze FROZEN w `test_power_flow_v2`), wkład falownika w golden (dowód w `test_enm_mapping`), mechanika E3 (bramki, cykl życia — bez zamiennika). Inwentarz karty skorygowany pomiarem: 15 plików testowych E3 (nie 5), w tym 2 z granicy C6 (`test_pr19_fault_scenario.py` — klasa `TestExecutionEngineScenarioIntegration` + 2 pomocniki; `test_fault_scenarios_run_integration.py` — fikstura wołała `get_engine`) naprawione chirurgicznie bez dotknięcia domeny C6 (53+39 testów zielone). Guard wskrzeszenia: +8 ścieżek importu, +5 nazw; `no_direct_fault_params_guard.WHITELISTED_PATHS` — martwy wpis wskazujący skasowany plik złapany przez własną zapadkę staleness; snapshot OpenAPI 318 → **315** (dokładnie `POST /api/runs/{power-flow,protection,short-circuit}`), MACIERZ 3 wiersze `usuniety`. Agent: 6836 passed / 7 skipped, mypy 795, 11 guardów + 98 testów własnych. Weryfikacja Fable na `fable-cv4`: cherry-pick bez konfliktów; pytest api/application/analysis/enm/invariants/network_model/e2e/domain/ci + 3 pliki graniczne 7374 passed / 7 skipped (698 s); 19 guardów rc=0 po zdjęciu wpisu zapadki `results_inspector.py` z `solver_input_substitute_guard` (własna kontrola staleness guarda złapała skutek kasacji; piny 3636 → 3609 pól, 597 → 583 plików, zapadka 66/346 → 65/345, application 341 → 328, api 66 → 65 — `d53decb2`) + 6 testów własnych guardów; mypy 783 plików 0; snapshot OpenAPI po regeneracji bez zmian (315 ścieżek / 225 schematów); ruff/black 0. Pozostałość → drugi commit agenta (klaster `result_mapping/*` + `AnalysisKind`, 0 konsumentów po kasacji E3) |
| CV-2-W | status przypadku wyprowadzany z biegów w API, kasacja pisarzy `ResultInvalidator`/`mark_*`/`invalidate*` (13 pisarzy w 7 warstwach + 2 końcówki HTTP + klient FE), ładunek komendy w dzienniku, `GET …/enm/rewizje/{n}`, ekran świeżości z listą zmian, `result_status_writer_guard` (budżet 0) | agent (worktree na `9f5fe90e`) | **scalona** (`d8fb25de`/`2a5cb667`/`a79700b8` przez `fable-cv2`, 1 konflikt: `api/enm.py::_invaliduj_wyniki_po_operacji` — wersja CV-2-W = kasacja; snapshot OpenAPI przeliczony na scalonym drzewie: 318 ścieżek / 228 schematów, bez różnic; weryfikacja: backend `tests/api tests/application tests/enm tests/invariants tests/infrastructure tests/e2e tests/domain tests/golden` + scenariusze zwarciowe/seria/kanon **5620 passed, 7 skipped** (515 s), powtórka po naprawie promocji `tests/api tests/invariants tests/application/project_archive` 1101 passed, mypy src 0 (809 plików), black/ruff (src, tests, scripts) 0, `guardy_z_ci.py` komplet (108 testów własnych w 3 guardach; jedyna czerwień `mypy_ratchet_guard` = artefakt `poetry run` w worktree → naprawiona u źródła w `a79700b8`: mypy interpreterem guarda), frontend vitest **890 plików / 12042 passed** (1 skipped, 14 todo), `tsc --noEmit` 0, eslint 0). Znaleziska agenta: (1) `study_cases[]` w ENM (`domain_operations_v2.py`, 8 miejsc, 2 zapisy `status=OUTDATED`) = ZAPIS W PRÓŻNIĘ (`EnergyNetworkModel` nie ma pola, `extra=ignore` gubi klucz przy `model_validate` — sprawdzone empirycznie), zero konsumentów → kasacja C3 procedurą (CV-3.2); (2) pułapka kolejności `migruj_projekt_z_legacy`: aktywny przypadek bez modelu zastanego (`BRAK_LEGACY`) blokował promocję następnych — modele pozostałych przypadków lądowały w `legacy_przypadki/` jako `ROZBIEZNY` wobec NIEISTNIEJĄCEGO modelu projektu → **naprawione u źródła** (`application/twin_key.py`: każdy przypadek w kolejności promocji ubiega się o rolę, magazyn promuje pierwszy z modelem; test `test_migracja_aktywny_bez_modelu_zastanego_nie_blokuje_promocji_nastepnego`, `tests/invariants` 34 passed) |
| CV-3.3-A | E3 (`ExecutionEngineService`, `application/execution_engine/**`, zero konsumenta produkcyjnego) + E2-widmo (`api/unified_runs.py` + wyłączny dyspozytor `application/analysis_dispatch/**`, zero konsumenta frontendu) + R2 podmoduły martwe (`analysis_run/{export_service,results_inspector,orchestrator,catalog_context,dtos}.py`, 1618 linii, zero konsumentów poza własnymi testami) | agent (worktree na `a5a68bf3`, `karta_cv33a.md`) | **wykonana** (jeden commit HEAD worktree gałęzi `claude/mv-design-pro-twin-audit-u4lhy0`, bez pusha — SHA nieprzytoczone celowo: commit nie może cytować własnego hasha bez zmiany go samego; `git log -1` na worktree po scaleniu; brak osobnego przeglądu/cherry-picku w tej sesji — oczekuje na weryfikację niezależną). Inwentarz karty zweryfikowany grepem na HEAD zamiast przyjęty z tekstu (zastrzeżenie zlecenia: CV-3-W/C6-PERSIST/CV-2-W weszły po spisaniu karty) — **usunięto** `application/execution_engine/{service,errors,load_flow_run_input,__init__}.py` (794+50+316+29=1189 linii), `api/unified_runs.py` (188), `application/unified_run_dispatch.py` (39), `application/analysis_dispatch/{service,summary,__init__}.py` (629+51+9=689, zero konsumenta produkcyjnego poza modułami E2-widmo — zweryfikowane grepem, kasacja niejawnie objęta zakresem karty), `analysis_run/{export_service,results_inspector,orchestrator,catalog_context,dtos}.py` (230+523+167+178+520=1618) — razem **3723 linie produkcyjne**; `api/execution_runs.py::get_engine()` i jego import zdjęte; trasy `/api/runs/{short-circuit,power-flow,protection}` zdjęte z `main.py`. Testy fizyki (zbieżność rozpływu, znak mocy P/Q generacja-dodatnia/odbiór-ujemna, wielomian ZIP, węzły tranzytowe, prądy SC3F/SC2F/SC1F, wymóg Z0 dla 1F, porządek 3F>2F, determinizm) przepisane na tor kanoniczny (`enm.canonical_analysis.create_run/execute_run` na TEJ SAMEJ sieci) w 3 nowych plikach `tests/enm/test_load_flow_migracja_e3_{radial_pierscien,znak_mocy}.py` + `test_short_circuit_migracja_e3_golden.py` (1114 linii, 33 testy — patrz §F dla tabeli stary→nowy); testy mechaniki silnika E3 (statusy, bramkowanie readiness/eligibility, wyjątki `execution_engine/errors.py`) usunięte bez zamiennika (nie niosą fizyki). **Konsumenci ukryci wykryci dopiero pełnym pytest, poza pierwotnym grepem symboli** (przyczyna: literał stringowy trasy zamiast importu symbolu; fikstura `get_engine()` w pliku spoza pierwotnie planowanego zakresu; C6-boundary): `tests/api/test_protection_api_contract.py` (asercja literału `/api/runs/*`), `tests/analysis/test_energy_validation.py::TestOrchestrator` (zaplanowane wcześniej, ale pominięte w wykonaniu — naprawione po czerwonym pytest), oraz DWA pliki z granicy C6 „nie dotykać" (`tests/test_pr19_fault_scenario.py`: klasa `TestExecutionEngineScenarioIntegration` + 2 pomocniki prywatne skasowane chirurgicznie, 53/58 testów pliku nietkniętych; `tests/test_fault_scenarios_run_integration.py`: fikstura `_reset_services` przestawiona z `get_engine()` na sam `reset_canonical_runs()`) — w obu przypadkach fizyka/logika C6 WŁASNA (scenariusze zwarciowe, ich domena/API/hash) nietknięta, skasowany był wyłącznie E3-owy dodatek zależny od kasowanego modułu; fizyka klasy `TestExecutionEngineScenarioIntegration` ma równoważny dowód w `test_fault_scenarios_run_integration.py` (CV-1-W/C6-PERSIST, tor kanoniczny) i w nowym `test_short_circuit_migracja_e3_golden.py` tej karty — zero utraconego dowodu fizyki. Weryfikacja: pełny pytest `tests/api tests/application tests/analysis tests/enm tests/invariants tests/network_model tests/e2e` + `tests/test_pr19_fault_scenario.py tests/test_fault_scenarios_run_integration.py tests/test_batch_execution.py` **6836 passed, 7 skipped (649 s)**, mypy src 0 (795 plików), black/ruff (src, tests) 0, guardy `api_lifecycle_guard`/`route_prefix_guard`/`router_mount_guard`/`legacy_public_path_guard`/`vulture_guard`/`import_graph_guard`/`no_direct_fault_params_guard` RC=0 (ten ostatni: własna zapadka staleness złapała martwy wpis `WHITELISTED_PATHS` wskazujący skasowany `execution_engine/service.py` — naprawiony), `legacy_public_path_guard` rozszerzony o 8 zakazanych ścieżek importu + 5 zakazanych nazw i 2 nowe testy własne (98 testów guardów razem PASS). Snapshot OpenAPI przeliczony skryptem: 318→315 ścieżek (dokładnie i wyłącznie `/api/runs/{power-flow,protection,short-circuit}`), oznaczone `usuniety` w `MACIERZ_KOMPATYBILNOSCI_API.md` z datą. **Świadomie poza kartą** (udokumentowane, nie przemilczane): (1) fizyka PV-bus (regulacja napięcia na szynie generatorowej, `PVSpec`) nie ma odpowiednika w torze kanonicznym — asembler kanonu nigdy nie buduje `PVSpec` (`pv_bus_ids=[]` zawsze, dług A3-04 zamykany w CV-4); ta fizyka pozostaje dowiedziona niezależnie w `tests/test_power_flow_v2.py`/`test_power_flow_fast_decoupled.py` (solver FROZEN, nietknięty), migracja E3 świadomie NIE odtwarza jej na kanonie — udokumentowane w module-docstringu `test_load_flow_migracja_e3_znak_mocy.py`; (2) skutek uboczny kasacji E3/E2-widmo: `application/result_mapping/{sc_binding_meta.py, short_circuit_to_resultset_v1.py, load_flow_to_resultset_v1.py, protection_to_resultset_v1.py, protection_to_overlay_v1.py}` i `domain/analysis_kind.py::AnalysisKind` straciły JEDYNEGO konsumenta produkcyjnego (E3) — zero konsumentów w `src/` po kasacji; pełna weryfikacja/kasacja odłożona do osobnej karty (CV-3.3-B) z powodu głębi wymaganej inwentaryzacji konsumentów testowych, nie z powodu „poza zakresem" — inwentarz zapisany w `CONVERGENCE_ROADMAP.md` CV-3.3-A. E4 (`BatchExecutionService`→orkiestrator) i R2 tabela żywa/R3/R4 — POZA TĄ KARTĄ (CV-3.3-B) |
| CV-3.3-A2 | domknięcie klastra osieroconego znalezionego przy CV-3.3-A (`application/result_mapping/{load_flow_to_resultset_v1,protection_to_overlay_v1}.py` + `domain/analysis_kind.py::AnalysisKind`) — na polecenie koordynatora (odbiór CV-3.3-A pkt 6.2) | agent (ten sam worktree, na `999d091a`; agent `fbdf65a9`) | **odebrana** (`e9e7c645` = cherry-pick `91ba3d60` z `fable-cv4`; pierwotny cherry-pick `fbdf65a9` na `cb2e242e` z 2 konfliktami: pin guarda podstawień i sąsiednie wiersze §E — rozwiązane; pin przeliczony pomiarem: 3614 → **3608** pól, 584 → **582** plików, `application` 328 → **326**, zapadka 65/345 bez zmian). Weryfikacja Fable na `fable-cv4`: pytest api/application/analysis/enm/invariants/network_model/e2e/domain/ci + 5 plików granicznych (`test_pr19_fault_scenario`, `test_fault_scenarios_run_integration`, `test_batch_execution`, `test_protection_determinism_guards`, `test_pr18_sc_integration`) **7441 passed / 2 skipped** (471 s; 2 skipy = klasa SKIP-INWENTARZ, osobna karta), 19 guardów rc=0 (`legacy_public_path`, `protection_no_heuristics`, `resultset_v1_schema`, `trace_ui_leak`, `grep_zero`, `api_lifecycle`, `route_prefix`, `router_mount`, `vulture`, `import_graph`, `no_direct_fault_params`, `solver_input_substitute`, `docs`, `docs_archive`, `utf8_mojibake`, `repo_hygiene`, `arch`, `verification_phantom_paths`, `v12xx_canon`) + 3 self-testy (21 + 67 + 3), mypy src 0 (782 pliki), snapshot OpenAPI po regeneracji bez zmian (317 ścieżek po FAB-J), ruff/black (src, tests, scripts) 0. Meldunek agenta: **Decyzja per moduł, nie jedna decyzja na klaster:** `AnalysisKind` i `load_flow_to_resultset_v1.py`/`protection_to_overlay_v1.py` **usunięte** (zero konsumenta produkcyjnego, zweryfikowane w `src/`/`tests/`/`scripts/`/`docs/`; tor kanoniczny ma własnego, ujednoliconego producenta `ResultSetV1` — `application/result_mapping/canonical_run_to_resultset_v1.py::build_resultset_v1_from_canonical_run`, żywe wywołania `api/result_contract_v1.py:46` i `application/analyses/lv_domain/projection_v1.py:151`); testy dedykowane usunięte w całości (`test_load_flow_resultset.py` 158 linii, `test_protection_overlay_v1.py` 572 linii), `test_protection_determinism_guards.py` chirurgicznie (klasa `TestOverlayDeterminism` skasowana, krok nakładki wycięty z `TestEndToEndPipeline`, determinizm silnika/koordynacji/raportu zostaje). **`sc_binding_meta.py`/`short_circuit_to_resultset_v1.py`/`protection_to_resultset_v1.py` — STOP, NIE usunięte**: `scripts/resultset_v1_schema_guard.py::PROTECTED_FILES` zamraża oba pliki `*_to_resultset_v1.py` SC/Protection mimo zera importerów w `src/` (jedyne trafienia to komentarze, nie importy) — kasacja pliku zamrożonego to B-01 (edycja rdzenia bez zgody właściciela), jedno z 3 dozwolonych zatrzymań; `sc_binding_meta.py` zostaje razem z nimi jako nierozdzielna testowana para w `TestResultMapper`. Weryfikacja: pytest ten sam zbiór karty macierzystej + `tests/domain tests/ci` **7403 passed, 7 skipped (586 s)**, mypy src 0 (792 pliki), black/ruff (src, tests) 0, guardy `legacy_public_path_guard` (+3 wpisy importu, +4 nazwy, +1 test własny)/`protection_no_heuristics_guard` (SCAN_FILES -1)/`resultset_v1_schema_guard`/`trace_ui_leak_guard`/`grep_zero_guard`/`api_lifecycle_guard`/`route_prefix_guard`/`router_mount_guard`/`vulture_guard`/`import_graph_guard`/`no_direct_fault_params_guard` RC=0, `solver_input_substitute_guard` RC=0 (65 plików/suma 345, w dół z 66/346 — zdjęty martwy wpis `results_inspector.py` pozostały po CV-3.3-A, jej test własny nie był w Definition of Done tamtej karty). Snapshot OpenAPI: bez zmian (315 ścieżek/225 schematów, plik bajt w bajt identyczny — żaden z 3 skasowanych modułów nie miał trasy `/api` ani nie karmił response_model żywej trasy). Pełny inwentarz per-moduł, w tym cytaty plik:linia dla decyzji STOP → `CONVERGENCE_ROADMAP.md` CV-3.3-A2 |
| CV-3.3-B | porównania A/B (rozpływ P20c, zabezpieczenia P15b, ogólne P10b) przepięte na R1 (B1); bieg zabezpieczeń jako `CanonicalRun.analysis_type="protection_sn"` (B2); kasacja R2 `AnalysisRunService` + R3 `study_runs`/`study_results` procedurą 7 kroków (B3); E4 podzielone do CV-3.3-C (preautoryzowany podział z inwentarzem) | agent (worktree, `karta_cv33b.md`) | **odebrana** (`554f7aa3` = cherry-pick `666632a4`, `97271345` = cherry-pick `61258aca` z worktree agenta na `f85ef79f`, oba bez konfliktów; 4 PNG zrzutów `docs/audit/visual/flow-ekspert/*` z commitu agenta przywrócone — artefakty testów nie wchodzą do repo). Domknięcie Fable `a3b31fef`: agent dopisał do zapadki R2 `scenario_copy_guard` wyjątek per plik (`project_archive/service.py`: `CanonicalRun(...)` poza fabryką) z uzasadnieniem „deserializacja to inna klasa" — klasa inna, lekarstwo to samo: jeden dom konstrukcji `CanonicalRun` to moduł fabryk, więc import archiwum woła nową trzecią fabrykę `enm.canonical_analysis.odtworz_bieg_z_archiwum` (pola historyczne 1:1, nowe identyfikatory biegu/przypadku/projektu, koperta i migawka nietknięte), zapadka ZASTANE wraca do `{}` (guard: 419 plików, 0 trafień), test własny pinuje zero, test fabryki (pola 1:1 × nowe id × brak pól opcjonalnych w starszym archiwum). Domknięcie Fable 2 (`e65782b6`): `readiness_consumption_guard` czerwony na integracji — jedyny emiter kanonicznego `source.connection_missing` był w skasowanym R2 (`analysis_run/service.py:751`, warunek legacy „BoundaryNode must have GRID supply source”), a tor kanoniczny nigdy go nie emitował; assembler (`enm/mapping.py`) POMIJAŁ źródło wskazujące nieistniejącą szynę cichym `continue` w obu pętlach (Z_Q i Y0) — sieć liczyła się bez zasilania, bez śladu i bez kodu gotowości (klasa A6-12). Naprawa u źródła, nie rezerwacja: reguła walidatora `sources.bus_missing` (BLOKADA, `SourceModal`/`bus_assignment`) → most `ODWZOROWANIE_WALIDATOR_NA_KANON` → `source.connection_missing`; assembler odmawia `ValueError` z nazwą źródła i szyny; testy iloczynu cech (nieistniejąca szyna × pusty `bus_ref` × źródło podłączone; wzbogacenie mostu; odmowa assemblera). Weryfikacja Fable na `fable-cv2`: pytest zestawu karty + `test_p10b_comparison`/`test_protection_analysis`/`test_protection_comparison`/`test_professional_invariants` ****6270 passed / 0 failed / 10 deselected** (536 s; zestaw karty + `tests/golden` + `tests/reference_networks` na `3449b087`)** (pierwszy bieg 6036 passed / 2 failed w `test_canonical_analysis_audit2.py` — artefakt edycji `canonical_analysis.py` W TRAKCIE biegu: `inspect.getsource` czytał plik z dysku z przesuniętymi liniami; plik przebiegł 8/8 na stanie zacommitowanym, pełny zestaw powtórzony), mypy 2 pliki domknięcia 0 (agent: 776 plików 0), black/ruff (config backendu, także `scripts/`) 0, snapshot OpenAPI po regeneracji bez zmian (317 ścieżek / 228 schematów), guardy przedpushowe P0 54 + 31 guardów innych workflowów: pierwszy bieg fail=1 (`readiness_consumption_guard` — naprawiony domknięciem 2, rc=0), reszta rc=0; `scenario_copy_guard` 419 plików / 0 trafień; frontend tsc 0, eslint 0 (14 plików), vitest `ui/power-flow-comparison ui/protection-comparison ui2/wyniki/porownanie` 8 plików / 153; e2e na własnych portach `flow-ekspert-screenshot` + `porownanie-zwarc-delty` + `s913-porownanie-z-kanwy` + `critical-run-flow` **19 passed** (1,9 min) i `wszystkie-sceny -g porownanie` **2 passed** (8043/5213, świeża baza; zrzuty odnowione i przywrócone). Decyzje agenta przyjęte z konsekwencją: (1) `analysis_runs_index` zostaje (koordynacja nastaw, byt niezależny od R2 — sprzeczność opisu wiersza B.2 rozstrzygnięta w konstytucji), (2) tabela `analysis_runs` + `AnalysisRunRepository` zostają jako zależność legacy kreatora → CV-4 (karta `karta_cv4.md` A4/P2), (3) E4 → **CV-3.3-C** (karta napisana, inwentarz w ROADMAP), (4) martwe strony `ui/{power-flow,protection}-comparison/*Page.tsx` i BRAK ekranu porównania zabezpieczeń w ui2 mimo A16 inwentarza funkcji → **CV-3.3-B2** (karta napisana: jeden ekran porównań z rodzajem rozpływ/zabezpieczenia, kasacja martwych stron) — zdolność bez ścieżki użytkownika nie zostaje bez karty |
| CV-3.3-B2 | porównanie zabezpieczeń A/B osiągalne w ui2 (jeden ekran porównań z przełącznikiem rodzaju rozpływ/zwarcia/zabezpieczenia, D1); model ekranu z wariantem dla zabezpieczeń bez duplikacji logiki wspólnej (koperta/etykieta biegu/proweniencja/dowód, D2); kasacja martwych stron `ui/{power-flow,protection}-comparison/*Page.tsx` (D3) | agent (worktree na `fd7cc0e5` CV-3.3-B, `karta_cv33b2.md`) | **odebrana** (`3d480e8b` = cherry-pick `00e7f4f9` z worktree agenta na `15e8cb0d`, zrebase'owany na `3d47c275`; jedyne konflikty: sąsiednie wiersze evidence). Nowy tryb „Zabezpieczenia" (`TrybZabezpieczen.tsx`) czyta `GET /projects/{id}/protection-runs` (lista A/B) i `POST /api/protection-comparisons` (wynik pełny inline, jak rozpływ) + `GET .../trace` (ślad porównania White Box, na żądanie, zwinięty domyślnie — nowa zdolność, PF jej nie ma). Reużycie zero-duplikacji: `rewizjaLubScenariuszBiegu` (etykieta biegu) wydzielone ze wspólnego ternary rozpływu i zabezpieczeń; `PanelProweniencji.tsx` wydzielony z `EkranPorownania.tsx` do osobnego pliku (unikniony import cykliczny), reużyty bez zmian przez oba tryby (`naLinieProweniencji` strukturalnie przyjmuje `RunProvenance` obu modułów — TS structural typing); `KLUCZ_PROBLEM` (klucz React wiersza) reużyty zamiast drugiej stałej o tej samej wartości; `wagaPL`/`ISSUE_CODE_LABELS` (zabezpieczeń) reużyte zamiast nowych map. Zero-state (D2) „brak przebiegów zabezpieczeń" → NOWA akcja `useAkcjaPrzejdzDoSchematu` (rejestr `akcjeStanuZerowego.ts`): nawigacja do schematu (SLD), gdzie dziś jedynie powstaje bieg zabezpieczeń (SC + `ProtectionRunButton`) — świadomie NIE `useAkcjaUruchomObliczenie` (jej `ExecutionAnalysisType` nie ma `protection_sn`; jedno kliknięcie „Oblicz" nie miałoby pokrycia w backendzie — zero fabrykacji akcji). **Znaleziska uboczne naprawione w tej samej karcie (Zero-Debt, KLASA nie instancja):** (1) `ProtectionComparisonRow.i_fault_a_a/i_fault_a_b/delta_i_fault_a` w `ui/protection-comparison/types.ts` typowane jako nienullowalny `number`, mimo że backend (`ComparisonRowResponse`, komentarz „FAB-E (E1)") jawnie wysyła `float | None` — naprawione na `number | null` (deklaracja bez pokrycia w kontrakcie, KLASA-NIE-INSTANCJA §4: „deklaracja bez testu = fałszywa pewność"); (2) kasacja martwych stron ujawniła DRUGI komplet Tailwind-owych map bez konsumenta produkcyjnego (jedyny konsument = kasowana strona) — nie tylko w `protection-comparison/types.ts` (`STATE_CHANGE_COLORS`, `SEVERITY_LABELS`, `SEVERITY_COLORS`), ale TA SAMA klasa w `power-flow-comparison/types.ts` (`ISSUE_CODE_LABELS`, `SEVERITY_LABELS`, `SEVERITY_COLORS`) oraz cztery eksporty specyficzne dla zakładek `PowerFlowComparisonPage` (`PowerFlowComparisonTab`, `COMPARISON_TAB_LABELS`, `CONVERGENCE_LABELS`, `getDeltaColor`, `getVoltageDeltaColor`) — wszystkie skasowane, oba testy modułów przycięte do kontraktu wciąż żywego (testowanie martwej stałej to martwy test). Weryfikacja w worktree: `npm ci` (środowisko puste na starcie sesji), `tsc --noEmit` 0 błędów (cały projekt); eslint (`ui2/wyniki/porownanie`, `ui2/wyniki/wzorzec`, `ui/power-flow-comparison`, `ui/protection-comparison`, `creator-harness-main.tsx`) `--max-warnings 0` 0; `tsconfig_gate_guard` 136 poza bramką (budżet 136, bez wzrostu), 0 w bramce; `dead_click_guard`/`ui_terminology_guard`/`forbidden_ui_terms_guard`/`no_codenames_guard`/`dialog_completeness_guard` RC=0; vitest `--no-file-parallelism` trzech obszarów **166 passed (10 plików)**, w tym 40 nowych (model zabezpieczeń × stan zerowy × proweniencja × filtr „tylko zmiany" × ślad na żądanie). E2E realny backend, porty własne (8053/5223): `flow-ekspert-screenshot` + `porownanie-zwarc-delty` (KD-3) + `s913-porownanie-z-kanwy` **18 passed** (3,3 min); `wszystkie-sceny-screenshot -g porownanie` **2 passed**; NOWY krok — drugi test w `porownanie-zwarc-delty.spec.ts` (dwa biegi SC_3F na modelu przed/po zmianie długości odcinka → dwa biegi zabezpieczeń → porównanie z ekranu): **1 passed** (50,5 s), z dowodem fizycznym (odcinek dłuższy → impedancja większa → prąd zwarciowy w ocenie zabezpieczeń MNIEJSZY, zgodnie z prawem Ohma, nie dowolna zmiana liczby) i zrzutami `docs/audit/visual/flow-ekspert/cv33b2-porownanie-zabezpieczen-{dark,light}.png` (oba motywy, tryb ekspercki, trzy zakładki rodzaju widoczne). Zrzuty PNG odnowione przez pełny bieg speców zrzutowych (8 scen niezwiązanych z kartą + KD-3/porownanie-q, gdzie treść ekranu się nie zmieniła poza pozycją zakładek) przywrócone `git checkout --` — jedyne NOWE zrzuty w commicie to para `cv33b2-*`. Backend NIETKNIĘTY (kontrakt CV-3.3-B już kompletny — zero rozbudowy addytywnej, zero pytest/snapshot OpenAPI wymaganych) **Odbiór koordynatora (Fable, 2026-09-05, worktree `fable-cv3`):** tsc 0 (cały projekt); eslint 19 zmienionych plików `--max-warnings 0` 0; vitest `ui2/wyniki/porownanie ui/power-flow-comparison ui/protection-comparison ui2/spaces/wyniki` **15 plików / 239 testów**; guardy dead_click/dialog_completeness/forbidden_ui_terms/no_codenames/ui_terminology/ui_no_physics/tsconfig_gate (136/136)/docs/local_truth rc=0; e2e na własnych portach (8061/5231, świeża baza): `flow-ekspert-screenshot` + `porownanie-zwarc-delty` + `s913-porownanie-z-kanwy` **19 passed** (2,3 min) + `wszystkie-sceny-screenshot -g porownanie` **2 passed** (40,9 s); zrzuty regenerowane przywrócone `git checkout` (dwa NOWE zrzuty `cv33b2-porownanie-zabezpieczen-{dark,light}.png` zostają jako dowód do werdyktu wizualnego właściciela — B-02, nie samocertyfikacja). Backend nietknięty przez kartę (jedyna zmiana poza FE to typ frontendowy) — pełna regresja backendu CV-4.1 na `3d47c275` obejmuje ten stan. |
| CV-3.3-C | E4 (`BatchExecutionService`, trzy słowniki w pamięci `_batches`/`_case_batches`/`_pinned_hashes`) → trwały rejestr `run_batches` (SQL); wykonanie ciągłe (status PARTIAL zamiast zatrzymania na pierwszej awarii); świeżość per pozycja | agent (worktree, `karta_cv33c.md`) | **odebrana** (`6d30e663` = cherry-pick `c0d28173` z worktree agenta na `68cafc9b`, konflikty: piny `test_solver_input_substitute_guard` z pomiaru 3604 pól / 576 plików / `application` 319, ROADMAP/EVIDENCE keep-both; domknięcie 1 `f15ed18e`; rebase na `5793a3e0` bez konfliktów). Tabela `run_batches` (ORM `RunBatchORM`, migracja `011_run_batches.sql`, repozytorium `RunBatchRepository` obok `CanonicalRunRepository` — reużyty `get_canonical_run_session_factory`, zero nowego cache'u silnika). Domena `domain/batch_job.py` (skasowany) → `domain/run_batch.py`: status serii pięciowartościowy CREATED/RUNNING/FINISHED/FAILED/PARTIAL (słownik dzielony z `CanonicalRun.status`, KLASA NIE INSTANCJA — nie nowy równoległy PENDING/DONE), `finalize_batch_status` wyprowadza status z pozycji. Wykonanie CIĄGŁE (zmiana zachowania wymuszona kartą §0 C2): poprzednia wersja zatrzymywała się na pierwszej awarii — teraz KAŻDA pozycja próbowana niezależnie; `stop_on_failure` sprawdzone w kontrakcie (nie istnieje) — brak fantomu. `BatchExecutionService` BEZSTANOWY (wzorzec `FaultScenarioService`, C6-PERSIST) — singleton zostaje wyłącznie jako punkt wstrzyknięcia testów. Kontrakt HTTP (4 końcówki) zachowany bit w bit + pola addytywne: `finished_at`, `name` (kolumna bez dostawcy UI w tej karcie, świadomie zawsze `null`), `envelope` (koperta rewizji z chwili utworzenia serii, `zbuduj_koperte` — TA SAMA funkcja co bieg pojedynczy, `scenario_ref=None`; seria NIE kopiuje migawki do pozycji — każda czyta model na własny rachunek przy `create_run`), `items[]` (position/scenario_id/analysis_type/options_hash/canonical_run_id/status/error_message + `result_freshness`/`result_freshness_reason`/`result_freshness_reason_pl` liczone NA ŻYWO per odczyt — `application/result_freshness.swiezosc_biegu_kanonicznego`, ta sama funkcja co nakładka pojedynczego biegu, nigdy „zielona na zawsze"). **Znalezisko uboczne naprawione (napotkany błąd typów, Zero-Debt pkt 1):** `SeriePanel.tsx` indeksował słownik wariantu tagu statusu SERII (`Record<BatchStatus, string>`) WARTOŚCIĄ statusu BIEGU (`RunStatus`) — działało przypadkiem, bo obie osie dzieliły cztery z pięciu nazw (`PENDING/RUNNING/DONE/FAILED`) do czasu zmiany słownika batcha w tej karcie; naprawione rozdzieleniem na dwa poprawnie typowane słowniki (`WARIANT_STATUSU_SERII`/`WARIANT_STATUSU_BIEGU`, drugi 1:1 ze wzorcem już istniejącym w `WierszPrzebiegu.tsx`) — `tsc --noEmit` złapał to jako błąd TS7053 dopiero po zmianie nazw, nie wcześniej. Dead code usunięty przy okazji (zero konsumentów produkcyjnych, zmierzone grepem): `application/read_models/results_workspace_projection.py` (PR-22/PR-23, duckypował na `domain.batch_job.BatchJob`) + 2 pliki testów. Pin `solver_input_substitute_guard` przeliczony z uzasadnieniem: 3599→**3594** pól (−5: `batch_id`/`scenario_ids`/`run_ids`/`result_set_ids`/`errors` — pięć nazw AnnAssign unikalnych dla starego `BatchJob`, zastąpionych w `RunBatch` przez `@property` wyprowadzone z `items`, których skaner AST nie widzi — reszta starych nazw już istniała gdzie indziej w CONTRACT_SOURCES, np. `study_case_id` w `domain/fault_scenario.py`), 576→**575 plików** (`application` 320→319: −1 plik po kasacji martwego modułu); zapadka fizyczna/wykluczenia bez zmian (63/312, 17/49). Weryfikacja: pytest `tests/api tests/application tests/enm tests/infrastructure tests/e2e tests/ci` + `tests/test_batch_execution.py` (`-m "not pandapower"`) — pierwszy bieg **5236 passed / 1 failed / 10 deselected** (633 s), jedyna awaria `test_openapi_snapshot.py` (kontrakt zmieniony świadomie — pola addytywne 2 końcówek + 1 nowy schemat) naprawiona regeneracją snapshotu (317 ścieżek / 229 schematów) i zweryfikowana osobno (2 passed); DRUGI, czysty bieg tego samego zestawu **5237 passed / 0 failed / 10 deselected (411,50 s / 6:51)**; mypy src **0 (776 plików)**; ruff/black (src, tests, `../scripts`) **0**; guardy `api_lifecycle`/`route_prefix`/`router_mount`/`legacy_public_path`/`scenario_copy` (zapadka **0**)/`solver_input_substitute` (PASS, zero nowych podstawień)/`grep_zero`/`vulture`/`import_graph`/`docs` **RC=0** pojedynczo; testy własne trzech guardów (`legacy_public_path`+`scenario_copy`+`solver_input_substitute`) **106 passed**. Frontend: `tsc --noEmit` **0 błędów** (całość, po naprawie błędu wyżej), eslint `ui2/spaces/obliczenia ui/study-cases` **0**, vitest tych samych dwóch obszarów **26 plików / 297 testów passed**, `tsconfig_gate_guard` **136/136 budżet bez wzrostu** (0 błędów w bramce, 1 wyciszenie bez zmian). E2E na własnych portach (8051/5221, świeża baza): `grep -l batch e2e/*.spec.ts` przed kartą — brak; nowy spec `critical-batch-flow.spec.ts` (seria 2 pozycji przez `SeriePanel` na realnym backendzie, IEC 60909 na realnej sieci GPZ, odświeżenie strony, seria+statusy nadal widoczne) **1 passed (48,9 s)**. Dokumentacja: `CANONICAL_TWIN_ARCHITECTURE.md` §B.2 (wiersze E4 + R4 → ZREALIZOWANE), `CONVERGENCE_ROADMAP.md` (wiersz CV-3.3-C z pełnym inwentarzem), `MACIERZ_KOMPATYBILNOSCI_API.md` (4 wiersze końcówek serii zaktualizowane, pola addytywne opisane) **Odbiór koordynatora (Fable, 2026-09-05, worktree `fable-cv4`):** domknięcie 1 — kolumna `name` (karta C1) była DEKLARACJĄ BEZ DOSTAWCY (POST nie przyjmował `name`, UI bez pola; klasa odwrotna do fantomu UI): pole „Nazwa serii (opcjonalnie)” end-to-end — `CreateBatchRequest.name` (max 200, pusty napis = brak nazwy, normalizacja w API), `create_batch(name=…)` → `new_run_batch(name=…)`, store `utworzIWykonajSerie(caseId, ids, nazwa?)`, `SeriePanel` (pole + nazwa na karcie serii, reset po uruchomieniu i po zmianie przypadku), adapter/strings/CSS; testy: backend (POST z nazwą → rekord → GET lista/szczegół; same białe znaki → `None`; 201 znaków → 422), vitest panelu (nazwa w body POST przycięta, na karcie, pole wyczyszczone; seria bez nazwy bez pustej etykiety), e2e realny backend `critical-batch-flow` (nazwa przeżywa odświeżenie strony) — **1 passed** (46,7 s, porty 8091/5261). ROADMAP: zastany wiersz „NIE ZROBIONE” usunięty (keep-both z wierszem DONE agenta dawał dwa wiersze CV-3.3-C). Snapshot OpenAPI przeliczony (317 ścieżek / 229 schematów, `test_openapi_snapshot` 2 passed). Weryfikacja niezależna: pełny pytest `tests/` z `-m "not pandapower"` **10930 passed / 0 failed** (1079 s, drzewo z domknięciem na `68cafc9b`); `test_batch_execution.py` 32 passed; `guardy_z_ci.py` 86/86 guardów z CI (KOMPLET ZIELONY) + 548 testów własnych guardów; `tests/golden/parytet_assemblera` po rebase na `5793a3e0` 4 passed (252/252 pod widokiem z `5793a3e0`); tsc 0; eslint 7 plików 0; vitest `ui2/spaces/obliczenia` + `ui/study-cases` **26 plików / 298**; guardy dead_click/dialog_completeness/forbidden_ui_terms/no_codenames/ui_terminology/ui_no_physics/tsconfig_gate (136/136)/api_lifecycle/docs/local_truth rc=0. |
| FAB-J | pozostałe katalogi statyczne kreatora DER (krzywe PF, transformatory dedykowane, profile NC RfG + krzywe LVRT/HVRT, poziomy napięcia nN, pakiety baterii BESS) jako DRUGA kopia danych katalogowych/normatywnych w UI (klasa FAB-I, pozycja P1-9) + klasyfikacja modułu NC RfG liczona w UI (`deriveModuleTypesForPowerKw`: 200 kW / 10 MW / 50 MW bez kryterium napięcia, bez oparcia w normie) | agent (worktree na `28433591`, rebase własny na `58b55777`, `karta_fabj.md`) | **odebrana** (`0346a527` = cherry-pick `af9da194` bez konfliktów na `d0fbc6de`): `station-der/catalogs.ts` −1116 linii — krzywe PF i transformatory dedykowane ze snapshotu audytu 2 (już pobieranego przez kreator, zero nowego zapytania), operatorzy NC RfG + JEDNA para krzywych LVRT/HVRT na operatora z `GET /api/ncrfg-tests/catalog` (pole addytywne `ride_through` z `NcRfgProfile.voltage_levels`; front przestał udawać wybór krzywej per moduł, którego backend nie oferuje), poziomy nN wyprowadzone z `un_kv < 1 kV` katalogu przekształtników (zero nowej końcówki), pakiety baterii BESS z NOWEGO katalogu backendu `network_model/catalog/mv_bess_battery_catalog.py` (namespace `BATERIA_BESS`, kontrakt `BESSBatteryType`, 2 profile referencyjne LFP bez marki w identyfikatorze — wzorzec K-Q; `GET /api/catalog/bess-battery-types`), `four_quadrant` wyłącznie z `qmin_mvar`/`qmax_mvar` katalogu (`null` = brak danych, nigdy założenie „każdy BESS"); klasyfikacja modułu NC RfG A/B/C/D w backendzie: `compliance/nc_rfg_modul.py` DELEGUJE do `NcRfgProfile.classify_module` zamrożonego profilu solvera PTPiREE (JEDNO źródło progów — agent najpierw wniósł własną tabelę URE równolegle do YAML solvera, przy odbiorze usunięta jako druga fizyka tej samej klasyfikacji; solver nietknięty, B-01), `GET /api/ncrfg-tests/modul` + weryfikacja w `POST …/generators` (422 `generator.nc_rfg_module_mismatch`, nigdy cicha korekta; napięcie punktu przyłączenia z MODELU: jawny `bus_ref` → strona górna transformatora dedykowanego → szyna nN; brak napięcia/deklaracji nie blokuje zapisu — domysł byłby fabrykacją); test parytetu compliance ↔ profil: 5 operatorów × 3 progi × {próg, próg−ε, próg+ε} (92 testy w `tests/compliance/test_nc_rfg_modul.py`). Zero-Debt przy okazji: `SldDetailDrawer` po zmianie mocy/napięcia wysyłał moduł SPRZED przeliczenia (422 bez ostrzeżenia w UI) → klasyfikacja pobierana świeżo w chwili zapisu dla dokładnie wysyłanej pary (moc, napięcie); rebase odsłonił `_seed_station_z_szyna_110kv` piszący pod surowym `case_id` mimo klucza projektu CV-2-W → translacja `_klucz_modelu` jak w siostrzanej fikstury. Rozbieżność progów YAML (1 MW / 50 MW = maksima NC RfG dla CE) vs decyzja URE 2018 (200 kW / 10 MW) → **OD-5** (§I), do decyzji kod podąża za profilem solvera. Weryfikacja Fable na `fable-cv5`: pytest zestawu karty (bez pandapower) **4140 passed, 10 deselected** (530 s), 21 guardów rc=0, mypy 0, ruff/black 0, snapshot OpenAPI 315 → **317** (`GET /api/catalog/bess-battery-types`, `GET /api/ncrfg-tests/modul`; 225 schematów; regeneracja na `0346a527` bez różnic), tsc 0, eslint 0, vitest network-build/workspace/sld-v2-canvas/ui2-kreatory/ui2-oze **199 plików / 2259**, e2e `critical-der-config` + `critical-oze-evidence` na WŁASNYM backendzie/frontcie (8014/5186, świeża baza) **2 passed**; pin guarda podstawień 3609 → 3614 pól, 583 → 584 plików, `network_model` 144 → 145 (nowy kontrakt `BESSBatteryType`), self-test 67/67. Nienaprawione, zgłoszone wprost → FAB-K: (a) wyspa danych referencji DER poza 9 polami wiązań V12K-238 (`regulation_profile_ref`, `bess_operation_mode_refs`, `ptpiree_certificate_ref`, `controller/battery/cable/bay_catalog_ref`, `connection_node_ref`, `voltage_level_ref`, `ct_accuracy_class`, `ct_application` — tylko Zustand, znikają po odświeżeniu/reimporcie), (b) 4 z 6 wariantów przyłączenia (`SN`, `at_zksn`, `at_branch_pole`, `at_cable_joint`) mapowane na `block_transformer` bez pickera transformatora w UI → gwarantowany 422 `generator.block_transformer_catalog_missing`. Domknięcia PO pushu (klasa „test montuje drzewo czytające snapshot audytu 2 przez React Query bez dostawcy" — zestaw vitest odbioru karty nie obejmował korzenia aplikacji; CI Frontend checks czerwony 7732c8be…6f814f71): `b40e1778` (App.test, ui2 integracja → `renderWithQueryClient`), `f790c99b` (helper jako `wrapper` RTL, bo `rerender` gubił dostawcę — etap10 H PV→BESS→FW), `92b63674` (shellA11y; inwentarz `<App`/`<AppRoot` we wszystkich testach = 3 pliki; komunikat tego commitu podaje 36 testów — zmierzone 23). Pełny lokalny vitest na głowie przed ostatnią poprawką: 891 plików / 12041 passed / 1 failed (shellA11y) / 1 skipped / 14 todo (1782 s). Zasada od tej karty: zmiana hooka współdzielonego = pełny vitest przed pushem, nie podzbiór |
| SKIP-INWENTARZ | 6 skipów pozostałych w pełnej regresji backendu po PANDAPOWER-DEP (pomiar `-rs` na `cabe9406`: 11000 passed / 6 skipped / 10 deselected) — inwentarz po nazwie, klasyfikacja, naprawa u źródła, zero skipów warunkowych bez uzasadnienia | agent (worktree na `cabe9406`, `karta_skip_inwentarz.md`) | **odebrana** (`f016e4cb` = cherry-pick `61ed1fa0` z `fable-cv3`, bez konfliktów): dwie KLASY, nie sześć przypadków — (A) 1 test bramkowany brakiem pakietu spoza `pyproject` (`test_export_reports.py::test_white_box_contains_latex_trace`: PyMuPDF jako jedyny sposób odczytu tekstu z wygenerowanego PDF — dowód WHITE BOX, że ślad LaTeX jest w dokumencie, nie tylko w kodzie) → `pymupdf ^1.28` jako zależność dev (lock: jeden nowy pakiet, zero zależności przechodnich, piny numpy/scipy/pandas/networkx nietknięte); (B) 5 testów „biblioteka niedostępna" bramkowanych REALNĄ dostępnością (`skipif(_PDF_AVAILABLE|_DOCX_AVAILABLE, …)` w `test_short_circuit_report_pdf.py`, `test_power_flow_reports.py` ×2, `tests/e2e/test_reference_pattern_reports.py` ×2) — reportlab/python-docx są zawsze obecne, więc gałąź `raise ImportError` NIGDY nie biegła w żadnym CI → symulacja braku przez `monkeypatch` flagi modułu raportującego (`_PDF_AVAILABLE`/`_DOCX_AVAILABLE`), test biegnie zawsze; inwentarz klasy grepem `skipif\(_[A-Za-z_]+_AVAILABLE,`: 0 pozostałych. Agent: pełna regresja po naprawie 11006 passed / 0 skipped / 10 deselected (722 s). Weryfikacja Fable na `fable-cv3` (cherry-pick na `7732c8be`): pełna regresja `-rs -m "not pandapower"` **10987 passed / 0 skipped / 10 deselected** (702 s; `SKIPPED` w wyjściu: 0); guardy `repo_hygiene`/`vulture`/`arch`/`grep_zero`/`verification_phantom_paths`/`docs` rc=0, ruff/black (tests) 0 |
| E2E-FULL-FIX | Frontend E2E full czerwony od pushu FAB-J (`7732c8be`, runy 325–330; ostatni zielony 324 na `9b78d322`): 5 testów `creator-screenshot` (oze light/dark, wiazania ×3) — korzeń harnessu `creator-harness-root` nigdy nie powstał | Fable | **naprawione** (`51cdb9df`): TRZY klasy, nie pięć testów. (1) Siedem punktów wejścia `*-harness-main.tsx` (creator, gallery, lv-domain, oze-source, screenshot, sld-symbol-pack, station-rozdzielnia) montowało drzewo bez `QueryClientProvider`, a od FAB-J komponenty kreatora DER/OZE czytają katalogi przez `useQuery` — każde wejście dostaje TEN SAM klient co `main.tsx` (`./query-client`); klasa zmierzona grepem po `createRoot(` w `src/*.tsx`. (2) 29 speców zrzutowych miało zaszyty adres `http://127.0.0.1:5173/...` — bieg na własnych portach (FE-HIGIENA) trafiał w cudzy/nieistniejący serwer; adres harnessu z `PLAYWRIGHT_FRONTEND_URL` przez `e2e/adresHarnessu.ts` (jedno miejsce). (3) Harness kreatora mockował `GET /api/ncrfg-tests/catalog` ręczną kopią payloadu, która zdryfowała dwa razy (progi klas sprzed OD-5: 200 kW/10 MW zamiast 1 MW/50 MW; brak `reactive_power`/`ride_through`/`frequency_response`/`p_recovery_after_fault` z FAB-J) — `DerSurfaceShell` (scena „wiazania") i krok zgodności kreatora OZE (`profil.ride_through.lvrt`) wywracały się na kontrakcie, którego backend nigdy nie złamał; mock USUNIĘTY, katalog bezstanowy idzie do realnego backendu przez proxy Vite (jak snapshot audytu 2), zero produktu do „uodpornienia" (cichy fallback ukryłby złamany kontrakt — klasa A6-12). Hipoteza „proxy Vite celuje w 8000 z `.env.development`" ODRZUCONA pomiarem: `playwright-run*.mjs` ustawia `VITE_API_URL_DEV=PLAYWRIGHT_BACKEND_URL`, a `loadEnv` Vite daje pierwszeństwo `process.env` — krok 4 kreatora OZE (`pf_droop_5` ze snapshotu audytu 2) przechodzi na własnych portach bez zmiany konfiguracji. Pozostałe kopie katalogów w harnessie (20 tras `/api/catalog/*` z identyfikatorami zmyślonymi + `complete-bay-templates`) → FAB-L L6 z inwentarzem (plik jest w edycji przez FAB-K; kasacja równoległa = pewny konflikt). Weryfikacja: e2e na własnych portach (8041/5211, świeża baza) `creator-screenshot` + `dowody-oze-screenshot` + `wszystkie-sceny-screenshot` ****99 passed** (8,5 min; 19 + 44 + 36 testów, zrzuty odnowione i przywrócone `git checkout`)**; tsc 0, eslint 0 (`--max-warnings 0`), P0 55/55 + 31 guardów innych workflowów fail=0 |
| CV-4.1 | jeden assembler wejścia solvera: krok 0 — wynik kanoniczny PF/SC bez `NaN`/`inf` (§35) + 252 złote hashe PRZED wycięciem; krok 1 — `enm/assembler.py` (wycięcie 1:1, parytet bit w bit, `canonical_analysis.py` 3873 → 3419 linii); krok 2 — guard `solver_input_assembler_guard` (P0, zapadka 7 plików / 31) + pin `solver_input_substitute_guard` przeniesiony 1:1 | Fable (worktree `fable-cv4` na `fd7cc0e5`, rebase na `c953e831` i `15e8cb0d`) | **odebrana** (`4b65cb14` krok 0, `4b9311ea` krok 1, `148f8de5` krok 2) — w worktree: krok 0/1 `tests/enm` + `tests/golden/parytet_assemblera` + 14 przepiętych plików **1879 passed** (295 s), 252/252 hashy bit w bit PRZED == PO; krok 2: guard assemblera rc=0 (pomiar 7/31), 15 testów własnych, `solver_input_substitute_guard` rc=0 po przeniesieniu pinu (67 testów własnych; 3609 pól na szczycie po FAB-K = 3601 + 8 / 577 plików / zapadka 64 pliki suma 312 — bez wzrostu długu), mypy `assembler`+`canonical_analysis` 0; pełna regresja backendu, `guardy_z_ci.py`, snapshot OpenAPI i frontendowe bramki PO rebase na szczyt po FAB-K — wykonane: pełny pytest `tests/` (parytet z CI) **10934 passed** + 47 czerwonych WYŁĄCZNIE w `test_archive_diff`/`test_incremental_archive` na drzewie sprzed `15e8cb0d` (klasa CI-ARCHIWUM, naprawiona na szczycie; po rebase 119 passed razem ze snapshotem OpenAPI bez dryfu), `guardy_z_ci.py` 86/86 guardów z CI (KOMPLET ZIELONY), testy własne guardów 548 passed; szczegóły w §F |
| FAB-K | łańcuch danych DER: jedno źródło zapisu i odczytu (`materialized_params` FE+BE), punkt przyłączenia SN jako element modelu (nie enum), bramka gotowości kreatorów OZE/GPZ wzorcem `KreatorMagistralaSn` — kontynuacja wysp danych i wariantów-widm zgłoszonych wprost przez FAB-J (pozycja (a)/(b) wyżej) | agent (worktree, `karta_fabk.md`) | **odebrana**: **R1** `materialized_params`(+`.profiles`) jedyne źródło wiązań DER po obu stronach — front czytał dotąd `meta`, więc wiązania `PATCH …/bindings` znikały po odświeżeniu; `derZGeneratora` (`zModelu.ts`) przepisany na odczyt PĘTLĄ po `DER_MATERIALIZED_BINDING_KEYS`/`_PROFILE_KEYS` (były eksportowane, ale DEKORACYJNE — osobne literały obok, zgodność nazw przypadkiem, nie konstrukcją: przypadek reguły KLASA NIE INSTANCJA pkt 4, „deklaracja bez testu"); test pinu parytetu z backendowym `DER_BINDING_KEYS`/`DER_PROFILE_KEYS` (`enm/domain_operations_v2.py`) + round-trip {zapis, wyczyszczenie}×9 kluczy obu stron (38 testów backend, 20 frontend). **R2** inwentarz 9 pól spoza wiązań V12K-238 z FAB-J (a): `battery_catalog_ref` wpięty end-to-end (payload → `_materializuj_bateria_bess` → katalog `BATERIA_BESS`); `dynamic_model_ref` zwalidowany wobec realnego katalogu `network_model.catalog.der_dynamic` (wcześniej dowolny string); `cable_catalog_ref`/`controller_catalog_ref`/`regulation_profile_ref` USUNIĘTE jako phantomy (zero konsumenta / zero katalogu backendu — `regulation_profile_ref` nigdy nie miał zapisu produkcyjnego, Q(U) jest liczbą na generatorze, nie referencją); `ct_accuracy_class`/`ct_application` pochodne `ct_catalog_ref`, nigdy przechowywane osobno; `bess_operation_mode_refs` świadomie POZA KARTĄ (katalog statyczny frontu, przekazane dalej do FAB-L). **R3** naprawia (b) z FAB-J: `connection_variant` ograniczony do `nn_side`/`block_transformer` (aliasy `sn_side`/`dedicated`/4 warianty-widma SKASOWANE bez kompatybilności wstecznej), nowy wymagany `sn_connection_bus_ref` dla transformatora dedykowanego bez istniejącego `blocking_transformer_ref` (422 `generator.sn_connection_bus_unknown`/`generator.sn_bus_voltage_mismatch`); front — dokładnie 2 poziomy + osobny picker punktu SN z ISTNIEJĄCYCH elementów modelu (szyna stacji / `BranchPointSN` przez ZK SN lub słup / `Junction`); `CableJoint` skasowany jako rodzaj punktu (brak topologii w modelu). Ten sam fabrykowany wzorzec „nN→0,4 kV / inaczej→15 kV" znaleziony i naprawiony w DWÓCH niezależnych miejscach (`SldDetailDrawer.tsx::pointVoltageForVariant`, `NcRfgTestsTab.tsx::inferVoltageKv`) — realne napięcie z szyny modelu w obu. **R4** `voltage_level_ref` (phantom, backend nigdy nie przyjmował) usunięty z kontraktu/store/kreatora/walidacji; `connection_voltage_kv` jedynym źródłem napięcia. **R5** wzorzec bramkowania gotowości `KreatorMagistralaSn` (`stanGotowosci: 'ladowanie'\|'zablokowany'\|'gotowy'` → `akcjaGlowna.zablokowana` + `KreatorRama.status`) zastosowany w `KreatorZrodlaOze`/`KreatorZrodloZasilania`; testy iloczynu cech (ładowanie × pole niepoprawne) natywnym `userEvent`, stylem E2E-S95. **R6**: solvery/NC RfG YAML/wynik FROZEN/golden nietknięte poza tym, co wymusił R2. Trzy niezależne ścieżki tworzenia DER: `AddDerWizard.tsx` (pełny picker SN), `SldDetailDrawer.tsx` (zawężony do `nn_side` — nie miał NIGDY maszynerii pickera SN, dawał gwarantowany 422 dla SN/dedicated; przyłączenie SN dedykowanym transformatorem zostaje w `AddDerWizard`), `KreatorZrodlaOze.tsx` (operacja domenowa bezpośrednio, nie dotyczy jej R3). **Znalezisko przy pełnej regresji, naprawione u źródła tą samą kolejką** (poza pierwotnym inwentarzem R1-R6, Zero-Debt §1 — błąd napotkany, nie własny): (1) nowy klucz `battery_catalog_ref` (R2) nie miał wiersza w `API_CATALOG_GATE_INVENTORY`/`V2_CATALOG_GATE_INVENTORY` (`api/domain_ops_policy.py`, `enm/domain_operations_v2.py`) — skan AST obu modułów operacji domenowych (dług 1 z V12K-316, karta U1) łapał to jako referencję katalogową spoza inwentarza; naprawione wierszem w OBU inwentarzach z `bramkowana=False` + uzasadnieniem (pozycja bramkowana w WARSTWIE DOMENOWEJ, `converter.battery_catalog_ref_unknown`/`_not_applicable`, kod bogatszy od generycznego `catalog.item_not_found` — bramkowanie na poziomie API zastąpiłoby ten kod i zepsuło już istniejący `TestBateriaBess::test_battery_catalog_ref_nieznany_jest_422`, sprawdzone empirycznie: próba ekstrakcji w `_referencje_dodatkowe` FAKTYCZNIE zmieniała kod, cofnięta) + wpis `POKRYCIE_POZA_INIEKCJAMI` (mechanizm inwentarza V2 wymaga DOWODU dla każdej pozycji — iniekcji albo jawnego uzasadnienia). (2) regresja SPRZED tej karty (FAB-J `0346a527`, potwierdzone `git merge-base --is-ancestor` — poza inwentarzem FAB-K, ale napotkana w tej kolejce): `useSynchronizacjaDerZModelu` (zamontowany raz w `AppRoot`) woła `useAudit2CatalogSnapshot` (`useQuery`, dług `blockTransformers` z FAB-J) BEZWARUNKOWO — 4 pliki testowe spoza obu wątków FAB-J/FAB-K (`src/__tests__/App.test.tsx`, `src/ui2/__tests__/integracja.test.tsx`, `src/ui2/shell/__tests__/shellA11y.test.tsx`, `src/ui/__tests__/etap10-acceptance.test.tsx`), renderujące `AppRoot`/`App` bez `QueryClientProvider`, padały „No QueryClient set" (19 testów) — niewidoczne dotąd, bo KAŻDA karta od FAB-J (włącznie z jej własną weryfikacją) skopowała vitest do tych samych 6 katalogów bramki, które nie obejmują żadnego z tych 4 plików. Dwie odrębne przyczyny tego samego objawu: (a) 3 pliki nigdy nie przyjęły współdzielonego `renderWithQueryClient` (`src/test/queryClientTestUtils.tsx`) — dopisany import w 3 miejscach; (b) `etap10-acceptance.test.tsx` PRZYJĄŁ helper, ale wzorzec `const { rerender } = render(...)` psuł go: RTL nie owija ponownie `ui` przekazanego do zwróconego `rerender(...)`, gdy `wrapper` jest ręcznym JSX (nie oficjalną opcją `render(ui, { wrapper })`) — naprawa KLASY w samym helperze (`wrapper` jako opcja RTL, jeden `QueryClient` przez całą sekwencję rerender), nie łatka jednego wywołania. Weryfikacja fixu QueryClient: 4 pliki 35/35 zielone. **Korekta wpisu wcześniejszego w TEJ SAMEJ karcie** (uczciwość w obie strony — poprzednia wersja tego wiersza meldowała `tsconfig_gate_guard` FAIL z „18 błędów w 9 podsystemach niezwiązanych, POZA ZAKRESEM": to była ocena z SAMEGO PODGLĄDU guarda (top 15 z 155 linii), nie z pełnego wyjścia — po doczytaniu pełnej listy 14 z tych plików (`station-der/__tests__/*`, `workspace/surfaces/__tests__/*`, `station-configurator/cards/__tests__/*`, `der-configurator/__tests__/*`, `ui2/oze/macierz/__tests__/fixtures.ts`, `ui2/spaces/wyniki/__tests__/*`) niosło DOKŁADNIE tę samą klasę pól/wartości sprzed R3/R4 (`voltage_level_ref`, `connection_node_ref`, `internal_cable_ref`, `regulation_profile_ref`, `connection_side: 'SN'`/nieistniejące `'mv_bay'`), zmierzone `git blame` jako REGRESJA TEJ KARTY (potwierdzone: przy bazowym `7732c8be` te same pliki kompilowały się czysto, bo `ConnectionSide`/pola miały jeszcze starą, szerszą postać) — naprawione U ŹRÓDŁA (nazwa/wartość pola per test, z zachowaniem intencji: gdzie topologia SN nie miała znaczenia dla asercji → `nN` jako domyślna neutralna wartość wzorem `audit-round2-fixes.test.ts`; gdzie miała → `dedicated_transformer`+`sn_connection_point_kind` dobrany tak, by NIE zmienić zbadanego zachowania — np. `tozsamoscWytworcy.test.ts`'a test „SN nie dostaje ogniw nN" testował stan FIZYCZNIE NIEMOŻLIWY po R3 (`torMocy`: `dedykowany` ZAWSZE niesie Szynę nN) i został przepisany na iloczyn cech dotąd nieprzetestowany w pliku — rodzaj ogniwa transformatora dla `nN`). **Znalezisko przy tej naprawie (KLASA NIE INSTANCJA, nie instancja z audytu)**: `DerConfigurator.tsx` (nagłówek karty DER, breadcrumb) niósł WŁASNĄ, DRUGĄ kopię etykiet `connectionSide` (`CONNECTION_SIDE_LABEL_PL`, typ lokalny `DerStationContext['connectionSide']` — NIGDY nie importował `ConnectionSide` z `station-der/types.ts`) z czterema martwymi kluczami sprzed R3 (`SN`/`at_zksn`/`at_branch_pole`/`at_cable_joint`) nieosiągalnymi przez żadną realną wartość od R3 — ekran NIGDY nie pokazywał rodzaju punktu SN (tylko zawsze ogólnik „transformator dedykowany"), mimo że `sn_connection_point_kind` istnieje na modelu od tej samej karty. Naprawione reużyciem (nie drugą kopią): `punktPrzylaczeniaOpisPl` (`station-der/readiness.ts`, już użyta w komunikatach blokerów anti-islanding) wyeksportowana z zawężonym parametrem (`Pick<StationDerConnection, 'connection_side'\|'sn_connection_point_kind'>`), `DerStationContext` zawężony do realnego `ConnectionSide` + nowe pole `snConnectionPointKind`, `DerSurfaces.tsx` dopięty do przekazywania go dalej — bez tego dopięcia naprawa etykiety zostałaby martwa (pole istniałoby w typie, nikt by go nie wypełniał). Nowy test klasy w `DerConfigurator.test.tsx` (iloczyn 4 rodzajów punktu + `null`/`undefined`, nie przykład z karty). `tsconfig_gate_guard` po naprawie: **129 POZA BRAMKA** (137→129, zmalał jako SKUTEK UBOCZNY tej naprawy — próg obniżony w guardzie z uzasadnieniem i klasą błędów w komentarzu, zapadka „w obie strony" domaga się utrwalenia poprawy) — **rc=0 (zielono)**, nie FAIL jak meldował poprzedni wpis. Pozostałe 129 błędów: potwierdzone `git blame` jako PRZEDsesyjne (m.in. `station-der/__tests__/store.test.ts` linia z konstrukcją typu `Parameters<...>[0] extends never` — `22bae3e2`, 2026-07-21; `workspace/surfaces/__tests__/Etap3Configurators.test.tsx` błąd `WorkspaceRouteState.route` — `0960e725`, 2026-08-06) — poza zakresem tej karty, nazwane uczciwie, nie ukryte. **Odbiór koordynatora (Fable, 2026-09-05, worktree `fable-cv3` na `fd7cc0e5`):** cherry-pick w dwóch krokach — backend `7c478519` bez konfliktów; frontend `10d9d33f` z konfliktami rozstrzygniętymi ręcznie (`queryClientTestUtils.tsx`, `shellA11y.test.tsx` — obie strony dodały alias `renderWithQueryClient`, zachowano HEAD; `tsconfig_gate_guard.py` — oba bloki komentarza; wiersze evidence — scalone). Domknięcie 1 (`957e8da6`): po scaleniu z `b40e1778` (E2E-FULL-FIX) dwa pliki (`src/__tests__/App.test.tsx`, `src/ui2/__tests__/integracja.test.tsx`) importowały alias bez użycia → zdjęte; bramka typów z pomiaru 129 → **128** (zapadka obniżona z uzasadnieniem w commicie). Domknięcie 2: test własny `solver_input_substitute_guard` czerwony na `957e8da6` — pin pól kontraktu 3599 → **3601** z pomiaru różnicy zbiorów (+`battery_catalog_ref`, +`sn_connection_bus_ref` — FAB-K R2/R3, `enm/domain_operations_v2.py`), dług/wykluczenia bez zmian. Weryfikacja niezależna na `957e8da6`: backend pytest (`-m "not pandapower"`, zakres CI) **6209 passed**; `tsc` 0; `eslint --max-warnings 0` (50 zmienionych plików) 0; vitest zakres dotknięty **273 pliki / 3196 testów**; e2e specy DoD karty na własnych portach **48 passed** (6,1 min; zrzuty odnowione i przywrócone `git checkout`); zestaw przedpushowy P0 54/54 (55. wpis listy — `solver_input_assembler_guard` z CV-4.1 — nie istnieje jeszcze w tym drzewie) + 31 guardów innych workflowów **fail=0**; testy własne guardów **533 passed** (po pinie 3601: 67/67 w teście guarda podstawień); snapshot OpenAPI bez dryfu. |
| CV-4.2 | kasacja czterech ścieżek równoległych budowy wejścia solvera (P2/S4 kreator `build_*_input`, P5 `power_flow_input_builder`, P12 `api/solver_input.run_audit2_power_flow`, P13 `domain/load_flow_input`) + przepięcie P11 (`solver_input/builder.py`, kontrakt LOCKED v1.1) na assembler zamiast składania równoległego — konstytucja C.2.3, kontynuacja CV-4.1 | agent (worktree, `karta_cv42.md`) | **wykonana** (agent — koordynator cherry-pickuje i weryfikuje niezależnie; SHA własnego commitu poza tym wpisem, patrz worktree): P2/S4 (`network_wizard/service.py` 2481→2228 linii) + P5 + P13 skasowane, 0 konsumentów produkcyjnych (pomiar grepem potwierdzony, zgodny z pierwotnym pomiarem C.2.3); P5 usunięty razem z testem parity P2↔P5 (`test_wezel_zlozony_wejscie_rozplywu.py`, 1032 linii) po dowiedzeniu pokrycia fizyki niezależnie na torze kanonicznym. P12 skasowany (`run_audit2_power_flow`, fabrykacja `pq=[]`/slack-stub); JEDYNY konsument FE (`ui/network-build/station-der`, zweryfikowany grepem literału trasy) przepięty na bieg kanoniczny (`POST .../runs` → `.../execute` → `.../results`), 12/12 `test_solver_input_audit2_integration.py`. P11 (K5): `api/solver_input.py::_graph_for_analysis` przepięty na `enm.assembler.zloz_wejscie_rozplywu`/`zloz_wejscie_zwarcia`, zero zmian `solver_input/builder.py`. **Odkrycie przy tej karcie, naprawione w tym samym mandacie (Zero-Debt, poza enumeracją K1–K7):** usunięty `_get_graph_for_case` ZAWSZE zwracał graf PUSTY (`NetworkGraph(network_model_id=case_id)`, zero elementów) niezależnie od treści przypadku — oba endpointy P11 HTTP fabrykowały wejście KLASY P12 od początku; dosłowny „parytet BEFORE/AFTER" K5 niewykonalny (BEFORE = zawsze zdegenerowany), zamiast tego dowiedzione: `build_solver_input` zero-diff + payload sieci rejestru NIE zdegenerowany + nowy golden `tests/golden/parytet_p11/` (harness reużywa `widok_parytetu`/`porownaj_wpis` z `parytet_assemblera`, 126 wpisów, 17 odmów krzyżowo zgodnych z `parytet_assemblera` dla tych samych sieci, 5/5 testów). Guard `solver_input_assembler_guard`: zapadka 7 plików/31 konstrukcji → **3 pliki/16 konstrukcji** (wyłącznie `application/reference_networks/**`, CV-4.3); `solver_input/builder.py` → ALLOWLIST (wzorzec `power_flow_gauss_seidel.py`). Pole addytywne K7 w `enm/assembler.py` (`WejscieRozplywu.audit2_applied`) — `parytet_assemblera` 5/5 po zmianie (252/252 bit w bit); mypy src 774 plików 0 błędów, black/ruff czyste (14 plików backendu), 35 guardów lokalnych PASS, frontend tsc/eslint (cały projekt) + vitest zakresu (49 plików/508 testów) czyste. **Pełna regresja `pytest tests/ -m "not pandapower"` NIE dokończona w tej sesji** — maszyna dzielona z ≥2 innymi agentami (obciążenie zmierzone: `loadavg` ~12 na 4 rdzeniach), bieg zatrzymał się na ~32% (~3480/10875) BEZ ŻADNEGO nieoczekiwanego niepowodzenia po dwóch naprawach opisanych niżej; log `tests/` w tle, ścieżka w worktree, do dokończenia przez koordynatora; szczegóły w §F |

**DRUGIE znalezisko, WIĘKSZE — bramka certyfikatu zgodności PTPiREE blokowała moduł Z REALNYM certyfikatem (odkryte przez e2e `critical-oze-evidence.spec.ts`, nie przez inspekcję kodu).** Po naprawie R1 (front czyta `ptpiree_certificate_ref` z `materialized_params`, nie z martwego `meta`) test e2e zaczął dostawać PRAWDZIWY `certificate_status: "ptpiree_verified"` dla modułu 215 kW/0,8 kV (falownik z wykazu PTPiREE) — i backend (`POST /api/oze-analysis/compliance-certificate`) zaczął odrzucać certyfikat kodem 422 z powodem „brak testów wymaganych — brak podstawy do certyfikacji" (`zbierz_braki`, `certyfikat_zgodnosci.py`). Diagnoza (backend `pytest` w pełni zielony — 5383/5383 — więc luka nie miała ŻADNEGO pokrycia testowego): moduł klasy „A" (najniższa, PPM) nie ma ANI JEDNEGO testu katalogu NC RfG w `default_for_modules` (wszystkie wpisy w `engine.py` to `["C","D"]`/`["B","C","D"]`/`[]`) — jedynym źródłem wymaganego testu dla klasy A jest T12 (`_is_required`), a T12 uruchamia się WYŁĄCZNIE, gdy certyfikatu BRAK (`certificate_status != "ptpiree_verified"`). Skutek logiczny: moduł klasy A z ISTNIEJĄCYM certyfikatem PTPiREE ma ZAWSZE `required_count == 0` — a `zbierz_braki` traktowało to jak brak dowodu, mimo że certyfikat producenta JEST dowodem, silniejszym niż bieg testów w warsztacie. Zdiagnozowane empirycznie (tymczasowy `print` w `engine.py`, usunięty przed commitem, potwierdzone `git diff` = 0 poza samą naprawą) — dwa uruchomienia solvera w locie potwierdziły `certificate_status='ptpiree_verified', module_type='A', required_count` liczone jako 0 mimo `stop_generation_enabled=True` poprawnie zadeklarowanego (deklaracja BEZ WPŁYWU na klasę A z certyfikatem — nie jej wina). Naprawa: `certificate_status` dopisany ADDYTYWNIE do `NcRfgPtpireeModuleResult` (kontrakt `…V1` bez zmiany numeru — pole domyślne `"unknown"` jak wejście, zero wpływu na istniejące porównania), `zbierz_braki` zwalnia z braku „required_count == 0" WYŁĄCZNIE gdy `certificate_status == "ptpiree_verified"` — PREDYKAT PAROWY spisany jako 3 nowe testy (`test_certyfikat_zgodnosci.py`): (1) klasa A + certyfikat → certyfikat POWSTAJE mimo zera testów, (2) TA SAMA klasa A BEZ certyfikatu → T12 staje się wymagany z INNEGO powodu (nie da się skonstruować „klasa A + zero wymaganych + bez certyfikatu" — T12 wypełnia lukę zawsze), więc dowód parowania idzie przez fakt matematyczny `required_count >= 1`, (3) moduł certyfikowany z INNYM prawdziwym brakiem (`no_data` na teście wymaganym z klasyfikacji klasy B) NADAL blokowany — naprawa nie zdejmuje bramki z certyfikowanych modułów w całości. 201/201 zielono w całym obszarze NC RfG/PTPiREE (`test_ncrfg_ptpiree_solver.py`, `compliance/test_nc_rfg_modul.py`, `test_ncrfg_ptpiree_api.py`, `test_klucz_przypadku_szablonow.py`, `test_wniosek_osd.py`, `test_dowod_certyfikatu_dokumentow.py`) + 27/27 `test_certyfikat_zgodnosci.py` (3 nowe). Snapshot OpenAPI przeliczony (317/225 bez zmian liczby ścieżek/schematów, +11 linii — jedno pole w istniejącym schemacie); wpis `docs/v12xx/MACIERZ_KOMPATYBILNOSCI_API.md` dla `POST /api/ncrfg-tests/run` + 3 warianty `compliance-certificate`. **Klasa błędu**: naprawa R1 (poprawny odczyt danych) ODSŁONIŁA pre-existing lukę logiki backendu, którą PRZEDTEM maskował INNY defekt (front zawsze wysyłał `certificate_status: "unknown"`, więc T12 ZAWSZE się uruchamiał i required_count nigdy nie był 0 dla klasy A/B) — dwa niezależne defekty w tej samej ścieżce, jeden ukrywający drugi; naprawiono OBA w tej karcie (ten sam wzór co „test maskujący defekt produktu = dwa defekty", tu: „defekt maskujący defekt").

**Koordynator w trakcie pracy**: cherry-pick `51cdb9df` (E2E-FULL-FIX — harnessy z `QueryClientProvider`, mock katalogu NC RfG usunięty z `creator-harness-main.tsx`, `e2e/adresHarnessu.ts`) scalony BEZ KONFLIKTU (hunki nierozłączne — auto-merge git, zweryfikowane `git diff` = wyłącznie moje linie derDemo/scen OZE po scaleniu). Dwie poprawki na żądanie koordynatora w tych samych liniach: `lvrt_curve_ref: 'lvrt-pse'/'lvrt-enea'` → `'pse'/'enea'` (operator_id, konwencja post-FAB-J) w scenach harnessu; audyt własnego diffu harnessu — zero nowych ręcznych kopii katalogów backendu (klasa FAB-L L6, nietknięta).

Weryfikacja końcowa: backend `pytest tests/api tests/enm tests/application tests/domain tests/ci -m "not pandapower"` **5386 passed, 10 deselected** (482 s; trzy pełne przebiegi w tej karcie — 5383 przed naprawą bramki certyfikatu, 5383 po dodaniu pola `certificate_status`, **5386 po dopisaniu 3 nowych testów** predykatu parowego, wszystkie zielone; pandapower — środowisko izolowane scipy<1.17, `PANDAPOWER-DEP`, brak w tym venv), mypy/ruff/black 0 dla wszystkich zmienionych plików, 9+2 wymaganych guardów (api_lifecycle/route_prefix/catalog_binding/catalog_gate/catalog_enforcement/readiness_codes/solver_input_substitute/grep_zero/docs_guard + resultset_v1_schema/severity_contract dla zmiany kontraktu solvera) rc=0, snapshot OpenAPI przeliczony 2×; frontend tsc 0, eslint 0 (niezmienione od poprzedniego biegu tej karty), gate-scope vitest (`network-build workspace sld/v2/canvas ui2/kreatory/zrodlo ui2/kreatory/zrodlo-oze ui2/oze ui2/spaces/wyniki` — rozszerzone o ostatni katalog po naprawie `wynikiWarsztat.test.tsx`) **204 pliki / 2347 passed**, `tsconfig_gate_guard` **rc=0 (129/129)**, 7/7 wymaganych guardów UI rc=0 (dead_click/dialog_completeness/ui_no_physics/forbidden_ui_terms/no_codenames/ui_terminology + `tsconfig_gate_guard`). e2e (realny backend, własne porty 187xx/157xx, `PLAYWRIGHT_BACKEND_PYTHON` na venv `8gO73INY`): `critical-oze-evidence` osobno PRZED naprawą bramki certyfikatu = **1 failed** (deterministyczne, 2/2 powtórzenia — dowód regresji, nie migotanie), PO naprawie = **1 passed**; pełny wymagany zestaw jednym biegiem (`creator-screenshot`, `critical-der-config`, `critical-oze-evidence`, `kreator-oze-max`, `s95-budowa-z-kanwy`, `ux-feedback-loop` — dwie ostatnie jako spec GPZ, testid `mvd-kreator-zrodlo-zapisz`) = **31 passed (3,7 min), 0 failed**. `docs/audit/visual/kreatory/*.png` (4 pliki — jedyne śledzone artefakty zmienione przez biegi e2e tej karty) przywrócone `git checkout --` po biegu, nieskomitowane; `test-results/` poza śledzeniem (gitignore), bez działania. |
| CV-4.1b (A3-04) | domknięcie A3-04 z CV-4.1: generator z regulacją napięcia liczony jak węzeł PQ (`pv_bus_ids=[]` zawsze) — łańcuch ENM meta → walidator → IR (mapping) → assembler → kreator OZE + karta techniczna DER + drawer SLD | agent (worktree `agent-a8921fe6f13ce9d8a`, `karta_cv41b_a304.md`) | **wykonana** (jeden commit HEAD worktree, bez pusha — SHA nieprzytoczone celowo, jak CV-3.3-A powyżej; brak przeglądu niezależnego w tej sesji — oczekuje weryfikacji). Inwentarz klasy pełny w `CONVERGENCE_ROADMAP.md` (wiersz CV-4.1b, 13 miejsc czytających/piszących/walidujących/wyświetlających nastawę). PRZY OKAZJI (KLASA NIE INSTANCJA, przegląd całego mechanizmu `_resolve_converter_defaults`) naprawione DWA pre-existing bugi gałęzi BESS (brak `control_mode` w ogóle; brak `q_min_mvar`/`q_max_mvar` w `meta`) — oba PV/wiatr już niosły, tylko BESS nie. Pomiar rejestru referencyjnego (`tests/golden/registry.py`, 17 wpisów): PRZED 0/17 miało zmaterializowaną sieć z generatorem w regulacji napięcia (G06 istniał jako wpis szkieletowy); PO 1/17 (G06, 2 warianty — nastawa osiągalna / nasycenie Q, oba na topologii `build_gn04_sn_nn_oze`). Złote hashe parytetu assemblera (baza `40e49c22` po dwóch korektach koordynatora w trakcie karty, `5793a3e0`→`158d9831`→`40e49c22`; widok tolerancyjny `porownaj_wpis`): 252→264 wpisów, +12 WYŁĄCZNIE G06, **0 rozbieżności ponad tolerancję w 252 wspólnych wpisach** (rachunek obejmuje sieci z generatorem BESS materializowanym przez ten sam mechanizm — G00/G04/G05/G08 — bez rozbieżności: `meta` z polem `None` nieodróżnialne od pola nieobecnego dla każdego konsumenta przez `.get()`), 0 usuniętych. ZNALEZISKO TRZECIE (pełna regresja backendu, nie audyt statyczny): `tests/application/test_companions_generated.py::test_fixtura_sld_network_53_jest_bajtowo_rowna_generatorowi` czerwony — jedyny GENEROWANY artefakt wrażliwy na nowe pola `meta` (`sldNetwork53.ts`, hash migawki wprost, nie wynik solvera); naprawiony regeneracją sankcjonowaną samym testem (`scripts/emit_sld_network_fixture.py`), diff dokładnie 1 linia (`source_hash`). Dowód niezależną wyrocznią: `tests/application/reference_networks/test_pandapower_cross_validation.py::TestPvNodeCrossValidatePandapower` (pandapower `create_gen(vm_pu=...)`; marker `pandapower` nieuruchamialny w tym venv z przyczyn środowiskowych opisanych w module-docstringu pliku — zweryfikowany niezależnie stroną kanoniczną: moduł U węzła PV = 1,02 pu dokładnie, `pv_to_pq_switches=[]`, Q≈5,72 Mvar w granicach ±10 Mvar zadeklarowanych kartą). Solver FROZEN (B-01) nietknięty. Weryfikacja końcowa: backend `pytest -p no:cacheprovider -m "not pandapower" tests/` **11009 passed, 11 deselected, 0 failed (931,7 s)**; `pytest tests/golden/parytet_assemblera` 5 passed (274,4 s); frontend `npx tsc --noEmit` 0 błędów; `npx eslint` (pliki zmienione) 0 ostrzeżeń; targetowany `vitest` (`ui2/kreatory/zrodlo-oze`, `ui/sld/v2/canvas`, `ui/network-build`, `ui/sld/shared`, `ui/tech-card`) **134 plików / 1599 testów passed**; guardy: `solver_input_assembler_guard`, `solver_input_substitute_guard`+self-test, `readiness_codes_guard`, `readiness_consumption_guard`, `resultset_v1_schema_guard`, `mypy_ratchet_guard` (Success, próg 0), `tsconfig_gate_guard`, `dead_click_guard`, `ui_no_physics_guard`, `physics_label_guard`, `docs_guard` oraz 20 dalszych guardów architektury/katalogu/fizyki/determinizmu z listy karty — wszystkie zielone; snapshot OpenAPI bez zmian (317 ścieżek/228 schematów) — kontrakt HTTP nietknięty, `MACIERZ_KOMPATYBILNOSCI_API.md` bez wpisu (zasadnie). |

## F. Dowody inżynierskie (testy, wyrocznie, przebiegi) — uzupełniane po każdym wycinku
- 2026-09-04: `pytest tests/application/analyses/lv_domain/` — komplet zielony po wdrożeniu trybu ścisłego (liczby w commicie).
- 2026-09-04: `tests/ci/test_guardy_z_ci.py` 4 passed; `snapshotStore.successToast.test.ts` 8 passed; guardy: `success_toast`, `claude_md_struktura`, `docs_guard`, `docs_archive_guard` zielone.
- 2026-09-04: CV-1 rdzeń (`9667235a`): `tests/invariants` + `tests/enm` + magazyn 1682 passed; `tests/api` + `station_templates` + `project_archive` 1086 passed po zmianie klucza magazynu (semantyka klucza surowego w testach niezmieniona).
- 2026-09-04: rejestr sieci wzorcowych w kodzie (`0c506744`): `tests/golden/test_registry.py` 14 passed (budowniczowie G02/G03/G04/G05/G07/G08/G13/G00/B-BENCH wykonalni; tabela generowana aktualna; zapadka pokrycia rodzin).
- 2026-09-04: SLD acceptance `npm run accept:sld-v3` RC=0 lokalnie (410 PASS) i w CI @ ee0ec472.
- 2026-09-04: snapshot OpenAPI (`0cb1c8d0`): 319 ścieżek, 227 schematów; test aktualności i determinizmu 2 passed — kontrakt HTTP jest odtąd diffowalny (M0-6).
- 2026-09-04: CI-B (`9b96ebcc`): `tsconfig_gate_guard` RC=0 na gałęzi (137 poza bramką / 1 wyciszenie / 0 w bramce); vitest na 48 zmienionych plikach testów: 1039 passed (odbiór niezależny Fable); w worktree agenta 70 plików / 1385 passed, type-check i lint RC=0.
- 2026-09-04: PERF-0 (`47d809a9`): pełny bieg `benchmark_baseline.py --powtorzenia 5 --sieci S,M` 435,6 s, RC=0; `tests/perf` 7 passed (bieg dymny na sieciach S, 1 powtórzenie); mtime bazy `mv_design_pro.db` niezmieniony (izolacja środowiska pomiaru).
- 2026-09-05: klasa `KLUCZE_ROZPLYWU` (wkłady + ślad podziału prądu): `tests/infrastructure/persistence/test_canonical_run_rozplyw.py` 15 passed (8 nowych), `test_enm_api` świeży bieg + końcówka rozpływu, `test_canonical_analysis_api` — 17 passed celowanych; guardy trace_ui_leak/resultset_v1/audit_contract/api_lifecycle/arch/vulture/solver_boundary/severity_contract RC=0; mypy_ratchet 0 błędów; regresja backendu (infrastructure, enm, api, e2e, analyses, proof_engine): **4206 passed, 2 skipped** (239 s) na `1e9f21c5`.
- 2026-09-05: lokalny komplet 82/82 guardów wołanych przez CI zielony na `f3b634d6` (`guardy_z_ci.py` interpreterem poetry) + 396 testów własnych guardów.
- 2026-09-05: FAB-D1 na gałęzi (`ab0c5e93`): pytest enm/api/application/invariants/golden **4804 passed, 5 skipped** (328 s); ruff/black (src, tests, scripts) RC=0; 11 guardów RC=0 (catalog_metadata/enforcement pod interpreterem poetry — pod systemowym python3 czerwone wyłącznie z braku `networkx`, artefakt środowiska); testy własne guarda podstawień 44 passed; vitest konsumentów `sldNetwork53.ts` 11 passed.
- 2026-09-05: CI `Python tests` na `f4e392f9` czerwony w kroku `readiness_consumption_guard` (F-K6): FAB-D1 zarejestrowała 3 kody gotowości bez emitera (`inverter.k_sc_missing`, `load.p_missing`, `catalog.ports_missing`) — klasa „deklaracja bez dostawcy”; naprawa u źródła `38cee389`: kody usunięte (punkt rozgałęźny bez portów zgłasza istniejąca brama `catalog.ref_required`; odbiór legacy bez mocy nie powstaje; `k_sc` wraca w FAB-H razem z emiterem); guardy readiness_codes/consumption/fix_action/domain_no_guessing/canonical_ops RC=0, 532 + 3 testy zielone. Wniosek metodyczny: łańcuch odbioru kart uruchamia odtąd KOMPLET guardów CI (`guardy_z_ci.py` interpreterem poetry), nie wybór.
- 2026-09-05: WB-ROZPLYW na gałęzi (`acd2862c`): vitest `ui2/wyniki/zwarcia`+`dowod` 14 plików / 172 passed, eslint zmienionych, `npm run type-check`, guardy ui_no_physics/trace_ui_leak/forbidden_ui_terms/no_codenames/dead_click/tsconfig_gate RC=0.
- 2026-09-05: FAB-D2 na gałęzi (`eefab9a0` + `d1229902`/`c71282e2`): mypy src 0 błędów na czystym cache (799 plików; 4 błędy zawężenia Optional w tabliczce agregatu z FAB-D1 naprawione, `has-type` w cyklu importów `domain_operations` ↔ `v2` domknięty jawną adnotacją), `guardy_z_ci.py` 82/82 (mypy_ratchet po naprawie RC=0), vitest `akademickie` 108 passed, type-check RC=0; **pełny pytest backendu 10742 passed, 11 skipped** (644 s) na `92310661`.
- 2026-09-05: CV-1-W na gałęzi (`a71bd91c`+`4ab4baec`): pełny pytest **10759 passed + 5 naprawionych w `4ab4baec`, 11 skipped** (779 s), mypy 0, black/ruff (src, tests, scripts) RC=0, `guardy_z_ci.py` komplet po obniżeniu zapadki `enm_store_key` (414 testów własnych guardów); WB-2 (`08ba0f4e`): vitest 35 plików / 457 testów, type-check, lint RC=0; E2E `critical-engineer-flow.spec.ts` lokalnie 8/8 z realnym backendem. CI na `4ab4baec`: Python tests zielone (run 33941020675); Frontend checks / E2E full w toku w chwili wpisu.
- 2026-09-05: FAB-H na gałęzi (`4ee77f56`): black/ruff/mypy RC=0; pytest zakres 10 katalogów **6895 passed, 5 failed** — wszystkie 5 w `test_v126_generator_q_missing_api.py` (seeding surowym kluczem sprzed CV-1-W) → naprawione w `54cb5356`; `guardy_z_ci.py` czerwony wyłącznie na `solver_input_substitute_guard` (3 nowe podstawienia FAB-H + 2 wpisy do obniżenia) → naprawione w `d58b949e`. Po domknięciu (`d58b949e`+`54cb5356`+port testu „strukturalnego zera”): `solver_input_substitute_guard` RC=0 (zapadka w obie strony, test własny zielony), `readiness_consumption_guard` RC=0, black/ruff/mypy RC=0, pytest `tests/enm tests/solver_input tests/api/test_v126_* tests/application/calculation_readiness tests/test_canonical_analysis_api.py tests/golden` **2010 passed** (1 test FAB-H przepisany do domkniętego kanonu: Q nieznane w powiązaniu falownika = `None`, nie 0,0).
- 2026-09-05 (~05:35 UTC, push `98ad6b6a`, 12 commitów): SUB-52s + SLD-LOC + fixtury (`536b0e11`/`caa92911`/`2024fb4b`), ADV-CV1 (`26542b66`), FAB-G korekta, FAB-E (`27e8a44b`/`0d549d4a`/`98ad6b6a`) — backend `enm`+`invariants`+`api`+`application`+`golden`+`reference_networks`+`infrastructure` **5003 passed / 5 skipped** (445 s); FAB-E w worktree 11 katalogów **7319 passed** + na gałęzi 998; frontend SLD **245 plików / 4746**, `accept:sld-v3` ALL PASS, tsc 0, eslint 0, `sld_determinism_guards` 0; `guardy_z_ci.py` 83 guardy: 81 zielone + 2 czerwone naprawione u źródła w `98ad6b6a` (`solver_input_substitute_guard`, `ui_production_codes_guard`), 460 testów własnych; 12 guardów generycznych RC=0 przed pushem; mypy 0 (807); black/ruff 0.
- 2026-09-05: GUARD-SUB (`0f4c3a59`) i E2E-DER (`8bb6589a`) na gałęzi: `solver_input_substitute_guard` PASS (597 plików, zapadka 89/592, wykluczenia 24/64), 54 testy własne; pytest `test_generators_api`+`test_openapi_snapshot`+`test_wspolbieznosc_koncowek_modelu` 26 passed; vitest `sld/v3/canvas`+`sld/v2/canvas`+`network-build` **167 plików / 2196 testów**, `SldDetailDrawer` 77; `tsc --noEmit`, eslint (`--max-warnings 0`) RC=0; e2e `critical-der-config` z realnym backendem 1 passed (44 s). CV-2 (rdzeń) na gałęzi (`9f5fe90e`): `guardy_z_ci.py` 83/83 + 445 testów własnych, mypy 0 (804 pliki), pytest `enm`+`api`+`application`+`invariants`+`e2e`+`infrastructure`+`cgmes`+`solver_input` **5044 passed, 7 skipped** (418 s). CI na `814443c5`: Python tests, Physics Label, Docs, Arch, P0 Extended, SLD Determinism, E2E smoke zielone; E2E full czerwony (DER — naprawa `8bb6589a`), Frontend checks w toku.
- 2026-09-05: FAB-F na gałęzi (`2239ce9c`): `npm run type-check`, `npm run lint`, `guard:codenames` RC=0; vitest zakres dotknięty (sld/v2/canvas, reports, ui2/kreatory/przekaznik, network-build/cards, sld/v3/export, station-der) **60 plików / 918 testów**; guardy no_codenames, export_codenames, tsconfig_gate (137/1), dead_click, forbidden_ui_terms, ui_terminology, vulture, enm_contract_parity, catalog_metadata RC=0; pytest `tests/ci` + `test_wizard_step_controller` + `test_station_field_validation` + `test_generator_validation` + `test_e2e_determinism` **309 passed**; testy własne guardów kryptonimów (`scripts/test_no_codenames_guard.py`, `scripts/test_export_codenames_guard.py`) **60 passed**; black/ruff (src, tests, scripts) RC=0.
- 2026-09-05: FAB-I na gałęzi (`a16b8e26`/`24d2257b`): vitest `network-build`+`workspace` 124 pliki / 1232 testy, tsc 0, eslint 0, guardy `dead_click`, `dialog_completeness`, `forbidden_ui_terms`, `no_codenames`, `ui_no_physics`, `ui_terminology` RC=0; `tsconfig_gate_guard` czerwony 138/137 (jawna adnotacja `ReturnType<typeof vi.fn>` w teście kreatora → `Mock<any[], unknown>`) → naprawa u źródła w `24d2257b` (typ wnioskowany; 137/137, 1/1, vitest 23/23); CI na `24d2257b`: 8/9 (E2E full czerwony od FAB-G — §A).
- 2026-09-05: CV-3.1 rdzeń (`e1bc25f1`, worktree `fable-cv2` na `24d2257b`): pytest `tests/enm tests/invariants tests/application tests/api tests/e2e tests/golden` + scenariusze zwarciowe + seria **5226 passed, 7 skipped** (457 s); nowe: `tests/enm/test_scenariusze.py` 38, `tests/invariants/test_scenariusz_i_bieg.py` 14, `scripts/test_scenario_copy_guard.py` 18; mypy src 0 (808 plików); black/ruff (src, tests, scripts) 0; guardy `docs`, `arch`, `import_graph`, `enm_store_key`, `provenance_constant`, `solver_input_substitute`, `no_direct_fault_params`, `vulture`, `api_lifecycle`, `readiness_codes`, `false_zero`, `scenario_copy` RC=0. Na gałęzi po cherry-picku (`3c4b3c13`): `tests/golden/parytet_scenariuszy` 30/30 (5 s), testy własne guardów 85 (po przeliczeniu pinu 3637 → 3653 pól: 16 pól kontraktu scenariusza), `solver_input_substitute_guard` PASS (601 plików, 66/346, 18/50), `scenario_copy_guard` OK (19/19), ruff/black 0.
- 2026-09-05: CV-3-W na gałęzi (`dc5fedd0` D4–D6, `9faa78c7` D1–D3, `498f4260` domknięcie z grafem w dyspozytorze): `tests/golden/parytet_scenariuszy` 30/30 (28 złotych hashy sprzed migracji bez zmian), rodziny D1–D6 + dyspozytor + scenariusze + inwarianty + `tests/api/test_oze_analysis_runs_api.py` **217 passed**; wcześniej po B: 175 passed, po A+B: 1167 passed (`tests/application/analyses` w całości + nastawy + pakiet + inwarianty); `scenario_copy_guard` **0 trafień / zapadka 0** (18 testów własnych), `no_direct_fault_params_guard`, `solver_input_substitute_guard`, `vulture_guard` RC 0; mypy src 0 (809 plików); black/ruff 0. Rdzeń CV-3.1 zmieniony addytywnie (`graf=` w dyspozytorze) z pinami w `tests/enm/test_wykonaj_bieg_w_pamieci.py` i `test_kontyngencje_n1_service.py::test_jedna_budowa_grafu_na_kontyngencje`.
- 2026-09-05: CV-3.3-A w worktree (jeden commit HEAD, `karta_cv33a.md` na `a5a68bf3`): mypy src 0 (795 plików, spadek z 809 — pliki E3 skasowane bez zamiennika w `src/`); black/ruff (src, tests) RC=0; guardy `api_lifecycle_guard`/`route_prefix_guard`/`router_mount_guard`/`legacy_public_path_guard`/`vulture_guard`/`import_graph_guard`/`no_direct_fault_params_guard` RC=0 (98 testów własnych guardów, w tym 2 nowe w `test_legacy_public_path_guard.py`); snapshot OpenAPI przeliczony 318→315 ścieżek (`test_openapi_snapshot.py` 2 passed). Pełny pytest `tests/api tests/application tests/analysis tests/enm tests/invariants tests/network_model tests/e2e` + `tests/test_pr19_fault_scenario.py tests/test_fault_scenarios_run_integration.py tests/test_batch_execution.py` **6836 passed, 7 skipped w 649,04 s (0:10:49)**, RC=0, zero failed/error. Szczegóły klasy/inwentarza → §E CV-3.3-A.
- 2026-09-05: CV-3.3-A2 w tym samym worktree (drugi commit na kartę macierzystą, polecenie koordynatora): mypy src 0 (792 pliki, spadek z 795 — `AnalysisKind`, `load_flow_to_resultset_v1.py`, `protection_to_overlay_v1.py` skasowane); black/ruff (src, tests) RC=0; guardy `legacy_public_path_guard`/`protection_no_heuristics_guard`/`resultset_v1_schema_guard`/`trace_ui_leak_guard`/`grep_zero_guard`/`api_lifecycle_guard`/`route_prefix_guard`/`router_mount_guard`/`vulture_guard`/`import_graph_guard`/`no_direct_fault_params_guard`/`solver_input_substitute_guard` RC=0; snapshot OpenAPI regenerowany, bajt w bajt bez zmian (315/225). Pełny pytest ten sam zbiór + `tests/domain tests/ci` **7403 passed, 7 skipped w 585,89 s (0:09:45)**, RC=0, zero failed/error. Szczegóły per-moduł (w tym decyzje STOP z cytatem plik:linia) → §E CV-3.3-A2.
- 2026-09-05: CV-3.3-B w worktree (`karta_cv33b.md`, kontynuacja po kompakcji — środowisko odtworzone od zera, `poetry install --with dev` + `npm ci`): backend pytest `tests/api tests/application tests/analysis tests/enm tests/invariants tests/infrastructure tests/e2e tests/domain tests/ci` z `-m "not pandapower"` (parytet z `python-tests.yml`, job `pandapower-cross-validation` osobny/izolowany) **5889 passed, 0 failed, 10 deselected w 503,25 s (0:08:23)**, RC=0; mypy src 0 (776 plików); black (src, tests) 0 do przeformatowania (1472 pliki bez zmian); ruff (src, tests) 0. `scripts/` pytest (komplet testów własnych guardów) **533 passed w 165,66 s**. Guardy wywołane pojedynczo, wszystkie RC=0: `api_lifecycle`/`route_prefix`/`router_mount`/`legacy_public_path`(+21 testów własnych)/`enm_store_key`/`result_status_writer`/`solver_input_substitute`(+67 testów własnych)/`grep_zero`/`vulture`/`import_graph`/`docs`/`pcc_zero`/`domain_no_guessing`/`arch`/`solver_boundary`/`canonical_ops`/`catalog_binding`/`catalog_enforcement`/`catalog_gate`/`catalog_metadata`/`overlay_no_physics`/`load_flow_no_heuristics`/`protection_no_heuristics`/`trace_ui_leak`/`sld_determinism`/`trace_determinism`/`fault_scenarios_determinism`/`resultset_v1_schema`/`readiness_codes`/`audit_contract`/`severity_contract`/`reference_networks`/`docs_archive`/`repo_hygiene`/`local_truth`/`no_codenames`/`forbidden_ui_terms`/`ui_terminology`/`dialog_completeness`/`dead_click`/`physics_label`/`ui_no_physics`/`interaction_matrix`/`guard_ux_flow_v1`/`fix_action_completeness`/`nn_source_menu`/`no_direct_fault_params`/`transformer_catalog_voltage`/`v12xx_canon`/`utf8_mojibake`/`active_public_layer`/`solver_diff`/`scenario_copy`(+18 testów własnych, +1 wyjątek udokumentowany R2 — deserializacja archiwum, INNA KLASA niż dług scenariuszy)/`tsconfig_gate`(budżet 137→136 U ŹRÓDŁA: 2 fikstury bez `provenance_a/b` + 1 martwy import pre-existing zdjęty przy okazji). Snapshot OpenAPI przeliczony: 317 ścieżek (bez zmian — nowy endpoint dokłada METODĘ do istniejącej ścieżki `/projects/{project_id}/protection-runs`), 228 schematów (+2: `ProtectionRunListItemResponse`/`ProtectionRunListResponse`). Frontend (`npm ci` od zera): `tsc --noEmit` 0 błędów (cały projekt); eslint `ui/power-flow-comparison ui/protection-comparison ui2/wyniki/porownanie` `--max-warnings 0` 0; vitest `--no-file-parallelism` tych samych trzech obszarów **153 passed (8 plików)**, w tym 9 nowych testów (`naLinieProweniencji` × 4, `etykietaPrzebiegu` obrona w głąb × 3, panel proweniencji ekranu × 1 + rozszerzony istniejący). E2E realny backend: `flow-ekspert-screenshot.spec.ts` **16/16 passed** (wszystkie 8 scen × 2 motywy, w tym scena „porownanie" — czerwona przed naprawą mocka harnessu, zielona po); `wszystkie-sceny-screenshot.spec.ts` scena „porownanie" **2/2 passed**. Zrzuty zaktualizowane: `docs/audit/visual/flow-ekspert/{flow_porownanie,porownanie-q}_{light,dark}.png` (panel proweniencji widoczny w obu motywach, tryb ekspercki). `porownanie-zwarc-delty.spec.ts`/`s913-porownanie-z-kanwy.spec.ts` (podsystem `/api/short-circuit-comparisons`, osobny od R1 PF/protection) i `wszystkie-sceny-screenshot.spec.ts` w pełnym zakresie (50+ scen niedotkniętych kartą) — NIE URUCHOMIONE, uzasadnienie: zerowe pokrycie zmienionego kodu, potwierdzone czytaniem źródła przed pominięciem. Cztery defekty środowiskowo-ujawnione i naprawione (poza trzema z pierwszej sesji karty, opisanymi w §E) → szczegóły §E CV-3.3-B.
- 2026-09-05: FAB-K odbiór koordynatora (`957e8da6` + domknięcie 2): backend pytest zakres CI **6209 passed**; tsc 0; eslint 0 (50 plików); vitest **273 plików / 3196 testów**; e2e własne porty **48 passed** (6,1 min); P0 54/54 + 31 guardów **fail=0**; testy własne guardów **533 passed** po pinie 3601 (`test_solver_input_substitute_guard` 67/67); snapshot OpenAPI bez dryfu; `tsconfig_gate_guard` 128/128 rc=0.
- 2026-09-05: CI-ARCHIWUM (`fd7cc0e5` Python tests czerwony, run 4868, 47 failed): `tests/test_archive_diff.py` + `tests/test_incremental_archive.py` przepisane na kanon R1 — **117 passed** (oba pliki + `tests/application/project_archive`), ruff/black 0; lekcja zakresu: pełny `tests/` = parytet z CI, 9 katalogów nim nie było.
- 2026-09-05: CV-4.1 na gałęzi (`4b65cb14`/`4b9311ea`/`148f8de5`, worktree `fable-cv4`): pełny pytest `tests/` z `-m "not pandapower"` **10934 passed** (907 s) + 47 czerwonych = wyłącznie klasa CI-ARCHIWUM na drzewie sprzed `15e8cb0d` (po rebase: `test_archive_diff` + `test_incremental_archive` + `project_archive` + `test_openapi_snapshot` **119 passed**, snapshot bez dryfu); `tests/golden/parytet_assemblera` 252/252 bit w bit PRZED == PO wycięciu; `tests/enm` + 14 przepiętych plików 1879 passed (krok 1); `solver_input_assembler_guard` rc=0 (7 plików / 31) + 15 testów własnych; `solver_input_substitute_guard` rc=0 (pin 3609 z pomiaru) + 67; `guardy_z_ci.py` 86/86 guardów z CI (KOMPLET ZIELONY); testy własne guardów 548 passed; `tsconfig_gate_guard` 136/136; docs/local_truth/utf8/claude_md_struktura guardy rc=0; black/ruff scripts 0; mypy `assembler`+`canonical_analysis` 0.
- 2026-09-05: CV-3.3-B2 w worktree (baza `fd7cc0e5` CV-3.3-B, `karta_cv33b2.md`, środowisko puste — `npm ci` od zera w tej sesji, `poetry install` niepotrzebny bo backend nietknięty): `tsc --noEmit` 0 błędów (cały projekt, 2401 plików); eslint `ui2/wyniki/porownanie ui2/wyniki/wzorzec ui/power-flow-comparison ui/protection-comparison frontend/src/creator-harness-main.tsx` `--max-warnings 0` 0; `tsconfig_gate_guard` 0 w bramce / **136 poza bramką** (budżet 136, BEZ WZROSTU) / 1 wyciszenie (budżet 1); `dead_click_guard`/`ui_terminology_guard`/`forbidden_ui_terms_guard`/`no_codenames_guard`/`dialog_completeness_guard` RC=0. vitest `--no-file-parallelism` `ui2/wyniki/porownanie ui/power-flow-comparison ui/protection-comparison` **166 passed (10 plików)**, w tym 40 nowych (`zabezpieczeniaPorownanieModel.test.ts` 17 + `trybZabezpieczen.test.tsx` 23: model zabezpieczeń klucz PARY element+punkt, komórki nullowalne FAB-E, filtr „tylko zmiany", ranking z punktem zwarcia, etykieta biegu bez zbieżności, proweniencja reużyta, ślad na żądanie z resetem między porównaniami). E2E realny backend, porty własne 8053/5223: `flow-ekspert-screenshot` + `porownanie-zwarc-delty` (test KD-3) + `s913-porownanie-z-kanwy` **18 passed** (3,3 min); `wszystkie-sceny-screenshot -g porownanie` **2 passed** (55,6 s); nowy test w `porownanie-zwarc-delty.spec.ts` (dwa biegi zabezpieczeń na dwóch biegach SC_3F, model zmieniony między nimi jak w KD-3) **1 passed** (50,5 s), dowód fizyczny: odcinek dłuższy → prąd zwarciowy w ocenie zabezpieczeń MNIEJSZY (prawo Ohma), ref elementu chronionego z odpowiedzi backendu widoczny w tabeli ekranu; zrzuty `cv33b2-porownanie-zabezpieczen-{dark,light}.png` (nowe, oba motywy). Zrzuty PNG odnowione przez pełny bieg speców zrzutowych (16 plików niezwiązanych z kartą + 3 pliki sceny „porownanie", gdzie trzecia zakładka nie zmienia wyrenderowanej treści testowanych trybów) przywrócone `git checkout --`. Backend NIETKNIĘTY poza jednym typem frontendowym (`ui/protection-comparison/types.ts::ProtectionComparisonRow` — patrz znalezisko uboczne w §E) — zero rozbudowy API, zero pytest/snapshot OpenAPI wymaganych przez definicję ukończenia karty. Szczegóły klasy/znalezisk ubocznych → §E CV-3.3-B2.
- 2026-09-05: CV-3.3-B2 odbiór koordynatora (`3d480e8b` na `3d47c275`): tsc 0; eslint 19 plików 0; vitest 15 plików / 239; 9 guardów FE rc=0 (tsconfig 136/136); e2e własne porty **19 + 2 passed**; PNG regenerowane przywrócone.
- 2026-09-05: CI-PARYTET (`3d47c275` Python tests czerwony, run 4871, 1 failed): harness `parytet_assemblera` na widoku parytetu (9 cyfr znaczących ∧ 9 miejsc dziesiętnych); 252 złote hashe przeliczone; PRZED (`4b65cb14`) == PO (szczyt) 252/252; test klasy widoku 1 passed; `test_parytet_assemblera` komplet zielony lokalnie; ruff/black 0.
- 2026-09-05: CV-3.3-C odbiór koordynatora (`6d30e663` + domknięcie `f15ed18e` na `5793a3e0`): pełny pytest `tests/` **10930 passed / 0 failed** (1079 s); `guardy_z_ci.py` 86/86 guardów z CI (KOMPLET ZIELONY) + 548 testów własnych guardów; parytet assemblera po rebase 4 passed (252/252 pod widokiem z `5793a3e0`); `test_batch_execution` 32; snapshot OpenAPI 317/229 bez dryfu; tsc/eslint 0; vitest 26 plików / 298; 10 guardów FE rc=0; e2e `critical-batch-flow` 1 passed (nazwa serii przeżywa odświeżenie).
- 2026-09-05: CI-PARYTET-2 (`5793a3e0` Python tests nadal czerwony, run 4873): harness `parytet_assemblera` = szkielet dokładnie + liczby kontraktu z tolerancją; złoty plik 835 KiB (95 553 liczb); PRZED (`4b65cb14`) == PO (252 dokładnie, 252/252 w tolerancji); testy własne widoku/porównania 2 passed; komplet pliku lokalnie zielony.
- 2026-09-05: CI-PARYTET-3 (`158d9831` Python tests czerwony, run 4875 — szkielet SC: napisy śladu + `proof_ref`; PF: ATOL): szkielet bez napisów śladu i skrótów wyniku, ATOL 10⁻⁶; złoty plik 835 KiB / 95 553 liczb; PRZED (`4b65cb14`) == PO (252 dokładnie, 252/252 w tolerancji); testy własne 2 passed; gate lokalny zielony.
- 2026-09-05: CI-PARYTET-4 (`40e49c22` Python tests czerwony, run 4876 — 155 wpisów SC, szkielet; klasa: ślad budowany progiem zerowym w solverze FROZEN): poddrzewa śladu poza porównaniem między maszynami, mapa skrótów poddrzew (głębokość 2) + szkielet rozbieżnego poddrzewa w meldunku; złoty plik 1213 KiB / 95 553 liczb / 8 836 skrótów; PRZED (`4b65cb14`) == PO (252 dokładnie, 252/252 w tolerancji); testy własne 3 passed; gate lokalny zielony.
- 2026-09-05: CV-4.1b (A3-04, worktree `agent-a8921fe6f13ce9d8a`, baza `40e49c22`): backend pytest `-m "not pandapower" tests/` **11009 passed, 11 deselected, 0 failed (931,7 s)** — 1 czerwony po drodze (`test_fixtura_sld_network_53_jest_bajtowo_rowna_generatorowi`, znalezisko: pola `meta` nowe → `source_hash` migawki inny; naprawiony regeneracją sankcjonowaną, diff 1 linia); `pytest tests/golden/parytet_assemblera` 5 passed (274,4 s), 0 rozbieżności ponad tolerancję w 252 wspólnych złotych wpisach, +12 wyłącznie G06; mypy_ratchet_guard Success (próg 0, bez wzrostu); ruff/black 0 (8 plików src); tsc 0; eslint 0 (10 plików zmienionych, w tym `sldNetwork53.ts` regenerowany); vitest targetowany **134 plików / 1599 testów** (`ui2/kreatory/zrodlo-oze`, `ui/sld/v2/canvas`, `ui/network-build`, `ui/sld/shared`, `ui/tech-card`); 30+ guardów architektury/katalogu/fizyki/determinizmu/repo rc=0; snapshot OpenAPI bez zmian (317/228).
- 2026-09-05: CV-4.2 w worktree (`karta_cv42.md`): 2 defekty NAPOTKANE i naprawione u źródła w tej samej kolejce (Zero-Debt) — (1) `test_klucz_przypadku_szablonow.py::test_inwentarz_sciezek_siegajacych_po_magazyn_jest_domkniety` czerwony po dodaniu `KluczTwin` do `api/solver_input.py` (inwentarz zamknięty modułów wołających `enm.store` — dopisano `"solver_input.py": KLUCZ_SUROWY`, wzorzec identyczny z `diagnostics.py`); (2) `scripts/ui_no_physics_guard.py` czerwony (allowlista z przypiętym numerem wiersza `WorkspaceSurfaceRouter.tsx:2633` zdryfowała do 2632 po dodaniu `activeCaseId` wcześniej w pliku — TEN SAM napis, numer poprawiony, `test_ui_no_physics_guard.py` 7/7 po naprawie). Weryfikacja: `test_klucz_przypadku_szablonow.py` 28/28, `test_solver_input_audit2_integration.py` 12/12, `tests/golden/parytet_p11/` (nowy, 5/5, 126 wpisów payloadu P11 z 17 odmów krzyżowo zgodnych z `parytet_assemblera` dla tych samych sieci), `parytet_assemblera` 5/5 po polu addytywnym `audit2_applied` (252/252 bit w bit), `test_professional_invariants.py` 10/10; guardy lokalne: `solver_input_assembler_guard` (zapadka 3 pliki/16 konstrukcji, w dół z 7/31), `solver_input_substitute_guard` (dług fizyczny bez wzrostu — 63 plików/290, martwy wpis P5 usunięty), `legacy_public_path_guard` (5 nowych testów resurrekcji), `ui_no_physics_guard`, `api_lifecycle_guard`, `route_prefix_guard`, `docs_guard`, `grep_zero_guard`, `pcc_zero_guard`, `domain_no_guessing_guard`, `canonical_ops_guard`, `readiness_codes_guard`, `catalog_binding_guard`, `catalog_gate_guard`, `catalog_enforcement_guard`, `catalog_metadata_guard`, `audit_contract_guard`, `repo_hygiene_guard`, `arch_guard`, `solver_boundary_guard`, `load_flow_no_heuristics_guard`, `protection_no_heuristics_guard`, `overlay_no_physics_guard`, `trace_determinism_guard`, `fault_scenarios_determinism_guard`, `ui_terminology_guard`, `forbidden_ui_terms_guard`, `no_codenames_guard`, `dialog_completeness_guard`, `local_truth_guard`, `physics_label_guard`, `resultset_v1_schema_guard`, `severity_contract_guard`, `reference_networks_guard`, `import_graph_guard`, `vulture_guard`, `docs_archive_guard`, `v12xx_canon_guard`, `utf8_mojibake_guard`, `no_direct_fault_params_guard`, `dead_click_guard`, `ui_no_physics_guard`, `interaction_matrix_guard`, `guard_ux_flow_v1`, `nn_source_menu_guard`, `fix_action_completeness_guard`, `trace_ui_leak_guard`, `solver_diff_guard` — WSZYSTKIE PASS (0 naruszeń, 0 nowego długu); mypy `src` 774 plików 0 błędów; black/ruff czyste (14 plików backendu dotkniętych kartą); frontend: `npm run type-check` (cały projekt) 0 błędów, `npm run lint` (cały projekt) 0 błędów/ostrzeżeń, vitest `network-build/station-der`+`workspace` 49 plików/508 testów PASS. OpenAPI snapshot przeliczony (316 ścieżek, 227 schematów) po usunięciu trasy P12. **Pełna regresja `pytest tests/ -m "not pandapower"` uruchomiona DWUKROTNIE**: pierwszy bieg z błędnego cwd (agent nie mógł trwale zmienić katalogu roboczego między wywołaniami narzędzia) dał 2 pozycje czerwone — `test_klucz_przypadku_szablonow.py` (defekt REALNY, naprawiony jw.) oraz `tests/cgmes/test_cgmes_roundtrip.py::test_export_identical_across_processes` (FAŁSZYWY alarm: test odpala subproces z `cwd="."` + `PYTHONPATH="src:tests"` WZGLĘDNYMI — zależny od katalogu roboczego WYWOŁUJĄCEGO pytest, którym w owym biegu NIE był `backend/`; potwierdzone: uruchomiony wprost z `cd backend && pytest tests/cgmes/test_cgmes_roundtrip.py` przechodzi 20/20 — plik testu nietknięty, poza mandatem karty, dokumentowane jako obserwacja, nie defekt). Drugi bieg (poprawny cwd = `backend/`) DOKOŃCZONY po spowolnieniu przez obciążenie maszyny dzielonej z ≥2 innymi agentami (`FAB-L katalogi frontu z fizyka`, `CV-4.1b A3-04 PV z modelu`; `loadavg` zmierzony ~12–13 na 4 rdzeniach, 3× nadsubskrypcja — bieg przyspieszał w miarę zwalniania maszyny, 30%→100% w oknie obserwacji): **`10875 passed, 10 deselected in 1874.53s (0:31:14)` — KOMPLET ZIELONY, zero niepowodzeń w całej regresji CI-parity.**

## G. Ustalenia adwersaryjne (§38) — po każdej granicy
| Data | Granica | Próba obalenia | Wynik |
|---|---|---|---|
| 2026-09-05 | CV-2: „każda rewizja ma migawkę, `checkout(n)` odtwarza hash z dziennika, awaria nie zostawia śladu” | iniekcja awarii po KAŻDYM z 5 kroków zapisu (dziennik roboczy, migawka robocza, podmiana HEAD, podmiana migawki, podmiana dziennika) × 2 sieci z prawdziwymi komendami domenowymi; sierota powyżej HEAD; migawka bieżącej o innym hashu; magazyn sprzed rejestru (bez `.rev/`, dziennik obcięty); import archiwum; migracja klucza | OBALONA w każdym wariancie: stan = ostatnia rewizja spójna (pamięć, HEAD co do bajtu, brak migawki n, brak wpisu n), po ponownym wczytaniu `checkout(n).hash == dziennik[n].hash` dla każdego n; sierota usunięta i nigdy promowana; migawka obca zastąpiona treścią HEAD; wpis odtworzony nazywa brak wprost. **Jedna hipoteza POTWIERDZONA po drodze**: test wycofania iniektował awarię w `Path.write_bytes` globalnie, więc po CV-2 trafiał w PRZYGOTOWANIE migawki zamiast w wycofanie (test mierzył co innego niż deklarował) — iniekcja zawężona do plików poza `.rev/` (`tests/enm/test_store_concurrency.py`) |
| 2026-09-05 | CV-2: „świeżość wyprowadzana z koperty nie może uznać wyniku za aktualny bez dowodu” | koperta niespójna z własnym odciskiem (edycja zapisu poza systemem); bieg bez koperty (sprzed CV-2); ta sama rewizja, inny hash; katalog inny przy tym samym modelu; brak modelu bieżącego | OBALONA: każdy wariant daje OUTDATED z nazwaną przyczyną (`koperta-rewizji-niespojna`, kotwice hashowe, `model-zmieniony`, `katalog-zmieniony`, `brak-modelu-biezacego`); FRESH wyłącznie przy zgodnej rewizji, hashu i odcisku katalogu; każdy kod przyczyny ma zdanie PL (test przypięty) |
| 2026-09-05 | CV-1 „PROJECT owns ENM" — ścieżka adresowana PROJEKTEM (nie przypadkiem) | eksport archiwum projektu w świeżym procesie, model nadal pod kluczem przypadku (`tests/invariants/test_wlasnosc_modelu_projektu_adwersaryjne.py::test_i5_eksport_archiwum_nie_gubi_modelu_zastanego_per_przypadek`) | **TWIERDZENIE OBALONE — defekt realny, naprawiony u źródła.** `project_archive/service.py::_collect_enm` budowało klucz przez `enm.klucz_twin.klucz_twin_projektu` (czysta funkcja, ZERO migracji), więc `has_enm` = `False` i archiwum ZIP wychodziło BEZ sekcji ENM (import takiego archiwum = projekt bez sieci). Naprawa: `application/twin_key.migruj_projekt_z_legacy_z_repozytorium(project_id, CaseRepository(session))` przed odczytem; test zielony |
| 2026-09-05 | CV-1 — ta sama klasa na drugiej ścieżce projektowej | dispatch analiz nN (`FAULT_LOOP_NN`/`SWZ_NN`) w świeżym procesie, model zastany per przypadek (`…::test_i7_dispatch_nn_nie_fabrykuje_pustego_modelu_projektu`) | **TWIERDZENIE OBALONE — defekt realny, naprawiony u źródła.** `analysis_dispatch/service.py` budowało klucz tą samą czystą funkcją; `get_enm` FABRYKOWAŁO pusty model domyślny i go ZAPISYWAŁO, więc realny model projektanta przy pierwszym wejściu przez `/api/cases/...` lądował w `legacy_przypadki/` jako ROZBIEZNY (podmiana sieci na pustą). Naprawa: `application/twin_key.klucz_twin_dla_projektu` (migracja + klucz) w obu dispatcherach; klasa domknięta bramką (`enm_store_key_guard` REGUŁA 2: `klucz_twin_projektu` niedostępne w `api/**` i `application/**`) |
| 2026-09-05 | CV-1 — migracja „nic nie gubi" (dziennik zmian) | promocja modelu przypadku aktywnego na model projektu z zachowaniem licznika rewizji (`…::test_i6_dziennik_promowanego_przypadku_idzie_za_modelem`) | **TWIERDZENIE OBALONE — defekt realny, naprawiony u źródła.** `store._odloz_do_legacy` odkładał dziennik KAŻDEGO przypadku (także promowanego) do `legacy_przypadki/`; projekt startował z historią pustą przy modelu w rewizji N, a `GET /enm/dziennik-zmian?od_rewizji=R` oddawał listę Z DZIURĄ wyglądającą na kompletną. Naprawa: `dziennik_zmian.przenies_dziennik` (dziennik idzie ZA modelem, odmawia nadpisania cudzej historii) + manifest niesie los dziennika (`ZA_MODELEM`/`ODLOZONY`/`BRAK`) |
| 2026-09-05 | CV-1 — bramka własna (`enm_store_key_guard`, krok CI `p0-extended-guards.yml`) | pomiar zapadki na HEAD po karcie wiring CV-1-W | **BRAMKA CZERWONA NA HEAD — naprawione.** CV-1-W przepisała konsumentów, ale nie obniżyła zapadki: 18 plików meldowało „Dług ZMALAŁ — obniż budżet", czyli krok CI przewracał się na własnej naprawie. Stan zmierzony: 1 plik / 1 wywołanie (`project_archive/service.py` — zapis TYMCZASOWY pod kluczem przypadku w imporcie archiwów legacy, droga poprawna, nie dług); zapadka obniżona 18/74 → 1/1. Zakres skanu rozszerzony o `enm/**` (440 plików zamiast 408) — warstwa, w której magazyn i klucz MIESZKAJĄ, weszła CZYSTA (zero naruszeń obu reguł, zapadka bez zmian); testy własne bramki 28 → 34 |
| 2026-09-05 | CV-1 — współbieżność i rozdzielność (hipotezy 1, 2, 3, 6, 7) | 11 prób: równoległe pierwsze tłumaczenie dwóch przypadków jednego projektu; dwa RÓŻNE przypadki tego samego projektu zapisujące równolegle; świeżość wyniku przypadku A po edycji przez przypadek B; wspólny `ENM_STORE_DIR` dla wielu projektów; plik zastany przypadku SKASOWANEGO z bazy; projekt bez przypadków; przypadek spoza bazy × brak warstwy DB × nie-UUID | **TWIERDZENIE SIĘ BRONI** — wszystkie próby nieudane, testy zostają jako inwarianty I-8…I-13 w `tests/invariants/test_wlasnosc_modelu_projektu_adwersaryjne.py` (19 testów w `tests/invariants`, RC=0). Nierozstrzygnięte i nazwane: blokada `blokada_twin` jest W PROCESIE (wiele workerów uvicorna = osobna decyzja wdrożeniowa, zapisana w docstringu `enm/store.blokada_twin`) |
| 2026-09-05 | CV-1 — dług wykryty przy okazji (Zero-Debt, nie własny) | pełna regresja `tests/api` na HEAD | **3 czerwone testy pre-existing, naprawione u źródła:** `tests/enm/test_f10_6_domain_fields.py` (jeden test klasy został przy dawnym podpisie `_seed`/`get_enm_field_view` → `TypeError`), `tests/api/test_v126_opf_loss_lcc_api.py` ×3 (literał `case_id` bez wiersza w bazie → 404 zamiast 422/200), `tests/api/test_openapi_snapshot.py` (snapshot nieodświeżony po CV-1-W: `case_id` traci `format: uuid` na 2 trasach, bo sub-zależność `KluczTwin` deklaruje `case_id: str` — runtime zweryfikowany: nie-UUID daje 404 z tłumacza, więc dokument mówi prawdę; snapshot wygenerowany ponownie) |
| 2026-09-05 | CV-3.1 rdzeń | Próba: (1) „koperta biegu na scenariuszu z nadpisaniami ma `snapshot_hash` = hash migawki efektywnej" → świeżość porównywała go z HEAD i meldowała `MODEL_ZMIENIONY` dla KAŻDEGO biegu scenariusza (3 czerwone testy I-S3); (2) wariant na biegu, który sam był scenariuszem z nadpisaniami, dostawał kopertę udającą bazę normalną; (3) guard R4 (`dict(migawka)`) łapał obronną kopię do odczytu w `werdykt_projektowy.py` i własny `deepcopy` w `hash_migawki_enm` | (1) obalone i naprawione u źródła: koperta identyfikuje BAZĘ (`snapshot_hash` = hash HEAD w `model_revision`) + scenariusz (`scenario_ref`, `scenario_hash`); hash migawki efektywnej niesie `CanonicalRun.snapshot_hash` — pin I-S2 („koperta ≠ bieg" dla nadpisań, „koperta == bieg" dla stanu normalnego); (2) `bieg_wariantu` ODMAWIA z nazwą (składanie scenariuszy nie jest modelowane — jedna referencja w kopercie; pin I-S5); (3) R4 doprecyzowane do pary „kopia → zapis po indeksie w tym samym zakresie" (test iloczynu cech: kopia bez zapisu, zapis w innej funkcji, `dict(dane)` bez tokenu migawki = nie-trafienia), hash ze słownika bez `deepcopy` (kopia dokładnie tak głęboka, jak sięgają mutacje) — zapadka 22 → 19 realnych trafień |
| 2026-09-05 | CV-4.1 — „wynik kanoniczny PF/SC jest kontraktem liczb (§35: bez `NaN`/`inf`, bez wartości niefizycznych)” | zbieranie złotych hashy assemblera przez `kwantyzuj_kontrakt` (tryb ścisły) na CAŁYM rejestrze sieci + scenariuszach nN × {PF, SC 3F max/min, 1F, 2F, 2FG} = 252 biegi torem kanonicznym | **TWIERDZENIE OBALONE — defekt realny, naprawiony u źródła (krok 0, `6d08d914`).** Wyspa nN bez zasilania: rozpływ oddawał `NaN` w `node_voltage_kv` węzłów nierozwiązanych; zwarcie κ ≈ 3·10¹², i_p ≈ 2·10¹⁴ A, I_th = ∞ (solver FROZEN liczy κ z R/X impedancji zastępczej o ujemnym R albo X; IEC 60909-0 §4.3.1.1: κ ∈ [1,02; 2,0]). Naprawa w WYKONAWCACH (solver nietknięty, B-01): `None` + `non_finite_fields` (predykaty parami: `NaN` ⇔ `not_solved_nodes`), wiersz zwarcia nieraportowalny (`not_reportable`/`proof_status="partial"`/`solver_result_non_physical` + `non_physical_fields`), bieg z `non_reportable_fault_node_ids`, ślad White Box tą samą funkcją; sieci zdrowe bit w bit bez zmian (3 testy `tests/enm/test_wyniki_finitowe.py`). Odmowy solvera przypięte jako hashe (42: 24× dwa SLACK w scenariuszu nN, 10× osobliwa Y0, 8× osobliwa Y) — odmowa z powodem jest kontraktem, nie awarią. Propozycja zmiany rdzenia (B-01) → OD-6 |
| 2026-09-05 | CV-4.1 — „hash parytetu surowego wyniku solvera (9 cyfr znaczących) jest przenośny między maszynami” | CI GitHub (run 4871 na `3d47c275`) vs bieg lokalny: 252 biegi PF/SC rejestru | **TWIERDZENIE OBALONE — naprawione u źródła (CI-PARYTET).** 252/252 hashy inne na CI przy identycznych odmowach; sonda surowego wyniku: zera fizyczne (−6,9·10⁻¹⁶ MW, 2,7·10⁻¹⁷ kA, 9·10⁻¹⁴ A) i reszty NR poniżej tolerancji (4,7·10⁻⁸ MW) — 9 cyfr znaczących takich liczb to szum BLAS/CPU. Widok parytetu z drugim, bezwzględnym progiem (9 miejsc dziesiętnych) + test klasy (niezmienniczość hasha pod zaburzeniem 3·10⁻¹⁶); parytet PRZED/PO udowodniony ponownie pod nowym hashem. Lekcja do kontraktu liczb (ADR-018): kwantyzacja znacząca nie obejmuje zer numerycznych — każdy hash SUROWEGO wyniku solvera wymaga progu bezwzględnego; widoki analiz (`parytet_scenariuszy`) nie niosą takich liczb (28 hashy zielone na CI, pomiar) **Ciąg dalszy (run 4873 na `5793a3e0`): próg bezwzględny 9 miejsc TEŻ nie przenosi — szum wzmocniony złym uwarunkowaniem sięga 10⁻⁸…10⁻⁹ względnie w wartościach ZNACZĄCYCH (Ik'', U), więc żadna siatka nie jest bezpieczna przy tysiącach wartości. Rozstrzygnięcie: hash wyłącznie dla części dyskretnej (szkielet), liczby z tolerancją względem zapisanych wartości (CI-PARYTET-2). Wniosek do ADR-018: „hash kanoniczny” jest kontraktem determinizmu NA JEDNEJ maszynie; między maszynami obowiązuje porównanie w tolerancji. **Ciąg dalszy 2 (run 4875 na `158d9831`): „szkielet” też nie był przenośny — liczby schowane w NAPISACH śladu White Box (LaTeX podstawień, 6 cyfr) i skrót SHA-256 wyniku w `proof_ref`; klasa: każda reprezentacja liczby (float, napis, skrót) niesie ten sam szum. Rozstrzygnięcie CI-PARYTET-3: napisy w śladzie i skróty wyniku maskowane w szkielecie, ATOL 10⁻⁶ jednostki dla przepływów gałęzi nieobciążonych. **Ciąg dalszy 3 (run 4876 na `40e49c22`): STRUKTURA śladu też nie jest przenośna — solver buduje listy śladu progiem zerowym na liczbie (`if i_a <= 0: continue`), więc członkostwo gałęzi o prądzie 0 vs 10⁻¹⁷ jest funkcją szumu; sonda z szumem 10⁻¹² na `np.linalg.inv` odtwarza klasę. Rozstrzygnięcie CI-PARYTET-4: między maszynami porównywane są WYŁĄCZNIE części wyniku będące funkcją wejścia (klucze, statusy, identyfikatory, długości list KONTRAKTU) i liczby kontraktu w tolerancji; ślad White Box tylko lokalnie. Diagnostyka: mapa skrótów poddrzew w złotym pliku. |

## H. Pozostałe P0/P1 (tylko realne)
| ID | Ryzyko | Klasa | Działanie |
|---|---|---|---|
| P0-1 | `main` bez ochrony; merge #472 z czerwonym CI (P0/vitest/pytest maskowane przez środowisko) | governance | owner action §B |
| P0-2 | ENM per `case_id` — kopia sieci per przypadek | architektura | CV-1 |
| P0-3 | fabrykacje użytkowe: fikcyjny katalog, fantom nastaw, `c_factor=1.0` domyślne w torze legacy | zaufanie | FAB-A/B; kasacja toru legacy (CV-4) |
| P0-4 | 12 czerwonych e2e na `main` od #472 | regresja produktu | **ZAMKNIĘTE**: 11/12 przez CI-D (`b349377b`), 12. (`industrial-template-mass-flow`) przez naprawę klasy `KLUCZE_ROZPLYWU` (`1e9f21c5`) — E2E full 408 speców zielone na `e2a0dc17`; wydajność zwarć pozostaje otwarta jako P0-5 (pomiar PERF-0), niezależnie od zielonej bramki |
| P1-1 | 4 rejestry biegów, 5 sposobów uruchomienia | spójność | CV-3.3 — **częściowy postęp (CV-3.3-A, 2026-09-05)**: E3 (`ExecutionEngineService`) skasowany, E2-widmo (`unified_runs`) skasowany (3 sposoby uruchomienia zostają, z 4 pierwotnych pod E2), R2 podmoduły martwe skasowane (tabela żywa R2 + R3 + R4 + E4 — dokładny stan w `CANONICAL_TWIN_ARCHITECTURE.md` §B.2 — poza tą kartą, CV-3.3-B) |
| P1-2 | uziemienie: 6 reprezentacji, fizyka nie czyta żadnej | fizyka doziemna | CV-5 |
| P1-3 | 12 benchmarków IEEE/CIGRE/IEC + `oze_pv_bess` istnieją tylko w dialekcie słownikowym P9 (nie walidują się jako ENM) — jedyna niezależna wyrocznia LF/SC liczy się poza torem kanonicznym | spójność / dowodowość | CV-4 (benchmarki jako ENM przez assembler; zapadka `BENCHMARK_DICT_ZASTANE`) |
| P1-4 | substrat 52 stacji (G00): 21 BLOCKER walidatora, budowa ≈ 40 s — sieć skali nieobliczalna | wydajność / rejestr | CV-4/CV-6 (naprawa u źródła; zapadka `BLOKERY_ZASTANE`) → SUB-52s (`42649500`, worktree): 21 → 0 BLOCKER (E063 ×20 przez kanał `nn_earthing.lv_system`, E003 przez pierścień `connect_secondary_ring_sn`); budowa ≈ 10–15 s (profil: walidator O(N³) = P1-7); scalenie po SLD-LOC |
| P1-5 | trzy prawdy o przekaźnikach (A6-17): `ProtectionDeviceType` (12, kanoniczny, jedyny przyjmowany przez `add_relay`), `devices_v0.json` (51, analityczny — z DUPLIKACJĄ schematów ID `EM_ETANGO_*`/`EM_E2TANGO_*` dla jednej realnej rodziny e2TANGO, warianty 600/800/1000 podwójnie), `protection_vendors.py` | dane / drugie prawdy | konwergencja do JEDNEGO katalogu zabezpieczeń z `ProtectionCapabilityRegistry` (PZ-07, ADR-022) — karta architektoniczna po CV-1; do tego czasu picker zawężony do katalogu kanonicznego (FAB-F F6); scalenie schematów e2TANGO = decyzja danych (jeden schemat ID, warianty z karty producenta) w tej samej karcie |
| P1-6 | GPZ: regeneracja fikstury substratu 52s pokazuje `hv_neutral: directly_grounded → null` i `i0_percent: 0.2 → 0.35` dla `tr-wn-sn-110-15-25mva-yd11`; katalog niesie 0.35 (fikstura była nieaktualna — katalog jest prawdą), ale `hv_neutral` GPZ dziś NIE jest ustawiane przez `add_grid_source_sn` (jedyny pisarz `_apply_station_neutral_grounding`, `domain_operations.py:4605`, woła tylko wstawianie stacji) i walidator tego nie zgłasza (brak kodu dla braku uziemienia punktu neutralnego WN/SN) | kompletność danych / uziemienie punktu neutralnego | CV-5 F-3 `NeutralGrounding` typowane dla KAŻDEGO transformatora (GPZ włącznie) + kod walidatora + ustawienie w `add_grid_source_sn` z karty typu; do tego czasu analizy doziemne WN nie mają danych GPZ (uczciwy brak, nie 0) |
| P1-7 | walidator: `pole_transformatorowe._szyny_stacji` O(stacje×szyny) wołane 5 287× i `_tekst` 4 451 480× przy budowie substratu 52s (każda ze 151 operacji domenowych uruchamia pełny `ENMValidator().validate()` przez `_build_readiness`) — łącznie O(N³) na rosnącej sieci; profil cProfile z karty SUB-52s | wydajność / walidator | indeks szyn per stacja budowany raz na walidację (słownik), nie na każdy transformator; pomiar B1/B2 po naprawie; osobna karta po CV-1-W (walidator jest współdzielony przez wszystkie operacje) |
| P2-1 | `network_model/core/generator.py::GeneratorSN/GeneratorNN.k_sc = 1.1` — trzecie miejsce zaszytego 1,1 (FAB-H): zero konsumentów produkcyjnych (`oze_validators.py` importowany wyłącznie przez `tests/test_oze_generators.py`) — martwa wyspa toru legacy P2–P12 | martwy kod / legacy | kasacja razem z torem legacy (CV-4, procedura 7 kroków); nie „naprawiać” martwego kodu |
| P1-8 | `frontend/src/ui/sld/v3/scene/__tests__/kosztSceny.test.ts`: próg czasowy (< 3 s) zależny od obciążenia maszyny — czerwony przy równoległych agentach (3,05 s), zielony w izolacji; test jednostkowy z bramką wall-clock = niedeterministyczna bramka | jakość testów / determinizm | próg czasowy → pomiar operacji (licznik) albo benchmark poza suitą jednostkową (`tests/perf` wzorzec PERF-0 z medianą i p95); naprawa po scaleniu SLD-LOC (ten sam plik) |
| P1-9 | `AddDerWizard` (`network-build/station-der`): statyczne listy `PV_INVERTER_CATALOG`/`BESS_PCS_CATALOG`/`WIND_TURBINE_CATALOG` (`catalogs.ts`, po ~97 pozycji) podstawiane jako „fallback lokalny", gdy backend zwróci pustą listę albo błąd — użytkownik wybiera urządzenie, którego backend nie zna; ta sama rodzina co `DER_CATALOG_OPTIONS` (FAB-F) i `_DEFAULT_CATALOG_BY_VARIANT` (E2E-DER) | fabrykacja / drugie źródło prawdy o katalogu | **FAB-I odebrana** (`a16b8e26`, cherry-pick `11510a9e`): katalog urządzeń DER w kreatorze wyłącznie z backendu, stan pusty/błąd blokuje krok z powodem, walidacja zapisu bez furtki lokalnej; przy inwentarzu naprawiona ta sama klasa w `DerSurfaces.tsx` (dla nierozpoznanej referencji podstawiano CUDZE urządzenie po najbliższej mocy — ścieżka normalna dla każdego DER z backendu); test maskujący (`Etap5Der`) przepisany; vitest `network-build`+`workspace` 124 pliki / 1232, tsc 0, lint 0, 5 guardów UI RC=0. **Pozostałość klasy (pomiar agenta, → karta FAB-J):** `PF_CURVE_CATALOG` 5 i `BLOCK_TRANSFORMER_CATALOG` 9 (backend MA końcówki audit2, snapshot już pobierany, selecty czytają plik lokalny), `NC_RFG_PROFILE_CATALOG` 5 / `LVRT_CURVE_CATALOG` 7 / `HVRT_CURVE_CATALOG` 7 (backend ma dane w `/api/ncrfg-tests/catalog` w innym kształcie — kontrakt do zaprojektowania), `LV_VOLTAGE_LEVEL_CATALOG` 5 i `BESS_BATTERY_CATALOG` 2 (brak końcówki backendu — katalog do zbudowania po stronie backendu); pola wyliczane we froncie bez dostawcy: `applicable_module_types` (moduł NC RfG z progów mocy 200 kW / 10 MW / 50 MW — klasyfikacja NORMATYWNA w UI; próg C/D w PL to 75 MW) i `four_quadrant` („każdy BESS 4-kwadrantowy" — założenie, nie atrybut); `DerSurfaces` pokazuje surową referencję zamiast nazwy z backendu (uczciwe, ale bez etykiety) |
| P0-5 | **wydajność zwarć na sieci M**: `POST /api/execution/runs/{id}/execute` (IEC 60909, 50 stacji) kończy się poprawnie (HTTP 200), ale w 297 s / 1 581 s pod obciążeniem (pomiar karty CI-D) i ponad budżetem klienta także w CI (spec `industrial-template-mass-flow.spec.ts` czerwony na `main` od #472); budżet B4 dla M = 1 s, L = 10 s (plan wydajności §1a) — przekroczony o 2–3 rzędy | wydajność / tor kanoniczny | **pomiar PERF-0 (`47d809a9`): B4 na G00 (315 szyn, 54 transformatory) = 376 794 ms (6,3 min) przy budżecie 1 s — 377×; B3 rozpływ 5 100 ms przy 200 ms (25×); B1 topologia 110 ms przy 30 ms**; przyczyna: pętla po wszystkich węzłach z gęstą algebrą (A3-10: SC O(N·n³)); naprawa = faktoryzacja rzadka + kolumny selektywne (CV-4, §C.3) — zmiana rdzenia wymaga B-01; **zakaz** podnoszenia timeoutu testu bez naprawy |
| P1-10 | `raw_result` biegu zwarciowego na sieci 52 stacji (G00/00, SC 3F) to **108 MB JSON / 549 k liczb** — per węzeł zwarcia (107) ślad `branch_contributions` (2 906 liczb, 0,8 MB), `branch_flow_trace` (1 579), `white_box_trace` (628); liczby kontraktu to ~64 na węzeł. Ten payload trafia do `canonical_runs.raw_result` i do odpowiedzi HTTP biegu (pomiar 2026-09-05 przy CI-PARYTET-2) | wydajność / magazyn (ta sama klasa co P0-5: 297 s biegu SC sieci M) | CV-4.x/PERF: ślad White Box per węzeł jako osobny artefakt na żądanie (proof engine czyta go READ-ONLY), nie część `raw_result`; kontrakt wyniku FROZEN — nowe pole/artefakt addytywnie, kasacja z toru odczytu UI procedurą |

## I. Decyzje właściciela (tylko nierozstrzygalne z repo/norm/danych)
| ID | Pytanie | Dlaczego nie da się rozstrzygnąć samodzielnie |
|---|---|---|
| OD-1 | Ochrona `main` (§B) | wymaga uprawnień administratora |
| OD-2 | Czy istnieją projekty użytkowników w legacy ORM `network_*` (XLSX/wizard) wymagające eksportu przed kasacją (D-03 krok 3) | dane produkcyjne poza repozytorium |
| OD-3 | Publikowane wyrocznie dla G01 (sieć kompensowana): wskazanie źródła literaturowego/normowego akceptowanego przez właściciela jako `PUBLISHED_BENCHMARK` | wybór źródła normatywnego/literaturowego jest decyzją właściciela produktu |
| OD-4 | Retencja rewizji modelu (CV-2): rewizje są append-only, każda z pełną migawką (gzip ≈ 80 kB przy 54 stacjach); rewizje referowane przez bieg lub przypadek są nieusuwalne; pytanie właściciela: czy dopuścić operację pruningu rewizji NIEREFEROWANYCH (jawną, na żądanie, z manifestem) czy trzymać pełną historię bez wyjątku | polityka retencji danych projektu (koszt nośnika vs kompletność audytu) |
| OD-5 | Progi mocy modułów wytwarzania NC RfG (klasyfikacja A/B/C/D): zamrożony profil solvera (`backend/src/catalog/profiles/nc_rfg/{pse,pge,energa,enea,tauron}.yaml`, 5 profili identyczne) niesie A/B = 1 000 kW i B/C = 50 000 kW — to MAKSIMA dopuszczone przez NC RfG dla obszaru synchronicznego Europy kontynentalnej, a nie progi ustalone dla Polski decyzją Prezesa URE (2018): A < 200 kW, B 200 kW–10 MW, C 10–75 MW, D ≥ 75 MW lub przyłączenie ≥ 110 kV (C/D = 75 MW zgodne; reguła napięciowa D w YAML `voltage_kv_max: 110` działa jako `> 110 kV`, URE: `≥ 110 kV`). Karta FAB-J ujednoliciła źródło prawdy (compliance i API delegują do `NcRfgProfile.classify_module` profilu; UI wcześniej niósł progi URE we własnej tabeli) — więc do czasu decyzji klasyfikacja w kreatorze DER i weryfikacja `POST …/generators` podążają za YAML: instalacja 500 kW jest dziś klasyfikowana jako moduł A (wg URE: B), 20 MW jako B (wg URE: C). Proponowany patch (do zgody właściciela): `threshold_kw_max` A 1000 → 200, B 50000 → 10000 (i odpowiadające `threshold_kw_min`), reguła D „≥ 110 kV" — 5 plików YAML + test przypinający progi URE w `tests/compliance/test_nc_rfg_modul.py` (istniejący test parytetu compliance ↔ profil pozostaje) | zmiana wartości w zamrożonym profilu solvera = B-01 (zgoda właściciela); wybór między maksimami NC RfG a decyzją krajową jest rozstrzygnięciem regulacyjnym właściciela produktu, nie kodu |
| OD-6 | Odmowa rdzenia IEC 60909 (B-01) zamiast wyniku niefizycznego: `short_circuit_iec60909.py` liczy κ = 1,02 + 0,98·e^(−3R/X) z impedancji zastępczej także wtedy, gdy R < 0 albo X < 0 (węzeł bez toru zasilania, wyspa nN) i oddaje κ ≫ 2, i_p/I_th nieskończone; od CV-4.1 (krok 0) WYKONAWCA kanoniczny oznacza taki wiersz jako nieraportowalny (`solver_result_non_physical`), ale sam rdzeń nadal produkuje liczby poza normą | rdzeń solvera zamrożony (B-01): jawna odmowa z powodem po polsku (jak przy osobliwej Y) dla R/X < 0 albo κ poza [1,02; 2,0] wymaga zgody właściciela, niezależnej wyroczni i testów trybów awarii |

## J. Następny vertical slice
**CV-1 — Project owns ENM** (`docs/architecture/CANONICAL_TWIN_ARCHITECTURE.md` §2): magazyn per projekt, fasada `case_id → project_id`, migracja per-case ENM → warianty, guard przeciw zapisowi kluczem przypadku, inwarianty I-1…I-4 w `tests/invariants/`.
