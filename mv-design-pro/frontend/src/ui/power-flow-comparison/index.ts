/**
 * P20c — Power Flow Comparison Module
 *
 * `PowerFlowComparisonPage` (UI) SKASOWANA (karta CV-3.3-B2, D3): martwa
 * strona bez montujących — realna ścieżka użytkownika to ekran ui2
 * (`ui2/wyniki/porownanie`, tryb „Rozpływ"), reużywający klienta i typy z
 * tego modułu. Ten plik zostaje jako KLIENT ui2:
 * - Types for power flow comparison
 * - API client functions
 */

export * from './types';
export * from './api';
